import{_ as s,o as i,c as a,a as t}from"./chunks/framework.8tiMeCK-.js";const k=JSON.parse('{"title":"6.5  Using slabinfo and Its Companion Tools","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch06_5.md","filePath":"en/notes/linux_kernel_debugging/ch06_5.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_debugging/ch06_5.md"};function o(l,e,r,h,p,c){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="_6-5-using-slabinfo-and-its-companion-tools" tabindex="-1">6.5 Using slabinfo and Its Companion Tools <a class="header-anchor" href="#_6-5-using-slabinfo-and-its-companion-tools" aria-label="Permalink to &quot;6.5  Using slabinfo and Its Companion Tools&quot;">​</a></h1><h4 id="a-new-tool-in-the-box" tabindex="-1">A New Tool in the Box <a class="header-anchor" href="#a-new-tool-in-the-box" aria-label="Permalink to &quot;A New Tool in the Box&quot;">​</a></h4><p>Up to this point, we&#39;ve been operating strictly &quot;inside&quot; the kernel—reading kernel logs, deciphering panic messages, or staring at hex dumps. This is essential, but sometimes we need to step back and observe from the outside, like a detached spectator.</p><p>For that, we need a handy &quot;external scope.&quot;</p><p>You might ask, <code>/proc/slabinfo</code> haven&#39;t we mentioned that file several times already? Yes, but reading that file directly is like reading a long sentence without commas—all the information is there, but it&#39;s exhausting for humans. We need a tool that can structure this data and even analyze it for us.</p><p>That tool is <code>slabinfo</code>.</p><p>Counterintuitively, even though this is a userspace program, it lives right inside the kernel source tree: <code>tools/vm/slabinfo.c</code></p><p>Compiling it is straightforward—no need to wrestle with the complex Kbuild system. Just enter the <code>tools/vm</code> directory in the source tree and type <code>make</code>. This produces a binary, and for convenience, we can create a symlink to <code>/usr/bin/slabinfo</code>.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ls</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -l</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">which</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> slabinfo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">lrwxrwxrwx</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> root</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> root</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 71</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Nov</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 20</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 16:26</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /usr/bin/slabinfo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> -</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">...</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/linux-5.10.60/tools/vm/slabinfo</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Before we start, run <code>-h</code> (or <code>--help</code>). You&#39;ll see a massive list of parameters. Don&#39;t be intimidated—many of these are for specific scenarios. The figure below shows all parameters for kernel version 5.10.60 (Figure 6.6):</p><p><em>(Figure 6.6 – The help screen of the kernel slabinfo utility)</em></p><p>A few things to keep in mind:</p><ul><li><strong>Default behavior</strong>: <code>slabinfo</code> only displays slab caches <strong>with data</strong> by default (equivalent to passing the <code>-l</code> flag). To see empty caches, run <code>slabinfo -e</code>.</li><li><strong>Prerequisites</strong>: Not all parameters work out of the box. Most features require the kernel to have <code>CONFIG_SLUB_DEBUG=y</code> enabled. The good news is that most distribution kernels enable this by default. Some features also require passing specific flags via <code>slub_debug</code> in the kernel boot parameters.</li><li><strong>Permissions</strong>: You must be root.</li></ul><h4 id="a-quick-health-check" tabindex="-1">A Quick &quot;Health Check&quot; <a class="header-anchor" href="#a-quick-health-check" aria-label="Permalink to &quot;A Quick &quot;Health Check&quot;&quot;">​</a></h4><p>Let&#39;s run it once without any arguments and see what it spits out.</p><p>Here we&#39;ll just look at the header and one specific example (the <code>kmalloc-32</code> cache):</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> slabinfo</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">head</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -n1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Objects</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Objsize</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Space</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Slabs/Part/Cpu</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> O/S</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> O</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> %Fr</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> %Ef</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Flg</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> slabinfo</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;^kmalloc-32&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kmalloc-32</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 35072</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 32</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">   1.1M</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">   224/0/50</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">     128</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">   0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> </span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>This line of output is a bit cramped, so let&#39;s break it down piece by piece. Each column represents a &quot;health metric&quot; for this cache:</p><ul><li><strong>Name</strong>: The cache name (here, <code>kmalloc-32</code>).</li><li><strong>Objects</strong>: How many objects are currently allocated (35,072).</li><li><strong>Objsize</strong>: The size of a single object (32 bytes, which is expected).</li><li><strong>Space</strong>: The total kernel memory space occupied by these objects (approximately 1.1MB, basically <code>Objects * Objsize</code>).</li><li><strong>Slabs/Part/Cpu</strong>: A triplet representing the number of <strong>full slabs</strong>, <strong>partially empty slabs</strong>, and <strong>per-CPU slabs</strong>. This ratio tells you how fragmented the system is.</li><li><strong>O/S (Objects per Slab)</strong>: How many objects fit in a single slab (128).</li><li><strong>O (Order)</strong>: The order of memory requested from the page allocator. 0 means 1 page ($2^0$), 1 means 2 pages ($2^1$), and so on. An order of 0 here means single pages are used to hold these objects.</li><li><strong>%Fr</strong>: The percentage of free memory.</li><li><strong>%Ef</strong>: The effective memory usage percentage.</li><li><strong>Flg</strong>: Flags. This shows the special attributes enabled for this cache (we&#39;ll dive into this below).</li></ul><p>If you want to see exactly how this line is generated, you can check the source code directly: <a href="https://elixir.bootlin.com/linux/v5.10.60/source/tools/vm/slabinfo.c#L640" target="_blank" rel="noreferrer">https://elixir.bootlin.com/linux/v5.10.60/source/tools/vm/slabinfo.c#L640</a></p><p>Regarding the <strong>Flg</strong> flags on the far right, they represent various attributes of the slab cache with the following meanings:</p><ul><li><strong>*</strong>: Aliases exist</li><li><strong>d</strong>: DMA memory</li><li><strong>A</strong>: Hardware cache line aligned</li><li><strong>P</strong>: Poisoned (used to detect uninitialized access)</li><li><strong>a</strong>: Reclaim statistics activated</li><li><strong>Z</strong>: Red Zoned (used to detect out-of-bounds access)</li><li><strong>F</strong>: Sanity checks on</li><li><strong>U</strong>: User tracking (records who allocated it)</li><li><strong>T</strong>: Traced</li></ul><p>Additionally, if you add the <code>-D</code> (Display active) option, the output column format changes to show more detailed (but wider) information.</p><h4 id="who-are-the-memory-hogs" tabindex="-1">Who Are the Memory Hogs? <a class="header-anchor" href="#who-are-the-memory-hogs" aria-label="Permalink to &quot;Who Are the Memory Hogs?&quot;">​</a></h4><p>A common engineering question arises here: out of all these slab caches, which one is consuming the most kernel memory?</p><p><code>slabinfo</code> provides two approaches to answer this:</p><ol><li>Use the <code>-B</code> parameter, which displays the bytes occupied, making it easy to sort manually.</li><li>An even simpler method: use the <code>-S</code> parameter directly. This makes <code>slabinfo</code> sort by space usage in descending order and automatically appends human-readable units (KB, MB, etc.).</li></ol><p>The figure below shows using <code>-S</code> to view the top 10 slab caches by memory usage (Figure 6.7):</p><p><em>(Figure 6.7 – The top 10 slab caches sorted by total kernel memory space taken)</em></p><p>As you can see, the top ranks are usually occupied by generic <code>kmalloc-*</code> caches or certain high-frequency kernel data structures.</p><p>Here&#39;s an interesting side note: the <code>-U</code> (Show unreclaimable slabs only) option was born entirely out of a real system panic. The story goes like this: unreclaimable slab memory usage on a system once approached 100%, causing the OOM Killer to fail to find any candidate processes to kill, ultimately leading to a direct panic. To make it easier to troubleshoot such desperate situations in the future, a developer submitted a patch that not only made <code>slabinfo</code> capable of displaying these stubborn caches but also updated the OOM Killer code path to print them. This patch was merged in kernel 4.15. You can feel the weight of this commit: <a href="https://github.com/torvalds/linux/commit/7ad3f188aac15772c97523dc4ca3e8e5b6294b9c" target="_blank" rel="noreferrer">https://github.com/torvalds/linux/commit/7ad3f188aac15772c97523dc4ca3e8e5b6294b9c</a></p><h4 id="where-did-the-waste-go-internal-fragmentation" tabindex="-1">Where Did the Waste Go? (Internal Fragmentation) <a class="header-anchor" href="#where-did-the-waste-go-internal-fragmentation" aria-label="Permalink to &quot;Where Did the Waste Go? (Internal Fragmentation)&quot;">​</a></h4><p>Next up is the <code>-L</code> (sort by loss) option. This &quot;loss&quot; might be more familiar to you by another name—<strong>internal fragmentation</strong>.</p><p>The slab layer uses a &quot;best fit&quot; model. When you request 100 bytes, the kernel can&#39;t carve out exactly 100 bytes for you; instead, it stuffs you into a <code>kmalloc-128</code> cache (because 96 bytes isn&#39;t enough). The result: you requested 100 bytes but actually consumed 128 bytes. That 28-byte difference is the so-called Loss or Waste.</p><p>Running <code>sudo slabinfo -L |head</code> displays a list of caches sorted by waste in descending order (look at the fourth column, <code>Loss</code>). This is incredibly helpful for evaluating your system&#39;s memory efficiency—sometimes you&#39;ll find that slightly optimizing the size of certain structures can save a surprising amount of fragmented space.</p><h4 id="digging-into-a-single-cache" tabindex="-1">Digging into a Single Cache <a class="header-anchor" href="#digging-into-a-single-cache" aria-label="Permalink to &quot;Digging into a Single Cache&quot;">​</a></h4><p>If you notice a cache that looks suspicious, or you simply want to study it in depth, the <code>-r</code> (report) option is your best friend.</p><p>By default, it spits out detailed statistics for all caches. A more practical approach is to pair it with a regular expression. For example, to only look at caches related to virtual memory:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> slabinfo</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -r</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vm.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">*</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This displays details for all caches matching the <code>vm.*</code> pattern. If SLUB debug flags (like U) were enabled earlier, you can even see who allocated and freed these objects, and where.</p><p>Sometimes you&#39;ll encounter a cache with an unfamiliar name and won&#39;t know what it&#39;s for. That&#39;s where the <code>-a</code> (or <code>--aliases</code>) option comes in handy—it shows which kernel object this cache is an alias for, pulling back the curtain.</p><p>Want a global overview? The <code>-T</code> option displays a summary snapshot of all caches: how many caches exist, how many are active, how much total memory is used, etc. If that&#39;s still not enough, the <code>-X</code> option provides an extended version with detailed information (Figure 6.8).</p><p><em>(Figure 6.8 – Screenshot showing extended summary information via slabinfo -X)</em></p><p>Finally, to see <strong>all</strong> caches (including empty ones), use the <code>-z</code> (zero) parameter.</p><h4 id="the-option-that-could-save-your-life" tabindex="-1">The Option That Could Save Your Life <a class="header-anchor" href="#the-option-that-could-save-your-life" aria-label="Permalink to &quot;The Option That Could Save Your Life&quot;">​</a></h4><p>Here we have two options dedicated to debugging: <code>-d</code> and <code>-v</code>. Both share a hard prerequisite: your system must boot with the <code>slub_debug</code> kernel parameter, and this parameter must have a non-empty value.</p><p>Let&#39;s look at something concrete first. When you boot with <code>slub_debug=FZPU</code>, you&#39;ll notice that the flags for all slab caches—even those that previously had none—will now have at least the letters <code>PZFU</code> attached (Figure 6.9).</p><p><em>(Figure 6.9 – Partial screenshot – the focus is on the SLUB debug flags being set)</em></p><p>This means the kernel has forcibly added these debug attributes to all caches.</p><p>Regarding the <code>-d</code> option:</p><ul><li>Running <code>slabinfo -d</code> alone <strong>turns debugging off</strong>. This isn&#39;t very intuitive, right? But it&#39;s consistent with the behavior of the <code>slub_debug</code> kernel parameter.</li><li>If you want to <strong>turn on</strong> certain debug flags, you need to pass them explicitly, such as <code>--debug=fzput</code>.</li></ul><p>The underlying mechanism is actually quite simple: when you execute <code>--debug=fzput</code>, the <code>slabinfo</code> tool (as root) writes to the corresponding pseudo-files under <code>/sys/kernel/slab/&lt;slabname&gt;/</code>, setting them to 1.</p><p>The specific mapping is as follows:</p><ul><li><code>f|F</code> -&gt; writes to <code>/sys/kernel/slab/&lt;slabname&gt;/sanity_checks</code></li><li><code>z|Z</code> -&gt; writes to <code>/sys/kernel/slab/&lt;slabname&gt;/red_zone</code></li><li>And so on...</li></ul><p>The code that does the dirty work is here: <a href="https://elixir.bootlin.com/linux/v5.10.60/source/tools/vm/slabinfo.c#L717" target="_blank" rel="noreferrer">https://elixir.bootlin.com/linux/v5.10.60/source/tools/vm/slabinfo.c#L717</a></p><p>Then there&#39;s the <code>-v</code> (validate) option. This option forces SLUB to traverse all objects in a specified cache and check the validity of their metadata. If it finds anything wrong, it blasts diagnostic information directly into the kernel log.</p><p>The format is identical to the &quot;kernel reporting an error&quot; messages you&#39;ve seen before. In fact, the essence of <code>slabinfo -v</code> is simply writing a 1 to <code>/sys/kernel/slab/&lt;slabcache&gt;/validate</code>.</p><p>This is extremely useful for troubleshooting production systems suspected of having memory corruption—proactively triggering a full scan to see if there are any hidden bombs lurking deep inside.</p><p>By the way, the kernel source tree even includes a <code>slabinfo-gnuplot.sh</code> script that can plot slab runtime behavior as graphs. If you&#39;re interested in visualization, check out the instructions in the kernel documentation: Documentation/vm/slub.txt (Extended slabinfo mode and plotting section)</p><h4 id="the-old-reliable-proc-slabinfo" tabindex="-1">The Old Reliable: /proc/slabinfo <a class="header-anchor" href="#the-old-reliable-proc-slabinfo" aria-label="Permalink to &quot;The Old Reliable: /proc/slabinfo&quot;">​</a></h4><p>Of course, the kernel itself exposes this information through procfs, namely <code>/proc/slabinfo</code>. This is actually the data source for tools like <code>slabtop</code>. It also requires root privileges to read.</p><p>Its format is quite detailed (perhaps even a bit verbose):</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> head</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -n2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /proc/slabinfo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> </span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">slabinfo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> -</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> version:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2.1</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># name &lt;active_objs&gt; &lt;num_objs&gt; &lt;objsize&gt; &lt;objperslab&gt; &lt;pagesperslab&gt; : </span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tunables</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">limi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">batchcoun</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">sharedfacto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">r</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> :</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> </span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">slabdata</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">active_slab</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">s</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">num_slab</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">s</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">sharedavai</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">l</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>The figure below offers a glimpse of actual data (Figure 6.10):</p><p><em>(Figure 6.10 – A screenshot showing some data from /proc/slabinfo)</em></p><p>As for how to interpret these columns, <code>man slabinfo(5)</code> explains it clearly. However, note that the man page is slightly outdated, and some of the statistics are primarily designed for the legacy Slab Allocator.</p><p>There&#39;s also an old friend, <code>vmstat</code>. With the <code>-m</code> parameter, it can also display slab statistics. It essentially reads <code>/proc/slabinfo</code> as well.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vmstat</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -m</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><h4 id="watching-slab-like-top-slabtop" tabindex="-1">Watching Slab Like top: slabtop <a class="header-anchor" href="#watching-slab-like-top-slabtop" aria-label="Permalink to &quot;Watching Slab Like top: slabtop&quot;">​</a></h4><p>Since we have <code>top</code> for watching CPU, is there a <code>top</code> for watching slab? Of course there is, and it&#39;s called <code>slabtop</code>.</p><p>Its usage is almost identical to <code>top</code>, refreshing in real time and sorting by object count by default (you can use <code>-s</code> to change the sort field). It&#39;s also based on <code>/proc/slabinfo</code> data, so it requires root.</p><p>Once running, you&#39;ll notice that besides those specific kernel structure caches, the small-size generic caches (the <code>kmalloc-*</code> series) are usually the workhorses of the system, being called most frequently.</p><h4 id="a-more-cutting-edge-perspective-slabratetop" tabindex="-1">A More Cutting-Edge Perspective: slabratetop <a class="header-anchor" href="#a-more-cutting-edge-perspective-slabratetop" aria-label="Permalink to &quot;A More Cutting-Edge Perspective: slabratetop&quot;">​</a></h4><p>Finally, there&#39;s a relatively new member—the eBPF-based <code>slabratetop</code> (which might be called <code>slabratetop-bpfcc</code> on your system).</p><p>Most of the tools mentioned above provide &quot;stock statistics,&quot; whereas <code>slabratetop</code> shows &quot;incremental rates.&quot; It displays the allocation rate (allocations per second) and total bytes of kernel slab caches in real time, with a default refresh interval of one second.</p><p>Internally, it achieves this by tracing the <code>kmem_cache_alloc</code> kernel API.</p><p>For example, to take 3 samples at 5-second intervals:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> slabratetop-bpfcc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 5</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This is incredibly helpful for analyzing the dynamic behavior of a system, letting you see which memory area is in a state of &quot;high-concurrency allocation.&quot;</p><h4 id="hands-on-exercise-who-s-eating-my-memory" tabindex="-1">Hands-On Exercise: Who&#39;s Eating My Memory? <a class="header-anchor" href="#hands-on-exercise-who-s-eating-my-memory" aria-label="Permalink to &quot;Hands-On Exercise: Who&#39;s Eating My Memory?&quot;">​</a></h4><p>Alright, the toolbox is open—how do we use these tools to solve real problems?</p><p>One of the most common questions is: <strong>Where did the memory go?</strong></p><p>This breaks down into two layers: userspace and kernel space. If it&#39;s a userspace process, tools like <code>smem</code>, <code>ps</code>, or simply reading <code>/proc/*/status</code> will do the trick. For example, to find the top 10 processes by physical memory (RSS) usage:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">grep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -r</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;^VmRSS&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /proc/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">*</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/status</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sed</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;s/kB$//&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sort</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -k3n</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tail</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>But if you suspect the kernel is secretly gobbling up memory, especially <strong>slab memory</strong>, things get interesting. Let&#39;s walk through a troubleshooting scenario:</p><ol><li><strong>Step 1</strong>: Use <code>slabratetop</code> to find which cache has an abnormally high allocation frequency or byte count.</li><li><strong>Step 2</strong>: Use dynamic kprobes to capture kernel stacks and see exactly which code path is frantically allocating from this cache.</li></ol><p>Let&#39;s try this in practice.</p><p>First, run <code>slabratetop</code> (or <code>slabratetop-bpfcc</code>):</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> slabratetop-bpfcc</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[...]</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">CACHE</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">                            ALLOCS</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      BYTES</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">names_cache</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">                          18</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      78336</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vm_area_struct</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">                      176</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      46464</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">...</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>In this output (top half of Figure 6.11), I noticed a cache called <code>vm_area_struct</code> with a particularly high allocation rate (176 times per second). That&#39;s suspicious.</p><p><em>(Figure 6.11 – Output from slabinfo and slabratetop)</em></p><p>Now the question is: <strong>Who is allocating <code>vm_area_struct</code>?</strong></p><p>We know that kernel allocation of specific cache objects ultimately calls <code>kmem_cache_alloc()</code>. If we could see the kernel stack for this function in real time, wouldn&#39;t we know exactly who&#39;s calling it?</p><p>This is where kprobe comes in (recalling what we covered in Chapter 4). We can use the <code>kprobe-perf</code> script to capture the call stack of <code>kmem_cache_alloc</code>.</p><p>But there&#39;s a detail: the first parameter of <code>kmem_cache_alloc</code> is a pointer to a <code>struct kmem_cache</code>. How do we know which cache this pointer actually refers to? On x86_64, the first parameter is passed via the <code>RDI</code> register. Inside the <code>struct kmem_cache</code> structure, at an offset of 96 bytes, lies the <code>name</code> member (the cache name).</p><p>So we can write our command like this:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> kprobe-perf</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;p:kmem_cache_alloc name=+0(+96(%di)):string&#39;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This prints the stack for all slab allocations. To filter for only the <code>vm_area_struct</code> we care about, pipe it through <code>grep</code>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> kprobe-perf</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;p:kmem_cache_alloc name=+0(+96(%di)):string&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -A10</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;name=.*vm_area_struct&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>You&#39;ll get output similar to the following (bottom half of Figure 6.11):</p><p><em>(Figure 6.11 – Kernel stack trace showing the call path to kmem_cache_alloc)</em></p><p>This stack trace is very clear: <code>sys_brk() --&gt; do_brk() --&gt; do_brk_flags() --&gt; vm_area_alloc() --&gt; kmem_cache_alloc()</code></p><p><code>sys_brk</code> is the entry point for the userspace <code>brk()</code> system call, typically used to extend the heap memory. To manage a process&#39;s memory areas, the kernel needs to maintain VMA (Virtual Memory Area) structures. Whenever a new memory mapping needs to be created or an existing one extended, the kernel must allocate an object from the dedicated <code>vm_area_struct</code> cache. This is the source of this chain of allocations.</p><p>If you dig into the source code, you&#39;ll find this exact line in <code>mm/mmap.c</code>: <a href="https://elixir.bootlin.com/linux/v5.10.60/source/mm/mmap.c#L3110" target="_blank" rel="noreferrer">https://elixir.bootlin.com/linux/v5.10.60/source/mm/mmap.c#L3110</a></p><p>At this point, you should have a sense of &quot;X-ray vision&quot; into your system&#39;s memory behavior.</p><hr><p><strong>Security Tip</strong></p><p>Finally, although slightly off-topic, this is important: security. To ensure that slab memory contents are thoroughly wiped upon both allocation and freeing (preventing information leaks), you can add the following to your kernel boot parameters: <code>init_on_alloc=1 init_on_free=1</code></p><p>This incurs a performance penalty, but for high-security environments, it&#39;s worth it. For reference, see the recommendations from the Kernel Self Protection Project: <a href="https://kernsec.org/wiki/index.php/Kernel_Self_Protection_Project/Recommended_Settings" target="_blank" rel="noreferrer">https://kernsec.org/wiki/index.php/Kernel_Self_Protection_Project/Recommended_Settings</a></p>`,109)])])}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
