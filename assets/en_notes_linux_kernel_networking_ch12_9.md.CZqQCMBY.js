import{_ as t,o as s,c as r,a as o}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch12_9.md","filePath":"en/notes/linux_kernel_networking/ch12_9.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_networking/ch12_9.md"};function i(a,e,l,d,c,g){return s(),r("div",null,[...e[0]||(e[0]=[o('<h3 id="_12-9-quick-reference" tabindex="-1">12.9 Quick Reference <a class="header-anchor" href="#_12-9-quick-reference" aria-label="Permalink to &quot;12.9  Quick Reference&quot;">​</a></h3><p>We&#39;ve come a long way in this chapter — from the fundamental CSMA/CA handshake, to 802.11n A-MPDU aggregation, and on to 802.11s multi-hop routing. On paper, these protocols are abstract diagrams, but in the kernel code, they take the form of concrete function calls, structure members, and flag bits.</p><p>When you actually sit down to write a driver or debug <code>mac80211</code>, you don&#39;t need lengthy theoretical explanations — you need a map that quickly tells you &quot;what does this function do&quot; and &quot;what does that flag mean.&quot;</p><p>That&#39;s the purpose of this section. It&#39;s not meant to be read — it&#39;s meant to be <strong>looked up</strong>.</p><p>Here I&#39;ve listed some of the core methods mentioned in this chapter, along with that extremely complex <code>ieee80211_rx_status</code> flag dictionary. Treat this page like a glossary — when you get lost in the code jungle of <code>drivers/net/wireless</code>, come back here.</p><hr><h3 id="core-methods-cheat-sheet" tabindex="-1">Core Methods Cheat Sheet <a class="header-anchor" href="#core-methods-cheat-sheet" aria-label="Permalink to &quot;Core Methods Cheat Sheet&quot;">​</a></h3><p>Most of these functions are defined under the <code>net/mac80211/</code> directory. For easy reference, I&#39;ve categorized them by function and added behavioral details critical during invocation that weren&#39;t mentioned in the main text.</p><h4 id="_1-block-ack-and-aggregation-a-mpdu-a-msdu" tabindex="-1">1. Block Ack and Aggregation (A-MPDU / A-MSDU) <a class="header-anchor" href="#_1-block-ack-and-aggregation-a-mpdu-a-msdu" aria-label="Permalink to &quot;1. Block Ack and Aggregation (A-MPDU / A-MSDU)&quot;">​</a></h4><p>This is the core of 802.11n&#39;s throughput improvements. Drivers typically don&#39;t manipulate these details directly, but if you&#39;re debugging why aggregation isn&#39;t enabled, this is where you end up.</p><ul><li><p><strong><code>void ieee80211_send_bar(struct ieee80211_vif *vif, u8 *ra, u16 tid, u16 ssn)</code></strong></p><ul><li><strong>Purpose</strong>: Sends a <strong>Block Ack Request (BAR)</strong> control frame.</li><li><strong>Scenario</strong>: Called when you&#39;ve sent a bunch of aggregated frames but the peer didn&#39;t send back a Block Ack, or when you want to explicitly trigger acknowledgment. <code>ssn</code> (Starting Sequence Number) tells the peer: &quot;acknowledge starting from this sequence number.&quot;</li></ul></li><li><p><strong><code>int ieee80211_start_tx_ba_session(struct ieee80211_sta *pubsta, u16 tid, u16 timeout)</code></strong></p><ul><li><strong>Purpose</strong>: Starts a transmitter-side Block Ack session (aggregation session).</li><li><strong>Mechanism</strong>: It calls the driver&#39;s <code>ampdu_action()</code> callback, passing in <code>IEEE80211_AMPDU_TX_START</code>.</li><li><strong>Note</strong>: This is asynchronous. The driver must call back <code>ieee80211_start_tx_ba_cb()</code> or the <code>_irqsafe</code> variant once ready before actual aggregation begins.</li></ul></li><li><p><strong><code>int ieee80211_stop_tx_ba_session(struct ieee80211_sta *pubsta, u16 tid)</code></strong></p><ul><li><strong>Purpose</strong>: Stops a transmitter-side Block Ack session.</li><li><strong>Mechanism</strong>: Calls the driver&#39;s <code>ampdu_action()</code>, passing <code>IEEE80211_AMPDU_TX_STOP</code>. The driver must then call back <code>ieee80211_stop_tx_ba_cb()</code> to complete cleanup.</li></ul></li><li><p><strong><code>static void ieee80211_send_addba_request(...)</code></strong></p><ul><li><strong>Purpose</strong>: Sends an <strong>ADDBA Request</strong> management frame (Action Frame).</li><li><strong>Parameters</strong>: <code>buf_size</code> determines the receiver&#39;s aggregation window size (typically 64), and <code>timeout</code> determines the session timeout.</li></ul></li><li><p><strong><code>void ieee80211_process_addba_request(...)</code></strong></p><ul><li><strong>Purpose</strong>: Handles a received ADDBA request on the receiver side. This is the core logic of <code>mac80211</code> and is usually not called by the driver — it&#39;s automatically triggered by the kernel upon receiving an Action Frame.</li></ul></li><li><p><strong><code>static void ieee80211_send_addba_resp(...)</code></strong></p><ul><li><strong>Purpose</strong>: Sends an ADDBA Response. In other words, telling the peer &quot;okay, I can aggregate&quot; or &quot;no, your window is too small.&quot;</li></ul></li><li><p><strong><code>void ieee80211_process_delba(...)</code></strong></p><ul><li><strong>Purpose</strong>: Handles a DELBA frame. The peer doesn&#39;t want to play anymore, or the peer is notifying you to stop sending aggregated frames.</li></ul></li><li><p><strong><code>void ieee80211_send_delba(...)</code></strong></p><ul><li><strong>Purpose</strong>: Actively sends a DELBA frame to end a session.</li></ul></li><li><p><strong><code>static ieee80211_rx_result ieee80211_rx_h_amsdu(struct ieee80211_rx_data *rx)</code></strong></p><ul><li><strong>Purpose</strong>: Processes A-MSDU (Aggregated MSDU) on the <strong>RX path</strong>.</li><li><strong>Details</strong>: This is one link in the RX Handler chain. If the hardware passes de-aggregated packets to the kernel, this function is responsible for splitting a large packet back into multiple SKBs.</li></ul></li><li><p><strong><code>static void ieee80211_rx_reorder_ampdu(...)</code></strong></p><ul><li><strong>Purpose</strong>: Manages the <strong>reordering buffer</strong> for A-MPDU.</li><li><strong>Pain point</strong>: Since out-of-order arrival is normal over wireless, this function inserts out-of-order MPDUs into the window, passes consecutively deliverable packets to the upper layer, and holds back missing-sequence packets to wait for a BAR.</li></ul></li></ul><h4 id="_2-power-management" tabindex="-1">2. Power Management <a class="header-anchor" href="#_2-power-management" aria-label="Permalink to &quot;2. Power Management&quot;">​</a></h4><p>Power save logic in wireless is full of pitfalls. A STA wants to sleep, so the AP must help buffer frames; when the STA wakes up, it needs to grab data quickly.</p><ul><li><p><strong><code>void ieee80211_send_nullfunc(struct ieee80211_local *local, ... int powersave)</code></strong></p><ul><li><strong>Purpose</strong>: Sends a <strong>Null Data</strong> frame.</li><li><strong>Key point</strong>: This is the signaling mechanism to tell the AP &quot;I&#39;m going to sleep&quot; or &quot;I&#39;m awake.&quot; Implemented via the Power Management bit in the frame header.</li></ul></li><li><p><strong><code>void ieee80211_send_pspoll(...)</code></strong></p><ul><li><strong>Purpose</strong>: Sends a <strong>PS-Poll</strong> control frame.</li><li><strong>Scenario</strong>: A STA wakes from sleep and actively asks the AP: &quot;Do you have any buffered unicast data for me? If so, send it now.&quot;</li></ul></li><li><p><strong><code>static inline bool ieee80211_check_tim(..., u16 aid)</code></strong></p><ul><li><strong>Purpose</strong>: Checks the <strong>TIM (Traffic Indication Map)</strong> bitmap.</li><li><strong>Scenario</strong>: After receiving a Beacon frame, the STA calls this function to check if the bit corresponding to its AID is set to 1. If so, it means the AP has goods waiting — time to send a PS-Poll.</li></ul></li></ul><h4 id="_3-scanning-authentication-and-association" tabindex="-1">3. Scanning, Authentication, and Association <a class="header-anchor" href="#_3-scanning-authentication-and-association" aria-label="Permalink to &quot;3. Scanning, Authentication, and Association&quot;">​</a></h4><p>This is the core flow of MLME.</p><ul><li><p><strong><code>int ieee80211_request_scan(...)</code></strong></p><ul><li><strong>Purpose</strong>: Initiates an <strong>active scan</strong>.</li></ul></li><li><p><strong><code>void ieee80211_send_probe_req(...)</code></strong></p><ul><li><strong>Purpose</strong>: Sends a <strong>Probe Request</strong> management frame.</li><li><strong>Parameters</strong>: <code>ssid</code> and <code>ie</code> (Information Elements) determine what you&#39;re asking about.</li></ul></li><li><p><strong><code>void ieee80211_send_auth(...)</code></strong></p><ul><li><strong>Purpose</strong>: Sends an <strong>Authentication</strong> frame. This is the first step of connection and can be either Open System or Shared Key.</li></ul></li><li><p><strong><code>static void ieee80211_send_assoc(...)</code></strong></p><ul><li><strong>Purpose</strong>: Sends an <strong>Association Request</strong> frame.</li><li><strong>Timing</strong>: Called after authentication succeeds. It carries all supported rates and capability information.</li></ul></li></ul><h4 id="_4-mesh-routing-802-11s-hwmp" tabindex="-1">4. Mesh Routing (802.11s / HWMP) <a class="header-anchor" href="#_4-mesh-routing-802-11s-hwmp" aria-label="Permalink to &quot;4. Mesh Routing (802.11s / HWMP)&quot;">​</a></h4><p>If you&#39;re writing Mesh-related code, these functions are the concrete implementation entry points for the HWMP protocol in the kernel.</p><ul><li><p><strong><code>struct mesh_path *mesh_path_lookup(..., const u8 *dst)</code></strong></p><ul><li><strong>Purpose</strong>: Looks up a path to <code>dst</code> in the Mesh routing table.</li><li><strong>Returns</strong>: NULL if not found, or a <code>mesh_path</code> structure if found. This is the first step in forwarding data.</li></ul></li><li><p><strong><code>void mesh_path_tx_pending(struct mesh_path *mpath)</code></strong></p><ul><li><strong>Purpose</strong>: Sends data packets backlogged in <code>mpath-&gt;frame_queue</code>.</li><li><strong>Scenario</strong>: When route discovery completes (e.g., upon receiving a PREP), the kernel calls this function to release the packets that were queued at the intersection.</li></ul></li><li><p><strong><code>static int mesh_path_sel_frame_tx(enum mpath_frame_type action, ...)</code></strong></p><ul><li><strong>Purpose</strong>: Generic HWMP frame transmission function.</li><li><strong>Supports</strong>: Sends both <strong>PREQ</strong> (Path Request) and <strong>PREP</strong> (Path Reply). The <code>metric</code> in the parameters is that famous &quot;airtime&quot; cost metric.</li></ul></li><li><p><strong><code>static void hwmp_preq_frame_process(...)</code></strong></p><ul><li><strong>Purpose</strong>: Handles received PREQ frames. This is the core logic where Mesh nodes cooperate to find routes.</li></ul></li></ul><h4 id="_5-device-registration-and-low-level-operations" tabindex="-1">5. Device Registration and Low-Level Operations <a class="header-anchor" href="#_5-device-registration-and-low-level-operations" aria-label="Permalink to &quot;5. Device Registration and Low-Level Operations&quot;">​</a></h4><p>These are the APIs that driver developers touch first.</p><ul><li><p><strong><code>struct ieee80211_hw *ieee80211_alloc_hw(...)</code></strong></p><ul><li><strong>Purpose</strong>: Allocates a <code>ieee80211_hw</code> device structure and the driver&#39;s private data <code>priv</code>. The beginning of everything.</li></ul></li><li><p><strong><code>int ieee80211_register_hw(struct ieee80211_hw *hw)</code></strong></p><ul><li><strong>Purpose</strong>: Registers this hardware device with the kernel. From this moment on, your network card is &quot;alive&quot; in the system.</li></ul></li><li><p><strong><code>int ieee80211_hw_config(..., u32 changed)</code></strong></p><ul><li><strong>Purpose</strong>: Configures hardware parameters.</li><li><strong>Parameters</strong>: <code>changed</code> specifies what to change (e.g., use <code>IEEE80211_CONF_CHANGE_CHANNEL</code> to change the channel). This function typically drops down to the driver&#39;s <code>config()</code> callback.</li></ul></li><li><p><strong><code>void ieee80211_rx_irqsafe(struct ieee80211_hw *hw, struct sk_buff *skb)</code></strong></p><ul><li><strong>Purpose</strong>: <strong>Receives data packets</strong>.</li><li><strong>Feature</strong>: As the name implies, it can be safely called from <strong>hardware interrupt context</strong>. It uses softirqs (tasklets) to defer the packet reception work.</li></ul></li></ul><h4 id="_6-rx-processing-and-status" tabindex="-1">6. RX Processing and Status <a class="header-anchor" href="#_6-rx-processing-and-status" aria-label="Permalink to &quot;6. RX Processing and Status&quot;">​</a></h4><p>Receiving packets isn&#39;t just about passing SKBs up the stack — you also need to check FCS, decryption status, and reordering.</p><ul><li><p><strong><code>struct ieee80211_rx_status *IEEE80211_SKB_RXCB(struct sk_buff *skb)</code></strong></p><ul><li><strong>Purpose</strong>: Extracts <code>rx_status</code> from the SKB&#39;s control block (<code>cb</code>).</li><li><strong>Key point</strong>: After the hardware driver receives a packet, it must fill in information like signal strength, rate, and MAC timestamp in this structure, place it in <code>skb-&gt;cb</code>, and then hand it to <code>mac80211</code>.</li></ul></li><li><p><strong><code>static ieee80211_rx_result ieee80211_rx_h_check(struct ieee80211_rx_data *rx)</code></strong></p><ul><li><strong>Purpose</strong>: The first checkpoint in RX processing.</li><li><strong>Logic</strong>: Checks if this is a duplicate frame (Retransmission). If so, it increments the <code>dot11FrameDuplicateCount</code> counter and drops the frame.</li></ul></li><li><p><strong><code>static bool ieee80211_sta_manage_reorder_buf(...)</code></strong></p><ul><li><strong>Purpose</strong>: The concrete implementation logic for managing the reordering buffer. It determines whether a packet should enter the buffer to wait or can be passed directly to the network stack.</li></ul></li></ul><hr><h3 id="flag-dictionary-ieee80211-rx-status" tabindex="-1">Flag Dictionary: <code>ieee80211_rx_status</code> <a class="header-anchor" href="#flag-dictionary-ieee80211-rx-status" aria-label="Permalink to &quot;Flag Dictionary: `ieee80211_rx_status`&quot;">​</a></h3><p>This is one of the most error-prone areas in <code>mac80211</code> driver development. The <code>flag</code> field in the <code>rx_status</code> structure is a 32-bit bitfield. Hardware drivers are responsible for setting these bits correctly, and <code>mac80211</code> is responsible for doing the right thing based on them.</p><p>The table below is the &quot;cryptic manual&quot; I frequently flip through when debugging packet reception issues. I&#39;ve expanded the meanings a bit so you don&#39;t end up staring blankly at the spec sheet.</p><p><strong>Table 12-1: Rx Flags (the flag field of <code>ieee80211_rx_status</code>)</strong></p><table tabindex="0"><thead><tr><th style="text-align:left;">Linux Symbolic Constant</th><th style="text-align:left;">Bit</th><th style="text-align:left;">True Meaning and Practical Tips</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>RX_FLAG_MMIC_ERROR</strong></td><td style="text-align:left;">0</td><td style="text-align:left;"><strong>Michael MIC error</strong>.<br>This frame passed CRC check, but its content was tampered with. Usually occurs with TKIP encryption. If this error is set, drop the packet immediately.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_DECRYPTED</strong></td><td style="text-align:left;">1</td><td style="text-align:left;"><strong>Hardware decrypted</strong>.<br>An extremely important flag. If the hardware decrypted it for you (WEP/WPA/WPA2), you must set this bit. When the kernel sees it, it knows not to run software decryption algorithms.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_MMIC_STRIPPED</strong></td><td style="text-align:left;">3</td><td style="text-align:left;"><strong>MIC stripped</strong>.<br>The hardware not only decrypted but also removed the trailing MIC checksum. The kernel will calculate the MIC accordingly.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_IV_STRIPPED</strong></td><td style="text-align:left;">4</td><td style="text-align:left;"><strong>IV/ICV stripped</strong>.<br>After hardware decryption, the header IV (Initialization Vector) and trailing ICV (Integrity Check Value) were both removed. Setting this bit means the data you&#39;re giving the kernel is &quot;clean&quot; plaintext payload.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_FAILED_FCS_CRC</strong></td><td style="text-align:left;">5</td><td style="text-align:left;"><strong>FCS check failed</strong>.<br>This packet is bad. Although you received it, the FCS (Frame Check Sequence) doesn&#39;t match. The kernel will usually silently drop it.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_FAILED_PLCP_CRC</strong></td><td style="text-align:left;">6</td><td style="text-align:left;"><strong>PLCP check failed</strong>.<br>The physical layer preamble and signal header are wrong. This is usually caused by a very weak signal.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_MACTIME_START</strong></td><td style="text-align:left;">7</td><td style="text-align:left;"><strong>Timestamp valid (start time)</strong>.<br>The <code>rx_status-&gt;mactime</code> field stores the <strong>time the first symbol of the frame arrived</strong>. Critical for scenarios requiring precise ranging or synchronization (e.g., certain positioning protocols).</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_SHORTPRE</strong></td><td style="text-align:left;">8</td><td style="text-align:left;"><strong>Short preamble</strong>.<br>Uses the 802.11b Short Preamble mode.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_HT</strong></td><td style="text-align:left;">9</td><td style="text-align:left;"><strong>High Throughput (11n)</strong>.<br>This is an 802.11n frame. Note that in this case, the <code>rate_idx</code> field stores the MCS index, not the traditional legacy rate.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_40MHZ</strong></td><td style="text-align:left;">10</td><td style="text-align:left;"><strong>HT40 mode</strong>.<br>Uses 40MHz channel bandwidth.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_SHORT_GI</strong></td><td style="text-align:left;">11</td><td style="text-align:left;"><strong>Short guard interval</strong>.<br>Uses the 400ns Short Guard Interval, slightly faster than the 800ns GI.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_NO_SIGNAL_VAL</strong></td><td style="text-align:left;">12</td><td style="text-align:left;"><strong>No signal strength</strong>.<br>The hardware didn&#39;t measure signal strength (RSSI), or doesn&#39;t support it at all. The <code>rx_status-&gt;signal</code> field is invalid at this point.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_HT_GF</strong></td><td style="text-align:left;">13</td><td style="text-align:left;"><strong>HT Greenfield</strong>.<br>This is 802.11n Greenfield mode, without the Training Field added for backward compatibility with 802.11a/g. Fastest speed, but legacy devices can&#39;t understand it.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_AMPDU_DETAILS</strong></td><td style="text-align:left;">14</td><td style="text-align:left;"><strong>A-MPDU details known</strong>.<br>This packet is part of an aggregated frame. In this case, <code>ampdu_ref</code> in <code>rx_status</code> must be valid, used to distinguish different A-MPDU bursts.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_AMPDU_REPORT_ZEROLEN</strong></td><td style="text-align:left;">15</td><td style="text-align:left;"><strong>Driver reports zero-length subframes</strong>.<br>This is a somewhat special hardware behavior where some hardware reports empty packets within aggregated frames.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_AMPDU_IS_ZEROLEN</strong></td><td style="text-align:left;">16</td><td style="text-align:left;"><strong>This is a zero-length subframe</strong>.<br>Corresponds to the report above, marking this packet itself as empty, typically used for monitoring.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_AMPDU_LAST_KNOWN</strong></td><td style="text-align:left;">17</td><td style="text-align:left;"><strong>Last frame known</strong>.<br>The driver knows this is the last packet of this A-MPDU burst.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_AMPDU_IS_LAST</strong></td><td style="text-align:left;">18</td><td style="text-align:left;"><strong>This is the last frame</strong>.<br>This packet is the terminator of this A-MPDU.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_AMPDU_DELIM_CRC_ERROR</strong></td><td style="text-align:left;">19</td><td style="text-align:left;"><strong>Delimiter CRC error</strong>.<br>In A-MPDU, there are Delimiters between subframes. If this Delimiter&#39;s CRC fails, it means the hardware&#39;s parsing synchronization has gone wrong.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_AMPDU_DELIM_CRC_KNOWN</strong></td><td style="text-align:left;">20</td><td style="text-align:left;"><strong>Delimiter CRC value valid</strong>.<br>If set, it means the <code>rx_status-&gt;ampdu_delimiter_crc</code> field stores that calculated CRC value.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_MACTIME_END</strong></td><td style="text-align:left;">21</td><td style="text-align:left;"><strong>Timestamp valid (end time)</strong>.<br><code>mactime</code> records the <strong>time the last symbol of the frame (including FCS) was fully received</strong>. Mutually exclusive with the START flag.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_VHT</strong></td><td style="text-align:left;">22</td><td style="text-align:left;"><strong>Very High Throughput (11ac)</strong>.<br>This is an 802.11ac frame. <code>rate_idx</code> now stores the VHT MCS index.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_80MHZ</strong></td><td style="text-align:left;">23</td><td style="text-align:left;"><strong>VHT80 mode</strong>.<br>802.11ac, using 80MHz bandwidth.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_80P80MHZ</strong></td><td style="text-align:left;">24</td><td style="text-align:left;"><strong>VHT80+80 mode</strong>.<br>Uses two non-contiguous 80MHz channels. This is an advanced feature of 802.11ac.</td></tr><tr><td style="text-align:left;"><strong>RX_FLAG_160MHZ</strong></td><td style="text-align:left;">25</td><td style="text-align:left;"><strong>VHT160 mode</strong>.<br>Contiguous 160MHz bandwidth. Not only does it require hardware support, but it also depends on whether local regulations allow you to use such a wide band.</td></tr></tbody></table><hr><h3 id="chapter-echo" tabindex="-1">Chapter Echo <a class="header-anchor" href="#chapter-echo" aria-label="Permalink to &quot;Chapter Echo&quot;">​</a></h3><p>Well, that&#39;s the last piece of the wireless networking puzzle in this book.</p><p>Looking back at this chapter, we were essentially rebuilding a <strong>Tower of Babel</strong> inside the Linux kernel. At the physical layer (PHY), we talked to hardware through <code>ieee80211_hw</code>; at the MAC layer, we established the order of CSMA/CA through <code>mac80211</code>; and through the tug-of-war between FullMAC and SoftMAC, we saw the driver developer&#39;s dilemma.</p><p>We saw how the 802.11 protocol family evolved step by step from simple <strong>yielding</strong> (CSMA/CA), to <strong>greed</strong> (802.11n aggregation), and finally to <strong>intelligence</strong> (802.11s Mesh routing).</p><p>Remember that question from the beginning of this chapter — why is Wi-Fi so complicated in Linux? Now the answer is clear: <strong>because it tries to simulate a reliable, high-speed point-to-point dedicated line over a noisy, unreliable shared medium.</strong></p><ul><li><strong>FullMAC</strong> shoves the complexity into a black box — hardware vendors take the bullet for you, but they also tie your hands.</li><li><strong>SoftMAC (mac80211)</strong> hands the complexity to the kernel — you have to handle TIM, buffering, and reordering yourself, but you gain absolute control over the airwaves.</li></ul><p>Next time you look at the rapid-fire output of <code>iw</code>, or stumble upon <code>ieee80211_rx: mmic_error</code> in the kernel logs, I hope you&#39;ll realize: this isn&#39;t just a log line — it&#39;s the battle damage report of this chip fighting through chaotic radio waves just to get that one bit of data from the antenna into memory.</p><p>This chapter is over. Retract your antennas — see you in the next chapter. There, we&#39;ll switch from &quot;invisible waves&quot; to &quot;visible but intangible fiber optics&quot; — <strong>RDMA</strong>. There, the kernel won&#39;t even intervene in data transfer; it merely paves the way.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: Understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: Understanding&quot;">​</a></h3><p><strong>Question</strong>: In Infrastructure BSS mode, when a wireless client (STA) sends a data frame to an access point (AP), and that data ultimately needs to be forwarded to an external wired network, what values should the ToDS and FromDS bits in the 802.11 MAC header&#39;s Frame Control field be set to?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: ToDS = 1, FromDS = 0</p><p><strong>Analysis</strong>: According to the 802.11 specification, the ToDS (Distribution System) bit indicates that the data frame is destined for the distribution system (i.e., the AP), and the FromDS bit indicates that the data frame comes from the distribution system. When a client sends data to the AP (to be forwarded out), the data is entering the distribution system, so ToDS is set to 1 and FromDS is set to 0. Conversely, when the AP sends data to the client, FromDS is 1 and ToDS is 0.</p></details><h3 id="exercise-2-understanding" tabindex="-1">Exercise 2: Understanding <a class="header-anchor" href="#exercise-2-understanding" aria-label="Permalink to &quot;Exercise 2: Understanding&quot;">​</a></h3><p><strong>Question</strong>: When an access point (AP) in the mac80211 subsystem receives a Null Data Packet from a client and finds that the Power Management bit in the Frame Control field is 1, what does the AP do next?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: The AP will mark the client as having entered sleep mode and begin buffering unicast data packets destined for that client into the ps_tx_buf buffer.</p><p><strong>Analysis</strong>: A Null Data Packet contains no payload and is primarily used for power management notification. According to the Power Save Mode mechanism, when the PM bit is 1, it indicates the station is about to enter power-save mode. Upon receiving this notification, the AP will store subsequent unicast packets destined for that station in the station&#39;s corresponding ps_tx_buf queue (up to 128 packets), until the station wakes up and retrieves the data via PS-Poll or a Null Data frame.</p></details><h3 id="exercise-3-application" tabindex="-1">Exercise 3: Application <a class="header-anchor" href="#exercise-3-application" aria-label="Permalink to &quot;Exercise 3: Application&quot;">​</a></h3><p><strong>Question</strong>: Suppose you are developing a FullMAC wireless network card driver. Compared to a SoftMAC (using mac80211) driver, what are the main differences in logical responsibility for handling 802.11 management frames (such as Association Request, Authentication) in kernel space?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: In the FullMAC driver architecture, MLME (MAC Layer Management Entity) logic is primarily handled by firmware or hardware, and the kernel driver usually only handles data-plane transmission and basic control; in the SoftMAC (mac80211) architecture, MLME logic (such as association, authentication, and scanning state machines) is jointly handled by the mac80211 subsystem in the kernel and user-space daemons (such as wpa_supplicant/hostapd).</p><p><strong>Analysis</strong>: FullMAC devices handle most MAC layer management functions themselves (such as state maintenance and frame exchanges), making the kernel driver relatively lightweight. mac80211 is a SoftMAC framework that places the complex protocol state machine in kernel software, thereby unifying the behavior of different hardware and allowing more flexible software-defined features (such as the complex routing of Mesh networks).</p></details><h3 id="exercise-4-thinking" tabindex="-1">Exercise 4: Thinking <a class="header-anchor" href="#exercise-4-thinking" aria-label="Permalink to &quot;Exercise 4: Thinking&quot;">​</a></h3><p><strong>Question</strong>: IEEE 802.11n introduced two aggregation technologies, A-MPDU (Aggregated MAC Protocol Data Unit) and A-MSDU (Aggregated MAC Service Data Unit), to improve throughput. From the perspective of &quot;retransmission overhead,&quot; why is A-MPDU generally more suitable than A-MSDU for wireless environments with high bit error rates?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Because A-MPDU&#39;s retransmission granularity is at the MPDU (MAC Protocol Data Unit) level, while A-MSDU&#39;s retransmission granularity is the entire aggregated MSDU. In A-MPDU, if only one subframe in the aggregated block errors or is lost, the receiver can use the Block Ack mechanism to request retransmission of only that one erroneous subframe, without needing to retransmit the entire aggregated block. Conversely, A-MSDU packs multiple MSDUs into one large frame — if a bit error occurs during transmission causing CRC check failure, the entire large frame must be retransmitted, which significantly reduces effective throughput when channel quality is poor (high bit error rate).</p><p><strong>Analysis</strong>: This question tests deep thinking about low-level link efficiency. Although A-MSDU reduces header overhead, its retransmission mechanism is &quot;all-or-nothing.&quot; A-MPDU, paired with the Block Ack mechanism, provides finer-grained error recovery capability (Selective Retransmission). In unreliable wireless channels, the efficiency gains from this characteristic typically outweigh its slightly higher header overhead.</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>The Linux kernel treats wireless networking as an independent subsystem <code>mac80211</code>, rather than a simple variant of Ethernet, primarily due to the high unreliability of the wireless physical medium (frequent packet loss) and its unique medium access method (shifting from collision detection CD to collision avoidance CA). To cope with the rapid iteration of 802.11 protocols, Linux mainstream adopts the <strong>SoftMAC</strong> architecture: complex MAC layer logic (such as frame construction, encryption, and retransmission) is uniformly taken over by the kernel, while drivers only handle hardware interaction, yielding extremely high flexibility and control.</p><p>The 802.11 MAC header is far more complex than Ethernet&#39;s, containing up to 4 address fields (MAC Header 4 Addresses) and rich frame control bits. These structures define the frame&#39;s specific identity (management/control/data), direction (ToDS/FromDS), and behavioral attributes (such as power save mode, retransmission flag). This complexity stems from traffic forwarding requirements (AP forwarding data) and special mechanism needs in wireless networks. Driver developers must deeply understand these fields to correctly parse and construct data frames.</p><p>In <strong>Infrastructure</strong> mode, a client must complete two steps — authentication and association — before accessing the network and obtaining a unique AID (Association ID). To address the battery life concerns of mobile devices (such as phones and laptops), the protocol designed a meticulous <strong>Power Save Mode</strong>: clients notify the AP of sleep via a Null Data frame flag bit, the AP maintains a <code>ps_tx_buf</code> for each station to buffer unicast packets, and uses the TIM (Traffic Indication Map) bitmap in Beacon frames to notify clients that &quot;goods are waiting for pickup,&quot; after which clients actively &quot;pick up&quot; their data via PS-Poll frames.</p><p><strong>MLME (MAC Layer Management Entity)</strong> is responsible for handling the underlying logic of network discovery and maintenance. To find an AP, a device performs <strong>Scanning</strong>: passive scanning (silently listening for Beacons) must be used in the 5GHz radar band, while active scanning (sending Probe Requests and waiting for Probe Responses) is typically used in normal bands. When a client discovers an AP with a better signal while moving, it triggers the <strong>Roaming</strong> mechanism, establishing a connection with the new AP by sending a Reassociation Request frame that includes the original AP&#39;s address, achieving seamless network handoff.</p><p>At the kernel implementation level, the <code>mac80211</code> framework decouples generic logic from hardware details. The core structure <code>ieee80211_hw</code> serves as the hardware&#39;s abstract ID card, while the <code>ieee80211_ops</code> callback function set defines the driver&#39;s operation specifications (such as <code>tx</code> for transmitting packets, <code>config</code> for configuring channels, and <code>start</code> for starting the device). Each connected peer station corresponds to a <code>sta_info</code> structure in the kernel — it not only stores the MAC address and statistics but also manages critical state such as the power-save buffer queue (<code>ps_tx_buf</code>), making it the core data structure for drivers to maintain connection relationships.</p>',62)])])}const p=t(n,[["render",i]]);export{u as __pageData,p as default};
