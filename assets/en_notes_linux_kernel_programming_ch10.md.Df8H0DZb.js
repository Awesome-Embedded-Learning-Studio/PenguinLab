import{_ as t,o as s,c as i,a as n}from"./chunks/framework.8tiMeCK-.js";const p=JSON.parse('{"title":"Chapter 10: CPU Scheduler (Part 1)","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_programming/ch10.md","filePath":"en/notes/linux_kernel_programming/ch10.md","lastUpdated":null}'),a={name:"en/notes/linux_kernel_programming/ch10.md"};function o(r,e,l,h,c,d){return s(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="chapter-10-cpu-scheduler-part-1" tabindex="-1">Chapter 10: CPU Scheduler (Part 1) <a class="header-anchor" href="#chapter-10-cpu-scheduler-part-1" aria-label="Permalink to &quot;Chapter 10: CPU Scheduler (Part 1)&quot;">​</a></h1><h2 id="chapter-introduction" tabindex="-1">Chapter Introduction <a class="header-anchor" href="#chapter-introduction" aria-label="Permalink to &quot;Chapter Introduction&quot;">​</a></h2><p>In the previous two chapters, we wrestled with the quagmire of memory management—MGLRU, DAMON, OOM Killer. These names should now sound familiar, if not headache-inducing. We were either dynamically allocating memory or desperately reclaiming it; in short, we were revolving entirely around memory.</p><p>Now, let&#39;s change the channel.</p><p>In this chapter and the next, we&#39;ll shift our focus from memory to the CPU. Specifically, CPU scheduling—the mechanism by which the operating system decides &quot;who gets to run on the CPU and who gets kicked off to wait.&quot; This isn&#39;t just foundational knowledge for writing kernels and drivers; as a system architect, if you want your user-space programs to stay responsive and crash-free under high concurrency, you must understand this stuff.</p><p>We&#39;ll start from scratch: first, we need to figure out exactly what the kernel is scheduling (processes or threads?), and then understand what those POSIX scheduling policies are all about. Next, we&#39;ll pick up <code>perf</code> as our &quot;scalpel&quot; and dissect the CPU&#39;s runtime—watching with our own eyes as threads jump between cores.</p><p>Once we&#39;ve mastered these visualization tools, we&#39;ll peel back the kernel layers like an onion to see how Linux implements complex scheduling logic through &quot;modular scheduling classes.&quot; Finally, we&#39;ll dive into code-level details to figure out exactly who calls the scheduler functions, when they are called, and what actually happens during a context switch.</p><p>But before we dive headfirst into the code, we need to clarify a frequently misunderstood fact: the CPU is a shared resource, but that doesn&#39;t mean only processes are fighting for it. Linux has a deeper resource management framework—cgroups (Control Groups). If you looked at Figure 10.1 earlier, you might have noticed a blurry &quot;cgroups&quot; layer at the top. Ignore it for now; we&#39;ll dedicate the next chapter to it. But keep in mind that the final scheduling decisions are often the combined result of the scheduler and cgroups.</p><p>Ready? This ride might be a bit dizzying, but it&#39;s absolutely worth it.</p><hr><h2 id="_10-1-cpu-scheduler-internals-part-1-essential-background-knowledge" tabindex="-1">10.1 CPU Scheduler Internals (Part 1): Essential Background Knowledge <a class="header-anchor" href="#_10-1-cpu-scheduler-internals-part-1-essential-background-knowledge" aria-label="Permalink to &quot;10.1 CPU Scheduler Internals (Part 1): Essential Background Knowledge&quot;">​</a></h2><p>Before we look at complex scheduling algorithms, we need to nail down a few of the most basic concepts. These form the foundation for understanding all the logic that follows.</p><h3 id="_10-1-1-what-exactly-is-linux-s-kernel-schedulable-entity-kse" tabindex="-1">10.1.1 What Exactly is Linux&#39;s Kernel Schedulable Entity (KSE)? <a class="header-anchor" href="#_10-1-1-what-exactly-is-linux-s-kernel-schedulable-entity-kse" aria-label="Permalink to &quot;10.1.1 What Exactly is Linux&#39;s Kernel Schedulable Entity (KSE)?&quot;">​</a></h3><p>Back in Chapter 6, when we talked about processes and threads, we mentioned that for every living user-mode thread, the kernel allocates a <code>task_struct</code> structure, along with a user stack and a kernel stack. Kernel threads also have a <code>task_struct</code>, but they only have a kernel stack.</p><p>The key question now is: <strong>when the scheduler says &quot;time to switch workers,&quot; who exactly is it switching?</strong></p><p>In operating system terminology, this thing is called the <strong>Kernel Schedulable Entity (KSE)</strong>.</p><p>Many people intuitively think that the &quot;process&quot; is the basic unit of scheduling. But on Linux, this intuition is <strong>wrong</strong>.</p><p>Linux&#39;s KSE is the <strong>thread</strong>, not the process (though, of course, every process contains at least one thread). The scheduler operates at the granularity of threads.</p><p>Let&#39;s use a hypothetical scenario to &quot;anchor&quot; this understanding.</p><p>Suppose your system has only <strong>1 CPU core</strong>, running 3 user-space processes:</p><ul><li><strong>P1</strong>: has only 1 thread.</li><li><strong>P2</strong>: has 2 threads.</li><li><strong>P3</strong>: has 5 threads.</li></ul><p>On top of that, there are 3 kernel threads running in the kernel.</p><p>So, how many entities are fighting for this single CPU core right now?</p><p>Not 3 (the number of processes), and not 6 (processes + kernel threads). The answer is <strong>1 + 2 + 5 + 3 = 11 threads</strong>.</p><p>These 11 threads, if they are all in a &quot;runnable&quot; state (i.e., Ready), will be fighting it out in the CPU&#39;s &quot;run queue.&quot; As for which process they belong to, the scheduler doesn&#39;t care. In the scheduler&#39;s eyes, there are only threads.</p><blockquote><p><strong>Analogy Moment 1: Establishing the Mapping</strong> You can think of the scheduler as a <strong>traffic cop</strong>. A process is a <strong>convoy</strong>, and a thread is <strong>each individual vehicle</strong> in that convoy. The cop doesn&#39;t direct &quot;convoys&quot;; they direct &quot;each vehicle.&quot; A convoy might have one car, or it might have ten, but that&#39;s irrelevant to the cop—they only look at cars.</p><p><strong>Analogy Moment 2: Revealing the Distance</strong> But this analogy breaks down when it comes to <strong>priority</strong>. In real traffic, vehicles in a convoy usually travel together. In Linux, however, different threads (cars) within the same process (convoy) can have one speeding down the highway (high priority) while another is broken down on the roadside (low priority). In the scheduler&#39;s eyes, they are independent individuals with no &quot;brotherly loyalty.&quot;</p><p><strong>Analogy Moment 3: Validating the Takeaway</strong> Returning to our traffic cop. When we say &quot;system load is high,&quot; it actually means there are 11 cars fighting for lane space (11 threads), not just 3 convoys running. If the 5 cars in convoy P3 are all flooring the gas pedal, the cop will keep focusing on those 5 cars, completely ignoring the lone car from convoy P1.</p></blockquote><p>Now we have it clear: <strong>the KSE is the thread</strong>. So in the following discussion, we will use the word &quot;thread&quot; instead of &quot;process.&quot; If you&#39;re used to reading old-school OS textbooks that still talk about &quot;process state machines,&quot; please automatically substitute &quot;thread state machines&quot; in your mind from now on.</p><h3 id="_10-1-2-the-linux-process-thread-state-machine" tabindex="-1">10.1.2 The Linux Process (Thread) State Machine <a class="header-anchor" href="#_10-1-2-the-linux-process-thread-state-machine" aria-label="Permalink to &quot;10.1.2 The Linux Process (Thread) State Machine&quot;">​</a></h3><p>Every Linux thread flows between different states during its lifetime. Drawing these states out gives us a state machine.</p><p>Since we&#39;ve established that the KSE is the thread, let&#39;s use standard Linux terminology to describe these states. The <code>ps</code> command typically represents these states with a single letter, such as <code>R</code>, <code>S</code>, <code>D</code>, and so on.</p><p><strong>Core State List</strong></p><ol><li><p><strong><code>TASK_RUNNING</code> (R state)</strong>:</p><ul><li><strong>Alias</strong>: Ready-to-run or Running.</li><li><strong>Meaning</strong>: This is the most easily misunderstood state. It does not mean &quot;currently running.&quot; It represents <strong>&quot;currently running&quot; OR &quot;ready to run&quot;</strong>.</li><li><strong>Key Point</strong>: As long as a thread is in this state, it is in the <strong>run queue</strong>. Linux doesn&#39;t bother to distinguish between &quot;running on the CPU&quot; and &quot;queued up waiting to run&quot;; both are <code>R</code>. They are all candidates, ready to hop onto the CPU at any time.</li></ul></li><li><p><strong><code>TASK_INTERRUPTIBLE</code> (S state)</strong>:</p><ul><li><strong>Alias</strong>: Interruptible sleep.</li><li><strong>Meaning</strong>: The thread is sleeping (waiting for an event, like a network packet or disk I/O).</li><li><strong>Characteristic</strong>: Although it&#39;s &quot;sleeping,&quot; it can be disturbed. If you send it a signal at this point, it will be forcibly woken up to handle the signal.</li></ul></li><li><p><strong><code>TASK_UNINTERRUPTIBLE</code> (D state)</strong>:</p><ul><li><strong>Alias</strong>: Uninterruptible sleep.</li><li><strong>Meaning</strong>: The thread is sleeping, and it is <strong>immovable</strong>.</li><li><strong>Characteristic</strong>: Usually used for extremely critical I/O waits where it cannot be interrupted by signals. If a large number of processes in your system are in the <code>D</code> state, it usually means the storage system is hung or very slow. This is also why it shows up as <code>D</code> in <code>ps</code> (Dead? No, it&#39;s Disk Wait).</li></ul></li><li><p><strong><code>TASK_STOPPED</code> (T state)</strong>:</p><ul><li><strong>Meaning</strong>: The thread has been paused. This usually happens when it receives a <code>SIGSTOP</code> signal or is being traced by a debugger.</li></ul></li><li><p><strong><code>TASK_DEAD / TASK_ZOMBIE</code> (X / Z state)</strong>:</p><ul><li><strong>Meaning</strong>: The thread is dead, or has become a zombie.</li><li><strong>Zombie</strong>: The thread has finished executing, but its parent process hasn&#39;t come to &quot;collect its body&quot; (called <code>wait()</code>). This is a half-dead, half-alive state that must be cleaned out of the system.</li></ul></li></ol><p><strong>Dynamic State Transitions</strong></p><p>When a thread is freshly created by <code>fork()</code> or <code>pthread_create()</code>, the kernel sets it to <code>TASK_RUNNING</code> (the <code>R</code> state). At this point, it enters the run queue, ready to be picked by the scheduler at any moment.</p><p>If this thread needs to wait for a resource (like reading a file), it will call a scheduler-related sleep function, change its state from <code>R</code> to <code>S</code> or <code>D</code>, and remove itself from the run queue, placing itself into a <strong>wait queue</strong>.</p><p>When the event it was waiting for occurs (like disk data being read up), the kernel &quot;wakes&quot; it up. This actually does two things:</p><ol><li>Removes it from the wait queue.</li><li>Puts it back onto the CPU&#39;s run queue, changing its state back to <code>TASK_RUNNING</code>.</li></ol><p>Note that being woken up does <strong>not</strong> mean it runs on the CPU <strong>immediately</strong>. It simply becomes a candidate in the <code>R</code> state again and has to wait in line for the scheduler to pick it.</p><p><strong>Code Perspective: <code>__state</code></strong></p><p>How is all of this implemented at the code level? The answer lies in the <code>task_struct</code> structure.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> task_struct {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __state;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // 注意：以前叫 &#39;state&#39;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>This member, <code>__state</code>, is simply an integer bitmask. When it is <code>TASK_RUNNING</code>, the thread is running; when it is <code>TASK_INTERRUPTIBLE</code>, the thread is sleeping.</p><blockquote><p><strong>⚠️ Pitfall Warning</strong> Never assume there is only one global run queue in the system, and never assume there is only one wait queue. <strong>Linux maintains a separate run queue for each CPU core.</strong> Wait queues, on the other hand, are everywhere—any driver or kernel module can create one whenever it wants.</p></blockquote><p>With this background knowledge, let&#39;s look at the specific scheduling policies Linux supports—that is, what rules it uses to pick someone from the run queue.</p><h3 id="_10-1-3-posix-scheduling-policies" tabindex="-1">10.1.3 POSIX Scheduling Policies <a class="header-anchor" href="#_10-1-3-posix-scheduling-policies" aria-label="Permalink to &quot;10.1.3 POSIX Scheduling Policies&quot;">​</a></h3><p>You can roughly think of a &quot;scheduling policy&quot; as an &quot;algorithm for selecting talent.&quot;</p><p>Linux doesn&#39;t have just one scheduling algorithm. The POSIX standard mandates that any compliant operating system must implement at least three policies. Linux not only implements these three but adds a few of its own, seamlessly integrating them all through a <strong>modular scheduling class</strong> design.</p><p>First, we need to establish a major premise: <strong>every thread in the system, at any given moment, is bound to a specific scheduling policy.</strong> This policy can be changed dynamically.</p><p>The table below is a cheat sheet for Linux scheduling policies. I recommend skimming it first to get a general impression; we&#39;ll pick out the key points for a deep dive later.</p><table tabindex="0"><thead><tr><th style="text-align:left;">Scheduling Policy</th><th style="text-align:left;">Key Characteristics</th><th style="text-align:left;">Priority Range</th></tr></thead><tbody><tr><td style="text-align:left;"><strong><code>SCHED_OTHER</code></strong> <br> (also known as <code>SCHED_NORMAL</code>)</td><td style="text-align:left;"><strong>Default policy</strong>. This is the &quot;ordinary person&#39;s&quot; policy, pursuing <strong>fairness</strong> and <strong>overall throughput</strong>. It belongs to the CFS (Completely Fair Scheduler) class.</td><td style="text-align:left;">Nice value:<br> -20 (highest) ~ +19 (lowest) <br> Default is 0</td></tr><tr><td style="text-align:left;"><strong><code>SCHED_FIFO</code></strong></td><td style="text-align:left;"><strong>Very aggressive real-time policy</strong>. Once it gets on the CPU, unless it voluntarily steps down (blocks/exits) or gets kicked off by a higher-priority thread, it <strong>holds it forever</strong> with an infinite time slice.</td><td style="text-align:left;">Real-time priority:<br> 1 ~ 99 <br> Higher number means more aggressive</td></tr><tr><td style="text-align:left;"><strong><code>SCHED_RR</code></strong></td><td style="text-align:left;"><strong>Round-robin real-time policy</strong>. Just as aggressive as <code>FIFO</code>, but it has a <strong>time slice</strong> (default 100ms). When the time slice expires, if the priority is the same, it swaps out for someone else—everyone takes turns.</td><td style="text-align:left;">Real-time priority:<br> 1 ~ 99</td></tr><tr><td style="text-align:left;"><strong><code>SCHED_BATCH</code></strong></td><td style="text-align:left;"><strong>Suitable for batch tasks</strong>. Designed for programs that don&#39;t interact with users and just do head-down computation. The kernel minimally preempts it to increase throughput.</td><td style="text-align:left;">Nice value: -20 ~ +19</td></tr><tr><td style="text-align:left;"><strong><code>SCHED_IDLE</code></strong></td><td style="text-align:left;"><strong>Extremely humble policy</strong>. Lower priority than Nice 19. It only gets to run when the CPU has nothing else to do. The PID 0 <code>swapper</code> idle thread falls into this category.</td><td style="text-align:left;">Lowest (even lower than nice 19)</td></tr></tbody></table><p><strong>Table 10.1: Overview of Linux (POSIX-compliant) Scheduling Policies</strong></p><blockquote><p><strong>⚠️ Concept Distinction: Hard Real-Time vs. Soft Real-Time</strong> The <code>SCHED_FIFO</code> and <code>SCHED_RR</code> mentioned here are known as <strong>soft real-time</strong> policies. As a general-purpose operating system (GPOS), Linux is <strong>not</strong> a hard real-time operating system (RTOS) by default. &quot;Hard real-time&quot; means &quot;foolproof, must be done before the deadline,&quot; while &quot;soft real-time&quot; means &quot;as fast as possible, but not life-or-death.&quot; You can patch Linux to turn it into a hard real-time system, but what we usually run is the general-purpose version.</p></blockquote><p><strong>Deep Dive: <code>SCHED_FIFO</code> vs <code>SCHED_RR</code></strong></p><p>Although both are real-time policies, there is a subtle difference that is crucial in practice:</p><ul><li><strong><code>SCHED_FIFO</code></strong>: <strong>No concept of a time slice</strong>. If two <code>FIFO</code> threads have the same priority (say, both are 50), the one that started first will run continuously until it voluntarily blocks or is interrupted by a higher priority. The other thread of the same priority can only watch helplessly. <strong>Consequence</strong>: If you design it poorly, threads at the same priority level might starve.</li><li><strong><code>SCHED_RR</code></strong>: <strong>Has a time slice</strong> (Round Robin). Threads of the same priority take turns; when the time slice is up, it moves to the next one. This is a bit friendlier than <code>FIFO</code>.</li></ul><p><strong>The Supreme Authority of Interrupts</strong></p><p>Here we must insert a &quot;safety brake&quot; mechanism.</p><p>No matter if your thread is at <code>SCHED_FIFO</code> priority 99 (theoretically the highest), <strong>hardware and software interrupts are always higher</strong>. When an interrupt comes, your thread must obediently get out of the way. This is the last line of defense for system stability.</p><p><strong>The Hierarchy of Thread Priorities</strong></p><p>Now let&#39;s zoom out and look at the overall priority ladder in Linux (see Figure 10.3).</p><ol><li><strong>Real-time Priority (1 ~ 99)</strong>: <ul><li>Used by <code>SCHED_FIFO</code> and <code>SCHED_RR</code>.</li><li><strong>Rule</strong>: As long as there is a real-time thread in the run queue, all <code>SCHED_OTHER</code> (normal threads) must step aside. They are not in the same arena.</li></ul></li><li><strong>Normal Priority (Nice value: -20 ~ +19)</strong>: <ul><li>Used by <code>SCHED_OTHER</code>.</li><li>The smaller the Nice value, the higher the priority. 0 is the default.</li><li>This is essentially a &quot;nice guy&quot; system: the more &quot;Nice&quot; (generous) you are, the more you yield the CPU (lower priority).</li></ul></li></ol><blockquote><p><strong>⚠️ Practical Experience: Interference from Autogroups</strong> Many modern distributions have <code>CONFIG_SCHED_AUTOGROUP</code> enabled. This uses cgroups technology to &quot;boost&quot; the priority of processes running under a terminal window. This can make traditional <code>nice</code> values appear to be broken. If you find that nice values aren&#39;t working during testing, check this kernel option.</p></blockquote><p>Now we know what policies are available. Next, we need to see intuitively how these policies actually operate on the CPU.</p><h3 id="_10-1-4-visualizing-the-scheduling-flow" tabindex="-1">10.1.4 Visualizing the Scheduling Flow <a class="header-anchor" href="#_10-1-4-visualizing-the-scheduling-flow" aria-label="Permalink to &quot;10.1.4 Visualizing the Scheduling Flow&quot;">​</a></h3><p>Multi-core systems allow threads to execute in parallel on different CPUs. This improves throughput but also brings debugging nightmares. If several threads are operating on a piece of shared memory at the same time, who goes first? This is hard to see with the naked eye.</p><p>We need a way to see, like watching a replay, which thread occupied which CPU core at which moment in time.</p><p>Here are two tools: one is the simple GUI tool <code>gnome-system-monitor</code>, and the other is the deeply powerful <code>perf</code>.</p><h4 id="method-1-using-gnome-system-monitor" tabindex="-1">Method 1: Using <code>gnome-system-monitor</code> <a class="header-anchor" href="#method-1-using-gnome-system-monitor" aria-label="Permalink to &quot;Method 1: Using \`gnome-system-monitor\`&quot;">​</a></h4><p>This is the system monitor tool that comes with the GNOME desktop environment.</p><p>To see something interesting, we need to generate some &quot;load.&quot; We will run three CPU-intensive processes and manually pin them to different CPU cores.</p><p>Here we&#39;ll use a powerful tool: <code>taskset</code> (part of the util-linux package).</p><p><strong>Experiment Steps</strong></p><p>Assume your system has 6 cores (numbered 0 to 5).</p><ol><li>Run a <code>yes</code> process on CPU 1 (this is an infinite loop that prints &#39;y&#39;, a real CPU killer):<div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">taskset</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> yes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /dev/null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div></li><li>Similarly, run one each on CPU 2 and CPU 3:<div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">taskset</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> yes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /dev/null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">taskset</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> yes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /dev/null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div></li></ol><p>Now open <code>gnome-system-monitor</code> and click the &quot;Resources&quot; tab.</p><p>You will see a screen similar to Figure 10.4: the load on CPUs 2, 3, and 4 (note that GUI display numbering usually starts at 1, so these correspond to cores 1, 2, and 3) is maxed out at 100%. This is intuitive concurrency and parallelism.</p><p>Use <code>pkill yes</code> to end this madness.</p><h4 id="method-2-using-the-perf-tool" tabindex="-1">Method 2: Using the <code>perf</code> Tool <a class="header-anchor" href="#method-2-using-the-perf-tool" aria-label="Permalink to &quot;Method 2: Using the \`perf\` Tool&quot;">​</a></h4><p>If you think <code>gnome-system-monitor</code> is too much of a toy, then <code>perf</code> will absolutely satisfy your inner geek. It is a performance analysis artifact built into the Linux kernel.</p><blockquote><p><strong>⚠️ Prerequisites</strong><code>perf</code> is strongly tied to the kernel version. On Ubuntu, you need to install <code>linux-tools-$(uname -r)</code>. If you compiled a custom-modified kernel (like the 6.1 included with the book), there might not be a corresponding perf package. It&#39;s recommended to use the distribution&#39;s default kernel for this experiment.</p></blockquote><p><strong>1. Command-Line Mode: <code>perf sched map</code></strong></p><p>Let&#39;s use the same experimental scenario. For convenience, the book provides a script, <code>ch10/concurrent_exercise/concurrency</code>, that can launch <code>yes</code> processes on all cores at once.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ch10/concurrent_exercise</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./concurrency</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 6</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # 在 6 个核心上各起一个 yes</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>In another terminal window, start recording scheduling events:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> perf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sched</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> record</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Let it run for ten-plus seconds, then press <code>Ctrl+C</code> to stop. This will generate a binary file named <code>perf.data</code>.</p><p>Now, let&#39;s turn it into a &quot;map&quot; that humans can read:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> perf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sched</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> map</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mymap.txt</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Open <code>mymap.txt</code>, and you&#39;ll see a stream of characters. It looks like gibberish, but look closely and it&#39;s very regular (refer to Figure 10.5).</p><ul><li>Each row represents a point in time.</li><li>Each column represents a CPU core.</li><li>Two-character codes represent threads. For example, <code>A0</code> is the migration thread, and <code>B0</code> is our <code>yes</code> thread.</li></ul><p>The most striking thing is that if you search for <code>B0..D0..F0...</code> using vim, you&#39;ll find these characters appearing <strong>on the same line</strong> simultaneously (as shown in Figure 10.6). What does this prove? It proves they are truly running in parallel!</p><p><strong>2. Graphical Mode: <code>perf timechart</code></strong></p><p>If the character stream makes you dizzy, <code>perf</code> can also generate SVG vector graphics. Open it in a browser, and the visual impact is maxed out.</p><p>The command is as follows:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> perf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> timechart</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./perf.data</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This will generate an <code>output.svg</code> file. Open it with Firefox or Chrome (as shown in Figures 10.7 and 10.8).</p><p>You will see colored bar charts.</p><ul><li><strong>Blue</strong> represents the <code>Running</code> state.</li><li>You will clearly see our <code>yes</code> processes spread out across the CPU cores like the Great Wall, running endlessly.</li><li>Interspersed below are other processes, mostly gray (sleeping state).</li></ul><p>This visualization capability is an absolute lifesaver for analyzing performance bottlenecks (like why my application stutters twice a second).</p><hr><p>With these intuitive feelings and background knowledge, we can now dive deep into the kernel to see how this complex mechanism is actually designed. In the next section, we will deeply analyze <strong>modular scheduling classes</strong>.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: Understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: Understanding&quot;">​</a></h3><p><strong>Question</strong>: In the Linux scheduler&#39;s state machine model, both TASK_UNINTERRUPTIBLE (D state) and TASK_INTERRUPTIBLE (S state) indicate that a process is sleeping. From a system management perspective, analyze: why does Linux need the D state? If a system experiences a large number of processes in the D state that cannot recover, what underlying system problem does this usually imply?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: The D state is used to handle uninterruptible sleep, typically applied when waiting for critical I/O operations (such as disk swap writes), to prevent the process from being interrupted by a signal during data writing, which could lead to data corruption. If a system experiences a large number of unrecoverable processes in the D state, it usually implies a severe blockage in the underlying storage subsystem (such as an unresponsive NFS mount, disk failure, or a &quot;hang&quot; caused by I/O stack congestion).</p><p><strong>Analysis</strong>: This question tests the understanding of the difference between the S and D states in the process state machine.</p><ol><li><strong>Concept Distinction</strong>: The S state allows waking up via signals (like the user pressing Ctrl+C) and is suitable for most waits; the D state ignores signals and must wait for the event it&#39;s waiting for (like hardware I/O completion) to occur.</li><li><strong>Design Intent</strong>: The D state guarantees the atomicity of certain critical operations (like writing disk data structures), preventing the process from being unexpectedly terminated while holding a lock or operating on critical hardware paths.</li><li><strong>Troubleshooting</strong>: The D state is usually brief. If a process is permanently in the D state, it means the resource it is waiting for will never become ready (for example, a server crash causing network I/O to hang). This is known as an &quot;uninterruptible sleep deadlock&quot; and is a fault signal that system administrators must be highly vigilant about.</li></ol></details><h3 id="exercise-2-application" tabindex="-1">Exercise 2: Application <a class="header-anchor" href="#exercise-2-application" aria-label="Permalink to &quot;Exercise 2: Application&quot;">​</a></h3><p><strong>Question</strong>: Suppose you are developing a high-performance computing application, and one of its threads needs to perform intensive mathematical calculations. By default, this thread uses the SCHED_OTHER policy. To ensure this thread gets the maximum CPU time slice and is preempted by other normal processes as little as possible, without using real-time policies (SCHED_FIFO/RR), how should you adjust its Nice value? What is the corresponding command?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: The Nice value should be adjusted to -20 (highest priority). You can use the command <code>nice -n -20 ./your_program</code> to start the program, or use <code>renice -n -20 -p &lt;PID&gt;</code> to modify the Nice value of a running process.</p><p><strong>Analysis</strong>: This question tests the practical application of the Nice value within the SCHED_OTHER policy.</p><ol><li><strong>Policy Constraints</strong>: SCHED_OTHER is the default policy for normal Linux processes, based on the CFS algorithm. Unlike real-time processes (priorities 1-99), it cannot preempt by changing policies.</li><li><strong>Nice Value Range</strong>: -20 to +19. -20 represents the highest priority, and +19 represents the lowest.</li><li><strong>Application Scenario</strong>: For CPU-intensive tasks, setting the Nice value to -20 causes the CFS scheduler to give this task a higher weight (smaller vruntime growth) when calculating the Virtual Runtime (vruntime), making it more likely to be scheduled in the red-black tree and reducing the frequency of being preempted by other lower-priority (higher Nice value) tasks.</li></ol></details><h3 id="exercise-3-application" tabindex="-1">Exercise 3: Application <a class="header-anchor" href="#exercise-3-application" aria-label="Permalink to &quot;Exercise 3: Application&quot;">​</a></h3><p><strong>Question</strong>: In a single-core CPU system, there are two threads:</p><ul><li>Thread A: SCHED_FIFO policy, priority 10, executing an infinite loop (<code>while(1);</code>).</li><li>Thread B: SCHED_FIFO policy, priority 5, trying to calculate 1+1.</li><li>The system also has several Interrupt Service Routines (ISRs) running.</li></ul><p>Question: Will Thread B get a chance to run? Can the Interrupt Service Routines run? Please explain the scheduler&#39;s behavior in detail.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Thread B will not get a chance to run (until Thread A voluntarily yields the CPU or exits). The Interrupt Service Routines (ISRs) can run normally and preempt Thread A.</p><p><strong>Analysis</strong>: This question tests the understanding of the SCHED_FIFO preemption model and interrupt priorities.</p><ol><li><strong>Inter-Thread Scheduling</strong>: SCHED_FIFO is a real-time preemption policy. Thread A (Prio 10) has a higher priority than Thread B (Prio 5). According to the rules, a lower-priority task (B) can only run when the higher-priority task (A) blocks (I/O), exits, or voluntarily gives up the CPU. Since A is an infinite loop with no blocking, B will starve permanently.</li><li><strong>Interrupt vs. Process Priority</strong>: Interrupts in the Linux kernel (hardware and softirqs) have a higher priority than all processes (including SCHED_FIFO at priority 99). Therefore, even though A occupies the CPU, when a hardware interrupt occurs (like a clock interrupt or a network packet arriving), the CPU will still pause Thread A and switch to executing the ISR. Only after the ISR finishes executing will Thread A&#39;s execution resume.</li><li><strong>Conclusion</strong>: B starves, and interrupts are unaffected. This is why you must be careful when writing SCHED_FIFO programs to avoid infinite loops that cause a soft system hang (if left unattended).</li></ol></details><h3 id="exercise-4-thinking" tabindex="-1">Exercise 4: Thinking <a class="header-anchor" href="#exercise-4-thinking" aria-label="Permalink to &quot;Exercise 4: Thinking&quot;">​</a></h3><p><strong>Question</strong>: In the Linux kernel source code, there is a scheduler design principle known as the &quot;golden rule&quot;: <strong>&quot;Scheduler code must never run in an atomic context (including interrupt context).&quot;</strong> Think about this: if this principle is broken and <code>schedule()</code> is allowed to be called to perform a process switch while holding a spinlock or in an interrupt context, what catastrophic consequences would occur? Please provide a deep analysis combining interrupt handling and locking mechanisms.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: If this principle is broken, it would lead to &quot;double-acquiring a lock&quot; or deadlocks, potentially causing the system to deadlock completely or the kernel to crash. The core reason is that <code>schedule()</code> triggers a <code>switch_to</code>, switching to another process that might also be trying to acquire the same spinlock, or triggering another interrupt on the same CPU.</p><p><strong>Analysis</strong>: This question tests a comprehensive understanding of deep kernel scheduling principles and concurrency control.</p><ol><li><strong>Definition of Atomic Context</strong>: Atomic context means the kernel is executing critical section code (holding a spinlock <code>spinlock</code>) or is handling an interrupt. At this point, it cannot sleep, and process scheduling cannot occur.</li><li><strong>Deadlock Scenario Analysis (Spinlock Perspective)</strong>: <ul><li>Suppose process P1 on CPU0 holds spinlock L (preemption is disabled at this point) and enters the critical section.</li><li>If calling <code>schedule()</code> were allowed while holding the lock, the scheduler would move P1 out of the run queue and swap in process P2.</li><li>If P2 (running on CPU0) also tries to acquire lock L, it will spin-wait for P1 to release the lock.</li><li>However, P1 can only release the lock when it is scheduled back to CPU0 and executes.</li><li><strong>Result</strong>: P2 waits for P1, and P1 waits for P2 (scheduler resources), forming a deadlock. The system will become unresponsive.</li></ul></li><li><strong>Interrupt Context Analysis</strong>: <ul><li>Interrupt handlers (ISRs) do not belong to any process; they &quot;borrow&quot; the current process&#39;s stack and resources to run.</li><li>If <code>schedule()</code> is called inside an ISR, the kernel will attempt to save the &quot;interrupt context&quot; and restore a &quot;process context.&quot; This is not only logically chaotic (who is sleeping in the interrupt?), but if the interrupt occurs again (recursive interrupt), the kernel stack could overflow or the state could become completely corrupted.</li></ul></li><li><strong>Design Philosophy</strong>: Therefore, Linux strictly requires that all function paths that might cause sleep (and consequently call the scheduler) must be in a non-atomic context. This is the most fundamental baseline for guaranteeing kernel concurrency safety.</li></ol></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>Linux&#39;s Kernel Schedulable Entity (KSE) is the thread, not the process. The scheduler operates at the granularity of threads, and different threads within the same process are completely independent individuals in the scheduler&#39;s eyes, with no &quot;brotherly loyalty&quot; or inherent synchronization relationship.</p><p>The key to understanding thread state transitions lies in distinguishing <code>TASK_RUNNING</code> from actual execution. The <code>R</code> state merely indicates that the thread is in the run queue (currently executing or ready to execute), whereas a non-<code>R</code> state (like <code>S</code> or <code>D</code>) means the thread has given up the CPU and entered a wait queue. A wake-up operation simply puts it back into the run queue and does not mean it immediately gains the CPU.</p><p>Linux strictly divides thread priorities into real-time (1-99) and normal categories. As long as there is a real-time thread in the run queue, all normal threads must step aside regardless of their Nice value. Furthermore, even the highest-priority real-time thread must yield to the absolute authority of hardware and software interrupts.</p><p>Although both <code>SCHED_FIFO</code> and <code>SCHED_RR</code> are soft real-time policies, the former, once it occupies the CPU, will run continuously as long as it doesn&#39;t block or get preempted by a higher priority, easily causing same-priority threads to starve. The latter introduces a time slice mechanism, guaranteeing fair round-robin among threads of the same priority.</p><p>Using tools like <code>perf sched map</code> and <code>perf timechart</code>, we can visualize CPU runtime data into character maps or SVG graphs. This not only intuitively verifies multi-core parallelism (different cores running threads simultaneously) but also serves as a &quot;scalpel&quot; for diagnosing complex scheduling scenarios and performance bottlenecks.</p>`,124)])])}const g=t(a,[["render",o]]);export{p as __pageData,g as default};
