import{_ as t,o as s,c as a,a as i}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"14.15  Macros and Utility Functions","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch14_15.md","filePath":"en/notes/linux_kernel_networking/ch14_15.md","lastUpdated":null}'),o={name:"en/notes/linux_kernel_networking/ch14_15.md"};function n(r,e,l,d,h,c){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="_14-15-macros-and-utility-functions" tabindex="-1">14.15 Macros and Utility Functions <a class="header-anchor" href="#_14-15-macros-and-utility-functions" aria-label="Permalink to &quot;14.15  Macros and Utility Functions&quot;">​</a></h1><hr><p>Although this section is titled &quot;Macros,&quot; there is really only one core question: <strong>how do packets flow through the kernel?</strong></p><p>As a network driver developer, when you write a PCI driver registration macro, you might wonder: how does this line of code actually hook into the kernel&#39;s massive device model? More importantly, when a packet arrives at the kernel from the hardware like a ghost, what does it become?</p><p>Furthermore, as the grand finale of this final chapter, the upcoming content will take you deep into the <strong>cornerstones</strong> of the Linux kernel network stack: <code>sk_buff</code> and <code>net_device</code>.</p><p>Before we proceed, I must be honest with you: the following content is extremely dense. If the previous chapters felt like &quot;telling a story,&quot; then Appendix A is &quot;tearing down an engine.&quot; We will dissect the two most core data structures in the kernel—Socket Buffer (SKB) and network devices—examining every field, every pointer, and every bit.</p><p>This isn&#39;t for you to memorize, but rather to ensure that when debugging crash logs, you can read the kernel&#39;s pathways as clearly as a map.</p><hr><h2 id="_15-1-registration-just-the-beginning" tabindex="-1">15.1 Registration: Just the Beginning <a class="header-anchor" href="#_15-1-registration-just-the-beginning" aria-label="Permalink to &quot;15.1  Registration: Just the Beginning&quot;">​</a></h2><p>When writing a PCI network driver, the first thing you do is usually not initializing the hardware, but <strong>reporting to the kernel</strong>.</p><p>You can think of this macro as an <strong>&quot;envelope&quot;</strong> with &quot;I handle devices with this ID&quot; written on it.</p><p>But there is one flaw in this &quot;envelope&quot; analogy: a real envelope is done once mailed, whereas the registration macro is just the beginning of the story. When the kernel finds a matching device on the PCI bus, it wakes up your driver, but packet flow doesn&#39;t depend on the driver itself—it depends on the <code>net_device</code> structure the kernel carefully prepared for this device, and the <code>sk_buff</code> that carries its soul.</p><p>Returning to the &quot;envelope&quot;: you should now see that the registration macro simply drops the envelope in the mailbox. The truly long journey begins only after the kernel takes it. Next, we will dive into the kernel to see exactly how it handles this letter.</p><hr><h2 id="appendix-a-deep-dive-into-kernel-data-structures" tabindex="-1">Appendix A: Deep Dive into Kernel Data Structures <a class="header-anchor" href="#appendix-a-deep-dive-into-kernel-data-structures" aria-label="Permalink to &quot;Appendix A: Deep Dive into Kernel Data Structures&quot;">​</a></h2><p>The following content is the <strong>&quot;hardcore appendix&quot;</strong> of the entire book. If you truly want to understand how the Linux network stack works, rather than just staying at the level of tweaking parameters, this is the necessary path.</p><p>It covers the two most core data structures in the Linux kernel network stack: <strong><code>sk_buff</code></strong> (Socket Buffer) and <strong><code>net_device</code></strong>.</p><p>To keep you from getting lost in an ocean of 27 fields, we won&#39;t read through them like a manual from start to finish. Instead, we will dissect <code>sk_buff</code> along the <strong>flow path</strong> of a packet through the kernel—from allocation, to pointer movement, to final freeing. We will see which fields control the packet&#39;s life, which fields optimize performance, and which fields are designed specifically to &quot;bypass copies.&quot;</p><p>Finally, we will briefly look at <code>net_device</code> and RDMA, which are the bridges connecting the physical world to the kernel&#39;s virtual interfaces.</p><hr><h3 id="a-1-sk-buff-the-soul-of-a-packet" tabindex="-1">A.1 <code>sk_buff</code>: The Soul of a Packet <a class="header-anchor" href="#a-1-sk-buff-the-soul-of-a-packet" aria-label="Permalink to &quot;A.1 \`sk_buff\`: The Soul of a Packet&quot;">​</a></h3><p>The <code>sk_buff</code> structure represents a <strong>network packet</strong>. SKB stands for Socket Buffer.</p><p>Imagine a packet that might be generated by a local user-space socket (via an HTTP request), or received from a physical NIC. It might be destined for the outside, or for another socket on the local machine. For the kernel, regardless of its source and destination, it will ultimately be wrapped in a <code>sk_buff</code> structure and passed between layers of the network stack.</p><p>You can think of <code>sk_buff</code> as an <strong>&quot;envelope&quot;</strong>.</p><ul><li>The <strong>back of the envelope</strong> says who sent it (<code>sk</code>), who it&#39;s for (<code>dev</code>), and which route to take (<code>dst</code>).</li><li><strong>Inside the envelope</strong> is the letter&#39;s content (the data pointed to by the <code>data</code> pointer).</li></ul><p>But there is one flaw in this &quot;envelope&quot; analogy: a real envelope, once sealed, cannot be changed, whereas the data area of <code>sk_buff</code> is <strong>dynamically resizable</strong>. Each layer of the protocol stack (TCP, IP, Ethernet) stuffs its own &quot;padding&quot; (protocol headers) into the envelope, or tears off the padding from the layer above. The most ingenious design of <code>sk_buff</code> is using pointer movement to achieve this &quot;stuffing&quot; and &quot;tearing&quot; process, rather than actually moving data around.</p><p>Returning to the &quot;envelope&quot;: you should now see that the <code>data</code> pointer is the top line of the currently visible page inside the envelope, while <code>head</code> and <code>end</code> are the physical boundaries of that page. If <code>head</code> reaches the position of <code>data</code>, it means there is no space to write a new address (Headroom is exhausted), and the kernel may have to find a larger piece of paper (copy the data), which is a performance killer.</p><h4 id="_1-pointer-management-tetris" tabindex="-1">1. Pointer Management: Tetris <a class="header-anchor" href="#_1-pointer-management-tetris" aria-label="Permalink to &quot;1. Pointer Management: Tetris&quot;">​</a></h4><p>This is the most ingenious and error-prone part of the SKB. The four pointers <code>head</code>, <code>data</code>, <code>tail</code>, and <code>end</code> define the layout of the packet in memory.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> transport_header;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // L4 头部位置</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> network_header;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // L3 头部位置</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mac_header;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       // L2 头部位置</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> tail;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">             // 数据的尾部</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> end;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">              // 缓冲区的尾部</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">head;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">             // 缓冲区的起始</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">data;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">             // 数据的起始</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p><strong>Core Concepts: Linear Data Area vs. Non-linear Data Area</strong></p><ul><li><strong>Linear Data Area</strong>: The area from <code>head</code> to <code>end</code> is the entire linear buffer allocated for the SKB.</li><li><strong>Valid Data</strong>: The area from <code>data</code> to <code>tail</code> is the current valid payload.</li><li><strong>Protocol Headers</strong>: <code>mac_header</code>, <code>network_header</code>, and <code>transport_header</code> are pointers to specific positions within the linear buffer, pointing to the start of the L2, L3, and L4 layers respectively.</li></ul><p><strong>Operation Macros (modifying <code>data</code> and <code>tail</code> pointers):</strong></p><ul><li><strong><code>skb_put(skb, len)</code></strong>: <strong>Add</strong> data at the tail. The <code>tail</code> pointer moves down, and <code>len</code> increases. Used for receiving data or building protocol headers.</li><li><strong><code>skb_push(skb, len)</code></strong>: <strong>Add</strong> data at the head. The <code>data</code> pointer moves up, and <code>len</code> increases. Used for adding protocol headers (e.g., the IP layer calls this function before sending to add an IP header).</li><li><strong><code>skb_pull(skb, len)</code></strong>: <strong>Remove</strong> data from the head. The <code>data</code> pointer moves down, and <code>len</code> decreases. Used for stripping protocol headers (e.g., after the IP layer finishes processing, it moves the <code>data</code> pointer past the IP header and hands it to the TCP layer).</li><li><strong><code>skb_reserve(skb, len)</code></strong>: Reserve head space. Both the <code>data</code> and <code>tail</code> pointers move down simultaneously. Usually called immediately after allocating an SKB to leave enough space in front for lower-layer protocols to add headers.</li></ul><p><strong>Headroom and Tailroom</strong></p><ul><li><strong>Headroom</strong>: <code>data</code> minus <code>head</code>. This is the reserved head space.</li><li><strong>Tailroom</strong>: <code>end</code> minus <code>tail</code>. This is the reserved tail space.</li></ul><p>This design allows the kernel to efficiently pass packets between protocol layers <strong>without copying data</strong>, simply by moving pointers.</p><h4 id="_2-mechanism-breakdown-a-packet-s-journey" tabindex="-1">2. Mechanism Breakdown: A Packet&#39;s Journey <a class="header-anchor" href="#_2-mechanism-breakdown-a-packet-s-journey" aria-label="Permalink to &quot;2. Mechanism Breakdown: A Packet&#39;s Journey&quot;">​</a></h4><p>Looking at pointer definitions alone can be dizzying. Let&#39;s walk through what actually happens inside an SKB as a packet goes from the NIC driver (L2) to TCP (L4).</p><p><strong>Scenario: A host receives a TCP packet</strong></p><ol><li><p><strong>L2 Entry (Driver Receive)</strong></p><ul><li>The driver allocates an SKB, typically using <code>netdev_alloc_skb()</code>. This step usually also calls <code>skb_reserve()</code> to reserve head space.</li><li>The driver DMAs the data to the <code>data</code> position.</li><li>At this point, <code>data</code> points to the <strong>Ethernet header</strong>.</li></ul></li><li><p><strong>L2 -&gt; L3 (Network Layer)</strong></p><ul><li>The kernel calls <code>eth_type_trans()</code>. This function does two things: <ul><li>Sets <code>skb-&gt;protocol</code> to <code>ETH_P_IP</code> (IPv4).</li><li>Adjusts <code>skb-&gt;mac_header</code> to point to the L2 header.</li><li>Calls <code>skb_pull(skb, hlen)</code> to strip the Ethernet header.</li></ul></li><li><strong>Key point</strong>: At this point, the <code>data</code> pointer skips past the Ethernet header and points directly to the <strong>IP header</strong>.</li><li>This is why the TCP layer doesn&#39;t need to handle the Ethernet header—because before delivery, the kernel already threw away the &quot;garbage&quot; by moving pointers.</li></ul></li><li><p><strong>L3 -&gt; L4 (Transport Layer)</strong></p><ul><li>After the IP layer finishes processing (checksum verification, routing, etc.), it calls <code>ip_rcv()</code>.</li><li>Similarly, it calls <code>skb_pull()</code> to strip the IP header.</li><li>At this point, the <code>data</code> pointer points to the <strong>TCP header</strong>.</li><li>Finally, the SKB is placed into the socket&#39;s receive queue.</li></ul></li></ol><p><strong>Why go through all this trouble?</strong> For efficiency. The kernel doesn&#39;t need to copy data; it only needs to move the <code>data</code> pointer to change what the current layer &quot;sees.&quot; This is also why the SKB structure is so complex—it must precisely record the position of each layer&#39;s protocol header so it can instantly find the IP header when needed (e.g., when calculating the TCP checksum).</p><h4 id="_3-routing-and-devices-the-address-on-the-envelope" tabindex="-1">3. Routing and Devices: The Address on the Envelope <a class="header-anchor" href="#_3-routing-and-devices-the-address-on-the-envelope" aria-label="Permalink to &quot;3. Routing and Devices: The Address on the Envelope&quot;">​</a></h4><p>When the kernel gets this SKB, it first needs to know: where did it come from? Where is it going?</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dev;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // 关联的网卡</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> dst_entry </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">_skb_refdst;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 路由缓存</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><ul><li><strong><code>dev</code></strong>: This &quot;NIC&quot; is the data entry and exit point. For a received packet, it represents the incoming interface; for a sent packet, it represents the outgoing interface.</li><li><strong><code>_skb_refdst</code></strong>: This is the kernel&#39;s <strong>routing decision result</strong>. After the kernel looks up the routing table once, it caches the result (next hop, output interface) here. This way, the next time this packet is processed (e.g., forwarding), it doesn&#39;t need to look up the table again. The lower bits of this field are borrowed for reference count marking, a classic kernel trick to save memory.</li></ul><h4 id="_4-protocol-private-control-area-cb" tabindex="-1">4. Protocol Private Control Area (<code>cb[]</code>) <a class="header-anchor" href="#_4-protocol-private-control-area-cb" aria-label="Permalink to &quot;4. Protocol Private Control Area (\`cb[]\`)&quot;">​</a></h4><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">char</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> cb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">48</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This is a <strong>&quot;Control Buffer.&quot;</strong> It is a notepad left by the kernel for each protocol layer to store private information.</p><ul><li><strong>Why it exists</strong>: For efficiency. The kernel doesn&#39;t want to define separate SKB variants for each protocol, so it reserves this space.</li><li><strong>Who uses it</strong>: <ul><li><strong>TCP protocol</strong>: Uses the macro <code>TCP_SKB_CB(__skb)</code> to cast it into TCP&#39;s control structure <code>tcp_skb_cb</code>, used to store TCP sequence numbers, acknowledgment numbers, etc.</li><li><strong>Bluetooth protocol</strong>: Uses the macro <code>bt_cb(skb)</code> to cast it into Bluetooth&#39;s control structure.</li></ul></li><li><strong>Note</strong>: This area is opaque; once written by one layer, the next layer may overwrite or reinterpret it.</li></ul><h4 id="_5-data-length-management" tabindex="-1">5. Data Length Management <a class="header-anchor" href="#_5-data-length-management" aria-label="Permalink to &quot;5. Data Length Management&quot;">​</a></h4><p>The length fields in an SKB can be confusing and need to be clearly distinguished:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> len;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           // 数据包的总字节数</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> data_len;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // 非线性数据的长度（即分页数据）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">__u16 mac_len;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">              // MAC 头部（L2）的长度</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">__wsum csum;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                // 校验和</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><ul><li><strong><code>len</code></strong>: The length of the entire packet (including both linear data and non-linear paged data).</li><li><strong><code>data_len</code></strong>: If the packet uses scatter/gather, part of the data is stored in separate memory pages, and <code>data_len</code> records the size of this portion. If <code>data_len</code> is 0, it means all data is stored linearly and contiguously.</li><li><strong><code>mac_len</code></strong>: The length of the link-layer header (e.g., an Ethernet header is typically 14 bytes).</li></ul><h4 id="_6-checksums-and-hardware-offload" tabindex="-1">6. Checksums and Hardware Offload <a class="header-anchor" href="#_6-checksums-and-hardware-offload" aria-label="Permalink to &quot;6. Checksums and Hardware Offload&quot;">​</a></h4><p>Modern NICs are smart; they can calculate checksums for you, and even fragment packets for you.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">__u8 ip_summed:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><ul><li><strong><code>CHECKSUM_NONE</code></strong>: Hardware doesn&#39;t support checksums; software must do it.</li><li><strong><code>CHECKSUM_UNNECESSARY</code></strong>: No checksum needed (e.g., loopback devices).</li><li><strong><code>CHECKSUM_COMPLETE</code></strong>: Hardware has completed checksum calculation (receive path).</li><li><strong><code>CHECKSUM_PARTIAL</code></strong>: Hardware will complete checksum calculation (send path, header only).</li></ul><h4 id="_7-fragmentation-and-cloning-cloned-users" tabindex="-1">7. Fragmentation and Cloning (<code>cloned</code>, <code>users</code>) <a class="header-anchor" href="#_7-fragmentation-and-cloning-cloned-users" aria-label="Permalink to &quot;7. Fragmentation and Cloning (\`cloned\`, \`users\`)&quot;">​</a></h4><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">atomic_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> users;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">__u8 cloned:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><ul><li><strong><code>users</code></strong>: The SKB&#39;s reference count. Initialized to 1.</li><li><strong><code>cloned</code></strong>: Marks whether this SKB is a <strong>clone</strong>.</li><li><strong>Mechanism</strong>: When the kernel only needs to modify the SKB&#39;s metadata (like the <code>dev</code> pointer) without modifying the data content, to avoid expensive memory copies, it clones the SKB structure itself but shares the underlying data block. At this point, both the original SKB and the cloned SKB will have their <code>cloned</code> flag set to 1.</li></ul><h4 id="_8-non-linear-data-skb-shared-info" tabindex="-1">8. Non-linear Data: <code>skb_shared_info</code> <a class="header-anchor" href="#_8-non-linear-data-skb-shared-info" aria-label="Permalink to &quot;8. Non-linear Data: \`skb_shared_info\`&quot;">​</a></h4><p>When the data volume is large (exceeding one page, typically 4KB), the kernel stores the excess data in scattered memory pages instead of squeezing it into the linear data area. Information about these scattered pages is stored in the <code>skb_shared_info</code> structure.</p><p>It is located at the <strong>very end</strong> of the linear data area (accessed via <code>skb_end_pointer()</code>).</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> skb_shared_info {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> nr_frags;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            // 分散页的数量</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ... 其他字段 ...</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    skb_frag_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> frags</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[MAX_SKB_FRAGS];</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 分散页数组</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><ul><li><strong><code>nr_frags</code></strong>: How many scatter pages are currently in use.</li><li><strong><code>frags[]</code></strong>: Stores the physical address, offset, and length of each page.</li><li><strong><code>frag_list</code></strong>: Another form of non-linear data, directly hanging an SKB list (used for IP fragment reassembly).</li></ul><hr><h3 id="a-2-net-device-the-general-garrisoning-the-fortress" tabindex="-1">A.2 <code>net_device</code>: The General Garrisoning the Fortress <a class="header-anchor" href="#a-2-net-device-the-general-garrisoning-the-fortress" aria-label="Permalink to &quot;A.2 \`net_device\`: The General Garrisoning the Fortress&quot;">​</a></h3><p>If <code>sk_buff</code> is the flowing &quot;soldiers,&quot; then <code>net_device</code> is the &quot;general&quot; garrisoning the fortress.</p><p>The <code>net_device</code> structure represents a network interface device. It can be a physical device (like <code>eth0</code>) or a virtual device (like <code>bridge0</code> or <code>vlan100</code>).</p><h4 id="_1-operation-set-the-soul-of-the-device" tabindex="-1">1. Operation Set: The Soul of the Device <a class="header-anchor" href="#_1-operation-set-the-soul-of-the-device" aria-label="Permalink to &quot;1. Operation Set: The Soul of the Device&quot;">​</a></h4><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device_ops </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">netdev_ops;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This is the <strong>soul</strong> of <code>net_device</code>. It is a set of function pointers that define how the kernel commands this device. When you execute <code>ip link set eth0 up</code> in user space, the kernel ultimately calls <code>ndo_open()</code> here.</p><ul><li><strong><code>ndo_open()</code></strong>: Start the device.</li><li><strong><code>ndo_stop()</code></strong>: Stop the device.</li><li><strong><code>ndo_start_xmit()</code></strong>: The <strong>most important</strong> function. The kernel network stack calls it to send packets. The driver must write the packet to the hardware&#39;s transmit queue.</li><li><strong><code>ndo_set_mac_address()</code></strong>: Change the MAC address.</li><li><strong><code>ndo_tx_timeout()</code></strong>: Watchdog callback when transmission stalls.</li></ul><h4 id="_2-hardware-features-and-offloads" tabindex="-1">2. Hardware Features and Offloads <a class="header-anchor" href="#_2-hardware-features-and-offloads" aria-label="Permalink to &quot;2. Hardware Features and Offloads&quot;">​</a></h4><p>By telling the kernel which hardware acceleration features it supports, the driver allows the kernel to decide whether to offload heavy tasks to the hardware.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">netdev_features_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> features;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       // 当前激活的特性</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">netdev_features_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hw_features;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 硬件支持的特性</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><ul><li><strong><code>NETIF_F_IP_CSUM</code></strong>: Hardware supports IPv4 checksum calculation.</li><li><strong><code>NETIF_F_TSO</code></strong>: TCP Segmentation Offload. The hardware is responsible for splitting large TCP segments into small packets that fit the MTU.</li><li><strong><code>NETIF_F_GRO</code></strong>: Generic Receive Offload. The hardware or kernel merges multiple small packets into a large one upon reception, reducing CPU interrupts.</li></ul><h4 id="_3-state-and-queues" tabindex="-1">3. State and Queues <a class="header-anchor" href="#_3-state-and-queues" aria-label="Permalink to &quot;3. State and Queues&quot;">​</a></h4><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> state;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> netdev_queue </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">_tx;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><ul><li><strong><code>state</code></strong>: Records the link state (e.g., <code>__LINK_STATE_START</code> up, <code>__LINK_STATE_NOCARRIER</code> cable unplugged).</li><li><strong><code>_tx</code></strong>: Transmit queue array. Modern high-performance NICs typically have multiple queues to leverage the parallel processing capabilities of multi-core CPUs.</li></ul><hr><h3 id="a-3-rdma-remote-dma" tabindex="-1">A.3 RDMA (Remote DMA) <a class="header-anchor" href="#a-3-rdma-remote-dma" aria-label="Permalink to &quot;A.3 RDMA (Remote DMA)&quot;">​</a></h3><p>RDMA is a high-performance networking technology that allows one computer to directly access another computer&#39;s memory <strong>without the involvement of the remote CPU</strong>. This drastically reduces latency and increases throughput.</p><p>You can think of RDMA as an <strong>&quot;express delivery service.&quot;</strong> Normal network communication is like sending a letter, requiring a post office (the remote CPU) to sort it; RDMA is like driving your truck straight into the other party&#39;s warehouse to move goods.</p><p>But this &quot;express delivery&quot; has a prerequisite: <strong>security</strong>. You can&#39;t just let anyone into your warehouse.</p><h4 id="_1-protection-domain-pd-the-security-perimeter" tabindex="-1">1. Protection Domain (PD) - The Security Perimeter <a class="header-anchor" href="#_1-protection-domain-pd-the-security-perimeter" aria-label="Permalink to &quot;1. Protection Domain (PD) - The Security Perimeter&quot;">​</a></h4><p>A PD is like a <strong>private club</strong>. All resources (memory regions, queue pairs) must belong to the same club (PD) to access each other. If you don&#39;t belong to this club, you won&#39;t be let in even if you have the address.</p><h4 id="_2-memory-region-mr-warehouse-registration" tabindex="-1">2. Memory Region (MR) - Warehouse Registration <a class="header-anchor" href="#_2-memory-region-mr-warehouse-registration" aria-label="Permalink to &quot;2. Memory Region (MR) - Warehouse Registration&quot;">​</a></h4><p>In RDMA, you can&#39;t just take a memory address and write to a remote machine. You must first <strong>register</strong> that memory.</p><ul><li><strong>Registration process</strong>: <ol><li><strong>Pinning</strong>: Prevents the memory from being swapped out to disk.</li><li><strong>Translation</strong>: Translates virtual addresses to physical addresses and informs the NIC&#39;s DMA engine.</li></ol></li><li><strong>Keys</strong>: After registration, you get two keys: <ul><li><strong>LKey (Local Key)</strong>: Used for local access.</li><li><strong>RKey (Remote Key)</strong>: The credential that the remote machine must provide to access this memory.</li></ul></li></ul><p>Returning to the &quot;delivery&quot; analogy: an MR is like the cargo manifest you registered with the warehouse (memory). Only registered cargo, and couriers (NICs) holding the corresponding RKey, can move that cargo. If the other party presents the wrong key or attempts to move unregistered cargo, the RDMA hardware will directly reject the access, protecting your memory safety.</p><h4 id="_3-queue-pair-qp-the-communication-channel" tabindex="-1">3. Queue Pair (QP) - The Communication Channel <a class="header-anchor" href="#_3-queue-pair-qp-the-communication-channel" aria-label="Permalink to &quot;3. Queue Pair (QP) - The Communication Channel&quot;">​</a></h4><p>This is the <strong>core channel</strong> of RDMA communication. A QP contains two queues:</p><ul><li><strong>Send Queue (SQ)</strong>: Submits send work requests.</li><li><strong>Receive Queue (RQ)</strong>: Submits receive work requests.</li></ul><p><strong>QP Types</strong>:</p><ul><li><strong>RC (Reliable Connected)</strong>: Reliable connection, similar to TCP.</li><li><strong>UD (Unreliable Datagram)</strong>: Unreliable datagram, similar to UDP.</li></ul><h4 id="_4-completion-queue-cq-the-receipt" tabindex="-1">4. Completion Queue (CQ) - The Receipt <a class="header-anchor" href="#_4-completion-queue-cq-the-receipt" aria-label="Permalink to &quot;4. Completion Queue (CQ) - The Receipt&quot;">​</a></h4><p>RDMA operations are <strong>asynchronous</strong>. When you initiate a send request, the function returns immediately. How do you know the send is complete? Through the CQ. The hardware writes completion status to the CQ, which you can poll or wait for notifications on.</p><hr><h2 id="chapter-echoes" tabindex="-1">Chapter Echoes <a class="header-anchor" href="#chapter-echoes" aria-label="Permalink to &quot;Chapter Echoes&quot;">​</a></h2><p>What this chapter is really doing is establishing the foundational understanding of the <strong>packet perspective</strong>.</p><p>On the surface, we are looking at individual structure fields, but in reality, we are understanding how the kernel balances the &quot;cost of copying&quot; against the &quot;complexity of pointers.&quot; The reason the <code>data</code> pointer in <code>sk_buff</code> jumps around like this is fundamentally to avoid data copies—because copying memory is the number one killer of network performance.</p><p>Remember the question from the beginning—<strong>how do you understand a packet flowing through the network stack?</strong> You should now be able to answer: it is not a static block of data, but a &quot;perspective&quot; that constantly changes between different protocol layers. L2 sees a frame, L3 sees a packet, L4 sees a stream, and the kernel, by cleverly moving pointers within <code>sk_buff</code>, allows all layers to efficiently process data in the same physical memory. That registration macro is merely the admission ticket to all of this; the real performance plays out inside these structures.</p><p>In the next chapter (if there is one), we will bring this knowledge back to reality to solve those maddening bugs that occur in the real world. Good luck.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><p>By this point, the mechanisms should be clear—or so you think. The following questions increase in difficulty. I recommend thinking independently before looking at the hints; only check them if you get stuck. If you can solve the third question, it means you truly understand.</p><h3 id="exercise-15-1-simulating-protocol-encapsulation-pointer-drill-⭐-understanding" tabindex="-1">Exercise 15.1 Simulating Protocol Encapsulation (Pointer Drill) ⭐ (Understanding) <a class="header-anchor" href="#exercise-15-1-simulating-protocol-encapsulation-pointer-drill-⭐-understanding" aria-label="Permalink to &quot;Exercise 15.1  Simulating Protocol Encapsulation (Pointer Drill) ⭐ (Understanding)&quot;">​</a></h3><p>Suppose you have just allocated an empty SKB in the kernel, where the <code>data</code> and <code>tail</code> pointers coincide, and you have already reserved 64 bytes of Headroom. Now you need to build a TCP packet in order (without filling in data for now, only considering header reservation):</p><ol><li>Call <code>skb_push(skb, 20)</code> to reserve the <strong>IP header</strong>.</li><li>Call <code>skb_push(skb, 20)</code> to reserve the <strong>TCP header</strong>.</li></ol><p><strong>Question</strong>: After these two operations, how many bytes has the <code>data</code> pointer moved relative to its initial position? Which layer&#39;s protocol header does the <code>data</code> pointer currently point to?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: It moved <strong>40 bytes</strong> (20+20). The <code>data</code> pointer currently points to the start of the <strong>TCP header</strong> (L4).</p><p><strong>Analysis</strong>: The purpose of <code>skb_push</code> is to move the <code>data</code> pointer toward lower addresses (expanding forward), freeing up space at the head. First, push 20 bytes (IP header), and the <code>data</code> pointer moves up by 20 bytes. Then, push 20 bytes (TCP header), and the <code>data</code> pointer continues moving up by 20 bytes. Total movement: 40 bytes. When building a transmit packet, we usually push headers in reverse order from L4 to L2, so at this point <code>data</code> points to the outermost (and first-built) TCP header. When actually transmitting, the driver will push the Ethernet header again.</p></details><h3 id="exercise-15-2-headroom-exhaustion-⭐⭐-application" tabindex="-1">Exercise 15.2 Headroom Exhaustion ⭐⭐ (Application) <a class="header-anchor" href="#exercise-15-2-headroom-exhaustion-⭐⭐-application" aria-label="Permalink to &quot;Exercise 15.2  Headroom Exhaustion ⭐⭐ (Application)&quot;">​</a></h3><p>When building a packet, if there are too many protocol headers or insufficient reserved space, the <code>data</code> pointer might collide with the <code>head</code> pointer (i.e., Headroom is exhausted).</p><p><strong>Question</strong>: When the kernel detects insufficient Headroom (e.g., when <code>skb_push</code> finds that <code>data</code> is about to be less than <code>head</code>), what happens? Is this a simple or expensive operation? Why?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: The kernel will call the <code>pskb_expand_head()</code> function. This is an <strong>extremely expensive</strong> operation. Because the kernel must allocate a <strong>larger</strong> new memory block, copy all existing data (including linear data and fragmented data) over, and then free the old SKB. This involves memory allocation and massive memory copy operations, which severely degrades network performance.</p></details><h3 id="exercise-15-3-skb-and-device-lifecycle-⭐⭐⭐-reflection" tabindex="-1">Exercise 15.3 SKB and Device Lifecycle ⭐⭐⭐ (Reflection) <a class="header-anchor" href="#exercise-15-3-skb-and-device-lifecycle-⭐⭐⭐-reflection" aria-label="Permalink to &quot;Exercise 15.3  SKB and Device Lifecycle ⭐⭐⭐ (Reflection)&quot;">​</a></h3><p>Consider Netfilter&#39;s <code>NF_QUEUE</code> mechanism: when the kernel sends a packet to a user-space program (like Suricata) via <code>NFQUEUE</code> rules for processing, the kernel must hold onto this SKB until user-space processing is complete and the packet is reinjected.</p><p><strong>Questions</strong>:</p><ol><li>Why does the kernel prefer to <strong>clone</strong> the SKB (<code>skb_clone()</code>) in this case, rather than directly referencing or copying it?</li><li>What changes occur in the <code>users</code> and <code>cloned</code> fields between the cloned SKB and the original SKB?</li><li>If the user-space program modifies the packet&#39;s content (e.g., rewrites the IP address), how does the kernel handle this &quot;copy-on-write&quot; requirement?</li></ol><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>:</p><ol><li><strong>Reason</strong>: Direct referencing would cause race conditions (if the driver is still transmitting while user space reads simultaneously, it would crash); a full copy (<code>skb_copy()</code>) is too expensive (requires copying all data and pages). <strong>Cloning</strong> is the compromise: it only copies the <code>sk_buff</code> structure itself (metadata) while <strong>sharing</strong> the same data buffer. This way, user space and the kernel can each have independent metadata pointers but share the data, greatly improving performance.</li><li><strong>Field changes</strong>: <ul><li>The <code>cloned</code> flag of both the new and old SKBs will be set to <strong>1</strong>.</li><li>The <code>users</code> reference count of the original SKB will increase (because two structures point to the same data).</li></ul></li><li><strong>Copy-on-write</strong>: If the user-space program attempts to modify the data content (e.g., via <code>skb_store_bits()</code> or by modifying the packet body through the queue), the kernel will detect that the data pages are shared. At this point, it triggers the &quot;copy-on-write&quot; mechanism, automatically copying the affected data pages to a new memory location, thereby breaking the sharing relationship and ensuring the modification doesn&#39;t affect other holders.</li></ol><p><strong>Analysis</strong>: This question tests a deep understanding of the core of SKB design. The cloning mechanism is one of the keys to the high performance of the Linux network stack, embodying the typical Linux design philosophy of &quot;reference counting + shared data.&quot;</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p><code>sk_buff</code> is the core data structure for handling packets in the Linux network stack. The essence of its design lies in using <strong>pointer operations</strong> (<code>skb_push</code>, <code>skb_pull</code>, <code>skb_reserve</code>) rather than data copies to achieve passing between protocol layers. The four pointers <code>head</code>, <code>data</code>, <code>tail</code>, and <code>end</code> define a dynamic linear data area, while the <code>protocol</code> field acts as the &quot;baton&quot; passed between layers, telling the next layer which type of protocol header to treat the <code>data</code> pointer as.</p><p>Headroom (reserved head space) is key to SKB performance optimization. Once the reserved space is insufficient, the kernel must perform an expensive <strong>data copy</strong> (<code>pskb_expand_head</code>), involving new memory allocation and full data migration. This is a trap that must be avoided at all costs in high-performance network processing. The correct reservation strategy (usually calling <code>skb_reserve</code> at allocation time) is a fundamental skill for driver developers.</p><p>The <code>net_device</code> structure, as the abstraction of a network interface, has its core in the <strong><code>net_device_ops</code></strong> function set, which defines the specific behaviors for the kernel to control the hardware (such as start, stop, send). Through <strong>hardware feature offloading</strong> (like <code>NETIF_F_IP_CSUM</code>, <code>TSO</code>, <code>GRO</code>), the kernel can offload heavy computational tasks (checksums, fragmentation, reassembly) to the NIC hardware, thereby freeing up CPU compute power.</p><p>The <strong>cloning</strong> mechanism is the solution for multiple paths sharing the same packet. When only metadata needs to be modified (like Netfilter changing the route), the kernel uses <code>skb_clone()</code> to copy only the <code>sk_buff</code> structure, shares the underlying data buffer, and sets the <code>cloned</code> flag while incrementing the <code>users</code> reference count. Only when a <strong>write operation</strong> occurs does the kernel trigger an actual data copy, i.e., &quot;copy-on-write.&quot;</p><p>RDMA technology achieves zero-copy network transmission by bypassing the remote CPU. It relies on <strong>Protection Domains (PDs)</strong> to delineate security boundaries, <strong>Memory Regions (MRs)</strong> to complete memory registration (pinning virtual addresses and translating them to physical addresses for NIC access), and uses <strong>Local/Remote Keys (LKey/RKey)</strong> as access credentials, ensuring that only authorized operations can read and write memory. This is a paradigm of balancing high performance with security.</p>`,129)])])}const g=t(o,[["render",n]]);export{u as __pageData,g as default};
