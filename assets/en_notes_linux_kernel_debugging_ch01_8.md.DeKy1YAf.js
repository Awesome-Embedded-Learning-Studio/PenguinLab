import{_ as n,o as s,c as a,a as t}from"./chunks/framework.8tiMeCK-.js";const h=JSON.parse('{"title":"1.8 Seeing the Difference — Production vs. Debug Kernel Config Showdown","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch01_8.md","filePath":"en/notes/linux_kernel_debugging/ch01_8.md","lastUpdated":null}'),i={name:"en/notes/linux_kernel_debugging/ch01_8.md"};function o(r,e,l,p,c,d){return s(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="_1-8-seeing-the-difference-—-production-vs-debug-kernel-config-showdown" tabindex="-1">1.8 Seeing the Difference — Production vs. Debug Kernel Config Showdown <a class="header-anchor" href="#_1-8-seeing-the-difference-—-production-vs-debug-kernel-config-showdown" aria-label="Permalink to &quot;1.8 Seeing the Difference — Production vs. Debug Kernel Config Showdown&quot;">​</a></h1><p>In the previous section, we turned on every debug switch we could find — which is great, but it raises a question: what exactly did we change? In other words, how much does all this <code>y</code> and <code>n</code> toggling actually divide this kernel from its production sibling?</p><p>This question matters more than you might think. If you don&#39;t know where the boundary lies, you might end up fixing a &quot;debug-only bug&quot; as if it were a universal issue, or worse — trying to load a module that only exists thanks to debug symbols in a production environment.</p><p>Let&#39;s make the invisible visible.</p><h3 id="the-config-comparison-microscope-diffconfig" tabindex="-1">The Config Comparison Microscope: <code>diffconfig</code> <a class="header-anchor" href="#the-config-comparison-microscope-diffconfig" aria-label="Permalink to &quot;The Config Comparison Microscope: \`diffconfig\`&quot;">​</a></h3><p>Hidden in the kernel source tree is a handy script called <code>scripts/diffconfig</code>. It acts like a microscope, purpose-built to compare two <code>.config</code> files and magnify the differences for you.</p><p>Now, suppose we have two config file paths:</p><ul><li>Production kernel config: <code>~/lkd_kernels/kconfig_prod01</code></li><li>Debug kernel config: <code>~/lkd_kernels/kconfig_dbg01</code></li></ul><p>We run this script from within the debug kernel&#39;s source directory, piping the differences into a text file:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scripts/diffconfig</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/lkd_kernels/kconfig_prod01</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/lkd_kernels/kconfig_dbg01</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ../../kconfig_diff_prod_to_debug.txt</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Once that command finishes, you have a complete &quot;health report.&quot;</p><p>On my system, this report exceeds 200 lines. That means the debug kernel isn&#39;t just &quot;slightly&quot; tweaked — it has undergone a <strong>genetic-level mutation</strong>.</p><h3 id="interpreting-the-difference-report" tabindex="-1">Interpreting the Difference Report <a class="header-anchor" href="#interpreting-the-difference-report" aria-label="Permalink to &quot;Interpreting the Difference Report&quot;">​</a></h3><p>Open <code>kconfig_diff_prod_to_debug.txt</code> in an editor. You&#39;ll notice the lines fall into three categories, each telling a different story.</p><h4 id="category-1-removed-features-prefix" tabindex="-1">Category 1: Removed Features (Prefix <code>-</code>) <a class="header-anchor" href="#category-1-removed-features-prefix" aria-label="Permalink to &quot;Category 1: Removed Features (Prefix \`-\`)&quot;">​</a></h4><p>Lines starting with <code>-</code> represent features that exist in the production kernel but were stripped out of our debug kernel.</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>-BPF_LSM y</span></span>
<span class="line"><span>-DEFAULT_SECURITY_APPARMOR y</span></span>
<span class="line"><span>-DEFAULT_SECURITY_SELINUX n</span></span>
<span class="line"><span>-DEFAULT_SECURITY_SMACK n</span></span>
<span class="line"><span>[ … ]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Seeing this list might give you pause: <strong>Wait, why would a debug kernel disable security modules?</strong></p><p>It sounds counterintuitive, but there&#39;s a very practical trade-off at play. Certain security mechanisms (like some modules under the LSM framework) perform deep interception of system calls, which can severely slow down the system or even interfere with a debugger&#39;s normal tracing. On a kernel dedicated to hunting bugs, we sometimes have to temporarily lower these shields for the sake of performance and purity.</p><h4 id="category-2-toggled-switches-n-y-or-y-n" tabindex="-1">Category 2: Toggled Switches (<code>n -&gt; y</code> or <code>y -&gt; n</code>) <a class="header-anchor" href="#category-2-toggled-switches-n-y-or-y-n" aria-label="Permalink to &quot;Category 2: Toggled Switches (\`n -&gt; y\` or \`y -&gt; n\`)&quot;">​</a></h4><p>This is the core section. It shows the options whose &quot;state has flipped.&quot; You&#39;ll notice that the vast majority of arrows point toward <code>y</code>.</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>DEBUG_ATOMIC_SLEEP n -&gt; y</span></span>
<span class="line"><span>DEBUG_BOOT_PARAMS n -&gt; y</span></span>
<span class="line"><span>DEBUG_INFO n -&gt; y</span></span>
<span class="line"><span>DEBUG_KMEMLEAK n -&gt; y</span></span>
<span class="line"><span>DEBUG_LOCK_ALLOC n -&gt; y</span></span>
<span class="line"><span>DEBUG_MUTEXES n -&gt; y</span></span>
<span class="line"><span>DEBUG_PLIST n -&gt; y</span></span>
<span class="line"><span>DEBUG_RT_MUTEXES n -&gt; y</span></span>
<span class="line"><span>DEBUG_RWSEMS n -&gt; y</span></span>
<span class="line"><span>DEBUG_SPINLOCK n -&gt; y</span></span>
<span class="line"><span>[ … ]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><p>Every single <code>n -&gt; y</code> line means we&#39;ve planted a pair of watching eyes in the debug kernel.</p><ul><li><code>DEBUG_ATOMIC_SLEEP</code>: Originally, sleeping in atomic context was forbidden; now it&#39;s not only forbidden, but the kernel will shout about it.</li><li><code>DEBUG_INFO</code>: Originally, symbols were omitted to save space; now they&#39;re all included so gdb can understand what&#39;s going on.</li><li><code>DEBUG_LOCK_ALLOC</code>, <code>DEBUG_MUTEXES</code>, <code>DEBUG_SPINLOCK</code>: Lock-related checks are fully enabled.</li></ul><p>Here, you&#39;ll also see the name suffix we specifically modified earlier:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>LKDTM n -&gt; m</span></span>
<span class="line"><span>LOCALVERSION &quot;-prod01&quot; -&gt; &quot;-dbg01&quot;</span></span>
<span class="line"><span>LOCK_STAT n -&gt; y</span></span>
<span class="line"><span>MMIOTRACE n -&gt; y</span></span>
<span class="line"><span>MODULE_SIG y -&gt; n</span></span>
<span class="line"><span>[ … ]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><ul><li><code>LOCALVERSION</code> changed from <code>-prod01</code> to <code>-dbg01</code>. This is exactly why you see <code>5.10.17-dbg01</code> instead of <code>5.10.17-prod01</code> in the boot log.</li><li><code>MODULE_SIG</code> changed from <code>y</code> to <code>n</code>. There&#39;s a subtle point here: production kernels typically require module signatures (<code>y</code>) to prevent loading malicious modules; but debug kernels usually turn forced signing off (<code>n</code>) to make it easier to tinker with test modules. Otherwise, loading a test module you wrote yourself would require wrestling with signature certificates — essentially asking for unnecessary pain.</li></ul><h4 id="category-3-added-features-prefix" tabindex="-1">Category 3: Added Features (Prefix <code>+</code>) <a class="header-anchor" href="#category-3-added-features-prefix" aria-label="Permalink to &quot;Category 3: Added Features (Prefix \`+\`)&quot;">​</a></h4><p>Lines starting with <code>+</code> represent entirely new entries that don&#39;t exist in the production kernel config at all.</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>+ARCH_HAS_EARLY_DEBUG y</span></span>
<span class="line"><span>+BITFIELD_KUNIT n</span></span>
<span class="line"><span>[ … ]</span></span>
<span class="line"><span>+IKCONFIG m</span></span>
<span class="line"><span>+KASAN_GENERIC y</span></span>
<span class="line"><span>[ … ]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>Pay attention to that last one, <code>KASAN_GENERIC</code>. This is <strong>Kernel Address SANitizer</strong>, a heavy-hitter in kernel debugging specifically designed to catch nasty memory bugs like out-of-bounds accesses and use-after-free. It drastically slows down the system (potentially by 2x or more), so it&#39;s absolutely forbidden in production kernels. In a debug kernel, however, it&#39;s priceless.</p><h3 id="back-to-reality-your-project-might-be-different" tabindex="-1">Back to Reality: Your Project Might Be Different <a class="header-anchor" href="#back-to-reality-your-project-might-be-different" aria-label="Permalink to &quot;Back to Reality: Your Project Might Be Different&quot;">​</a></h3><p>Although we demonstrated a &quot;standard&quot; production vs. debug pairing approach here, I have to be honest:</p><blockquote><p>Specific kernel configurations are merely representative.</p></blockquote><p>Your project or product might have a bunch of peculiar requirements. Maybe your production kernel needs certain special real-time patches, or maybe your debug environment needs to run on a quirky virtualization platform.</p><p>Additionally, if you&#39;re working on modern embedded Linux, you&#39;re probably not typing <code>make menuconfig</code> on the command line — you&#39;re using a heavyweight build system like <strong>Yocto</strong> or <strong>Buildroot</strong>.</p><p>If that&#39;s the case, the &quot;manual .config editing&quot; skills you just learned will need to be applied elsewhere. In Yocto, you typically need to write a <code>bbappend</code> recipe file to inject your debug configuration. That&#39;s a deeper rabbit hole, but the core logic remains the same: one is a trimmed-down athlete, the other is a test subject covered in sensors.</p><h3 id="final-check" tabindex="-1">Final Check <a class="header-anchor" href="#final-check" aria-label="Permalink to &quot;Final Check&quot;">​</a></h3><p>At this point, I&#39;m secretly hoping you&#39;ve actually been following along — meaning you should now really have two compiled kernels in hand: one production, one debug.</p><p>If you don&#39;t, I strongly recommend going back and running the compilation step now. Don&#39;t just read without practicing.</p><p>If all went well, you should now have your custom 5.10 LTS production and debug kernels, ready for battle. In the upcoming chapters, we&#39;ll actually boot them and see how they behave differently when things go wrong.</p><p>Before that, let&#39;s use the final section to equip you with a few pieces of &quot;close-fitting debugging gear.&quot;</p>`,42)])])}const g=n(i,[["render",o]]);export{h as __pageData,g as default};
