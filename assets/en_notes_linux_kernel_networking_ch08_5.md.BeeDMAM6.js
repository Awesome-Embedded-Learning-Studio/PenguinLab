import{_ as t,o,c as s,a as i}from"./chunks/framework.8tiMeCK-.js";const g=JSON.parse('{"title":"8.5 Autoconfiguration: Stateless Magic","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch08_5.md","filePath":"en/notes/linux_kernel_networking/ch08_5.md","lastUpdated":null}'),a={name:"en/notes/linux_kernel_networking/ch08_5.md"};function n(r,e,l,d,h,c){return o(),s("div",null,[...e[0]||(e[0]=[i('<h1 id="_8-5-autoconfiguration-stateless-magic" tabindex="-1">8.5 Autoconfiguration: Stateless Magic <a class="header-anchor" href="#_8-5-autoconfiguration-stateless-magic" aria-label="Permalink to &quot;8.5 Autoconfiguration: Stateless Magic&quot;">​</a></h1><p>When we wrapped up <code>ipv6_rcv()</code> in the previous section, I actually had a lingering concern.</p><p>If you followed my earlier debugging steps, you might have noticed a counterintuitive phenomenon: before you even configure an address, or even type <code>ip addr add</code>, <code>ip addr show</code> sometimes already displays an IPv6 address—and a daunting 128-bit one at that.</p><p>Who configured it? No DHCP was run, and no one typed it manually. Where did this address come from?</p><p>This is IPv6&#39;s most &quot;magical&quot; feature: <strong>Autoconfiguration</strong>. With this mechanism, a host can conjure up a globally unique address out of thin air. No DHCP server needed, no manual intervention required—just plug in the cable, and it comes alive on its own.</p><p>But behind this magic lies a rigorous, step-by-step ritual. Let&#39;s break it down into four stages.</p><hr><h3 id="phase-1-starting-quietly-on-the-local-link" tabindex="-1">Phase 1: Starting Quietly on the Local Link <a class="header-anchor" href="#phase-1-starting-quietly-on-the-local-link" aria-label="Permalink to &quot;Phase 1: Starting Quietly on the Local Link&quot;">​</a></h3><p>When the system boots, the first thing the IPv6 stack does upon waking up isn&#39;t asking a router for an address—it&#39;s creating a &quot;temporary ID&quot; for itself.</p><p>This is the <strong>link-local address</strong>.</p><p>The generation logic is straightforward:</p><ol><li>Take the prefix <code>fe80::/10</code> (commonly written as <code>fe80::/64</code>).</li><li>Append its own Interface ID (usually the MAC address in EUI-64 format).</li></ol><p>At this point, this address is tagged internally in the kernel with: <code>IFA_F_TENTATIVE</code> (tentative).</p><p><strong>This tag is crucial.</strong></p><p>In this state, although the interface has an address, it is &quot;crippled&quot;—it cannot send or receive regular traffic, <strong>and can only process Neighbor Discovery Protocol messages</strong>.</p><p>Why? Because it&#39;s like walking into a room and shouting, &quot;My name is John Doe.&quot; Until you&#39;re sure there isn&#39;t another &quot;John Doe&quot; in the room, you wouldn&#39;t dare actually socialize under that name. This is the job of DAD (Duplicate Address Detection), which we thoroughly dissected in the Neighbor Discovery chapter.</p><p>Only when DAD passes and the <code>IFA_F_TENTATIVE</code> flag is cleared can this address officially go to work.</p><blockquote><p>⚠️ <strong>Pitfall Warning</strong> If DAD fails (an address conflict is detected), the autoconfiguration process <strong>terminates immediately</strong>. The kernel won&#39;t assign a second address, nor will it throw an error—it just fails silently. At this point, your only options are manual configuration or changing the MAC address. This is particularly common in VM cloning scenarios—two VMs end up with the same MAC, their IPv6 addresses collide, and neither can connect. Even ping fails, making it look like the network interface is broken.</p></blockquote><hr><h3 id="phase-2-looking-for-a-guide" tabindex="-1">Phase 2: Looking for a Guide <a class="header-anchor" href="#phase-2-looking-for-a-guide" aria-label="Permalink to &quot;Phase 2: Looking for a Guide&quot;">​</a></h3><p>Once the link-local address is established (proving &quot;I am unique&quot;), if this machine isn&#39;t a router, it starts looking for an exit.</p><p>At this stage, the host has a clear need: <strong>&quot;I want to know what the outside network looks like, and is there a prefix I can use?&quot;</strong></p><p>It doesn&#39;t just wait around; it actively calls out to the link. Inside the kernel, it calls <code>ndisc_send_rs()</code> to send a <strong>Router Solicitation (RS)</strong> message.</p><ul><li><strong>Destination Address</strong>: <code>ff02::2</code> (the all-routers multicast address).</li><li><strong>Protocol Type</strong>: ICMPv6, Type <code>NDISC_ROUTER_SOLICITATION (133)</code>.</li></ul><p>This step is like the host shouting down the hallway: &quot;There&#39;s a machine here that needs internet access—any router hear me?&quot;</p><hr><h3 id="phase-3-the-router-s-proclamation" tabindex="-1">Phase 3: The Router&#39;s Proclamation <a class="header-anchor" href="#phase-3-the-router-s-proclamation" aria-label="Permalink to &quot;Phase 3: The Router&#39;s Proclamation&quot;">​</a></h3><p>The router hears it. In response, it sends a <strong>Router Advertisement (RA)</strong> message.</p><ul><li><strong>Source Address</strong>: The router&#39;s link-local address.</li><li><strong>Destination Address</strong>: <code>ff02::1</code> (the all-nodes multicast address).</li><li><strong>Protocol Type</strong>: ICMPv6, Type <code>NDISC_ROUTER_ADVERTISEMENT (134)</code>.</li></ul><p>In a Linux environment, this &quot;shouting&quot; role is usually played by a userspace daemon called <code>radvd</code>.</p><p>You can install <code>radvd</code> on the router and write the prefix you want to broadcast (e.g., <code>2001:db8:abcd::/64</code>) in its configuration file. <code>radvd</code> does two things:</p><ol><li><strong>Periodic Broadcasting</strong>: Sends RAs to <code>ff02::1</code> at regular intervals, telling newly arrived machines, &quot;This is my turf, and here is the prefix.&quot;</li><li><strong>Responding to Requests</strong>: Listens on <code>ff02::2</code>, and immediately replies with an RA as soon as it hears an RS request.</li></ol><p>This RA message is the core of the entire autoconfiguration process. It holds the two things the host wants most:</p><ol><li><strong>Prefix Information</strong>: For example, <code>2001:db8:abcd::/64</code>.</li><li><strong>Various Flags</strong>: Telling the host whether it &quot;can use the prefix I gave you to configure its own address (stateless)&quot; or &quot;must go find a DHCPv6 server (stateful).&quot;</li></ol><hr><h3 id="phase-4-address-synthesis-and-privacy-mode" tabindex="-1">Phase 4: Address Synthesis and &quot;Privacy Mode&quot; <a class="header-anchor" href="#phase-4-address-synthesis-and-privacy-mode" aria-label="Permalink to &quot;Phase 4: Address Synthesis and &quot;Privacy Mode&quot;&quot;">​</a></h3><p>After receiving the RA and seeing the prefix inside, the host knows what to do.</p><p>What it does now is simple assembly: <code>IPv6 Address = Prefix (from RA) + Interface ID</code></p><ul><li><strong>Prefix</strong>: Specified by the router (must be 64 bits).</li><li><strong>Interface ID</strong>: Usually its own MAC address (EUI-64).</li></ul><p>The result is a global unicast address.</p><p><strong>But there&#39;s a hidden catch here.</strong></p><p>If your Interface ID directly uses your MAC address, then wherever you go, the last 64 bits of your IPv6 address remain the same. This means that Google, advertisers, or even your next-door neighbor can track your physical device just by looking at the IPv6 packets you send—you switch to a different Wi-Fi, the prefix changes, but the last 64 bits are still there, and they still know it&#39;s you.</p><p>To solve this problem, Linux introduced <strong>Privacy Extensions</strong> (kernel config <code>CONFIG_IPV6_PRIVACY</code>).</p><p>When this feature is enabled, the kernel appends a <strong>randomly generated</strong> Interface ID after the prefix when generating an IPv6 address, instead of using the MAC address.</p><p>This brings two benefits:</p><ol><li>The outside world cannot reverse-engineer your MAC address from your IPv6 address.</li><li>This temporary address periodically expires and a new one is generated, making tracking extremely difficult.</li></ol><blockquote><p><strong>Returning to our analogy:</strong></p><p>Earlier, we compared autoconfiguration to &quot;getting an ID card.&quot;</p><p><strong>Normal Mode</strong>: Your ID number is always <code>110101[你的MAC地址]</code>. No matter where you move, the second half never changes, and anyone can look up your records.</p><p><strong>Privacy Mode</strong>: Every time you go to a new place, you give yourself a new name, like <code>110101[随机字符串]</code>. People in that place (under that prefix) know you, but the next time you move away, no one knows that &quot;random string&quot; was you.</p></blockquote><hr><h3 id="the-art-of-time-lifetime" tabindex="-1">The Art of Time: Lifetime <a class="header-anchor" href="#the-art-of-time-lifetime" aria-label="Permalink to &quot;The Art of Time: Lifetime&quot;">​</a></h3><p>Automatically configured addresses aren&#39;t permanent; they come with a strict countdown. In the RA message, the router attaches two time parameters to each prefix:</p><ol><li><p><strong>Valid Lifetime</strong>:</p><ul><li>This is the &quot;shelf life&quot; of the address.</li><li>When the countdown ends, this address is deleted directly by the kernel and disappears from the interface.</li><li>Before it expires, already established TCP connections can continue to use it, but new connections cannot use this address.</li></ul></li><li><p><strong>Preferred Lifetime</strong>:</p><ul><li>This is shorter than the Valid Lifetime.</li><li>Once the countdown ends, this address enters the &quot;Deprecated&quot; state.</li><li>At this point, the system <strong>will no longer actively use this address to initiate communication</strong> (for example, if you want to <code>ping6</code> someone, the system won&#39;t select this address as the source address), but it will still receive packets sent to this address (to maintain existing connections).</li></ul></li></ol><p>In the kernel&#39;s <code>inet6_ifaddr</code> structure (<code>include/net/if_inet6.h</code>), these two parameters correspond to the <code>valid_lft</code> and <code>prefered_lft</code> fields.</p><p>These two designs exist to support <strong>Renumbering</strong>.</p><p>Imagine you&#39;re the network administrator and want to replace the entire company&#39;s IPv6 prefix (for example, because you switched ISPs). You don&#39;t need to run commands on every single machine. You just need to do the following in the <code>radvd</code> configuration:</p><ol><li>Add the <strong>new prefix</strong> and give it a long Lifetime.</li><li>Set the <strong>old prefix&#39;s</strong> Lifetime to something very short (or to 0).</li></ol><p>After hosts receive the new RA, they will automatically:</p><ul><li>Let the old address slowly expire.</li><li>Configure the new address.</li><li>Smoothly switch subsequent traffic to the new address.</li></ul><p>This is the seamless renumbering process described in RFC 4192.</p><hr><h3 id="stateless-vs-stateful-dhcpv6" tabindex="-1">Stateless vs. Stateful (DHCPv6) <a class="header-anchor" href="#stateless-vs-stateful-dhcpv6" aria-label="Permalink to &quot;Stateless vs. Stateful (DHCPv6)&quot;">​</a></h3><p>So far, everything we&#39;ve discussed has been <strong>Stateless Address Autoconfiguration (SLAAC)</strong>. Its core characteristic is that the router only broadcasts the prefix, and the host calculates its own address. The router doesn&#39;t need to keep track of who is using which address—it is &quot;stateless.&quot;</p><p>But sometimes, the network administrator (that&#39;s you) wants more fine-grained control. You want to know who has which IP, or you want to forcefully push down DNS server addresses (although RA can also push DNS, it&#39;s not supported in all scenarios).</p><p>This is where <strong>DHCPv6</strong> comes in.</p><ul><li><strong>Stateful</strong>: The host gets an address from a DHCPv6 server, and the server maintains a database recording the <code>MAC -&gt; IP</code> mapping. This is the old IPv4 approach.</li><li><strong>Stateless</strong>: The host still calculates its own address, but can get other configuration information (like DNS) via DHCPv6. This is called &quot;stateless DHCPv6.&quot;</li></ul><p>If the kernel receives an RA with the &quot;Managed&quot; flag (M flag), it understands: &quot;In this place, I need to find a DHCPv6 server to get an address,&quot; and only then will it start a DHCPv6 client.</p><hr><h3 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h3><p>In this section, we walked through the entire process of an IPv6 host going from &quot;bare metal&quot; to &quot;connected&quot;:</p><ol><li><strong>Self-Assertion</strong>: Generate a link-local address and confirm uniqueness via DAD.</li><li><strong>Looking for Signposts</strong>: Send an RS to request a router.</li><li><strong>Receiving Gifts</strong>: Receive an RA and obtain the prefix.</li><li><strong>Assembling an Identity</strong>: Prefix + Interface ID (with optional privacy extensions) generates a global unicast address.</li><li><strong>Birth and Death</strong>: As the Preferred/Valid Lifetime ticks down, addresses naturally age out or are renewed.</li></ol><p>Now, your machine has a legitimate, routable IPv6 address.</p><p>The cable is plugged in, and the address is set. Next, when a real IPv6 packet comes in from the outside, how does the kernel handle it? In the next section, we&#39;ll dive inside <code>ipv6_rcv()</code> to look at the details on the receive path that we&#39;ve glossed over so far.</p>',71)])])}const p=t(a,[["render",n]]);export{g as __pageData,p as default};
