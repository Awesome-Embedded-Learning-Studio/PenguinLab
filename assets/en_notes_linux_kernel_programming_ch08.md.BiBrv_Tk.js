import{_ as t,o as a,c as s,a as o}from"./chunks/framework.8tiMeCK-.js";const p=JSON.parse('{"title":"Chapter 8: Kernel Memory Allocators: The Buddy System and Slab","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_programming/ch08.md","filePath":"en/notes/linux_kernel_programming/ch08.md","lastUpdated":null}'),i={name:"en/notes/linux_kernel_programming/ch08.md"};function n(l,e,r,h,c,d){return a(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="chapter-8-kernel-memory-allocators-the-buddy-system-and-slab" tabindex="-1">Chapter 8: Kernel Memory Allocators: The Buddy System and Slab <a class="header-anchor" href="#chapter-8-kernel-memory-allocators-the-buddy-system-and-slab" aria-label="Permalink to &quot;Chapter 8: Kernel Memory Allocators: The Buddy System and Slab&quot;">​</a></h1><h3 id="_8-0-introduction-when-the-kernel-starts-managing-memory" tabindex="-1">8.0 Introduction: When the Kernel Starts Managing Memory <a class="header-anchor" href="#_8-0-introduction-when-the-kernel-starts-managing-memory" aria-label="Permalink to &quot;8.0 Introduction: When the Kernel Starts Managing Memory&quot;">​</a></h3><p>In previous chapters, we dissected the kernel&#39;s internal architecture and process management like taking apart a clock. Now, we&#39;re going to touch the most sensitive and core nervous system of this operating system—<strong>Memory Management</strong>.</p><p>For driver developers, this is often where the &quot;nightmare&quot; begins. You&#39;ve written the logic, registered the interrupts, and everything looks perfect—but the system crashes intermittently, or mysteriously slows down after running for a while. More often than not, the problem lies in the details of memory allocation: you&#39;re unknowingly wasting precious physical memory, or sleeping in the wrong context.</p><p>Memory allocation in the Linux kernel isn&#39;t as simple as &quot;giving you a piece of land.&quot; It&#39;s divided into two main layers: the underlying <strong>Page Allocator</strong>, which is the Buddy System we&#39;ll discuss; and the <strong>Slab Allocator</strong> built on top of it. Understanding how both work and knowing when to use which will save you a few hairs during code reviews.</p><p>In this chapter, we won&#39;t just cover how to use the APIs—we&#39;ll also dig into those counterintuitive memory waste issues hidden behind the scenes, using code and kernel logs.</p><hr><h3 id="_8-1-kernel-memory-allocators-who-s-in-charge-of-ram" tabindex="-1">8.1 Kernel Memory Allocators: Who&#39;s in Charge of RAM? <a class="header-anchor" href="#_8-1-kernel-memory-allocators-who-s-in-charge-of-ram" aria-label="Permalink to &quot;8.1 Kernel Memory Allocators: Who&#39;s in Charge of RAM?&quot;">​</a></h3><p>The core question we need to solve is simple: <strong>How is memory allocated and freed in the kernel?</strong></p><p>But behind this lies a deep cognitive trap. If you bring your user-space <code>malloc()</code> intuitions into the kernel, you&#39;re going to fall hard. Kernel memory is physically contiguous, precious, and <strong>never swapped out to disk</strong>.</p><h4 id="_8-1-1-two-engines-the-page-allocator-and-the-slab-allocator" tabindex="-1">8.1.1 Two Engines: The Page Allocator and the Slab Allocator <a class="header-anchor" href="#_8-1-1-two-engines-the-page-allocator-and-the-slab-allocator" aria-label="Permalink to &quot;8.1.1 Two Engines: The Page Allocator and the Slab Allocator&quot;">​</a></h4><p>You can think of kernel memory management as a large building materials supermarket.</p><ul><li>The <strong>Page Allocator</strong> is the warehouse manager driving the forklift. He has a checklist tracking all bundles of lumber (physical page blocks in powers of two). If you need 128 pieces of wood, he&#39;ll give you a bundle of 128; if he doesn&#39;t have that, he&#39;ll cut a bundle of 256 in half. He only handles these bulk transactions. His algorithm is called the <strong>Buddy System</strong>.</li><li>The <strong>Slab Allocator</strong> is the retail counter at the warehouse entrance. He knows people often only need a few nails (small objects), so there&#39;s no need to go to the warehouse for a whole bundle every time. So he pre-cuts some wood and places it in boxes of various sizes. When you need 20 nails, he gives you a box of 32. It&#39;s a little more than you asked for, but it&#39;s much better than giving you an entire bundle.</li></ul><p>This brings up a key point: <strong>The Slab Allocator lives on top of the Page Allocator</strong>. The Slab&#39;s &quot;boxes&quot; ultimately still need to ask the Page Allocator for &quot;whole bundles of wood.&quot; But for us developers, knowing which counter to use saves a lot of trouble.</p><h4 id="_8-1-2-the-iron-law-of-kernel-memory-non-swappable" tabindex="-1">8.1.2 The Iron Law of Kernel Memory: Non-swappable <a class="header-anchor" href="#_8-1-2-the-iron-law-of-kernel-memory-non-swappable" aria-label="Permalink to &quot;8.1.2 The Iron Law of Kernel Memory: Non-swappable&quot;">​</a></h4><p>Here is a rule you must etch into your brain: <strong>Kernel memory is non-swappable</strong>.</p><p>User-space programs can use up physical memory and then get thrown into a swap partition (i.e., disk) by the kernel to free up RAM. But the data structures used by the kernel code itself—such as the data structures used to manage memory—must never be thrown out. Otherwise, when the kernel wants to read data back from disk, it needs memory to operate, but the memory management code is on disk... This is like needing to wear your glasses in order to find your glasses—it&#39;s a deadlock.</p><p>So, kernel memory is resident in RAM. This also means that <strong>wasting kernel memory is far more costly than wasting user memory</strong>.</p><hr><h3 id="_8-2-the-page-allocator-secrets-of-the-buddy-system" tabindex="-1">8.2 The Page Allocator: Secrets of the Buddy System <a class="header-anchor" href="#_8-2-the-page-allocator-secrets-of-the-buddy-system" aria-label="Permalink to &quot;8.2 The Page Allocator: Secrets of the Buddy System&quot;">​</a></h3><p>Let&#39;s step into that large warehouse. The core data structure of the Page Allocator is <code>struct zone</code>, and inside it, there is an array <code>free_area[MAX_ORDER]</code>. This is the &quot;Buddy System free list&quot; we&#39;re going to talk about.</p><h4 id="_8-2-1-understanding-the-structure-of-the-free-list" tabindex="-1">8.2.1 Understanding the Structure of the Free List <a class="header-anchor" href="#_8-2-1-understanding-the-structure-of-the-free-list" aria-label="Permalink to &quot;8.2.1 Understanding the Structure of the Free List&quot;">​</a></h4><p><code>MAX_ORDER</code> is an architecture-dependent constant, usually 11 on x86 and ARM. This means we have 11 lists (numbered 0 to 10), each holding &quot;memory blocks&quot; of different sizes.</p><ul><li><strong>Order 0</strong>: Holds 1-page (4 KB) blocks.</li><li><strong>Order 1</strong>: Holds 2-page (8 KB) blocks.</li><li><strong>Order 2</strong>: Holds 4-page (16 KB) blocks.</li><li>...</li><li><strong>Order 10</strong>: Holds 1024-page (4 MB) blocks.</li></ul><p>All blocks are <strong>physically contiguous</strong>. This is why the allocator is called the &quot;Buddy System&quot;: if you have an Order N block and cut it in half, these two Order N-1 blocks are &quot;buddies.&quot; If they both become free, they will merge back into an Order N block. This is the magic of &quot;anti-fragmentation.&quot;</p><p>You can see the current inventory through <code>/proc/buddyinfo</code>, the &quot;warehouse ledger&quot;:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /proc/buddyinfo</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 0,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> zone</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      DMA</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 0,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> zone</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    DMA32</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">   3551</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">   1542</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    476</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    255</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    158</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">     77</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">     41</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">     21</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">     11</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      3</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Each column of numbers corresponds to the number of free blocks on Order 0 through Order 10. If you see a 0 in the Order 10 column, it means the system can&#39;t find even a single 4 MB chunk of physically contiguous memory.</p><h4 id="_8-2-2-how-the-algorithm-actually-works-the-life-of-an-allocation-request" tabindex="-1">8.2.2 How the Algorithm Actually Works: The Life of an Allocation Request <a class="header-anchor" href="#_8-2-2-how-the-algorithm-actually-works-the-life-of-an-allocation-request" aria-label="Permalink to &quot;8.2.2 How the Algorithm Actually Works: The Life of an Allocation Request&quot;">​</a></h4><p>Suppose your driver requests 128 KB of memory. Let&#39;s calculate: 128 KB / 4 KB = 32 pages. 32 is 2 to the 5th power. So the allocator will look at the Order 5 list.</p><ol><li><strong>If in stock</strong>: Take it directly. Done.</li><li><strong>If out of stock</strong>: Look at Order 6. If Order 6 has a 256 KB block, the allocator will cut it in half (into two 128 KB buddies).</li><li><strong>Handling the excess</strong>: One half goes to you (fulfilling the request), and the other half is hung back on the Order 5 list for next time.</li></ol><p>Sounds perfect, right? But there&#39;s a huge pitfall here called <strong>internal fragmentation</strong>.</p><h4 id="_8-2-3-pitfall-alert-the-trap-of-internal-fragmentation" tabindex="-1">8.2.3 Pitfall Alert: The Trap of Internal Fragmentation <a class="header-anchor" href="#_8-2-3-pitfall-alert-the-trap-of-internal-fragmentation" aria-label="Permalink to &quot;8.2.3 Pitfall Alert: The Trap of Internal Fragmentation&quot;">​</a></h4><p>Suppose you need 132 KB of memory. The allocator takes a look: 132 KB is not a power of two. The next &quot;box&quot; that can hold it is an Order 7 block, which is 256 KB. Result: You requested 132 KB, but the kernel gave you 256 KB. <strong>The remaining 124 KB is just wasted</strong>.</p><p>This is the cost of &quot;cutting wood.&quot; If you only need a tiny bit more than 128 KB, the warehouse manager is still forced to cut a 256 KB bundle for you.</p><p>To alleviate this problem, the kernel provides a pair of APIs: <code>alloc_pages_exact()</code> and <code>free_pages_exact()</code>. They cleverly allocate a bit more and then &quot;return&quot; the excess. While it can&#39;t completely eliminate waste, it stops the bleeding.</p><hr><h3 id="_8-3-using-the-page-allocator-api-trading-with-the-kernel" tabindex="-1">8.3 Using the Page Allocator API: &quot;Trading&quot; with the Kernel <a class="header-anchor" href="#_8-3-using-the-page-allocator-api-trading-with-the-kernel" aria-label="Permalink to &quot;8.3 Using the Page Allocator API: &quot;Trading&quot; with the Kernel&quot;">​</a></h3><p>Now let&#39;s write some code. The Page Allocator provides a set of APIs, and their names usually contain <code>page</code> or <code>free_page</code>.</p><h4 id="_8-3-1-core-cheat-sheet" tabindex="-1">8.3.1 Core Cheat Sheet <a class="header-anchor" href="#_8-3-1-core-cheat-sheet" aria-label="Permalink to &quot;8.3.1 Core Cheat Sheet&quot;">​</a></h4><p>These APIs are your tools for making &quot;bulk transactions&quot; in the kernel:</p><table tabindex="0"><thead><tr><th style="text-align:left;">API / Macro</th><th style="text-align:left;">Function</th><th style="text-align:left;">Return Value</th></tr></thead><tbody><tr><td style="text-align:left;"><code>__get_free_page(gfp)</code></td><td style="text-align:left;">Allocate 1 page</td><td style="text-align:left;">Kernel logical address</td></tr><tr><td style="text-align:left;"><code>__get_free_pages(gfp, order)</code></td><td style="text-align:left;">Allocate $2^{order}$ pages</td><td style="text-align:left;">Kernel logical address</td></tr><tr><td style="text-align:left;"><code>get_zeroed_page(gfp)</code></td><td style="text-align:left;">Allocate 1 page and <strong>zero it</strong></td><td style="text-align:left;">Kernel logical address</td></tr><tr><td style="text-align:left;"><code>alloc_page(gfp)</code></td><td style="text-align:left;">Allocate 1 page</td><td style="text-align:left;"><code>struct page *</code> structure pointer</td></tr><tr><td style="text-align:left;"><code>alloc_pages(gfp, order)</code></td><td style="text-align:left;">Allocate $2^{order}$ pages</td><td style="text-align:left;"><code>struct page *</code> structure pointer</td></tr></tbody></table><p><strong>Note the difference</strong>: <code>__get_free_page</code> returns an <strong>address</strong> (which you can use directly), while <code>alloc_page</code> returns a <strong>page descriptor</strong> (<code>struct page</code>). You need to call <code>page_address()</code> to convert it to an address before you can use it. It&#39;s like one directly handing you the key, while the other gives you a registration card to exchange for a key at the front desk.</p><h4 id="_8-3-2-freeing-memory-don-t-mess-it-up" tabindex="-1">8.3.2 Freeing Memory: Don&#39;t Mess It Up <a class="header-anchor" href="#_8-3-2-freeing-memory-don-t-mess-it-up" aria-label="Permalink to &quot;8.3.2 Freeing Memory: Don&#39;t Mess It Up&quot;">​</a></h4><p>The corresponding free APIs are simple:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> free_page</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> addr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> free_pages</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> addr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> order</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> __free_pages</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> page </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">page</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> order</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Here&#39;s a classic pitfall: if you allocate with <code>alloc_page</code> and get a <code>page</code> pointer, never treat it as a long integer address and pass it to <code>free_pages</code>, or mix up the first argument when passing it to <code>__free_pages</code>. Always use them in pairs.</p><hr><h3 id="_8-4-gfp-flags-telling-the-kernel-your-bottom-line" tabindex="-1">8.4 GFP Flags: Telling the Kernel Your Bottom Line <a class="header-anchor" href="#_8-4-gfp-flags-telling-the-kernel-your-bottom-line" aria-label="Permalink to &quot;8.4 GFP Flags: Telling the Kernel Your Bottom Line&quot;">​</a></h3><p>All allocation functions have a parameter called <code>gfp_mask</code> (e.g., <code>GFP_KERNEL</code>, <code>GFP_ATOMIC</code>). This isn&#39;t just for show—it&#39;s a &quot;life-or-death contract&quot; between you and the kernel.</p><h4 id="_8-4-1-the-golden-rule-process-context-vs-atomic-context" tabindex="-1">8.4.1 The Golden Rule: Process Context vs. Atomic Context <a class="header-anchor" href="#_8-4-1-the-golden-rule-process-context-vs-atomic-context" aria-label="Permalink to &quot;8.4.1 The Golden Rule: Process Context vs. Atomic Context&quot;">​</a></h4><p>You must memorize this rule; violating it will crash or hang the system:</p><ol><li><strong>GFP_KERNEL</strong>: You are in <strong>process context</strong> (e.g., in your module&#39;s <code>init</code> function, or in a system call implementation), and <strong>sleeping is allowed</strong>. This is the most commonly used flag. It tells the kernel: &quot;If there&#39;s not enough memory, you can reclaim memory, or even read/write to disk. Take your time, I can wait.&quot;</li><li><strong>GFP_ATOMIC</strong>: You are in <strong>atomic context</strong> (e.g., in an Interrupt Handler <code>ISR</code>, or while holding a spinlock <code>spinlock</code>). Sleeping is <strong>absolutely forbidden</strong> here. You must tell the kernel: &quot;Give it to me now. If you can&#39;t, just fail and return immediately. Whatever you do, don&#39;t try to schedule or do I/O.&quot;</li></ol><p>If you use <code>GFP_KERNEL</code> while holding a spinlock, the kernel might try to sleep to swap for memory, causing scheduler chaos. This is what&#39;s known as &quot;sleeping in an atomic region,&quot; and the consequence is usually a system deadlock or panic.</p><h4 id="_8-4-2-other-common-flags" tabindex="-1">8.4.2 Other Common Flags <a class="header-anchor" href="#_8-4-2-other-common-flags" aria-label="Permalink to &quot;8.4.2 Other Common Flags&quot;">​</a></h4><ul><li><strong><code>__GFP_ZERO</code></strong>: This is a modifier, usually used in combination with the two above (<code>GFP_KERNEL | __GFP_ZERO</code>). It tells the kernel to zero out the allocated memory. This is a good programming practice that prevents information leaks and uninitialized memory vulnerabilities.</li></ul><hr><h3 id="_8-5-the-slab-allocator-making-small-object-allocation-more-efficient" tabindex="-1">8.5 The Slab Allocator: Making Small Object Allocation More Efficient <a class="header-anchor" href="#_8-5-the-slab-allocator-making-small-object-allocation-more-efficient" aria-label="Permalink to &quot;8.5 The Slab Allocator: Making Small Object Allocation More Efficient&quot;">​</a></h3><p>Let&#39;s return to our &quot;building materials supermarket&quot; analogy. If you only need a few bytes each time (like a network packet&#39;s struct <code>sk_buff</code>), the Page Allocator&#39;s big saw is way too clumsy. This is where we need the Slab Allocator.</p><h4 id="_8-5-1-object-caches-and-general-caches" tabindex="-1">8.5.1 Object Caches and General Caches <a class="header-anchor" href="#_8-5-1-object-caches-and-general-caches" aria-label="Permalink to &quot;8.5.1 Object Caches and General Caches&quot;">​</a></h4><p>The Slab Allocator was designed with two goals in mind:</p><ol><li><strong>Object Caches</strong>: The kernel has many frequently used data structures (like <code>task_struct</code>, <code>inode</code>, <code>dentry</code>). They are allocated and freed constantly. The Slab Allocator pre-creates &quot;cache pools&quot; for these objects during system initialization. When you need a <code>task_struct</code>, you simply grab a ready-made one from the pool and throw it back when you&#39;re done. This is much faster than initializing a new structure every time.</li><li><strong>General Caches</strong>: For regular memory allocations where we have no special requirements, the kernel maintains a set of <code>kmalloc-N</code> caches (e.g., <code>kmalloc-8</code>, <code>kmalloc-16</code>, ... <code>kmalloc-8192</code>). When you call <code>kmalloc(20, ...)</code>, the kernel sees that you need more than 16 bytes, so it directly gives you a 32-byte slab.</li></ol><h4 id="_8-5-2-core-cheat-sheet-kmalloc-and-kzalloc" tabindex="-1">8.5.2 Core Cheat Sheet: kmalloc and kzalloc <a class="header-anchor" href="#_8-5-2-core-cheat-sheet-kmalloc-and-kzalloc" aria-label="Permalink to &quot;8.5.2 Core Cheat Sheet: kmalloc and kzalloc&quot;">​</a></h4><p>These are the APIs you&#39;ll use most often when writing drivers day-to-day:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kmalloc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">size_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">gfp_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> flags</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kzalloc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">size_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">gfp_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> flags</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> kfree</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><ul><li><strong>kmalloc</strong>: Returns uninitialized memory (the contents are random, garbage data).</li><li><strong>kzalloc</strong>: Returns <strong>zeroed</strong> memory. <strong>It&#39;s recommended to prioritize this one</strong>—it saves worry and is safer.</li><li><strong>kfree</strong>: Frees memory. Note that passing <code>NULL</code> to <code>kfree</code> is safe; it simply does nothing.</li></ul><p><strong>Important characteristic</strong>: Memory allocated by the Slab Allocator is also <strong>physically contiguous</strong> and guarantees <strong>CPU cache line alignment</strong> (Cache Line Aligned), which is crucial in high-performance network and storage drivers.</p><hr><h3 id="_8-6-the-ultimate-limit-kmalloc-s-ceiling" tabindex="-1">8.6 The Ultimate Limit: kmalloc&#39;s Ceiling <a class="header-anchor" href="#_8-6-the-ultimate-limit-kmalloc-s-ceiling" aria-label="Permalink to &quot;8.6 The Ultimate Limit: kmalloc&#39;s Ceiling&quot;">​</a></h3><p>Since Slab is so handy, can I just use <code>kmalloc</code> to request 100 MB of memory?</p><p><strong>No.</strong></p><p>Let&#39;s go back to the building materials supermarket analogy. Although the Slab counter is flexible, its inventory (Slab objects) ultimately comes from the warehouse (Page Allocator). And the largest single order in the warehouse (<code>MAX_ORDER</code>) is typically limited to 4 MB (1024 pages).</p><p>Therefore, <strong>kmalloc&#39;s single allocation upper limit is 4 MB</strong>.</p><p>If you try to allocate memory larger than this (e.g., 5 MB), <code>kmalloc</code> will directly return <code>NULL</code>, and the kernel might even print a warning message, because such an allocation request usually indicates a design flaw. If you really need that much physically contiguous memory, you should consider other DMA allocation mechanisms, or redesign your data structures to handle non-contiguous memory pages.</p><hr><h3 id="_8-7-resource-management-and-tips" tabindex="-1">8.7 Resource Management and Tips <a class="header-anchor" href="#_8-7-resource-management-and-tips" aria-label="Permalink to &quot;8.7 Resource Management and Tips&quot;">​</a></h3><p>Finally, as kernel developers, we have a few tricks to make our code more robust.</p><ol><li><strong><code>devm_kzalloc</code></strong>: If you&#39;re writing a device driver, try to use this API. It&#39;s &quot;resource-managed.&quot; When your driver is unloaded or the device is disconnected, the kernel will <strong>automatically</strong> free this memory for you. You don&#39;t need to worry about forgetting <code>kfree</code> and causing a memory leak, which is incredibly useful in error handling paths.</li><li><strong><code>kcalloc</code> and <code>kmalloc_array</code></strong>: If you need to allocate an array, don&#39;t manually calculate <code>count * size</code>. Use these two APIs directly. They internally check for integer overflow. This prevents your calculation of <code>2000000000 * 2</code> from overflowing to 0, which would lead to allocating a tiny amount of memory and triggering a crash.</li><li><strong>Mind your alignment</strong>: The Slab Allocator already handles cache line alignment for you, so try to put frequently accessed data structure members at the beginning of the struct so they fall on the same cache line, improving the hit rate.</li></ol><h2 id="chapter-echoes" tabindex="-1">Chapter Echoes <a class="header-anchor" href="#chapter-echoes" aria-label="Permalink to &quot;Chapter Echoes&quot;">​</a></h2><p>Think back to the &quot;nightmare&quot; scenario mentioned at the beginning of this chapter. Now you should understand why simply &quot;allocating memory&quot; is so complex in the kernel.</p><p>The kernel&#39;s memory allocation system—from the underlying Page Allocator to the upper Slab—is essentially balancing three contradictory goals: <strong>physical contiguity</strong>, <strong>speed</strong>, and <strong>fragmentation</strong>.</p><ul><li>If you need <strong>physically contiguous</strong> large blocks of memory (e.g., for DMA), you must face the <strong>internal fragmentation</strong> problem of the Buddy System.</li><li>If you need <strong>speed</strong> and small blocks of memory, you rely on Slab, but you&#39;re still limited by the underlying Buddy System&#39;s ceiling (4 MB).</li><li>Regardless of which choice you make, you must always be acutely aware of which <strong>context</strong> you&#39;re in (GFP flags), otherwise the price could be the crash of the entire system.</li></ul><p>In the next chapter, we&#39;ll continue diving deeper into this topic. You&#39;ll discover that sometimes even &quot;physical contiguity&quot; can&#39;t be guaranteed, and we&#39;ll need to handle more complex scenarios like vmalloc and the OOM Killer that strikes fear into the hearts of all server administrators.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-application" tabindex="-1">Exercise 1: application <a class="header-anchor" href="#exercise-1-application" aria-label="Permalink to &quot;Exercise 1: application&quot;">​</a></h3><p><strong>Question</strong>: Suppose you are writing a Network Device Driver, and its Interrupt Handler (ISR) needs to allocate a physically contiguous block of memory for a received data packet. Given that this function runs in atomic context, which memory allocation flag (GFP flag) should you use? What are the potential consequences of choosing the wrong one?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: You should use <code>GFP_ATOMIC</code>. Using <code>GFP_KERNEL</code> could lead to a system deadlock or crash.</p><p><strong>Analysis</strong>: 1. <strong>Concept Application</strong>: An Interrupt Handler (ISR) runs in Atomic Context, where process sleeping is not allowed. 2. <strong>GFP Differences</strong>: <code>GFP_KERNEL</code> is the standard allocation flag that allows the kernel to wait by reclaiming memory or performing I/O operations when memory is low, which puts the calling process to sleep. <code>GFP_ATOMIC</code> explicitly forbids the allocator from sleeping; if memory is insufficient, it fails immediately rather than waiting. 3. <strong>Consequence Analysis</strong>: If you call <code>GFP_KERNEL</code> while holding a spinlock or in interrupt context, the kernel might attempt to schedule another process, leading to a hang or system crash. Therefore, you must use <code>GFP_ATOMIC</code> in atomic context.</p></details><h3 id="exercise-2-understanding" tabindex="-1">Exercise 2: understanding <a class="header-anchor" href="#exercise-2-understanding" aria-label="Permalink to &quot;Exercise 2: understanding&quot;">​</a></h3><p><strong>Question</strong>: If you request the allocation of 132 KB of physically contiguous memory, how much memory will the kernel actually allocate under the standard Buddy System algorithm (assuming a page size of 4 KB)? What is this phenomenon called? If there is 50 MB of available memory but it is severely fragmented, could this cause the allocation to fail?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: It will actually allocate 256 KB. This phenomenon is called internal fragmentation. Yes, it could fail.</p><p><strong>Analysis</strong>: 1. <strong>Calculation Logic</strong>: The Buddy System can only allocate memory blocks of $2^{order}$ page frames. 132 KB is approximately 33 page frames (132/4). Rounding up to the nearest power of two gives $2^6 = 64$ page frames, which is 256 KB. 2. <strong>Concept Definition</strong>: The requested 132 KB is less than the actually allocated 256 KB, and the remaining 124 KB cannot be effectively utilized by the system. This waste is called internal fragmentation. 3. <strong>Thought/Analysis</strong>: Although the system might have 50 MB of total free memory, if this 50 MB is scattered as fragments in low-order lists (like order 0, 1, 2) and cannot be pieced together into a large contiguous block (like order 5 or higher), the 256 KB allocation request will fail. This is the limitation of the Buddy System.</p></details><h3 id="exercise-3-thinking" tabindex="-1">Exercise 3: thinking <a class="header-anchor" href="#exercise-3-thinking" aria-label="Permalink to &quot;Exercise 3: thinking&quot;">​</a></h3><p><strong>Question</strong>: The Linux kernel has a pre-allocated Slab cache named <code>kmalloc-192</code> (on systems supporting <code>CONFIG_SLAB</code> or <code>CONFIG_SLUB</code>). Analyze: Why doesn&#39;t the kernel just use the Buddy System to fulfill small memory allocation requests of less than a page, instead of maintaining this Slab layer? What potential impact does this have on CPU cache performance?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Using the Slab layer reduces internal fragmentation, improves allocation speed, and optimizes object initialization costs. The impact on CPU cache is: The Slab Allocator typically guarantees memory alignment to cache lines, thereby improving cache hit rates.</p><p><strong>Analysis</strong>: 1. <strong>Architectural Design</strong>: If you directly use the Buddy System to allocate 192 bytes, the Buddy System allocates a minimum of one page (4 KB), resulting in massive internal fragmentation (wasting ~95%). The Slab Allocator &quot;slices&quot; pages into fixed-size slots (like 192 bytes), greatly improving memory utilization. 2. <strong>Performance Considerations</strong>: Buddy System allocation/freeing involves complex linked list operations and metadata updates, which is relatively slow. The Slab Allocator, based on object caches, usually performs allocation and freeing via simple pointer operations, making it extremely fast. 3. <strong>Cache Coherence</strong>: The kernel frequently needs to allocate/free specific data structures (like <code>task_struct</code>, <code>dentry</code>). Slab allows caching these objects and even supports object constructor functions, avoiding repeated initialization overhead. 4. <strong>CPU Cache Alignment</strong>: The Slab Allocator (especially the general <code>kmalloc</code> caches) typically aligns allocated memory to CPU cache lines (like 64 bytes). This avoids &quot;False Sharing&quot;—the performance jitter caused by different CPU cores modifying different variables on the same cache line—thereby improving performance in multi-core systems.</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>Linux kernel memory management is divided into two core layers: the underlying <strong>Buddy System</strong> manages physically contiguous page frames and is suitable for large block memory allocations; the upper <strong>Slab Allocator</strong> is built on top of the Buddy System, efficiently handling frequent allocation and freeing of small memory blocks through cached object pools.</p><p>The Buddy System divides memory into blocks sized as powers of two, managed via the <code>MAX_ORDER</code> array. While its &quot;split and merge&quot; mechanism effectively reduces external fragmentation, it is highly prone to <strong>internal fragmentation</strong> (e.g., requesting 132 KB but having to occupy 256 KB), so extra caution against waste is needed when allocating memory in non-power-of-two sizes.</p><p>Memory allocation in the kernel must strictly follow the <strong>rules for using GFP flags</strong>: use <code>GFP_KERNEL</code> in process context when sleeping is allowed, and you must use <code>GFP_ATOMIC</code> in <strong>atomic context</strong> such as when holding a spinlock or in an Interrupt Handler, otherwise it will lead to a system deadlock or crash.</p><p>Developers should choose the appropriate API based on the size of the data structure: for <strong>large blocks of memory</strong> (typically multiple pages), prioritize Page Allocator interfaces like <code>get_free_pages</code>; for <strong>small object allocations</strong> (less than a page), use <code>kmalloc</code> or <code>kzalloc</code> (the latter is recommended as it auto-zeros), leveraging the Slab cache for efficiency.</p><p>Although the Slab Allocator is flexible, its maximum single allocation limit is constrained by the underlying Buddy System&#39;s <code>MAX_ORDER</code> (typically <strong>4 MB</strong>). For driver development, make good use of managed APIs like <code>devm_kzalloc</code> for automatic freeing, and prioritize <code>kmalloc_array</code> to prevent integer overflow risks when allocating arrays.</p>`,101)])])}const u=t(i,[["render",n]]);export{p as __pageData,u as default};
