import{_ as t,o,c as n,a as i}from"./chunks/framework.8tiMeCK-.js";const g=JSON.parse('{"title":"Chapter Core Concepts Review","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch09_10.md","filePath":"en/notes/linux_kernel_networking/ch09_10.md","lastUpdated":null}'),a={name:"en/notes/linux_kernel_networking/ch09_10.md"};function r(s,e,l,c,d,h){return o(),n("div",null,[...e[0]||(e[0]=[i('<p>9.10 Quick Reference and Toolchain</p><blockquote><h1 id="chapter-core-concepts-review" tabindex="-1">Chapter Core Concepts Review <a class="header-anchor" href="#chapter-core-concepts-review" aria-label="Permalink to &quot;Chapter Core Concepts Review&quot;">​</a></h1><ul><li><strong>Netfilter</strong>: Kernel network packet processing framework providing a Hook mechanism.</li><li><strong>Netfilter Hooks</strong>: 5 key points (PRE_ROUTING, LOCAL_IN, FORWARD, LOCAL_OUT, POST_ROUTING).</li><li><strong>Connection Tracking (conntrack)</strong>: Tracks connection states; the foundation of NAT and stateful firewalls.</li><li><strong>NAT</strong>: Network Address Translation, including SNAT and DNAT.</li><li><strong>IPTables / nftables</strong>: Userspace rule management tools. ==================</li></ul></blockquote><p>Reaching the end of this chapter, your head is probably stuffed with macros, structure pointers, and callback functions. That&#39;s completely normal—Netfilter isn&#39;t a simple &quot;plug-and-play&quot; mechanism; it&#39;s a patch panel tangled with network cables.</p><p>In this final section, we won&#39;t introduce any new concepts. Instead, we&#39;ll lay out all the &quot;tools&quot; we&#39;ve used in this chapter and inspect them. This comes in two parts: the core methods we call in kernel code, and the real-world (userspace) tools we use to debug it.</p><h3 id="kernel-core-methods-cheat-sheet" tabindex="-1">Kernel Core Methods Cheat Sheet <a class="header-anchor" href="#kernel-core-methods-cheat-sheet" aria-label="Permalink to &quot;Kernel Core Methods Cheat Sheet&quot;">​</a></h3><p>These are the symbols you&#39;ll actually type on your keyboard during kernel module development. Each one corresponds to a specific action we broke down earlier.</p><h4 id="_1-hook-registration-and-unregistration" tabindex="-1">1. Hook Registration and Unregistration <a class="header-anchor" href="#_1-hook-registration-and-unregistration" aria-label="Permalink to &quot;1. Hook Registration and Unregistration&quot;">​</a></h4><p>This is the mandatory path to hooking your code into the network stack.</p><ul><li><p><strong><code>int nf_register_hook(struct nf_hook_ops *reg);</code></strong></p><ul><li><strong>Purpose</strong>: Register a single <code>nf_hook_ops</code> object. This is the most basic usage—fill out the structure, call this, and your callback function takes effect.</li><li><strong>Note</strong>: If it returns non-zero, registration failed (e.g., priority conflict or out of memory), and your module should handle this error.</li></ul></li><li><p><strong><code>int nf_register_hooks(struct nf_hook_ops *reg, unsigned int n);</code></strong></p><ul><li><strong>Purpose</strong>: Batch registration. The second parameter, <code>n</code>, is the array length.</li><li><strong>Scenario</strong>: For modules like <code>nf_conntrack</code> that need to plant &quot;spies&quot; across multiple protocol families and hook points simultaneously, this method pushes all hooks in the array in one go, saving you from looping through individual calls.</li></ul></li><li><p><strong><code>void nf_unregister_hook(struct nf_hook_ops *reg);</code></strong> / <strong><code>void nf_unregister_hooks(struct nf_hook_ops *reg, unsigned int n);</code></strong></p><ul><li><strong>Purpose</strong>: Unregister hooks.</li><li><strong>Warning</strong>: Don&#39;t assume this step is unimportant. If your module unloads without unregistering its hooks, the kernel still holds function pointers to that freed memory—the result is a &quot;beautiful&quot; kernel panic.</li></ul></li></ul><h4 id="_2-iptables-table-registration" tabindex="-1">2. IPTables Table Registration <a class="header-anchor" href="#_2-iptables-table-registration" aria-label="Permalink to &quot;2. IPTables Table Registration&quot;">​</a></h4><p>This code belongs to the iptables kernel framework implementation. If you want to write a custom iptables module or extension, you&#39;ll use these.</p><ul><li><p><strong><code>struct xt_table *ipt_register_table(struct net *net, const struct xt_table *table, const struct ipt_replace *repl);</code></strong></p><ul><li><strong>Purpose</strong>: Register a table in the kernel (e.g., the <code>filter</code> table or <code>nat</code> table). This typically happens during module initialization.</li><li><strong>Parameter</strong>: <code>repl</code> contains the initial rules and replacement information.</li></ul></li><li><p><strong><code>void ipt_unregister_table(struct net *net, struct xt_table *table);</code></strong></p><ul><li><strong>Purpose</strong>: Unregister the table. This frees all kernel resources associated with that table.</li></ul></li></ul><h4 id="_3-connection-tracking-lifecycle" tabindex="-1">3. Connection Tracking Lifecycle <a class="header-anchor" href="#_3-connection-tracking-lifecycle" aria-label="Permalink to &quot;3. Connection Tracking Lifecycle&quot;">​</a></h4><p>This is Netfilter&#39;s core memory system.</p><ul><li><p><strong><code>static inline void nf_conntrack_get(struct nf_conntrack *nfct);</code></strong></p><ul><li><strong>Purpose</strong>: Increment the reference count of a connection tracking object.</li><li><strong>Mechanism</strong>: Just like C++&#39;s <code>shared_ptr</code> or Rust&#39;s <code>Arc</code>. Whenever you store this connection&#39;s pointer somewhere else (like in an SKB), you must call this function to prevent the object from being prematurely freed.</li></ul></li><li><p><strong><code>static inline void nf_conntrack_put(struct nf_conntrack *nfct);</code></strong></p><ul><li><strong>Purpose</strong>: Decrement the reference count. When the count reaches zero, <code>nf_conntrack_destroy()</code> is called, and the connection&#39;s file is permanently destroyed.</li></ul></li><li><p><strong><code>static inline struct nf_conn *resolve_normal_ct(...)</code></strong></p><ul><li><strong>Purpose</strong>: The entry point for connection tracking logic. <code>nf_conntrack_in()</code> calls it to determine the packet&#39;s fate.</li><li><strong>Logic</strong>: It first calls <code>__nf_conntrack_find_get()</code> to search the hash table; if found, it binds the existing <code>nf_conn</code> to the SKB; if not found, it calls <code>init_conntrack()</code> to create a new one. Remember the &quot;unconfirmed list&quot; we talked about? Newly created entries start out right there.</li></ul></li><li><p><strong><code>struct nf_conntrack_tuple_hash *init_conntrack(...)</code></strong></p><ul><li><strong>Purpose</strong>: Allocate and initialize a new <code>nf_conn</code> object.</li><li><strong>Details</strong>: Inside, it calls <code>nf_ct_find_expectation()</code> to check if this is FTP traffic with &quot;expected connections.&quot; If so, it directly associates the new connection with the main connection and sets <code>IPS_EXPECTED_BIT</code>.</li></ul></li><li><p><strong><code>static struct nf_conn *__nf_conntrack_alloc(...)</code></strong></p><ul><li><strong>Purpose</strong>: The underlying memory allocator. It grabs a chunk of memory from the Slab Allocator for <code>nf_conn</code> and conveniently attaches a timeout timer—when this timer fires, it calls <code>death_by_timeout()</code> to execute the connection.</li></ul></li></ul><h4 id="_4-extension-registration" tabindex="-1">4. Extension Registration <a class="header-anchor" href="#_4-extension-registration" aria-label="Permalink to &quot;4. Extension Registration&quot;">​</a></h4><p>Whether it&#39;s a Target (action), a Match (condition), or a Conntrack extension, they all follow this set of interfaces.</p><ul><li><p><strong>Target (Action)</strong></p><ul><li><code>int xt_register_target(struct xt_target *target);</code></li><li><code>void xt_unregister_target(struct xt_target *target);</code></li><li><strong>Purpose</strong>: Register actions like <code>NF_DROP</code>, <code>NF_ACCEPT</code>, or <code>LOG</code>. Use this when writing a custom &quot;drop and alert&quot; module.</li></ul></li><li><p><strong>Match (Condition)</strong></p><ul><li><code>int xt_register_match(struct xt_match *target);</code></li><li>**void xt_unregister_match(struct xt_match *target);`</li><li><strong>Purpose</strong>: Register matching conditions, such as &quot;match by string content&quot; or &quot;match by time.&quot;</li></ul></li><li><p><strong>Conntrack Extensions (Connection Extensions)</strong></p><ul><li><code>int nf_ct_extend_register(struct nf_ct_ext_type *type);</code></li><li><code>void nf_ct_extend_unregister(struct nf_ct_ext_type *type);</code></li><li><strong>Purpose</strong>: We saw those extensions in the previous section (timestamp, acct, ecache). They use this interface to tell the kernel: &quot;Hey, if you need to store custom data in connection tracking, please expand capacity according to this structure.&quot;</li></ul></li></ul><hr><h3 id="core-macro-definitions" tabindex="-1">Core Macro Definitions <a class="header-anchor" href="#core-macro-definitions" aria-label="Permalink to &quot;Core Macro Definitions&quot;">​</a></h3><p>Besides functions, there are a few unavoidable macros in the code.</p><h4 id="nf-ct-direction-hash" tabindex="-1"><code>NF_CT_DIRECTION(hash)</code> <a class="header-anchor" href="#nf-ct-direction-hash" aria-label="Permalink to &quot;`NF_CT_DIRECTION(hash)`&quot;">​</a></h4><ul><li><strong>Definition</strong>: <code>include/net/netfilter/nf_conntrack_tuple.h</code></li><li><strong>Input</strong>: A <code>nf_conntrack_tuple_hash</code> object (usually pulled from the hash table).</li><li><strong>Output</strong>: A direction enum value. <ul><li><code>IP_CT_DIR_ORIGINAL</code> (0): Original direction (the direction of the request).</li><li><code>IP_CT_DIR_REPLY</code> (1): Reply direction.</li></ul></li><li><strong>Why we need it</strong>: Because the <code>tuplehash</code> array in <code>nf_conn</code> only has two elements ([0] and [1]), the kernel uses this macro to quickly determine which direction the current hash entry belongs to, thereby knowing which IP and port to parse.</li></ul><hr><h3 id="data-structure-reference-table" tabindex="-1">Data Structure Reference Table <a class="header-anchor" href="#data-structure-reference-table" aria-label="Permalink to &quot;Data Structure Reference Table&quot;">​</a></h3><p>When writing code, APIs alone aren&#39;t enough; you also need to know what manages what.</p><h4 id="table-9-2-tables-under-the-ipv4-namespace" tabindex="-1">Table 9-2: Tables under the IPv4 Namespace <a class="header-anchor" href="#table-9-2-tables-under-the-ipv4-namespace" aria-label="Permalink to &quot;Table 9-2: Tables under the IPv4 Namespace&quot;">​</a></h4><p>These are the names that appear in <code>/proc/net/ip_tables_names</code> after you load the iptables-related kernel modules.</p><table tabindex="0"><thead><tr><th style="text-align:left;">Linux Symbol (netns_ipv4)</th><th style="text-align:left;">Corresponding Kernel Module Path</th><th style="text-align:left;">Purpose</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>iptable_filter</strong></td><td style="text-align:left;"><code>net/ipv4/netfilter/iptable_filter.c</code></td><td style="text-align:left;">The default filter table, used for firewall filtering.</td></tr><tr><td style="text-align:left;"><strong>iptable_mangle</strong></td><td style="text-align:left;"><code>net/ipv4/netfilter/iptable_mangle.c</code></td><td style="text-align:left;">The mangle table, used for modifying packet headers (TOS, etc.).</td></tr><tr><td style="text-align:left;"><strong>iptable_raw</strong></td><td style="text-align:left;"><code>net/ipv4/netfilter/iptable_raw.c</code></td><td style="text-align:left;">The raw table, used for processing packets before connection tracking (configuring NOTRACK).</td></tr><tr><td style="text-align:left;"><strong>arptable_filter</strong></td><td style="text-align:left;"><code>net/ipv4/netfilter/arp_tables.c</code></td><td style="text-align:left;">ARP packet filtering table.</td></tr><tr><td style="text-align:left;"><strong>nat_table</strong></td><td style="text-align:left;"><code>net/ipv4/netfilter/iptable_nat.c</code></td><td style="text-align:left;">The NAT table, responsible for address translation.</td></tr><tr><td style="text-align:left;"><strong>iptable_security</strong></td><td style="text-align:left;"><code>net/ipv4/netfilter/iptable_security.c</code></td><td style="text-align:left;">SELinux-related security table (requires <code>CONFIG_SECURITY</code> to be enabled).</td></tr></tbody></table><h4 id="table-9-3-tables-under-the-ipv6-namespace" tabindex="-1">Table 9-3: Tables under the IPv6 Namespace <a class="header-anchor" href="#table-9-3-tables-under-the-ipv6-namespace" aria-label="Permalink to &quot;Table 9-3: Tables under the IPv6 Namespace&quot;">​</a></h4><p>The structure is almost identical to IPv4, just with an extra <code>6</code> in the names.</p><table tabindex="0"><thead><tr><th style="text-align:left;">Linux Symbol (netns_ipv6)</th><th style="text-align:left;">Corresponding Kernel Module Path</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>ip6table_filter</strong></td><td style="text-align:left;"><code>net/ipv6/netfilter/ip6table_filter.c</code></td></tr><tr><td style="text-align:left;"><strong>ip6table_mangle</strong></td><td style="text-align:left;"><code>net/ipv6/netfilter/ip6table_mangle.c</code></td></tr><tr><td style="text-align:left;"><strong>ip6table_raw</strong></td><td style="text-align:left;"><code>net/ipv6/netfilter/ip6table_raw.c</code></td></tr><tr><td style="text-align:left;"><strong>ip6table_nat</strong></td><td style="text-align:left;"><code>net/ipv6/netfilter/ip6table_nat.c</code></td></tr><tr><td style="text-align:left;"><strong>ip6table_security</strong></td><td style="text-align:left;"><code>net/ipv6/netfilter/ip6table_security.c</code></td></tr></tbody></table><h4 id="table-9-4-netfilter-hook-priorities" tabindex="-1">Table 9-4: Netfilter Hook Priorities <a class="header-anchor" href="#table-9-4-netfilter-hook-priorities" aria-label="Permalink to &quot;Table 9-4: Netfilter Hook Priorities&quot;">​</a></h4><p>This is the key to whether your hook function runs before or after <code>conntrack</code>. The smaller the value, the higher the priority (the earlier it executes).</p><table tabindex="0"><thead><tr><th style="text-align:left;">Priority Macro</th><th style="text-align:left;">Value</th><th style="text-align:left;">Typical Purpose</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>NF_IP_PRI_FIRST</strong></td><td style="text-align:left;"><code>INT_MIN</code></td><td style="text-align:left;">Executes first, for certain operations that must preempt others.</td></tr><tr><td style="text-align:left;"><strong>NF_IP_PRI_CONNTRACK_DEFRAG</strong></td><td style="text-align:left;">-400</td><td style="text-align:left;"><strong>Defragmentation</strong>. Must reassemble packets before connection tracking.</td></tr><tr><td style="text-align:left;"><strong>NF_IP_PRI_RAW</strong></td><td style="text-align:left;">-300</td><td style="text-align:left;"><code>raw</code> table. Used for <code>-j NOTRACK</code>, to intercept packets before tracking.</td></tr><tr><td style="text-align:left;"><strong>NF_IP_PRI_CONNTRACK</strong></td><td style="text-align:left;">-200</td><td style="text-align:left;"><strong>Connection Tracking</strong>. This is the most critical step.</td></tr><tr><td style="text-align:left;"><strong>NF_IP_PRI_MANGLE</strong></td><td style="text-align:left;">-150</td><td style="text-align:left;"><code>mangle</code> table. Modifies packet headers.</td></tr><tr><td style="text-align:left;"><strong>NF_IP_PRI_NAT_DST</strong></td><td style="text-align:left;">-100</td><td style="text-align:left;"><strong>DNAT</strong>. Destination address translation (usually before routing).</td></tr><tr><td style="text-align:left;"><strong>NF_IP_PRI_FILTER</strong></td><td style="text-align:left;">0</td><td style="text-align:left;"><code>filter</code> table. This is the default iptables filtering point.</td></tr><tr><td style="text-align:left;"><strong>NF_IP_PRI_SECURITY</strong></td><td style="text-align:left;">50</td><td style="text-align:left;"><code>security</code> table.</td></tr><tr><td style="text-align:left;"><strong>NF_IP_PRI_NAT_SRC</strong></td><td style="text-align:left;">100</td><td style="text-align:left;"><strong>SNAT</strong>. Source address translation (usually after routing).</td></tr><tr><td style="text-align:left;"><strong>NF_IP_PRI_CONNTRACK_HELPER</strong></td><td style="text-align:left;">300</td><td style="text-align:left;"><strong>Helper</strong>. Auxiliary connection handling for FTP/SIP.</td></tr><tr><td style="text-align:left;"><strong>NF_IP_PRI_LAST</strong></td><td style="text-align:left;"><code>INT_MAX</code></td><td style="text-align:left;">Executes last.</td></tr></tbody></table><p><em>Defined at: <code>include/uapi/linux/netfilter_ipv4.h</code></em></p><hr><h3 id="tools-and-libraries" tabindex="-1">Tools and Libraries <a class="header-anchor" href="#tools-and-libraries" aria-label="Permalink to &quot;Tools and Libraries&quot;">​</a></h3><p>Finally, let&#39;s step back from kernel code into the real world. Debugging Netfilter requires more than just <code>printk</code>; you need a capable set of tools.</p><h4 id="conntrack-tools" tabindex="-1">conntrack-tools <a class="header-anchor" href="#conntrack-tools" aria-label="Permalink to &quot;conntrack-tools&quot;">​</a></h4><p>This is the official userspace toolset provided by Netfilter, consisting of two parts:</p><ol><li><strong><code>conntrack</code></strong>: Command-line tool. <ul><li><strong>Purpose</strong>: Directly view the connection tracking table.</li><li><strong>Common commands</strong>: <ul><li><code>conntrack -L</code>: List all current connections (like looking at a police station&#39;s roster of registered suspects).</li><li><code>conntrack -D &lt;ip&gt;</code>: Delete all connections for a specific IP (sometimes connections get stuck and need to be manually severed).</li></ul></li></ul></li><li><strong><code>conntrackd</code></strong>: Daemon. <ul><li><strong>Purpose</strong>: Used for connection synchronization (e.g., in active-passive load balancing scenarios, where the conntrack tables on two machines must stay consistent) and event logging.</li></ul></li></ol><h4 id="libnetfilter-libraries" tabindex="-1">libnetfilter_* Libraries <a class="header-anchor" href="#libnetfilter-libraries" aria-label="Permalink to &quot;libnetfilter_* Libraries&quot;">​</a></h4><p>If you want to write C programs to manipulate Netfilter instead of writing kernel modules directly, you need these libraries:</p><ul><li><strong>libnetfilter_conntrack</strong>: Manipulate the connection tracking table in userspace (the <code>conntrack</code> command is written using this library).</li><li><strong>libnetfilter_queue</strong>: Queue packets from the kernel to userspace for processing (commonly used in complex IDS/IPS).</li><li><strong>libnetfilter_log</strong>: Retrieve logged packets from the kernel.</li></ul><p><strong>Official resource</strong>: For details, see the <a href="http://www.netfilter.org/" target="_blank" rel="noreferrer">Netfilter official website</a>.</p><hr><h3 id="chapter-echoes" tabindex="-1">Chapter Echoes <a class="header-anchor" href="#chapter-echoes" aria-label="Permalink to &quot;Chapter Echoes&quot;">​</a></h3><p>In this chapter, we dismantled the &quot;control hub&quot; of the Linux network subsystem—Netfilter.</p><p>Remember the suspense we left at the beginning? How can <code>iptables</code> calmly decide whether a packet should stay, be disguised, or be dropped the moment it flies past the network card?</p><p>The answer isn&#39;t in the iptables commands, but in the kernel&#39;s five Hook points and that massive hash table. The essence of Netfilter is a <strong>collaborative framework</strong>: it defines a set of rules (Hooks) that allow various independent modules—firewalls, NAT, connection tracking, or even your own custom modules—to intercept the packet&#39;s path without modifying the main network stack code.</p><p>We saw how connection tracking acts like a &quot;seasoned detective,&quot; not just looking at the packet in front of it, but remembering who is &quot;talking&quot; to whom (Tuple); we saw how NAT quietly modifies the addresses in the packet header based on this memory, enabling conversations between internal and external networks; and we saw how Helpers act like &quot;special task forces&quot; to handle complex protocols like FTP that not only talk but also &quot;open another room.&quot;</p><p>Most importantly, we established a <strong>priority- and extension-based mindset</strong>. All modules queue up on the same chain; priority determines who acts first, and the extension mechanism determines who can sneak custom data into the file.</p><p>In the next chapter, we&#39;ll leave this complex network logic behind and enter a more foundational domain—<strong>Socket Buffers (SKB)</strong>. If Netfilter is the brain&#39;s decision-making logic, then SKB is the data blood that carries the information. Only by understanding the structure of SKB can you truly understand the physical form of Linux network data.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: Understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: Understanding&quot;">​</a></h3><p><strong>Question</strong>: Suppose you need to intercept and analyze all IPv4 packets on a Linux server that are &quot;about to arrive at the local machine but have not yet undergone a routing lookup.&quot; Based on the Netfilter hook workflow, at which hook point should you register your callback function? Please specify which specific function in the kernel network stack this hook point is located in.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: NF_INET_PRE_ROUTING, located in the ip_rcv() function.</p><p><strong>Analysis</strong>: According to the definition of Netfilter hooks, NF_INET_PRE_ROUTING is the first hook point reached by all inbound packets. At this point, the packet has just entered the kernel via the network card driver and is undergoing initial protocol stack processing, but has not yet undergone a routing lookup (which determines whether to deliver it locally or forward it to another host). Therefore, to intercept all inbound packets, this is the earliest opportunity. The text explicitly states that this hook is located in the IPv4 ip_rcv() method.</p></details><h3 id="exercise-2-application" tabindex="-1">Exercise 2: Application <a class="header-anchor" href="#exercise-2-application" aria-label="Permalink to &quot;Exercise 2: Application&quot;">​</a></h3><p><strong>Question</strong>: If an administrator wants to write a Netfilter kernel module to modify the IP headers of all &quot;locally generated and about to be sent&quot; packets (e.g., to implement a transparent proxy or IP masquerading), at which Netfilter hook point should the module register? What is the corresponding kernel function?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: NF_INET_LOCAL_OUT, located in the __ip_local_out() function.</p><p><strong>Analysis</strong>: Locally generated outbound packets, after a routing lookup determines the output interface but before finally leaving the kernel for the network card driver, pass through the NF_INET_LOCAL_OUT hook. This is the optimal location to modify the contents of locally sent packets (such as performing SNAT or modifying ports). The text indicates that this hook is located in the __ip_local_out() method, processing before NF_INET_POST_ROUTING (ip_output), at which point the packet has already passed the local routing lookup.</p></details><h3 id="exercise-3-application" tabindex="-1">Exercise 3: Application <a class="header-anchor" href="#exercise-3-application" aria-label="Permalink to &quot;Exercise 3: Application&quot;">​</a></h3><p><strong>Question</strong>: In the Linux kernel&#39;s connection tracking mechanism, when a packet arrives for the first time, the system creates a new <code>nf_conn</code> entry. Please describe the intermediate state of this new entry from creation to正式生效 (insertion into the hash table), and which function is responsible for transitioning it from the &quot;intermediate state&quot; to the &quot;officially effective&quot; state.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: The intermediate state is called the &quot;unconfirmed&quot; state, where the entry resides on the network namespace&#39;s unconfirmed list. The <code>__nf_conntrack_confirm()</code> function is responsible for moving it from the unconfirmed list into the hash table.</p><p><strong>Analysis</strong>: When <code>resolve_normal_ct()</code> creates a new connection, it calls <code>init_conntrack()</code>. At this point, the entry is not immediately placed into the main hash table, but rather into the <code>unconfirmed</code> list of <code>netns_ct</code>. This is done to prevent the hash table from being polluted by invalid entries if the packet is subsequently dropped (e.g., due to a checksum error). Only when the packet successfully passes subsequent validation (typically in the <code>NF_INET_POST_ROUTING</code> or <code>NF_INET_LOCAL_IN</code> hook), the <code>ipv4_confirm</code> callback calls <code>__nf_conntrack_confirm()</code> to officially insert the entry into the hash table and remove it from the unconfirmed list.</p></details><h3 id="exercise-4-thinking" tabindex="-1">Exercise 4: Thinking <a class="header-anchor" href="#exercise-4-thinking" aria-label="Permalink to &quot;Exercise 4: Thinking&quot;">​</a></h3><p><strong>Question</strong>: Before actual data transfer, the FTP protocol negotiates a dynamic port through the control channel for the data connection. Simple connection tracking (matching only IP and port) cannot correctly associate such dynamic data flows. Combining your knowledge from this chapter, analyze how the Netfilter subsystem uses the &quot;connection tracking helper module&quot; and &quot;expected connection&quot; mechanisms to solve the problem of FTP data packets being incorrectly dropped?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Netfilter uses a dedicated Helper (such as nf_conntrack_ftp) to parse the control channel&#39;s payload, extract the port about to be negotiated, and create an &quot;Expectation&quot; for it. When subsequent packets matching this characteristic arrive, the kernel marks them with the <code>RELATED</code> state instead of treating them as invalid new connections.</p><p><strong>Analysis</strong>: For complex protocols like FTP, the packet payload in the control connection (e.g., port 21) contains the address and port information for the subsequent data connection. The <code>nf_conntrack_ftp</code> helper loaded in the kernel performs deep inspection of these packets, reading the PORT or PASV commands in the payload. Based on this information, the kernel creates an <code>nf_conntrack_expect</code> structure, telling the kernel: &quot;A connection to port X is coming soon, and it belongs to the current control connection.&quot; When that data packet actually arrives, the connection tracking system finds that it matches the Expectation, thereby establishing the association and marking the connection state as <code>IPS_EXPECTED</code> (expected connection). This way, firewall rules (like <code>iptables -m conntrack --ctstate RELATED</code>) can recognize and allow this dynamic packet, rather than treating it as an unauthorized inbound connection and dropping it.</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>The Netfilter framework allows kernel modules to intercept packets at these checkpoints by pre-setting five &quot;hooks&quot; (such as <code>PRE_ROUTING</code> and <code>LOCAL_IN</code>) on the critical paths of the protocol stack. Whether implementing firewall filtering, NAT translation, or more advanced IPVS load balancing, it essentially boils down to calling the <code>NF_HOOK</code> macro to register callback functions at these stubs and returning verdicts like NF_ACCEPT or NF_DROP, thereby intervening in the fate of traffic without modifying the mainline code.</p><p>Connection tracking is the cornerstone of NAT and stateful firewalls. It records tuple information for bidirectional communication through a hash table, transforming the stateless network stack into stateful session management. This mechanism has a very high priority, intervening before packets enter routing decisions; it creates <code>struct nf_conn</code> entries when it sees new traffic, and quickly matches subsequent arriving packets to update state or timeouts. Only when a packet finally passes all checks is the entry officially confirmed and written to the global hash table.</p><p>Netfilter is not a single firewall, but an extensible foundation supporting upper-layer functionalities like IPVS (L4 load balancing), IP sets (efficiently managing massive IP sets), and iptables. IPVS leverages hooks to modify forwarding destination addresses for traffic distribution, while IP sets optimizes matching performance through kernel hash tables, solving the efficiency bottleneck of traditional firewalls when handling large numbers of addresses.</p><p>To address the multi-channel problems of complex protocols like FTP, Netfilter introduces the connection tracking helper mechanism. These helpers act as &quot;protocol interpreters,&quot; listening for specific patterns in the control channel (like FTP&#39;s PORT command) and dynamically creating &quot;expected connection&quot; records. This allows the subsequently initiated data connections to be associated with the main connection via the <code>master</code> pointer, and recognized by the firewall as being in the RELATED state for automatic allowance, solving the challenge of dynamic ports traversing the firewall.</p><p>As a userspace frontend tool, the core role of iptables is to push rules down to the kernel via <code>setsockopt</code>. The tables in the kernel (like the filter table) then register to the corresponding Netfilter hooks based on protocol family and priority. Although iptables is currently mainstream, the kernel has evolved toward nftables. The latter solves the code duplication and rule update performance issues of the old architecture through a unified codebase and a virtual machine execution engine.</p>',75)])])}const p=t(a,[["render",r]]);export{g as __pageData,p as default};
