import{_ as i,o as e,c as a,a as n}from"./chunks/framework.8tiMeCK-.js";const k=JSON.parse('{"title":"6.3  Multicast Router","description":"","frontmatter":{},"headers":[],"relativePath":"notes/linux_kernel_networking/ch06_3.md","filePath":"notes/linux_kernel_networking/ch06_3.md","lastUpdated":null}'),p={name:"notes/linux_kernel_networking/ch06_3.md"};function t(l,s,d,o,c,r){return e(),a("div",null,[...s[0]||(s[0]=[n(`<h1 id="_6-3-multicast-router" tabindex="-1">6.3 Multicast Router <a class="header-anchor" href="#_6-3-multicast-router" aria-label="Permalink to &quot;6.3  Multicast Router&quot;">​</a></h1><p>上一节我们拆解了内核里的 <code>mr_table</code> 和 <code>mfc_cache</code>——那是组播转发的<strong>骨架</strong>。</p><p>但这骨架是死的。它躺在那儿，等着谁来推它一把，告诉它哪些接口应该参与转发，哪些数据包该往哪走。</p><p>这一节，我们要聊那个「推手」：<strong>Multicast Router（组播路由器）</strong>。</p><p>你会看到它是如何在用户空间和内核之间建立连接的，以及那个关键的握手协议——<code>setsockopt</code>。</p><hr><h3 id="配置组播路由器" tabindex="-1">配置组播路由器 <a class="header-anchor" href="#配置组播路由器" aria-label="Permalink to &quot;配置组播路由器&quot;">​</a></h3><p>要把一台 Linux 机器变成组播路由器，光有内核代码是不够的。</p><p>首先，你得在编译内核时把 <code>CONFIG_IP_MROUTE</code> 这个选项打开。这就像给车装上四驱套件——没这个，后面的事都免谈。</p><p>其次，你得在用户空间跑一个<strong>懂行的守夜人</strong>。</p><p>前面我们提过 <code>pimd</code> 或者 <code>mrouted</code>。这些守护进程不是瞎跑的，它们干的第一件事，就是跟内核建立一条「热线」。</p><p>这条热线的建立方式非常经典：创建一个 Raw Socket。</p><p>以 <code>pimd</code> 为例，它在启动时会调用这样一个系统调用：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">socket</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(AF_INET, SOCK_RAW, IPPROTO_IGMP);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>这行代码的意思是：「内核，给我开一个直通 IGMP 协议层的后门，我不想要 TCP/UDP 那套封装，我要直接发 IGMP 消息。」</p><p>拿到这个 socket 文件描述符后，真正的控制才刚刚开始。</p><hr><h4 id="握手-mrt-init" tabindex="-1">握手：MRT_INIT <a class="header-anchor" href="#握手-mrt-init" aria-label="Permalink to &quot;握手：MRT_INIT&quot;">​</a></h4><p>守护进程有了 socket，它怎么告诉内核「我想启用组播路由」？</p><p>答案是：<code>setsockopt()</code>。</p><p>这不是普通的参数设置，这是一次<strong>身份声明</strong>。</p><p>当守护进程调用 <code>setsockopt(sock, IPPROTO_IP, MRT_INIT, ...)</code> 时，信号直接传到了内核的 <code>ip_mroute_setsockopt()</code> 方法里。</p><p>内核在处理 <code>MRT_INIT</code> 命令时，会做两件极其重要的事：</p><ol><li><strong>保存接头暗号</strong>：内核会把当前这个 socket 的引用，小心翼翼地存到 <code>mr_table</code> 结构体的 <code>mroute_sk</code> 字段里。这意味着，从这一刻起，内核认定了这个 socket 是唯一的「总指挥」。</li><li><strong>拉下电闸</strong>：内核会自动将 <code>/proc/sys/net/ipv4/conf/all/mc_forwarding</code> 这个 procfs 条目设为 1（通过宏 <code>IPV4_DEVCONF_ALL(net, MC_FORWARDING)++</code>）。</li></ol><p>等等，这里有个细节值得玩味。</p><p>那个 <code>mc_forwarding</code> 文件，你在 <code>/proc</code> 里能看到它是<strong>只读</strong>的。你没法用 <code>echo 1 &gt; ...</code> 去改它。</p><p>为什么？因为它是<strong>状态，不是配置</strong>。</p><p>它是内核根据「有没有组播路由守护进程在运行」自动判断出来的。只有在 <code>MRT_INIT</code> 发生时，它才会翻转；守护进程一退出，它就会自动归零。这种设计防止了你在没有守护进程的情况下强行开启转发，导致内核行为错乱。</p><hr><h4 id="独占性-一次只能有一个老大" tabindex="-1">独占性：一次只能有一个老大 <a class="header-anchor" href="#独占性-一次只能有一个老大" aria-label="Permalink to &quot;独占性：一次只能有一个老大&quot;">​</a></h4><p>既然提到了 <code>mroute_sk</code>，这里有一个硬性规定。</p><p><strong>同一时间，只能有一个组播路由守护进程存在。</strong></p><p>你可以想象一下，如果两个 <code>pimd</code> 同时在运行，一个说「往左转」，一个说「往右转」，内核听谁的？</p><p>所以，在 <code>ip_mroute_setsockopt()</code> 处理 <code>MRT_INIT</code> 的时候，第一件事就是检查 <code>mroute_sk</code> 是否已经被占用：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (mrt</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mroute_sk) </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">EADDRINUSE;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>如果已经有 socket 占了坑，你试图启动第二个守护进程，内核会直接甩你一个 <code>-EADDRINUSE</code>（Address already in use）。这是内核在说：「这地儿有人了，一边凉快去。」</p><hr><h4 id="添加接口-mrt-add-vif" tabindex="-1">添加接口：MRT_ADD_VIF <a class="header-anchor" href="#添加接口-mrt-add-vif" aria-label="Permalink to &quot;添加接口：MRT_ADD_VIF&quot;">​</a></h4><p>一旦初始化完成，守护进程接下来的任务就是把物理网卡注册到组播路由表里。</p><p>这不再是干喊口号，而是要给每个网卡办一张「入场券」。</p><p>入场券的办理方式，还是靠 <code>setsockopt()</code>，不过这次命令换成了 <code>MRT_ADD_VIF</code>（Virtual Interface Add）。</p><p>你需要填一张表——<code>struct vifctl</code>。</p><p>这就是那张「表」的样子：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> vifctl {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    vifi_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    vifc_vifi;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                /* 虚拟接口的索引 ID */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> vifc_flags;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           /* 各种标志位 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> vifc_threshold;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       /* TTL 阈值限制 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> vifc_rate_limit;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       /* 流量限速值（未实现） */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    union</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> in_addr vifc_lcl_addr;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   /* 本地接口 IP 地址 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            vifc_lcl_ifindex;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 本地接口索引 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    };</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> in_addr vifc_rmt_addr;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       /* 隧道远端地址（如果是隧道的话）*/</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><p>这张表里的字段非常精简，但每一个都直指要害。我们来拆几个关键的：</p><ul><li><strong><code>vifc_vifi</code></strong>：这是你给这个接口起的「代号」。内核不关心你叫它什么，只要它是 0 到 31 之间的唯一整数。</li><li><strong><code>vifc_flags</code></strong>：这里决定了这张入场券的性质。 <ul><li><code>VIFF_TUNNEL</code>：如果你打上这个标志，说明你要把数据包封装在另一个 IP 包里传输（IPIP 隧道）。这常用于跨越不支持组播的公网。</li><li><code>VIFF_REGISTER</code>：这是 PIM-SM（稀疏模式）专用的标志位，用来注册一个特殊的接口。</li><li><code>VIFF_USE_IFINDEX</code>：默认情况下，我们用 IP 地址来指代接口。但现在的机器网卡多，IP 变得不可靠（有可能一个网卡没配 IP）。如果你设了这个标志，<code>union</code> 里就会改用 <code>vifc_lcl_ifindex</code>（网卡索引）来定位设备。这是 2.6.33 内核以后才有的高级特性。</li></ul></li><li><strong><code>vifc_lcl_addr</code> vs <code>vifc_lcl_ifindex</code></strong>：这是一个 <code>union</code>。也就是说，你要么用 IP 地址找设备，要么用网卡索引找设备，二选一。推荐用索引，更稳。</li><li><strong><code>vifc_rmt_addr</code></strong>：如果是隧道模式，这里填的是隧道那头的机器 IP。</li></ul><p>守护进程填好这个结构体，通过 <code>setsockopt</code> 扔给内核，内核就会调用 <code>vif_add()</code> 把这个设备挂到路由表里。</p><p>如果要删除呢？</p><p>把命令换成 <code>MRT_DEL_VIF</code>，填上 <code>vifc_vifi</code>，内核就会调用 <code>vif_delete()</code> 把它摘下来。</p><hr><h4 id="收尾-mrt-done" tabindex="-1">收尾：MRT_DONE <a class="header-anchor" href="#收尾-mrt-done" aria-label="Permalink to &quot;收尾：MRT_DONE&quot;">​</a></h4><p>当守护进程决定退休（无论是正常退出还是被 <code>kill</code> 掉），它必须做一个有始有终的人。</p><p>最后一次调用 <code>setsockopt</code>，命令是 <code>MRT_DONE</code>。</p><p>这会触发内核的 <code>mrtsock_destruct()</code> 方法。</p><p>这个名字很有意思——<code>destruct</code>（析构）。</p><p>它会干两件事：</p><ol><li>把 <code>mr_table</code> 里的 <code>mroute_sk</code> 清空（设为 NULL），表示「老大走了，现在群龙无首」。</li><li>把 <code>mc_forwarding</code> 的状态改回 0，关掉组播转发。</li></ol><p>到这一步，这台机器就从「路由器」退化回了一台普通的「主机」。</p><hr><h3 id="the-vif-device-虚拟接口设备" tabindex="-1">The Vif Device（虚拟接口设备） <a class="header-anchor" href="#the-vif-device-虚拟接口设备" aria-label="Permalink to &quot;The Vif Device（虚拟接口设备）&quot;">​</a></h3><p>上一节我们提到的 <code>vifctl</code>，是用户空间填的「申请表」。</p><p>而内核真正用的，是 <code>struct vif_device</code>。这才是那个活在内核里、干活的「对象」。</p><p>组播路由支持两种模式：<strong>直连组播</strong> 和 <strong>隧道组播</strong>。</p><p>无论是哪种，内核都用同一个结构体——<code>vif_device</code> 来表示。</p><p>你可以把它想象成一个高级网卡驱动抽象。如果它是隧道模式（<code>VIFF_TUNNEL</code> 被设置），那它的 <code>dev</code> 指针指的可能就是那个虚拟的 <code>tunl0</code> 设备。</p><p>让我们看看它的真面目：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> vif_device {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device  </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dev;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        /* 正在使用的真实网卡设备 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      bytes_in, bytes_out;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      pkt_in, pkt_out;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     /* 统计信息：收发了多少包，多少字节 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      rate_limit;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          /* 流量整形（暂未实现）*/</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      threshold;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           /* TTL 阈值 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> short</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">     flags;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               /* 控制标志位 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __be32             local, remote;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       /* 地址：local 是本地地址，remote 是隧道远端地址 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                link;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                /* 底层物理接口的索引 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>大部分字段都一目了然——它就是一个增强版的网卡描述符。</p><p>这里有两个细节要注意：</p><ol><li><p><strong><code>dev_set_allmulti(dev, 1)</code></strong>： 当调用 <code>vif_add()</code> 添加接口时，内核不仅分配这个 <code>vif_device</code> 结构体，还会调用 <code>dev_set_allmulti(dev, 1)</code>。 这一行代码的作用是：把底层真实网卡的 <code>allmulti</code> 计数器加 1。 这就像告诉网卡驱动：「兄弟，从现在开始，别光帮我收单播包了，把路过的<strong>所有组播包</strong>都往上递，我要自己挑。」 如果没有这一步，网卡会在硬件层把不属于本机的组播包直接丢掉，内核根本没机会转发。</p></li><li><p><strong>清理工作</strong>： 当调用 <code>vif_delete()</code> 时，必须调用 <code>dev_set_allmulti(dev, -1)</code> 把计数器减回去。这是一种礼貌——「我不干组播了，你别再傻乎乎地收所有垃圾包了，恢复节电模式吧。」</p></li></ol><hr><h3 id="主机视角-加入与离开组" tabindex="-1">主机视角：加入与离开组 <a class="header-anchor" href="#主机视角-加入与离开组" aria-label="Permalink to &quot;主机视角：加入与离开组&quot;">​</a></h3><p>上面说的都是「路由器」干的事——转发数据包。</p><p>但如果这台机器自己也想看组播流（比如它是个视频服务器，既转发流，自己也得解码看看画面），它就得变回「主机」身份，<strong>加入</strong>一个组播组。</p><p>这个过程是应用层发起的。</p><p>你在 C 语言里写网络编程，这是标准操作：</p><ol><li>创建一个 socket。</li><li>调用 <code>setsockopt(socket, IPPROTO_IP, IP_ADD_MEMBERSHIP, ...)</code>。</li></ol><p>这里的 <code>IP_ADD_MEMBERSHIP</code> 就是一个申请单。</p><p>应用层会准备一个 <code>struct ip_mreq</code> 结构体（定义在 <code>&lt;netinet/in.h&gt;</code> 里），填上两个东西：</p><ul><li><code>imr_multiaddr</code>：你想加入的那个组播组的地址（比如 <code>239.1.1.1</code>）。</li><li><code>imr_interface</code>：你本地网卡的身份（IP 地址），告诉内核「我想用这张网卡去听」。</li></ul><p>这个系统调用进入内核后，最终会落到 <code>net/ipv4/igmp.c</code> 里的 <code>ip_mc_join_group()</code> 方法。</p><p><strong><code>ip_mc_join_group()</code> 的核心逻辑很简单</strong>： 它会把你要加入的那个组播地址，挂到网卡结构体（<code>in_device</code>）的 <code>mc_list</code> 链表上。</p><p>同时，内核会通过 IGMP 协议，向网络里发一个「报告」，告诉路由器：「嘿，我在这儿，我对 <code>239.1.1.1</code> 感兴趣，有流量往这发。」</p><p>当然，有进就有出。</p><p>当你不想看了，调用 <code>setsockopt</code> 并指定 <code>IP_DROP_MEMBERSHIP</code>。内核会调用 <code>ip_mc_leave_group()</code>，把那个地址从 <code>mc_list</code> 里摘下来，并通知路由器「我退群了」。</p><hr><h4 id="限额-20-个上限" tabindex="-1">限额：20 个上限 <a class="header-anchor" href="#限额-20-个上限" aria-label="Permalink to &quot;限额：20 个上限&quot;">​</a></h4><p>这里有一个你可能不太注意的限制。</p><p><strong>一个 socket，最多只能加入 20 个组播组。</strong></p><p>这个限制硬编码在内核的 <code>sysctl_igmp_max_memberships</code> 里。</p><p>如果你贪心，试图用同一个 socket 加入第 21 个组，<code>ip_mc_join_group()</code> 会毫不留情地返回 <code>-ENOBUFS</code>（No buffer space available）。</p><p>为什么是 20？</p><p>这是历史包袱和资源平衡的产物。每个成员资格都要占内核内存，还要维护定时器。对于普通应用来说，20 个绰绰有余；如果你真的需要加入几百个组（比如做组播网关），那你应该开多个 socket 分担压力，或者去改 <code>/proc/sys/net/ipv4/igmp_max_memberships</code> 的默认值。</p><p>到这里，我们清楚了： 内核里有一张<strong>路由表</strong>，用户空间有个<strong>守护进程</strong>，中间用 <code>setsockopt</code> 串起来。</p><p>但光配置好有什么用？数据包真的来了，它是怎么流动的？</p><p>下一节，我们会切入到 Rx Path，看看当一个组播数据包抵达网卡时，内核是如何一步步把它「端」到正确的输出队列里的。</p>`,96)])])}const g=i(p,[["render",t]]);export{k as __pageData,g as default};
