import{_ as e,o,c as d,a as l}from"./chunks/framework.8tiMeCK-.js";const _=JSON.parse('{"title":"第 5 章  IPv4 路由子系统","description":"","frontmatter":{},"headers":[],"relativePath":"notes/linux_kernel_networking/ch05_8.md","filePath":"notes/linux_kernel_networking/ch05_8.md","lastUpdated":null}'),i={name:"notes/linux_kernel_networking/ch05_8.md"};function n(r,t,c,a,s,g){return o(),d("div",null,[...t[0]||(t[0]=[l('<h1 id="第-5-章-ipv4-路由子系统" tabindex="-1">第 5 章 IPv4 路由子系统 <a class="header-anchor" href="#第-5-章-ipv4-路由子系统" aria-label="Permalink to &quot;第 5 章  IPv4 路由子系统&quot;">​</a></h1><h2 id="_5-8-快速参考面板-quick-reference" tabindex="-1">5.8 快速参考面板（Quick Reference） <a class="header-anchor" href="#_5-8-快速参考面板-quick-reference" aria-label="Permalink to &quot;5.8  快速参考面板（Quick Reference）&quot;">​</a></h2><p>写代码的时候，IDE 里的自动补全很好用，但当你在大段内核代码里跳来跳去时，或者在 <code>grep</code> 结果里翻找时，你需要的是一个更直接的东西——一个贴在墙上的「速查表」。</p><p>这一节就是那张表。</p><p>这里没有故事，没有铺垫，只有我们在这一章里拆解过的那些核心数据结构、API 函数和宏定义。它们散落在 <code>net/ipv4</code> 的几个关键文件里，现在我把它们聚在一起，方便你在未来的调试（或者填坑）生涯中快速检索。</p><h3 id="模块与文件清单" tabindex="-1">模块与文件清单 <a class="header-anchor" href="#模块与文件清单" aria-label="Permalink to &quot;模块与文件清单&quot;">​</a></h3><p>首先，我们把「案发现场」的位置再确认一遍。IPv4 路由子系统的实现主要集中在以下模块：</p><ul><li><strong><code>fib_frontend.c</code></strong>: FIB 的「前台」，处理用户空间来的请求（比如 <code>ip route add</code>）。</li><li><strong><code>fib_trie.c</code></strong>: 核心查找逻辑，也就是那棵高效的 LC-trie 树住的地方。</li><li><strong><code>fib_semantics.c</code></strong>: 处理 FIB 条目的语义，比如 <code>fib_info</code> 的管理。</li><li><strong><code>route.c</code></strong>: 路由缓存的老家（虽然现代内核里缓存机制已经大变样，但核心依然在这里）。</li><li><strong><code>fib_rules.c</code></strong>: 策略路由的实现者。注意，这个模块只有在你开启了 <code>CONFIG_IP_MULTIPLE_TABLES</code> 时才会被编译进去——如果你需要基于源地址或别的条件来做路由决策，这就是你需要的开关。</li></ul><p>代码之外，这几个头文件值得你加到书签里：</p><ul><li><code>include/net/ip_fib.h</code>: FIB 的核心定义。</li><li><code>fib_lookup.h</code>: 查找逻辑的头文件。</li><li><code>include/net/route.h</code>: 路由层与更高层的接口。</li></ul><p>别忘了目的缓存（<code>dst_entry</code>）的通用实现，它住在 <code>net/core/dst.c</code> 和 <code>include/net/dst.h</code> 里。</p><hr><h3 id="核心-api-方法" tabindex="-1">核心 API 方法 <a class="header-anchor" href="#核心-api-方法" aria-label="Permalink to &quot;核心 API 方法&quot;">​</a></h3><p>以下是本章提到的关键函数。如果你在阅读源码或写自己的内核模块时需要和 FIB 打交道，这些大概率是你需要调用的接口。</p><h4 id="路由表操作" tabindex="-1"><strong>路由表操作</strong> <a class="header-anchor" href="#路由表操作" aria-label="Permalink to &quot;**路由表操作**&quot;">​</a></h4><ul><li><p><strong><code>int fib_table_insert(struct fib_table *tb, struct fib_config *cfg);</code></strong></p><ul><li><strong>做什么</strong>：向指定的 FIB 表（<code>tb</code>）插入一条路由。</li><li><strong>参数</strong>：<code>cfg</code> 包含了用户空间传下来的路由配置（目标地址、网关、掩码等）。</li><li><strong>注意</strong>：这是 Netlink socket 触发 <code>ip route add</code> 后最终落入内核的那一跳。</li></ul></li><li><p><strong><code>int fib_table_delete(struct fib_table *tb, struct fib_config *cfg);</code></strong></p><ul><li><strong>做什么</strong>：从指定的 FIB 表中删除一条路由。</li><li><strong>对应命令</strong>：<code>ip route del</code>。</li></ul></li><li><p><strong><code>struct fib_table *fib_trie_table(u32 id);</code></strong></p><ul><li><strong>做什么</strong>：分配并初始化一个基于 TRIE 结构的 FIB 路由表。如果你想创建一个自定义的路由表（不仅仅是 <code>local</code> 或 <code>main</code>），这是你要找的构造函数。</li></ul></li></ul><h4 id="fib-条目管理" tabindex="-1"><strong>FIB 条目管理</strong> <a class="header-anchor" href="#fib-条目管理" aria-label="Permalink to &quot;**FIB 条目管理**&quot;">​</a></h4><ul><li><p><strong><code>struct fib_info *fib_create_info(struct fib_config *cfg);</code></strong></p><ul><li><strong>做什么</strong>：根据配置 <code>cfg</code> 构造一个 <code>fib_info</code> 对象。</li><li><strong>机制</strong>：这是路由条目的「肉」。它会检查是否已经有相同的 <code>fib_info</code> 存在（为了节省内存），如果没有才创建新的。</li></ul></li><li><p><strong><code>void free_fib_info(struct fib_info *fi);</code></strong></p><ul><li><strong>做什么</strong>：释放一个 <code>fib_info</code> 对象。</li><li><strong>条件</strong>：只有当引用计数归零（<code>fib_dead</code> 标志被设置）时，它才会真正被释放，并递减全局计数器 <code>fib_info_cnt</code>。</li></ul></li><li><p><strong><code>void fib_alias_accessed(struct fib_alias *fa);</code></strong></p><ul><li><strong>做什么</strong>：标记一个 <code>fib_alias</code> 条目为「已访问」。</li><li><strong>细节</strong>：它只是简单地把 <code>fa-&gt;fa_state</code> 设置为 <code>FA_S_ACCESSED</code>。这在垃圾回收或统计时可能会用到，用来区分哪些路由是「热」的。</li></ul></li></ul><h4 id="查找与-trie-遍历" tabindex="-1"><strong>查找与 TRIE 遍历</strong> <a class="header-anchor" href="#查找与-trie-遍历" aria-label="Permalink to &quot;**查找与 TRIE 遍历**&quot;">​</a></h4><ul><li><strong><code>struct leaf *fib_find_node(struct trie *t, u32 key);</code></strong><ul><li><strong>做什么</strong>：在 TR 树 <code>t</code> 中查找匹配 <code>key</code> 的节点。</li><li><strong>返回</strong>：成功返回 <code>leaf</code> 节点，失败返回 <code>NULL</code>。这是 LPM（最长前缀匹配）算法在数据结构层面的直接体现。</li></ul></li></ul><h4 id="重定向与异常处理" tabindex="-1"><strong>重定向与异常处理</strong> <a class="header-anchor" href="#重定向与异常处理" aria-label="Permalink to &quot;**重定向与异常处理**&quot;">​</a></h4><ul><li><p><strong><code>void ip_rt_send_redirect(struct sk_buff *skb);</code></strong></p><ul><li><strong>做什么</strong>：发送一个 ICMPv4 Redirect 消息。</li><li><strong>场景</strong>：当内核发现主机在走一条「冤枉路」（通过本机转发，但本机不是最佳下一跳）时，调用这个函数好心地告诉对方「别绕了，走那边」。</li></ul></li><li><p><strong><code>void __ip_do_redirect(struct rtable *rt, struct sk_buff *skb, struct flowi4 *fl4, bool kill_route);</code></strong></p><ul><li><strong>做什么</strong>：处理接收到的 ICMPv4 Redirect 消息。</li><li><strong>核心逻辑</strong>：这就是创建 FIB nexthop exception (FNHE) 的地方。如果 <code>kill_route</code> 为真，它会彻底干掉旧路由；否则，它会创建一个异常条目，让特定的流量绕过 FIB 表直奔新网关。</li></ul></li><li><p><strong><code>void update_or_create_fnhe(struct fib_nh *nh, __be32 daddr, __be32 gw, u32 pmtu, unsigned long expires);</code></strong></p><ul><li><strong>做什么</strong>：正如其名，更新或创建一个 FNHE。</li><li><strong>参数</strong>：指定了下一跳 <code>nh</code>、目标地址 <code>daddr</code>、新网关 <code>gw</code>、PMTU 以及过期时间。这是 Redirect 和 PMTU 发现机制修改路由行为的统一入口。</li></ul></li></ul><h4 id="度量值查询" tabindex="-1"><strong>度量值查询</strong> <a class="header-anchor" href="#度量值查询" aria-label="Permalink to &quot;**度量值查询**&quot;">​</a></h4><ul><li><strong><code>u32 dst_metric(const struct dst_entry *dst, int metric);</code></strong><ul><li><strong>做什么</strong>：从 <code>dst_entry</code> 中提取指定的度量值（比如 MTU、初始窗口等）。</li></ul></li></ul><hr><h3 id="核心宏定义" tabindex="-1">核心宏定义 <a class="header-anchor" href="#核心宏定义" aria-label="Permalink to &quot;核心宏定义&quot;">​</a></h3><p>内核代码里充满了宏，这里挑几个我们在解析 FIB 逻辑时遇到过的关键角色。</p><h4 id="fib-查找结果提取" tabindex="-1"><strong>FIB 查找结果提取</strong> <a class="header-anchor" href="#fib-查找结果提取" aria-label="Permalink to &quot;**FIB 查找结果提取**&quot;">​</a></h4><p>这些宏通常接收一个 <code>fib_result</code> 结构体作为参数，把查找结果里的关键字段抠出来。</p><ul><li><strong><code>FIB_RES_GW(res)</code></strong>: 返回下一跳的网关地址 (<code>nh_gw</code>)。</li><li><strong><code>FIB_RES_DEV(res)</code></strong>: 返回下一步要走的网络设备 (<code>net_device</code>)。</li><li><strong><code>FIB_RES_OIF(res)</code></strong>: 返回输出接口的索引 (<code>nh_oif</code>)。</li><li><strong><code>FIB_RES_NH(res)</code></strong>: 返回完整的 <code>fib_nh</code> 结构体。 <ul><li><em>细节</em>：如果开启了多路径路由，这里会根据 <code>res-&gt;nh_sel</code> 作为索引，从 <code>fib_info</code> 的 <code>nexthop</code> 数组里选出正确的那一个。</li></ul></li></ul><h4 id="设备行为检查" tabindex="-1"><strong>设备行为检查</strong> <a class="header-anchor" href="#设备行为检查" aria-label="Permalink to &quot;**设备行为检查**&quot;">​</a></h4><p>在决定「要不要转发」或「听不听话」之前，内核得先问问网卡的态度。</p><ul><li><strong><code>IN_DEV_FORWARD(in_dev)</code></strong>: 检查该设备是否开启了 IP 转发。如果这个关了，它就是个安静的终点，不是路由器。</li><li><strong><code>IN_DEV_RX_REDIRECTS(in_dev)</code></strong>: 检查该设备是否<strong>接收</strong> ICMP Redirect。如果你要做路由器，通常会关掉这个，防止被别人的 Redirect 搞乱。</li><li><strong><code>IN_DEV_TX_REDIRECTS(in_dev)</code></strong>: 检查该设备是否<strong>发送</strong> ICMP Redirect。</li></ul><h4 id="trie-树结构遍历" tabindex="-1"><strong>TRIE 树结构遍历</strong> <a class="header-anchor" href="#trie-树结构遍历" aria-label="Permalink to &quot;**TRIE 树结构遍历**&quot;">​</a></h4><ul><li><strong><code>IS_LEAF(node)</code></strong>: 判断这个节点是不是叶子节点（终点）。</li><li><strong><code>IS_TNODE(node)</code></strong>: 判断这个节点是不是内部节点（Trie Node，还要继续往下找）。</li></ul><h4 id="多路径路由迭代" tabindex="-1"><strong>多路径路由迭代</strong> <a class="header-anchor" href="#多路径路由迭代" aria-label="Permalink to &quot;**多路径路由迭代**&quot;">​</a></h4><ul><li><strong><code>change_nexthops(fi)</code></strong>: 定义在 <code>fib_semantics.c</code> 里的一个宏。它提供了一个遍历 <code>fib_info</code> 里所有 <code>nexthop</code> 的循环机制。当你需要检查每一条可能的路径时，你会用到它。</li></ul><hr><h3 id="关键数据表" tabindex="-1">关键数据表 <a class="header-anchor" href="#关键数据表" aria-label="Permalink to &quot;关键数据表&quot;">​</a></h3><h4 id="表-5-1-路由度量值" tabindex="-1"><strong>表 5-1：路由度量值</strong> <a class="header-anchor" href="#表-5-1-路由度量值" aria-label="Permalink to &quot;**表 5-1：路由度量值**&quot;">​</a></h4><p>内核不仅管路怎么走，还管路上的路况。这里有 15 种（<code>RTAX_MAX</code>）度量值，其中一些是专门给 TCP 用的。</p><table tabindex="0"><thead><tr><th style="text-align:left;">Linux 符号</th><th style="text-align:center;">TCP 相关 (Y/N)</th><th style="text-align:left;">含义</th></tr></thead><tbody><tr><td style="text-align:left;"><code>RTAX_UNSPEC</code></td><td style="text-align:center;">N</td><td style="text-align:left;">未指定</td></tr><tr><td style="text-align:left;"><code>RTAX_LOCK</code></td><td style="text-align:center;">N</td><td style="text-align:left;">锁定度量值（防止被更新）</td></tr><tr><td style="text-align:left;"><code>RTAX_MTU</code></td><td style="text-align:center;">N</td><td style="text-align:left;">路径最大传输单元</td></tr><tr><td style="text-align:left;"><code>RTAX_WINDOW</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">TCP 广播窗口大小</td></tr><tr><td style="text-align:left;"><code>RTAX_RTT</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">往返时间</td></tr><tr><td style="text-align:left;"><code>RTAX_RTTVAR</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">往返时间方差</td></tr><tr><td style="text-align:left;"><code>RTAX_SSTHRESH</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">慢启动阈值</td></tr><tr><td style="text-align:left;"><code>RTAX_CWND</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">拥塞窗口</td></tr><tr><td style="text-align:left;"><code>RTAX_ADVMSS</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">对方 MSS (建议)</td></tr><tr><td style="text-align:left;"><code>RTAX_REORDERING</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">包重排序阈值</td></tr><tr><td style="text-align:left;"><code>RTAX_HOPLIMIT</code></td><td style="text-align:center;">N</td><td style="text-align:left;">跳数限制</td></tr><tr><td style="text-align:left;"><code>RTAX_INITCWND</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">初始拥塞窗口</td></tr><tr><td style="text-align:left;"><code>RTAX_FEATURES</code></td><td style="text-align:center;">N</td><td style="text-align:left;">特性标志位</td></tr><tr><td style="text-align:left;"><code>RTAX_RTO_MIN</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">最小重传超时时间</td></tr><tr><td style="text-align:left;"><code>RTAX_INITRWND</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">初始接收窗口</td></tr></tbody></table><p><em>(来源：<code>include/uapi/linux/rtnetlink.h</code>)</em></p><h4 id="表-5-2-路由类型与错误码" tabindex="-1"><strong>表 5-2：路由类型与错误码</strong> <a class="header-anchor" href="#表-5-2-路由类型与错误码" aria-label="Permalink to &quot;**表 5-2：路由类型与错误码**&quot;">​</a></h4><p>当路由查找命中时，不同的 <code>type</code> 意味着不同的命运。这里列出了 <code>fib_props</code> 数组中的映射关系：特定路由类型对应的错误码和作用域。</p><table tabindex="0"><thead><tr><th style="text-align:left;">Linux 符号</th><th style="text-align:left;">错误码</th><th style="text-align:left;">作用域</th><th style="text-align:left;">含义</th></tr></thead><tbody><tr><td style="text-align:left;"><code>RTN_UNSPEC</code></td><td style="text-align:left;">0</td><td style="text-align:left;"><code>RT_SCOPE_NOWHERE</code></td><td style="text-align:left;">未指定（通常不出现在最终结果里）</td></tr><tr><td style="text-align:left;"><code>RTN_UNICAST</code></td><td style="text-align:left;">0</td><td style="text-align:left;"><code>RT_SCOPE_UNIVERSE</code></td><td style="text-align:left;">普通单播路由</td></tr><tr><td style="text-align:left;"><code>RTN_LOCAL</code></td><td style="text-align:left;">0</td><td style="text-align:left;"><code>RT_SCOPE_HOST</code></td><td style="text-align:left;">本地地址</td></tr><tr><td style="text-align:left;"><code>RTN_BROADCAST</code></td><td style="text-align:left;">0</td><td style="text-align:left;"><code>RT_SCOPE_LINK</code></td><td style="text-align:left;">广播地址</td></tr><tr><td style="text-align:left;"><code>RTN_ANYCAST</code></td><td style="text-align:left;">0</td><td style="text-align:left;"><code>RT_SCOPE_LINK</code></td><td style="text-align:left;">任播地址</td></tr><tr><td style="text-align:left;"><code>RTN_MULTICAST</code></td><td style="text-align:left;">0</td><td style="text-align:left;"><code>RT_SCOPE_UNIVERSE</code></td><td style="text-align:left;">组播路由</td></tr><tr><td style="text-align:left;"><code>RTN_BLACKHOLE</code></td><td style="text-align:left;"><code>-EINVAL</code></td><td style="text-align:left;"><code>RT_SCOPE_UNIVERSE</code></td><td style="text-align:left;">黑洞（静默丢弃，不报错）</td></tr><tr><td style="text-align:left;"><code>RTN_UNREACHABLE</code></td><td style="text-align:left;"><code>-EHOSTUNREACH</code></td><td style="text-align:left;"><code>RT_SCOPE_UNIVERSE</code></td><td style="text-align:left;">不可达（丢弃并发送 ICMP 目标不可达）</td></tr><tr><td style="text-align:left;"><code>RTN_PROHIBIT</code></td><td style="text-align:left;"><code>-EACCES</code></td><td style="text-align:left;"><code>RT_SCOPE_UNIVERSE</code></td><td style="text-align:left;">禁止（被管理员拒绝，发送 ICMP 禁止访问）</td></tr><tr><td style="text-align:left;"><code>RTN_THROW</code></td><td style="text-align:left;"><code>-EAGAIN</code></td><td style="text-align:left;"><code>RT_SCOPE_UNIVERSE</code></td><td style="text-align:left;">也就是「继续查下一张表」（策略路由用）</td></tr><tr><td style="text-align:left;"><code>RTN_NAT</code></td><td style="text-align:left;"><code>-EINVAL</code></td><td style="text-align:left;"><code>RT_SCOPE_NOWHERE</code></td><td style="text-align:left;">NAT（旧用法）</td></tr><tr><td style="text-align:left;"><code>RTN_XRESOLVE</code></td><td style="text-align:left;"><code>-EINVAL</code></td><td style="text-align:left;"><code>RT_SCOPE_NOWHERE</code></td><td style="text-align:left;">需要外部解析（如通过 daemon）</td></tr></tbody></table><hr><h3 id="路由标志位" tabindex="-1">路由标志位 <a class="header-anchor" href="#路由标志位" aria-label="Permalink to &quot;路由标志位&quot;">​</a></h3><p>当你敲下 <code>route -n</code> 或 <code>ip route show</code> 时，输出里那一串缩写字母（<code>UG</code>, <code>UH</code>）并不是乱码。它们是内核对这条路由的「批注」。</p><p>以下是常见标志位的含义，对应表 5-3 的示例输出：</p><ul><li><strong><code>U</code> (Route is up)</strong>: 路由是激活的。</li><li><strong><code>H</code> (Target is a host)</strong>: 目标是一个具体的主机（通常掩码是 255.255.255.255）。</li><li><strong><code>G</code> (Use gateway)</strong>: 使用网关（数据包不是直接发到目标网段，而是要经过中间人）。</li><li><strong><code>R</code> (Reinstate route)</strong>: 恢复动态路由（被路由守护进程重启）。</li><li><strong><code>D</code> (Dynamically installed)</strong>: 动态安装（由 redirect 或守护进程创建）。</li><li><strong><code>M</code> (Modified)</strong>: 已被修改（由 redirect 或守护进程修改）。</li><li><strong><code>A</code> (Installed by addrconf)</strong>: 由 addrconf 自动配置（通常是 IPv6 相关，但也出现在这里）。</li><li><strong><code>!</code> (Reject route)</strong>: 拒绝路由（对应 <code>RTN_PROHIBIT</code> 等类型）。</li></ul><h4 id="表-5-3-路由表示例" tabindex="-1"><strong>表 5-3：路由表示例</strong> <a class="header-anchor" href="#表-5-3-路由表示例" aria-label="Permalink to &quot;**表 5-3：路由表示例**&quot;">​</a></h4><table tabindex="0"><thead><tr><th style="text-align:left;">Destination</th><th style="text-align:left;">Gateway</th><th style="text-align:left;">Genmask</th><th style="text-align:left;">Flags</th><th style="text-align:left;">Metric</th><th style="text-align:left;">Ref</th><th style="text-align:left;">Use</th><th style="text-align:left;">Iface</th></tr></thead><tbody><tr><td style="text-align:left;">169.254.0.0</td><td style="text-align:left;">0.0.0.0</td><td style="text-align:left;">255.255.0.0</td><td style="text-align:left;"><strong>U</strong></td><td style="text-align:left;">1002</td><td style="text-align:left;">0</td><td style="text-align:left;">0</td><td style="text-align:left;">eth0</td></tr><tr><td style="text-align:left;">192.168.3.0</td><td style="text-align:left;">192.168.2.1</td><td style="text-align:left;">255.255.255.0</td><td style="text-align:left;"><strong>UG</strong></td><td style="text-align:left;">0</td><td style="text-align:left;">0</td><td style="text-align:left;">0</td><td style="text-align:left;">eth1</td></tr></tbody></table><p><strong>解读</strong>：</p><ul><li>第一行：去往链路本地地址 <code>169.254.0.0/16</code> 的流量直接走 <code>eth0</code>（没网关，所以没 G）。</li><li>第二行：去往 <code>192.168.3.0/24</code> 的流量，需要先发给网关 <code>192.168.2.1</code>，然后从 <code>eth1</code> 出去。</li></ul><hr><h3 id="本章回响" tabindex="-1">本章回响 <a class="header-anchor" href="#本章回响" aria-label="Permalink to &quot;本章回响&quot;">​</a></h3><p>这一章，我们像剥洋葱一样剥开了内核路由子系统。</p><p>最外层是 <code>ip route</code> 命令，那是你看到的；往里是 FIB 表，那是内核存储规则的地方；最深处是 LC-trie 树和 <code>fib_lookup</code> 算法，那是每一秒都在处理百万级流量的引擎。</p><p>如果你只记得本章的一件事，请记住这个：<strong>路由决策不是一个简单的匹配，而是一个从查找、到缓存、再到动态修正（Redirect/FNHE）的完整闭环。</strong> 表 5-1 的那些度量值，表 5-2 的那些错误码，以及那些以 <code>fib_</code> 开头的结构体，都是为了让这个闭环既能跑得快，又能在网络拓扑发生变化时灵活地调整姿态。</p><p>下一章，我们将从「怎么走」转向「怎么发」。我们将离开路由层的冷静决策，进入邻居层（Neighbor Subsystem）的热烈握手——看看 ARP 和 ND 是如何把抽象的 IP 地址变成真实的以太网帧，真正把数据推到网线上的。</p><hr><h2 id="练习题" tabindex="-1">练习题 <a class="header-anchor" href="#练习题" aria-label="Permalink to &quot;练习题&quot;">​</a></h2><h3 id="练习-1-understanding" tabindex="-1">练习 1：understanding <a class="header-anchor" href="#练习-1-understanding" aria-label="Permalink to &quot;练习 1：understanding&quot;">​</a></h3><p><strong>题目</strong>：在 Linux 内核的路由查找过程中，<code>fib_lookup()</code> 函数是核心入口。请简述 <code>fib_lookup()</code> 函数通过输入参数 <code>flowi4</code> 和输出参数 <code>fib_result</code> 进行路由查找的基本流程，并说明当查找成功时，内核如何基于 <code>fib_result</code> 构建 <code>dst_entry</code> (目的缓存)。</p><details><summary>答案与解析</summary><p><strong>答案</strong>：<code>fib_lookup()</code> 使用 <code>flowi4</code>（包含目标地址、源地址、TOS 等信息）作为键值，首先在 Local 表中查找，若失败则在 Main 表中进行最长前缀匹配（LPM）。查找成功后，它填充 <code>fib_result</code> 结构体（包含前缀长度、<code>fib_info</code> 指针、路由类型等）。随后，内核根据 <code>fib_result</code> 中的信息（如 <code>type</code> 是 RTN_LOCAL 还是 RTN_UNICAST）创建一个 <code>dst_entry</code> 对象（嵌入在 <code>rtable</code> 中），并设置其 <code>input</code> 或 <code>output</code> 回调函数（如 <code>ip_local_deliver</code> 或 <code>ip_forward</code>）。</p><p><strong>解析</strong>：本题考察对 IPv4 路由子系统核心数据流的理解。<code>flowi4</code> 定义了查找的“钥匙”，<code>fib_result</code> 存储了查找的“结果”。<code>fib_info</code> 是路由条目的具体参数载体。查找成功后，内核必须将这些静态的 FIB 信息转化为动态可用的路由缓存对象（<code>dst_entry</code>），其中最重要的就是设置处理数据包的回调函数，这决定了数据包是本地接收、转发还是丢弃。</p></details><h3 id="练习-2-application" tabindex="-1">练习 2：application <a class="header-anchor" href="#练习-2-application" aria-label="Permalink to &quot;练习 2：application&quot;">​</a></h3><p><strong>题目</strong>：假设你是一名网络管理员，需要配置 Linux 服务器禁止来自 192.168.1.0/24 网段的流量访问 10.0.0.5。请写出实现这一策略的 <code>ip route</code> 命令，并结合 <code>fib_props</code> 和 <code>RTN_PROHIBIT</code> 的原理，解释内核收到匹配该规则的数据包时会返回什么错误码以及发送什么 ICMP 消息。</p><details><summary>答案与解析</summary><p><strong>答案</strong>：命令：<code>ip route add prohibit 10.0.0.5 from 192.168.1.0/24</code>。</p><p>原理：内核中 <code>fib_props</code> 数组定义了不同路由类型的行为。<code>RTN_PROHIBIT</code> 类型对应的错误码是 <code>-EACCES</code>。当数据包匹配此规则时，路由查找返回该错误，内核随即调用 <code>ip_error()</code> 处理，丢弃数据包并向发送方回复 ICMP “Destination Unreachable” 消息，代码为 “Packet Filtered” (ICMP_PKT_FILTERED)。</p><p><strong>解析</strong>：本题考察如何应用策略路由（Policy Routing）概念进行流量过滤。理解 <code>RTN_PROHIBIT</code> 是 <code>fib_type</code> 的一种，它不是简单地丢弃数据包，而是有特定的交互行为（发送 ICMP）。通过查找 <code>fib_props</code> 数组中 <code>RTN_PROHIBIT</code> 对应的 <code>error</code> 字段，可以确定具体的内核行为。</p></details><h3 id="练习-3-thinking" tabindex="-1">练习 3：thinking <a class="header-anchor" href="#练习-3-thinking" aria-label="Permalink to &quot;练习 3：thinking&quot;">​</a></h3><p><strong>题目</strong>：在早期的内核版本中（&lt; 3.6），Linux 使用路由缓存来加速查找，但在 3.6 版本后将其移除，转而完全依赖 FIB TRIE。请从“性能”和“安全性/稳定性”两个维度，分析内核开发团队做出这一改动（采用 FIB TRIE 而非 Routing Cache）的主要原因。</p><details><summary>答案与解析</summary><p><strong>答案</strong>：1. <strong>性能</strong>：随着路由表规模的增长（互联网核心路由表条目极多），维护庞大的哈希缓存及其一致性（如路由失效更新）的开销变得非常大。LC-trie（基于最长前缀匹配的树结构）本身查找速度非常快（O(key length)），且内存效率高，使得额外的缓存层变得不再必要。</p><ol start="2"><li><strong>安全性与稳定性</strong>：路由缓存容易受到“Shadow Master”类 DoS 攻击。攻击者可以通过发送大量随机目标 IP 的数据包，迫使内核不断进行缓存未命中查找并填充缓存，耗尽系统内存和 CPU。移除缓存后，直接在 FIB TRIE 上查找，消除了这种基于缓存溢出的攻击面。</li></ol><p><strong>解析</strong>：本题考察对内核网络子系统演进的深度思考。这是一个经典的“用空间换时间”到“算法优化”的转变案例。理解这一点需要认识到：虽然缓存通常能加速访问，但在高动态变化或特定攻击场景下，缓存的管理成本（锁竞争、表项刷新）和脆弱性（易被攻击）可能超过其带来的收益。FIB TRIE 提供了足够高的查找效率，从而允许移除复杂的缓存逻辑。</p></details><hr><h2 id="要点提炼" tabindex="-1">要点提炼 <a class="header-anchor" href="#要点提炼" aria-label="Permalink to &quot;要点提炼&quot;">​</a></h2><p>Linux 内核通过 <strong>FIB（转发信息库）</strong> 与路由查找机制来决定数据包的去向，核心函数 <code>fib_lookup()</code> 根据目标地址等参数查询路由表，最终生成包含 <code>input</code> 和 <code>output</code> 函数指针的 <code>dst_entry</code>（路由缓存项），将复杂的查表过程转化为对特定回调函数（如 <code>ip_local_deliver</code> 或 <code>ip_forward</code>）的调用，从而实现本机接收与转发的逻辑分流。</p><p><strong><code>fib_info</code> 结构体</strong> 是路由条目在内核中的完整“身份证”，它封装了除目的地之外的所有元数据。它不仅记录了路由来源（如静态配置或内核生成）、作用域和优先级，还通过 <code>fib_metrics</code> 数组管理 MTU、RTT 等 TCP 性能参数。这种设计将路由的决策属性与物理路径分离，使得内核能通过引用计数高效管理路由条目的生命周期，并支持多路径路由配置。</p><p>路由并非总是意味着允许通行，内核通过 <strong><code>fib_props</code> 映射表</strong> 将不同类型的路由（如 <code>RTN_PROHIBIT</code>）转化为具体的操作行为。当路由查找命中“禁止”类型时，内核不会静默丢包，而是依据配置触发 ICMP “目标不可达”或“过滤”消息，这种机制将路由表本身升级为一套高效的流量控制策略系统。</p><p>为了应对网络环境的动态变化，内核在 <strong><code>fib_nh</code>（下一跳）</strong> 层面引入了 <strong>Exception（例外）机制</strong>。针对特定目标地址，内核利用哈希表记录 ICMP 重定向带来的网关变更或 PMTU 发现带来的 MTU 调整。这种“便签式”的微调避免了频繁修改庞大的全局路由表，实现了对个别路径的精准修正与隔离。</p><p>当多条路由指向相同目的地和物理路径但属性（如 TOS、优先级）不同时，内核采用 <strong><code>fib_alias</code></strong> 机制来优化内存。它允许多个轻量级的 <code>fib_alias</code> 结构体共享同一个存储实际路径信息的 <code>fib_info</code>，这种设计避免了为微小差异复制大量路由数据，显著提升了大规模路由表（如 BGP 场景）的内存利用率。</p>',79)])])}const h=e(i,[["render",n]]);export{_ as __pageData,h as default};
