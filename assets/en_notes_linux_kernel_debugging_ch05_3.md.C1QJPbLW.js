import{_ as t,o,c as s,a as r}from"./chunks/framework.8tiMeCK-.js";const n="/PenguinLab/assets/ch05_fig047.BHKfsra1.webp",g=JSON.parse('{"title":"5.3 Understanding the Fundamentals of KASAN","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch05_3.md","filePath":"en/notes/linux_kernel_debugging/ch05_3.md","lastUpdated":null}'),i={name:"en/notes/linux_kernel_debugging/ch05_3.md"};function a(d,e,l,h,c,u){return o(),s("div",null,[...e[0]||(e[0]=[r('<h1 id="_5-3-understanding-the-fundamentals-of-kasan" tabindex="-1">5.3 Understanding the Fundamentals of KASAN <a class="header-anchor" href="#_5-3-understanding-the-fundamentals-of-kasan" aria-label="Permalink to &quot;5.3 Understanding the Fundamentals of KASAN&quot;">​</a></h1><p>In the previous section, we laid out a long &quot;wish list,&quot; exposing the various memory bugs that can lurk in the kernel. The question now is: how exactly does KASAN track down the culprits on this list one by one?</p><p>Before answering that, we need to establish a few key concepts. Without a clear grasp of these, reading KASAN logs later will feel like deciphering gibberish.</p><h3 id="compile-time-instrumentation-how-does-it-work" tabindex="-1">Compile-Time Instrumentation: How Does It Work? <a class="header-anchor" href="#compile-time-instrumentation-how-does-it-work" aria-label="Permalink to &quot;Compile-Time Instrumentation: How Does It Work?&quot;">​</a></h3><p>First and foremost, KASAN is a <strong>dynamic analysis tool</strong>. What does that mean?</p><p>It means it doesn&#39;t find bugs by statically scanning code; it only works when the code is <strong>actually running</strong>. If a particular code path is never executed, or if your test cases aren&#39;t aggressive enough (failing to cover edge cases), KASAN is essentially useless. This is why I constantly stress the importance of &quot;good test cases&quot; and &quot;Fuzzing&quot;—if you don&#39;t hit the wall, KASAN won&#39;t sound the alarm.</p><p>This brings us to its core technique: <strong>Compile-Time Instrumentation</strong>.</p><p>The name sounds sophisticated, but the underlying principle is quite brute-force. When we compile the kernel using GCC or Clang with the <code>-fsanitize=kernel-address</code> option enabled, the compiler secretly inserts &quot;check code&quot; before and after every single memory access instruction.</p><p>What does this check code do?</p><p>It maintains an extra memory region called <strong>Shadow Memory</strong>.</p><p>You can think of shadow memory as the &quot;ledger&quot; for real memory—or, more vividly, as a security camera set up outside your house.</p><p>However, the camera analogy isn&#39;t entirely accurate: a camera passively records video, whereas KASAN&#39;s shadow memory actively intercepts accesses. Here&#39;s how it works: every 8 bytes of real memory map to 1 byte in shadow memory.</p><ul><li>If the shadow byte is <code>0</code>, all 8 bytes are accessible.</li><li>If the shadow byte is <code>1</code>, only the first byte is accessible.</li><li>If the shadow byte is a negative number (like <code>0xFF</code>), the memory block is completely invalid.</li></ul><p><strong>But the reality is even more brute-force</strong>: every time you read from or write to memory, the compiler-inserted check code consults this &quot;ledger&quot; first. If the ledger says &quot;red zone&quot; or &quot;freed,&quot; it triggers a panic right on the spot. It doesn&#39;t just log the problem; it executes immediate justice.</p><hr><h3 id="what-s-the-cost-when-can-we-use-it" tabindex="-1">What&#39;s the Cost? When Can We Use It? <a class="header-anchor" href="#what-s-the-cost-when-can-we-use-it" aria-label="Permalink to &quot;What&#39;s the Cost? When Can We Use It?&quot;">​</a></h3><p>This brute-force checking comes with a cost, mainly from two aspects:</p><ol><li><strong>Time (CPU)</strong>: Every memory access requires a shadow memory lookup first. This adds extra instructions and can disrupt branch prediction.</li><li><strong>Space (RAM)</strong>: The shadow memory itself consumes space.</li></ol><p><strong>Here is a counterintuitive fact</strong>: KASAN&#39;s CPU overhead is actually surprisingly low—typically only 2x to 4x. If you&#39;ve ever used dynamic instrumentation tools like Valgrind (which usually incur a 20x to 50x overhead), you&#39;ll find KASAN incredibly fast.</p><p>The real pain point lies in <strong>RAM</strong>.</p><p>Remember that 1:8 ratio? For every 8 bytes of real memory, 1 byte of shadow memory is consumed. For an architecture like x86_64, which easily has a 128 TB kernel virtual address space (VAS), this means KASAN carves out <strong>16 TB</strong> of virtual address space for shadow memory (even if physical RAM isn&#39;t entirely consumed, the address space resource is still occupied).</p><p>For enterprise servers, this is no big deal. But for resource-constrained embedded systems—like your Android phone, TV box, or low-end router—this overhead might simply be unbearable.</p><p>This is why the modern Linux kernel supports three different &quot;tiers&quot; of KASAN modes. We&#39;ve summarized their behaviors in Table 5.2:</p><p><img src="'+n+'" alt="table"><em>(Table 5.2: Comparison of the three KASAN modes and their overhead)</em></p><table tabindex="0"><thead><tr><th>Mode</th><th>Nickname</th><th>Memory/CPU Overhead</th><th>Use Case</th><th>Architecture Constraints</th></tr></thead><tbody><tr><td>Generic KASAN</td><td><strong>Generic</strong></td><td>High / Medium</td><td>Active debugging, bug hunting</td><td>x86_64, ARM, ARM64, RISC-V, etc.</td></tr><tr><td>Software tag-based</td><td>Software Tag</td><td>Medium / Low</td><td>Real workload stress testing</td><td><strong>ARM64 only</strong></td></tr><tr><td>Hardware tag-based</td><td>Hardware Tag</td><td>Low / Very Low</td><td>Can even be used in production</td><td><strong>ARM64 only (MTE)</strong></td></tr></tbody></table><p><strong>Returning to the &quot;security camera&quot; analogy</strong>:</p><ul><li><strong>Generic KASAN</strong> is like assigning a 24/7 bodyguard to every single item in your house. It&#39;s secure, but incredibly expensive—you can only afford it during critical times (the debugging phase), not for everyday use.</li><li><strong>Tag-based modes</strong> are like attaching RFID tags to items. The security alarm only triggers when you try to walk out the door with them. This is much more lightweight.</li></ul><p>Seeing this, you might ask: why is something so good (Tag-based modes) only available on ARM64?</p><p>The answer lies in the market. The Android ecosystem is almost entirely dominated by ARM64. Google desperately needs the ability to detect memory errors in production environments (on users&#39; phones) because they can&#39;t ask hundreds of millions of users to run their phones with Generic KASAN enabled. Therefore, <strong>MTE (Memory Tagging Extension)</strong>, a hardware-based feature, was introduced to make low-overhead memory checking possible.</p><hr><h3 id="prerequisites-compilers-and-hardware" tabindex="-1">Prerequisites: Compilers and Hardware <a class="header-anchor" href="#prerequisites-compilers-and-hardware" aria-label="Permalink to &quot;Prerequisites: Compilers and Hardware&quot;">​</a></h3><p>Since this is a compiler-based technology, the compiler version is critical. You can&#39;t expect a decades-old GCC to generate modern instrumentation code.</p><p>The current hard requirements are as follows:</p><ul><li><strong>GCC</strong>: Must be <strong>8.3.0</strong> or later.</li><li><strong>Clang</strong>: Theoretically, any version works, but if you want to detect out-of-bounds accesses on global variables, you need <strong>Clang 11 or later</strong>.</li></ul><p>On the hardware side, KASAN has traditionally been a privilege exclusive to the &quot;64-bit club.&quot;</p><blockquote><p><strong>Why 64-bit?</strong></p><p>Recall that 1:8 shadow memory ratio. On a 32-bit system, the address space is only 4GB. Slicing off 1/8th for shadow memory leaves very little room for anything else. Not to mention, the kernel address space is typically only 1GB or 3GB (depending on configuration), which would practically suffocate the kernel.</p></blockquote><p><strong>But things are changing</strong>.</p><p>If you look closely at the kernel documentation, you&#39;ll notice that <strong>Generic KASAN now supports the 32-bit ARM architecture</strong>—a new feature introduced in Linux kernel 5.11. This means that even older ARM development boards with limited computing power can benefit from this (though you will feel the memory pressure much more acutely).</p><p>As for the two seemingly more advanced Tag-based modes, they currently remain stubbornly limited to <strong>ARM64</strong>.</p><p><strong>Why still ARM64?</strong></p><p>Because of Android. The core of almost all modern smartphones, wearables, and smart TVs is ARM64. For the mobile ecosystem, the ability to catch memory errors in production environments at an extremely low cost is invaluable. This isn&#39;t just a technical choice; it&#39;s driven by business needs.</p><hr><p><strong>The Road Ahead</strong></p><p>In the rest of this chapter, for the sake of demonstration, we will default to using <strong>Generic KASAN</strong>. This is not only because it supports the widest range of architectures (including the x86_64 PC or 32-bit ARM board you might have on hand), but also because it is the most aggressive and effective mode for debugging.</p><p>If you happen to have an ARM64 board handy, you can try switching to a Tag-based mode to experience that silky-smooth feeling of &quot;even daring to enable it in production.&quot; But regardless of the mode, the underlying principles are the same—once you understand the shadow memory mechanism in Generic mode, the rest is just a difference in implementation details.</p>',45)])])}const p=t(i,[["render",a]]);export{g as __pageData,p as default};
