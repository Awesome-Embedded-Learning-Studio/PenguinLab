import{_ as t,o as s,c as n,a as i}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"9.7  Hands-on: Tracing a Single Ping Request with the set_event Interface","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch09_7.md","filePath":"en/notes/linux_kernel_debugging/ch09_7.md","lastUpdated":null}'),a={name:"en/notes/linux_kernel_debugging/ch09_7.md"};function o(r,e,l,h,c,p){return s(),n("div",null,[...e[0]||(e[0]=[i(`<h1 id="_9-7-hands-on-tracing-a-single-ping-request-with-the-set-event-interface" tabindex="-1">9.7 Hands-on: Tracing a Single Ping Request with the set_event Interface <a class="header-anchor" href="#_9-7-hands-on-tracing-a-single-ping-request-with-the-set-event-interface" aria-label="Permalink to &quot;9.7  Hands-on: Tracing a Single Ping Request with the set_event Interface&quot;">​</a></h1><p>In the previous section, we used the <code>available_filter_functions</code> &quot;cast a wide net&quot; approach to extract the function call graph of the entire network stack. While intuitive, it&#39;s like turning on everyone&#39;s microphone in a building just to hear what two people are saying—too much noise, too much information overload.</p><p>Actually, ftrace offers another perspective. Instead of watching &quot;functions,&quot; it watches &quot;events.&quot;</p><p>This isn&#39;t just about swapping interfaces—the kernel we see through these two methods represents two completely different facets.</p><h3 id="from-functions-to-events" tabindex="-1">From Functions to Events <a class="header-anchor" href="#from-functions-to-events" aria-label="Permalink to &quot;From Functions to Events&quot;">​</a></h3><p>Recall the workflow from the previous section: we wrote function names into <code>set_ftrace_filter</code> and told ftrace to &quot;watch these functions.&quot; This is intuitive, but it has a catch: you have to know the function names beforehand.</p><p>For those &quot;key nodes&quot; already defined in the kernel, why should we have to guess the function names? Kernel developers have already planted the markers for us. These markers are <strong>Tracepoints</strong>.</p><blockquote><p><strong>Think of Tracepoints as induction loops buried under a highway.</strong> The previous method (<code>available_filter_functions</code>) is like stationing someone on the roadside to watch every single car; whereas <code>set_event</code> directly reads the records from the induction loops.</p><p>But be careful with this analogy: induction loops only tell you &quot;a car passed,&quot; lacking the rich context of a physical stakeout. In ftrace, this means you&#39;ll see specific <strong>parameter values</strong>, but you might lose the <strong>indentation hierarchy of function calls</strong>.</p></blockquote><p>All these events are neatly organized under the <code>/sys/kernel/tracing/events</code> directory.</p><p>Take a look at this figure (Figure 9.16)—this is the kernel&#39;s &quot;event supermarket&quot;:</p><p><em>Figure 9.16 – Contents of the <code>/sys/kernel/tracing/events</code> directory—all kernel tracepoints</em></p><p>Remember the dynamic kprobes we discussed in Chapter 4 (<em>Debug via Instrumentation – Kprobes</em>)? We actually used this event tracing framework back then. At the time, we used it to trace functions without built-in tracepoints; now, we are using the kernel&#39;s native, &quot;stock&quot; tracepoints.</p><h3 id="narrowing-the-scope—focusing-on-networking" tabindex="-1">Narrowing the Scope—Focusing on Networking <a class="header-anchor" href="#narrowing-the-scope—focusing-on-networking" aria-label="Permalink to &quot;Narrowing the Scope—Focusing on Networking&quot;">​</a></h3><p>If we only care about networking, the <code>net</code>, <code>sock</code>, <code>skb</code>, <code>tcp</code>, and <code>udp</code> subdirectories under <code>events</code> are our targets.</p><p>The syntax for using the <code>set_event</code> interface is very simple: just echo a &quot;system:event&quot; string into it. If we want to enable all events under a specific subsystem at once, the <code>*</code> wildcard comes in handy.</p><p>This is much cleaner than the script from the previous section. We just need to set <code>FILTER_VIA_AVAIL_FUNCS</code> to 0, and the script will take a different branch:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">//</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ch9/ftrace/ping_ftrace.sh</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[...]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">FILTER_VIA_AVAIL_FUNCS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">0</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;[+] Function filtering:&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [ \${FILTER_VIA_AVAIL_FUNCS} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-eq</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ] ; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">then</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">   [... 上一节的逻辑 ...] </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 通过 set_event 接口进行过滤</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 这种方法很快，但细节会少一些！</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 我们会失去函数图的缩进，但能看到函数参数！</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">   echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot; Alternate event-based filtering (via set_event):&quot;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">   echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;net:* sock:* skb:* tcp:* udp:* napi:* qdisc:* neigh:* syscalls:*&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set_event</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">fi</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p>Notice that I added <code>syscalls:*</code> here. Why look at system calls? Because we need context. Tracing the ins and outs of network packets without knowing who sent them is like watching a movie with no main character.</p><h3 id="output-results—trade-offs" tabindex="-1">Output Results—Trade-offs <a class="header-anchor" href="#output-results—trade-offs" aria-label="Permalink to &quot;Output Results—Trade-offs&quot;">​</a></h3><p>Run this script, and then initiate another Ping. This time, the content in the <code>trace</code> file looks completely different.</p><p>Here is a partial output (Figure 9.17):</p><p><em>Figure 9.17 – ftrace report obtained via the set_event interface (filtered)</em></p><p>See the difference?</p><p><strong>We lost some things, but we gained others.</strong></p><p>❌ <strong>Lost</strong>: That beautiful, code-indentation-like function call hierarchy is gone. It&#39;s as if the call stack was flattened; you can&#39;t tell at a glance whether <code>udp_sendmsg</code> is nested inside <code>ip_send_fill_frag</code>.</p><p>✅ <strong>Gained</strong>: Detailed information is appended to each line—<strong>the values of the function parameters</strong>. This is crucial. For debugging, knowing &quot;this function was called&quot; is only the first step; knowing &quot;what the buffer pointer passed in was&quot; is what ultimately settles the issue.</p><p>If we use the <code>trace-cmd</code> tool (covered in the next section) without the <code>function_graph</code> plugin, it prints the parameters by default, which is consistent with the behavior we see here.</p><blockquote><p><strong>Returning to the &quot;highway induction loop&quot; analogy:</strong> In the report output now, each parameter value is like a photo snapped by the induction loop. You might not see how the car drove in (lost the indentation hierarchy), but you can clearly see the license plate and the payload (gained the parameter values). For troubleshooting issues like &quot;why did a wrong parameter cause packet drops,&quot; this perspective is often much more effective than staring at thousands of lines of indented graphs.</p></blockquote><h3 id="quick-debugging-insertions—trace-printk" tabindex="-1">Quick Debugging Insertions—trace_printk() <a class="header-anchor" href="#quick-debugging-insertions—trace-printk" aria-label="Permalink to &quot;Quick Debugging Insertions—trace_printk()&quot;">​</a></h3><p>Speaking of debugging parameters, here is an incredibly useful API to interject: <code>trace_printk()</code>.</p><p>Many kernel novices like to use <code>printk()</code> for logging, only to realize when they run it: &quot;Damn, the logging is so slow it messes up the system timing,&quot; or even worse, &quot;The <code>printk()</code> buffer overflowed and the critical information is lost.&quot;</p><p><code>trace_printk()</code> exists specifically to solve this pain point. Its syntax is exactly the same as <code>printf()</code>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">trace_printk</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;myprj: at </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">():</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%d\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__FILE__</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, __func__, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__LINE__</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><strong>The fundamental differences from printk are</strong>:</p><ol><li><strong>Speed</strong>: It only writes to memory (ftrace&#39;s ring buffer) and never touches the console device. This is critical when handling &quot;lightning-fast&quot; code paths like interrupts; otherwise, you get Heisenbugs—observing it changes its behavior.</li><li><strong>Capacity</strong>: ftrace&#39;s buffer is typically much larger than printk&#39;s log buffer, and it&#39;s adjustable.</li><li><strong>How to read it</strong>: You won&#39;t see it in <code>dmesg</code>. You must read it from <code>/sys/kernel/tracing/trace</code> or <code>trace_pipe</code>.</li></ol><p>Here&#39;s another small trick: if you use the <code>function_graph</code> tracer, the output from <code>trace_printk()</code> will appear as comments (<code>/* ... */</code>) mixed into the function call flow, making it very easy to pinpoint exactly which step printed the message.</p><p>⚠️ <strong>Warning</strong> The kernel documentation explicitly states that this is for debugging use only. Do not leave it in production code.</p><p>If you only need to print a fixed string (no formatting required), using <code>trace_puts()</code> is faster. The kernel developers even thought of that.</p><p>If you want to disable this feature (to prevent <code>trace_printk</code> from polluting your trace), you can write <code>notrace_printk</code> into <code>trace_options</code>, or write <code>0</code> into <code>options/trace_printk</code>.</p>`,39)])])}const g=t(a,[["render",o]]);export{u as __pageData,g as default};
