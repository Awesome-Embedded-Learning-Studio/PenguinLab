import{_ as t,o,c as i,a as n}from"./chunks/framework.8tiMeCK-.js";const g=JSON.parse('{"title":"Chapter 5: The IPv4 Routing Subsystem","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch05_8.md","filePath":"en/notes/linux_kernel_networking/ch05_8.md","lastUpdated":null}'),r={name:"en/notes/linux_kernel_networking/ch05_8.md"};function a(s,e,l,d,c,h){return o(),i("div",null,[...e[0]||(e[0]=[n('<h1 id="chapter-5-the-ipv4-routing-subsystem" tabindex="-1">Chapter 5: The IPv4 Routing Subsystem <a class="header-anchor" href="#chapter-5-the-ipv4-routing-subsystem" aria-label="Permalink to &quot;Chapter 5: The IPv4 Routing Subsystem&quot;">​</a></h1><h2 id="_5-8-quick-reference-panel" tabindex="-1">5.8 Quick Reference Panel <a class="header-anchor" href="#_5-8-quick-reference-panel" aria-label="Permalink to &quot;5.8  Quick Reference Panel&quot;">​</a></h2><p>IDE auto-completion is great when writing code, but when you&#39;re jumping around large stretches of kernel code or digging through <code>grep</code> results, you need something more direct—a cheat sheet pinned to your wall.</p><p>This section is that sheet.</p><p>There&#39;s no narrative here, no buildup—just the core data structures, API functions, and macro definitions we dissected in this chapter. They&#39;re scattered across a few key files in <code>net/ipv4</code>, and now we&#39;ve gathered them together so you can quickly look them up during your future debugging (or fire-fighting) endeavors.</p><h3 id="module-and-file-inventory" tabindex="-1">Module and File Inventory <a class="header-anchor" href="#module-and-file-inventory" aria-label="Permalink to &quot;Module and File Inventory&quot;">​</a></h3><p>First, let&#39;s confirm the &quot;crime scene&quot; locations one more time. The IPv4 routing subsystem implementation is primarily concentrated in the following modules:</p><ul><li><strong><code>fib_frontend.c</code></strong>: The FIB &quot;front desk,&quot; handling requests from user space (such as <code>ip route add</code>).</li><li><strong><code>fib_trie.c</code></strong>: The core lookup logic—where the highly efficient LC-trie tree lives.</li><li><strong><code>fib_semantics.c</code></strong>: Handles the semantics of FIB entries, such as the management of <code>fib_info</code>.</li><li><strong><code>route.c</code></strong>: The original home of the routing cache (although the caching mechanism has changed drastically in modern kernels, the core still resides here).</li><li><strong><code>fib_rules.c</code></strong>: The implementer of policy routing. Note that this module is only compiled in when you enable <code>CONFIG_IP_MULTIPLE_TABLES</code>—if you need to make routing decisions based on source address or other conditions, this is the switch you need.</li></ul><p>Outside of the source code, these header files are worth bookmarking:</p><ul><li><code>include/net/ip_fib.h</code>: Core FIB definitions.</li><li><code>fib_lookup.h</code>: Header file for the lookup logic.</li><li><code>include/net/route.h</code>: Interface between the routing layer and upper layers.</li></ul><p>Don&#39;t forget the generic implementation of the destination cache (<code>dst_entry</code>), which lives in <code>net/core/dst.c</code> and <code>include/net/dst.h</code>.</p><hr><h3 id="core-api-methods" tabindex="-1">Core API Methods <a class="header-anchor" href="#core-api-methods" aria-label="Permalink to &quot;Core API Methods&quot;">​</a></h3><p>Below are the key functions mentioned in this chapter. If you&#39;re reading source code or writing your own kernel module and need to interact with the FIB, these are most likely the interfaces you&#39;ll call.</p><h4 id="routing-table-operations" tabindex="-1"><strong>Routing Table Operations</strong> <a class="header-anchor" href="#routing-table-operations" aria-label="Permalink to &quot;**Routing Table Operations**&quot;">​</a></h4><ul><li><p><strong><code>int fib_table_insert(struct fib_table *tb, struct fib_config *cfg);</code></strong></p><ul><li><strong>What it does</strong>: Inserts a route into the specified FIB table (<code>tb</code>).</li><li><strong>Parameters</strong>: <code>cfg</code> contains the routing configuration passed down from user space (destination address, gateway, netmask, etc.).</li><li><strong>Note</strong>: This is the final hop into the kernel after a Netlink socket triggers <code>ip route add</code>.</li></ul></li><li><p><strong><code>int fib_table_delete(struct fib_table *tb, struct fib_config *cfg);</code></strong></p><ul><li><strong>What it does</strong>: Deletes a route from the specified FIB table.</li><li><strong>Corresponding command</strong>: <code>ip route del</code>.</li></ul></li><li><p><strong><code>struct fib_table *fib_trie_table(u32 id);</code></strong></p><ul><li><strong>What it does</strong>: Allocates and initializes a TRIE-based FIB routing table. If you want to create a custom routing table (not just <code>local</code> or <code>main</code>), this is the constructor you&#39;re looking for.</li></ul></li></ul><h4 id="fib-entry-management" tabindex="-1"><strong>FIB Entry Management</strong> <a class="header-anchor" href="#fib-entry-management" aria-label="Permalink to &quot;**FIB Entry Management**&quot;">​</a></h4><ul><li><p><strong><code>struct fib_info *fib_create_info(struct fib_config *cfg);</code></strong></p><ul><li><strong>What it does</strong>: Constructs a <code>fib_info</code> object based on the configuration <code>cfg</code>.</li><li><strong>Mechanism</strong>: This is the &quot;meat&quot; of the routing entry. It checks whether an identical <code>fib_info</code> already exists (to save memory) and only creates a new one if it doesn&#39;t.</li></ul></li><li><p><strong><code>void free_fib_info(struct fib_info *fi);</code></strong></p><ul><li><strong>What it does</strong>: Frees a <code>fib_info</code> object.</li><li><strong>Condition</strong>: It is only actually freed when the reference count drops to zero (the <code>fib_dead</code> flag is set), and it decrements the global counter <code>fib_info_cnt</code>.</li></ul></li><li><p><strong><code>void fib_alias_accessed(struct fib_alias *fa);</code></strong></p><ul><li><strong>What it does</strong>: Marks a <code>fib_alias</code> entry as &quot;visited.&quot;</li><li><strong>Details</strong>: It simply sets <code>fa-&gt;fa_state</code> to <code>FA_S_ACCESSED</code>. This might be used during garbage collection or statistics gathering to distinguish &quot;hot&quot; routes from cold ones.</li></ul></li></ul><h4 id="lookup-and-trie-traversal" tabindex="-1"><strong>Lookup and TRIE Traversal</strong> <a class="header-anchor" href="#lookup-and-trie-traversal" aria-label="Permalink to &quot;**Lookup and TRIE Traversal**&quot;">​</a></h4><ul><li><strong><code>struct leaf *fib_find_node(struct trie *t, u32 key);</code></strong><ul><li><strong>What it does</strong>: Looks up a node matching <code>key</code> in the TR tree <code>t</code>.</li><li><strong>Returns</strong>: Returns a <code>leaf</code> node on success, or <code>NULL</code> on failure. This is the direct manifestation of the Longest Prefix Match (LPM) algorithm at the data structure level.</li></ul></li></ul><h4 id="redirect-and-exception-handling" tabindex="-1"><strong>Redirect and Exception Handling</strong> <a class="header-anchor" href="#redirect-and-exception-handling" aria-label="Permalink to &quot;**Redirect and Exception Handling**&quot;">​</a></h4><ul><li><p><strong><code>void ip_rt_send_redirect(struct sk_buff *skb);</code></strong></p><ul><li><strong>What it does</strong>: Sends an ICMPv4 Redirect message.</li><li><strong>Scenario</strong>: When the kernel discovers a host taking a &quot;detour&quot; (forwarding through itself when it&#39;s not the optimal next hop), it calls this function to kindly tell the other party, &quot;don&#39;t go the long way, go that way.&quot;</li></ul></li><li><p><strong><code>void __ip_do_redirect(struct rtable *rt, struct sk_buff *skb, struct flowi4 *fl4, bool kill_route);</code></strong></p><ul><li><strong>What it does</strong>: Handles received ICMPv4 Redirect messages.</li><li><strong>Core logic</strong>: This is where FIB nexthop exceptions (FNHEs) are created. If <code>kill_route</code> is true, it completely wipes out the old route; otherwise, it creates an exception entry that allows specific traffic to bypass the FIB table and head straight to the new gateway.</li></ul></li><li><p><strong><code>void update_or_create_fnhe(struct fib_nh *nh, __be32 daddr, __be32 gw, u32 pmtu, unsigned long expires);</code></strong></p><ul><li><strong>What it does</strong>: As the name implies, updates or creates an FNHE.</li><li><strong>Parameters</strong>: Specifies the next hop <code>nh</code>, destination address <code>daddr</code>, new gateway <code>gw</code>, PMTU, and expiration time. This is the unified entry point for the Redirect and PMTU discovery mechanisms to modify routing behavior.</li></ul></li></ul><h4 id="metric-queries" tabindex="-1"><strong>Metric Queries</strong> <a class="header-anchor" href="#metric-queries" aria-label="Permalink to &quot;**Metric Queries**&quot;">​</a></h4><ul><li><strong><code>u32 dst_metric(const struct dst_entry *dst, int metric);</code></strong><ul><li><strong>What it does</strong>: Extracts the specified metric (such as MTU, initial window, etc.) from a <code>dst_entry</code>.</li></ul></li></ul><hr><h3 id="core-macro-definitions" tabindex="-1">Core Macro Definitions <a class="header-anchor" href="#core-macro-definitions" aria-label="Permalink to &quot;Core Macro Definitions&quot;">​</a></h3><p>Kernel code is full of macros; here are a few key players we encountered while dissecting the FIB logic.</p><h4 id="fib-lookup-result-extraction" tabindex="-1"><strong>FIB Lookup Result Extraction</strong> <a class="header-anchor" href="#fib-lookup-result-extraction" aria-label="Permalink to &quot;**FIB Lookup Result Extraction**&quot;">​</a></h4><p>These macros typically take a <code>fib_result</code> structure as an argument and extract key fields from the lookup result.</p><ul><li><strong><code>FIB_RES_GW(res)</code></strong>: Returns the gateway address of the next hop (<code>nh_gw</code>).</li><li><strong><code>FIB_RES_DEV(res)</code></strong>: Returns the network device for the next step (<code>net_device</code>).</li><li><strong><code>FIB_RES_OIF(res)</code></strong>: Returns the output interface index (<code>nh_oif</code>).</li><li><strong><code>FIB_RES_NH(res)</code></strong>: Returns the complete <code>fib_nh</code> structure. <ul><li><em>Details</em>: If multipath routing is enabled, this uses <code>res-&gt;nh_sel</code> as an index to select the correct one from the <code>nexthop</code> array in <code>fib_info</code>.</li></ul></li></ul><h4 id="device-behavior-checks" tabindex="-1"><strong>Device Behavior Checks</strong> <a class="header-anchor" href="#device-behavior-checks" aria-label="Permalink to &quot;**Device Behavior Checks**&quot;">​</a></h4><p>Before deciding &quot;should I forward this?&quot; or &quot;should I obey?&quot;, the kernel has to ask the NIC&#39;s attitude first.</p><ul><li><strong><code>IN_DEV_FORWARD(in_dev)</code></strong>: Checks whether IP forwarding is enabled on the device. If this is off, it&#39;s a quiet endpoint, not a router.</li><li><strong><code>IN_DEV_RX_REDIRECTS(in_dev)</code></strong>: Checks whether the device <strong>receives</strong> ICMP Redirects. If you&#39;re building a router, you usually turn this off to prevent being disrupted by someone else&#39;s Redirects.</li><li><strong><code>IN_DEV_TX_REDIRECTS(in_dev)</code></strong>: Checks whether the device <strong>sends</strong> ICMP Redirects.</li></ul><h4 id="trie-tree-structure-traversal" tabindex="-1"><strong>TRIE Tree Structure Traversal</strong> <a class="header-anchor" href="#trie-tree-structure-traversal" aria-label="Permalink to &quot;**TRIE Tree Structure Traversal**&quot;">​</a></h4><ul><li><strong><code>IS_LEAF(node)</code></strong>: Checks if this node is a leaf node (an endpoint).</li><li><strong><code>IS_TNODE(node)</code></strong>: Checks if this node is an internal node (a Trie Node, meaning we need to keep looking deeper).</li></ul><h4 id="multipath-routing-iteration" tabindex="-1"><strong>Multipath Routing Iteration</strong> <a class="header-anchor" href="#multipath-routing-iteration" aria-label="Permalink to &quot;**Multipath Routing Iteration**&quot;">​</a></h4><ul><li><strong><code>change_nexthops(fi)</code></strong>: A macro defined in <code>fib_semantics.c</code>. It provides a loop mechanism to iterate over all <code>nexthop</code> entries in a <code>fib_info</code>. When you need to inspect every possible path, this is what you&#39;ll use.</li></ul><hr><h3 id="key-data-tables" tabindex="-1">Key Data Tables <a class="header-anchor" href="#key-data-tables" aria-label="Permalink to &quot;Key Data Tables&quot;">​</a></h3><h4 id="table-5-1-routing-metrics" tabindex="-1"><strong>Table 5-1: Routing Metrics</strong> <a class="header-anchor" href="#table-5-1-routing-metrics" aria-label="Permalink to &quot;**Table 5-1: Routing Metrics**&quot;">​</a></h4><p>The kernel doesn&#39;t just manage where the road goes; it also manages road conditions. There are 15 (<code>RTAX_MAX</code>) metrics here, some of which are specifically for TCP.</p><table tabindex="0"><thead><tr><th style="text-align:left;">Linux Symbol</th><th style="text-align:center;">TCP Related (Y/N)</th><th style="text-align:left;">Meaning</th></tr></thead><tbody><tr><td style="text-align:left;"><code>RTAX_UNSPEC</code></td><td style="text-align:center;">N</td><td style="text-align:left;">Unspecified</td></tr><tr><td style="text-align:left;"><code>RTAX_LOCK</code></td><td style="text-align:center;">N</td><td style="text-align:left;">Lock metric (prevent updates)</td></tr><tr><td style="text-align:left;"><code>RTAX_MTU</code></td><td style="text-align:center;">N</td><td style="text-align:left;">Path Maximum Transmission Unit</td></tr><tr><td style="text-align:left;"><code>RTAX_WINDOW</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">TCP initial window size</td></tr><tr><td style="text-align:left;"><code>RTAX_RTT</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">Round-Trip Time</td></tr><tr><td style="text-align:left;"><code>RTAX_RTTVAR</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">Round-Trip Time Variance</td></tr><tr><td style="text-align:left;"><code>RTAX_SSTHRESH</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">Slow Start Threshold</td></tr><tr><td style="text-align:left;"><code>RTAX_CWND</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">Congestion Window</td></tr><tr><td style="text-align:left;"><code>RTAX_ADVMSS</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">Peer MSS (suggested)</td></tr><tr><td style="text-align:left;"><code>RTAX_REORDERING</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">Packet reordering threshold</td></tr><tr><td style="text-align:left;"><code>RTAX_HOPLIMIT</code></td><td style="text-align:center;">N</td><td style="text-align:left;">Hop limit</td></tr><tr><td style="text-align:left;"><code>RTAX_INITCWND</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">Initial Congestion Window</td></tr><tr><td style="text-align:left;"><code>RTAX_FEATURES</code></td><td style="text-align:center;">N</td><td style="text-align:left;">Feature flags</td></tr><tr><td style="text-align:left;"><code>RTAX_RTO_MIN</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">Minimum Retransmission Timeout</td></tr><tr><td style="text-align:left;"><code>RTAX_INITRWND</code></td><td style="text-align:center;"><strong>Y</strong></td><td style="text-align:left;">Initial Receive Window</td></tr></tbody></table><p><em>(Source: <code>include/uapi/linux/rtnetlink.h</code>)</em></p><h4 id="table-5-2-route-types-and-error-codes" tabindex="-1"><strong>Table 5-2: Route Types and Error Codes</strong> <a class="header-anchor" href="#table-5-2-route-types-and-error-codes" aria-label="Permalink to &quot;**Table 5-2: Route Types and Error Codes**&quot;">​</a></h4><p>When a routing lookup hits, different <code>type</code> values mean different fates. Here we list the mappings in the <code>fib_props</code> array: the error codes and scopes corresponding to specific route types.</p><table tabindex="0"><thead><tr><th style="text-align:left;">Linux Symbol</th><th style="text-align:left;">Error Code</th><th style="text-align:left;">Scope</th><th style="text-align:left;">Meaning</th></tr></thead><tbody><tr><td style="text-align:left;"><code>RTN_UNSPEC</code></td><td style="text-align:left;">0</td><td style="text-align:left;"><code>RT_SCOPE_NOWHERE</code></td><td style="text-align:left;">Unspecified (usually doesn&#39;t appear in final results)</td></tr><tr><td style="text-align:left;"><code>RTN_UNICAST</code></td><td style="text-align:left;">0</td><td style="text-align:left;"><code>RT_SCOPE_UNIVERSE</code></td><td style="text-align:left;">Normal unicast route</td></tr><tr><td style="text-align:left;"><code>RTN_LOCAL</code></td><td style="text-align:left;">0</td><td style="text-align:left;"><code>RT_SCOPE_HOST</code></td><td style="text-align:left;">Local address</td></tr><tr><td style="text-align:left;"><code>RTN_BROADCAST</code></td><td style="text-align:left;">0</td><td style="text-align:left;"><code>RT_SCOPE_LINK</code></td><td style="text-align:left;">Broadcast address</td></tr><tr><td style="text-align:left;"><code>RTN_ANYCAST</code></td><td style="text-align:left;">0</td><td style="text-align:left;"><code>RT_SCOPE_LINK</code></td><td style="text-align:left;">Anycast address</td></tr><tr><td style="text-align:left;"><code>RTN_MULTICAST</code></td><td style="text-align:left;">0</td><td style="text-align:left;"><code>RT_SCOPE_UNIVERSE</code></td><td style="text-align:left;">Multicast route</td></tr><tr><td style="text-align:left;"><code>RTN_BLACKHOLE</code></td><td style="text-align:left;"><code>-EINVAL</code></td><td style="text-align:left;"><code>RT_SCOPE_UNIVERSE</code></td><td style="text-align:left;">Blackhole (silently drop, no error)</td></tr><tr><td style="text-align:left;"><code>RTN_UNREACHABLE</code></td><td style="text-align:left;"><code>-EHOSTUNREACH</code></td><td style="text-align:left;"><code>RT_SCOPE_UNIVERSE</code></td><td style="text-align:left;">Unreachable (drop and send ICMP Destination Unreachable)</td></tr><tr><td style="text-align:left;"><code>RTN_PROHIBIT</code></td><td style="text-align:left;"><code>-EACCES</code></td><td style="text-align:left;"><code>RT_SCOPE_UNIVERSE</code></td><td style="text-align:left;">Prohibit (rejected by admin, send ICMP Administratively Prohibited)</td></tr><tr><td style="text-align:left;"><code>RTN_THROW</code></td><td style="text-align:left;"><code>-EAGAIN</code></td><td style="text-align:left;"><code>RT_SCOPE_UNIVERSE</code></td><td style="text-align:left;">&quot;Continue to the next table&quot; (used by policy routing)</td></tr><tr><td style="text-align:left;"><code>RTN_NAT</code></td><td style="text-align:left;"><code>-EINVAL</code></td><td style="text-align:left;"><code>RT_SCOPE_NOWHERE</code></td><td style="text-align:left;">NAT (legacy usage)</td></tr><tr><td style="text-align:left;"><code>RTN_XRESOLVE</code></td><td style="text-align:left;"><code>-EINVAL</code></td><td style="text-align:left;"><code>RT_SCOPE_NOWHERE</code></td><td style="text-align:left;">Requires external resolution (e.g., via a daemon)</td></tr></tbody></table><hr><h3 id="route-flags" tabindex="-1">Route Flags <a class="header-anchor" href="#route-flags" aria-label="Permalink to &quot;Route Flags&quot;">​</a></h3><p>When you type <code>route -n</code> or <code>ip route show</code>, that string of abbreviated letters (<code>UG</code>, <code>UH</code>) in the output isn&#39;t gibberish. They are the kernel&#39;s &quot;annotations&quot; for that route.</p><p>Below are the meanings of the common flags, corresponding to the example output in Table 5-3:</p><ul><li><strong><code>U</code> (Route is up)</strong>: The route is active.</li><li><strong><code>H</code> (Target is a host)</strong>: The target is a specific host (usually with a netmask of 255.255.255.255).</li><li><strong><code>G</code> (Use gateway)</strong>: Uses a gateway (the packet isn&#39;t sent directly to the destination subnet, but goes through an intermediary).</li><li><strong><code>R</code> (Reinstate route)</strong>: Reinstate a dynamic route (restarted by a routing daemon).</li><li><strong><code>D</code> (Dynamically installed)</strong>: Dynamically installed (created by a redirect or a daemon).</li><li><strong><code>M</code> (Modified)</strong>: Modified (altered by a redirect or a daemon).</li><li><strong><code>A</code> (Installed by addrconf)</strong>: Auto-configured by addrconf (usually IPv6-related, but appears here too).</li><li><strong><code>!</code> (Reject route)</strong>: Reject route (corresponds to types like <code>RTN_PROHIBIT</code>).</li></ul><h4 id="table-5-3-routing-table-example" tabindex="-1"><strong>Table 5-3: Routing Table Example</strong> <a class="header-anchor" href="#table-5-3-routing-table-example" aria-label="Permalink to &quot;**Table 5-3: Routing Table Example**&quot;">​</a></h4><table tabindex="0"><thead><tr><th style="text-align:left;">Destination</th><th style="text-align:left;">Gateway</th><th style="text-align:left;">Genmask</th><th style="text-align:left;">Flags</th><th style="text-align:left;">Metric</th><th style="text-align:left;">Ref</th><th style="text-align:left;">Use</th><th style="text-align:left;">Iface</th></tr></thead><tbody><tr><td style="text-align:left;">169.254.0.0</td><td style="text-align:left;">0.0.0.0</td><td style="text-align:left;">255.255.0.0</td><td style="text-align:left;"><strong>U</strong></td><td style="text-align:left;">1002</td><td style="text-align:left;">0</td><td style="text-align:left;">0</td><td style="text-align:left;">eth0</td></tr><tr><td style="text-align:left;">192.168.3.0</td><td style="text-align:left;">192.168.2.1</td><td style="text-align:left;">255.255.255.0</td><td style="text-align:left;"><strong>UG</strong></td><td style="text-align:left;">0</td><td style="text-align:left;">0</td><td style="text-align:left;">0</td><td style="text-align:left;">eth1</td></tr></tbody></table><p><strong>Breakdown</strong>:</p><ul><li>First row: Traffic destined for the link-local address <code>169.254.0.0/16</code> goes directly out <code>eth0</code> (no gateway, so no G).</li><li>Second row: Traffic destined for <code>192.168.3.0/24</code> must first be sent to the gateway <code>192.168.2.1</code>, and then goes out via <code>eth1</code>.</li></ul><hr><h3 id="chapter-echoes" tabindex="-1">Chapter Echoes <a class="header-anchor" href="#chapter-echoes" aria-label="Permalink to &quot;Chapter Echoes&quot;">​</a></h3><p>In this chapter, we peeled back the kernel routing subsystem layer by layer, like an onion.</p><p>The outermost layer is the <code>ip route</code> command—that&#39;s what you see; deeper in is the FIB table, where the kernel stores the rules; at the very core are the LC-trie tree and the <code>fib_lookup</code> algorithm—the engine processing millions of flows every second.</p><p>If you take away only one thing from this chapter, let it be this: <strong>A routing decision is not a simple match, but a complete closed loop from lookup, to caching, to dynamic correction (Redirect/FNHE).</strong> The metrics in Table 5-1, the error codes in Table 5-2, and those structures starting with <code>fib_</code> all exist to make this closed loop run fast while remaining flexible enough to adjust its posture when the network topology changes.</p><p>In the next chapter, we&#39;ll shift from &quot;where to go&quot; to &quot;how to send.&quot; We&#39;ll leave the calm deliberations of the routing layer and enter the enthusiastic handshakes of the Neighbor Subsystem—seeing how ARP and ND turn abstract IP addresses into real Ethernet frames and actually push data onto the wire.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: Understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: Understanding&quot;">​</a></h3><p><strong>Question</strong>: In the Linux kernel&#39;s routing lookup process, the <code>fib_lookup()</code> function is the core entry point. Briefly describe the basic flow of how the <code>fib_lookup()</code> function performs a route lookup using the input parameter <code>flowi4</code> and the output parameter <code>fib_result</code>, and explain how the kernel constructs the <code>dst_entry</code> (destination cache) based on <code>fib_result</code> when the lookup succeeds.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: <code>fib_lookup()</code> uses <code>flowi4</code> (which contains the destination address, source address, TOS, etc.) as a key. It first looks up in the Local table; if that fails, it performs a Longest Prefix Match (LPM) in the Main table. On a successful lookup, it populates the <code>fib_result</code> structure (which includes the prefix length, <code>fib_info</code> pointer, route type, etc.). Subsequently, the kernel creates a <code>dst_entry</code> object (embedded in a <code>rtable</code>) based on the information in <code>fib_result</code> (such as whether <code>type</code> is RTN_LOCAL or RTN_UNICAST), and sets its <code>input</code> or <code>output</code> callback functions (like <code>ip_local_deliver</code> or <code>ip_forward</code>).</p><p><strong>Analysis</strong>: This question tests your understanding of the core data flow in the IPv4 routing subsystem. <code>flowi4</code> defines the &quot;key&quot; for the lookup, while <code>fib_result</code> stores the &quot;result.&quot; <code>fib_info</code> is the specific parameter carrier for the routing entry. After a successful lookup, the kernel must translate this static FIB information into a dynamically usable routing cache object (<code>dst_entry</code>). The most important part of this is setting the callback functions that handle the packet, which determines whether the packet is received locally, forwarded, or dropped.</p></details><h3 id="exercise-2-application" tabindex="-1">Exercise 2: Application <a class="header-anchor" href="#exercise-2-application" aria-label="Permalink to &quot;Exercise 2: Application&quot;">​</a></h3><p><strong>Question</strong>: Suppose you are a network administrator and need to configure a Linux server to block traffic from the 192.168.1.0/24 subnet from accessing 10.0.0.5. Write the <code>ip route</code> command to implement this policy, and explain, based on the principles of <code>fib_props</code> and <code>RTN_PROHIBIT</code>, what error code the kernel returns and what ICMP message it sends when it receives a packet matching this rule.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Command: <code>ip route add prohibit 10.0.0.5 from 192.168.1.0/24</code>.</p><p>Principle: The <code>fib_props</code> array in the kernel defines the behavior for different route types. The <code>RTN_PROHIBIT</code> type corresponds to the error code <code>-EACCES</code>. When a packet matches this rule, the routing lookup returns this error, and the kernel immediately invokes <code>ip_error()</code> to handle it, dropping the packet and replying to the sender with an ICMP &quot;Destination Unreachable&quot; message with the code &quot;Packet Filtered&quot; (ICMP_PKT_FILTERED).</p><p><strong>Analysis</strong>: This question tests how to apply the concept of Policy Routing for traffic filtering. Understanding that <code>RTN_PROHIBIT</code> is a type of <code>fib_type</code> is key—it doesn&#39;t just silently drop packets, but has specific interactive behavior (sending ICMP). By looking up the <code>error</code> field corresponding to <code>RTN_PROHIBIT</code> in the <code>fib_props</code> array, you can determine the specific kernel behavior.</p></details><h3 id="exercise-3-thinking" tabindex="-1">Exercise 3: Thinking <a class="header-anchor" href="#exercise-3-thinking" aria-label="Permalink to &quot;Exercise 3: Thinking&quot;">​</a></h3><p><strong>Question</strong>: In early kernel versions (&lt; 3.6), Linux used a routing cache to accelerate lookups, but removed it after version 3.6, relying entirely on the FIB TRIE instead. Analyze the main reasons the kernel development team made this change (adopting FIB TRIE over Routing Cache) from both &quot;performance&quot; and &quot;security/stability&quot; perspectives.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: 1. <strong>Performance</strong>: As routing table sizes grew (internet core routing tables have a massive number of entries), the overhead of maintaining a huge hash cache and its consistency (such as updating invalidated routes) became very large. The LC-trie (a tree structure based on Longest Prefix Match) is inherently very fast at lookups (O(key length)) and highly memory-efficient, making an additional caching layer unnecessary.</p><ol start="2"><li><strong>Security and Stability</strong>: The routing cache was vulnerable to &quot;Shadow Master&quot; type DoS attacks. An attacker could send a massive volume of packets with random destination IPs, forcing the kernel to constantly perform cache-miss lookups and fill the cache, exhausting system memory and CPU. By removing the cache and looking up directly in the FIB TRIE, this attack surface based on cache overflow was eliminated.</li></ol><p><strong>Analysis</strong>: This question tests deep thinking about the evolution of the kernel networking subsystem. This is a classic case of transitioning from &quot;trading space for time&quot; to &quot;algorithmic optimization.&quot; Understanding this requires recognizing that while caching usually accelerates access, in highly dynamic or specific attack scenarios, the management costs of the cache (lock contention, entry refreshes) and its fragility (susceptibility to attack) can outweigh its benefits. The FIB TRIE provides sufficiently high lookup efficiency to allow the removal of complex caching logic.</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>The Linux kernel uses the <strong>FIB (Forwarding Information Base)</strong> and routing lookup mechanisms to determine where packets should go. The core function <code>fib_lookup()</code> queries the routing table based on parameters like the destination address, ultimately generating a <code>dst_entry</code> (routing cache entry) that contains the <code>input</code> and <code>output</code> function pointers. This transforms the complex table lookup process into a call to a specific callback function (such as <code>ip_local_deliver</code> or <code>ip_forward</code>), thereby implementing the logical branching between local reception and forwarding.</p><p>The <strong><code>fib_info</code> structure</strong> is the complete &quot;ID card&quot; of a routing entry in the kernel, encapsulating all metadata except the destination. It not only records the route&#39;s origin (such as static configuration or kernel-generated), scope, and priority, but also manages TCP performance parameters like MTU and RTT through the <code>fib_metrics</code> array. This design separates the routing decision attributes from the physical path, allowing the kernel to efficiently manage the lifecycle of routing entries via reference counting and supporting multipath routing configurations.</p><p>Routing doesn&#39;t always mean permitting traffic. The kernel uses the <strong><code>fib_props</code> mapping table</strong> to translate different route types (like <code>RTN_PROHIBIT</code>) into specific operational behaviors. When a routing lookup hits a &quot;prohibit&quot; type, the kernel doesn&#39;t silently drop the packet; instead, it triggers an ICMP &quot;Destination Unreachable&quot; or &quot;Filtered&quot; message based on the configuration. This mechanism elevates the routing table itself into an efficient traffic control policy system.</p><p>To cope with the dynamic nature of network environments, the kernel introduces an <strong>Exception mechanism</strong> at the <strong><code>fib_nh</code> (next hop)</strong> level. For specific destination addresses, the kernel uses a hash table to record gateway changes brought by ICMP redirects or MTU adjustments brought by PMTU discovery. This &quot;sticky note&quot; style of fine-tuning avoids frequently modifying the massive global routing table, achieving precise correction and isolation for individual paths.</p><p>When multiple routes point to the same destination and physical path but have different attributes (such as TOS or priority), the kernel employs a <strong><code>fib_alias</code></strong> mechanism to optimize memory. It allows multiple lightweight <code>fib_alias</code> structures to share a single <code>fib_info</code> that stores the actual path information. This design avoids duplicating large amounts of routing data for minor differences, significantly improving memory utilization in large-scale routing tables (such as BGP scenarios).</p>',79)])])}const f=t(r,[["render",a]]);export{g as __pageData,f as default};
