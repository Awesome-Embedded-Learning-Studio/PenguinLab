import{_ as t,o as a,c as o,a as n}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"10.7 NAT Traversal in IPsec","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch10_7.md","filePath":"en/notes/linux_kernel_networking/ch10_7.md","lastUpdated":null}'),s={name:"en/notes/linux_kernel_networking/ch10_7.md"};function i(r,e,h,l,c,p){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="_10-7-nat-traversal-in-ipsec" tabindex="-1">10.7 NAT Traversal in IPsec <a class="header-anchor" href="#_10-7-nat-traversal-in-ipsec" aria-label="Permalink to &quot;10.7 NAT Traversal in IPsec&quot;">​</a></h1><p>In the previous section, we were enjoying the sense of certainty brought by <code>xfrm_lookup()</code> — the policy matched, the state was found, the encryption completed, and the packet was sent out. Everything looked great.</p><p>But the real world loves to slap you in the face at moments like this.</p><p>Imagine this: you&#39;ve set up an IPsec VPN at home, connecting to your company&#39;s server. At home, you&#39;re a machine behind NAT (e.g., <code>192.168.1.5</code>), and your router has a public IP. When you eagerly try to bring up the tunnel, you find it either fails to establish or drops immediately after coming up.</p><p>Why? Because in the eyes of that NAT device in the middle — the one responsible for &quot;translating&quot; addresses — your ESP packets are complete gibberish.</p><hr><h3 id="why-nat-devices-break-esp" tabindex="-1">Why NAT Devices Break ESP <a class="header-anchor" href="#why-nat-devices-break-esp" aria-label="Permalink to &quot;Why NAT Devices Break ESP&quot;">​</a></h3><p>Let&#39;s look at this from the NAT device&#39;s perspective. The core job of NAT is simple: rewrite source/destination IP addresses (and sometimes ports), then fix up the checksums in the TCP or UDP headers.</p><p>The keyword here is <strong>fix up the checksums</strong>.</p><p>When the transport layer (TCP/UDP) calculates checksums, it includes the source and destination IP addresses from the IP header (known as the Pseudo-Header). So, once NAT changes the IP address, it must synchronously update the TCP/UDP checksum. Otherwise, the checksum calculated by the receiver won&#39;t match, and the packet gets dropped immediately.</p><p>In transport mode, ESP encrypts the entire TCP/UDP header.</p><p>This is awkward.</p><p>A NAT device looking at an ESP packet faces a dilemma:</p><ol><li>It wants to change the IP address.</li><li>It wants to update the TCP/UDP checksum.</li><li>But the TCP/UDP header is encrypted — it can&#39;t read it, let alone calculate the checksum.</li></ol><p><strong>Result</strong>: The NAT device can only do one of two things: either forward the packet as-is (which usually fails because private IPs can&#39;t route out), or — in the vast majority of cases — drop the packet outright. This causes IPsec to fail completely in NAT environments.</p><p>But not all protocols have this problem. SCTP, for example, has a checksum that doesn&#39;t cover the IP header, making it &quot;immune&quot; to NAT changes. Unfortunately, what we primarily use day-to-day are TCP and UDP.</p><p>To solve this problem, the IETF came up with a standard (RFC 3948) with a very straightforward name — <strong>UDP Encapsulation of IPsec ESP Packets</strong>. This is what we commonly call NAT-T (NAT Traversal).</p><p>Its core idea is simple: since you don&#39;t recognize ESP, I&#39;ll just disguise it as your favorite protocol — UDP.</p><hr><h3 id="the-rules-of-nat-t" tabindex="-1">The Rules of NAT-T <a class="header-anchor" href="#the-rules-of-nat-t" aria-label="Permalink to &quot;The Rules of NAT-T&quot;">​</a></h3><p>Before diving into the code, let&#39;s highlight a few key points to save you from some pitfalls:</p><ol><li><strong>ESP only, not AH</strong>: The AH protocol performs integrity checks on the IP header itself. Once NAT changes the IP address, the AH verification will fail. Therefore, NAT-T only works with ESP. If you&#39;re still using AH, there&#39;s basically no solution in a NAT environment.</li><li><strong>No manual keys</strong>: NAT-T cannot be used with manual keys. It must rely on the negotiation mechanism of IKEv1 or IKEv2. Why? Because enabling NAT-T requires a handshake confirmation between both parties, and this process is part of the IKE protocol.</li><li><strong>Software-level compromises</strong>: Although NAT-T is an IPsec matter, many P2P applications (especially VoIP) actually deal with similar problems. Protocols like STUN, TURN, and ICE all exist to solve the problem of &quot;how to establish a connection from behind a NAT.&quot; strongSwan even implements a TURN-like relay service, allowing two clients both behind NAT to establish a connection through a third party.</li></ol><hr><h3 id="⚙️-how-nat-t-works" tabindex="-1">⚙️ How NAT-T Works <a class="header-anchor" href="#⚙️-how-nat-t-works" aria-label="Permalink to &quot;⚙️ How NAT-T Works&quot;">​</a></h3><p>The goal of NAT-T is very clear: forcefully insert a UDP header between the IP header and the ESP header.</p><p>This way, the NAT device sees a standard UDP packet. It can change the IP, change the port, and calculate the UDP checksum — everyone is happy.</p><h4 id="_1-enabling-and-negotiation" tabindex="-1">1. Enabling and Negotiation <a class="header-anchor" href="#_1-enabling-and-negotiation" aria-label="Permalink to &quot;1. Enabling and Negotiation&quot;">​</a></h4><p>This step happens in user space, but the kernel must cooperate.</p><p>In older software (like Openswan), you had to explicitly write <code>nat_traversal=yes</code> in <code>/etc/ipsec.conf</code> to activate this feature. In strongSwan&#39;s IKEv2 daemon Charon, however, NAT-T is enabled by default and cannot be turned off — the author clearly believes that in today&#39;s network environment, NAT is the norm, and not supporting NAT-T is just shooting yourself in the foot.</p><p>The negotiation process goes roughly like this:</p><ul><li><p><strong>Step 1: Confirming Capability</strong> During the Main Mode phase of IKEv1, both parties exchange ISAKMP messages. If they support NAT-T, they include a specific marker in the Vendor ID field. In IKEv2, this is already part of the standard, so no special declaration is needed.</p></li><li><p><strong>Step 2: NAT Detection</strong> Both parties send each other NAT-D (NAT Discovery) payloads. This is essentially a probe packet: I know my own IP, and I know the IP I see for you. If these values don&#39;t match, or if the IP addresses along the path have changed, we know there&#39;s a NAT device in the middle &quot;messing things up.&quot;</p></li></ul><p>Once it&#39;s confirmed that there&#39;s NAT on the path, both parties immediately switch to NAT-T mode.</p><h4 id="_2-udp-encapsulation-structure" tabindex="-1">2. UDP Encapsulation Structure <a class="header-anchor" href="#_2-udp-encapsulation-structure" aria-label="Permalink to &quot;2. UDP Encapsulation Structure&quot;">​</a></h4><p>After switching modes, an ESP packet that originally looked like this:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[ IP Header ] [ ESP Header ] [ Encrypted Data ] [ ESP Trailer ] [ ICV ]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Now looks like this:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[ IP Header ] [ UDP Header ] [ ESP Header ] [ Encrypted Data ] [ ESP Trailer ] [ ICV ]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><strong>Key details</strong>:</p><ul><li><strong>Port numbers</strong>: Both the source and destination ports in the UDP header are <strong>4500</strong>. This is the NAT-T dedicated port assigned by IANA.</li><li><strong>Keep-Alive mechanism</strong>: NAT mappings have a timeout. If there&#39;s no traffic for a long time, the NAT device will delete the mapping. To prevent this, NAT-T requires sending a Keep-Alive packet every 20 seconds. <ul><li><strong>What the Keep-Alive looks like</strong>: It&#39;s a packet sent to UDP port 4500 with a payload of exactly one byte, with the value <code>0xFF</code>. When the kernel receives this packet, it knows this is just &quot;keeping the connection alive&quot; — no decryption is needed, and it doesn&#39;t need to be delivered to the upper layers.</li></ul></li></ul><h4 id="_3-kernel-space-receive-path-xfrm4-udp-encap-rcv" tabindex="-1">3. Kernel-Space Receive Path (<code>xfrm4_udp_encap_rcv</code>) <a class="header-anchor" href="#_3-kernel-space-receive-path-xfrm4-udp-encap-rcv" aria-label="Permalink to &quot;3. Kernel-Space Receive Path (`xfrm4_udp_encap_rcv`)&quot;">​</a></h4><p>When this &quot;disguised&quot; UDP packet arrives at the receiver&#39;s kernel, what happens?</p><p>When we looked at the IPsec receive path before, the packet went directly from the IP layer (the callback registered by <code>ip_protocol</code>) into the ESP processing logic. But that won&#39;t work now, because the IP layer sees a UDP protocol, and the kernel will treat it as a normal UDP packet and hand it to the UDP stack.</p><p>After receiving the packet, the UDP stack checks whether this is an &quot;encapsulated&quot; packet.</p><p>In <code>net/ipv4/xfrm4_input.c</code>, there&#39;s a function specifically designed to handle this case: <strong><code>xfrm4_udp_encap_rcv()</code></strong>.</p><p>Its logic is actually very straightforward (Mode B):</p><ol><li><strong>Intercept</strong>: Before processing the data, the UDP stack calls this function.</li><li><strong>Strip the disguise</strong>: The function checks if the UDP port is 4500, then inspects the UDP payload. <ul><li>If the payload is <code>0xFF</code> (Keep-Alive), it drops it immediately. Job done.</li><li>If it&#39;s an encapsulated ESP packet, it calls <code>xfrm4_rcv_encap()</code>.</li></ul></li><li><strong>Hand off to ESP</strong>: <code>xfrm4_rcv_encap()</code> strips the UDP header and passes a pointer to the ESP header to the normal IPsec receive path (<code>xfrm_input()</code>).</li></ol><p>To the kernel, after stripping the UDP header, the packet is indistinguishable from a normal ESP packet. The remaining decryption, verification, and reassembly processes all reuse the logic we covered in the previous section.</p><hr><p><strong>Returning to the &quot;translator&quot; analogy</strong>:</p><p>You can think of NAT-T as putting an encrypted document inside a standard shipping envelope. The post office (NAT) only looks at the address on the envelope (IP and UDP port). It doesn&#39;t care what&#39;s inside the envelope — as long as the address is correct, it handles the delivery. Only when the envelope reaches its destination does the recipient (the kernel) tear open the envelope and take out the encrypted document for processing.</p><p>The sole purpose of this envelope (the UDP header) is to fool the post offices along the way. While this might seem like a roundabout approach, given the reality of IPv4 address exhaustion and NAT being everywhere, it&#39;s the most practical solution.</p><hr><p>At this point, we&#39;ve connected all the dots of IPsec&#39;s core mechanisms — from the underlying XFRM framework, to the integration of cryptographic algorithms, to the interplay between policies and states, and finally the UDP encapsulation necessitated by traversing NAT.</p><p>This isn&#39;t just a bunch of protocols and code; it&#39;s a complex engineering system that evolved to establish secure channels over insecure networks. Even for a design like NAT that &quot;breaks&quot; the internet&#39;s end-to-end principle, IPsec can adapt by adding a layer of encapsulation. This kind of compromise and adaptation is, perhaps, the true norm of the engineering world.</p>',54)])])}const g=t(s,[["render",i]]);export{u as __pageData,g as default};
