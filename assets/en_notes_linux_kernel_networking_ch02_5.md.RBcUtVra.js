import{_ as t,o as s,c as i,a}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"2.5 Adding and Removing Routing Table Entries: Dancing in the FIB","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch02_5.md","filePath":"en/notes/linux_kernel_networking/ch02_5.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_networking/ch02_5.md"};function o(r,e,l,d,h,p){return s(),i("div",null,[...e[0]||(e[0]=[a('<h1 id="_2-5-adding-and-removing-routing-table-entries-dancing-in-the-fib" tabindex="-1">2.5 Adding and Removing Routing Table Entries: Dancing in the FIB <a class="header-anchor" href="#_2-5-adding-and-removing-routing-table-entries-dancing-in-the-fib" aria-label="Permalink to &quot;2.5 Adding and Removing Routing Table Entries: Dancing in the FIB&quot;">​</a></h1><p>In the previous section, we broke down the Netlink message header and those dazzling flag bits. Now, it&#39;s time to throw that theory into a real-world scenario.</p><p>We&#39;re going to look at a very specific and frequent operation: <strong>adding a route to the kernel&#39;s routing table (FIB), or removing it.</strong></p><p>This is an excellent observation sample because it completely demonstrates how a user-space command pulls the kernel&#39;s FIB subsystem along through the Netlink thread. Moreover, it hides a killer feature of Netlink—event broadcasting.</p><hr><h3 id="behind-the-scenes-from-ip-commands-to-kernel-callbacks" tabindex="-1">Behind the Scenes: From <code>ip</code> Commands to Kernel Callbacks <a class="header-anchor" href="#behind-the-scenes-from-ip-commands-to-kernel-callbacks" aria-label="Permalink to &quot;Behind the Scenes: From `ip` Commands to Kernel Callbacks&quot;">​</a></h3><p>Let&#39;s look at adding a route first.</p><p>You type this line in the command line:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.2.11</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> via</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.2.20</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This command doesn&#39;t directly modify memory. What it does is very pure: it builds a Netlink message of type <code>RTM_NEWROUTE</code> in user space, then throws it into the kernel through a <code>AF_NETLINK</code> socket.</p><p>Once the message crosses the boundary, the kernel&#39;s <code>rtnetlink</code> mechanism starts turning.</p><ol><li><strong>Receiving</strong>: The kernel&#39;s <code>rtnetlink</code> socket receives this message and hands it to the <code>rtnetlink_rcv()</code> method. This is the main entry point for the entire routing subsystem.</li><li><strong>Dispatching</strong>: <code>rtnetlink_rcv()</code> sees that the message type is <code>RTM_NEWROUTE</code> and looks for who should handle this.</li><li><strong>Execution</strong>: Ultimately, execution is handed to the <code>inet_rtm_newroute()</code> function in <code>net/ipv4/fib_frontend.c</code>. This is where the real heavy lifting happens.</li></ol><p><code>inet_rtm_newroute()</code> doesn&#39;t just simply insert a row into an array—it has to manipulate the FIB (Forwarding Information Base). This is the &quot;bible&quot; of kernel routing decisions; any slight error will cause network packets to either fail to send or go straight to hell. It calls <code>fib_table_insert()</code> to actually write this route into the hardware-independent routing table database.</p><h3 id="broadcasting-don-t-keep-it-a-secret" tabindex="-1">Broadcasting: Don&#39;t Keep It a Secret <a class="header-anchor" href="#broadcasting-don-t-keep-it-a-secret" aria-label="Permalink to &quot;Broadcasting: Don&#39;t Keep It a Secret&quot;">​</a></h3><p>If things ended here, Netlink would only be a &quot;fancy IOCTL.&quot;</p><p>But Netlink is different. After <code>fib_table_insert()</code> writes the route into the database, it does another thing—<strong>shouts it out</strong>.</p><p>It calls the <code>rtmsg_fib()</code> method, passing in the <code>RTM_NEWROUTE</code> parameter. <code>rtmsg_fib()</code>&#39;s job isn&#39;t to manipulate routes, but to construct a new Netlink notification message and then call <code>rtnl_notify()</code>.</p><p>This &quot;shout&quot; is sent to the <code>RTNLGRP_IPV4_ROUTE</code> multicast group.</p><p>It&#39;s like someone shouting in a noisy room: &quot;Hey everyone, a route to 192.168.2.11 was just added!&quot; Everyone who &quot;subscribed&quot; to this channel can hear it.</p><ul><li>Tools in the <code>iproute2</code> suite are listening.</li><li>Advanced user-space routing daemons (like <code>xorp</code> or <code>bird</code>) are also listening.</li><li>Even other kernel modules, if they registered for this group, can hear it too.</li></ul><p>This &quot;work and broadcast simultaneously&quot; design is the foundation of modern Linux network management. It means user-space daemons don&#39;t need to poll the kernel to check for table changes; the kernel proactively pushes them.</p><hr><h3 id="deleting-a-route-same-script-different-ending" tabindex="-1">Deleting a Route: Same Script, Different Ending <a class="header-anchor" href="#deleting-a-route-same-script-different-ending" aria-label="Permalink to &quot;Deleting a Route: Same Script, Different Ending&quot;">​</a></h3><p>The delete operation follows almost exactly the same flow, just with a few swapped nouns.</p><p>You execute:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> del</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.2.11</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This time, the <code>ip</code> command generates a <code>RTM_DELROUTE</code> message.</p><ol><li>The message flies into the kernel and hits <code>rtnetlink_rcv()</code> again.</li><li>This time it&#39;s dispatched to the <code>inet_rtm_delroute()</code> callback function (also in <code>fib_frontend.c</code>).</li><li><code>inet_rtm_delroute()</code> calls <code>fib_table_delete()</code> to erase this record from the FIB.</li><li>Similarly, it calls <code>rtmsg_fib()</code> here, except this time it passes in <code>RTM_DELROUTE</code>.</li><li><code>rtnl_notify()</code> sets off again, broadcasting to <code>RTNLGRP_IPV4_ROUTE</code>: &quot;Hey everyone, that route was just deleted.&quot;</li></ol><p>You&#39;ll notice that when the kernel handles &quot;add&quot; and &quot;delete,&quot; the logic is symmetric, with the focus on both: <strong>completing the actual data structure operations and ensuring subscribers receive notifications.</strong></p><hr><h3 id="hands-on-verification-listening-to-the-kernel-s-heartbeat" tabindex="-1">Hands-on Verification: Listening to the Kernel&#39;s Heartbeat <a class="header-anchor" href="#hands-on-verification-listening-to-the-kernel-s-heartbeat" aria-label="Permalink to &quot;Hands-on Verification: Listening to the Kernel&#39;s Heartbeat&quot;">​</a></h3><p>Talk is cheap. Let&#39;s see this broadcasting mechanism with our own eyes.</p><p>We need two terminals. It&#39;s like if you want to verify whether someone is shouting in a room, the best way is to bring your own radio in.</p><p><strong>Terminal 1: Start Listening</strong></p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> monitor</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This command starts a background daemon. What does it do? It opens a Netlink socket and executes <code>setsockopt()</code> to join the <code>RTNLGRP_IPV4_ROUTE</code> multicast group. Now, it&#39;s all ears waiting for messages.</p><p><strong>Terminal 2: Generate an Event</strong></p><p>Now in the second terminal, let&#39;s add a random route:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.1.10</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> via</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.2.200</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>In that instant, the <code>ip</code> command sends a request, the kernel processes the request, and then the kernel broadcasts a notification.</p><p><strong>Back in Terminal 1</strong>, you should immediately see a line of output:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>192.168.1.10 via 192.168.2.200 dev em1</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>See that? You didn&#39;t type that command; this is the notification broadcast back from the kernel. Your listening process received the kernel&#39;s <code>rtnl_notify()</code> and printed it out.</p><p>Next, delete it in Terminal 2:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> del</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.1.10</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Terminal 1 will immediately show:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Deleted 192.168.1.10 via 192.168.2.200 dev em1</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Notice that <code>Deleted</code> prefix—that&#39;s a friendly hint from <code>ip monitor</code>, identifying that the broadcast message type is <code>RTM_DELROUTE</code>.</p><h3 id="more-channels-links-and-vlans" tabindex="-1">More Channels: Links and VLANs <a class="header-anchor" href="#more-channels-links-and-vlans" aria-label="Permalink to &quot;More Channels: Links and VLANs&quot;">​</a></h3><p>Routes aren&#39;t the only things that can be broadcast. <code>rtnetlink</code> defines various multicast groups.</p><p>You can try listening to &quot;link&quot; layer events. Execute this in Terminal 1:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> monitor</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> link</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This makes the Netlink socket join the <code>RTNLGRP_LINK</code> group. As long as any network interface state changes—UP/DOWN, new VLAN, added Bridge—you can catch it.</p><p>Now go to Terminal 2 and try adding a VLAN interface:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vconfig</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> eth1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 200</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Terminal 1 will instantly spit out a bunch of information:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>4: eth1.200@eth1: &lt;BROADCAST,MULTICAST&gt; mtu 1500 qdisc noop state DOWN</span></span>\n<span class="line"><span>    link/ether 00:e0:4c:53:44:58 brd ff:ff:ff:ff:ff:ff</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Let&#39;s try adding a bridge too:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">brctl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> addbr</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mybr</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Terminal 1 continues to follow up:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>5: mybr: &lt;BROADCAST,MULTICAST&gt; mtu 1500 qdisc noop state DOWN</span></span>\n<span class="line"><span>    link/ether a2:7c:be:62:b5:b6 brd ff:ff:ff:ff:ff:ff</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>This is why tools like NetworkManager can react so fast—they don&#39;t need to poll files under <code>/sys</code> at all. They just sit quietly next to a Netlink socket, waiting for the kernel to tell them what happened.</p><hr><h3 id="chapter-summary" tabindex="-1">Chapter Summary <a class="header-anchor" href="#chapter-summary" aria-label="Permalink to &quot;Chapter Summary&quot;">​</a></h3><p>By this point, you&#39;ve seen through the full bag of tricks of Netlink Sockets:</p><ol><li>It is a standard socket-based IPC channel, replacing the legacy IOCTL.</li><li>It has a strict message format (header + TLV attributes) capable of carrying complex network configurations.</li><li>Most importantly, it is <strong>multicast</strong>. The kernel doesn&#39;t just passively receive commands; it proactively broadcasts network topology changes to subscribers.</li></ol><p>This mechanism forms the cornerstone of modern Linux network management. Whether it&#39;s the <code>ip</code> command or the underlying <code>rtnetlink</code>, they&#39;re essentially all playing by these rules.</p><p>But there&#39;s a problem.</p><p>If you&#39;re a kernel developer and you want to add a Netlink interface to some non-standard kernel module you wrote, what would you do? <code>rtnetlink</code> is for the networking subsystem; you can&#39;t just force your random hardware control messages into a type like <code>RTM_NEWROUTE</code>.</p><p>The 32 standard Netlink protocol family slots are actually in short supply. To solve this &quot;address space crisis,&quot; the kernel later introduced an extension mechanism.</p><p>In the next section, we&#39;ll talk about <strong>Generic Netlink</strong>—the ultimate multiplexing scheme designed to solve the &quot;not enough sockets&quot; problem.</p>',71)])])}const g=t(n,[["render",o]]);export{u as __pageData,g as default};
