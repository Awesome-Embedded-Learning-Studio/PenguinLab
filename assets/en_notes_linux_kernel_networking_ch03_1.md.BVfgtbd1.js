import{_ as e,o as i,c as a,a as n}from"./chunks/framework.8tiMeCK-.js";const k=JSON.parse(`{"title":"3.0  The Network's Nervous System: Why We Need ICMP","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch03_1.md","filePath":"en/notes/linux_kernel_networking/ch03_1.md","lastUpdated":null}`),t={name:"en/notes/linux_kernel_networking/ch03_1.md"};function r(l,s,o,h,p,c){return i(),a("div",null,[...s[0]||(s[0]=[n(`<h1 id="_3-0-the-network-s-nervous-system-why-we-need-icmp" tabindex="-1">3.0 The Network&#39;s Nervous System: Why We Need ICMP <a class="header-anchor" href="#_3-0-the-network-s-nervous-system-why-we-need-icmp" aria-label="Permalink to &quot;3.0  The Network&#39;s Nervous System: Why We Need ICMP&quot;">​</a></h1><p>Imagine managing a massive, globally distributed package delivery system.</p><p>Billions of packages flow through this network every day. As the manager, you don&#39;t need to physically handle every package, but you absolutely need a mechanism to tell you: <strong>is something wrong with the system?</strong></p><p>If a sorting center suddenly goes offline, if a route becomes completely blocked, or if a sender fills in the wrong address, you need to know immediately. You can&#39;t wait until packages vanish into thin air to start investigating—that would be too late.</p><p>The designers of the internet faced the exact same problem. The IP protocol is essentially a &quot;best-effort&quot; delivery service—it handles sending but doesn&#39;t care about the result. Without a feedback mechanism, the network would be an undebuggable black box: packets would simply disappear, with no one knowing why or where to look.</p><p>That is the very purpose of ICMP. It acts as the nervous system of the internet, dedicated to delivering error reports, diagnostic information, and control messages.</p><p>You use it every day. When you type <code>ping google.com</code> to test connectivity, you&#39;re using ICMP; when you use <code>traceroute</code> to trace the routers a packet passes through, you&#39;re also leveraging ICMP&#39;s behavioral characteristics.</p><p>In this chapter, we&#39;re going to take this &quot;nervous system&quot; apart. We won&#39;t be looking at how user tools are used, but rather at what exactly happens inside the kernel when it receives these peculiar packets.</p><hr><h2 id="_3-1-icmpv4-the-diagnostic-core-of-ipv4" tabindex="-1">3.1 ICMPv4: The Diagnostic Core of IPv4 <a class="header-anchor" href="#_3-1-icmpv4-the-diagnostic-core-of-ipv4" aria-label="Permalink to &quot;3.1  ICMPv4: The Diagnostic Core of IPv4&quot;">​</a></h2><h3 id="the-two-faces-of-icmpv4-messages" tabindex="-1">The Two Faces of ICMPv4 Messages <a class="header-anchor" href="#the-two-faces-of-icmpv4-messages" aria-label="Permalink to &quot;The Two Faces of ICMPv4 Messages&quot;">​</a></h3><p>ICMPv4 messages fall into two broad categories: <strong>Error Messages</strong> and <strong>Information Messages</strong> (also known as Query Messages in RFC 1812).</p><p>The most famous tool—<strong>ping</strong>—is essentially a user-space test program (usually included in the <code>iputils</code> package). Its working principle is very straightforward: it opens a <strong>Raw Socket</strong>, sends an ICMP_ECHO (Echo Request) message, and then waits for the other end to reply with an ICMP_ECHOREPLY (Echo Reply).</p><p>Another commonly used tool, <strong>Traceroute</strong>, is used to probe the path a packet takes from a host to a destination IP. Its design is quite clever, leveraging a field in the IP header—<strong>TTL (Time To Live)</strong>. This field represents how many more routers (hops) a packet can pass through. Traceroute exploits the following rule: <strong>when a forwarding device receives a packet whose TTL has been decremented to 0, it must drop the packet and send back an ICMP_TIME_EXCEEDED message</strong>.</p><p>Traceroute starts by sending a packet with a TTL of 1. The first-hop router receives it, the TTL reaches zero, and it sends back an ICMP Time Exceeded message; next, the TTL is set to 2, and the second-hop router replies... and so on, until the final destination returns an ICMP Echo Reply. Through these intermediate ICMP reports, Traceroute can piece together a complete routing map.</p><p>Although we&#39;ll discuss this in detail in later chapters, it&#39;s worth mentioning that starting from kernel version 3.0, Linux introduced a new feature: <strong>ICMP Sockets (Ping Sockets)</strong>. This allows regular users (non-root) to send Ping requests by creating a non-Raw Socket, eliminating the need for the ping tool to set the dangerous <code>setuid root</code> bit.</p><hr><h3 id="initialization-inet-init-and-icmp-sk-init" tabindex="-1">Initialization: inet_init() and icmp_sk_init() <a class="header-anchor" href="#initialization-inet-init-and-icmp-sk-init" aria-label="Permalink to &quot;Initialization: inet_init() and icmp_sk_init()&quot;">​</a></h3><p>ICMPv4 initialization doesn&#39;t happen during driver loading because it is part of the kernel&#39;s core Network Stack and <strong>cannot be compiled as a Kernel Module</strong>. All of this happens early in the kernel boot process.</p><p>The starting point of the entire process is in <code>inet_init()</code>. In this function, the kernel does two major things: first, it registers the ICMP handler with the protocol stack, and second, it prepares for the transmission of ICMP messages.</p><h4 id="registering-the-protocol-handler" tabindex="-1">Registering the Protocol Handler <a class="header-anchor" href="#registering-the-protocol-handler" aria-label="Permalink to &quot;Registering the Protocol Handler&quot;">​</a></h4><p>Just as we saw in the TCP/UDP chapters, ICMP is also a protocol, and it must be registered in the kernel&#39;s protocol dispatch array. This happens in <code>inet_init()</code>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_protocol icmp_protocol </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .handler        </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  icmp_rcv,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .err_handler    </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  icmp_err,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .no_policy      </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .netns_ok       </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><ul><li><code>icmp_rcv</code>: This is the core callback function. When the IP layer receives a packet with a protocol field of <code>IPPROTO_ICMP (0x1)</code>, this function is ultimately called.</li><li><code>no_policy</code>: Setting this flag to 1 means IPsec policy checks are <strong>not required</strong>. This is an important optimization—for example, in <code>ip_local_deliver_finish()</code>, the kernel sees this flag and skips <code>xfrm4_policy_check()</code>, because for ICMP control messages, security policies are usually not the primary concern.</li><li><code>netns_ok</code>: Setting this to 1 indicates that this protocol is <strong>network Namespace aware</strong>. If set to 0, <code>inet_add_protocol()</code> will directly fail and return <code>-EINVAL</code>.</li></ul><p>The registration process is straightforward:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">inet_add_protocol</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">icmp_protocol</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, IPPROTO_ICMP) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    pr_crit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">: Cannot add ICMP protocol</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, __func__);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><h4 id="creating-sockets-for-transmission" tabindex="-1">Creating Sockets for Transmission <a class="header-anchor" href="#creating-sockets-for-transmission" aria-label="Permalink to &quot;Creating Sockets for Transmission&quot;">​</a></h4><p>Just knowing how to receive isn&#39;t enough; the kernel also needs to know how to <strong>send</strong> ICMP packets. This is where <code>icmp_sk_init()</code> comes in.</p><p>There is an interesting design detail here: the kernel <strong>creates a dedicated Raw Socket for each CPU</strong>.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __net_init </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">icmp_sk_init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">net</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    for_each_possible_cpu</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(i) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sock </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sk;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> </span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        err </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> inet_ctl_sock_create</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sk, PF_INET,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                       SOCK_RAW, IPPROTO_ICMP, net);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (err </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            goto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> fail;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> </span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        net-&gt;ipv4.icmp_sk[i] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        . . .</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        sock_set_flag</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(sk, SOCK_USE_WRITE_QUEUE);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        inet_sk</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(sk)-&gt;pmtudisc </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> IP_PMTUDISC_DONT;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br></div></div><p>Why go to this trouble? Because in a multi-core system, if all CPUs contend for a single Socket to send data, lock contention would be severe. Through the <code>icmp_sk(struct net *net)</code> method, the kernel can quickly obtain the Socket corresponding to the current CPU and use it directly for <code>icmp_push_reply()</code> to send data.</p><p>Here we see two configurations:</p><ul><li><code>SOCK_USE_WRITE_QUEUE</code>: Indicates the use of a write queue.</li><li><code>IP_PMTUDISC_DONT</code>: Disables PMTU (Path MTU) discovery. Because for control messages like ICMP error notifications, we want to ensure they are delivered as much as possible, and they shouldn&#39;t be dropped or fragmented due to MTU issues.</li></ul><hr><h3 id="icmpv4-message-header-struct-icmphdr" tabindex="-1">ICMPv4 Message Header: struct icmphdr <a class="header-anchor" href="#icmpv4-message-header-struct-icmphdr" aria-label="Permalink to &quot;ICMPv4 Message Header: struct icmphdr&quot;">​</a></h3><p>The header of every ICMP packet shares the same skeleton, but the payload varies.</p><p>The header contains: Type (8 bits), Code (8 bits), Checksum (16 bits), and a 32-bit variable part. The exact content of these 32 bits depends on the type and code.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> icmphdr {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  __u8        type;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  __u8        code;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  __sum16     checksum;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  union</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        __be16    id;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        __be16    sequence;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } echo;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __be32      gateway;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        __be16    __unused;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        __be16    mtu;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } frag;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  } un;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br></div></div><p>Immediately following this header is usually <strong>the header and partial payload of the original IP packet that triggered this ICMP message</strong>. RFC 1812 specifies that this should include as much of the original datagram as possible, but the total length of the ICMP datagram must not exceed <strong>576 bytes</strong>. This is the minimum MTU requirement for IPv4 (originating from RFC 791), ensuring that any network device can handle a packet of this size.</p><hr><h3 id="dispatch-mechanism-the-icmp-pointers-array" tabindex="-1">Dispatch Mechanism: The icmp_pointers Array <a class="header-anchor" href="#dispatch-mechanism-the-icmp-pointers-array" aria-label="Permalink to &quot;Dispatch Mechanism: The icmp_pointers Array&quot;">​</a></h3><p>How does the kernel know whether a received ICMP packet should be handed to Ping or to the routing table?</p><p>The answer is table lookup.</p><p>The kernel defines a global array, <code>icmp_pointers</code>, which uses the ICMP message type as an index and stores the corresponding <code>icmp_control</code> structure.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> icmp_control {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">handler)(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">skb);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    short</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> error;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        /* 是否归类为错误消息 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> icmp_control </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">icmp_pointers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[NR_ICMP_TYPES</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p><code>NR_ICMP_TYPES</code> is the maximum ICMP type number (18).</p><p>If the <code>error</code> field of a certain type is 1, it means it is an <strong>Error Message</strong> (such as Destination Unreachable); if it is 0 (implicit), it is an <strong>Information Message</strong> (such as Echo).</p><p>Next, let&#39;s look at a few key <code>handler</code> entries, which are the soul of ICMP processing logic.</p><h4 id="_1-ping-rcv-more-than-just-an-echo" tabindex="-1">1. ping_rcv(): More Than Just an Echo <a class="header-anchor" href="#_1-ping-rcv-more-than-just-an-echo" aria-label="Permalink to &quot;1. ping_rcv(): More Than Just an Echo&quot;">​</a></h4><p>Before kernel 3.0, Ping replies (ICMP_ECHOREPLY) were handled directly by user-space Raw Sockets. Back then, <code>ip_local_deliver_finish()</code> would first try to deliver the packet to a Raw Socket, and if the Raw Socket consumed it, the protocol layer&#39;s <code>handler</code> would not be called again.</p><p>But after the introduction of the <strong>ICMP Sockets</strong> mechanism, things changed. You no longer need root privileges to create a non-Raw ICMP Socket (such as <code>socket(PF_INET, SOCK_DGRAM, PROT_ICMP)</code>). Since the sender is not a Raw Socket, the returning reply cannot be matched to a Raw Socket, and naturally, no one would receive it.</p><p>To solve this problem, a hook called <code>ping_rcv()</code> was specifically attached to <code>ICMP_ECHOREPLY</code> in <code>icmp_pointers</code>.</p><p>This function is implemented in <code>net/ipv4/ping.c</code>. Interestingly, this file is <strong>dual-stack</strong>, meaning it handles both IPv4 Echo Replies and IPv6 ones (ICMPV6_ECHO_REPLY).</p><h4 id="_2-icmp-discard-silence-is-golden" tabindex="-1">2. icmp_discard(): Silence is Golden <a class="header-anchor" href="#_2-icmp-discard-silence-is-golden" aria-label="Permalink to &quot;2. icmp_discard(): Silence is Golden&quot;">​</a></h4><p>For some messages, the kernel doesn&#39;t need to do anything upon receiving them and simply drops them.</p><p>The most typical example is <code>ICMP_TIMESTAMPREPLY</code> (Timestamp Reply). Although ICMP has a timestamp feature, modern networks have long used <strong>NTP (Network Time Protocol)</strong> for time synchronization, which offers higher precision and more functionality. The ICMP Timestamp is mostly a legacy feature.</p><p>Address Mask related messages are also discarded. Previously, hosts would use <code>ICMP_ADDRESS</code> to ask routers for the subnet mask, but now everyone uses DHCP, making this feature obsolete.</p><h4 id="_3-icmp-unreach-icmp-redirect-and-others" tabindex="-1">3. icmp_unreach(), icmp_redirect(), and Others <a class="header-anchor" href="#_3-icmp-unreach-icmp-redirect-and-others" aria-label="Permalink to &quot;3. icmp_unreach(), icmp_redirect(), and Others&quot;">​</a></h4><ul><li><strong>icmp_unreach()</strong>: Handles <code>ICMP_DEST_UNREACH</code> (Destination Unreachable), <code>ICMP_TIME_EXCEED</code> (Time Exceeded), <code>ICMP_PARAMETERPROB</code> (Parameter Problem), etc. <ul><li><strong>TTL reaching zero</strong>: In <code>ip_forward()</code>, the TTL is decremented by 1 on each forward. Once it reaches zero, the router calls <code>icmp_send(skb, ICMP_TIME_EXCEEDED, ICMP_EXC_TTL, 0)</code> and drops the packet.</li><li><strong>Fragment reassembly timeout</strong>: If fragment reassembly times out, <code>ip_expire()</code> sends a <code>ICMP_EXC_FRAGTIME</code> error.</li></ul></li><li><strong>icmp_redirect()</strong>: Handles <code>ICMP_REDIRECT</code> (Redirect). According to RFC 1122, hosts should not send redirects; only gateways do. Previously, this would call <code>ip_rt_redirect()</code> to update the routing table, but starting from kernel 3.6, this logic was moved to the protocol handling code, and <code>icmp_redirect()</code> now only handles checksum verification and dispatch. This is an architectural adjustment the kernel made to address security risks (such as ICMP redirect attacks).</li><li><strong>icmp_echo()</strong>: Handles <code>ICMP_ECHO</code> requests. It changes the packet&#39;s type to <code>ICMP_ECHOREPLY</code> and calls <code>icmp_reply()</code> to send it back. Unless you have set <code>sysctl_icmp_echo_ignore_all</code>.</li></ul><hr><h3 id="receiving-flow-the-rigorous-logic-of-icmp-rcv" tabindex="-1">Receiving Flow: The Rigorous Logic of icmp_rcv() <a class="header-anchor" href="#receiving-flow-the-rigorous-logic-of-icmp-rcv" aria-label="Permalink to &quot;Receiving Flow: The Rigorous Logic of icmp_rcv()&quot;">​</a></h3><p>All ICMPv4 packets ultimately converge in the <code>icmp_rcv()</code> function. Although this function doesn&#39;t directly handle specific business logic (that&#39;s the job of individual handlers), it performs a substantial amount of security checks.</p><ol><li><p><strong>Statistics and Checksum</strong>: First, it increments the <code>InMsgs</code> counter. Immediately after, the <strong>checksum must be correct</strong>. If it&#39;s wrong, it increments <code>InCsumErrors</code> and <code>InErrors</code>, and then directly calls <code>kfree_skb</code>.</p><blockquote><p>Here&#39;s a detail: <code>icmp_rcv()</code> always returns 0, even if the checksum is wrong, it doesn&#39;t return a negative value. This is because if the protocol layer returns an error, the kernel might attempt retransmission or other processing, but for a corrupted ICMP packet, we just want it to quietly disappear.</p></blockquote></li><li><p><strong>Type Check</strong>: Checks if the message type is valid (less than <code>NR_ICMP_TYPES</code>). If it&#39;s an unknown type, RFC 1122 requires that it must be <strong>silently discarded</strong>. It increments <code>InErrors</code> and drops the packet.</p></li><li><p><strong>Broadcast and Multicast Suppression</strong>: If the received packet is destined for a broadcast or multicast address, and it&#39;s an Echo Request or Timestamp Request, the kernel checks <code>sysctl_icmp_echo_ignore_broadcasts</code>.</p><ul><li>This is to prevent network storms. If someone pings the entire network via broadcast and every machine replies, the network would be paralyzed. This switch is turned on by default (ignore).</li></ul></li><li><p><strong>Final Dispatch</strong>: Finds the corresponding callback function via <code>icmp_pointers[type].handler</code> and executes it.</p></li></ol><hr><h3 id="sending-flow-icmp-send-and-icmp-reply" tabindex="-1">Sending Flow: icmp_send() and icmp_reply() <a class="header-anchor" href="#sending-flow-icmp-send-and-icmp-reply" aria-label="Permalink to &quot;Sending Flow: icmp_send() and icmp_reply()&quot;">​</a></h3><p>The kernel primarily uses two pathways to send ICMP messages:</p><ol><li><strong>Proactive error reporting</strong>: Such as <code>icmp_send()</code>. Called when network anomalies occur (e.g., port unreachable, fragmentation needed).</li><li><strong>Passive replies</strong>: Such as <code>icmp_reply()</code>. Used to reply to Echo or Timestamp requests.</li></ol><p>Regardless of the pathway, both ultimately call <code>icmp_push_reply()</code> and rely on the previously mentioned per-CPU Socket to pass data into the IP layer.</p><h4 id="structure-struct-icmp-bxm" tabindex="-1">Structure: struct icmp_bxm <a class="header-anchor" href="#structure-struct-icmp-bxm" aria-label="Permalink to &quot;Structure: struct icmp_bxm&quot;">​</a></h4><p>Before sending, the kernel uses <code>struct icmp_bxm</code> to construct the message to be sent. This acts as a temporary &quot;assembly workshop&quot;.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> icmp_bxm {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">skb;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        /* 触发消息的原始包 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> offset;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 /* 网络头偏移量 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> data_len;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               /* 载荷长度 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> icmphdr icmph;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   /* ICMP 头部 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        __be32 </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">times</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        /* 时间戳（用于 Timestamp 消息） */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } data;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> head_len;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               /* 头部总长度 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ip_options_data replyopts;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* IP 选项 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><h4 id="rate-limiting-don-t-let-the-network-explode" tabindex="-1">Rate Limiting: Don&#39;t Let the Network Explode <a class="header-anchor" href="#rate-limiting-don-t-let-the-network-explode" aria-label="Permalink to &quot;Rate Limiting: Don&#39;t Let the Network Explode&quot;">​</a></h4><p>If ICMP messages are sent without restriction, it can easily trigger an &quot;avalanche&quot;. For example, if a router is flapping and receives tens of thousands of erroneous packets per second, and it replies with an ICMP unreachable for each one, it would crash itself.</p><p>The kernel implements <strong>rate limiting</strong> through <code>icmpv4_xrlim_allow()</code>.</p><p>Rate limiting is only <strong>skipped</strong> under the following circumstances:</p><ul><li>The message type is unknown (this is very rare).</li><li>It&#39;s a PMTU discovery message (<code>ICMP_FRAG_NEEDED</code>)—this must be sent as soon as possible, otherwise the TCP connection will break.</li><li>The device is loopback.</li><li>This ICMP type is not enabled in the rate mask.</li></ul><p>In all other cases, the kernel calls <code>inet_peer_xrlim_allow()</code> to check if too many ICMP packets have been sent to that destination.</p><hr><h3 id="diving-into-sending-scenarios-destination-unreachable" tabindex="-1">Diving into Sending Scenarios: Destination Unreachable <a class="header-anchor" href="#diving-into-sending-scenarios-destination-unreachable" aria-label="Permalink to &quot;Diving into Sending Scenarios: Destination Unreachable&quot;">​</a></h3><p>Let&#39;s look at a few specific scenarios that trigger <code>icmp_send()</code>, which are very helpful for debugging network issues.</p><h4 id="code-2-protocol-unreachable-icmp-prot-unreach" tabindex="-1">Code 2: Protocol Unreachable (ICMP_PROT_UNREACH) <a class="header-anchor" href="#code-2-protocol-unreachable-icmp-prot-unreach" aria-label="Permalink to &quot;Code 2: Protocol Unreachable (ICMP_PROT_UNREACH)&quot;">​</a></h4><p>Scenario: You send a packet with the protocol field in the IP header set to 137 (hypothetically), but the kernel has no handler registered for protocol number 137.</p><p>In <code>ip_local_deliver_finish()</code>, the kernel looks up the table <code>inet_protos[protocol]</code> and finds it&#39;s NULL. If no Raw Socket claims it, the kernel triggers:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">icmp_send</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb, ICMP_DEST_UNREACH, ICMP_PROT_UNREACH, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Meaning: &quot;I wanted to handle this packet, but I couldn&#39;t find a protocol (like TCP/UDP) that can handle it, so it&#39;s unreachable.&quot;</p><h4 id="code-3-port-unreachable-icmp-port-unreach" tabindex="-1">Code 3: Port Unreachable (ICMP_PORT_UNREACH) <a class="header-anchor" href="#code-3-port-unreachable-icmp-port-unreach" aria-label="Permalink to &quot;Code 3: Port Unreachable (ICMP_PORT_UNREACH)&quot;">​</a></h4><p>Scenario: You send a UDP packet to port 8888 on a certain machine, but that machine has no program listening on port 8888.</p><p>The UDP protocol layer looks up the Socket in <code>__udp4_lib_rcv()</code> and finds nothing. If the checksum is correct, it will reply with:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">icmp_send</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb, ICMP_DEST_UNREACH, ICMP_PORT_UNREACH, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This is typically the most common ICMP error message you will encounter.</p><h4 id="code-4-fragmentation-needed-icmp-frag-needed" tabindex="-1">Code 4: Fragmentation Needed (ICMP_FRAG_NEEDED) <a class="header-anchor" href="#code-4-fragmentation-needed-icmp-frag-needed" aria-label="Permalink to &quot;Code 4: Fragmentation Needed (ICMP_FRAG_NEEDED)&quot;">​</a></h4><p>Scenario: The key to PMTU discovery.</p><p>You want to send a large 9000-byte packet, but it passes through a router whose egress MTU is only 1500, and the DF (Don&#39;t Fragment) bit in your packet header is set to 1.</p><p>In <code>ip_forward()</code>, the kernel discovers that <code>skb-&gt;len &gt; dst_mtu(&amp;rt-&gt;dst)</code> and the DF flag is set. Since it cannot fragment, it can only drop the packet and notify:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">icmp_send</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb, ICMP_DEST_UNREACH, ICMP_FRAG_NEEDED,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">          htonl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">dst_mtu</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">rt</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">dst</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)));</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Note the last parameter here: it tells the sender the correct MTU value. This is how PMTU works.</p><h4 id="code-5-source-route-failed-icmp-sr-failed" tabindex="-1">Code 5: Source Route Failed (ICMP_SR_FAILED) <a class="header-anchor" href="#code-5-source-route-failed-icmp-sr-failed" aria-label="Permalink to &quot;Code 5: Source Route Failed (ICMP_SR_FAILED)&quot;">​</a></h4><p>Scenario: Using Strict Source Routing.</p><p>If <code>ip_forward()</code> finds that the packet specifies it must pass through Gateway A, but the routing table shows the next hop is indeed Gateway A, or there is a configuration conflict, this error is triggered. Although source routing is rarely used today, the kernel still retains this logic.</p><h3 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h3><p>ICMPv4 may look simple, but it is actually a crucial part of the network&#39;s self-regulation mechanism.</p><ul><li>It is the &quot;exception handling mechanism&quot; of the IP layer.</li><li>It is the cornerstone of user diagnostic tools (Ping/Traceroute).</li><li>Through rate limiting and specific processing logic, it protects itself from being abused.</li></ul><p>Next, we will enter the world of IPv6. You will find that, although the names are similar, in IPv6, ICMP is endowed with a more core responsibility—it is the foundation of Neighbor Discovery, almost entirely replacing the functionality of ARP.</p>`,103)])])}const g=e(t,[["render",r]]);export{k as __pageData,g as default};
