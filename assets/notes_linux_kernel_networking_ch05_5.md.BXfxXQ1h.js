import{_ as o,o as i,c as t,a}from"./chunks/framework.8tiMeCK-.js";const g=JSON.parse('{"title":"5.5  Policy Routing：地图之外的选择权","description":"","frontmatter":{},"headers":[],"relativePath":"notes/linux_kernel_networking/ch05_5.md","filePath":"notes/linux_kernel_networking/ch05_5.md","lastUpdated":null}'),n={name:"notes/linux_kernel_networking/ch05_5.md"};function s(l,e,d,r,c,p){return i(),t("div",null,[...e[0]||(e[0]=[a('<h1 id="_5-5-policy-routing-地图之外的选择权" tabindex="-1">5.5 Policy Routing：地图之外的选择权 <a class="header-anchor" href="#_5-5-policy-routing-地图之外的选择权" aria-label="Permalink to &quot;5.5  Policy Routing：地图之外的选择权&quot;">​</a></h1><p>上一节我们讲到 <code>fib_nh</code> 就像是一个尽职的向导，手里拿着写明出接口和网关地址的便签，指引着数据包怎么离开内核。我们建立了一个非常直观的认知：<strong>目的地决定路由</strong>。只要知道你想去哪（目标 IP），路由表就能告诉你怎么走。</p><p>这个认知在 99% 的情况下是够用的。但作为一个正在探究底层机制的工程师，你可能会遇到那剩下的 1%。</p><p>想象这样一个场景：你有一台机器，插了两根网线。</p><ul><li>一根是 <code>eth0</code>，接的是内网，用来管理，不花钱。</li><li>一根是 <code>eth1</code>，接的是外网，按流量计费，很贵。</li></ul><p>现在你跑了一个程序，它需要从 <code>10.0.0.1</code> 下载数据。按照老规矩，内核查路由表发现 <code>10.0.0.1</code> 走 <code>eth0</code> 能通，于是把流量全打出去了。 但你不想这样。你心里有本账：<strong>虽然目标地址一样，但如果这个流量是系统备份产生的，我就想走昂贵的 <code>eth1</code>（因为快），如果是普通浏览，我才走 <code>eth0</code>。</strong></p><p>如果只看目的地，传统的路由表无能为力。它眼里只有目标，不问出身。</p><p>这就是 <strong>Policy Routing（策略路由）</strong> 要解决的问题。它让路由决策不再单纯基于「我要去哪」，而是可以基于「我是谁」、「我要干什么」甚至「我用了什么协议」。在引入这个机制之前，让我们先看看在它的对立面——即没有策略路由的世界里，事情是怎么运作的。</p><h3 id="没有-policy-routing-的日子-两张表" tabindex="-1">没有 Policy Routing 的日子：两张表 <a class="header-anchor" href="#没有-policy-routing-的日子-两张表" aria-label="Permalink to &quot;没有 Policy Routing 的日子：两张表&quot;">​</a></h3><p>在内核配置项 <code>CONFIG_IP_MULTIPLE_TABLES</code> 没有被开启的情况下，内核的路由世界是非常简单的：只有两张表。</p><ol><li><p><strong>Local Table (RT_TABLE_LOCAL, ID 255)</strong> 这是内核的「私人领地」。里面全是给本机 IP 地址的路由（比如 <code>127.0.0.1</code> 或者你配给 <code>eth0</code> 的 IP）。这张表非常敏感，<strong>只有内核自己能往里面加东西</strong>。管理员（User）如果试图用 <code>ip route</code> 往 Local 表里塞条目，会被拒绝。这张表决定了「哪些地址是属于我的」。</p></li><li><p><strong>Main Table (RT_TABLE_MAIN, ID 254)</strong> 这是我们的「世界地图」。绝大多数你通过 <code>ip route add</code> 命令配置的路由，都在这张表里。它决定了「如果地址不是我的，该往哪扔」。</p></li></ol><p>这个初始化过程发生在 <code>net/ipv4/fib_frontend.c</code> 的 <code>fib4_rules_init()</code> 方法里。</p><blockquote><p><strong>历史的注脚</strong>： 在 2.6.25 之前的内核里，这两个表还是两个全局变量：<code>ip_fib_local_table</code> 和 <code>ip_fib_main_table</code>。那时候代码里到处都是直接访问这两个变量的逻辑。 后来内核开发者意识到这太不灵活了——如果你想加一张表？你得改代码重编译。于是他们重构了一下，把所有对表的操作统一收敛到了 <code>fib_get_table()</code> 方法。不管你有没有开启策略路由，也不管你有几张表，大家都是用 <code>fib_get_table(net, table_id)</code> 来拿表指针。</p></blockquote><p>这种「统一访问」的感觉，就像是把「专用抽屉」变成了「带编号的储物柜系统」，无论柜子有多少个，拿钥匙开锁的动作是一模一样的。</p><h3 id="策略路由开启时-255-张地图" tabindex="-1">策略路由开启时：255 张地图 <a class="header-anchor" href="#策略路由开启时-255-张地图" aria-label="Permalink to &quot;策略路由开启时：255 张地图&quot;">​</a></h3><p>当你开启了 <code>CONFIG_IP_MULTIPLE_TABLES</code>，世界变了。</p><p>内核不再局限于 Local 和 Main 两张表，而是支持最多 <strong>255 个路由表</strong>。 启动时，默认会初始化三张表：</p><ul><li><strong>Local</strong> (255)</li><li><strong>Main</strong> (254)</li><li><strong>Default</strong> (253)</li></ul><p>（注：关于 Default 表的具体用途和策略路由规则集 <code>fib_rules</code> 的详细交互，我们将在第 6 章深聊。这里先专注于「表」本身的管理机制。）</p><p>现在的问题是：<strong>这么多表，我怎么往里面塞东西？</strong></p><h3 id="管理员的操作界面-netlink-与-ioctl" tabindex="-1">管理员的操作界面：Netlink 与 IOCTL <a class="header-anchor" href="#管理员的操作界面-netlink-与-ioctl" aria-label="Permalink to &quot;管理员的操作界面：Netlink 与 IOCTL&quot;">​</a></h3><p>作为一个内核工程师，你肯定熟悉 <code>ip route</code> 命令。但你知道它在内核眼里是什么吗？是一段 Netlink 消息。</p><h4 id="_1-增删路由-ip-route-add-del" tabindex="-1">1. 增删路由：<code>ip route add/del</code> <a class="header-anchor" href="#_1-增删路由-ip-route-add-del" aria-label="Permalink to &quot;1. 增删路由：`ip route add/del`&quot;">​</a></h4><p>当你敲下：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 192.168.1.0/24</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> eth0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>你的用户空间工具（iproute2）实际上通过 Netlink socket 发送了一个 <strong>RTM_NEWROUTE</strong> 消息给内核。</p><p>内核那边接招的是 <code>inet_rtm_newroute()</code> 方法（位于 <code>net/ipv4/fib_frontend.c</code>）。</p><ul><li>它会解析你带来的参数（目标网段、出接口、优先级等）。</li><li>创建对应的 <code>fib_info</code> 和 <code>fib_alias</code>。</li><li>把它挂到对应的 FIB 表（默认是 Main 表）的哈希或 TRIE 结构里。</li></ul><p>当你敲下 <code>ip route del ...</code> 时，流程类似，只是消息类型变成了 <strong>RTM_DELROUTE</strong>，内核由 <code>inet_rtm_delroute()</code> 接手，负责从 FIB 表中摘下对应的条目。</p><p>这里有个反直觉的细节，值得停下来想一想： <strong>路由并不总是意味着「允许通行」。</strong></p><p>你可以这样配：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> prohibit</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.1.17</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> from</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.2.103</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>这行命令是在路由表里加了一个「禁止令」。内核在查路由时，如果匹配到了这一条，不仅不会转发数据包，还会直接丢弃，并回复一个 ICMP 「Packet Filtered」（被过滤）的错误消息。 这在防火墙或策略控制场景里非常有用——路由表本身就是一种规则集。</p><p><strong>查看路由：<code>ip route show</code></strong> 这对应的是 <strong>RTM_GETROUTE</strong> 消息，由 <code>inet_dump_fib()</code> 处理。</p><ul><li>默认情况下，<code>ip route show</code> 只看 <strong>Main 表</strong>。</li><li>如果你想看 Local 表，必须显式指定：<code>ip route show table local</code>。</li></ul><h4 id="_2-老派操作-route-add-del" tabindex="-1">2. 老派操作：<code>route add/del</code> <a class="header-anchor" href="#_2-老派操作-route-add-del" aria-label="Permalink to &quot;2. 老派操作：`route add/del`&quot;">​</a></h4><p>虽然 <code>ip</code> 命令是现在的标准，但 <code>route</code> 命令依然存在。它的内核接口是完全不同的另一条路——<strong>IOCTL</strong>。</p><ul><li><code>route add</code> 发送 <code>SIOCADDRT</code> IOCTL。</li><li><code>route del</code> 发送 <code>SIOCDELRT</code> IOCTL。</li></ul><p>这两个 IOCTL 都由 <code>ip_rt_ioctl()</code> 方法处理（也在 <code>net/ipv4/fib_frontend.c</code>）。 这是为了兼容古老的网络工具留下的接口，虽然功能上和 Netlink 看起来差不多，但在内核内部，IOCTL 的处理路径通常比 Netlink 要死板一些。</p><h4 id="_3-动态路由协议-bgp-ospf-等" tabindex="-1">3. 动态路由协议：BGP, OSPF 等 <a class="header-anchor" href="#_3-动态路由协议-bgp-ospf-等" aria-label="Permalink to &quot;3. 动态路由协议：BGP, OSPF 等&quot;">​</a></h4><p>除了人手敲命令，路由表的另一个主要数据来源是<strong>路由守护进程</strong>。 这些是运行在骨干网路由器上的重型软件（如 Quagga, Bird, Zebra）。它们实现了 BGP、OSPF 等复杂协议。</p><p>这些进程在后台跑着，通过协议跟邻居路由器聊天。一旦发现网络拓扑变了（比如某条光纤断了），它们会立刻调用 Netlink API，狂发 <code>RTM_NEWROUTE</code> 或 <code>RTM_DELROUTE</code> 消息，瞬间更新内核里的 FIB 表。 <strong>对于内核来说，它不在乎这些路由是管理员手动敲进去的，还是 OSPF 协议算出来的——它们最终都是 <code>fib_info</code> 结构体，挂在一模一样的表里。</strong></p><h3 id="例外与微调-再次回到-fib-exception" tabindex="-1">例外与微调：再次回到 FIB Exception <a class="header-anchor" href="#例外与微调-再次回到-fib-exception" aria-label="Permalink to &quot;例外与微调：再次回到 FIB Exception&quot;">​</a></h3><p>我们在这一节的开头提到，虽然我们正在讨论「表」级别的管理，但千万不要忘了上一节讲的 <strong>FIB nexthop exception</strong>。</p><ul><li>如果是因为 ICMP Redirect 导致下一跳变了。</li><li>或者是因为 Path MTU Discovery 导致 MTU 变了。</li></ul><p>这些变更<strong>不会</strong>去动那张庞大的、共享的 FIB 路由表。它们只会修改特定 <code>fib_nh</code> 头上的那张小哈希表（exception table）。 这是一种极佳的隔离设计：<strong>不要让个例污染全局规则。</strong> 如果把 PMTU 发现的路径直接改到全局 Main 表里，那么所有去往那个网段的流量可能都会被错误地套用一个还没验证过的 MTU 值，这简直是灾难。通过 exception 机制，内核只在「确实需要的那条流」上做微调。</p><h3 id="小结" tabindex="-1">小结 <a class="header-anchor" href="#小结" aria-label="Permalink to &quot;小结&quot;">​</a></h3><p>这一节我们把视角从微观的 <code>fib_nh</code> 拉到了宏观的 <strong>FIB Tables</strong> 架构上。</p><p>我们理解了：</p><ol><li><strong>双表模式</strong>：没有策略路由时，内核只认 Local 和 Main 两张表。</li><li><strong>统一访问</strong>：通过 <code>fib_get_table()</code>，内核把对表的操作抽象化了，为支持多表打下了基础。</li><li><strong>用户接口</strong>：<code>ip route</code> 命令背后的 Netlink 消息（<code>RTM_NEWROUTE</code> 等）是管理员和路由守护进程操控 FIB 的方式。</li><li><strong>路由即策略</strong>：路由条目不仅仅是导航，还可以是 <code>prohibit</code> 这样的禁令。</li></ol><p>现在的 FIB 体系已经比较清晰了：有表，有条目，有下一跳，还有针对下一跳的微调机制。 但就像我们在开头提到的「双网卡」场景一样，光有表还不够，我们还需要一套<strong>规则</strong>来决定「什么时候查哪张表」。</p><p>这就是下一章的主角——<strong>FIB Rules</strong>。那里才是策略路由真正的灵魂所在。</p>',52)])])}const u=o(n,[["render",s]]);export{g as __pageData,u as default};
