import{_ as t,o as a,c as n,a as s}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"12.3 Network Topologies","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch12_3.md","filePath":"en/notes/linux_kernel_networking/ch12_3.md","lastUpdated":null}'),o={name:"en/notes/linux_kernel_networking/ch12_3.md"};function i(r,e,h,l,d,c){return a(),n("div",null,[...e[0]||(e[0]=[s(`<h1 id="_12-3-network-topologies" tabindex="-1">12.3 Network Topologies <a class="header-anchor" href="#_12-3-network-topologies" aria-label="Permalink to &quot;12.3 Network Topologies&quot;">​</a></h1><p>With the frame header byte ordering sorted out, we need to take a step back and look at the bigger picture.</p><p>These frames don&#39;t fly around in a vacuum—they are produced and consumed within a specific &quot;social structure.&quot; This is the network topology. 802.11 defines the &quot;basic rules&quot; for device interaction, and built on top of those rules, the most common is the first mode we&#39;ll discuss.</p><hr><h3 id="infrastructure-bss" tabindex="-1">Infrastructure BSS <a class="header-anchor" href="#infrastructure-bss" aria-label="Permalink to &quot;Infrastructure BSS&quot;">​</a></h3><p>This is the mode most people interact with daily. Your home router, your company&#39;s Wi-Fi—they are fundamentally all running on this same setup.</p><p>In this mode, the world is not equal: there is a central node called an <strong>AP (Access Point)</strong>, surrounded by a circle of <strong>Client Stations</strong>. The AP and its clients together form a <strong>BSS (Basic Service Set)</strong>.</p><p>It&#39;s like a star topology: all roads lead to the AP.</p><p>But you can&#39;t just connect and start using it. For a client to send data through the AP, it must first complete two steps: <strong>Authentication</strong> and <strong>Association</strong>.</p><p>This might sound like bureaucratic red tape, but think of it this way: the wireless medium is shared, and the AP needs to know &quot;who you are&quot; and &quot;what you want to do.&quot; In most cases, before a client even knocks on the AP&#39;s door, it will perform a <strong>Scanning</strong> to see which APs are nearby, how strong their signals are, and what data rates they support. It&#39;s like checking the menu and reviews before walking into a restaurant.</p><p>Association is exclusive. At any given time, a client can only be bound to a single AP. You can&#39;t have a foot in both camps.</p><p>Once association succeeds, the AP assigns the client an <strong>AID (Association ID)</strong>. This is a number between 1 and 2007 that uniquely identifies the client within the current BSS.</p><h4 id="what-s-behind-the-ap" tabindex="-1">What&#39;s Behind the AP? <a class="header-anchor" href="#what-s-behind-the-ap" aria-label="Permalink to &quot;What&#39;s Behind the AP?&quot;">​</a></h4><p>From a kernel developer&#39;s perspective, what exactly is an AP?</p><p>Essentially, an AP is just a wireless network card with some peripheral hardware attached (like an Ethernet port, LEDs, and that infuriating Reset button). But what truly turns it into an AP is the software running on top of it.</p><p>Enter a famous player: <strong>hostapd</strong>.</p><p>hostapd is a user-space daemon. It handles the heavy management tasks within the MLME (MAC Layer Management Entity), such as processing authentication requests and association requests. How does it work? It registers itself through the <strong>nl80211</strong> interface to specifically receive relevant management frames. If we compare these management frames to administrative paperwork, hostapd is the person sitting behind the window stamping them.</p><p>The entire hostapd project is open source, empowering countless ordinary wireless network cards with the ability to transform into APs.</p><h4 id="how-does-data-flow" tabindex="-1">How Does Data Flow? <a class="header-anchor" href="#how-does-data-flow" aria-label="Permalink to &quot;How Does Data Flow?&quot;">​</a></h4><p>In Infrastructure mode, communication almost always goes through the AP.</p><ul><li><strong>Client → AP</strong>: The client sends the data frame to the AP.</li><li><strong>AP → Destination</strong>: After receiving it, the AP forwards it to the final destination (which could be another wireless client, or an external device bridged out through a wired network).</li></ul><p>This &quot;everyone must cross the bridge to get across the river&quot; design might seem inefficient, but it greatly simplifies management. The AP holds a global view.</p><p>If you need to cover a large area (like a big office building), a single AP won&#39;t cut it. In this case, we deploy multiple APs and connect them with Ethernet cables. This deployment architecture is called an <strong>ESS (Extended Service Set)</strong>. In an ESS, two or more BSSs exist simultaneously.</p><p>Here is a tricky detail: a broadcast frame sent in BSS A might drift into neighboring BSS B. Stations in BSS B will receive it, but they must <strong>discard</strong> it. Why? Because the <strong>BSSID</strong> in the 802.11 header doesn&#39;t match. It&#39;s like hearing the roll call in the next classroom—you hear it, but they aren&#39;t calling your name.</p><p>So, in an ESS deployment, to avoid interfering with each other, adjacent APs typically operate on different channels.</p><hr><h3 id="ibss-independent-bss-a-k-a-ad-hoc-mode" tabindex="-1">IBSS (Independent BSS), a.k.a. Ad Hoc Mode <a class="header-anchor" href="#ibss-independent-bss-a-k-a-ad-hoc-mode" aria-label="Permalink to &quot;IBSS (Independent BSS), a.k.a. Ad Hoc Mode&quot;">​</a></h3><p>If Infrastructure mode is &quot;centralized authority,&quot; then IBSS mode is &quot;anarchism.&quot;</p><p><strong>IBSS (Independent BSS)</strong> is commonly known as an <strong>Ad Hoc network</strong>. It doesn&#39;t need an AP, doesn&#39;t need a router—devices just gather together and communicate. This type of network is usually set up temporarily, used on the fly, and requires no pre-planning.</p><h4 id="setting-up-an-ibss" tabindex="-1">Setting Up an IBSS <a class="header-anchor" href="#setting-up-an-ibss" aria-label="Permalink to &quot;Setting Up an IBSS&quot;">​</a></h4><p>Setting up an IBSS is ridiculously simple. You don&#39;t need to write complex logic in the kernel; just type a couple of commands in the terminal.</p><p>Using the <code>iw</code> tool (the modern generation of tools):</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iw</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wlan0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ibss</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> join</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> AdHocNetworkName</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2412</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Here, <code>2412</code> refers to the center frequency of channel 1 (in MHz).</p><p>Or using the old-school <code>iwconfig</code>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iwconfig</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wlan0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mode</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ad-hoc</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iwconfig</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wlan0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> essid</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> AdHocNetworkName</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>The moment you press Enter, the kernel calls the <code>ieee80211_sta_create_ibss()</code> method (defined in <code>net/mac80211/ibss.c</code>), and the IBSS network is born.</p><p>Of course, you&#39;ll need to figure out a way to tell the SSID (network name) to everyone who wants to join—type it in manually, or write it on a piece of paper and send a photo, whatever works for you.</p><h4 id="what-makes-ibss-unique" tabindex="-1">What Makes IBSS Unique? <a class="header-anchor" href="#what-makes-ibss-unique" aria-label="Permalink to &quot;What Makes IBSS Unique?&quot;">​</a></h4><p>With no AP around, who&#39;s in charge?</p><p>Nobody.</p><p>In IBSS mode, the <strong>BSSID</strong> is a randomly generated 48-bit address (implemented by calling <code>get_random_bytes()</code>). Because there is no central node, many tasks that the AP would handle in Infrastructure mode must now be negotiated among peers, or simply abandoned altogether.</p><p>One of the mechanisms that is complex enough to cause headaches is <strong>Power Save</strong>.</p><p>In Infrastructure mode, when you sleep, the AP keeps watch for you. But in Ad Hoc mode, there is no AP to act as your nanny. IBSS uses a message called <strong>ATIM (Announcement Traffic Indication Map)</strong> to coordinate who should be awake and who should be asleep.</p><p>Frankly, the ATIM mechanism is quite complex and fragile, and <strong>mac80211 currently does not support ATIM</strong>. So if you&#39;re writing a driver or debugging and counting on this feature, you&#39;re going to be disappointed. We won&#39;t dive into that rabbit hole in this chapter.</p><hr><p>Now that we have a clear understanding of the two most basic network formations. In the next section, we will dive into one of the most important mechanisms in the mac80211 stack: <strong>Power Save Mode</strong>. We&#39;ll see exactly how the AP &quot;delivers mail&quot; for clients in Infrastructure mode, and explore the pitfalls that have caused countless driver developers to pull their hair out.</p>`,47)])])}const g=t(o,[["render",i]]);export{u as __pageData,g as default};
