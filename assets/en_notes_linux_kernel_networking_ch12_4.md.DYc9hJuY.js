import{_ as t,o,c as a,a as n}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"12.4 Power Save Mode","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch12_4.md","filePath":"en/notes/linux_kernel_networking/ch12_4.md","lastUpdated":null}'),s={name:"en/notes/linux_kernel_networking/ch12_4.md"};function i(r,e,l,c,h,d){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="_12-4-power-save-mode" tabindex="-1">12.4 Power Save Mode <a class="header-anchor" href="#_12-4-power-save-mode" aria-label="Permalink to &quot;12.4 Power Save Mode&quot;">​</a></h1><p>Beyond forwarding packets, an AP has another critical function: acting as a caretaker for &quot;sleeping&quot; clients by caching their data.</p><p>Most wireless clients run on battery power—phones, laptops, and IoT devices. To conserve energy, the wireless NIC can&#39;t keep its receiver on all the time; it needs to take naps periodically. But this raises a problem: <strong>what happens to the messages sent to you while you&#39;re asleep?</strong></p><p>In wired networks, this isn&#39;t an issue because the other end of the cable is always powered. In the wireless world, however, it&#39;s a major challenge. In this section, we&#39;ll look at how APs and clients in Infrastructure mode solve this problem using an elegant &quot;push-pull&quot; mechanism.</p><hr><h3 id="entering-power-save-mode" tabindex="-1">Entering Power Save Mode <a class="header-anchor" href="#entering-power-save-mode" aria-label="Permalink to &quot;Entering Power Save Mode&quot;">​</a></h3><p>When a client decides to enter power save mode, it can&#39;t just quietly doze off—it needs to notify the AP first.</p><p>How? Typically by sending a <strong>Null Data Packet</strong>. The name sounds convoluted, but it&#39;s simply a data frame with only an 802.11 header and no payload. The real purpose of this packet isn&#39;t to carry data, but to flip a flag in the Frame Control field of the header: the <strong>PM (Power Management) bit</strong>.</p><p>Once this PM bit is set to 1, the AP understands: this client is going to sleep.</p><p>You can think of this as hanging a <strong>&quot;Do Not Disturb&quot;</strong> sign on your door. You put up the sign (send the Null Packet), and when the waiter (AP) passes by and sees it, they stop knocking on your door to answer calls.</p><p>But the &quot;hanging a sign&quot; analogy falls short in one respect: a real waiter might just not knock, but the caller might hang up. In 802.11, however, the AP not only avoids disturbing you, but also <strong>records and stores all incoming calls (packets) for you</strong> (caching).</p><p>Upon receiving a packet with PM=1, the AP immediately activates a caching mechanism. Inside the kernel (specifically in the mac80211 subsystem), it maintains a queue for each associated station called <strong>ps_tx_buf</strong>.</p><p>This <code>ps_tx_buf</code> is a linked list structure dedicated to storing unicast packets destined for that station.</p><p>However, memory is finite. mac80211 sets a hard limit on the <code>ps_tx_buf</code> for each station: <strong>128 packets</strong> (<code>STA_MAX_TX_BUFFER</code>).</p><p>⚠️ <strong>Warning</strong></p><p>This limit is strict. If a client sleeps for too long while the sender is aggressive, these 128 slots will fill up quickly.</p><p>What happens when it&#39;s full? The AP won&#39;t crash; it starts &quot;throwing things out.&quot; It follows the <strong>FIFO (First In, First Out)</strong> principle—dropping the oldest packets to make room for new ones. This means if your phone goes into a deep sleep in power save mode, you might find that the earliest messages are missing when you wake up, and they are gone for good.</p><p>Beyond unicast packets, there&#39;s an even trickier customer: <strong>multicast/broadcast packets</strong>.</p><p>Why are they tricky? Because by definition in 802.11, a broadcast packet must be delivered to <strong>everyone</strong> in the BSS. As long as even one person is sleeping, the broadcast packet cannot be transmitted—because the sleeping client wouldn&#39;t receive it anyway.</p><p>To solve this, the AP maintains a global, shared buffer: <strong>bc_buf</strong>.</p><p>As long as <strong>at least one</strong> station in the BSS is in sleep mode, the AP will stuff all multicast and broadcast packets into this <code>bc_buf</code>. This buffer also has a limit, which is likewise <strong>128 packets</strong> (<code>AP_MAX_BC_BUFFER</code>).</p><p>Now we can refine our analogy: the AP is like a front desk. When you&#39;re asleep (PM=1), the front desk stuffs your personal mail into your private mailbox, and locks the mass-mailed flyers in a drawer—as long as even one person in the room is sleeping, the flyers aren&#39;t distributed; they wait until everyone is awake.</p><hr><h3 id="exiting-power-save-mode" tabindex="-1">Exiting Power Save Mode <a class="header-anchor" href="#exiting-power-save-mode" aria-label="Permalink to &quot;Exiting Power Save Mode&quot;">​</a></h3><p>Clients sleep to save power, not to enter a coma. They need to wake up and do work. How do they know when to wake up? Usually via a timer.</p><p>What&#39;s the first thing to do upon waking up? Listen to the broadcast.</p><p>The AP <strong>periodically</strong> sends a special management frame called a <strong>Beacon</strong>. This frequency is quite high, typically 10 per second (<code>100ms</code> interval). You can think of it as the AP&#39;s heartbeat, or a clock chiming on the hour.</p><p>Beacon frames don&#39;t just carry timing synchronization; they also carry a critical Information Element called the <strong>TIM (Traffic Indication Map)</strong>.</p><p>This is a very clever bitmap design.</p><p>Remember the AID we mentioned earlier? Every associated station has an ID from 1 to 2007. The TIM is an array containing 2008 bits (corresponding to AID 0-2007). If the AP finds packets sitting in your <code>ps_tx_buf</code>, it sets <strong>your corresponding bit in the TIM to 1</strong>.</p><p>After waking up, the client catches a Beacon and starts checking the TIM.</p><p>This check is implemented in the kernel via the <code>ieee80211_check_tim()</code> method (defined in <code>include/linux/ieee80211.h</code>). It doesn&#39;t need to read the entire TIM; it only needs to calculate whether the bit corresponding to its AID is 1.</p><ul><li>If it&#39;s 0: Great, nobody is looking for me, go back to sleep.</li><li>If it&#39;s 1: Wake up, there&#39;s work to do.</li></ul><p>Once a client sees its bit set to 1, it knows the AP is holding packages for it. At this point, it needs to actively &quot;pick up the goods.&quot;</p><p>How? By sending a <strong>PS-Poll (Power Save Poll)</strong> control frame.</p><p>You can think of PS-Poll as you slapping the front desk: &quot;Hey, where&#39;s my mail?&quot;.</p><p>Upon receiving the PS-Poll, the AP pulls a packet from the <code>ps_tx_buf</code> and sends it to you. Here&#39;s the subtle part: <strong>the AP sets a flag called <code>IEEE80211_FCTL_MOREDATA</code> in the Frame Control field of every packet it sends out</strong>.</p><ul><li>If this bit is 1: It means &quot;there&#39;s more, keep polling.&quot;</li><li>If this bit is 0: It means &quot;this is the last one, take it and go back to sleep.&quot;</li></ul><p>The client relies on this bit to decide whether to send another PS-Poll. Once it receives a packet with <code>MOREDATA=0</code>, it knows the inventory is empty and will typically re-enter sleep mode (of course, the standard doesn&#39;t force you to sleep; you can stay awake, but your battery will drain fast).</p><blockquote><p><strong>Back to the &quot;front desk&quot; analogy</strong>: The TIM is like a scrolling screen in the lobby displaying &quot;Package for Room 101.&quot; You wake up, glance at the screen, see your name, and go tap your card at the counter. The front desk hands you the first package and casually says, &quot;There&#39;s one more.&quot; You take it and tap your card again, until the front desk hands you the last one without saying anything—then you know you&#39;re done and can go back to your room to sleep.</p></blockquote><hr><h3 id="handling-the-multicast-broadcast-buffer" tabindex="-1">Handling the Multicast/Broadcast Buffer <a class="header-anchor" href="#handling-the-multicast-broadcast-buffer" aria-label="Permalink to &quot;Handling the Multicast/Broadcast Buffer&quot;">​</a></h3><p>The logic for unicast packets is straightforward: cache for whoever is sleeping. But the logic for multicast/broadcast packets is slightly more complex.</p><p>Per the standard, the AID for multicast/broadcast is defined as <strong>0</strong>.</p><p>So, when the AP has a broadcast packet to send and someone in the room is sleeping, it sets bit 0 of the TIM (TIM[0]) to 1. But that alone isn&#39;t enough.</p><p>Broadcast packets are noisy and power-hungry. If a client had to process broadcast packets every time it woke up, it would be miserable. Moreover, many devices aren&#39;t very interested in broadcast packets. Therefore, 802.11 introduced a concept called <strong>DTIM (Delivery TIM)</strong>.</p><p>DTIM is a special type of TIM that doesn&#39;t appear in every Beacon, but rather every few Beacons. This interval is called the <strong>DTIM Period</strong>, typically configured to 3 or 5 (i.e., once every 300ms - 500ms).</p><p>Only after a Beacon carrying a DTIM does the AP dump all the backlogged broadcast/multicast packets from the <code>bc_buf</code>. This means clients can ignore the TIM[0] in normal Beacons and only wake up during the DTIM cycle to handle broadcast packets, drastically saving power.</p><p>In kernel code, we need to call the <code>ieee80211_get_buffered_bc()</code> method to &quot;extract&quot; these packets from the <code>bc_buf</code>.</p><p>Let&#39;s look at a diagram to clarify the positions of these buffers.</p><p><em>(Figure 12-3 here: Diagram of buffered packets in an AP, showing the sta_info linked list, their respective ps_tx_buf, and the shared bc_buf)</em></p><p>In the mac80211 implementation, the AP is abstracted as a <code>ieee80211_if_ap</code> object. This object has a member called <code>ps</code> (an instance of the <code>ps_data</code> structure). The shared <code>bc_buf</code> mentioned earlier lives right inside the <code>ps</code> structure.</p><p>The next diagram shows the process of a client frantically &quot;picking up goods&quot; using PS-Poll.</p><p><em>(Figure 12-4 here: Flowchart of a client sending PSPOLL packets to retrieve data packets from the AP&#39;s ps_tx_buf)</em></p><p>Notice the data flow in the diagram: every packet sent by the AP, except the last one, <strong>carries the <code>MOREDATA</code> flag</strong>. This is the signal that keeps the client hungry for more. Once this signal disappears, the interaction ends.</p><p>(For simplicity, the ACK flow isn&#39;t drawn in the diagram, but in reality every data packet requires an ACK acknowledgment. Don&#39;t forget this when implementing the driver.)</p><hr><h3 id="⚠️-power-save-vs-power-management-don-t-mix-them-up" tabindex="-1">⚠️ Power Save vs. Power Management: Don&#39;t Mix Them Up <a class="header-anchor" href="#⚠️-power-save-vs-power-management-don-t-mix-them-up" aria-label="Permalink to &quot;⚠️ Power Save vs. Power Management: Don&#39;t Mix Them Up&quot;">​</a></h3><p>There&#39;s a terminology trap here that trips up many beginners (and even veterans).</p><p><strong>Power Management</strong> and <strong>Power Save Mode</strong> are two entirely different things.</p><ul><li><strong>Power Save Mode</strong>: This is what we discussed above. The client sleeps for a few milliseconds, and the AP helps cache packets. This is a <strong>protocol-level task within 802.11</strong> that happens during normal operation.</li><li><strong>Power Management</strong>: This is a system-level task. For example, when you close your laptop lid (Suspend to RAM) or click hibernate (Hibernate). At this point, not only is the NIC down, but the entire CPU has nearly stopped. This is handled by the kernel&#39;s PM framework, in <code>net/mac80211/pm.c</code>, where it corresponds to the <code>resume</code>/<code>suspend</code> callbacks in the driver.</li></ul><p>Don&#39;t confuse the two. If you find yourself trying to handle the TIM bitmap inside the <code>suspend</code> callback when writing a driver, you&#39;ve walked into the wrong room.</p><hr><p>In this section, we really only talked about one thing: <strong>trust</strong>.</p><p>The client trusts that the AP will safeguard its data packets while it sleeps; the AP trusts that the client will proactively pick them up when it wakes. To maintain this trust, they need seemingly tedious handshake signals like TIM, PS-Poll, and Null Data.</p><p>In the next section, we&#39;ll zoom out and look at the behind-the-scenes hero that supports all connections, scanning, and authentication—<strong>MLME (MAC Layer Management Entity)</strong>. Without it, wireless networks would just be a disorganized mess.</p>',66)])])}const g=t(s,[["render",i]]);export{u as __pageData,g as default};
