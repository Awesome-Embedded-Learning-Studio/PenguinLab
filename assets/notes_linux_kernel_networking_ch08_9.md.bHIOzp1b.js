import{_ as e,o,c as d,a as l}from"./chunks/framework.8tiMeCK-.js";const _=JSON.parse('{"title":"8.9 速查表与内核碎片（Quick Reference）","description":"","frontmatter":{},"headers":[],"relativePath":"notes/linux_kernel_networking/ch08_9.md","filePath":"notes/linux_kernel_networking/ch08_9.md","lastUpdated":null}'),r={name:"notes/linux_kernel_networking/ch08_9.md"};function c(i,t,n,a,s,p){return o(),d("div",null,[...t[0]||(t[0]=[l('<h1 id="_8-9-速查表与内核碎片-quick-reference" tabindex="-1">8.9 速查表与内核碎片（Quick Reference） <a class="header-anchor" href="#_8-9-速查表与内核碎片-quick-reference" aria-label="Permalink to &quot;8.9 速查表与内核碎片（Quick Reference）&quot;">​</a></h1><p>我们在这本书里经常说：「别死记硬背，要去理解机制。」但在你真正深入内核代码之前，手里有一张准确的「地图」还是很有必要的。这一节就是那张地图。</p><p>前面八节我们拆解了 IPv6 的接收、发送、路由、组播和自动配置。现在，我们要把这些散落在 <code>net/ipv6/</code> 目录各处的函数调用关系收拢一下。这不是那种只给你函数原型的无聊 API 手册——我会尽量把这些函数背后的「上下文」也讲清楚，让你知道它们在整个流程的大棋局里到底坐在哪个位置。</p><p>最后，我们会整理几个关键的常量表和几个容易被忽略的「特殊角落」。</p><hr><h3 id="核心方法——ipv6-内核的工具箱" tabindex="-1">核心方法——IPv6 内核的工具箱 <a class="header-anchor" href="#核心方法——ipv6-内核的工具箱" aria-label="Permalink to &quot;核心方法——IPv6 内核的工具箱&quot;">​</a></h3><p>先来看看我们在代码里最常碰到的那几个内核方法。它们有的负责摆弄数据结构，有的负责触发关键的协议动作。</p><h4 id="_1-地址检查与操作" tabindex="-1">1. 地址检查与操作 <a class="header-anchor" href="#_1-地址检查与操作" aria-label="Permalink to &quot;1. 地址检查与操作&quot;">​</a></h4><p>很多时候你拿到一个 <code>struct in6_addr *</code>，第一件事就是想知道它是不是个「合法的」或者「特殊的」地址。</p><p><strong><code>bool ipv6_addr_any(const struct in6_addr *a)</code></strong></p><p>这是最基础的检查。它判断给定的地址是不是全零地址（<code>::</code>）。</p><blockquote><p><strong>为什么这很重要？</strong> 当你初始化一个 socket 或者还没绑定具体地址时，内核经常用全零地址表示「任意地址」或「未指定」。如果你在处理路由查找时传入全零地址，路由子系统会直接崩溃（或者查到一条默认路由，取决于上下文）。所以，拿到地址先问一句「你是 <code>any</code> 吗？」，通常是保命的第一步。</p></blockquote><p><strong><code>bool ipv6_addr_equal(const struct in6_addr *a1, const struct in6_addr *a2)</code></strong></p><p>直接比较两个 <code>in6_addr</code> 结构体是否相等。这比 <code>memcmp</code> 稍微快一点，而且语义更清晰——它明确告诉读代码的人：这是在比 IPv6 地址。</p><p><strong><code>static inline void ipv6_addr_set(struct in6_addr *addr, __be32 w1, __be32 w2, __be32 w3, __be32 w4)</code></strong></p><p>这是一个「手动挡」函数。<code>in6_addr</code> 本质上是 128 位的一坨数据（通常是 4 个 32 位整数）。如果你想手动拼凑一个地址——比如拼一个 link-local 地址的前缀——这个函数就是用来把这 4 个 <code>u32</code> 塞进去的。</p><blockquote><p><strong>⚠️ 踩坑预警</strong>：注意字节序。这里的 <code>w1</code> 到 <code>w4</code> 是网络字节序（<code>__be32</code>）。如果你直接填本地整数，在 PC 上通常没问题（因为 x86 是小端），但移植到某些大端架构或者嵌入式板上时，你会得到一个反过来的地址。</p></blockquote><p><strong><code>bool ipv6_addr_is_multicast(const struct in6_addr *addr)</code></strong></p><p>判断是不是组播地址。实现的原理非常粗暴：检查前 8 位是不是 <code>0xFF</code>。如果是，返回 <code>true</code>。 不用自己写 <code>(addr-&gt;s6_addr[0] == 0xff)</code>，用这个宏或函数，不仅代码整洁，而且如果将来标准改了（虽然不太可能），内核改一处就行了。</p><hr><h4 id="_2-数据包解剖——从-skb-中提取-ipv6-信息" tabindex="-1">2. 数据包解剖——从 SKB 中提取 IPv6 信息 <a class="header-anchor" href="#_2-数据包解剖——从-skb-中提取-ipv6-信息" aria-label="Permalink to &quot;2. 数据包解剖——从 SKB 中提取 IPv6 信息&quot;">​</a></h4><p>一旦数据包到达了协议栈，它就被塞进了 <code>struct sk_buff</code>（SKB）。你需要从 SKB 里把 IPv6 头部「掏」出来。</p><p><strong><code>struct ipv6hdr *ipv6_hdr(const struct sk_buff *skb)</code></strong></p><p>这是最常用的「取头」操作。它返回 SKB 里的 IPv6 头部指针。</p><blockquote><p><strong>注意</strong>：这个函数假设 SKB 已经被正确地设置了网络层头指针（<code>skb-&gt;network_header</code>）。在 Netfilter 钩子里或者接收路径的早期阶段，这个通常是安全的。但如果你在一个奇怪的角落（比如某个驱动程序的回调里）拿到 SKB，指针可能还没归位，用这个之前最好先确认 <code>skb-&gt;network_header</code> 是否合法。</p></blockquote><p><strong><code>bool ipv6_ext_hdr(u8 nexthdr)</code></strong></p><p>我们在前面讲过，IPv6 的 <code>nexthdr</code> 字段既可能是上层协议（TCP/UDP），也可能是下一个扩展头。这个函数帮你做判断：如果 <code>nexthdr</code> 的值是已知的扩展头类型（比如 Hop-by-Hop, Routing, Fragment 等），它就返回 <code>true</code>。</p><p>这在你遍历扩展头链表时非常有用——你需要知道什么时候该停（遇到传输层协议），什么时候继续（遇到扩展头）。</p><hr><h4 id="_3-协议注册与分发——让内核认得你的协议" tabindex="-1">3. 协议注册与分发——让内核认得你的协议 <a class="header-anchor" href="#_3-协议注册与分发——让内核认得你的协议" aria-label="Permalink to &quot;3. 协议注册与分发——让内核认得你的协议&quot;">​</a></h4><p>如果你想写一个新的协议（比如你自己在 UDP 之上搞了一层），或者你想写一个 Netfilter 模块来精确匹配某个扩展头，你会碰到这些。</p><p><strong><code>int inet6_add_protocol(const struct inet6_protocol *prot, unsigned char protocol)</code></strong></p><p>这是向内核注册协议处理器的核心函数。</p><ul><li>如果你写的是 TCPv6，你会把 <code>protocol</code> 设为 <code>IPPROTO_TCP</code>。</li><li>如果你写的是 ICMPv6，就是 <code>IPPROTO_ICMPV6</code>。</li><li>但有趣的是，<strong>Fragment 扩展头也是通过这个函数注册的</strong>（值为 <code>NEXTHDR_FRAGMENT</code>）。</li></ul><p>调用这个函数后，内核会把你的处理函数指针填入全局的 <code>inet6_protos</code> 数组。之后，当 IPv6 核心代码解析完头部，发现 <code>nexthdr</code> 是你注册的值时，就会直接 call 你的函数。这是典型的「发布-订阅」模式。</p><hr><h4 id="_4-mld-与组播——当主机想要入会" tabindex="-1">4. MLD 与组播——当主机想要入会 <a class="header-anchor" href="#_4-mld-与组播——当主机想要入会" aria-label="Permalink to &quot;4. MLD 与组播——当主机想要入会&quot;">​</a></h4><p>我们在前面花了很多篇幅讲 MLDv2。内核里那些负责把网卡加入组播组，或者向外发送 MLD 报告的逻辑，就在这些函数里。</p><p><strong><code>bool ipv6_is_mld(struct sk_buff *skb, int nexthdr, int offset)</code></strong></p><p>这是一个很贴心的辅助函数。它帮你检查：这个包是不是 ICMPv6？并且，ICMPv6 的类型是不是 MLD 相关的？ 它会检查以下四种类型之一，只要命中一个就返回 <code>true</code>：</p><ul><li><code>ICMPV6_MGM_QUERY</code> (MLD 查询)</li><li><code>ICMPV6_MGM_REPORT</code> (MLDv1 报告)</li><li><code>ICMPV6_MGM_REDUCTION</code> (离开组播)</li><li><code>ICMPV6_MLD2_REPORT</code> (MLDv2 报告)</li></ul><p><strong><code>int ipv6_dev_mc_inc(struct net_device *dev, const struct in6_addr *addr)</code></strong></p><p>这是「让网卡听组播」的底层实现。</p><ul><li>当你在用户空间调用 <code>setsockopt(..., IPV6_ADD_MEMBERSHIP, ...)</code> 时，内核最终会走到这里（或者走到它的 socket 变体）。</li><li>它会做两件事： <ol><li>在网卡硬件层面设置过滤，让网卡接收发往这个组播地址的帧。</li><li>在内核的 <code>inet6_dev</code> 结构里维护一个软件列表，记录这个接口属于哪些组。</li></ol></li></ul><p><strong><code>int ipv6_sock_mc_join(struct sock *sk, int ifindex, const struct in6_addr *addr)</code></strong></p><p>这是用户空间 <code>IPV6_JOIN_GROUP</code> 的内核入口。它不仅调用上面的 <code>_inc</code> 来更新硬件，还会把这个 socket 挂到该组的成员列表上，这样当组播数据包进来时，内核才知道要把包复制给谁。</p><hr><h4 id="_5-接收与转发——数据包的旅途" tabindex="-1">5. 接收与转发——数据包的旅途 <a class="header-anchor" href="#_5-接收与转发——数据包的旅途" aria-label="Permalink to &quot;5. 接收与转发——数据包的旅途&quot;">​</a></h4><p><strong><code>int ipv6_rcv(struct sk_buff *skb, struct net_device *dev, struct packet_type *pt, struct net_device *orig_dev)</code></strong></p><p>这是 IPv6 协议栈的「总入口」。当驱动程序收到一个 IPv6 包（以太网类型为 0x86DD）并交给内核后，<code>netif_receive_skb()</code> 最终会把这个包扔给 <code>ipv6_rcv</code>。</p><p>它的主要工作是「安检」：</p><ol><li>版本号是不是 6？</li><li>包长是不是小于基本的 IPv6 头部长度？</li><li>如果有问题，直接丢弃并发送 ICMPv6 Parameter Problem。</li><li>如果没问题，调用 <code>ip6_rcv_finish()</code> 进入下一步处理（路由查找）。</li></ol><p><strong><code>int ip6_forward(struct sk_buff *skb)</code></strong></p><p>这是转发路径的核心。只有当这台机器被配置成路由器（<code>/proc/sys/net/ipv6/conf/all/forwarding = 1</code>）时，数据包才会走到这里。</p><p>它会做几件关键的事：</p><ul><li><strong>跳数限制（Hop Limit）检查</strong>：如果 TTL 减到了 0，发 ICMPv6 Time Exceeded，然后丢弃包。</li><li><strong>PMTU 检查</strong>：如果包太大，超过了下一跳的 MTU，它会发 ICMPv6 Packet Too Big，并丢弃包（IPv6 中间路由器绝不分片）。</li><li><strong>发送 ICMPv6 Redirect</strong>：如果它发现源主机选路很蠢（明明走本网段的另一个接口更近），它会调用 <code>ndisc_send_redirect()</code> 喊源主机一声。</li></ul><hr><h4 id="_6-路由查找——问路" tabindex="-1">6. 路由查找——问路 <a class="header-anchor" href="#_6-路由查找——问路" aria-label="Permalink to &quot;6. 路由查找——问路&quot;">​</a></h4><p><strong><code>void ip6_route_input(struct sk_buff *skb)</code></strong></p><p>这是「输入路由查找」。当包是发给本机，或者需要本机转发时，内核需要知道：「这个包到底该去哪？」 它通过调用底层的 <code>fib6_lookup()</code> 在 FIB（Forwarding Information Base）表中搜索，并把结果（一个 <code>dst_entry</code>）挂在 SKB 上。</p><blockquote><p><strong>Note</strong>：查找的结果不仅仅是「本地投递」或「转发」；对于转发包，它还决定了下一跳的 MAC 地址是什么。</p></blockquote><p><strong><code>struct dst_entry *ip6_route_output(struct net *net, const struct sock *sk, struct flowi6 *fl6)</code></strong></p><p>这是「输出路由查找」。当本机（比如 Apache 或者你的客户端程序）要发包时，它会问内核：「我要连这个 IP，该从哪个接口出去？下一跳是谁？」 它同样调用 <code>fib6_lookup()</code>，但是是在发送路径的上下文中。</p><hr><h3 id="宏与常量表——协议的通用语" tabindex="-1">宏与常量表——协议的通用语 <a class="header-anchor" href="#宏与常量表——协议的通用语" aria-label="Permalink to &quot;宏与常量表——协议的通用语&quot;">​</a></h3><p>内核代码里充满了各种魔数。为了不让人看疯头，内核用宏把这些魔数包装了一下。以下是我们在处理 IPv6 头部和组播时最常碰到的几个表。</p><h4 id="表-8-2-ipv6-扩展头部与-next-header-值" tabindex="-1">表 8-2：IPv6 扩展头部与 Next Header 值 <a class="header-anchor" href="#表-8-2-ipv6-扩展头部与-next-header-值" aria-label="Permalink to &quot;表 8-2：IPv6 扩展头部与 Next Header 值&quot;">​</a></h4><p>这是 IPv6 头部里 <code>nexthdr</code> 字段的所有可能取值。注意，扩展头和上层协议（TCP/UDP）共用这个命名空间。</p><table tabindex="0"><thead><tr><th style="text-align:left;">Linux 符号</th><th style="text-align:left;">值</th><th style="text-align:left;">描述</th></tr></thead><tbody><tr><td style="text-align:left;"><code>NEXTHDR_HOP</code></td><td style="text-align:left;">0</td><td style="text-align:left;"><strong>Hop-by-Hop Options</strong> header。必须紧跟在 IPv6 头部之后，所有路由器都要看。</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_TCP</code></td><td style="text-align:left;">6</td><td style="text-align:left;">TCP segment。老朋友了。</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_UDP</code></td><td style="text-align:left;">17</td><td style="text-align:left;">UDP message。DNS 和很多游戏协议都在这上面。</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_IPV6</code></td><td style="text-align:left;">41</td><td style="text-align:left;"><strong>IPv6 in IPv6</strong>（隧道协议）。当你看到这个时，说明你剥开了一层 IPv6，里面还有一层 IPv6。</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_ROUTING</code></td><td style="text-align:left;">43</td><td style="text-align:left;"><strong>Routing</strong> header（源路由）。用于指定数据包必须经过的中间节点。</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_FRAGMENT</code></td><td style="text-align:left;">44</td><td style="text-align:left;"><strong>Fragment</strong> header（分片）。IPv6 的分片只能由源发，这个头记录了分片信息。</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_GRE</code></td><td style="text-align:left;">47</td><td style="text-align:left;">GRE header。VPN 常用。</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_ESP</code></td><td style="text-align:left;">50</td><td style="text-align:left;">Encapsulating Security Payload（IPsec 的一部分）。加密数据。</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_AUTH</code></td><td style="text-align:left;">51</td><td style="text-align:left;">Authentication Header（IPsec 的另一部分）。校验数据完整性。</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_ICMP</code></td><td style="text-align:left;">58</td><td style="text-align:left;"><strong>ICMP for IPv6</strong>。NDP 和 MLD 都在这里面。</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_NONE</code></td><td style="text-align:left;">59</td><td style="text-align:left;"><strong>No next header</strong>。这表示后面没东西了。这在某些选项或安全包头中用来表示「到此为止」。</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_DEST</code></td><td style="text-align:left;">60</td><td style="text-align:left;"><strong>Destination Options</strong> header。只有最终目的地才需要处理的信息。</td></tr></tbody></table><h4 id="表-8-3-mldv2-组播地址记录类型" tabindex="-1">表 8-3：MLDv2 组播地址记录类型 <a class="header-anchor" href="#表-8-3-mldv2-组播地址记录类型" aria-label="Permalink to &quot;表 8-3：MLDv2 组播地址记录类型&quot;">​</a></h4><p>这是 MLDv2 报告消息里用到的类型码，用来精确描述主机对某个组播组的「态度」。</p><table tabindex="0"><thead><tr><th style="text-align:left;">Linux 符号</th><th style="text-align:left;">值</th><th style="text-align:left;">描述</th></tr></thead><tbody><tr><td style="text-align:left;"><code>MLD2_MODE_IS_INCLUDE</code></td><td style="text-align:left;">1</td><td style="text-align:left;"><strong>Include 模式</strong>：我只想收 <em>Sources List</em> 里列出的源发来的包。</td></tr><tr><td style="text-align:left;"><code>MLD2_MODE_IS_EXCLUDE</code></td><td style="text-align:left;">2</td><td style="text-align:left;"><strong>Exclude 模式</strong>：我想收这个组的所有包，<strong>除了</strong> <em>Sources List</em> 里的。</td></tr><tr><td style="text-align:left;"><code>MLD2_CHANGE_TO_INCLUDE</code></td><td style="text-align:left;">3</td><td style="text-align:left;">切换到 Include 模式。</td></tr><tr><td style="text-align:left;"><code>MLD2_CHANGE_TO_EXCLUDE</code></td><td style="text-align:left;">4</td><td style="text-align:left;">切换到 Exclude 模式。</td></tr><tr><td style="text-align:left;"><code>MLD2_ALLOW_NEW_SOURCES</code></td><td style="text-align:left;">5</td><td style="text-align:left;">允许新源加入（在 Include 模式下追加列表）。</td></tr><tr><td style="text-align:left;"><code>MLD2_BLOCK_OLD_SOURCES</code></td><td style="text-align:left;">6</td><td style="text-align:left;">阻断旧源（在 Include 模式下删除列表）。</td></tr></tbody></table><h4 id="表-8-4-icmpv6-parameter-problem-代码" tabindex="-1">表 8-4：ICMPv6 Parameter Problem 代码 <a class="header-anchor" href="#表-8-4-icmpv6-parameter-problem-代码" aria-label="Permalink to &quot;表 8-4：ICMPv6 Parameter Problem 代码&quot;">​</a></h4><p>当内核遇到解析错误时，它会发送「Parameter Problem」消息。这个 Code 字段告诉对方到底哪里错了。</p><table tabindex="0"><thead><tr><th style="text-align:left;">Linux 符号</th><th style="text-align:left;">值</th><th style="text-align:left;">描述</th></tr></thead><tbody><tr><td style="text-align:left;"><code>ICMPV6_HDR_FIELD</code></td><td style="text-align:left;">0</td><td style="text-align:left;"><strong>Erroneous header field</strong>。也就是头部字段里有烂货。</td></tr><tr><td style="text-align:left;"><code>ICMPV6_UNK_NEXTHDR</code></td><td style="text-align:left;">1</td><td style="text-align:left;"><strong>Unrecognized Next Header type</strong>。内核收到了一个它不认识的 <code>nexthdr</code> 类型，不知道该怎么剥下一层。</td></tr><tr><td style="text-align:left;"><code>ICMPV6_UNK_OPTION</code></td><td style="text-align:left;">2</td><td style="text-align:left;"><strong>Unrecognized IPv6 option</strong>。在解析 Hop-by-Hop 或 Destination Options 时，遇到了未知的 Option。</td></tr></tbody></table><hr><h3 id="特殊地址——那些「不能动」的常量" tabindex="-1">特殊地址——那些「不能动」的常量 <a class="header-anchor" href="#特殊地址——那些「不能动」的常量" aria-label="Permalink to &quot;特殊地址——那些「不能动」的常量&quot;">​</a></h3><p>内核用几个全局的 <code>struct in6_addr</code> 实例来定义那些几乎在每个网卡上都会出现的特殊地址。你可以直接引用它们，而不需要自己填数组。</p><p><strong>注意</strong>：这些实例都是只读的，直接用来比较或赋值即可。</p><ul><li><strong><code>in6addr_any</code></strong>：全零地址 <code>::</code>。代表「任意地址」或「未指定」。监听 socket 时常用。</li><li><strong><code>in6addr_loopback</code></strong>：环回地址 <code>::1</code>。永远代表本机。</li><li><strong><code>in6addr_linklocal_allnodes</code></strong>：链路本地所有节点地址 <code>ff02::1</code>。发给这个地址，本链路所有启用 IPv6 的主机都能收到。</li><li><strong><code>in6addr_linklocal_allrouters</code></strong>：链路本地所有路由器地址 <code>ff02::2</code>。只有路由器才监听这个。</li><li><strong><code>in6addr_interfacelocal_allnodes</code></strong>：接口本地所有节点 <code>ff01::1</code>。</li><li><strong><code>in6addr_interfacelocal_allrouters</code></strong>：接口本地所有路由器 <code>ff01::2</code>。</li><li><strong><code>in6addr_sitelocal_allrouters</code></strong>：站点本地所有路由器 <code>ff05::2</code>。注意，Site-Local 地址（fec0::/10）已经被 RFC 3879 废弃了，但为了兼容性，内核里还留着这些常量。</li></ul><hr><h3 id="路由表管理——用户空间命令如何落进内核" tabindex="-1">路由表管理——用户空间命令如何落进内核 <a class="header-anchor" href="#路由表管理——用户空间命令如何落进内核" aria-label="Permalink to &quot;路由表管理——用户空间命令如何落进内核&quot;">​</a></h3><p>我们在 Linux 上配置路由，一般用两个工具：古老的 <code>route</code> (net-tools) 和现代的 <code>iproute2</code> (<code>ip</code> 命令)。这两个工具虽然界面不同，但在内核底层的殊途同归。</p><h4 id="用-iproute2-添加-删除路由" tabindex="-1">用 <code>iproute2</code> 添加/删除路由 <a class="header-anchor" href="#用-iproute2-添加-删除路由" aria-label="Permalink to &quot;用 `iproute2` 添加/删除路由&quot;">​</a></h4><p>这是现在推荐的方式。它通过 Netlink Socket 与内核通信。</p><ol><li><p><strong>添加路由</strong> (<code>ip -6 route add ...</code>)：</p><ul><li>用户态发送 Netlink 消息。</li><li>内核侧 <code>net/ipv6/route.c</code> 收到消息，调用 <code>inet6_rtm_newroute()</code>。</li><li><code>inet6_rtm_newroute()</code> 解析参数，最终调用 <code>ip6_route_add()</code> 把路由条目塞进 FIB 表。</li></ul></li><li><p><strong>删除路由</strong> (<code>ip -6 route del ...</code>)：</p><ul><li>用户态发送 Netlink 消息。</li><li>内核侧调用 <code>inet6_rtm_delroute()</code>。</li><li>最终调用 <code>ip6_route_del()</code> 去掉那条路由。</li></ul></li><li><p><strong>查看路由</strong> (<code>ip -6 route show</code>)：</p><ul><li>内核侧调用 <code>inet6_dump_fib()</code>，把整个 FIB 表遍历一遍，通过 Netlink 把每一条目吐给用户空间。</li></ul></li></ol><h4 id="用-route-命令-老派做法" tabindex="-1">用 <code>route</code> 命令（老派做法） <a class="header-anchor" href="#用-route-命令-老派做法" aria-label="Permalink to &quot;用 `route` 命令（老派做法）&quot;">​</a></h4><p>这是经典的 <code>ioctl</code> 方式。虽然 <code>route</code> 命令本身已经很老了，但内核为了兼容性还保留着这部分代码。</p><ol><li><p><strong>添加路由</strong> (<code>route -A inet6 add ...</code>)：</p><ul><li><code>route</code> 工具调用 <code>ioctl(sockfd, SIOCADDRT, &amp;rt)</code>。</li><li>内核的 <code>ipv6_route_ioctl()</code> 捕获这个请求。</li><li>它内部依然是调用 <code>ip6_route_add()</code> 做正事。</li></ul></li><li><p><strong>删除路由</strong> (<code>route -A inet6 del ...</code>)：</p><ul><li>调用 <code>ioctl(sockfd, SIOCDELRT, &amp;rt)</code>。</li><li><code>ipv6_route_ioctl()</code> 处理，内部调用 <code>ip6_route_del()</code>。</li></ul></li></ol><blockquote><p><strong>结论</strong>：不管你用哪种工具，最底层干活的那两个函数永远是一样的。这再次印证了 Linux 内核设计的分层思想——用户接口可以变，但核心逻辑不动。</p></blockquote><hr><h3 id="本章回响" tabindex="-1">本章回响 <a class="header-anchor" href="#本章回响" aria-label="Permalink to &quot;本章回响&quot;">​</a></h3><p>好了，我们终于走完了 IPv6 这一整章——从最初的 128 位地址设计，到邻居发现的那些细碎报文，再到组播的源过滤和今天的这份速查表。</p><p>这章真正留给你的东西，应该不是这几十个函数的名字，而是对 IPv6 <strong>「大一统」</strong> 设计的体会。 你看，IPv4 里的 ARP、RARP、ICMP Redirect、IGMP……这些散落在各处的协议，在 IPv6 里统统被归拢到了 ICMPv6 这一面大旗下。NDP 是 ICMPv6，MLD 是 ICMPv6，连报错都是 ICMPv6。这种统一让协议栈变得更瘦、更高效，但也让每一个 ICMPv6 包的解析变得更加关键——漏看一个类型，可能就意味着丢了一个关键的邻居信息。</p><p>我们花了很多时间在 <code>inet6_dev</code>、<code>mc_list</code> 和 <code>fib6_table</code> 这些结构体里打转，是因为网络协议不只是理论上的状态机，它是实实在在的内核内存结构。如果你不知道组播过滤器挂在哪个链表上，当你调试丢包问题时，你就只会对着 tcpdump 抓下来的报文发呆，而不知道去内核代码里的哪个 <code>if</code> 语句里加断点。</p><p>下一章，我们将跨过协议栈的处理层，进入那个传说中的「网络大过滤器」——<strong>Netfilter</strong>。那是内核网络栈里最险峻的一段路，也是防火墙、NAT、Conntrack 这些神奇功能的发生地。你会看到，当一个数据包终于被解析完 IPv6 头部，正准备欢天喜地交给 socket 时，是如何被 Netfilter 这只「上帝之手」一把抓住，审判生死的。</p><p>那将是一场关于拦截与改写的游戏。</p><hr><h2 id="练习题" tabindex="-1">练习题 <a class="header-anchor" href="#练习题" aria-label="Permalink to &quot;练习题&quot;">​</a></h2><h3 id="练习-1-understanding" tabindex="-1">练习 1：understanding <a class="header-anchor" href="#练习-1-understanding" aria-label="Permalink to &quot;练习 1：understanding&quot;">​</a></h3><p><strong>题目</strong>：在 IPv6 协议栈的初始化过程中，内核需要将特定的协议处理逻辑（如 TCPv6、UDPv6 或扩展头处理）注册到系统中。假设你是内核开发者，需要编写一个模块来处理一个新的 IPv6 扩展头（Next Header 号为 N）。你应该调用哪个函数来完成注册？该函数的签名是什么，以及它会将处理逻辑存储在内核的哪个全局数据结构中？</p><details><summary>答案与解析</summary><p><strong>答案</strong>：应调用 <code>inet6_add_protocol()</code> 函数。其签名为 <code>int inet6_add_protocol(const struct inet6_protocol *prot, unsigned char protocol)</code>。该处理逻辑会被存储在全局数组 <code>inet6_protos[]</code> 中。</p><p><strong>解析</strong>：根据知识点 <code>inet6_add_protocol()</code>，这是内核用于注册 IPv6 协议处理器（包括传输层协议和扩展头）的标准函数。注册时，协议号（如 IPPROTO_UDP 或扩展头标识）作为索引，将 <code>struct inet6_protocol</code> 指针存入 <code>inet6_protos</code> 数组。这样当 <code>ipv6_rcv</code> 解析数据包时，就能通过 <code>nexthdr</code> 字段找到对应的处理回调函数。</p></details><h3 id="练习-2-application" tabindex="-1">练习 2：application <a class="header-anchor" href="#练习-2-application" aria-label="Permalink to &quot;练习 2：application&quot;">​</a></h3><p><strong>题目</strong>：一个主机刚刚生成了一个链路本地 IPv6 地址。在进行重复地址检测（DAD）通过之前，该地址处于“临时”状态。为了验证该地址的唯一性，邻居发现协议需要使用一种特殊的组播地址。如果主机的链路本地地址是 <code>fe80::2aa:ff:fe3f:4a21</code>（忽略 <code>ff:fe</code> 插入位），那么它应该加入哪个请求节点组播地址来进行 DAD 验证？请写出计算过程和结果。</p><details><summary>答案与解析</summary><p><strong>答案</strong>：结果为 <code>ff02::1:ff3f:4a21</code>。计算过程：取单播地址的低 24 位（3f:4a:21），加上前缀 <code>ff02:0:0:0:0:1:ff00::/104</code>。</p><p><strong>解析</strong>：根据知识点 <code>Solicited-Node Multicast Address</code>，请求节点组播地址用于邻居发现和 DAD。其生成规则是将单播/任播地址的低位 24 位拼接到固定前缀 <code>ff02:0:0:0:0:1:ff00::/104</code> 上。题目中地址低位 24 位为 <code>3f:4a:21</code>，拼接后即得 <code>ff02::1:ff3f:4a21</code>。主机发送邻居请求报文到此地址，若有其他主机拥有该地址，会响应邻居通告。</p></details><h3 id="练习-3-thinking" tabindex="-1">练习 3：thinking <a class="header-anchor" href="#练习-3-thinking" aria-label="Permalink to &quot;练习 3：thinking&quot;">​</a></h3><p><strong>题目</strong>：IPv6 的“自动配置”允许主机无状态生成 IP 地址。然而，标准的 EUI-64 接口 ID 生成方式（基于 MAC 地址）可能导致用户隐私被追踪。Linux 内核提供了一种名为“Privacy Extensions”的机制来解决这个问题。请结合 RFC 4941 分析，为什么基于 MAC 地址生成 IPv6 接口 ID 存在隐私风险？内核是如何通过 Privacy Extensions 机制缓解这一风险的？</p><details><summary>答案与解析</summary><p><strong>答案</strong>：基于 MAC 地址生成接口 ID 会导致接口 ID 固定不变，且 MAC 地址是全球唯一的硬件标识。只要用户接入网络，其 IPv6 地址的后半部分始终不变，使得跨网络的用户行为追踪变得容易。Privacy Extensions（RFC 4941）机制通过生成随机的接口 ID（而非基于 MAC）来创建临时地址，并且这些临时地址会定期过期和更新，从而增加了追踪难度，保护了用户隐私。</p><p><strong>解析</strong>：此题考察对“知识点 Autoconfiguration”和“Privacy Extensions”的深度理解。IPv6 的无状态自动配置通常结合 MAC 地址生成接口 ID（如 EUI-64），这虽然在地址唯一性上很方便，但也暴露了设备身份。Privacy Extensions 的核心在于“去关联性”，通过随机化接口 ID 并引入生命周期（Preferred Lifetime），使得同一个设备在不同时间段或不同网络中使用不同的 IP 地址，从而防止被持续监控。这是在保持网络层连通性同时，提升用户安全性的典型设计。</p></details><hr><h2 id="要点提炼" tabindex="-1">要点提炼 <a class="header-anchor" href="#要点提炼" aria-label="Permalink to &quot;要点提炼&quot;">​</a></h2><p>IPv6 的内核实现建立在数据结构 <code>in6_addr</code> 和全新的地址分类体系之上。通过使用 Union（联合体）将 128 位地址划分为 8 位、16 位和 32 位视图，内核能够高效地处理不同粒度的内存操作与位运算。同时，IPv6 彻底摒弃了广播，转而采用单播、任播和组播三种模式。特别是引入了“被请求节点组播地址”（Solicited-Node Multicast Address），它通过将地址映射到特定的 <code>ff02::1:ffxx:xxxx</code> 组播组，使得邻居发现过程只在极小范围内进行，从而极大地解决了 IPv4 时代 ARP 广报带来的“以太网噪音”问题。</p><p>协议头部设计体现了 IPv6 “精简骨架、灵活扩展” 的工程哲学。IPv6 头部被固定为 40 字节，去除了校验和（Checksum）以减轻路由器负担，并将所有可选功能移出。通过 <code>nexthdr</code> 字段，IPv6 引入了“扩展头部”的链式处理机制（如 Hop-by-Hop、Routing、Fragment），每个头部像链表节点一样串联。这种设计使得中间路由器只需处理 IPv6 主头和极个别的逐跳选项，极大地提升了转发效率，实现了从“复杂变长结构”到“固定快速处理”的回归。</p><p>自动配置（Autoconfiguration）机制赋予了主机“即插即用”的能力，无需 DHCP 服务器即可生成全球路由地址。这一过程分为四个阶段：首先生成链路本地地址并进行重复地址检测（DAD）；接着发送路由器请求（RS）；然后接收包含前缀信息的路由器公告（RA）；最后结合接口 ID（通常基于 MAC 地址或随机生成的隐私扩展）合成全球单播地址。配合 Valid Lifetime 和 Preferred Lifetime 机制，网络管理员可以通过调整 RA 参数实现全网前缀的平滑无缝重编号。</p><p>在数据包接收路径 <code>ipv6_rcv()</code> 中，内核通过严格的“安检”规则来维护协议的纯洁性。函数首先会丢弃版本号错误、目标地址为回环地址或源地址为组播地址的非法数据包，并强制解析 Hop-by-Hop 扩展头。随后，数据包通过 Netfilter 钩子进入 <code>ip6_rcv_finish()</code>，在这里执行路由查找（<code>fib6_lookup</code>）。根据查找结果，内核决定数据包的命运：是交给本地协议栈（<code>ip6_input</code>）进行扩展头剥洋葱式解析并分发至上层，还是进行转发（<code>ip6_forward</code>）。</p><p>分片处理机制的变革是 IPv6 区别于 IPv4 的关键特性之一，同时也带来了潜在的 MTU 陷阱。IPv6 禁止中间路由器进行分片，所有分片行为必须在源主机完成。如果在传输路径上遇到 MTU 更小的链路，路由器会直接丢弃数据包并回传 ICMPv6 “Packet Too Big” 消息，强制源主机进行 Path MTU Discovery（PMTUD）。因此，在配置防火墙或路由时，必须确保不阻断 ICMPv6 流量，否则会导致大包连通性静默失败，这是 IPv6 网络运维中最常见的故障点。</p>',115)])])}const u=e(r,[["render",c]]);export{_ as __pageData,u as default};
