import{_ as s,o as i,c as a,a as n}from"./chunks/framework.8tiMeCK-.js";const _=JSON.parse('{"title":"Mac80211 实现细节","description":"","frontmatter":{},"headers":[],"relativePath":"notes/linux_kernel_networking/ch12_6.md","filePath":"notes/linux_kernel_networking/ch12_6.md","lastUpdated":null}'),t={name:"notes/linux_kernel_networking/ch12_6.md"};function o(l,e,d,r,c,p){return i(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="mac80211-实现细节" tabindex="-1">Mac80211 实现细节 <a class="header-anchor" href="#mac80211-实现细节" aria-label="Permalink to &quot;Mac80211 实现细节&quot;">​</a></h1><p>现在，让我们把目光从空中的协议交互收回内核。 上一节我们看着数据包怎么变换身份，怎么在省电模式下玩捉迷藏。 但你可能一直有个疑问：<strong>这些是谁在管？代码到底长什么样？</strong></p><p>Linux 的无线世界并不是杂乱无章的。它有一套标准的「施工规范」叫 <code>mac80211</code>。 无论是 Intel 的 iwlwifi，还是 Marvell 的 lbtf，只要想进 Linux 的无线门，就得按 <code>mac80211</code> 的规矩办。 这一节，我们要掀开引擎盖，看看里面的活塞和连杆。 如果不去摸透 <code>ieee80211_hw</code> 和 <code>sta_info</code> 这几块核心骨头，你对无线栈的理解就永远是隔着一层玻璃看花。</p><hr><h3 id="_12-6-核心骨架-从-ieee80211-hw-到驱动回调" tabindex="-1">12.6 核心骨架：从 ieee80211_hw 到驱动回调 <a class="header-anchor" href="#_12-6-核心骨架-从-ieee80211-hw-到驱动回调" aria-label="Permalink to &quot;12.6  核心骨架：从 ieee80211_hw 到驱动回调&quot;">​</a></h3><p><code>mac80211</code> 的 API 复杂到令人发指，里面全是让人眼晕的细节。 我不打算把这几十兆的代码全搬出来（那也没意义），但我会给你一张最精确的寻宝图，让你知道哪里是入口，哪里是陷阱。</p><p>一切始于一个叫 <code>struct ieee80211_hw</code> 的结构体。 你可以把它理解成 <strong>「硬件设备的身份证」</strong>。</p><p>内核用这个结构体来告诉无线栈：「嘿，这块网卡长什么样，它有哪些绝活。」 但这里有一个关键的设计细节：<code>ieee80211_hw</code> 里面有一个 <code>priv</code> 指针。 这可不是个普通的指针，它是驱动开发者的私房钱。 这个 <code>priv</code> 指针被定义为 <code>void *</code>，意味着内核根本不关心也不看里面存的是什么。 只有驱动自己知道，这通常指向一个更大的、驱动自己定义的私有结构体——比如 Intel 的驱动里叫 <code>iwl_priv</code>，Marvell 的叫 <code>lbtf_private</code>。 这种设计把「通用框架」和「私有实现」切得干干净净。</p><p>要开始干活，驱动得先做几件事：</p><ol><li><strong>申领空间</strong>：调用 <code>ieee80211_alloc_hw(size_t priv_data_len, const struct ieee80211_ops *ops)</code>。 这一步不仅分配了 <code>ieee80211_hw</code>，还顺带分配了你要求的 <code>priv_data_len</code> 大小的私有空间，并把两者绑在一起。</li><li><strong>注册设备</strong>：拿着分配好的 <code>hw</code>，调用 <code>ieee80211_register_hw(struct ieee80211_hw *hw)</code>。 这一步就像是给设备上户口，从此它就在内核里存在了。</li><li><strong>接收数据</strong>：当硬件收到包时，驱动通常会调用 <code>ieee80211_rx_irqsafe()</code>（在 <code>net/mac80211/rx.c</code> 里实现）。 为什么叫 <code>irqsafe</code>？因为这是在中断上下文里调用的，不能睡觉。</li></ol><p>那张你在文档里见过的架构图（Figure 12-5），其实画的就是这层关系： 驱动层和 <code>mac80211</code> 层之间，就是靠 <code>ieee80211_ops</code> 这组回调函数握手连接的。</p><hr><h3 id="_12-6-1-驱动的操作集" tabindex="-1">12.6.1 驱动的操作集 <a class="header-anchor" href="#_12-6-1-驱动的操作集" aria-label="Permalink to &quot;12.6.1  驱动的操作集&quot;">​</a></h3><p>刚才提到的 <code>ieee80211_ops</code>，就是驱动写给内核的一份「承诺书」。 这也是一个结构体，里面全是函数指针。不是每个都要填，但有几个是命根子。</p><p>让我们看几个最核心的回调：</p><ul><li><strong><code>tx()</code></strong>：这是发工资的部门。每次内核要发包，都会调这个函数。 通常情况下它得返回 <code>NETDEV_TX_OK</code>，除非出了岔子。</li><li><strong><code>start()</code></strong> 和 <strong><code>stop()</code></strong>：这对兄弟负责电源开关。 <code>start()</code> 用来激活硬件，打开接收帧的开关；<code>stop()</code> 则是关机，通常会把硬件彻底断电。</li><li><strong><code>add_interface()</code></strong> 和 <strong><code>remove_interface()</code></strong>： 这听起来很抽象，实际上很简单：当你 <code>ifconfig wlan0 up</code> 的时候，这个回调就会被触发。 内核在问驱动：「嘿，我要把虚拟网络设备绑上来了，你硬件那边准备好接客了吗？」 <code>remove_interface</code> 则是反向操作。</li><li><strong><code>config()</code></strong>：这是调参旋钮。 比如你要切换信道（从 CH6 切到 CH11），内核就会调这个函数通知硬件去改频率。</li><li><strong><code>configure_filter()</code></strong>：这是看门狗的设置。 它告诉硬件：「我只想要这几种类型的包，其他的别吵我。」</li></ul><hr><h3 id="_12-6-2-站点管理-sta-info-结构" tabindex="-1">12.6.2 站点管理：sta_info 结构 <a class="header-anchor" href="#_12-6-2-站点管理-sta-info-结构" aria-label="Permalink to &quot;12.6.2  站点管理：sta_info 结构&quot;">​</a></h3><p>除了硬件本身，无线世界里最重要的是什么？是 <strong>「人」</strong>（ Stations，站点）。 无论是连上来的手机，还是隔壁的路由器，在内核里都是一个 <code>struct sta_info</code>（定义在 <code>net/mac80211/sta_info.h</code>）。</p><p>这个结构体内容非常丰富，简直就是一个站点的黑匣子：</p><ul><li><strong>统计计数器</strong>：收了多少包，发了多少包，丢了多少包。</li><li><strong>标志位</strong>：它是不是在省电模式？它有没有授权？</li><li><strong>ps_tx_buf</strong>：还记得上一节说的省电缓冲吗？就是这个数组，用来存给睡着了站点的单播包。</li><li><strong>debugfs 条目</strong>：方便我们在用户空间窥探它的状态。</li></ul><p>内核怎么管理成百上千个这样的站点？ 用哈希表（<code>sta_hash</code>）和链表（<code>sta_list</code>）。 当你要找一个站点时，通常会用到这三个工具：</p><ul><li><code>sta_info_insert(struct sta_info *sta)</code>：把一个新站点加入表格。</li><li><code>sta_info_destroy_addr(...)</code>：踢人。通过 MAC 地址把站点干掉（内部会调用 <code>__sta_info_destroy</code>）。</li><li><code>sta_info_get(...)</code>：查户口。给定一个 MAC 地址（通常是 BSSID），把这个站点的 <code>sta_info</code> 结构体捞出来。</li></ul><hr><h3 id="_12-6-3-rx-path-当数据包飞进来" tabindex="-1">12.6.3 RX Path：当数据包飞进来 <a class="header-anchor" href="#_12-6-3-rx-path-当数据包飞进来" aria-label="Permalink to &quot;12.6.3  RX Path：当数据包飞进来&quot;">​</a></h3><p>接收路径是整个栈里最繁忙的地方。 主角是 <code>ieee80211_rx()</code> 函数（在 <code>net/mac80211/rx.c</code>）。</p><p>当驱动把一个 SKB 递上来时，它不是空手来的。它在 SKB 的控制缓冲区里塞了一张小纸条，叫 <code>ieee80211_rx_status</code>。 你可以用 <code>IEEE80211_SKB_RXCB()</code> 这个宏把这张纸条抽出来看。 上面写着什么？写着这包收过来的质量怎么样：FCS 校验有没有挂？信号强度多少？ 如果 flag 字段里带上了 <code>RX_FLAG_FAILED_FCS_CRC</code>，那恭喜，这包是废的。</p><p>接下来是一系列流水线作业：</p><ol><li><p><strong>监听模式处理</strong>：<code>ieee80211_rx_monitor()</code> 会出场。 如果你开了 Monitor Mode（抓包用的），它会先帮你剥掉 FCS（校验位），顺便处理一下可能存在的 <code>radiotap</code> 头部。 <em>注意：不是所有网卡都支持 Monitor Mode。</em></p></li><li><p><strong>802.11n 的重排序</strong>：如果你用的是 HT（802.11n），乱序的包需要排队。 这时 <code>ieee80211_rx_reorder_ampdu()</code> 会被叫来把乱了次序的积木排好。</p></li><li><p><strong>真正的处理</strong>：<code>__ieee80211_rx_handle_packet()</code> 最终会调用 <code>ieee80211_invoke_rx_handlers()</code>。</p></li></ol><p>这里有一个非常精妙的设计，叫 <strong>「责任链模式」</strong>。 内核有一串处理器，每个处理器都看一眼这个包：</p><ul><li>如果它说「我不处理，下一个」，就返回 <code>RX_CONTINUE</code>。</li><li>如果它说「这包我吞了」，就返回 <code>RX_QUEUED</code>。</li><li>如果它说「这包是垃圾」，就返回 <code>RX_DROP_MONITOR</code> 或 <code>RX_DROP_UNUSABLE</code>。</li></ul><p>看一段真实的代码（<code>net/mac80211/rx.c</code>）：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ieee80211_rx_result</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ieee80211_rx_h_mgmt_check</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ieee80211_rx_data </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">rx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ieee80211_mgmt </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mgmt </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ieee80211_mgmt </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) rx-&gt;skb-&gt;data;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ieee80211_rx_status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> IEEE80211_SKB_RXCB</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(rx-&gt;skb);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        . . .</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (rx-&gt;skb-&gt;len </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 24</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> RX_DROP_MONITOR;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ieee80211_is_mgmt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(mgmt-&gt;frame_control))</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> RX_DROP_MONITOR;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               .  .  .</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br></div></div><p>这代码在干嘛？ 它在检查：这包既然说是管理帧，长度连 24 字节都不够（802.11 头部的最小长度），或者是假的不是管理帧，直接 <code>RX_DROP_MONITOR</code>——扔进垃圾桶。 如果是 PS-Poll 包，但收包的不是 AP 角色，那也是 <code>RX_DROP_UNUSABLE</code>。 这种层层过滤的设计，保证了内核不会被非法包把 CPU 跑满。</p><hr><h3 id="_12-6-4-tx-path-当数据包飞出去" tabindex="-1">12.6.4 TX Path：当数据包飞出去 <a class="header-anchor" href="#_12-6-4-tx-path-当数据包飞出去" aria-label="Permalink to &quot;12.6.4  TX Path：当数据包飞出去&quot;">​</a></h3><p>发送路径的主角是 <code>ieee80211_tx()</code>（在 <code>net/mac80211/tx.c</code>）。 它的逻辑和 RX 很像，也是先准备，再过流水线。</p><ol><li><p><strong>准备工作</strong>：<code>__ieee80211_tx_prepare()</code>。 这里会做各种预检查，比如，如果你发的是一个长度小于 10 字节的包，内核会觉得你在开玩笑，直接 <code>dev_kfree_skb(skb)</code> 丢掉。</p></li><li><p><strong>发送处理器链</strong>：<code>invoke_tx_handlers()</code>。 和 RX 一样，这也是用宏 <code>CALL_TXH</code> 串起来的一排处理函数。</p><ul><li><code>TX_CONTINUE</code>：没我的事，走下一个。</li><li><code>TX_QUEUED</code>：这包被我接管了（比如进了缓冲区）。</li><li><code>TX_DROP</code>：这包有坑，发不得。</li></ul></li></ol><p>如果这一路绿灯，最后会调用 <code>__ieee80211_tx()</code> 把数据包真正推向硬件驱动的 <code>tx()</code> 回调。</p><hr><h3 id="_12-6-5-分片-当大象装进冰箱" tabindex="-1">12.6.5 分片：当大象装进冰箱 <a class="header-anchor" href="#_12-6-5-分片-当大象装进冰箱" aria-label="Permalink to &quot;12.6.5  分片：当大象装进冰箱&quot;">​</a></h3><p>现在的无线很快，但在以前，为了保证可靠性，大象也是要切碎了的。 这就是 <strong>分片</strong>。</p><p>802.11 规定，每个站点都有一个分片阈值。 如果单播包的大小超过了这个阈值，就必须切成小片。 每切一片，都要单独确认一次（ACK）。 你可以在命令行里用 <code>iwconfig</code> 看到这个阈值，或者改掉它：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iwconfig</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wlan0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> frag</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 512</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>这就把阈值设成了 512 字节。 阈值越小，碰撞的概率越低，但开销越大。 在发送端，切肉的刀是 <code>ieee80211_tx_h_fragment()</code> 方法。 在接收端，拼肉的神器是 <code>ieee80211_rx_h_defragment()</code> 方法。</p><p><strong>但这里有个巨大的坑</strong>： 分片和聚合（802.11n 用来提速的技术）是水火不容的。 既然现在的 802.11n/ac 速度这么快，空中时间很短，分片这玩意儿现在已经很少见到了。 如果你在现代的高速率网络里开启分片，可能会发现网络反而变慢了，甚至因为不支持聚合而导致吞吐量暴跌。</p><hr><h3 id="_12-6-6-debugfs-内核的黑匣子" tabindex="-1">12.6.6 debugfs：内核的黑匣子 <a class="header-anchor" href="#_12-6-6-debugfs-内核的黑匣子" aria-label="Permalink to &quot;12.6.6  debugfs：内核的黑匣子&quot;">​</a></h3><p>内核怕你搞不懂它在想什么，所以留了个后门叫 <code>debugfs</code>。 这是一个虚拟文件系统，专门用来往外吐调试信息。</p><p>首先你得把它挂载上来：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mount</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> debugfs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> none_debugs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /sys/kernel/debug</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>⚠️ <strong>注意</strong> 你的内核编译时必须选上 <code>CONFIG_DEBUG_FS</code>，否则这一步会报错，你也看不到下面的任何内容。</p><p>假设你的物理设备是 <code>phy0</code>，你就可以去 <code>/sys/kernel/debug/ieee80211/phy0</code> 下面挖宝。</p><p>这里有几个特别好用的条目：</p><ol><li><p><strong><code>total_ps_buffered</code></strong>： 这就是 AP 的「暂存仓」。 它显示了 AP 为那些睡着的站点（PS 模式）缓存了多少个包（包括单播和组播）。 这是 <code>ieee80211_tx_h_unicast_ps_buf</code> 和 <code>ieee80211_tx_h_multicast_ps_buf</code> 两个人一起加出来的数。</p></li><li><p><strong><code>statistics</code> 目录</strong>： 这里面全是战绩。</p><ul><li><code>frame_duplicate_count</code>：收到了多少个重复的包（有人在重传）。</li><li><code>transmitted_frame_count</code>：成功发出了多少个包。</li><li><code>retry_count</code>：重试了多少次。如果这个数字飙升，说明你的信号质量很差，或者干扰很大。</li><li><code>fragmentation_threshold</code>：当前的分片阈值。</li></ul></li><li><p><strong><code>netdev:wlan0</code> 目录</strong>（假设你的接口叫 wlan0）： 这里能看到具体的接口信息。</p><ul><li><code>aid</code>：关联 ID。</li><li><code>assoc_tries</code>：试了多少次才连上。</li><li><code>bssid</code>：你连的路由器地址。</li></ul></li><li><p><strong><code>rc/name</code></strong>： 这里写着当前用的速率控制算法名字。 选对算法比选对路由器还重要。</p></li></ol><hr><h3 id="_12-6-7-无线模式-网卡的身份卡" tabindex="-1">12.6.7 无线模式：网卡的身份卡 <a class="header-anchor" href="#_12-6-7-无线模式-网卡的身份卡" aria-label="Permalink to &quot;12.6.7  无线模式：网卡的身份卡&quot;">​</a></h3><p>最后，网卡不是只有一种形态。 根据你想干什么，你可以把它切成不同的模式。 这取决于硬件支不支持（你可以查 <code>wiphy</code> 的 <code>interface_modes</code> 字段，或者去 linuxwireless.org 查你的驱动列表）。</p><ul><li><strong>AP 模式 (<code>NL80211_IFTYPE_AP</code>)</strong>： 网卡变成路由器。它维护一个站点列表，网络名字（SSID）由它定义。</li><li><strong>Managed 模式 (<code>NL80211_IFTYPE_STATION</code>)</strong>： 网卡变成客户端。这就是你手机连 WiFi 的模式。</li><li><strong>Monitor 模式 (<code>NL80211_IFTYPE_MONITOR</code>)</strong>： 嗅探者模式。所有经过空中的包，不管是不是发给它的，统统收下。 这是抓包神器（比如 airodump-ng）的依赖。 在这个模式下发包叫 <strong>注入</strong>，数据包会被打上 <code>IEEE80211_TX_CTL_INJECTED</code> 的特殊标记。</li><li><strong>Ad Hoc 模式 (<code>NL80211_IFTYPE_ADHOC</code>)</strong>： 点对点，没有 AP，大家平等。</li><li><strong>WDS 模式 (<code>NL80211_IFTYPE_WDS</code>)</strong>： 无线分发系统，用来做桥接的。</li><li><strong>Mesh 模式 (<code>NL80211_IFTYPE_MESH_POINT</code>)</strong>： 网状网，我们下一节会专门讲这个。</li></ul><hr><p>至此，我们已经看完了 <code>mac80211</code> 的骨架和肌肉。 但现在的无线世界不仅要通，还要快。 下一节，我们要进入 <strong>802.11n 的高速公路</strong>，看看多天线（MIMO）和聚合技术是怎么把无线网变成光纤的。 那个世界里的逻辑，又完全是另一套了。</p>`,61)])])}const k=s(t,[["render",o]]);export{_ as __pageData,k as default};
