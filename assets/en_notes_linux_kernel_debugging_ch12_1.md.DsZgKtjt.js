import{_ as t,o as s,c as o,a as n}from"./chunks/framework.8tiMeCK-.js";const d=JSON.parse('{"title":"Chapter 12: The Debugging Arsenal: No Silver Bullet","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch12_1.md","filePath":"en/notes/linux_kernel_debugging/ch12_1.md","lastUpdated":null}'),i={name:"en/notes/linux_kernel_debugging/ch12_1.md"};function a(r,e,l,h,c,u){return s(),o("div",null,[...e[0]||(e[0]=[n(`<h1 id="chapter-12-the-debugging-arsenal-no-silver-bullet" tabindex="-1">Chapter 12: The Debugging Arsenal: No Silver Bullet <a class="header-anchor" href="#chapter-12-the-debugging-arsenal-no-silver-bullet" aria-label="Permalink to &quot;Chapter 12: The Debugging Arsenal: No Silver Bullet&quot;">​</a></h1><p>There is a class of problems that appear to be about tools, but are actually philosophical in nature.</p><p>In this chapter, we tackle exactly such a problem: <strong>how much can you really trust your tools?</strong></p><p>Many beginners (and even experienced engineers) share an obsession: finding a &quot;universal debugger&quot; — just run it, and it highlights all bugs in red, points out every memory leak, and refactors your code as a bonus.</p><p>Frankly, this obsession is dangerous. It breeds a false sense of security.</p><p>The mission of this chapter is to shatter that illusion. We take static analysis, dynamic analysis, kernel crash dumps (kdump), fuzzing, and code coverage — tools that seem unrelated — and piece them together into a complete &quot;debugging map.&quot; You will see that no single tool can cover all scenarios alone — and that is exactly why we need to combine them.</p><p>There is no silver bullet. But if you melt all those silver fragments and forge them into a sword, that is an entirely different story.</p><hr><p>Let&#39;s begin.</p><h2 id="_12-1-the-remaining-weapons-from-static-to-post-mortem" tabindex="-1">12.1 The Remaining Weapons: From Static to Post-Mortem <a class="header-anchor" href="#_12-1-the-remaining-weapons-from-static-to-post-mortem" aria-label="Permalink to &quot;12.1 The Remaining Weapons: From Static to Post-Mortem&quot;">​</a></h2><p>This chapter is a bit of a &quot;mixed bag&quot; — but do not underestimate these miscellanea. We previously covered many dynamic tracing and live debugging techniques, but they all share a limitation: you need the system to be alive, or you need to be able to reproduce the problem.</p><p>What if the issue happens at 3 AM while you are asleep? What if the bug is hiding in some extremely obscure code path?</p><p>In this section, we fill in the final pieces of the puzzle.</p><h3 id="⚫️-kdump-taking-an-autopsy-photo-of-the-kernel" tabindex="-1">⚫️ kdump: Taking an Autopsy Photo of the Kernel <a class="header-anchor" href="#⚫️-kdump-taking-an-autopsy-photo-of-the-kernel" aria-label="Permalink to &quot;⚫️ kdump: Taking an Autopsy Photo of the Kernel&quot;">​</a></h3><p>First, let&#39;s talk about that blood-pressure-spiking scenario: <strong>Kernel Panic</strong>.</p><p>When a production server suddenly freezes, the screen locked on that dreaded &quot;Kernel panic - not syncing: Attempted to kill init!&quot;, what do you do? Reboot? And then pray it does not happen again?</p><p>No, what you need is an <strong>autopsy report</strong>.</p><p>That is exactly what <strong>kdump</strong> is for.</p><p>You can think of kdump as the kernel&#39;s &quot;black box.&quot; When the airplane (the main kernel) goes down, the black box (the capture kernel) is ejected, recording all the data at the crash scene (a memory image).</p><p><strong>But the &quot;black box&quot; analogy is slightly inaccurate</strong>: a real black box is an independent device, whereas kdump&#39;s &quot;capture kernel&quot; is actually another stripped-down Linux kernel that you preload into memory. It does nothing normally, sitting quietly in a reserved memory region sleeping; only when the main kernel crashes is it &quot;woken up.&quot;</p><p>Behind this lies a mechanism called <strong>kexec</strong>. <strong>kexec</strong> allows the kernel to bypass the sluggish BIOS/firmware boot process and jump directly to the entry address of another kernel. It is like playing a parkour game where you do not need to return to the starting point — you are instantly teleported to the next level&#39;s entrance.</p><p><strong>Back to the &quot;black box&quot;</strong>: <code>crashkernel=size@offset</code> is a boot parameter where you tell the main kernel, &quot;Please reserve this much space (size) at this location (offset) for me; I am going to place my black box there.&quot;</p><p>Configuring and using it typically involves a few steps:</p><ol><li><strong>Reserve memory</strong>: Add <code>crashkernel=256M</code> to the main kernel&#39;s grub configuration (or adjust according to your physical memory).</li><li><strong>Load the capture kernel</strong>: Usually done via <code>kdumpctl</code> or your distribution&#39;s script services. This maps <code>/proc/kcore</code> to a specific location, or directly loads a specific <code>vmlinuz</code> using kexec.</li><li><strong>Trigger a crash</strong>: Never do this in production, but in a test environment, you can use <code>echo c &gt; /proc/sysrq-trigger</code> to manually simulate a crash.</li></ol><p>Once the capture kernel boots, you will find that although it is tiny, it can access what the main kernel left behind: <strong><code>/proc/vmcore</code></strong>.</p><p>This file is not an ordinary file; it is an ELF-format pseudo-file representing the entire physical memory at the time of the main kernel&#39;s crash. You can <code>cp</code> it out, and that becomes your <strong>vmcore</strong> file.</p><p>Once you have the vmcore, the real forensic work begins. You will need <strong>crash (utility)</strong>.</p><p>Crash is a powerful user-space analysis tool. It can disassemble that massive dump of seemingly garbled memory into readable data structures.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> crash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vmlinux</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vmcore</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">...</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">crash</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">bt</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # 查看 panic 时的调用栈</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">crash</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ps</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # 查看当时的进程列表</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">crash</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">log</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # 查看内核环形缓冲区里的日志</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>This is the power of <strong>Post-mortem analysis</strong>. Although you cannot change the past, you can at least see how it happened. For those eerie bugs that &quot;only crash once a month,&quot; this is almost the only lifeline.</p><h3 id="⚫️-static-analysis-finding-bugs-without-running-code" tabindex="-1">⚫️ Static Analysis: Finding Bugs Without Running Code <a class="header-anchor" href="#⚫️-static-analysis-finding-bugs-without-running-code" aria-label="Permalink to &quot;⚫️ Static Analysis: Finding Bugs Without Running Code&quot;">​</a></h3><p>Next, let&#39;s shift our gaze from &quot;runtime&quot; to &quot;the code itself.&quot;</p><p><strong>Static Analysis</strong> sounds boring — like nitpicking at compile time. But frankly, it might offer the best return on investment of any debugging technique.</p><p>Think about it: if the compiler could tell you &quot;this will divide by zero&quot; while you are writing the code, would you still need to wake up at 3 AM to look at <code>dmesg</code>?</p><p>The kernel community has a few familiar faces: <strong>sparse</strong>, <strong>smatch</strong>, and <strong>Coccinelle</strong>.</p><ul><li><strong>Sparse</strong>: Written primarily by Linus Torvalds, it checks for type errors. For example, if you directly dereference a <code>__user</code> pointer, sparse will warn you immediately.</li><li><strong>Smatch</strong>: Built on top of sparse, it goes a step further by building control flow graphs to check for complex logic errors, such as forgetting to unlock (mutex_unlock).</li><li><strong>Coccinelle</strong>: This is a heavy hitter. It does not just check — it can <strong>modify code</strong>. It uses a scripting language called Semantic Patches. If you want to replace one API with another across the entire kernel tree, Coccinelle can finish in minutes what would take humans days.</li></ul><p>Do not ignore these tools. What they catch are often <strong>code smells</strong> — things that do not affect compilation but will rot into real bugs over time.</p><p>Things like <strong>Uninitialized Memory Reads (UMR)</strong> or <strong>Use-After-Return (UAR)</strong>. Some of these are invisible to the human eye no matter how many times you review the code, but a tool can spot them instantly.</p><h3 id="⚫️-code-coverage-and-fault-injection-testing-those-impossible-paths" tabindex="-1">⚫️ Code Coverage and Fault Injection: Testing Those &quot;Impossible&quot; Paths <a class="header-anchor" href="#⚫️-code-coverage-and-fault-injection-testing-those-impossible-paths" aria-label="Permalink to &quot;⚫️ Code Coverage and Fault Injection: Testing Those &quot;Impossible&quot; Paths&quot;">​</a></h3><p>If static analysis can find so many problems, why do we still need runtime testing?</p><p>Because static analysis does not understand &quot;logic.&quot; It knows you forgot to initialize a pointer, but it does not know whether your algorithm will deadlock under network congestion.</p><p>Two concepts must be mentioned here: <strong>Code Coverage</strong> and <strong>Fault Injection</strong>.</p><p><strong>Code Coverage</strong> is simple: when you run a test, how much of the code actually gets executed?</p><p>If the coverage report shows that line 452 (the error handling path) of your <code>drivers/foobar.c</code> is red (never executed), you should feel uneasy. That path is like a sealed-off corridor during a home renovation — there might be a monster living inside.</p><p>In the kernel, we typically use <strong>gcov</strong> (gcc&#39;s native tool) paired with <strong>lcov</strong> (a graphical frontend) to generate nice HTML reports. The kernel also has a dedicated <strong>kcov</strong>, which is a low-level interface designed for fuzzing.</p><p>But simply running through is not enough. You need to force the code down that &quot;error path.&quot;</p><p><strong>Fault Injection</strong> does exactly that. The kernel&#39;s <code>fault-injection</code> framework allows you to do mischievous things, such as: &quot;make all <code>kmalloc()</code> calls fail on the 100th invocation and return NULL.&quot;</p><p>This is incredibly powerful.</p><p>The vast majority of code runs flawlessly when <code>malloc</code> succeeds, but might crash outright when <code>malloc</code> fails. Through fault injection, you can force code into those <strong>pesky and possibly buggy error code paths</strong>.</p><h3 id="⚫️-kernel-testing-self-discipline-kselftest-and-kunit" tabindex="-1">⚫️ Kernel Testing Self-Discipline: Kselftest and KUnit <a class="header-anchor" href="#⚫️-kernel-testing-self-discipline-kselftest-and-kunit" aria-label="Permalink to &quot;⚫️ Kernel Testing Self-Discipline: Kselftest and KUnit&quot;">​</a></h3><p>Kernel development is no longer a free-for-all; we have formal testing frameworks.</p><p>Located at <code>tools/testing/selftests</code> is <strong>kselftest</strong>. It primarily tests the kernel from user space through system calls and filesystem interfaces. Its output format follows <strong>TAP (Test Anything Protocol)</strong>.</p><p><strong>KUnit</strong>, on the other hand, is a &quot;unit testing&quot; framework inside the kernel. It allows you to directly test internal kernel functions during the kernel build or at runtime — without even booting a full operating system.</p><p>If you write drivers without writing KUnit tests, it is like driving without a seatbelt. You might be fine, but if something goes wrong, you are on your own.</p><h3 id="⚫️-fuzzing-firing-at-chaos" tabindex="-1">⚫️ Fuzzing: Firing at Chaos <a class="header-anchor" href="#⚫️-fuzzing-firing-at-chaos" aria-label="Permalink to &quot;⚫️ Fuzzing: Firing at Chaos&quot;">​</a></h3><p>If testing is using a ruler to measure your code, then <strong>Fuzzing</strong> is using a machine gun to spray it.</p><p>The principle is simple: feed the program massive amounts of random data, malformed data, and garbage data, and see if it crashes.</p><p>Sounds stupid? No, this is currently the most effective method for discovering security vulnerabilities.</p><ul><li><strong>syzkaller / syzbot</strong>: Google&#39;s system is currently the Linux kernel&#39;s nightmare generator (and also its guardian). It continuously sends random system call sequences to the kernel and has discovered countless hard-to-reproduce bugs.</li><li><strong>AFL (American Fuzzy Lop)</strong>: The classic coverage-guided fuzzing tool.</li><li><strong>Trinity</strong>: A fuzzer specifically targeting Linux system calls.</li></ul><p>If you want to go far in this domain, learning to use fuzzing tools is a required course.</p><h3 id="⚫️-logging-and-assertions-the-last-line-of-defense" tabindex="-1">⚫️ Logging and Assertions: The Last Line of Defense <a class="header-anchor" href="#⚫️-logging-and-assertions-the-last-line-of-defense" aria-label="Permalink to &quot;⚫️ Logging and Assertions: The Last Line of Defense&quot;">​</a></h3><p>Finally, we return to the most fundamental things: <strong>logging</strong> and <strong>assertions</strong>.</p><p>On modern Linux systems (systemd), stop using that relic <code>cat /var/log/messages</code>. You need to master <strong>journalctl</strong>.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 查看本次启动的内核日志</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">journalctl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -k</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -b</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 实时跟踪</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">journalctl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -k</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>This is not just convenient; it is structured data.</p><p>As for <strong>Assertions</strong> and the <strong>BUG()/WARN()</strong> macros:</p><p>We may have glossed over them in previous chapters, but we must emphasize them here. Adding <code>BUG_ON(condition)</code> or <code>WARN_ON(condition)</code> in your code is like leaving a note for yourself.</p><p><code>WARN_ON()</code> simply prints a warning and a call trace, and the system keeps running; <code>BUG_ON()</code> directly triggers a Panic.</p><p>They are your <strong>hypothesis tests</strong> on the system state. If <code>BUG_ON(ptr != NULL)</code> triggers, it means your assumption (that this cannot be null here) was wrong — spectacularly wrong — and the system is no longer trustworthy. It is better to die early.</p><hr><h3 id="⚫️-echoes-no-silver-bullet-but-a-sword" tabindex="-1">⚫️ Echoes: No Silver Bullet, But a Sword <a class="header-anchor" href="#⚫️-echoes-no-silver-bullet-but-a-sword" aria-label="Permalink to &quot;⚫️ Echoes: No Silver Bullet, But a Sword&quot;">​</a></h3><p>At this point, the core message of this chapter — and arguably this entire book — should be crystal clear.</p><p>Remember the question we opened with: <strong>how much can you really trust your tools?</strong></p><p>The answer: <strong>do not fully trust any single one, but use every single one.</strong></p><p>Fred Brooks has a famous quote that has been overused, but I am going to use it here anyway: <strong>No Silver Bullet</strong>.</p><p>No tool is a magic bullet. <code>-Wall</code> and <code>-Wextra</code> can only tell you about compile-time suspicions; KASAN can find memory errors, but it cannot catch logical deadlocks; kdump lets you see the crash scene, but it cannot prevent the crash from happening; fuzzing can stumble upon vulnerabilities, but it cannot prove your code is free of them.</p><p>A true debugging master is not someone who uses one hammer to hit every nail.</p><p>They hold a Swiss Army knife:</p><ul><li>Use compiler warnings as the first line of defense;</li><li>Use Sparse/Smatch for static checking;</li><li>Use KASAN/KCSAN for dynamic memory detection;</li><li>Use ftrace/trace-cmd for behavioral analysis;</li><li>Use KUnit/kselftest for automated testing;</li><li>Use kdump/crash for post-mortem autopsies.</li></ul><p>Our so-called &quot;better Makefile&quot; (you can find <code>ch3/printk_loglevels/Makefile</code> in this book&#39;s GitHub repository) is really an attempt to enforce this discipline. It has multiple targets corresponding to different checking tools. Do not be lazy — put them to use.</p><p>Although you have read this far, this is not the end.</p><p><strong>It is more like a new beginning.</strong></p><p>You are no longer the novice staring blankly at a Kernel Panic. You have the code, the tools, the theory, and more importantly, a <strong>methodology for &quot;how to think about low-level problems.&quot;</strong></p><p>Go forth, tinker, hunt down those bugs lurking deep in memory, and kill them.</p><p>That is our sincere hope!</p>`,85)])])}const g=t(i,[["render",a]]);export{d as __pageData,g as default};
