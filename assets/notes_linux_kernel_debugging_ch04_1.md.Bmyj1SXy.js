import{_ as s,o,c as r,a as t}from"./chunks/framework.8tiMeCK-.js";const g=JSON.parse('{"title":"第 4 章  透过针眼看大象——内核探针与动态追踪","description":"","frontmatter":{},"headers":[],"relativePath":"notes/linux_kernel_debugging/ch04_1.md","filePath":"notes/linux_kernel_debugging/ch04_1.md","lastUpdated":null}'),p={name:"notes/linux_kernel_debugging/ch04_1.md"};function n(a,e,i,d,l,c){return o(),r("div",null,[...e[0]||(e[0]=[t('<h1 id="第-4-章-透过针眼看大象——内核探针与动态追踪" tabindex="-1">第 4 章 透过针眼看大象——内核探针与动态追踪 <a class="header-anchor" href="#第-4-章-透过针眼看大象——内核探针与动态追踪" aria-label="Permalink to &quot;第 4 章  透过针眼看大象——内核探针与动态追踪&quot;">​</a></h1><p>我们面对着一团高速运转的乱麻。</p><p>当你试图理解一个复杂的系统——比如 Linux 内核——时，最直观的工具是「打印」。你在代码里塞满 <code>printf</code>，重新编译，运行，观察输出。这在用户空间开发中是家常便饭。但在内核里，这不仅是麻烦，简直是灾难。</p><p>为什么？因为内核<strong>没有</strong>它自己的运行环境。一旦内核代码停下来打印日志，整个系统——包括那个负责接收日志的打印系统——可能都暂停了。更糟糕的是，你想要观察的 bug 可能发生在并发竞争的微妙瞬间，或者发生在中断处理的深水区。如果你为了插入一行日志而重新编译并重启了内核，那个 bug 可能就像受惊的鱼一样游走了。</p><p>我们需要一种能力：<strong>在不打断系统运行、不重新编译内核的前提下，在任意内核函数的入口或出口插入「钩子」</strong>。当系统流经那个点时，钩子触发，记录下我们想知道的寄存器状态、参数值、返回值，然后像什么都没发生过一样让系统继续运行。</p><p>这就是本章的核心任务：构建这种「上帝视角」的观测能力。</p><p>但说实话，这条路并不平坦。老派的内核调试要求你编写 C 语言内核模块，手动注册名为 <code>kprobe</code> 的探针接口——这就是所谓的「静态探针」。它管用，但写起来极其繁琐，改个打印格式都要重新编译加载。而现代 Linux 内核提供了一种更优雅的方式：利用 <code>ftrace</code> 和 <code>tracepoints</code> 框架，直接通过用户空间的命令行工具或者脚本，动态地在内核里安插探针。这被称为「动态探针」，甚至 <code>eBPF</code> 这样革命性的技术也是建立在这些底层机制之上的。</p><p>在这一章，我们将从最原始的「静态探针」开始，亲手写代码、填结构体、处理寄存器；然后再逐步解放双手，体验动态追踪和 eBPF 带来的便利。只有先痛苦过，你才会真正理解现代工具为什么设计成那样。</p><p>现在，让我们先从那个最基础、最通用的机制开始：<strong>kprobes</strong>。</p><hr><h2 id="_4-1-kprobes-的基本原理-在函数上动刀" tabindex="-1">4.1 Kprobes 的基本原理：在函数上动刀 <a class="header-anchor" href="#_4-1-kprobes-的基本原理-在函数上动刀" aria-label="Permalink to &quot;4.1  Kprobes 的基本原理：在函数上动刀&quot;">​</a></h2><p>想象你是一个拥有特权的系统内部观察者。你想要监控内核空间里发生的每一次「文件打开」操作，不论它是哪个进程发起的，也不论它调用的是 <code>open()</code> 还是 <code>openat()</code>。</p><p>如果是传统的调试方法，你会去找到 <code>do_sys_open</code> 这个内核函数的源码，在第一行加个 <code>printk</code>，然后重新编译内核。但这太笨重了。Kprobes 提供了一种更轻量级的介入方式：它允许你动态地在内核函数的特定地址上「打入」一个断点。</p><p>你可以把它理解为<strong>内核调试界的瑞士军刀</strong>。</p><p>但这把刀有一个特殊的刀刃——它不仅能切入函数，还能在函数返回时截获数据。为了用好它，我们首先得搞清楚它到底长什么样。</p><h3 id="探针的三种状态" tabindex="-1">探针的三种状态 <a class="header-anchor" href="#探针的三种状态" aria-label="Permalink to &quot;探针的三种状态&quot;">​</a></h3><p>Kprobes 不仅仅是一个简单的「打断点」机制。为了让调试更灵活，它在执行流中为你提供了三个切入点。</p><p>假设我们要探测大名鼎鼎的内核函数 <code>do_sys_open()</code>（这是用户空间调用 <code>open(2)</code> 系统调用时最终落入的内核函数，详情请参见「系统调用在内核中的着陆点」一节）。通过 kprobes 基础设施，你可以挂接三种不同类型的处理程序：</p><ol><li><p><strong>Pre-handler（前置处理程序）</strong>： 在 <code>do_sys_open()</code> 函数的第一条指令执行<strong>之前</strong>触发。 这是最常用的钩子，通常用来打印函数参数（通过寄存器）、检查调用栈，或者在这一步就决定是否跳过该函数的执行。</p></li><li><p><strong>Post-handler（后置处理程序）</strong>： 在 <code>do_sys_open()</code> 函数的所有指令执行完毕，准备返回<strong>之后</strong>立即触发。 它适合用来检查函数执行后的副作用，或者验证状态是否被修改。</p></li><li><p><strong>Fault-handler（故障处理程序）</strong>： 这是一个保底机制。如果在执行 pre-handler 或 post-handler 的过程中，CPU 发生了异常（比如缺页错误，Page Fault），或者 kprobes 自身在单步执行指令时出了问题，这个处理程序就会被调用。 很多时候，仅仅是因为你的 handler 代码访问了一个非法的内存地址。没有 fault-handler 的话，内核可能会直接 panic；有了它，你至少有机会优雅地报错退出。</p></li></ol><p>这三个处理程序都是<strong>可选的</strong>。你可以只设 pre-handler，也可以三个全设，全看你的需求。</p><h3 id="kprobe-还是-kretprobe" tabindex="-1">Kprobe 还是 Kretprobe？ <a class="header-anchor" href="#kprobe-还是-kretprobe" aria-label="Permalink to &quot;Kprobe 还是 Kretprobe？&quot;">​</a></h3><p>除了上述的「常规探针」，Linux 内核还提供了一种特殊的探针，专门用来解决一个痛点：<strong>我想知道这个函数的返回值是多少？</strong></p><p>这就是 <strong>Kretprobe（Return Probe）</strong>。</p><p>为什么需要它？因为当函数执行完毕时，CPU 的指令指针已经回到了调用者那里。此时你再去检查常规的 post-handler，虽然能触发，但想拿到函数的返回值往往得去深挖堆栈或者寄存器，非常麻烦（而且与 CPU 架构强相关）。</p><p>Kretprobe 做了一件很聪明的事：它在函数入口处通过修改堆栈帧等方式，偷偷记录了返回地址，并在函数真正返回前拦截了控制流。这样，它就能轻而易举地把返回值塞给你。</p><p>这两种探针的注册 API 也是分开的：</p><ul><li><strong>Regular Kprobe</strong>：使用 <code>register_kprobe[s]()</code> / <code>unregister_kprobe[s]()</code></li><li><strong>Kretprobe</strong>：使用 <code>register_kretprobe[s]()</code> / <code>unregister_kretprobe[s]()</code></li></ul><p>我们先从最基础的 <code>kprobe</code> 开始，把 <code>kretprobe</code> 留到后面稍微进阶的部分再讲。</p><h3 id="手术刀的说明书-struct-kprobe" tabindex="-1">手术刀的说明书：<code>struct kprobe</code> <a class="header-anchor" href="#手术刀的说明书-struct-kprobe" aria-label="Permalink to &quot;手术刀的说明书：`struct kprobe`&quot;">​</a></h3><p>要在代码里真的用起来，你需要准备一个核心的数据结构：<code>struct kprobe</code>。把它想象成一张「手术清单」，内核拿着这张单子才知道要把刀子插在哪里，以及插进去之后干什么。</p><p>注册这个结构体的 API 长这样：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;linux/kprobes.h&gt;</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> register_kprobe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kprobe </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">p</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>为了不让你迷失在结构的海洋里，我们只关注这几个最关键的字段（其他的交给默认值就行）：</p><ul><li><p><strong><code>const char *symbol_name</code></strong>： 这就是你要「开刀」的那个内核函数的名字，比如 <code>&quot;do_sys_open&quot;</code>。 底层机制上，kprobes 框架会调用 <code>kallsyms_lookup()</code> 这类 API，把这个符号字符串解析成内核虚拟地址（KVA），并填入结构体内部的 <code>addr</code> 成员中。 <strong>注意</strong>：不是所有的函数都能被探测。有一些函数被列入了黑名单（比如 kprobes 自己的内部函数），探测它们会导致内核崩溃或死锁。我们会在后面的「Kprobes 的限制」一节里细说。</p></li><li><p><strong><code>kprobe_pre_handler_t pre_handler</code></strong>： 这是一个函数指针，指向你的 pre-handler 代码。它会在目标指令执行前被调用。</p></li><li><p><strong><code>kprobe_post_handler_t post_handler</code></strong>： 同理，这是 post-handler 的函数指针。</p></li><li><p><strong><code>kprobe_fault_handler_t fault_handler</code></strong>： 如果你的 pre 或 post handler 引发了异常，内核就会跳到这个函数里来。 <strong>关键点</strong>：这个函数的返回值很有讲究。返回 <code>0</code> 意味着「我没法处理，交给内核的默认异常处理机制吧」（这是通常情况）；返回 <code>1</code> 意味着「搞定了，错误已经修复，继续执行」（这很少见，需要你真的知道自己在做什么，比如手动修复了页表）。</p></li></ul><blockquote><p><strong>这里有个进阶玩法</strong></p><p><code>kprobe</code> 不仅能挂在函数开头，还能挂在函数<strong>内部的任意偏移处</strong>。你只需要设置 <code>struct kprobe</code> 的 <code>offset</code> 成员即可。 这在调试复杂的汇编代码块或者只关心函数后半段逻辑时非常有用。但在 CISC 架构（比如 x86）上，如果你偏移量设置不当，直接跳到了一条指令的中间，CPU 会直接报错。这就像在心脏跳动的一瞬间给它扎了一针——后果自负。</p></blockquote><p>用完了别忘了一件事：<strong>清理</strong>。</p><p>当你的模块卸载时，必须调用反注册函数把探针拿掉：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> unregister_kprobe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kprobe </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">p</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>如果你忘了这一步，后果很严重。下次任何代码流经那个地址时，内核会试图触发一个已经不存在的探针回调。结果？内核 Bug，甚至直接死机。这是一个典型的资源泄漏场景，只不过泄漏的不是内存，而是「控制流劫持点」。</p><h3 id="它是怎么做到的-黑盒之外的一瞥" tabindex="-1">它是怎么做到的？（黑盒之外的一瞥） <a class="header-anchor" href="#它是怎么做到的-黑盒之外的一瞥" aria-label="Permalink to &quot;它是怎么做到的？（黑盒之外的一瞥）&quot;">​</a></h3><p>你可能会好奇，把断点打进正在运行的内核里，到底是怎么实现的？是停掉所有 CPU 吗？还是某种黑魔法？</p><p>实话告诉你，这背后的实现细节（比如如何临时替换指令、如何处理单步执行、如何保证在多核环境下的正确性）极其复杂，涉及到架构相关的汇编技巧和内核底层的异常处理机制。</p><p>如果你真的对那些<strong>血淋淋的细节</strong>感兴趣，强烈建议去读内核官方文档。它解释了 kprobes 如何利用 CPU 的调试寄存器（如 x86 的 DR0-DR7）和断点指令（如 INT3）来实现这一切。</p><ul><li><strong>参考文档</strong>：<a href="https://www.kernel.org/doc/html/latest/trace/kprobes.html#concepts-kprobes-and-return-probes" target="_blank" rel="noreferrer">Concepts: Kprobes and Return Probes</a></li></ul><p>在这本书里，我们暂时把这些黑盒子关上，专注于<strong>如何使用</strong>这把强大的刀。</p><h3 id="为什么要这么麻烦-从静态到动态" tabindex="-1">为什么要这么麻烦？（从静态到动态） <a class="header-anchor" href="#为什么要这么麻烦-从静态到动态" aria-label="Permalink to &quot;为什么要这么麻烦？（从静态到动态）&quot;">​</a></h3><p>这一节我们要介绍的这种方法——编写内核模块，填充 <code>struct kprobe</code>，注册 handler ——被称为 <strong>Static Kprobes（静态探针）</strong>。</p><p>之所以叫「静态」，是因为每次你想探测一个新的函数，或者想改变一下日志的输出格式，你就得修改 C 代码，重新编译模块，卸载旧模块，加载新模块。</p><p>这听起来有点过时，对吧？在这种时代，还要为了看一眼日志而重新编译？现代 Linux 内核确实有更高级的玩法。</p><p>这就是 <strong>Dynamic Probing（动态探针）</strong>，或者叫 <strong>Kprobe-based Event Tracing</strong>。它深度结合了 <code>ftrace</code> 和 <code>tracepoints</code> 框架。你可以不需要写一行 C 代码，只需要通过 <code>debugfs</code> 写入一行字符串配置，或者使用 perf 工具、eBPF 脚本，就能在内核里动态地插桩。没有编译，没有重启，极其丝滑。</p><p>但在我们飞向那个现代化的世界之前，先让我们把「地基」打牢。接下来的几个演示（Demo），我们将从最原始的手写静态 kprobe 开始，一步步逼近那个文件打开的真相：</p><ul><li><strong>Demo 1</strong>：最简单的「硬编码」探针——写死探测 <code>open</code> 系统调用。</li><li><strong>Demo 2</strong>：稍微聪明点——通过模块参数指定要探测的函数名，不用改代码重新编译了。</li><li><strong>Demo 3</strong>：真正的干货——不仅仅是「看见」函数被调用，还要<strong>抓出文件名</strong>。</li></ul><p>准备好了吗？我们要开始写代码了。</p>',53)])])}const k=s(p,[["render",n]]);export{g as __pageData,k as default};
