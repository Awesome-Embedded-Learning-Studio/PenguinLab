import{_ as i,o as s,c as t,a}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"6.8 Multipath Routing","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch06_8.md","filePath":"en/notes/linux_kernel_networking/ch06_8.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_networking/ch06_8.md"};function o(h,e,l,r,p,d){return s(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="_6-8-multipath-routing" tabindex="-1">6.8 Multipath Routing <a class="header-anchor" href="#_6-8-multipath-routing" aria-label="Permalink to &quot;6.8 Multipath Routing&quot;">​</a></h1><p>Policy routing gives us the freedom to choose different routes, but it solves the problem of &quot;selecting a route based on criteria other than the destination (such as the source address).&quot;</p><p>Here is a more subtle requirement: suppose you are a network administrator with two broadband connections to the internet—China Telecom and China Unicom—with similar bandwidth. You don&#39;t want either link to sit idle, and you don&#39;t want to manually configure half of your computers to use Telecom and the other half to use Unicom. What you want is simple: <strong>spread the traffic out</strong>.</p><p>Or, suppose you are a server administrator with two NICs connected to the same switch, and you simply want to max out the 1Gbps bandwidth limit by &quot;using both pipes.&quot;</p><p>This is the problem that <strong>multipath routing</strong> solves.</p><hr><h3 id="what-is-multipath-routing" tabindex="-1">What is Multipath Routing <a class="header-anchor" href="#what-is-multipath-routing" aria-label="Permalink to &quot;What is Multipath Routing&quot;">​</a></h3><p>From the kernel&#39;s perspective, this sounds a bit counterintuitive. In previous chapters, we consistently emphasized that route lookups are &quot;exact matches&quot;—given a destination, return a single, definitive next hop. If there are two next hops, which one wins?</p><p><strong>All</strong> of them.</p><p>Multipath routing allows you to configure <strong>multiple next hops</strong> for a single route entry. You can express it like this:</p><blockquote><p>&quot;For packets destined for 192.168.1.10, you can go via 192.168.2.1, or you can go via 192.168.2.10. Figure it out, just make sure to distribute them.&quot;</p></blockquote><p>On the command line, it looks like this:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 简单的多路径：两条路平分秋色</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> default</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> scope</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> global</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nexthop</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> eth0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nexthop</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> eth1</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 加权的多路径：第二条路承载更多流量</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.1.10</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nexthop</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> via</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.2.1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> weight</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">                          nexthop</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> via</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.2.10</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> weight</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 5</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>You can think of each <code>nexthop</code> as a lane. The second command above means: I have 8 units of traffic volume, 3 units go to the first lane, and 5 units go to the second lane.</p><p>But the kernel doesn&#39;t just &quot;figure it out&quot;—it needs a precise algorithm to decide which specific path each individual packet should take. This mechanism is hidden inside <code>fib_info</code>.</p><hr><h3 id="kernel-representation-fib-info-and-fib-nh" tabindex="-1">Kernel Representation: fib_info and fib_nh <a class="header-anchor" href="#kernel-representation-fib-info-and-fib-nh" aria-label="Permalink to &quot;Kernel Representation: fib_info and fib_nh&quot;">​</a></h3><p>In the IPv4 FIB (Forwarding Information Base) subsystem, the <code>fib_info</code> structure is the core carrier of routing information. As we discussed earlier, a normal route entry has a single <code>fib_nh</code> (FIB Nexthop), but in a multipath scenario, this field becomes an array.</p><p>There is a subtle distinction here:</p><ul><li><strong>Single path</strong>: <code>fib_info</code> points to a single <code>fib_nh</code> structure.</li><li><strong>Multipath</strong>: <code>fib_info</code> contains a <code>fib_nh</code> array, whose length is specified by the <code>fib_nhs</code> member.</li></ul><p>When you execute the <code>ip route add</code> command above with two <code>nexthop</code> options, the kernel creates a <code>fib_info</code> object, sets its <code>fib_nhs</code> to 2, and packs two <code>fib_nh</code> objects into the <code>fib_nh</code> array.</p><p>At this point, each <code>fib_nh</code> object also has a key field: <strong><code>nh_weight</code></strong>.</p><p>This field is the &quot;weight&quot; we just mentioned. If you explicitly specify <code>weight 3</code> in the <code>ip route</code> command, the kernel fills it in; if you don&#39;t, the <code>fib_create_info()</code> method defaults it to 1.</p><hr><h3 id="making-the-decision-fib-select-multipath" tabindex="-1">Making the Decision: fib_select_multipath() <a class="header-anchor" href="#making-the-decision-fib-select-multipath" aria-label="Permalink to &quot;Making the Decision: fib_select_multipath()&quot;">​</a></h3><p>The routing table determines &quot;which paths are available,&quot; but when a packet arrives, we must pick exactly <strong>one</strong> path to take. The black box that makes this decision is called <code>fib_select_multipath()</code>.</p><p>This function is called in two key places:</p><ol><li><strong>Tx path</strong>: Inside <code>__ip_route_output_key()</code>. This is the path taken when locally generated packets are ready to go out.</li><li><strong>Rx path</strong>: Inside <code>ip_mkroute_input()</code>. This is the path taken when forwarding packets.</li></ol><p>But there is a conditional check here. In the Tx path code, you will see this logic:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rtable </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__ip_route_output_key</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">net</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> flowi4 </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">fl4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#ifdef</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CONFIG_IP_ROUTE_MULTIPATH</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 只有当存在多条路径，并且没有强制指定输出设备时，才做选择</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (res.fi-&gt;fib_nhs </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> fl4-&gt;flowi4_oif </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        fib_select_multipath</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">res);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    else</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#endif</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>Why is there a check for <code>flowi4_oif == 0</code>?</p><p>Because when an application sends data, it can explicitly specify &quot;must go out via eth0&quot; through <code>bind()</code> or ancillary messages of <code>sendmsg()</code>. Once the user makes this hard requirement, multipath load balancing becomes meaningless—even if the other path is completely idle, the packet must take this one. In this case, the kernel skips <code>fib_select_multipath()</code> and directly uses the user-specified path.</p><p>In the Rx path, this restriction typically doesn&#39;t exist, because forwarded packets don&#39;t carry a preset instruction like &quot;please go out via eth1.&quot;</p><h4 id="selection-algorithm" tabindex="-1">Selection Algorithm <a class="header-anchor" href="#selection-algorithm" aria-label="Permalink to &quot;Selection Algorithm&quot;">​</a></h4><p>The internal implementation of <code>fib_select_multipath()</code> is actually quite interesting. It doesn&#39;t simply do round-robin (1, 2, 1, 2...) as our intuition might suggest. It introduces <strong>randomness</strong>.</p><p>This randomness is not for unpredictability, but for <strong>weighted fairness</strong>.</p><p>It uses the system time (<code>jiffies</code>) as a seed for hash computation. For each incoming packet, the kernel calculates a hash value (based on source IP, destination IP, source port, destination port, etc.), combines it with the <code>nh_weight</code> of each path, and determines which <code>fib_nh</code> this packet should go to.</p><p>The design goals of this algorithm are:</p><ul><li>Packets belonging to <strong>the same flow</strong> (identical 5-tuple) should take the same path. Otherwise, TCP out-of-order delivery would cause a sharp performance drop.</li><li><strong>Different flows</strong> should be distributed across different paths according to their weight ratios.</li></ul><p>The最终选定的路径索引，会被保存到 <code>fib_result</code> 结构体的 <code>nh_sel</code> (Nexthop Selector) field, and subsequent forwarding logic uses this index to look up the <code>fib_nh</code> array.</p><hr><h3 id="where-the-code-lives" tabindex="-1">Where the Code Lives <a class="header-anchor" href="#where-the-code-lives" aria-label="Permalink to &quot;Where the Code Lives&quot;">​</a></h3><p>If you go digging through the source code looking for a file called <code>multipath.c</code>, you will be disappointed.</p><p>For multicast routing, the kernel has a dedicated <code>net/ipv4/ipmr.c</code> module, nice and clean. But for multipath routing, the code is like a &quot;ghost&quot;—scattered across various corners of the generic routing code, wrapped in numerous <code>#ifdef CONFIG_IP_ROUTE_MULTIPATH</code> conditionals.</p><p>This shows that in the eyes of the kernel designers, multipath routing is not an independent subsystem, but rather <strong>an enhancement feature of the route lookup logic</strong>.</p><p>This approach has a historical side effect. If you look at older kernel code (before 2007), you will find that IPv4 once had a dedicated &quot;multipath route cache.&quot; This cache was removed in the 2.6.23 kernel.</p><p>Be careful not to confuse this: <strong>removal of the multipath route cache</strong> ≠ <strong>removal of the route cache</strong>.</p><ul><li>The multipath cache was removed in 2007 because, as an experimental feature, it never worked very well.</li><li>The real route cache was removed in 2012 in kernel 3.6, in order to solve the cache synchronization overhead problem on multi-core CPUs.</li></ul><p>The current multipath implementation is completed directly during the FIB lookup phase, with no additional cache layer, which actually makes the logic clearer.</p><hr><h3 id="configuration-switch-config-ip-route-multipath" tabindex="-1">Configuration Switch: CONFIG_IP_ROUTE_MULTIPATH <a class="header-anchor" href="#configuration-switch-config-ip-route-multipath" aria-label="Permalink to &quot;Configuration Switch: CONFIG_IP_ROUTE_MULTIPATH&quot;">​</a></h3><p>Finally, remember to check your kernel configuration.</p><p>To make all of this work, your kernel configuration must include <code>CONFIG_IP_ROUTE_MULTIPATH=y</code>. Many distributions may disable this feature or build it as a module to streamline the kernel. If you find that adding the <code>nexthop</code> command doesn&#39;t take effect, check <code>make menuconfig</code> first.</p>`,53)])])}const k=i(n,[["render",o]]);export{u as __pageData,k as default};
