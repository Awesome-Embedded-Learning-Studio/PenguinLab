import{_ as s,o as i,c as a,a as n}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"3.4 Remaining printk Miscellanea and Ultimate Debugging Techniques","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch03_5.md","filePath":"en/notes/linux_kernel_debugging/ch03_5.md","lastUpdated":null}'),t={name:"en/notes/linux_kernel_debugging/ch03_5.md"};function r(o,e,l,p,h,d){return i(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="_3-4-remaining-printk-miscellanea-and-ultimate-debugging-techniques" tabindex="-1">3.4 Remaining printk Miscellanea and Ultimate Debugging Techniques <a class="header-anchor" href="#_3-4-remaining-printk-miscellanea-and-ultimate-debugging-techniques" aria-label="Permalink to &quot;3.4 Remaining printk Miscellanea and Ultimate Debugging Techniques&quot;">​</a></h1><p>At this point, we&#39;ve actually covered 90% of your day-to-day debugging scenarios. From the most basic <code>printk</code>, to the leveled <code>pr_debug</code>, to the rate-limiting <code>ratelimit</code> that prevents log flooding, and that god-like <strong>Dynamic Debug</strong>. Your toolbox should now contain a pretty solid set of gear.</p><p>But the kernel world is never flat. There are edge cases—what about when the kernel just booted and the console isn&#39;t ready yet? What about when the system has completely hung and even the keyboard is unresponsive? Or what if you want to inject markers into the kernel log from userspace?</p><p>Then there are a few scattered but extremely useful tricks, like the &quot;print-once&quot; macros, or tools for elegantly dumping memory blocks. If we don&#39;t tie up these loose ends, your debugging skill tree will always be missing a few key nodes.</p><p>In this section, we&#39;ll round up these scattered &quot;ultimate moves.&quot;</p><hr><h2 id="_3-4-1-shouting-before-console-init-—-early-printk" tabindex="-1">3.4.1 Shouting Before Console Init — Early printk <a class="header-anchor" href="#_3-4-1-shouting-before-console-init-—-early-printk" aria-label="Permalink to &quot;3.4.1 Shouting Before Console Init — Early printk&quot;">​</a></h2><p>You&#39;ve definitely encountered this moment: the kernel boots for a few lines, the screen suddenly goes black, and the serial port produces no output. You know perfectly well the problem lies in very early initialization code—maybe a driver probe failed, or something went wrong with memory mapping.</p><p>But the problem is, the real console isn&#39;t initialized yet. You issued <code>printk</code>, but nobody is listening. These messages sit quietly in the ring buffer in memory, and you have no way to see them.</p><p>It&#39;s like an engine failure before takeoff, before the black box even started recording.</p><h3 id="what-is-a-console" tabindex="-1">What Is a Console? <a class="header-anchor" href="#what-is-a-console" aria-label="Permalink to &quot;What Is a Console?&quot;">​</a></h3><p>Before solving this problem, let&#39;s clarify what a &quot;console&quot; actually is.</p><p>In traditional Linux terminology, the console is the terminal where the kernel directly outputs,也就是那个 <code>/dev/console</code>. In the era before graphical interfaces took over everything, this was the plain-text window where the superuser logged in.</p><p>Interestingly, Linux can define multiple types of &quot;consoles&quot;:</p><ul><li><strong>TTY</strong>: Such as <code>/dev/ttyS0</code> serial devices.</li><li><strong>VGA text mode</strong>: The old-school black-and-white-blue monitors.</li><li><strong>Framebuffer</strong>: Graphical consoles.</li><li><strong>USB serial</strong>: Common on embedded development boards.</li></ul><p>When you use a USB-to-TTL serial cable to connect a Raspberry Pi to your laptop and connect using <code>minicom</code> or <code>screen</code>, what you&#39;re seeing is the <code>tty</code> device. This is the developer&#39;s closest friend.</p><h3 id="the-early-printing-dilemma" tabindex="-1">The Early Printing Dilemma <a class="header-anchor" href="#the-early-printing-dilemma" aria-label="Permalink to &quot;The Early Printing Dilemma&quot;">​</a></h3><p>The problem boils down to &quot;timing.&quot;</p><p>In the very early stages of kernel boot, the kernel itself is still an infant—it hasn&#39;t even learned to walk, let alone operate a GPU or complex serial drivers. If the kernel crashes at this point, a normal <code>printk</code> simply cannot reach the physical device (because the driver hasn&#39;t started yet).</p><p>You can only watch helplessly as the system freezes, while the only clues—those crucial early error messages—remain locked in a pipeline that isn&#39;t connected yet.</p><h3 id="the-solution-early-printk" tabindex="-1">The Solution: early_printk() <a class="header-anchor" href="#the-solution-early-printk" aria-label="Permalink to &quot;The Solution: early_printk()&quot;">​</a></h3><p>This calls for some unconventional measures. The kernel community provides a mechanism called <strong>early printk</strong>.</p><p>The principle sounds very geeky: without relying on any complex drivers, it uses the lowest-level, most primitive hardware operations to &quot;hammer&quot; characters onto the serial line one by one (or write directly to video memory). This is called bit-banging. This is why it&#39;s called <code>early_</code>—because it&#39;s not only fast, but extremely primitive, requiring very little infrastructure.</p><p>To enable it, you need to follow three steps:</p><h3 id="_1-configure-the-kernel-one-time" tabindex="-1">1. Configure the Kernel (One-Time) <a class="header-anchor" href="#_1-configure-the-kernel-one-time" aria-label="Permalink to &quot;1. Configure the Kernel (One-Time)&quot;">​</a></h3><p>This is architecture-dependent.</p><ul><li><strong>x86</strong>: Enable <code>CONFIG_EARLY_PRINTK=y</code>. The path is usually under <code>Kernel hacking</code> -&gt; <code>x86 Debugging</code> -&gt; <code>Early printk</code>. The documentation will typically warn you: this thing doesn&#39;t use proper formatting and will interfere with normal logging, so don&#39;t enable it unless you&#39;re debugging boot issues.</li><li><strong>ARM (32-bit)</strong>: The option is called <code>CONFIG_DEBUG_LL</code>, located at <code>Kernel hacking</code> -&gt; <code>Kernel low-level debugging functions</code>. Entering the submenu, you also need to select the specific debug port (whether it&#39;s EmbeddedICE DCC or UART).</li></ul><h3 id="_2-boot-parameters" tabindex="-1">2. Boot Parameters <a class="header-anchor" href="#_2-boot-parameters" aria-label="Permalink to &quot;2. Boot Parameters&quot;">​</a></h3><p>Compiling it in isn&#39;t enough; you need to tell the kernel to use it at boot time. Add this to your bootargs:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>earlyprintk=serial,ttyS0,115200</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Or the simpler x86 VGA:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>earlyprintk=vga</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The kernel documentation has a ton of parameter combinations (like <code>sclp</code>, <code>xen</code>, <code>dbgp</code>), but the core idea is: specify a device, specify a baud rate, and tell the kernel &quot;spit the early characters here.&quot;</p><p>There&#39;s also a handy parameter called <code>keep</code>. With this added, even after the real console (like the normal serial driver) takes over, the early <code>early_printk</code> output won&#39;t be turned off. This is extremely useful for diagnosing &quot;what happened at the exact moment of handover.&quot;</p><h3 id="_3-calling-it-from-code" tabindex="-1">3. Calling It from Code <a class="header-anchor" href="#_3-calling-it-from-code" aria-label="Permalink to &quot;3. Calling It from Code&quot;">​</a></h3><p>The API is very intuitive, called <code>early_printk()</code>, looking exactly like <code>printf</code>.</p><p>There are ready-made examples in the kernel code. For instance, in <code>kernel/events/core.c</code>, if the interrupt handling time for performance sampling is too long, it will use <code>early_printk</code> to issue a warning:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// kernel/events/core.c</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">irq_work_queue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">perf_duration_work</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    early_printk</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;perf: interrupt took too long (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%lld</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &gt; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%lld</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">), lowering &quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">                 &quot;kernel.perf_event_max_sample_rate to </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%d\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                 __report_avg, __report_allowed,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                 sysctl_perf_event_sample_rate);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>This trick is an absolute lifesaver when debugging those &quot;dies after two steps&quot; bugs.</p><hr><h2 id="_3-4-2-tagging-printk-messages" tabindex="-1">3.4.2 Tagging printk Messages <a class="header-anchor" href="#_3-4-2-tagging-printk-messages" aria-label="Permalink to &quot;3.4.2 Tagging printk Messages&quot;">​</a></h2><p>Some errors have a strong sense of identity. If the firmware is written wrong, you should blame the firmware vendor; if the hardware is dead, you should tell people to find the hardware engineer; if I don&#39;t want you using a certain API anymore, I need to tell you &quot;this thing is deprecated.&quot;</p><p>The kernel provides a set of macros specifically for adding these &quot;official prefixes&quot; to printk.</p><p>They are defined in <code>include/linux/printk.h</code>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FW_BUG</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;[Firmware Bug]: &quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FW_WARN</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">     &quot;[Firmware Warn]: &quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FW_INFO</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">     &quot;[Firmware Info]: &quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/*</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * HW_ERR</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * Add this to a message for hardware errors, so that user</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * can report it to hardware vendor instead of LKML or software</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * vendor.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> HW_ERR</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &quot;[Hardware Error]: &quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/*</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * DEPRECATED</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * Add this to a message whenever you want to warn userspace</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * about the use of a deprecated aspect of an API so they can stop using it</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DEPRECATED</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;[Deprecated]: &quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br></div></div><p>Look at the comment for <code>HW_ERR</code>—you can feel the maintainer&#39;s exasperation between the lines: &quot;Don&#39;t send this to the Linux Kernel Mailing List (LKML), go argue with the hardware vendor.&quot;</p><p>Using them is very straightforward, like this:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// drivers/acpi/thermal.c</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (tmp </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2732</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    pr_warn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(FW_BUG </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Invalid critical threshold (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%llu</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">)</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, tmp);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>The output looks like this:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[ 123.456] [Firmware Bug]: Invalid critical threshold (2730)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This kind of prefixed log is especially easy to identify in automated log analysis systems—you can tell at a glance who is responsible.</p><hr><h2 id="_3-4-3-saying-it-only-once-—-printk-once" tabindex="-1">3.4.3 Saying It Only Once — printk_once() <a class="header-anchor" href="#_3-4-3-saying-it-only-once-—-printk-once" aria-label="Permalink to &quot;3.4.3 Saying It Only Once — printk_once()&quot;">​</a></h2><p>Some warnings are very important, but if you print them in a high-frequency interrupt or loop, your console will be instantly flooded, and system performance will plummet.</p><p>What you want to say is: &quot;Hey, something&#39;s wrong here. I&#39;m telling you once, remember it, but don&#39;t make me keep nagging you.&quot;</p><p>This is when you use <code>printk_once()</code>.</p><p>It guarantees that this message will only be printed once during the entire system runtime. No matter if you call it ten thousand times, it only spits out the first one.</p><p>Naturally, that whole family of <code>pr_*</code> macros all have corresponding <code>_once</code> versions: <code>pr_err_once()</code>, <code>pr_warn_once()</code>, <code>pr_info_once()</code>, and so on.</p><p>Let&#39;s look at an example. The TCP stack encounters a SYN packet that shouldn&#39;t be there—this is definitely an anomaly, but you can&#39;t bring down the system because of it:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// net/ipv4/tcp.c</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">unlikely</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">TCP_SKB_CB</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">tcp_flags </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> TCPHDR_SYN</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    pr_err_once</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">: found a SYN, please report !</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, __func__);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>If you&#39;re writing a driver, remember to use <code>dev_*_once()</code>.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// drivers/rtc/rtc-mxc_v2.c</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!--</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">timeout) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    dev_err_once</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(dev, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SRTC_LPSCLR stuck! Check your hw.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>There&#39;s also another ultimate move: <code>dev_WARN_ONCE()</code>. This macro not only prints once, but also conveniently dumps a stack trace for you, just like <code>WARN_ON()</code>. This thing is powerful—use it with caution.</p><hr><h2 id="_3-4-4-shouting-into-the-kernel-from-userspace-—-dev-kmsg" tabindex="-1">3.4.4 Shouting Into the Kernel From Userspace — /dev/kmsg <a class="header-anchor" href="#_3-4-4-shouting-into-the-kernel-from-userspace-—-dev-kmsg" aria-label="Permalink to &quot;3.4.4 Shouting Into the Kernel From Userspace — /dev/kmsg&quot;">​</a></h2><p>Sometimes we&#39;re writing automated test scripts. The script runs in userspace, controlling the test flow of a kernel module.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>echo &quot;test data 123&quot; &gt; /dev/mydevnode</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>At this point, you&#39;ll see a bunch of output from the kernel module in <code>dmesg</code>. But you have a problem: in this massive pile of logs, which line was triggered by which test case?</p><p>If only you could insert a bookmark in the logs. For example, when a test case starts, write a line into the kernel log: <code>my_test_script: start testcase 1</code>.</p><p>Actually, you don&#39;t need to write kernel code to do this. The kernel provides a special device file: <code>/dev/kmsg</code>.</p><p>You can directly use <code>echo</code> to write things into it:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;echo </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">my_test_script: start testcase 1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &gt; /dev/kmsg&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Messages written this way are treated as KERN_WARNING (level 4) by default. If you want to specify a level, like KERN_INFO (level 6), add angle brackets at the front:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;echo </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&lt;6&gt;my_test_script: start testcase 1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &gt; /dev/kmsg&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Then in <code>dmesg</code>, you&#39;ll see:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>user  :info  : [33561.862960] my_test_script: start testcase 1</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Here&#39;s an interesting point: once a message enters <code>/dev/kmsg</code>, it looks exactly the same as a <code>printk</code> generated by the kernel itself. You can&#39;t tell who is whose parent.</p><p>So, <strong>we recommend adding a conspicuous prefix</strong> (like <code>@US_TEST@</code>), so that when you&#39;re scrolling through logs and grepping, you can spot these markers you injected at a glance.</p><hr><h2 id="_3-4-5-elegantly-dumping-memory-—-print-hex-dump-bytes" tabindex="-1">3.4.5 Elegantly Dumping Memory — print_hex_dump_bytes() <a class="header-anchor" href="#_3-4-5-elegantly-dumping-memory-—-print-hex-dump-bytes" aria-label="Permalink to &quot;3.4.5 Elegantly Dumping Memory — print_hex_dump_bytes()&quot;">​</a></h2><p>One last common requirement: you want to see the contents of a certain data structure or network packet, but you don&#39;t want to write that disgusting loop yourself—what <code>for</code> loop to print hex, whether a character is printable, calculating alignment for half a day...</p><p>Don&#39;t waste your time. The kernel has it ready for you.</p><p><code>print_hex_dump_bytes()</code> is specifically for this.</p><p>Its definition is quite long, but the parameters are explained clearly:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/**</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * print_hex_dump_bytes - shorthand form of print_hex_dump() with default params</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@prefix_str:</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> string to prefix each line with; caller supplies trailing spaces for alignment if desired</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@prefix_type:</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> controls whether prefix of an offset, address, or none is printed</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> *               (%DUMP_PREFIX_OFFSET, %DUMP_PREFIX_ADDRESS, %DUMP_PREFIX_NONE)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@buf:</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> data blob to dump</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@len:</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> number of bytes in the @buf</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> print_hex_dump_bytes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">prefix_str</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">prefix_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">buf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">len</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        print_hex_dump_debug</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(prefix_str, prefix_type, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">16</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, buf, len, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>This macro is essentially a wrapper around <code>print_hex_dump_debug()</code>. Notice anything? It uses the <code>_debug</code> suffix.</p><p><strong>What does this mean?</strong> It means it&#39;s hooked into <strong>Dynamic Debug</strong>! The more you use it, the more you&#39;ll appreciate the power of Dynamic Debug—these hex dumps are normally turned off, don&#39;t affect performance, and you just need to enable them in <code>/sys/kernel/debug/dynamic_debug/control</code> at the critical moment.</p><p>Let&#39;s look at an actual usage in a wireless network driver:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// drivers/net/wireless/ath/ath6kl/debug.c</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ath6kl_dbg_dump</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">enum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ATH6K_DEBUG_MASK </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">mask</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">             const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">msg</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">prefix</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">             const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">buf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">size_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> len</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (debug_mask </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mask) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (msg)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">            ath6kl_dbg</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(mask, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, msg);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        print_hex_dump_bytes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(prefix,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            DUMP_PREFIX_OFFSET, buf, len);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    [...]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p>When this code runs, the output looks roughly like this:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>00000000: 00 11 22 33 44 55 66 77 88 99 aa bb cc dd ee ff  .&quot;3DUw........</span></span>
<span class="line"><span>00000010: 01 02 03 04 05 06 07 08 09 0a 0b 0c 0d 0e 0f 10  ................</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Clear, aligned, and with ASCII display. When debugging network packets or DMA buffers, this is your eyes.</p><hr><h2 id="_3-4-6-survival-in-desperate-situations-what-to-do-when-the-system-crashes" tabindex="-1">3.4.6 Survival in Desperate Situations: What to Do When the System Crashes? <a class="header-anchor" href="#_3-4-6-survival-in-desperate-situations-what-to-do-when-the-system-crashes" aria-label="Permalink to &quot;3.4.6 Survival in Desperate Situations: What to Do When the System Crashes?&quot;">​</a></h2><p>If the system has completely hung, can&#39;t even output <code>dmesg</code>, and can&#39;t even invoke <code>printk</code>, what do we do?</p><p>Here are a few &quot;hardcore&quot; backup plans.</p><h3 id="warm-reset-and-bootloader-snooping" tabindex="-1">Warm Reset and Bootloader Snooping <a class="header-anchor" href="#warm-reset-and-bootloader-snooping" aria-label="Permalink to &quot;Warm Reset and Bootloader Snooping&quot;">​</a></h3><p>Some advanced techniques involve doing a &quot;warm reset&quot; after the system crashes.</p><p>A so-called Warm Reset means restarting the CPU but keeping the RAM contents powered. It&#39;s like when your computer freezes, but you haven&#39;t pulled the plug—the data in the RAM sticks is still there.</p><p>Some boards have dedicated soft reset buttons, or you can trigger this via Magic SysRq (which we&#39;ll cover in a dedicated chapter later).</p><p>At this point, if you can quickly get into the Bootloader&#39;s (like U-Boot) command line, you can use the Bootloader&#39;s memory display command (like <code>md</code> - memory display) to read the physical address where the kernel log buffer was located.</p><p><strong>There&#39;s a catch here</strong>: The <code>__log_buf</code> address you find in <code>/proc/kallsyms</code> is a <strong>virtual address</strong>, and the Bootloader only understands physical addresses. You need to consult the chip manual to translate this virtual address into a physical address.</p><p>Once translated successfully, a quick sweep with the <code>md</code> command will let you see the kernel logs from the last few seconds before the crash. Although this trick is obscure, in situations where &quot;reproducing the scene is too hard,&quot; it can save your life.</p><h3 id="led-blink-debugging" tabindex="-1">LED Blink Debugging <a class="header-anchor" href="#led-blink-debugging" aria-label="Permalink to &quot;LED Blink Debugging&quot;">​</a></h3><p>Even more extreme, what if the board doesn&#39;t even have a serial port connected, or even the Bootloader has crashed?</p><p>Return to the most primitive method: <strong>GPIO LED toggling</strong>.</p><p>This is called the &quot;poor man&#39;s printf.&quot; You pull a GPIO pin high and low on critical code paths, and the external LED will blink.</p><ul><li>Blink twice during the initialization phase?</li><li>Blink three times in the interrupt?</li><li>Stay solid in a dead loop?</li></ul><p>This is exactly how the Raspberry Pi does it—if boot fails, the red LED blinks a specific number of times to represent the error code.</p><p>Some people even connect OLED screens via I2C and draw error messages directly on the screen. Although it sounds crude, on black-box devices with no output, this is often the only clue.</p><h3 id="network-console" tabindex="-1">Network Console <a class="header-anchor" href="#network-console" aria-label="Permalink to &quot;Network Console&quot;">​</a></h3><p>Finally, we have to mention <strong>netconsole</strong>.</p><p>This mechanism allows the kernel&#39;s <code>printk</code> to be thrown directly to another machine on the LAN via UDP packets.</p><p>Even if the local disk is dead or the GPU has crashed, as long as the network card still works, logs can be sent in real-time to a remote server for safekeeping. This is the ultimate tool for debugging server kernel crashes. We&#39;ll dive into its detailed configuration in a later chapter.</p><hr><h2 id="chapter-echoes" tabindex="-1">Chapter Echoes <a class="header-anchor" href="#chapter-echoes" aria-label="Permalink to &quot;Chapter Echoes&quot;">​</a></h2><p>We went deep in this chapter, from the simplest instrumentation printing all the way to memory forensics after a system crash.</p><p>If you look back, you&#39;ll notice a clear path of mental model upgrades:</p><p>At first, you just treated <code>printk</code> as a higher-level <code>printf</code>, used to confirm &quot;did the code reach this point.&quot;</p><p>Then, you started to realize it&#39;s part of the kernel&#39;s logging system, and began to care about <strong>log levels</strong> and the <strong>ring buffer</strong>.</p><p>Later, when you encountered performance issues in production environments, you learned about <strong>rate limiting</strong> and <strong>dynamic debugging</strong>, understanding how to toggle diagnostic information on demand without rebooting the system.</p><p>Finally, in extreme crash scenarios, you learned <strong>Early printk</strong> and <strong>memory dumps</strong>—these techniques are no longer just &quot;looking at logs,&quot; but directly conversing with the lowest levels of the hardware.</p><p>This isn&#39;t just an accumulation of tools, but a shift in perspective. Now, you&#39;re no longer afraid of that dark console window.</p><p>With this set of instrumentation and printing skills under our belt, we can confidently move on to the next, grander topic. In the next chapter, we&#39;ll introduce the kernel&#39;s <strong>Kprobes framework</strong>—a magic trick even more powerful than instrumentation, allowing you to dynamically &quot;hook&quot; any kernel function at runtime.</p><p>When that time comes, you will truly possess the ability to &quot;see through&quot; the kernel&#39;s execution trajectory.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: Understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: Understanding&quot;">​</a></h3><p><strong>Question</strong>: In Linux kernel programming, why can&#39;t we directly use the standard C library&#39;s <code>printf()</code> function, and why must we use <code>printk()</code>? Briefly describe the core differences between the two in terms of runtime environment and availability.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: The Linux kernel does not depend on the standard C library (libc); <code>printf()</code> is a userspace library function. <code>printk()</code> is a native kernel API that can be safely called in any context (such as Interrupt Context or while holding a spinlock), and it writes directly to the kernel ring buffer.</p><p><strong>Analysis</strong>: This knowledge point tests your understanding of the boundary between the kernel and userspace. <code>printf</code> relies on extensive userspace library support that simply doesn&#39;t exist in the kernel environment. <code>printk</code> not only implements formatted output but also handles kernel-specific log levels and buffering mechanisms, and is designed to be safe in any context (SMP-safe)—something a normal <code>printf</code> cannot guarantee.</p></details><h3 id="exercise-2-application" tabindex="-1">Exercise 2: Application <a class="header-anchor" href="#exercise-2-application" aria-label="Permalink to &quot;Exercise 2: Application&quot;">​</a></h3><p><strong>Question</strong>: Suppose you are writing a Network Device Driver and need to print debug information in the <code>open()</code> function. To make the logs more identifiable and automatically include device information, which of the following macros would you choose? Explain your reasoning.</p><p>A. <code>printk(KERN_DEBUG &quot;device opened\\n&quot;)</code> B. <code>pr_info(&quot;device opened\\n&quot;)</code> C. <code>dev_dbg(&amp;pdev-&gt;dev, &quot;device opened\\n&quot;)</code></p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: C</p><p><strong>Analysis</strong>: When writing device drivers, the best practice is to use <code>dev_dbg()</code> (or other <code>dev_*</code> macros like <code>dev_info()</code>). Although <code>pr_info</code> is more convenient than <code>printk</code>, the <code>dev_*</code> family of macros takes a <code>struct device *</code> pointer as its first argument. This means the printed logs will automatically include specific device information (such as device name, bus number, etc.), which is extremely useful for distinguishing logs from multiple identical devices in a system. Additionally, <code>dev_dbg</code> is much more powerful when paired with Dynamic Debug.</p></details><h3 id="exercise-3-application" tabindex="-1">Exercise 3: Application <a class="header-anchor" href="#exercise-3-application" aria-label="Permalink to &quot;Exercise 3: Application&quot;">​</a></h3><p><strong>Question</strong>: You need to print a kernel-space pointer and the contents of a 16-byte data buffer in a driver. For security reasons, write the correct <code>printk</code> code (assuming the buffer pointer is <code>buf</code>).</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: printk(&quot;Pointer: %pK, Data: %*ph\\n&quot;, ptr, 16, buf);</p><p><strong>Analysis</strong>: This tests your mastery of kernel format specifiers. 1. To prevent security vulnerabilities caused by kernel pointer leaks, you should use <code>%pK</code> (which prints a hashed value) instead of <code>%px</code> (the real address) when printing pointers, unless you are in a debugging phase. 2. For printing a small hex buffer, <code>%*ph</code> is the most efficient approach, where <code>*</code> represents the length, <code>16</code> is the length value, <code>h</code> stands for little-endian hex bytes, and <code>buf</code> is the data source.</p></details><h3 id="exercise-4-thinking" tabindex="-1">Exercise 4: Thinking <a class="header-anchor" href="#exercise-4-thinking" aria-label="Permalink to &quot;Exercise 4: Thinking&quot;">​</a></h3><p><strong>Question</strong>: Suppose you have a high-frequency interrupt handler in your kernel module, and there&#39;s a line of <code>printk(KERN_INFO &quot;IRQ fired\\n&quot;);</code> inside it. Analyze the two main negative impacts this might cause, and provide at least one solution.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Negative impacts: 1. Performance degradation/livelock: High-frequency printing causes the CPU to spend excessive time on I/O, potentially leading to slow system response or deadlocks. 2. Log flooding: Useful information gets buried, and it easily causes the log buffer to overwrite old data too quickly (syslog explosion). Solution: Use <code>pr_info_ratelimited()</code> or remove/replace it with <code>trace_printk()</code> on non-critical paths.</p><p><strong>Analysis</strong>: This is a comprehensive engineering problem. Although printk is synchronous, executing it frequently in Interrupt Context will severely slow down the system. The question tests your awareness of the costs of <code>printk</code> and your understanding of &quot;rate limiting&quot; and &quot;tracing&quot; mechanisms. The <code>printk_ratelimit</code> mechanism (through macro wrappers) can limit the number of prints per second, while <code>trace_printk</code> is specifically designed for high-frequency paths with extremely low overhead and no blocking.</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>Debugging mechanisms in the kernel environment are far more complex than in userspace. The core tool, <code>printk</code>, is not a simple <code>printf</code> replacement. It temporarily stores logs in a ring buffer and determines whether messages are output to the console based on log levels 0-7 (such as <code>KERN_EMERG</code> to <code>KERN_DEBUG</code>). Developers should prioritize using wrapper macros like <code>pr_info()</code> to keep code clean. Using <code>pr_fmt()</code> automatically adds module name, function name, and line number prefixes to all logs, which greatly improves information filtering efficiency when debugging multiple modules concurrently.</p><p>To prevent debug printing in high-frequency paths from causing system &quot;livelocks&quot; or log flooding, the kernel provides the <code>pr_*_ratelimited()</code> family of macros for rate-limiting protection. This mechanism drops excess messages based on a <code>/proc/sys/kernel/printk_ratelimit</code> (time window) and <code>burst</code> (burst threshold) to prevent critical information from being overwritten. If you need to trace in extremely high-frequency scenarios, consider using <code>trace_printk()</code>, which writes directly to the trace buffer with a performance overhead far lower than <code>printk</code>.</p><p>The most powerful debugging technique is <strong>Dynamic Debug</strong>, which allows you to dynamically enable or disable any <code>pr_debug()</code> or <code>dev_dbg()</code> call site at runtime without recompiling the kernel. By writing match specifications and flags (such as <code>module misc +p</code>) to <code>/proc/dynamic_debug/control</code> or <code>debugfs</code>, developers can precisely control log output for specific modules, files, or even line numbers, achieving &quot;zero-overhead&quot; on-demand debugging in production environments.</p><p>For driver development, you must use the <code>dev_*()</code> family of functions (like <code>dev_dbg()</code>) instead of generic <code>pr_*()</code>. These functions not only automatically carry device context information (such as device name and bus number) but also allow you to pinpoint the source of a problem in systems with multiple identical devices. When paired with Dynamic Debug, <code>dev_dbg()</code> can provide extremely detailed hardware interaction logs without impacting performance.</p><p>When the system is in the early boot stage and the console hasn&#39;t been initialized yet, a normal <code>printk</code> cannot output anything. In this case, you need to rely on <strong>Early printk</strong>. This mechanism bypasses complex driver layers and writes characters directly to the serial port or VGA video memory through low-level hardware operations. It is a critical lifesaver for diagnosing &quot;black screen&quot; issues like kernel boot crashes and memory initialization failures. Enabling it typically requires specific kernel configuration options (like <code>CONFIG_EARLY_PRINTK</code>) and boot parameters.</p>`,147)])])}const k=s(t,[["render",r]]);export{u as __pageData,k as default};
