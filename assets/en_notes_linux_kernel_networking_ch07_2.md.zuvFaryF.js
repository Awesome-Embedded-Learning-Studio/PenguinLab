import{_ as t,o,c as s,a}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"7.2  Interacting with the Neighbor Subsystem from Userspace","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch07_2.md","filePath":"en/notes/linux_kernel_networking/ch07_2.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_networking/ch07_2.md"};function i(r,e,d,c,l,h){return o(),s("div",null,[...e[0]||(e[0]=[a('<h1 id="_7-2-interacting-with-the-neighbor-subsystem-from-userspace" tabindex="-1">7.2 Interacting with the Neighbor Subsystem from Userspace <a class="header-anchor" href="#_7-2-interacting-with-the-neighbor-subsystem-from-userspace" aria-label="Permalink to &quot;7.2  Interacting with the Neighbor Subsystem from Userspace&quot;">​</a></h1><p>In the previous section, we discussed the internal &quot;housekeeping&quot; of the neighbor subsystem: how memory is allocated, how reference counts are managed, and when entries are destroyed. If you are a kernel developer, these are your bricks and mortar.</p><p>But if you are a system administrator, or a developer debugging network issues, you don&#39;t directly interact with these structures. You hold the <code>iproute2</code> toolbox, and you type <code>ip neigh</code>. Between you and the kernel lies an API layer. This section is about that API layer—how we peek into, modify, or even forcefully &quot;instruct&quot; the kernel&#39;s neighbor table from userspace.</p><hr><h2 id="command-line-tools-from-net-tools-to-iproute2" tabindex="-1">Command-Line Tools: From net-tools to iproute2 <a class="header-anchor" href="#command-line-tools-from-net-tools-to-iproute2" aria-label="Permalink to &quot;Command-Line Tools: From net-tools to iproute2&quot;">​</a></h2><p>There are two main ways to manage the ARP table (or rather, the neighbor table), representing two different eras:</p><ol><li><strong>The old-school approach</strong>: using the <code>arp</code> command from the <code>net-tools</code> package.</li><li><strong>The modern approach</strong>: using the <code>ip neigh</code> (or <code>ip neighbour</code>) command from the <code>iproute2</code> package.</li></ol><p>Although they do the same thing, the underlying paths they take are completely different.</p><p>If you run the classic <code>arp</code> command:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ arp</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This request is ultimately handled by the <code>arp_seq_show()</code> method in the kernel, with the code located in <code>net/ipv4/arp.c</code>. Note that this is IPv4-specific—it can only see the ARP table.</p><p>If you run the modern <code>ip neigh show</code>:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ ip neigh show</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This time, it calls the generic <code>neigh_dump_info()</code> method, located in <code>net/core/neighbour.c</code>.</p><p>The biggest difference in output between the two is the amount of information. The <code>arp</code> command gives a brief list, whereas <code>ip neigh show</code> dumps the <strong>NUD state</strong> (Neighbour Unreachability Detection state) of the neighbor entries all at once—such as <code>NUD_REACHABLE</code> (reachable), <code>NUD_STALE</code> (stale), or <code>NUD_DELAY</code> (delay). These states are essentially the &quot;thermometers&quot; of the neighbor subsystem, and we will focus on them in the coming chapters.</p><p>Additionally, the <code>ip</code> command is dual-mode. With the <code>-6</code> parameter:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ ip -6 neigh show</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>It displays the IPv6 neighbor table (NDISC table). This is something the <code>arp</code> command cannot do.</p><p>Besides command-line tools, the kernel also exposes this data through <code>procfs</code>. If you are an old-school administrator accustomed to <code>cat</code> files, you can still operate this way:</p><ul><li><strong>View the ARP table</strong>: <code>cat /proc/net/arp</code>. Behind the scenes, this actually calls <code>arp_seq_show()</code>, which is essentially no different from typing the <code>arp</code> command.</li><li><strong>View statistics</strong>: <code>cat /proc/net/stat/arp_cache</code> (ARP statistics) or <code>cat /proc/net/stat/ndisc_cache</code> (NDISC statistics). Both files are handled uniformly by the <code>neigh_stat_seq_show()</code> method.</li></ul><hr><h2 id="manual-additions-and-deletions-laying-down-the-law-for-the-kernel" tabindex="-1">Manual Additions and Deletions: Laying Down the Law for the Kernel <a class="header-anchor" href="#manual-additions-and-deletions-laying-down-the-law-for-the-kernel" aria-label="Permalink to &quot;Manual Additions and Deletions: Laying Down the Law for the Kernel&quot;">​</a></h2><p>Normally, the kernel dynamically learns the MAC addresses of its neighbors (passive learning). But sometimes, we want to forcefully tell the kernel something—for example, &quot;192.168.0.121 is always at 00:30:48:5b:cc:45,&quot; regardless of whether it actually is. This is a <strong>static neighbor entry</strong>.</p><p>You can use <code>ip neigh add</code> to do this:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ ip neigh add 192.168.0.121 dev eth0 lladdr 00:30:48:5b:cc:45 nud permanent</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This command triggers the <code>neigh_add()</code> method. Note the final <code>nud permanent</code>; the <code>NUD_PERMANENT</code> state we mentioned in the previous section comes into play right here. This state tells the kernel: &quot;Don&#39;t delete, don&#39;t modify, don&#39;t expire this entry—it is absolute.&quot;</p><p>To delete it, use <code>ip neigh del</code>, which calls the <code>neigh_delete()</code> method behind the scenes:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ ip neigh del 192.168.0.121 dev eth0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Another very useful scenario is <strong>Proxy ARP</strong>.</p><p>Let&#39;s briefly recall this concept: Host A asks &quot;Who has IP B?&quot;, and Host C (a router), even though it isn&#39;t IP B, can answer on B&#39;s behalf saying &quot;I do, the MAC is xxx.&quot; It then receives the traffic and forwards it to B. This is useful in certain special network topologies.</p><p>To configure a Proxy ARP entry in the kernel, use the <code>proxy</code> keyword:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ ip neigh add proxy 192.168.2.11 dev eth0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Although the command looks similar, the internal path taken by the kernel is completely different. The <code>neigh_add()</code> method will notice that the user-provided data carries a <code>NTF_PROXY</code> flag (stored in the <code>ndm_flags</code> field of the <code>ndm</code> object). Once it sees this flag, the kernel won&#39;t check the regular neighbor table; instead, it calls the <code>pneigh_lookup()</code> method to look up the <strong>proxy neighbor hash table</strong> (<code>phash_buckets</code>). If it finds nothing, it creates a new entry in this table.</p><p>Deleting a proxy entry follows the same logic:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ ip neigh del proxy 192.168.2.11 dev eth0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>After detecting the <code>NTF_PROXY</code> flag, the <code>neigh_delete()</code> method calls <code>pneigh_delete()</code> to clean up the proxy table.</p><p>Besides manipulating entries, you can also manipulate the parameters of the entire table. The <code>ip ntable</code> command is designed for this:</p><ul><li><p><code>ip ntable show</code>: Displays the parameter configuration of all neighbor tables.</p></li><li><p><code>ip ntable change</code>: Dynamically modifies parameters. For example, changing the ARP cache queue length on the <code>eth0</code> interface to 20:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ ip ntable change name arp_cache queue 20 dev eth0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This calls the <code>neightbl_set()</code> method. This is highly useful for tuning scenarios with massive numbers of neighbors (such as data center gateways).</p></li></ul><p>Finally, a word on the old relic <code>arp</code> command. You can also use it to add static entries:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$ arp -s &lt;IPAddress&gt; &lt;MacAddress&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The effect is similar to <code>ip neigh add ... nud permanent</code>, but don&#39;t expect static entries to survive a reboot—they only live in kernel memory and are lost once the power is off. If you want persistence, you need to add them to your startup scripts.</p><hr><h2 id="network-event-callbacks-the-kernel-s-ears" tabindex="-1">Network Event Callbacks: The Kernel&#39;s Ears <a class="header-anchor" href="#network-event-callbacks-the-kernel-s-ears" aria-label="Permalink to &quot;Network Event Callbacks: The Kernel&#39;s Ears&quot;">​</a></h2><p>Having covered how userspace knocks on the door, let&#39;s look at how the kernel listens to &quot;broadcasts&quot; internally.</p><p>Here, &quot;broadcasts&quot; refer to network event notifications.</p><p>Interestingly, <strong>the core neighbor subsystem itself does not register for network event notifications</strong>. It is a quiet core that doesn&#39;t directly care about network interfaces being plugged in or MAC addresses changing. The ones actually worrying about these things are the specific protocol modules—ARP and NDISC.</p><p>In the IPv4 ARP module, <code>arp_netdev_event()</code> is registered as the network event callback function. It mainly watches for two types of events:</p><ol><li><strong>MAC address changes</strong>: For example, if you run <code>ifconfig eth0 hw ether ...</code> on a network interface. At this point, the kernel triggers an event, and <code>arp_netdev_event()</code> calls the generic <code>neigh_changeaddr()</code> method to flush the relevant neighbor entries, while also calling <code>rt_cache_flush()</code> to clear the routing cache. Because the L2 address changed, the previous caches might all be invalid.</li><li><strong>Flag changes (starting from kernel 3.11)</strong>: If the <code>IFF_NOARP</code> flag of a network interface changes, it also triggers a <code>NETDEV_CHANGE</code> event. Similarly, <code>neigh_changeaddr()</code> steps in to handle it.</li></ol><p>In the IPv6 NDISC module, <code>ndisc_netdev_event()</code> takes on a similar responsibility, but it watches for a more diverse set of signals: <code>NETDEV_CHANGEADDR</code> (address change), <code>NETDEV_DOWN</code> (interface shutdown), and <code>NETDEV_NOTIFY_PEERS</code> (notify neighbors, typically used in NIC failover scenarios).</p><hr><p><strong>Chapter Echoes</strong></p><p>In this section, we stood on the boundary between userspace and kernel space.</p><p>We saw how the same data—the neighbor table—is viewed from different perspectives. For <code>iproute2</code>, it is a set of objects that can be shown, added, or deleted; for the kernel, it is a set of hash tables, reference counts, and state machines.</p><p>More importantly, we introduced the concepts of <strong>static configuration</strong> and <strong>dynamic events</strong>. Although manually adding entries seems convenient, the real network world is fluid—MAC addresses change, network interfaces go down, and traffic migrates.</p><p><strong>(Leading into the next section)</strong> Now, we have the containers and the means to manage them. But the containers are still empty. In the next section, we will shift our focus to the IPv4 world and see how the <strong>ARP protocol</strong> moves in as the first resident into this ready-made house—especially that most core question: when the kernel realizes &quot;I don&#39;t know who the neighbor is,&quot; what exact signal does it send out?</p>',55)])])}const g=t(n,[["render",i]]);export{u as __pageData,g as default};
