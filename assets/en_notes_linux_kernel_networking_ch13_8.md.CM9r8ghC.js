import{_ as a,o as s,c as i,a as n}from"./chunks/framework.8tiMeCK-.js";const h=JSON.parse('{"title":"13.8 Cheat Sheet","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch13_8.md","filePath":"en/notes/linux_kernel_networking/ch13_8.md","lastUpdated":null}'),t={name:"en/notes/linux_kernel_networking/ch13_8.md"};function r(o,e,d,l,c,p){return s(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="_13-8-cheat-sheet" tabindex="-1">13.8 Cheat Sheet <a class="header-anchor" href="#_13-8-cheat-sheet" aria-label="Permalink to &quot;13.8 Cheat Sheet&quot;">​</a></h1><p>By now, we&#39;ve dissected most of the bones in the RDMA stack. You should have a lot of loose parts on hand: <code>ib_client</code>, <code>PD</code>, <code>QP</code>, <code>CQ</code>, <code>MR</code>...</p><p>Before closing this book (or before you start writing your own driver), you need a blueprint. That&#39;s the whole point of this section—it&#39;s not here to teach you anything new, but to gather the APIs scattered across the previous hundreds of pages and organize them by function.</p><p>You&#39;ll find that when you&#39;re staring at a blank <code>.c</code> file, what you need isn&#39;t a lengthy philosophical discourse, but that damn function signature and what parameters it actually takes.</p><p>Below is a cheat sheet for the core APIs of the kernel RDMA subsystem. Every function is worth an extra two seconds of your attention in the <code>grep</code> results.</p><hr><h3 id="client-and-device-management" tabindex="-1">Client and Device Management <a class="header-anchor" href="#client-and-device-management" aria-label="Permalink to &quot;Client and Device Management&quot;">​</a></h3><p>Everything starts with registration.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_register_client(struct ib_client *client);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Registers a client with the kernel RDMA stack. This is how you tell the kernel &quot;I&#39;m here.&quot; On successful registration, your <code>add</code> callback is invoked, and you receive notifications for existing RDMA devices in the system.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>void ib_unregister_client(struct ib_client *client);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Unregisters. Tells the kernel &quot;I&#39;m out&quot; and that you no longer care about device events.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>void ib_set_client_data(struct ib_device *device, struct ib_client *client, void *data);</span></span>
<span class="line"><span>void *ib_get_client_data(struct ib_device *device, struct ib_client *client);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Each device can bind a private data pointer (context) for each client. Typically, you <code>set</code> this in the <code>add</code> callback and <code>get</code> it in subsequent operations.</p><hr><h3 id="event-handling" tabindex="-1">Event Handling <a class="header-anchor" href="#event-handling" aria-label="Permalink to &quot;Event Handling&quot;">​</a></h3><p>RDMA devices are asynchronous; you need to listen for their &quot;screams.&quot;</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_register_event_handler(struct ib_event_handler *event_handler);</span></span>
<span class="line"><span>int ib_unregister_event_handler(struct ib_event_handler *event_handler);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Registers/unregisters an event handler. When an asynchronous event occurs on a device (such as a port state going down or a device hot-unplug), your registered callback is triggered.</p><hr><h3 id="device-and-port-query" tabindex="-1">Device and Port Query <a class="header-anchor" href="#device-and-port-query" aria-label="Permalink to &quot;Device and Port Query&quot;">​</a></h3><p>Before operating, figure out who you&#39;re dealing with.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_query_device(struct ib_device *device, struct ib_device_attr *device_attr);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Queries device attributes. This tells you what the device supports (maximum MR size, maximum QP count, whether it supports atomic operations, etc.). <strong>Don&#39;t guess capabilities; query them.</strong></p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_query_port(struct ib_device *device, u8 port_num, struct ib_port_attr *port_attr);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Queries the state of a specified port (rate, link layer state, physical state).</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>enum rdma_link_layer rdma_port_get_link_layer(struct ib_device *device, u8 port_num);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>What is running underneath the port? InfiniBand, Ethernet, or something else? This function gives you the answer. This is crucial for deciding whether to run RoCE or native IB at the upper layer.</p><hr><h3 id="address-query" tabindex="-1">Address Query <a class="header-anchor" href="#address-query" aria-label="Permalink to &quot;Address Query&quot;">​</a></h3><p>The RDMA address hierarchy is complex: GID, P_Key, LID.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_query_gid(struct ib_device *device, u8 port_num, int index, union ib_gid *gid);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Queries the GID at a specified index in the port&#39;s GID table.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_query_pkey(struct ib_device *device, u8 port_num, u16 index, u16 *pkey);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Queries the partition key at a specified index in the port&#39;s P_Key table.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_find_gid(struct ib_device *device, union ib_gid *gid, u8 *port_num, u16 *index);</span></span>
<span class="line"><span>int ib_find_pkey(struct ib_device *device, u8 port_num, u16 pkey, u16 *index);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Reverse query: given a GID or P_Key, find the port number and table index where it resides.</p><hr><h3 id="protection-domain-pd" tabindex="-1">Protection Domain (PD) <a class="header-anchor" href="#protection-domain-pd" aria-label="Permalink to &quot;Protection Domain (PD)&quot;">​</a></h3><p>The container for all resources. Almost all of your operations start with allocating a PD.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>struct ib_pd *ib_alloc_pd(struct ib_device *device);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Allocates a PD. This is a prerequisite for allocating QPs and MRs.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_dealloc_pd(struct ib_pd *pd);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Destroys a PD. <strong>Note:</strong> Before destruction, you must ensure that all QPs and MRs dependent on it have already been destroyed; otherwise, it will return <code>-EBUSY</code>.</p><hr><h3 id="address-handle-ah" tabindex="-1">Address Handle (AH) <a class="header-anchor" href="#address-handle-ah" aria-label="Permalink to &quot;Address Handle (AH)&quot;">​</a></h3><p>Used for UD (Unreliable Datagram) QPs. Since these are datagrams, you need to tell the hardware &quot;where to send this packet.&quot;</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>struct ib_ah *ib_create_ah(struct ib_pd *pd, struct ib_ah_attr *ah_attr);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Creates an AH. The <code>ah_attr</code> is filled with the destination LID, GID, path MTU, and other information.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_init_ah_from_wc(struct ib_device *device, u8 port_num, struct ib_wc *wc, struct ib_grh *grh, struct ib_ah_attr *ah_attr);</span></span>
<span class="line"><span>struct ib_ah *ib_create_ah_from_wc(struct ib_pd *pd, struct ib_wc *wc, struct ib_grh *grh, u8 port_num);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>&quot;Reverse-engineering the address from a received packet.&quot; If you receive a UD message and want to reply to it immediately, these two functions help you extract the correct AH attributes from the Work Completion (WC) and GRH, saving you from looking up the routing table yourself.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_modify_ah(struct ib_ah *ah, struct ib_ah_attr *ah_attr);</span></span>
<span class="line"><span>int ib_query_ah(struct ib_ah *ah, struct ib_ah_attr *ah_attr);</span></span>
<span class="line"><span>int ib_destroy_ah(struct ib_ah *ah);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Standard modify, query, and destroy operations.</p><hr><h3 id="memory-region-mr-and-dma" tabindex="-1">Memory Region (MR) and DMA <a class="header-anchor" href="#memory-region-mr-and-dma" aria-label="Permalink to &quot;Memory Region (MR) and DMA&quot;">​</a></h3><p>This is the most complex part. You need to map kernel virtual addresses into DMA addresses that the hardware understands.</p><h4 id="dma-mapping-operations" tabindex="-1">DMA Mapping Operations <a class="header-anchor" href="#dma-mapping-operations" aria-label="Permalink to &quot;DMA Mapping Operations&quot;">​</a></h4><p>Low-level DMA interfaces for handling memory coherency.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>static inline int ib_dma_mapping_error(struct ib_device *dev, u64 dma_addr);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><strong>Never skip this step.</strong> After every <code>ib_dma_map_xxx</code>, you must call this function to check if the returned address is valid. Hardware mapping can fail.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>static inline u64 ib_dma_map_single(struct ib_device *dev, void *cpu_addr, size_t size, enum dma_data_direction direction);</span></span>
<span class="line"><span>static inline void ib_dma_unmap_single(struct ib_device *dev, u64 addr, size_t size, enum dma_data_direction direction);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>The simplest mapping: maps a kernel virtual address (one that came from <code>kmalloc</code> or is on the stack) to a DMA address.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>static inline u64 ib_dma_map_page(struct ib_device *dev, struct page *page, unsigned long offset, size_t size, enum dma_data_direction direction);</span></span>
<span class="line"><span>static inline void ib_dma_unmap_page(struct ib_device *dev, u64 addr, size_t size, enum dma_data_direction direction);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Mapping based on <code>struct page</code>. Use this if you are dealing with page-level data.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>static inline int ib_dma_map_sg(struct ib_device *dev, struct scatterlist *sg, int nents, enum dma_data_direction direction);</span></span>
<span class="line"><span>static inline void ib_dma_unmap_sg(struct ib_device *dev, struct scatterlist *sg, int nents, enum dma_data_direction direction);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Scatter/gather mapping. Use this when handling non-contiguous physical memory blocks.</p><h4 id="dma-synchronization" tabindex="-1">DMA Synchronization <a class="header-anchor" href="#dma-synchronization" aria-label="Permalink to &quot;DMA Synchronization&quot;">​</a></h4><p>If your CPU and NIC are accessing this memory simultaneously, you need to coordinate ownership.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>static inline void ib_dma_sync_single_for_cpu(struct ib_device *dev, u64 addr, size_t size, enum dma_data_direction dir);</span></span>
<span class="line"><span>static inline void ib_dma_sync_single_for_device(struct ib_device *dev, u64 addr, size_t size, enum dma_data_direction dir);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p><code>for_device</code> before DMA, and <code>for_cpu</code> before CPU reads. Skipping this step can lead to data corruption on certain architectures.</p><h4 id="coherent-memory" tabindex="-1">Coherent Memory <a class="header-anchor" href="#coherent-memory" aria-label="Permalink to &quot;Coherent Memory&quot;">​</a></h4><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>static inline void *ib_dma_alloc_coherent(struct ib_device *dev, size_t size, u64 *dma_handle, gfp_t flag);</span></span>
<span class="line"><span>static inline void ib_dma_free_coherent(struct ib_device *dev, size_t size, void *cpu_addr, u64 *dma_handle);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Allocates a block of memory accessed by both the CPU and the device. This memory is coherently mapped and doesn&#39;t require frequent syncs, but allocation is more expensive. Suitable for control structures (like shared Ring Buffers).</p><h4 id="memory-registration-mr" tabindex="-1">Memory Registration (MR) <a class="header-anchor" href="#memory-registration-mr" aria-label="Permalink to &quot;Memory Registration (MR)&quot;">​</a></h4><p>Handing memory over to the RDMA device for management.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>struct ib_mr *ib_get_dma_mr(struct ib_pd *pd, int mr_access_flags);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This is a &quot;clever shortcut&quot; function. It directly returns an MR that covers the entire system address space (based on DMA addresses). Simple and brute-force, but in certain scenarios with low security requirements, it saves you from tedious registration steps.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>struct ib_mr *ib_reg_phys_mr(struct ib_pd *pd, struct ib_phys_buf *phys_buf_array, int num_phys_buf, int mr_access_flags, u64 *iova_start);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The standard approach. Registers an MR based on an array of physical pages. You need to prepare the physical page list.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_rereg_phys_mr(struct ib_mr *mr, int mr_rereg_mask, ...);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><strong>This is a very useful performance optimization point.</strong> When you need to change the size or permissions of an MR, the traditional approach is to destroy the old one and register a new one (which is very slow). <code>rereg</code> allows you to hot-modify its attributes without destroying the MR, avoiding the need to re-establish mappings.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_query_mr(struct ib_mr *mr, struct ib_mr_attr *mr_attr);</span></span>
<span class="line"><span>int ib_dereg_mr(struct ib_mr *mr);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Query attributes and deregister.</p><hr><h3 id="memory-window-mw" tabindex="-1">Memory Window (MW) <a class="header-anchor" href="#memory-window-mw" aria-label="Permalink to &quot;Memory Window (MW)&quot;">​</a></h3><p>A dynamic, temporary remote access authorization mechanism.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>struct ib_mw *ib_alloc_mw(struct ib_pd *pd, enum ib_mw_type type);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Allocates an MW.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>static inline int ib_bind_mw(struct ib_qp *qp, struct ib_mw *mw, struct ib_mw_bind *mw_bind);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Binds an MW to an MR and specifies the remote access permissions for this binding (such as read-only, read-write). Once bound, remote nodes holding this window&#39;s key can access the corresponding memory segment. Upon unbinding, permissions immediately become invalid.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_dealloc_mw(struct ib_mw *mw);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Deallocates an MW.</p><hr><h3 id="completion-queue-cq" tabindex="-1">Completion Queue (CQ) <a class="header-anchor" href="#completion-queue-cq" aria-label="Permalink to &quot;Completion Queue (CQ)&quot;">​</a></h3><p>The end of the line for producers, the starting point for consumers.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>struct ib_cq *ib_create_cq(struct ib_device *device, </span></span>
<span class="line"><span>                          ib_comp_handler comp_handler, </span></span>
<span class="line"><span>                          void (*event_handler)(struct ib_event *, void *), </span></span>
<span class="line"><span>                          void *cq_context, </span></span>
<span class="line"><span>                          int cqe, </span></span>
<span class="line"><span>                          int comp_vector);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>Creates a CQ.</p><ul><li><code>comp_handler</code>: A callback function invoked directly in kernel space when a WC is generated (hard interrupt context).</li><li><code>event_handler</code>: Handles CQ-related asynchronous events (such as CQ overflow).</li><li><code>cqe</code>: Queue depth.</li></ul><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_resize_cq(struct ib_cq *cq, int cqe);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Dynamically resizes the CQ. Useful if your application&#39;s workload suddenly changes.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_modify_cq(struct ib_cq *cq, u16 cq_count, u16 cq_period);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><strong>Key to performance tuning.</strong> Sets the CQ&#39;s interrupt moderation parameters. If you generate an interrupt for every single packet, your CPU will die of exhaustion. Setting reasonable <code>cq_count</code> and <code>cq_period</code> values allows the device to notify the kernel only after accumulating a certain number of WCs.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_peek_cq(struct ib_cq *cq, int wc_cnt);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Take a sneak peek. Non-blockingly checks if the CQ has at least <code>wc_cnt</code> WCs.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>static inline int ib_req_notify_cq(struct ib_cq *cq, enum ib_cq_notify_flags flags);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><strong>Tells the kernel &quot;I&#39;m ready.&quot;</strong> Usually called before a <code>poll</code> loop. If set to <code>IB_CQ_NEXT_COMP</code>, the next arriving WC will trigger a notification.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>static inline int ib_poll_cq(struct ib_cq *cq, int num_entries, struct ib_wc *wc);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The real workhorse function. Pulls WCs out of the CQ.</p><hr><h3 id="shared-receive-queue-srq" tabindex="-1">Shared Receive Queue (SRQ) <a class="header-anchor" href="#shared-receive-queue-srq" aria-label="Permalink to &quot;Shared Receive Queue (SRQ)&quot;">​</a></h3><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>struct ib_srq *ib_create_srq(struct ib_pd *pd, struct ib_srq_init_attr *srq_init_attr);</span></span>
<span class="line"><span>int ib_modify_srq(struct ib_srq *srq, struct ib_srq_attr *srq_attr, enum ib_srq_attr_mask srq_attr_mask);</span></span>
<span class="line"><span>int ib_query_srq(struct ib_srq *srq, struct ib_srq_attr *srq_attr);</span></span>
<span class="line"><span>int ib_destroy_srq(struct ib_srq *srq);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Creates, modifies, queries, and destroys an SRQ. The logic is similar to a regular QP, but it is passive and only handles receiving.</p><hr><h3 id="queue-pair-qp" tabindex="-1">Queue Pair (QP) <a class="header-anchor" href="#queue-pair-qp" aria-label="Permalink to &quot;Queue Pair (QP)&quot;">​</a></h3><p>This is the heart of the entire system.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>struct ib_qp *ib_create_qp(struct ib_pd *pd, struct ib_qp_init_attr *qp_init_attr);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Creates a QP. You need to specify here whether it&#39;s RC, UC, or UD, the SQ/RQ depths, and which CQ and SRQ to use.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_modify_qp(struct ib_qp *qp, struct ib_qp_attr *qp_attr, int qp_attr_mask);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><strong>This is the function that will make you tear your hair out.</strong> It&#39;s responsible for moving the QP from the <code>RESET</code> state all the way to the <code>RTS</code> state. You need to carefully fill in the <code>qp_attr</code> according to the state machine. Miss a single mask bit, or fill in the wrong state (like trying to <code>RTS</code> before you&#39;ve <code>RTR</code>), and the hardware will flat-out reject your request.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_query_qp(struct ib_qp *qp, struct ib_qp_attr *qp_attr, int qp_attr_mask, struct ib_qp_init_attr *qp_init_attr);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Queries the current QP attributes. Extremely useful for debugging to see what mess you&#39;ve actually turned it into.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_destroy_qp(struct ib_qp *qp);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Destroys a QP.</p><hr><h3 id="data-posting" tabindex="-1">Data Posting <a class="header-anchor" href="#data-posting" aria-label="Permalink to &quot;Data Posting&quot;">​</a></h3><p>Finally, send the requests out.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>static inline int ib_post_send(struct ib_qp *qp, struct ib_send_wr *send_wr, struct ib_send_wr **bad_send_wr);</span></span>
<span class="line"><span>static inline int ib_post_recv(struct ib_qp *qp, struct ib_recv_wr *recv_wr, struct ib_recv_wr **bad_recv_wr);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p><strong>Pay attention to that <code>bad_xxx_wr</code> pointer.</strong> If <code>ib_post_send</code> returns an error, it won&#39;t tell you which WR failed; it will only fill in the pointer to the failed WR into <code>bad_send_wr</code>. You must check this pointer to know which link in the chain broke. If it&#39;s a batch post, the earlier ones might have succeeded while the later ones failed.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>static inline int ib_post_srq_recv(struct ib_srq *srq, struct ib_recv_wr *recv_wr, struct ib_recv_wr **bad_recv_wr);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Posts receive requests to an SRQ.</p><hr><h3 id="multicast" tabindex="-1">Multicast <a class="header-anchor" href="#multicast" aria-label="Permalink to &quot;Multicast&quot;">​</a></h3><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>int ib_attach_mcast(struct ib_qp *qp, union ib_gid *gid, u16 lid);</span></span>
<span class="line"><span>int ib_detach_mcast(struct ib_qp *qp, union ib_gid *gid, u16 lid);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Joins or leaves a multicast group with a UD QP. Note that you can only truly start receiving multicast data after the QP state is ready via <code>RESET</code>.</p><hr><h3 id="chapter-echoes" tabindex="-1">Chapter Echoes <a class="header-anchor" href="#chapter-echoes" aria-label="Permalink to &quot;Chapter Echoes&quot;">​</a></h3><p>At this point, we have completed a full deconstruction of the Linux kernel&#39;s InfiniBand/RDMA subsystem.</p><p>Think back to the starting point of this chapter: we were faced with a mysterious black box that promised &quot;zero-copy&quot; and &quot;kernel bypass.&quot; To open this box, we had to introduce a whole slew of new vocabulary—Verbs, QP, CQ, MR, PD...</p><p>The cheat sheet you now hold in your hands is essentially the key list for opening this black box.</p><p>Behind every API lies a set of register operations on the hardware or a piece of firmware logic. When you call <code>ib_post_send</code>, you are actually writing a descriptor into the NIC&#39;s send queue ring buffer; when you call <code>ib_req_notify_cq</code>, you are telling the interrupt controller &quot;you can start bothering me now.&quot;</p><p>The RDMA learning curve is indeed steep, not just because of the inherent complexity of the technology, but because it challenges our intuition about traditional network programming—we&#39;re used to the kernel buffering, retransmitting, and queuing for us, while RDMA requires you to take over all of this yourself.</p><p>But as we hinted at the beginning of this chapter, this complexity comes with a cost, but also with a reward. When you truly master this mechanism, you can transfer data within microsecond-level latency—something the traditional TCP/IP stack can never achieve.</p><p>In the next chapter, we will leave this high-speed physical wire and turn our gaze toward more macroscopic system orchestration—network namespaces and the Bluetooth subsystem. A completely different philosophy of connection awaits us there.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: Understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: Understanding&quot;">​</a></h3><p><strong>Question</strong>: In the InfiniBand network architecture, GUID, GID, and LID represent different address identifiers. Briefly describe the main differences between the three: How is the LID generated? In what scenarios is the GID primarily used for packet routing?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: 1. LID (Local IDentifier): A 16-bit address assigned by the Subnet Manager (SM), used for routing and forwarding within a subnet. 2. GID (Global IDentifier): A 128-bit identifier generated based on the port GUID and Subnet ID, primarily used for cross-subnet routing or multicast packets (GRH header). 3. Difference: LID is a locally assigned short address used for efficient forwarding within a subnet; GID is a globally unique long address, similar in format to IPv6.</p><p><strong>Analysis</strong>: Tests the understanding of the InfiniBand address hierarchy. As described in the text, the LID is assigned by the SM and used for switch forwarding table lookups within a subnet; the GID is generated based on the GUID, and when crossing subnets (using routers) or in multicast communication, the packet must include a GRH (Global Routing Header), which uses the GID for addressing.</p></details><h3 id="exercise-2-understanding" tabindex="-1">Exercise 2: Understanding <a class="header-anchor" href="#exercise-2-understanding" aria-label="Permalink to &quot;Exercise 2: Understanding&quot;">​</a></h3><p><strong>Question</strong>: When using RDMA technology, why must memory buffers be &quot;registered&quot;? What two critical keys are generated after registration, and what are their respective purposes?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Registration is required to ensure that physical address mappings are fixed (preventing them from being swapped out) and to set hardware access permissions. The two generated keys are:</p><ol><li>lkey (Local Key): Used by local Work Requests to access local memory.</li><li>rkey (Remote Key): Provided to remote machines, used by remote RDMA operations (Read/Write) to access this memory.</li></ol><p><strong>Analysis</strong>: Tests the understanding of the Memory Region (MR) concept. The registration process maps virtual memory to physical memory and pins it. The lkey and rkey are credential tokens for hardware DMA access to memory, ensuring that only requests holding the correct key can access the memory region.</p></details><h3 id="exercise-3-application" tabindex="-1">Exercise 3: Application <a class="header-anchor" href="#exercise-3-application" aria-label="Permalink to &quot;Exercise 3: Application&quot;">​</a></h3><p><strong>Question</strong>: Suppose you are developing a network module for a high-performance distributed database. You&#39;ve chosen an InfiniBand network and want to avoid CPU handling of network packets in the main flow to reduce latency. Combining the advantages of RDMA, explain which RDMA operation you would choose to directly write local modification logs into the standby node&#39;s memory without requiring the standby node&#39;s CPU involvement? Why?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Choose the <strong>RDMA Write</strong> operation. Reason: RDMA Write allows the local node to directly write data into the remote node&#39;s memory without any intervention from the remote CPU (no need for the remote node to execute a receive call). This fully aligns with the Kernel Bypass and CPU Offload characteristics, drastically reducing the latency of the copy operation and imposing near-zero load on the standby node&#39;s CPU.</p><p><strong>Analysis</strong>: This is an application question testing the practical use of RDMA operation types. A Send operation typically requires the remote node to post a receive WR first, which consumes CPU. RDMA Read requires the remote node to expose memory but is initiated as a pull from the local side; while it also doesn&#39;t consume remote CPU, it&#39;s usually used for pulling data. For a &quot;write&quot; scenario (like log synchronization), RDMA Write is the most direct and efficient choice, achieving true zero-copy and zero-intervention.</p></details><h3 id="exercise-4-application" tabindex="-1">Exercise 4: Application <a class="header-anchor" href="#exercise-4-application" aria-label="Permalink to &quot;Exercise 4: Application&quot;">​</a></h3><p><strong>Question</strong>: When designing a highly concurrent RDMA server application, you find that creating an independent QP and its corresponding RQ (Receive Queue) for each connection consumes a massive amount of memory, because you need to reserve a large number of WQEs (Work Queue Entries) for each RQ to avoid RNR (Receiver Not Ready) errors. Which kernel mechanism would you introduce to optimize the receiver&#39;s memory consumption and scalability? Briefly describe how it works.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Introduce the <strong>SRQ (Shared Receive Queue)</strong>. Principle: An SRQ allows multiple QPs to share a single receive queue. The application layer only needs to fill the SRQ with enough Receive WQEs, and all associated QPs can consume receive requests from it. This avoids reserving a large buffer for each QP individually, thereby drastically reducing memory usage and improving scalability.</p><p><strong>Analysis</strong>: Tests the analysis of SRQ use cases. RNR errors occur because the receive queue has no WQEs ready. When the number of connections is huge, reserving a queue of sufficient depth for each connection&#39;s RQ leads to a memory explosion. The SRQ pools receive resources and is the standard solution for solving RDMA receiver-side scalability issues.</p></details><h3 id="exercise-5-thinking" tabindex="-1">Exercise 5: Thinking <a class="header-anchor" href="#exercise-5-thinking" aria-label="Permalink to &quot;Exercise 5: Thinking&quot;">​</a></h3><p><strong>Question</strong>: RDMA provides the Kernel Bypass feature, allowing user-space applications to send and receive data directly through the HCA (NIC), bypassing the kernel network stack to reduce latency. However, the Linux kernel still maintains the InfiniBand subsystem (such as <code>drivers/infiniband/core</code>). Think about this: if user space directly operates the hardware, why is the kernel&#39;s RDMA subsystem still needed? What indispensable roles does the kernel subsystem play in the RDMA ecosystem? (List 2-3 points)</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Although the data plane is bypassed, the kernel RDMA subsystem is still necessary. Its main responsibilities include:</p><ol><li><strong>Resource Allocation and Security Management</strong>: Creating resource objects like PDs, MRs, CQs, and QPs, and ensuring memory and access isolation between different processes or tenants through the lkey/rkey and P_Key mechanisms (preventing unauthorized DMA access).</li><li><strong>Hardware Initialization and Configuration</strong>: Responsible for loading drivers, initializing HCA hardware, and interacting with the Subnet Manager (SM) (such as obtaining the LID, configuring routing tables).</li><li><strong>Control Plane and Multiplexing</strong>: Although a single application can bypass the kernel, the operating system needs to coordinate multiple applications&#39; usage of the same NIC and handle asynchronous events (like NIC hot-plugs, link state changes).</li><li><strong>Providing Non-Verbs Protocol Support</strong>: The implementation of upper-layer protocols (ULPs) such as IPoIB and iSER still requires kernel stack support.</li></ol><p><strong>Analysis</strong>: This is a deep-thinking question testing the understanding of the RDMA software-hardware interaction boundary. Kernel Bypass is primarily for zero-copy and low latency on the data plane, but &quot;control&quot; still requires operating system intervention. Bare-metal applications directly writing to hardware registers is unrealistic and insecure; the OS must abstract the hardware, manage global resources (like address mappings), and guarantee system security through the kernel subsystem.</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>The core value of RDMA (Remote Direct Memory Access) lies in bypassing the kernel and CPU to enable direct data interaction between the NIC and user-space memory. This mechanism eliminates the heavy context switches, kernel copies, and interrupt handling overhead of the traditional TCP/IP stack, achieving ultra-low latency in the nanosecond range and extremely high bandwidth utilization. To achieve this, RDMA relies on intelligent hardware like HCAs (Host Channel Adapters) to offload transport protocol processing and memory management tasks from the CPU to the NIC, completely freeing up compute resources.</p><p>The RDMA software stack is built upon the unified Verbs API, abstracting away the differences of underlying physical media such as InfiniBand, RoCE, or iWARP. In the Linux kernel, this subsystem resides in the <code>drivers/infiniband</code> directory, where the core layer handles the logical implementation, the hardware layer handles driver adaptation, and upper-layer protocols (like IPoIB, iSER) provide specific business support. Developers can operate the hardware simply by calling the <code>ib_*</code> family of functions, without needing to care whether the underlying link layer is fiber or Ethernet.</p><p>Memory registration is the cornerstone of RDMA security and performance; its role is to &quot;pin&quot; virtual memory to physical memory and establish mappings. Through the MR (Memory Region) mechanism, the system generates a local key and a remote key for the memory region. The NIC can directly access authorized memory using only these keys, without CPU intervention. Additionally, the PD (Protection Domain) provides a resource isolation sandbox, ensuring that resources (like QPs, MRs) within different security domains cannot be mixed, preventing unauthorized data access.</p><p>The core carrier for data transmission is the QP (Queue Pair), which consists of two independent unidirectional channels: the Send Queue (SQ) and the Receive Queue (RQ). QPs support multiple transport service types: RC provides reliable connections and retransmission, UC pursues speed but allows packet loss, and UD is similar to connectionless UDP. To handle high-concurrency scenarios, RDMA also introduces the SRQ (Shared Receive Queue), allowing multiple QPs to share a single receive buffer pool, which drastically saves memory resources and improves scalability.</p><p>Since all operations are executed asynchronously, RDMA introduces the CQ (Completion Queue) as a feedback mechanism for processing results. After an application submits a Work Request to a QP, the NIC processes it asynchronously in the background and writes status information to the CQ upon completion. Developers must poll or subscribe to CQ events to obtain &quot;completion receipts,&quot; and release memory or replenish receive resources based on these receipts. Although complex, this &quot;post and forget, confirm later&quot; model is key to RDMA achieving zero-copy and kernel bypass.</p>`,167)])])}const b=a(t,[["render",r]]);export{h as __pageData,b as default};
