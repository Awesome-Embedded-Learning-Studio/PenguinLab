import{_ as s,o as t,c as a,a as n}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"14.7  The Linux Bluetooth Subsystem","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch14_7.md","filePath":"en/notes/linux_kernel_networking/ch14_7.md","lastUpdated":null}'),i={name:"en/notes/linux_kernel_networking/ch14_7.md"};function o(r,e,l,h,d,c){return t(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="_14-7-the-linux-bluetooth-subsystem" tabindex="-1">14.7 The Linux Bluetooth Subsystem <a class="header-anchor" href="#_14-7-the-linux-bluetooth-subsystem" aria-label="Permalink to &quot;14.7  The Linux Bluetooth Subsystem&quot;">​</a></h1><p>In the previous section, we were stressing over network latency, discussing how to squeeze every last drop of performance out of the system using &quot;hardcore&quot; techniques like Busy Poll. This is an extreme strategy that trades CPU idle time for speed.</p><p>Now, let&#39;s relax a bit and shift our gaze away from gigabit Ethernet cables toward a completely different kind of network world—<strong>Wireless Personal Area Networks (PANs)</strong>. There are no blazing speeds of direct fiber connections here, nor the microsecond-level anxieties of HFT (High-Frequency Trading), but there is an ever-present need for connectivity: your wireless mouse, your fitness tracker, your wireless earbuds.</p><p>In this section, we&#39;ll talk about <strong>Bluetooth</strong>, and how Linux supports this massive and complex ecosystem through the <strong>BlueZ</strong> protocol stack.</p><hr><h3 id="background-and-motivation" tabindex="-1">Background and Motivation <a class="header-anchor" href="#background-and-motivation" aria-label="Permalink to &quot;Background and Motivation&quot;">​</a></h3><p>If you told an engineer in 1994 that &quot;one day we&#39;re going to cut the wires off our earbuds,&quot; they might have thought you were crazy. But that same year, Ericsson indeed launched a project called &quot;Bluetooth.&quot; Initially, its goal was incredibly humble: <strong>replace RS-232 data cables</strong>. No one expected it to evolve into a wireless standard connecting billions of devices.</p><p>Today&#39;s Bluetooth is no longer a simple &quot;wireless serial port.&quot; It operates in the free 2.4GHz ISM band (crowded alongside Wi-Fi and microwaves) and is maintained by the Bluetooth SIG (Special Interest Group). Although we often joke that Bluetooth is the &quot;king of disconnections,&quot; in close-range, low-power scenarios, it remains the undisputed champion.</p><p>Linux kernel support for Bluetooth began in 2001 with kernel 2.4.6, in a project called <strong>BlueZ</strong>. This is still the default Bluetooth protocol stack for almost all Linux distributions today. Understanding the BlueZ architecture is crucial for debugging all sorts of bizarre connection issues or developing Bluetooth-based applications.</p><hr><h3 id="protocol-stack-panorama-layers-and-responsibilities" tabindex="-1">Protocol Stack Panorama: Layers and Responsibilities <a class="header-anchor" href="#protocol-stack-panorama-layers-and-responsibilities" aria-label="Permalink to &quot;Protocol Stack Panorama: Layers and Responsibilities&quot;">​</a></h3><p>The Bluetooth protocol stack looks a bit like TCP/IP, but it&#39;s more complex because it has to handle entirely different things like audio streams, device discovery, and file transfers. We can roughly map it to the OSI model:</p><ol><li><strong>Lower Layers (Radio / Link Controller / LMP)</strong>: These are typically implemented in <strong>hardware</strong> or <strong>firmware</strong>. As kernel driver developers, we usually only interact with the layer above and don&#39;t touch this directly.</li><li><strong>HCI (Host Controller Interface)</strong>: This is the bridge for the kernel to talk to the hardware. It&#39;s also the main focus of this chapter.</li><li><strong>L2CAP (Logical Link Control and Adaptation Protocol)</strong>: Equivalent to the transport layer. It handles packet encapsulation, segmentation, reassembly, and provides multiplexing.</li><li><strong>Application Layer Protocols</strong>: Built on top of L2CAP, such as RFCOMM (serial port emulation), SDP (Service Discovery), BNEP (Network Encapsulation), and so on.</li></ol><blockquote><p><strong>💡 Analogy Time: Three-Stage Callback</strong></p><p>You can think of the Bluetooth protocol stack as an <strong>office building</strong>.</p><ol><li><p><strong>Establishing the Mapping</strong>:</p><ul><li>The <strong>lower-level hardware</strong> is the building&#39;s <strong>foundation and plumbing</strong> (electricity, water, ventilation).</li><li><strong>HCI</strong> is the <strong>front desk on the first floor</strong>. All letters and instructions must go through the front desk to be passed inside the building or sent out. The front desk doesn&#39;t care about the content of the letters; it only handles delivery.</li><li><strong>L2CAP</strong> is the <strong>mailroom on each floor</strong>. It&#39;s responsible for breaking large packages into smaller ones (segmentation) or assembling small ones into large ones (reassembly), and distributing them to different rooms (Sockets) based on door numbers (Channel IDs).</li><li><strong>Upper-layer protocols (like RFCOMM)</strong> are the <strong>specific rooms</strong>. Some rooms are dedicated to making phone calls (audio), others to sending emails (data transfer).</li></ul></li><li><p><strong>Revealing the Distance</strong>: But there&#39;s a flaw in this analogy: <strong>the front desk (HCI) usually isn&#39;t just one</strong>. Your computer might have a built-in Bluetooth adapter (USB) and also have a USB Bluetooth dongle plugged in. The HCI layer in the kernel has to manage both &quot;front desks&quot; simultaneously, and they are completely independent of each other. Additionally, L2CAP&#39;s &quot;mailroom&quot; is smarter than its real-world counterpart—it can negotiate packet sizes (MTU) and guarantee ordering—something a UDP postal system doesn&#39;t offer.</p></li><li><p><strong>Callback Verification</strong>: When you see the <code>hci_dev</code> structure later, you&#39;ll realize it&#39;s essentially the <strong>employee files for the front desk staff</strong>; and <code>l2cap_chan</code> is the <strong>mailroom&#39;s logbook</strong>. If the front desk goes on strike (driver fails to load), no matter how luxurious the rooms upstairs are, the building is paralyzed.</p></li></ol></blockquote><hr><h3 id="bluez-architecture-in-the-linux-kernel" tabindex="-1">BlueZ Architecture in the Linux Kernel <a class="header-anchor" href="#bluez-architecture-in-the-linux-kernel" aria-label="Permalink to &quot;BlueZ Architecture in the Linux Kernel&quot;">​</a></h3><p>BlueZ is not a single module; it&#39;s a suite of components.</p><p>The core code resides in the <code>net/bluetooth/</code> directory. If you don&#39;t want to tear your hair out, it&#39;s best to first understand these key players:</p><ul><li><strong>HCI Core</strong> (<code>hci_core.c</code>, <code>mgmt.c</code>): The brain of the Bluetooth controller, responsible for managing devices, connections, and scheduling.</li><li><strong>Socket Layer</strong> (<code>af_bluetooth.c</code>): Provides the BSD Socket API, allowing user-space programs to interact with Bluetooth just like TCP/IP.</li><li><strong>L2CAP</strong> (<code>l2cap*.c</code>): Handles logical link control; this is the mandatory path for all data streams.</li><li><strong>Driver Layer</strong> (<code>drivers/bluetooth/</code>): This is where the kernel shakes hands with the hardware. For example, <code>btusb.c</code> is the generic USB Bluetooth driver, and <code>hci_uart.c</code> is the serial port-based driver (used by many embedded boards).</li></ul><hr><h3 id="core-mechanism-breakdown-the-hci-layer" tabindex="-1">Core Mechanism Breakdown: The HCI Layer <a class="header-anchor" href="#core-mechanism-breakdown-the-hci-layer" aria-label="Permalink to &quot;Core Mechanism Breakdown: The HCI Layer&quot;">​</a></h3><p>HCI is the interface between the Host and the Controller. In the Linux kernel, every physical Bluetooth adapter is represented by an <code>struct hci_dev</code> structure.</p><p>This structure is massive (over 100 members) and serves as the &quot;ID card&quot; of the kernel&#39;s Bluetooth world. Let&#39;s look at a few of the most important fields:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hci_dev {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    char</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">            name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">   flags;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __u8            bus;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    bdaddr_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        bdaddr;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __u8            dev_type;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> work_struct      rx_work;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> work_struct      cmd_work;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff_head     rx_q;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff_head     raw_q;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff_head     cmd_q;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">open)(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hci_dev </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">hdev);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">close)(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hci_dev </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">hdev);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flush)(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hci_dev </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">hdev);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">send)(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">skb);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br></div></div><p>Let&#39;s do a &quot;manual&quot; breakdown of these fields:</p><ul><li><strong><code>bdaddr</code></strong>: This is the Bluetooth device&#39;s MAC address, 48 bits, globally unique. You can see it in <code>/sys/class/bluetooth/hci0/address</code>.</li><li><strong><code>bus</code></strong>: Where is this controller attached? Is it USB (<code>HCI_USB</code>)? PCIe (<code>HCI_PCI</code>)? Or UART (<code>HCI_UART</code>)? The driver fills this in during initialization.</li><li><strong><code>flags</code></strong>: Describes the state. For example, <code>HCI_UP</code> means the device is up, and <code>HCI_INIT</code> means it&#39;s initializing.</li><li><strong>Workqueues (<code>rx_work</code>, <code>cmd_work</code>)</strong>: Bluetooth handles a massive amount of asynchronous events. The kernel can&#39;t do too much work inside a hardware interrupt, so it puts received packets (<code>rx_q</code>) and pending commands (<code>cmd_q</code>) into queues, handing them over to <code>work_struct</code> for slow processing.</li><li><strong>Callback Functions (<code>open</code>, <code>close</code>, <code>send</code>)</strong>: This is the stage for driver developers. When you write a Bluetooth driver, your main job is to implement these functions to tell the kernel how to push data to your hardware.</li></ul><h4 id="the-four-journeys-of-a-packet" tabindex="-1">The Four Journeys of a Packet <a class="header-anchor" href="#the-four-journeys-of-a-packet" aria-label="Permalink to &quot;The Four Journeys of a Packet&quot;">​</a></h4><p>The HCI layer defines four types of packets, and these four types make up the entire conversation between the kernel and the hardware:</p><ol><li><strong><code>HCI_COMMAND_PKT</code></strong>: <strong>Commands</strong> sent from the host to the hardware. For example, &quot;start scanning for nearby devices&quot; or &quot;connect to XX:XX:XX:XX:XX:XX&quot;.</li><li><strong><code>HCI_ACLDATA_PKT</code></strong>: <strong>Asynchronous data</strong>. This is the main body of file transfers, corresponding to ACL links.</li><li><strong><code>HCI_SCODATA_PKT</code></strong>: <strong>Synchronous data</strong>. Primarily audio (SCO links).</li><li><strong><code>HCI_EVENT_PKT</code></strong>: <strong>Events</strong> sent from the hardware to the host. For example, &quot;found a device&quot; or &quot;connection completed&quot;.</li></ol><p><strong>The interaction flow looks like this</strong>: When you run <code>hcitool scan</code>, the kernel sends a <code>HCI_COMMAND_PKT</code> to the hardware via <code>hci_send_cmd()</code>. The hardware doesn&#39;t reply immediately; instead, after scanning and finding a device, it throws an <code>HCI_EVENT_PKT</code> back via an interrupt. This packet is received and processed by <code>hci_event_packet()</code>. There is no synchronous waiting here; it&#39;s all <strong>asynchronous callbacks</strong>.</p><hr><h3 id="upward-integration-the-l2cap-layer" tabindex="-1">Upward Integration: The L2CAP Layer <a class="header-anchor" href="#upward-integration-the-l2cap-layer" aria-label="Permalink to &quot;Upward Integration: The L2CAP Layer&quot;">​</a></h3><p>After the HCI layer pulls data from the hardware, it tosses it up to the L2CAP layer. L2CAP is the <strong>central nervous system</strong> of the Bluetooth protocol stack.</p><p>Its job isn&#39;t just transportation; it also includes:</p><ul><li><strong>Segmentation and Reassembly (SAR)</strong>: The lower-level HCI can only transmit packets of limited size. L2CAP is responsible for chopping your 1MB file into small chunks, sending them, and reassembling them on the other end.</li><li><strong>Multiplexing</strong>: You&#39;re listening to music over Bluetooth (A2DP) while moving your mouse (HID). L2CAP separates these two data streams using <strong>Channel IDs (CIDs)</strong>.</li></ul><p>The kernel uses <code>struct l2cap_chan</code> to represent a channel.</p><p>When HCI receives ACL data, it calls <code>hci_acldata_packet()</code> -&gt; <code>l2cap_recv_acldata()</code>. This function checks the packet header:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> l2cap_hdr {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __le16     len;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 长度</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __le16     cid;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 通道 ID</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__attribute__</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ((packed));</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><ul><li>If <code>cid == 0x0001</code>, it means this is the <strong>signaling channel</strong>, which handles control commands. For example, if another device wants to connect to you, the connection request (<code>L2CAP_CONN_REQ</code>) it sends will go through this channel. The kernel calls <code>l2cap_sig_channel()</code> to handle it, creates a new <code>l2cap_chan</code>, sets its state to <code>BT_OPEN</code>, and begins configuration.</li><li>If <code>cid</code> is any other value, the data is simply thrown to the corresponding Socket.</li></ul><hr><h3 id="hands-on-bnep-and-bluetooth-ip" tabindex="-1">Hands-on: BNEP and Bluetooth IP <a class="header-anchor" href="#hands-on-bnep-and-bluetooth-ip" aria-label="Permalink to &quot;Hands-on: BNEP and Bluetooth IP&quot;">​</a></h3><p>Bluetooth isn&#39;t just for transferring files or audio; it can also <strong>run IP</strong>. This is what <strong>BNEP (Bluetooth Network Encapsulation Protocol)</strong> does. It stuffs Ethernet frames into Bluetooth L2CAP channels, allowing you to connect two computers into a local area network over Bluetooth.</p><p>This is much more efficient than using PPP dial-up.</p><blockquote><p><strong>Returning to the &quot;office building&quot; analogy:</strong> BNEP is like laying a <strong>dedicated freight line</strong> between buildings. Whether you&#39;re sending a parcel (IP packet) or a letter, as long as you pack it into a shipping container (Ethernet frame) and toss it onto the freight line, it gets transported to the other building. The other building&#39;s mailroom (L2CAP) receives it, uncrates it, and distributes it to the specific rooms.</p></blockquote><h4 id="how-to-play-with-bnep" tabindex="-1">How to Play with BNEP? <a class="header-anchor" href="#how-to-play-with-bnep" aria-label="Permalink to &quot;How to Play with BNEP?&quot;">​</a></h4><p>You don&#39;t need to write code; Linux has a ready-made tool called <code>pand</code> (BlueZ PAN daemon).</p><p><strong>Server (acting as a router/hotspot):</strong></p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pand</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --listen</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --role=NAP</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># NAP = Network Access Point</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p><strong>Client (connecting to the hotspot):</strong></p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pand</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --connect</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 00:1A:7D:DA:71:13</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Once connected, a virtual network interface called <code>bnep0</code> will appear on your machine.</p><p>At this point, you can assign it an IP address just like a regular network interface:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ifconfig</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bnep0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.1.1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netmask</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 255.255.255.0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><h4 id="the-underlying-mechanism" tabindex="-1">The Underlying Mechanism <a class="header-anchor" href="#the-underlying-mechanism" aria-label="Permalink to &quot;The Underlying Mechanism&quot;">​</a></h4><p>When <code>pand --connect</code> is executed, the user-space program creates an L2CAP Socket and initiates a connection. After receiving the request, the server sends an IOCTL command of <code>BNEPCONNADD</code> to the kernel.</p><p>The function handling this command in the kernel is <code>bnep_add_connection()</code> (<code>net/bluetooth/bnep/core.c</code>). It does the following:</p><ol><li><strong>Creates a session</strong>: Generates a <code>bnep_session</code> object to record the connection state.</li><li><strong>Registers the network device</strong>: Calls <code>register_netdev()</code>, which is why you see <code>bnep0</code> appear in the <code>ip link</code> list.</li><li><strong>Starts a kernel thread</strong>: Creates a daemon thread called <code>kbnepd</code>. This thread is an infinite loop dedicated to receiving packets (<code>bnep_rx_frame</code>) and sending packets (<code>bnep_tx_frame</code>). <ul><li>Receive packet -&gt; Strip BNEP header -&gt; What remains is a pure Ethernet frame -&gt; Throw it to <code>netif_rx()</code> to enter the TCP/IP stack.</li><li>Send packet -&gt; Add BNEP header -&gt; Throw it to L2CAP -&gt; Throw it to HCI -&gt; Transmit.</li></ul></li></ol><hr><h3 id="full-data-flow-panorama" tabindex="-1">Full Data Flow Panorama <a class="header-anchor" href="#full-data-flow-panorama" aria-label="Permalink to &quot;Full Data Flow Panorama&quot;">​</a></h3><p>Let&#39;s trace how an IP packet is transmitted over Bluetooth:</p><ol><li><strong>Application Layer</strong>: <code>ping 192.168.1.2</code></li><li><strong>TCP/IP Stack</strong>: Constructs an ICMP packet, looks up the routing table, and determines it should go out through <code>bnep0</code>.</li><li><strong>BNEP</strong>: <code>bnep_netdev_xmit()</code> is called. It encapsulates the Ethernet frame into BNEP format.</li><li><strong>L2CAP</strong>: BNEP calls the L2CAP Socket send interface. L2CAP adds its own header (containing the CID) and may fragment the data.</li><li><strong>HCI</strong>: L2CAP calls the HCI send interface. HCI puts the packet on <code>cmd_q</code> or sends it directly to the hardware driver via <code>hci_send_frame()</code>.</li><li><strong>Hardware Driver</strong> (e.g., <code>btusb</code>): Converts the SKB into a USB URB and sends it flying to the USB Bluetooth dongle.</li><li><strong>Over-the-Air Interface</strong>: The Bluetooth chip turns the data into 2.4GHz radio waves and beams them to the other device.</li></ol><p>Reception is the reverse: Over-the-air -&gt; Hardware -&gt; HCI (<code>hci_rx_work</code>) -&gt; L2CAP (<code>l2cap_recv_acldata</code>) -&gt; BNEP (<code>bnep_rx_frame</code>) -&gt; TCP/IP Stack -&gt; Application.</p><hr><h3 id="extended-features-and-tools" tabindex="-1">Extended Features and Tools <a class="header-anchor" href="#extended-features-and-tools" aria-label="Permalink to &quot;Extended Features and Tools&quot;">​</a></h3><p>As the standard evolved, L2CAP is no longer the &quot;simple UDP&quot; it once was. Kernel 2.6.36 introduced <strong>Extended Features (eL2CAP)</strong>:</p><ul><li><strong>ERTM (Enhanced Retransmission Mode)</strong>: Reliable transmission with retransmissions. This is similar to TCP, not just UDP&#39;s &quot;best effort&quot; approach.</li><li><strong>Streaming Mode (SM)</strong>: A streaming mode that sacrifices reliability for real-time performance.</li><li><strong>FCS (Frame Check Sequence)</strong>: Adds a checksum to each packet to prevent data corruption.</li></ul><p>These features are mandatory for certain medical devices (HDP Profile) that have extremely high reliability requirements.</p><p>Finally, how do we troubleshoot issues? Don&#39;t expect <code>dmesg</code> to tell you everything; you need a Swiss Army knife:</p><ul><li><strong><code>hciconfig</code></strong>: View and configure HCI devices. <code>hciconfig hci0 up</code> brings up the device.</li><li><strong><code>hcitool</code></strong>: A debugging powerhouse. <code>hcitool scan</code> for scanning, <code>hcitool lescan</code> for scanning Low Energy devices.</li><li><strong><code>hcidump</code> / <code>btmon</code></strong>: Packet capture artifacts. Just like Wireshark, but they capture raw Bluetooth data over the air (or at the HCI layer). If you want to see why your mouse won&#39;t connect, just run <code>btmon</code> and it will be clear at a glance.</li></ul><hr><h3 id="chapter-echoes" tabindex="-1">Chapter Echoes <a class="header-anchor" href="#chapter-echoes" aria-label="Permalink to &quot;Chapter Echoes&quot;">​</a></h3><p>We completed a full traversal from the lower-level hardware drivers up to the upper-level IP protocol.</p><p>On the surface, this section is about &quot;how Bluetooth connects to the network,&quot; but in reality, we&#39;re looking at <strong>how a complete network subsystem collaborates across layers</strong>.</p><ul><li>The <strong>HCI</strong> layer abstracts away hardware differences (USB, UART, SDIO).</li><li>The <strong>L2CAP</strong> layer provides multiplexing and basic transport services.</li><li>The <strong>BNEP</strong> layer disguises the heterogeneous Bluetooth link as a standard Ethernet device, allowing the existing TCP/IP stack to run without modification.</li></ul><p>Remember the &quot;office building&quot; analogy from the beginning? Now you should understand that the Linux Bluetooth subsystem is complex because it not only has to manage the building&#39;s <strong>front desk</strong> (HCI) and <strong>mailroom</strong> (L2CAP), but also lay <strong>dedicated freight lines</strong> (BNEP) between buildings. Each layer has its own independent state machine and protocols, yet they tightly interlock.</p><p>In the next section, we&#39;ll jump from Personal Area Networks (PANs) to a broader, but more low-level wireless domain—<strong>IEEE 802.15.4</strong> and <strong>6LoWPAN</strong>. That is the world of the Internet of Things (IoT), where protocols are more streamlined and stingier with resources, but the design philosophy shares striking similarities with Bluetooth.</p>`,76)])])}const g=s(i,[["render",o]]);export{u as __pageData,g as default};
