import{_ as t,o,c as a,a as r}from"./chunks/framework.8tiMeCK-.js";const g=JSON.parse('{"title":"11.7  Further Reading","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch11_7.md","filePath":"en/notes/linux_kernel_debugging/ch11_7.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_debugging/ch11_7.md"};function s(i,e,d,l,h,c){return o(),a("div",null,[...e[0]||(e[0]=[r('<h1 id="_11-7-further-reading" tabindex="-1">11.7 Further Reading <a class="header-anchor" href="#_11-7-further-reading" aria-label="Permalink to &quot;11.7  Further Reading&quot;">​</a></h1><p>The main text ended with the previous section.</p><p>If you&#39;ve followed along with the hands-on exercises throughout this chapter, you now have a solid grasp of the core KGDB workflow—from configuring the kernel to attaching GDB, from breakpoints to scripting. This is enough to handle 90% of kernel debugging scenarios.</p><p>But the world of kernel debugging is vast. Even stretching this chapter to its limits, we&#39;ve barely scratched the surface. There are countless edge cases, advanced techniques, and hard-won lessons from the trenches that no textbook can cover.</p><p>At this point, the best teacher is the internet—not for searching error messages, but for seeing how others stumbled, and discovering the crazy things kernel developers have done to shoehorn a debugger into the kernel.</p><p>The list below contains links I believe deserve a permanent spot in your bookmarks bar. Some are serious, some are hardcore, and some... well, are just plain fun.</p><hr><h2 id="📚-core-documentation-and-kernel-guides" tabindex="-1">📚 Core Documentation and Kernel Guides <a class="header-anchor" href="#📚-core-documentation-and-kernel-guides" aria-label="Permalink to &quot;📚 Core Documentation and Kernel Guides&quot;">​</a></h2><p>First, while official documentation can sometimes be dry, it is the single source of truth.</p><p><strong><a href="https://www.kernel.org/doc/html/v5.10/dev-tools/kgdb.html" target="_blank" rel="noreferrer">Kernel Doc: Using kgdb, kdb and the kernel debugger internals</a></strong> This is the bible of KGDB. No matter what blogs say, you&#39;ll eventually need to come back here to verify parameters. It provides the clearest explanation of how to switch between KDB (the built-in minimalist debugger) and KGDB, and the details of how the underlying locks are implemented.</p><p><strong><a href="https://lwn.net/Articles/374633/" target="_blank" rel="noreferrer">Merging kdb and kgdb (LWN, 2010)</a></strong> LWN&#39;s article quality is consistently excellent. This 2010 article looks back at how the KDB and KGDB subsystems went from being separate rivals to merging into one. Understanding this history explains why current configurations have both <code>CONFIG_KGDB</code> and <code>CONFIG_KDB</code>, and how they share the underlying I/O driver.</p><p><strong><a href="https://manpages.org/kdb/8" target="_blank" rel="noreferrer">Man page on kdb(8)</a></strong> If you don&#39;t want to fire up two machines and prefer typing commands directly over a serial console, KDB is your only choice. This man page is your map when you get lost.</p><hr><h2 id="🛠️-hardcore-practice-and-embedded-platforms" tabindex="-1">🛠️ Hardcore Practice and Embedded Platforms <a class="header-anchor" href="#🛠️-hardcore-practice-and-embedded-platforms" aria-label="Permalink to &quot;🛠️ Hardcore Practice and Embedded Platforms&quot;">​</a></h2><p>There&#39;s a chasm between theory and practice, and the resources below are the bridges.</p><p><strong><a href="https://www.youtube.com/watch?v=HBOwoSyRmys" target="_blank" rel="noreferrer">Using Serial kdb / kgdb to Debug the Linux Kernel - Douglas Anderson (Google, 2019)</a></strong> If you think we simply set up a basic serial port in this chapter, Douglas&#39;s talk will show you what a &quot;complex environment&quot; really looks like. He covers how to multiplex the Console and GDB over a single physical serial port. On real boards, serial ports are a scarce resource. Being able to watch kernel logs while debugging is the hallmark of a practical embedded engineer.</p><p><strong><a href="https://eastrivervillage.com/KGDB-KDB-over-serial-with-RaspberryPi/" target="_blank" rel="noreferrer">KGDB/KDB over serial with Raspberry Pi</a></strong> The Raspberry Pi is the most accessible ARM experimentation platform. This tutorial has a bit of a Yocto flavor, but it thoroughly demonstrates how to wire things up on real hardware. When you jump from QEMU&#39;s virtual world to real hardware and find your serial output is garbled due to voltage mismatches, this article can save your life.</p><p><strong><a href="https://kaiwantech.wordpress.com/2013/07/04/a-kdb-kgdb-session-on-the-popular-raspberry-pi-embedded-linux-board/" target="_blank" rel="noreferrer">A KDB / KGDB SESSION ON THE POPULAR RASPBERRY PI</a></strong> Though it&#39;s a 2013 article, many of the underlying mechanisms (like the early ARM boot process) haven&#39;t changed much. Seeing how veterans debugged kernels in an era without fancy tools is almost a spiritual experience.</p><hr><h2 id="🕵️‍♂️-advanced-tricks-and-toolbox" tabindex="-1">🕵️‍♂️ Advanced Tricks and Toolbox <a class="header-anchor" href="#🕵️‍♂️-advanced-tricks-and-toolbox" aria-label="Permalink to &quot;🕵️‍♂️ Advanced Tricks and Toolbox&quot;">​</a></h2><p>When you feel regular breakpoints aren&#39;t enough, here are some fun things to explore.</p><p><strong><a href="https://undo.io/resources/gdb-watchpoint/5-ways-reduce-debugging-hours/" target="_blank" rel="noreferrer">5 Easy Ways to Reduce Your Debugging Hours (Undo.io)</a></strong> This article isn&#39;t just about KGDB; it&#39;s about debugging mindset. How do you cover more code paths with fewer breakpoints? How do you leverage watchpoints instead of blindly using print statements? Most of the techniques mentioned here are universal, useful whether you&#39;re debugging the kernel or user-space applications.</p><p><strong><a href="https://lwn.net/Articles/600359/" target="_blank" rel="noreferrer">Debugging ARM kernels using fast interrupts (LWN)</a></strong> This is advanced content even among advanced topics. Normal FIQ handling is already complex; if you want to debug within a FIQ context, this is your reference.</p><hr><h2 id="🎓-external-perspectives-and-gdb-tutorial-series" tabindex="-1">🎓 External Perspectives and GDB Tutorial Series <a class="header-anchor" href="#🎓-external-perspectives-and-gdb-tutorial-series" aria-label="Permalink to &quot;🎓 External Perspectives and GDB Tutorial Series&quot;">​</a></h2><p>Step outside the kernel circle and see what GDB itself can do.</p><p><strong><a href="https://developers.redhat.com/blog/2021/04/30/the-gdb-developers-gnu-debugger-tutorial-part-1-getting-started-with-the-debugger/" target="_blank" rel="noreferrer">Red Hat Developer series on GDB</a></strong> A three-part GDB tutorial series produced by Red Hat. It covers everything from getting started to the principles of <code>debuginfo</code>, and even printf-style debugging. Although it focuses mainly on user-space programs, GDB&#39;s command syntax and Python extension mechanisms are exactly the same in KGDB. After reading this, your understanding of GDB will elevate from &quot;knowing how to use it&quot; to &quot;understanding it.&quot;</p><p><strong><a href="https://sourceware.org/gdb/onlinedocs/gdb/TUI.html" target="_blank" rel="noreferrer">Using GDB in TUI mode (Official Manual)</a> &amp; <a href="https://www.youtube.com/watch?v=mm0b_H0KIRw" target="_blank" rel="noreferrer">Debug faster with gdb layouts (Video)</a></strong> We mentioned TUI in Section 11.6. If you want to customize that window layout, or figure out why your TUI windows are flickering, look here. The video tutorial demonstrates highly efficient &quot;keyboard-driven&quot; operations that are incredibly satisfying to watch.</p><hr><h2 id="🐛-common-issues-and-firefighting-manual" tabindex="-1">🐛 Common Issues and Firefighting Manual <a class="header-anchor" href="#🐛-common-issues-and-firefighting-manual" aria-label="Permalink to &quot;🐛 Common Issues and Firefighting Manual&quot;">​</a></h2><p>Finally, when you&#39;ve pressed every key and GDB still won&#39;t stop, go here for answers.</p><p><strong><a href="https://stackoverflow.com/a/36861909/779269" target="_blank" rel="noreferrer">StackOverflow: KGDB remote debugging connection issue</a></strong> Failing to connect is the first hurdle for KGDB beginners. This thread discusses the various arcane issues with USB-to-serial adapters.</p><p><strong><a href="https://stackoverflow.com/questions/70874764/breakpoints-not-working-for-gdb-while-debugging-remote-arm-target-on-qemu" target="_blank" rel="noreferrer">Breakpoints not being hit... (QEMU)</a></strong> If you set a breakpoint in QEMU and the program just flies right past it, it&#39;s highly likely an issue with <code>CONFIG_STRICT_KERNEL_RWX</code> or compiler optimizations. Someone in this Q&amp;A has fallen into the exact same trap as you.</p><hr><h2 id="🎵-bonus-easter-egg" tabindex="-1">🎵 Bonus Easter Egg <a class="header-anchor" href="#🎵-bonus-easter-egg" aria-label="Permalink to &quot;🎵 Bonus Easter Egg&quot;">​</a></h2><p><strong><a href="https://www.gnu.org/music/gdb-song.en.html" target="_blank" rel="noreferrer">The GDB Song (GNU)</a></strong> When you&#39;re tired of coding, give this a listen. It&#39;s genuinely... full of nostalgia.</p><blockquote><p><em>&quot;I&#39;m a hacker, and that&#39;s what I do...&quot;</em></p><p>Don&#39;t tell anyone I recommended this.</p></blockquote><hr><p><strong><a href="https://lwn.net/Articles/789641/" target="_blank" rel="noreferrer">A kernel debugger in Python: drgn (LWN)</a></strong> This last one points to the future. <code>drgn</code> is a debugger written entirely in Python. It doesn&#39;t rely on GDB and reads kernel memory directly. Although we&#39;ve been focusing on KGDB, keeping an eye on this new technology will show you another possibility for system-level debugging.</p><hr><p><em>(End of this section)</em></p><hr><h1 id="chapter-11-echoes" tabindex="-1">Chapter 11 Echoes <a class="header-anchor" href="#chapter-11-echoes" aria-label="Permalink to &quot;Chapter 11 Echoes&quot;">​</a></h1><p>Remember the question we asked at the beginning of this chapter—when the kernel is flying high, how do we actually grab it?</p><p>In this chapter, we essentially built an invisible &quot;umbilical cord.&quot;</p><p>One end connects to your host machine where you&#39;re typing code, and the other end connects to that lonely kernel running inside QEMU (or on a real ARM board). In the past, you could only throw one-way notes into it via <code>printk</code> and pray they would drift back in the logs.</p><p>Things are different now.</p><p>Through KGDB, you are no longer just an observer; you are a ghost in the kernel world. You can freeze time at will, lift the memory cover to peek at variables, and even command it to &quot;rewind.&quot; We learned how to understand the machine&#39;s layout through the massive symbol table that is <code>vmlinux</code>, and how to automate tedious module loading processes using Python scripts like <code>lx-symbols</code>.</p><p>More importantly, we built an intuition: <strong>Software doesn&#39;t lie; it only executes.</strong> When something feels &quot;unscientific,&quot; it usually means the symbol table is wrong, or optimizations have hidden the variables. The process of solving these problems is the evolution from &quot;black-box debugging&quot; to &quot;white-box dissection.&quot;</p><p>But this is only the beginning.</p><p>In the next chapter, the final part of this book, we will push these debugging capabilities to the limit. We will see how to use these tools to track down the most bizarre, hardest-to-reproduce system-level bugs, and witness how top-tier system maintainers can spot a missing semicolon in a screen full of errors at a single glance.</p><p>Are you ready? We are about to reach the end of the final leg.</p><hr><p><em>(The book is about to conclude)</em></p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: Understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: Understanding&quot;">​</a></h3><p><strong>Question</strong>: When using QEMU to emulate an ARM target system for kernel debugging, why can&#39;t we directly load the compressed kernel image zImage into GDB for source-level debugging? If we need to set a breakpoint early in the boot process (e.g., at the <code>start_kernel</code> function) to wait for the debugger to connect, how should the QEMU startup parameters be configured, and what is the purpose of the &#39;nokaslr&#39; kernel boot parameter?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: The reason we cannot directly load zImage is that zImage is a compressed, self-extracting image that does not contain complete debugging symbol information. GDB requires an uncompressed ELF format file (vmlinux) with debugging symbols for source-level debugging.</p><p>QEMU startup parameter configuration: Use the <code>-S -s</code> parameter. Among these, <code>-S</code> causes the CPU to freeze at startup, and <code>-s</code> is shorthand for <code>-gdb tcp::1234</code>, which enables the GDB remote debugging port.</p><p>The purpose of the &#39;nokaslr&#39; parameter is to disable Kernel Address Space Layout Randomization. If KASLR is enabled, the addresses of kernel code and data will randomize on every boot, causing the static symbol addresses in GDB to mismatch the actual runtime addresses. This makes it impossible to set breakpoints correctly or inspect variables.</p><p><strong>Analysis</strong>: This question tests the understanding of the basic KGDB debugging environment. Debugging the kernel is essentially debugging a process, which requires a symbol table. zImage self-extracts at runtime, but GDB cannot parse symbols inside a compressed payload, so it must be used in conjunction with vmlinux. Regarding startup parameters, QEMU&#39;s <code>-S</code> pauses the machine, buying time for GDB to connect; <code>nokaslr</code> ensures the memory layout is &quot;fixed,&quot; which is a prerequisite for static analysis tools to work.</p></details><h3 id="exercise-2-application" tabindex="-1">Exercise 2: Application <a class="header-anchor" href="#exercise-2-application" aria-label="Permalink to &quot;Exercise 2: Application&quot;">​</a></h3><p><strong>Question</strong>: Suppose you are debugging a kernel module named <code>my_led</code> in a KGDB environment. The module is already loaded into the target kernel, but you find that GDB cannot hit the breakpoint at the <code>my_led_init</code> function, or the breakpoint is invalid. Given the <code>/sys/module</code> directory under <code>sections</code>, write the complete command steps to load the module&#39;s symbol table into GDB (assuming the .text section address is 0xbf012000).</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: The steps are as follows:</p><ol><li><p><strong>Find the section addresses</strong>: Check the addresses of each section on the target system. <code>cat /sys/module/my_led/sections/.text</code> (Output: 0xbf012000)</p></li><li><p><strong>Load the symbols</strong>: Use the <code>add-symbol-file</code> command in the host machine&#39;s GDB, specifying the addresses of key sections simultaneously to ensure GDB can correctly locate code and data. <code>add-symbol-file path/to/my_led.ko 0xbf012000 -s .data 0xbf01a000 -s .bss 0xbf01c000</code><em>(Note: In actual use, replace the .data and .bss addresses with the actual values seen from the cat command. If .data/.bss are not specified, you may not be able to inspect global variables.)</em></p></li></ol><p><strong>Analysis</strong>: This question tests the practical skill of debugging LKMs. Kernel modules are loaded dynamically, and GDB cannot automatically know their load locations. Sysfs exposes this information through the <code>sections</code> directory. The core command is <code>add-symbol-file</code>. A common mistake here is providing only the <code>.text</code> address (function address). If the module uses global variables, failing to provide the <code>.data</code> and <code>.bss</code> section addresses will cause variable inspections to display <code>&lt;optimized out&gt;</code> or incorrect values.</p></details><h3 id="exercise-3-thinking" tabindex="-1">Exercise 3: Thinking <a class="header-anchor" href="#exercise-3-thinking" aria-label="Permalink to &quot;Exercise 3: Thinking&quot;">​</a></h3><p><strong>Question</strong>: When configuring a kernel to support KGDB, it is generally recommended to disable the <code>CONFIG_STRICT_KERNEL_RWX</code> option (kernel code segment read-only, data segment non-executable). Please analyze why traditional software breakpoints might fail if this option is not disabled. In this case, besides modifying the kernel configuration, what alternative GDB command can be used, and what is its underlying principle?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Principle analysis: The implementation principle of software breakpoints is that the CPU executes a <code>INT 3</code> (x86) or a specific illegal instruction at the target location. This requires modifying the code segment in memory. If <code>CONFIG_STRICT_KERNEL_RWX</code> is enabled, the kernel code segment is forcibly marked as read-only by hardware. Any attempt to write to the code segment (including GDB inserting a breakpoint instruction) will trigger a page fault, causing a system crash or breakpoint write failure.</p><p>Alternative solution and principle: We can use <strong>hardware breakpoints</strong>. Use the <code>hbreak</code> (hardware breakpoint) command in GDB.</p><p>Principle: Hardware breakpoints utilize the CPU&#39;s internal debug registers (such as DR0-DR7 on x86) to set up address monitoring. When the CPU&#39;s execution flow accesses a specific address, the hardware automatically triggers an exception. This method does not require modifying target memory and is therefore unaffected by RWX permissions. However, the number of hardware breakpoints is typically very limited (e.g., x86 usually only has 4).</p><p><strong>Analysis</strong>: This question tests the understanding of the underlying principles of debugging mechanisms (memory and CPU features). Software breakpoints rely on memory writes and fail when encountering write protection. This guides the thinker toward mechanisms that don&#39;t rely on memory modification—namely, utilizing the CPU&#39;s hardware debug registers. This is essential knowledge for advanced debuggers and also explains why <code>hbreak</code> is more important in embedded or security-hardened environments.</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>Kernel debugging faces a dilemma that user-space debuggers cannot reach: when code runs at Ring 0 privilege or a system crash prevents output buffers from flushing, traditional debugging methods completely fail. The key to solving this problem is KGDB (Kernel Debugger), which adopts a client-server architecture to split GDB&#39;s functionality: the Host runs a feature-rich GDB client as the commander, while the Target runs a lightweight KGDB server inside the kernel as the frontline scout. The two communicate over a network or serial port, enabling source-level kernel pausing and inspection.</p><p>The key to setting up a KGDB environment lies in correctly distinguishing between <code>vmlinux</code> and <code>bzImage</code>: the former is the uncompressed ELF image containing complete debugging symbols, which must be provided to GDB to read the symbol table; the latter is the compressed image loaded by the bootloader for system startup. To make the kernel support debugging, you must forcibly enable <code>CONFIG_DEBUG_INFO</code> (generate debugging symbols) and <code>CONFIG_KGDB</code> (embed the GDB server) at compile time. It is also recommended to disable <code>CONFIG_STRICT_KERNEL_RWX</code> to prevent memory protection mechanisms from blocking software breakpoint writes, and add the <code>nokaslr</code> boot parameter to disable address randomization, ensuring instruction addresses are fixed and predictable.</p><p>In practice, debugging dynamically loaded kernel modules is one of the biggest challenges because GDB cannot automatically predict where a module will be loaded in memory. Debuggers must read the files under <code>/sys/module/&lt;module_name&gt;/sections/</code> to obtain the actual load addresses of the module&#39;s sections and use the <code>add-symbol-file</code> command to manually inform GDB of these addresses. To handle extreme cases where a module crashes immediately upon loading, you can pre-set a hardware breakpoint at the kernel&#39;s <code>do_init_module</code> function entry, or use a delayed execution mechanism to buy yourself a precious time window to connect the debugger and load the symbols.</p><p>KGDB provides watchpoint functionality that is more powerful than traditional breakpoints. By utilizing the CPU&#39;s hardware debug registers (such as DR0-DR3 on x86), it can pause execution the instant a specified variable is read or written. This &quot;data breakpoint&quot; is extremely effective for tracking bizarre memory corruption or &quot;phantom&quot; pointer modifications in the kernel, directly pinpointing the exact line of code that modified the variable. Additionally, by leveraging the Python GDB scripts included in the Linux kernel source code (enabled via <code>CONFIG_GDB_SCRIPTS</code>), we can directly execute commands like <code>lx-dmesg</code> or <code>lx-lsmod</code> within GDB to extract system state directly from memory without resuming kernel execution, greatly improving debugging efficiency.</p>',71)])])}const m=t(n,[["render",s]]);export{g as __pageData,m as default};
