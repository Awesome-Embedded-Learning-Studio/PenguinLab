import{_ as t,o as n,c as a,a as o}from"./chunks/framework.8tiMeCK-.js";const p=JSON.parse('{"title":"Chapter 6: Who Touched My Memory? (Part 1) — Catching Ghosts in the Kernel Stack","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch06_1.md","filePath":"en/notes/linux_kernel_debugging/ch06_1.md","lastUpdated":null}'),s={name:"en/notes/linux_kernel_debugging/ch06_1.md"};function i(r,e,l,h,d,u){return n(),a("div",null,[...e[0]||(e[0]=[o(`<h1 id="chapter-6-who-touched-my-memory-part-1-—-catching-ghosts-in-the-kernel-stack" tabindex="-1">Chapter 6: Who Touched My Memory? (Part 1) — Catching Ghosts in the Kernel Stack <a class="header-anchor" href="#chapter-6-who-touched-my-memory-part-1-—-catching-ghosts-in-the-kernel-stack" aria-label="Permalink to &quot;Chapter 6: Who Touched My Memory? (Part 1) — Catching Ghosts in the Kernel Stack&quot;">​</a></h1><h2 id="_6-1-preparation-and-slub-debugging-basics" tabindex="-1">6.1 Preparation and SLUB Debugging Basics <a class="header-anchor" href="#_6-1-preparation-and-slub-debugging-basics" aria-label="Permalink to &quot;6.1  Preparation and SLUB Debugging Basics&quot;">​</a></h2><h3 id="_6-1-1-introduction-the-invisible-destroyer" tabindex="-1">6.1.1 Introduction: The Invisible Destroyer <a class="header-anchor" href="#_6-1-1-introduction-the-invisible-destroyer" aria-label="Permalink to &quot;6.1.1  Introduction: The Invisible Destroyer&quot;">​</a></h3><p>There is a class of bugs more frustrating than logic errors, and far harder to reproduce.</p><p>When your code logic goes astray, the program usually crashes or produces an incorrect result — at least you know something is wrong. But when memory is corrupted, the world is often silent. Your driver might only occasionally hang, the system might suddenly panic after running for three days, or a variable&#39;s value might change for no apparent reason — as if an invisible hand were secretly modifying your code while you sleep.</p><p>This is memory corruption. It is the kernel developer&#39;s nightmare.</p><p>To catch these ghosts, we need a few weapons in our arsenal. In the previous chapter, we discussed KASAN and UBSAN — two heavy artillery pieces. While powerful, they can sometimes be too heavy, carrying significant performance overhead and complex configuration. In this chapter, we&#39;ll pull back our focus and look at the kernel&#39;s built-in, lighter-weight detection mechanisms: the Slub Allocator&#39;s debugging features, and the dedicated memory leak detection tool, kmemleak.</p><p>Our mission in this chapter is to build a &quot;memory debugging intuition&quot;: when you suspect a memory issue, how to quickly pinpoint the culprit through SLUB&#39;s feedback; when you suspect a memory leak, how to let kmemleak help you drag out that forgotten pointer.</p><p>Ready? Let&#39;s start ghost hunting.</p><hr><h3 id="_6-1-2-preparing-the-lab-environment" tabindex="-1">6.1.2 Preparing the Lab Environment <a class="header-anchor" href="#_6-1-2-preparing-the-lab-environment" aria-label="Permalink to &quot;6.1.2  Preparing the Lab Environment&quot;">​</a></h3><p>First, as always, the environment must be set up.</p><p>The lab environment configuration for this chapter is exactly the same as what we covered in Chapter 1, &quot;Introduction to Debugging Software.&quot; If you&#39;ve already built a debug kernel following the previous chapters, you&#39;re ready to jump right in; if you haven&#39;t, or if you&#39;ve switched to a new machine, go back and review the checklist in Chapter 1.</p><p>For easy reference, all the example code is available in this book&#39;s GitHub repository:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>https://github.com/PacktPublishing/Linux-Kernel-Debugging</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><code>git clone</code> it and place it in your virtual machine — we&#39;ll need it shortly.</p><hr><h3 id="_6-1-3-why-do-we-need-slub-debugging" tabindex="-1">6.1.3 Why Do We Need SLUB Debugging? <a class="header-anchor" href="#_6-1-3-why-do-we-need-slub-debugging" aria-label="Permalink to &quot;6.1.3  Why Do We Need SLUB Debugging?&quot;">​</a></h3><p>Before diving into the configuration, let&#39;s agree on one thing: what exactly are we debugging?</p><p>Memory corruption in the kernel takes countless forms, but it ultimately boils down to a few familiar faces:</p><ul><li><strong>UMR (Uninitialized Memory Read)</strong>: Reading memory that hasn&#39;t been initialized. You get garbage values, which are typically random, making the bugs random as well.</li><li><strong>UAF (Use After Free)</strong>: Using memory after it has been freed. It&#39;s like selling a house but keeping the key, then going back one night to sleep, only to find the house now belongs to someone else.</li><li><strong>UAR (Use After Return)</strong>: Accessing stack memory via a pointer after the function has returned and that stack frame is no longer valid.</li><li><strong>Double Free</strong>: Freeing the same block of memory twice.</li><li><strong>OOB (Out Of Bounds)</strong>: Out-of-bounds access. An array index is too large, a linked list pointer goes wild, and you step into someone else&#39;s territory.</li></ul><p><strong>These issues are incredibly common.</strong> They will find you before you even have a chance to write elegant code. We already know that KASAN is the silver bullet against them, but sometimes you want something lighter-weight — something you can even occasionally enable in a production environment.</p><p>That is exactly why the Slub Allocator&#39;s built-in debugging features exist.</p><p><strong>Recall the kernel&#39;s memory hierarchy:</strong></p><p>At the very bottom of the kernel is the page allocator, also known as the Buddy System. It manages large chunks of physical pages. However, the kernel frequently needs small chunks of memory — structs of a few dozen or a few hundred bytes. If you request a 4KB page from the Buddy System just to hold a 64-byte struct, that&#39;s a massive waste and leads to severe <strong>internal fragmentation</strong>.</p><p>To solve this, the kernel adds a layer on top of the page allocator: the <strong>Slab Allocator</strong>.</p><p>You can think of Slab as a &quot;parts warehouse.&quot; The Buddy System handles moving &quot;shipping containers&quot; (whole pages), while Slab breaks the containers open and neatly arranges the &quot;screws&quot; and &quot;washers&quot; (small objects) on shelves. When a kernel driver needs a screw, Slub simply grabs one off the shelf and hands it over; when it&#39;s done, the screw goes back on the shelf. This is both fast and space-efficient.</p><p><strong>But in the modern Linux kernel, this &quot;warehouse&quot; has three different implementations:</strong></p><ol><li><strong>SLAB</strong>: The earliest implementation. Classic, but outdated.</li><li><strong>SLUB (Unqueued Allocator)</strong>: The current default. More modern in design, better performance, and the star of this chapter.</li><li><strong>SLOB</strong>: Designed for extremely memory-constrained embedded systems; almost never seen on standard desktops or servers.</li></ol><p><strong>⚠️ Note:</strong> Everything that follows in this chapter applies specifically to the <strong>SLUB</strong> implementation. If you selected SLAB or SLOB in your kernel configuration, the parameters and paths below may not match up. Modern Linux distributions (Ubuntu, Fedora, etc.) default to SLUB almost universally (config option <code>CONFIG_SLUB</code>); you can verify this in <code>General setup | Choose SLAB allocator</code>.</p><p>Our goal now is to install &quot;surveillance cameras&quot; in this warehouse — to see who took a screw and didn&#39;t return it, or who kicked over the shelving unit.</p><hr><h3 id="_6-1-4-configuring-the-kernel-to-enable-slub-debugging" tabindex="-1">6.1.4 Configuring the Kernel to Enable SLUB Debugging <a class="header-anchor" href="#_6-1-4-configuring-the-kernel-to-enable-slub-debugging" aria-label="Permalink to &quot;6.1.4  Configuring the Kernel to Enable SLUB Debugging&quot;">​</a></h3><p>Alright, before we get our hands dirty, let&#39;s survey the landscape. We need to adjust some kernel configuration options.</p><p>SLUB provides a full suite of debugging support, primarily controlled by two config options. Open your <code>make menuconfig</code> (or directly modify <code>.config</code>), and look for these two locations:</p><h4 id="_1-enable-the-debug-support-master-switch" tabindex="-1">1. Enable the Debug Support Master Switch <a class="header-anchor" href="#_1-enable-the-debug-support-master-switch" aria-label="Permalink to &quot;1. Enable the Debug Support Master Switch&quot;">​</a></h4><p>Path:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>General setup | Enable SLUB debugging support</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Config option:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CONFIG_SLUB_DEBUG=y</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><strong>This step is mandatory.</strong> Enabling it grants you the following abilities:</p><ul><li>Viewing detailed status of all slab caches under <code>/sys/kernel/debug/slab</code> (or <code>/sys/slab</code> on some systems).</li><li>Runtime cache validation.</li><li>Using the advanced debugging features we&#39;ll cover in the next section (Red Zone, Poisoning, etc.).</li></ul><p><strong>A quick fact:</strong> If you enable <strong>Generic KASAN</strong> (<code>CONFIG_KASAN</code>), the kernel will <strong>automatically select</strong> this option. This is one of the reasons KASAN slows down your system — it&#39;s already doing a lot of work under the hood.</p><h4 id="_2-enable-by-default" tabindex="-1">2. Enable by Default? <a class="header-anchor" href="#_2-enable-by-default" aria-label="Permalink to &quot;2. Enable by Default?&quot;">​</a></h4><p>Path:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Kernel hacking | Memory Debugging | SLUB debugging on by default</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Config option:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CONFIG_SLUB_DEBUG_ON=y</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This option is a double-edged sword.</p><p>If set to <code>y</code>, SLUB debugging will be fully enabled at kernel boot. That sounds great — like installing 24/7 surveillance in your system. But the cost is enormous — <strong>the performance penalty will be very noticeable</strong>. For production environments or everyday development debugging, this is usually too heavy.</p><p><strong>The recommended strategy is:</strong> Leave <code>CONFIG_SLUB_DEBUG_ON</code> disabled (it&#39;s off by default), and turn on <code>CONFIG_SLUB_DEBUG</code>. This way, the debugging capabilities are <strong>mounted on the system but not activated</strong>.</p><p>When you need to hunt a bug, you enable them on-demand via the kernel boot parameter <code>slub_debug</code>. That&#39;s the flexible approach.</p><hr><h4 id="verifying-the-configuration" tabindex="-1">Verifying the Configuration <a class="header-anchor" href="#verifying-the-configuration" aria-label="Permalink to &quot;Verifying the Configuration&quot;">​</a></h4><p>After changing the configuration, rebuild your kernel (if you haven&#39;t already) and reboot into the new kernel.</p><p>Once in the system, use <code>grep</code> to confirm the current configuration state:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> SLUB_DEBUG</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /boot/config-5.10.60-dbg02</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CONFIG_SLUB_DEBUG</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">y</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># CONFIG_SLUB_DEBUG_ON is not set</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>The <code># CONFIG_SLUB_DEBUG_ON is not set</code> here is critical.</p><p>It means: <strong>I have a gun, but it&#39;s not loaded yet.</strong></p><p>This &quot;on standby without over-intervening&quot; state is the foundation for our hands-on work ahead. If you accidentally turned on <code>CONFIG_SLUB_DEBUG_ON</code>, you&#39;ll noticeably feel your mouse lagging and file reads/writes slowing down — don&#39;t say I didn&#39;t warn you.</p><hr><h3 id="_6-1-5-next-step-the-magic-of-slub-debug" tabindex="-1">6.1.5 Next Step: The Magic of slub_debug <a class="header-anchor" href="#_6-1-5-next-step-the-magic-of-slub-debug" aria-label="Permalink to &quot;6.1.5  Next Step: The Magic of slub_debug&quot;">​</a></h3><p>Now that the gun is ready, it&#39;s time to learn how to pull the trigger.</p><p>The core of SLUB lies in the kernel command-line parameter <strong><code>slub_debug</code></strong>. It allows you to finely control which caches get which debugging methods, all without recompiling the kernel.</p><p>The detailed official documentation for this parameter is available here: <code>https://www.kernel.org/doc/html/latest/vm/slub.html</code></p><p>Of course, reading the raw documentation can be a bit dry. In the next section, we&#39;ll translate those obscure parameters into practical scenarios. Through a few concrete examples, we&#39;ll show you exactly how <code>slub_debug</code> helps us catch the culprits behind &quot;out-of-bounds access&quot; or &quot;use-after-free.&quot;</p><p>Keep this intuition in mind for now: <strong>SLUB debugging is about planting landmines around memory objects</strong> — step out of bounds, and it blows up. In the next section, we&#39;ll plant those mines.</p>`,67)])])}const g=t(s,[["render",i]]);export{p as __pageData,g as default};
