import{_ as i,o as s,c as n,a}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"Chapter 4: Hello, Kernel — Linux Kernel Modules and Kernel Architecture Basics","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_programming/ch04.md","filePath":"en/notes/linux_kernel_programming/ch04.md","lastUpdated":null}'),t={name:"en/notes/linux_kernel_programming/ch04.md"};function o(l,e,r,d,h,p){return s(),n("div",null,[...e[0]||(e[0]=[a(`<h1 id="chapter-4-hello-kernel-—-linux-kernel-modules-and-kernel-architecture-basics" tabindex="-1">Chapter 4: Hello, Kernel — Linux Kernel Modules and Kernel Architecture Basics <a class="header-anchor" href="#chapter-4-hello-kernel-—-linux-kernel-modules-and-kernel-architecture-basics" aria-label="Permalink to &quot;Chapter 4: Hello, Kernel — Linux Kernel Modules and Kernel Architecture Basics&quot;">​</a></h1><h2 id="chapter-prologue-privilege-and-boundaries" tabindex="-1">Chapter Prologue: Privilege and Boundaries <a class="header-anchor" href="#chapter-prologue-privilege-and-boundaries" aria-label="Permalink to &quot;Chapter Prologue: Privilege and Boundaries&quot;">​</a></h2><p>Imagine this scenario: you&#39;re sitting in front of a running Linux machine with a freshly written piece of C code. In user space, you can freely compile and run it, watch it print &quot;Hello, world&quot; in the terminal, and exit — if it crashes, no big deal, the worst that happens is the process gets killed, and the operating system remains rock solid.</p><p>Now, I want you to imagine that code as a bullet about to be injected into the heart of the operating system.</p><p>This isn&#39;t an exaggeration. In the world of Linux kernel programming, once code enters kernel space, it is no longer a supervised process — it <em>is</em> the operating system. It runs at the highest privilege level, with the ability to access all physical memory, directly manipulate hardware ports, and even intercept system calls. There are no protective mechanisms to pull you back here; if a pointer goes astray, the consequence isn&#39;t a segmentation fault — it&#39;s a system crash or reboot. This is exactly why kernel development is both thrilling and terrifying.</p><p>But the traditional kernel development workflow is downright brutal: if you want to change a single line of driver code, you have to reconfigure the entire kernel tree, spend hours recompiling <code>bzImage</code>, and then reboot the machine. This &quot;modify-compile-reboot&quot; endless loop is enough to drain all your creativity.</p><p>Is there a way to load and unload code as flexibly as we do with user-space programs, while retaining the sheer power of kernel mode?</p><p>This is the core problem we&#39;ll solve in this chapter. Linux&#39;s answer is the <strong>LKM (Loadable Kernel Module)</strong> framework. It shatters the stereotype of the kernel as a &quot;static monolith&quot; and breathes a soul of dynamic extensibility into Linux.</p><p>In this chapter, we&#39;ll build a foundational understanding of kernel architecture — understanding what the &quot;boundary&quot; dividing user and kernel space truly means. Then, we&#39;ll write our first kernel module with our own hands, watching it transform from a simple <code>.c</code> file into a <code>.ko</code> binary, get fired into kernel memory by the <code>insmod</code> command, and leave its first cry in the <code>dmesg</code> logs.</p><p>This isn&#39;t just about writing code; it&#39;s about making &quot;physical contact&quot; with the underlying logic of an operating system.</p><hr><h2 id="_4-1-architectural-landscape-user-space-and-kernel-space" tabindex="-1">4.1 Architectural Landscape: User Space and Kernel Space <a class="header-anchor" href="#_4-1-architectural-landscape-user-space-and-kernel-space" aria-label="Permalink to &quot;4.1 Architectural Landscape: User Space and Kernel Space&quot;">​</a></h2><p>Before we write any code, we need to take a step back and see what the &quot;territory&quot; we&#39;re entering actually looks like.</p><h3 id="_4-1-1-two-worlds-two-privilege-levels" tabindex="-1">4.1.1 Two Worlds, Two Privilege Levels <a class="header-anchor" href="#_4-1-1-two-worlds-two-privilege-levels" aria-label="Permalink to &quot;4.1.1 Two Worlds, Two Privilege Levels&quot;">​</a></h3><p>Modern processors aren&#39;t just calculators that execute instructions — they&#39;re strict gatekeepers. They support different <strong>privilege levels</strong>.</p><p>For the x86 architecture, these are called Ring 0 through Ring 3; for ARM, they&#39;re called Exception Level 0 through 3. Regardless of the naming, the core idea is the same: <strong>security and isolation</strong>.</p><p>The operating system leverages these hardware features to divide the virtual address space into two distinct regions:</p><ol><li><p><strong>User Space</strong>:</p><ul><li>This is where all applications — your browser, text editor, database processes — live.</li><li>They run in unprivileged mode.</li><li>They are strictly restricted: they cannot directly access hardware, cannot arbitrarily read or write memory, and can only request services through specific &quot;gates.&quot;</li></ul></li><li><p><strong>Kernel Space</strong>:</p><ul><li>This is the fortress where the core of the operating system — the kernel, drivers, and network stack — resides.</li><li>It runs in the highest privilege mode.</li><li>It is the master of the system: it can access all memory, manipulate all hardware, and execute any CPU instruction.</li></ul></li></ol><blockquote><p><strong>💡 Key Point Beyond the Code</strong></p><p>Many beginners confuse the <strong>Root user</strong> with <strong>kernel privilege</strong>.</p><ul><li><strong>Root</strong> is a &quot;superuser&quot; defined at the operating system&#39;s software layer, with a UID of 0.</li><li><strong>Kernel privilege</strong> is a state at the CPU&#39;s hardware layer.</li></ul><p>Although a Root user&#39;s process can typically enter kernel privilege mode via system calls, conceptually, the permissions of kernel code itself are far greater than simply &quot;running a script as Root.&quot; When you write a kernel module, your code wields this absolute power of life and death.</p></blockquote><h3 id="_4-1-2-the-bridge-system-calls" tabindex="-1">4.1.2 The Bridge: System Calls <a class="header-anchor" href="#_4-1-2-the-bridge-system-calls" aria-label="Permalink to &quot;4.1.2 The Bridge: System Calls&quot;">​</a></h3><p>Since user space and kernel space are isolated, how do applications ask the kernel to do work? For example, how does a program display &quot;Hello, world&quot; on the screen?</p><p>It can&#39;t directly manipulate the graphics card. It has to knock on the door.</p><p>The only legitimate entry point is a <strong>system call</strong>. APIs like <code>open()</code>, <code>read()</code>, <code>write()</code>, and <code>fork()</code> might look like ordinary library functions, but they are actually doors leading to kernel space. When a user process calls them, the CPU switches from unprivileged mode to privileged mode, jumps to predefined code in the kernel to execute, and then returns to user space.</p><p>This is the rule we must follow: user space communicates with the kernel through library APIs and system calls.</p><h3 id="_4-1-3-inside-the-kernel-monolithic-kernel-and-modules" tabindex="-1">4.1.3 Inside the Kernel: Monolithic Kernel and Modules <a class="header-anchor" href="#_4-1-3-inside-the-kernel-monolithic-kernel-and-modules" aria-label="Permalink to &quot;4.1.3 Inside the Kernel: Monolithic Kernel and Modules&quot;">​</a></h3><p>The Linux kernel uses a <strong>monolithic kernel</strong> architecture. This means all core components of the kernel — the scheduler, memory management, VFS, drivers, and network stack — share the same kernel address space. They can call each other&#39;s functions directly, making it highly efficient.</p><p>Although Linux is monolithic, it isn&#39;t rigid. Through the LKM framework, modern kernels allow us to write certain parts of the code (mainly drivers and filesystems) as independent modules that can be dynamically inserted into or removed from this massive address space.</p><hr><h2 id="_4-2-the-dynamics-why-do-we-need-lkm" tabindex="-1">4.2 The Dynamics: Why Do We Need LKM? <a class="header-anchor" href="#_4-2-the-dynamics-why-do-we-need-lkm" aria-label="Permalink to &quot;4.2 The Dynamics: Why Do We Need LKM?&quot;">​</a></h2><p>Let&#39;s return to our original motivation: why do kernel modules exist?</p><h3 id="_4-2-1-the-pain-points-of-the-traditional-approach" tabindex="-1">4.2.1 The Pain Points of the Traditional Approach <a class="header-anchor" href="#_4-2-1-the-pain-points-of-the-traditional-approach" aria-label="Permalink to &quot;4.2.1 The Pain Points of the Traditional Approach&quot;">​</a></h3><p>Suppose you need to write a driver for a newly purchased network card.</p><ul><li><strong>The old way</strong>: Throw the driver code into the <code>drivers/</code> directory of the kernel source tree, modify <code>Kconfig</code>, configure it as <code>Y</code> (built-in), and then... recompile the entire kernel and reboot the system.</li><li><strong>The cost</strong>: Even if you only changed one line of code, this entire process could take half an hour. Development efficiency is extremely low.</li></ul><h3 id="_4-2-2-the-elegance-of-lkm" tabindex="-1">4.2.2 The Elegance of LKM <a class="header-anchor" href="#_4-2-2-the-elegance-of-lkm" aria-label="Permalink to &quot;4.2.2 The Elegance of LKM&quot;">​</a></h3><p>The LKM framework provides a dynamic extension mechanism. You can compile your driver code into an independent <code>.ko</code> (Kernel Object) file.</p><ul><li><strong>Insertion</strong>: Use a tool to load the <code>.ko</code> into kernel memory. The code instantly becomes part of the kernel with full kernel privileges.</li><li><strong>Removal</strong>: If you don&#39;t want it anymore, or need to update to a new version, simply remove it from memory without even rebooting the system.</li></ul><p>This &quot;plug-and-play&quot; capability makes Linux incredibly flexible.</p><ul><li><strong>Distribution-friendly</strong>: Ubuntu doesn&#39;t know exactly which network card your computer uses, so it compiles thousands of drivers as modules and automatically loads the correct one when you plug in the hardware, rather than bloating the kernel image to the size of a balloon.</li><li><strong>Development-friendly</strong>: We can <code>insmod</code> -&gt; <code>rmmod</code> -&gt; <code>编辑</code> -&gt; <code>重新编译</code> -&gt; <code>insmod</code>, iterating in a matter of seconds.</li></ul><p><strong>Analogy:</strong></p><p>You can think of the kernel as a massive aircraft carrier.</p><ul><li><strong>Traditional built-in code</strong> is the hull itself — it&#39;s welded shut during construction, and modifying it requires a dry dock.</li><li><strong>LKM modules</strong> are shipping containers. We can hoist a new container aboard or toss an old one overboard while sailing on the open sea. Once on board, it belongs to the ship&#39;s cargo (in the same address space), but it remains modular.</li></ul><hr><h2 id="_4-3-first-practical-lesson-writing-a-hello-world-kernel-module" tabindex="-1">4.3 First Practical Lesson: Writing a Hello World Kernel Module <a class="header-anchor" href="#_4-3-first-practical-lesson-writing-a-hello-world-kernel-module" aria-label="Permalink to &quot;4.3 First Practical Lesson: Writing a Hello World Kernel Module&quot;">​</a></h2><p>Enough theory — let&#39;s start building this &quot;shipping container.&quot;</p><h3 id="_4-3-1-preparation-tools-and-environment" tabindex="-1">4.3.1 Preparation: Tools and Environment <a class="header-anchor" href="#_4-3-1-preparation-tools-and-environment" aria-label="Permalink to &quot;4.3.1 Preparation: Tools and Environment&quot;">​</a></h3><p>Before we begin, you need to confirm two things. It&#39;s like cooking: you need a pot and rice first.</p><ol><li><strong>Toolchain</strong>: The compiler. This is usually already installed on modern distributions. If not:<div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gcc</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div></li><li><strong>Kernel headers</strong>: This is the most critical part. Because a kernel module interfaces intimately with the kernel, it must use the exact same data structure definitions and function prototypes as the currently running kernel.<div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> linux-headers-generic</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div>After installation, you&#39;ll see the <code>/lib/modules/$(uname -r)/build</code> symbolic link, which points to the installed headers directory (usually in <code>/usr/src/linux-headers-...</code>). This is the &quot;cornerstone&quot; for compiling our modules.</li></ol><blockquote><p><strong>⚠️ Pitfall Warning</strong></p><p>Never try to compile a kernel module C file directly using the <code>gcc</code> command! The compilation process for kernel modules is extremely complex and relies on the kernel build system. You must use a <strong>Makefile</strong> and the <code>make</code> command. We&#39;ll explain exactly why later.</p></blockquote><h3 id="_4-3-2-first-look-at-code-breaking-the-main-obsession" tabindex="-1">4.3.2 First Look at Code: Breaking the <code>main()</code> Obsession <a class="header-anchor" href="#_4-3-2-first-look-at-code-breaking-the-main-obsession" aria-label="Permalink to &quot;4.3.2 First Look at Code: Breaking the \`main()\` Obsession&quot;">​</a></h3><p>The hardest part is often the first line of code. Let&#39;s see what the simplest kernel module looks like. There is no <code>main()</code> function here.</p><p><strong>File: <code>helloworld_lkm.c</code></strong></p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;linux/init.h&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;linux/module.h&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 模块元数据：这些信息可以通过 modinfo 看到 */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">MODULE_AUTHOR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Your Name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">MODULE_DESCRIPTION</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;LKP2E book:ch4: hello, world LKM&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">MODULE_LICENSE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Dual MIT/GPL&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">MODULE_VERSION</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0.2&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 初始化入口点：当模块加载时执行 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __init </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">helloworld_lkm_init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    printk</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(KERN_INFO </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Hello, world</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 返回 0 表示成功 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 清理出口点：当模块卸载时执行 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __exit </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">helloworld_lkm_exit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    printk</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(KERN_INFO </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Goodbye, world!</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 注册我们的入口和出口函数 */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">module_init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(helloworld_lkm_init);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">module_exit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(helloworld_lkm_exit);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br><span class="line-number">24</span><br><span class="line-number">25</span><br></div></div><p>Let&#39;s dissect this &quot;organism&quot; line by line:</p><ol><li><strong>Header files</strong>: <ul><li><code>&lt;linux/init.h&gt;</code> and <code>&lt;linux/module.h&gt;</code> are the cornerstones of kernel modules. Notice there&#39;s no <code>&lt;stdio.h&gt;</code> here — that&#39;s for user space. The kernel space does not use the standard C library.</li></ul></li><li><strong>Module metadata</strong>: <ul><li>Those macros starting with <code>MODULE_</code> aren&#39;t just comments. They get embedded into the compiled <code>.ko</code> file. Users can read this information with the <code>modinfo ./helloworld_lkm.ko</code> command. This is very important in formal product development for managing copyrights and versions.</li></ul></li><li><strong>Entry and exit</strong>: <ul><li>Kernel modules don&#39;t have a <code>main()</code>. They are event-driven.</li><li><code>module_init(helloworld_lkm_init)</code>: Tells the kernel, &quot;When you load me, please execute this function.&quot;</li><li><code>module_exit(helloworld_lkm_exit)</code>: Tells the kernel, &quot;When you unload me, please execute this function.&quot;</li></ul></li><li><strong>The <code>__init</code> and <code>__exit</code> macros</strong>: <ul><li>This is kernel optimization magic.</li><li><code>__init</code> tells the linker: &quot;Put this function into a special initialization memory segment (<code>.init.text</code>).&quot; Once initialization is complete, the kernel frees this memory, reclaiming precious RAM.</li><li><code>__exit</code> does the same for cleanup code. For built-in modules (non-LKM), this macro is ignored because built-in code cannot be unloaded.</li></ul></li></ol><h3 id="_4-3-3-the-philosophy-of-return-values-the-0-e-convention" tabindex="-1">4.3.3 The Philosophy of Return Values: The <code>0/-E</code> Convention <a class="header-anchor" href="#_4-3-3-the-philosophy-of-return-values-the-0-e-convention" aria-label="Permalink to &quot;4.3.3 The Philosophy of Return Values: The \`0/-E\` Convention&quot;">​</a></h3><p>Notice that the <code>helloworld_lkm_init</code> function returns <code>0</code>.</p><p>Here is an ironclad rule of kernel programming, counterintuitive to user-space programming:</p><ul><li><strong>Success returns <code>0</code></strong>.</li><li><strong>Failure returns a negative error code</strong> (such as <code>-ENOMEM</code>, <code>-EINVAL</code>).</li></ul><p><strong>Why?</strong> Inside this initialization function, the kernel is doing work on behalf of a user-space process (the one that called <code>insmod</code>). According to POSIX system call conventions, if it fails, the kernel needs to set the global variable <code>errno</code>. By returning a negative error code (like <code>-12</code>, which is <code>-ENOMEM</code>), the upper-level system call wrapper (glibc) negates it into a positive number (<code>12</code>), assigns it to <code>errno</code>, and returns <code>-1</code> to user space.</p><blockquote><p><strong>🚫 Pitfall in Action</strong></p><p>If you write a <code>init</code> function and forget to write <code>return 0;</code> at the end, or if you return a positive number (like <code>1</code>), on some older kernels your module will be immediately rejected from loading; on newer kernels, you&#39;ll receive a warning and the module might not work properly. <strong>Conclusion:</strong> On success, you must explicitly return <code>0</code>.</p></blockquote><hr><h2 id="_4-4-the-build-system-magic-in-the-makefile" tabindex="-1">4.4 The Build System: Magic in the Makefile <a class="header-anchor" href="#_4-4-the-build-system-magic-in-the-makefile" aria-label="Permalink to &quot;4.4 The Build System: Magic in the Makefile&quot;">​</a></h2><p>The code is written — how do we turn it into a <code>.ko</code>? We need a special recipe.</p><p><strong>File: <code>Makefile</code></strong></p><div class="language-makefile vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">makefile</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 获取当前目录路径</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PWD := </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">$(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">shell</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pwd)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 指向内核构建目录（这是关键！）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">KDIR := /lib/modules/</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">$(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">shell</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> uname -r)</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/build/</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 核心目标：告诉 Kbuild 我们要编译哪个模块</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># obj-m 表示“编译成模块”</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">obj-m += helloworld_lkm.o</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 默认目标：编译模块</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">all</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	make -C </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">$(</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">KDIR</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">)</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> M=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">$(</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PWD</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">)</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> modules</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 清理目标</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">clean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	make -C </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">$(</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">KDIR</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">)</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> M=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">$(</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PWD</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">)</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> clean</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br></div></div><p><strong>What exactly happens with this <code>make -C __PRESERVED_16__(PWD) modules</code>?</strong> This command is incredibly elegant and is the core of LKM building:</p><ol><li><p><strong><code>make -C $(KDIR)</code></strong>: We aren&#39;t running <code>make</code> in the current directory; instead, we&#39;re handing control over to the <strong>top-level Makefile of the kernel source tree</strong> (located at <code>/lib/modules/.../build</code>, which is that <code>linux-headers</code> directory). This ensures our module is built strictly according to the current kernel&#39;s configuration, compiler flags, and macro definitions.</p></li><li><p><strong><code>M=$(PWD)</code></strong>: This is a parameter. We&#39;re telling the kernel&#39;s Makefile: &quot;Hey, boss, even though you live in <code>/usr/src/...</code>, I want you to turn around and compile the module in <strong>my current directory</strong>.&quot;</p></li><li><p><strong><code>modules</code></strong>: This is the target of the kernel build system, telling it we want to build modules.</p></li></ol><p><strong>The <code>obj-m</code> variable</strong>: This is Kbuild system syntax.</p><ul><li><code>obj-y</code>: If you write <code>obj-y += foo.o</code>, it means <code>foo.c</code> will be compiled and linked into the <strong>static kernel image</strong> (<code>vmlinux</code>).</li><li><code>obj-m</code>: If you write <code>obj-m += foo.o</code>, it means generating a <strong>loadable module</strong> <code>foo.ko</code>.</li></ul><p>Now, run the build command:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>If all goes well, you&#39;ll see compilation output and a file named <strong><code>helloworld_lkm.ko</code></strong> will be generated in the current directory. This is the &quot;shipping container&quot; we&#39;re about to inject into the kernel.</p><hr><h2 id="_4-5-the-lifecycle-loading-running-and-unloading" tabindex="-1">4.5 The Lifecycle: Loading, Running, and Unloading <a class="header-anchor" href="#_4-5-the-lifecycle-loading-running-and-unloading" aria-label="Permalink to &quot;4.5 The Lifecycle: Loading, Running, and Unloading&quot;">​</a></h2><p>Now, with the <code>.ko</code> file in hand, it&#39;s time to get to work.</p><h3 id="_4-5-1-injecting-into-the-kernel-insmod" tabindex="-1">4.5.1 Injecting into the Kernel: <code>insmod</code> <a class="header-anchor" href="#_4-5-1-injecting-into-the-kernel-insmod" aria-label="Permalink to &quot;4.5.1 Injecting into the Kernel: \`insmod\`&quot;">​</a></h3><p>We use the <code>insmod</code> command to insert the module into the kernel.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> insmod</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./helloworld_lkm.ko</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>What happens here?</p><ol><li><code>insmod</code> is a user-space tool.</li><li>It reads the contents of the <code>.ko</code> file.</li><li>It initiates a system call (<code>finit_module</code> or the older <code>init_module</code>).</li><li>The kernel takes over, loads the code and data segments into kernel memory, resolves symbols, and executes your <code>helloworld_lkm_init</code> function.</li></ol><h3 id="_4-5-2-invisible-output-printk-and-dmesg" tabindex="-1">4.5.2 Invisible Output: <code>printk</code> and <code>dmesg</code> <a class="header-anchor" href="#_4-5-2-invisible-output-printk-and-dmesg" aria-label="Permalink to &quot;4.5.2 Invisible Output: \`printk\` and \`dmesg\`&quot;">​</a></h3><p>Did the code run? Let&#39;s check the logs. Inside the kernel, we can&#39;t use <code>printf</code>. The kernel has no C library. It has its own printing function: <code>printk</code>. <code>printk</code> writes messages to the <strong>kernel log buffer</strong>.</p><p>To view this buffer, we use the <code>dmesg</code> tool:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dmesg</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> tail</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -n</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 5</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[ 4123.028252] Hello, world</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><blockquote><p><strong>⚠️ Permission Issues</strong></p><p>On distributions like Ubuntu, running <code>dmesg</code> as a regular user might throw an error: <code>dmesg: read kernel buffer failed: Operation not permitted</code> This is because the <code>dmesg_restrict</code> security mechanism is enabled, preventing regular users from peeking into kernel details. Make sure to use <code>sudo</code>.</p></blockquote><blockquote><p><strong>💡 About &quot;Tainted Kernel&quot;</strong></p><p>In the logs, you might see a warning like this: <code>kernel: loading out-of-tree module taints kernel.</code><strong>Don&#39;t panic!</strong> This is just a &quot;taint&quot; flag. Because you loaded an unsigned, out-of-tree module that isn&#39;t part of the official kernel tree, kernel developers want to express: &quot;If the system blows up now, don&#39;t blame us, because we don&#39;t know what this module did.&quot; For learning and development, this is perfectly fine.</p></blockquote><h3 id="_4-5-3-checking-survival-status-lsmod" tabindex="-1">4.5.3 Checking Survival Status: <code>lsmod</code> <a class="header-anchor" href="#_4-5-3-checking-survival-status-lsmod" aria-label="Permalink to &quot;4.5.3 Checking Survival Status: \`lsmod\`&quot;">​</a></h3><p>Your module is now alive as a kernel entity. We can use the <code>lsmod</code> command to view all modules residing in memory.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lsmod</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> helloworld</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">helloworld_lkm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">         16384</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>The output contains three columns:</p><ol><li><strong>Module name</strong></li><li><strong>Memory size</strong> (bytes)</li><li><strong>Reference count</strong> (0 means it&#39;s not depended on by other modules and can be safely unloaded)</li></ol><h3 id="_4-5-4-exiting-the-stage-rmmod" tabindex="-1">4.5.4 Exiting the Stage: <code>rmmod</code> <a class="header-anchor" href="#_4-5-4-exiting-the-stage-rmmod" aria-label="Permalink to &quot;4.5.4 Exiting the Stage: \`rmmod\`&quot;">​</a></h3><p>When the show is over, we unload it.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rmmod</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> helloworld_lkm</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>At this point, the kernel will call the <code>helloworld_lkm_exit</code> function you wrote. Let&#39;s verify this once more:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dmesg</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> tail</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -n</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 5</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[ 4123.028252] Hello, world</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[ 40280.138269] Goodbye, world</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>See that? &quot;Goodbye, world!&quot; has appeared. Your module has gracefully left memory.</p><hr><h2 id="_4-6-deep-dive-kernel-logging-and-advanced-usage-of-printk" tabindex="-1">4.6 Deep Dive: Kernel Logging and Advanced Usage of <code>printk</code> <a class="header-anchor" href="#_4-6-deep-dive-kernel-logging-and-advanced-usage-of-printk" aria-label="Permalink to &quot;4.6 Deep Dive: Kernel Logging and Advanced Usage of \`printk\`&quot;">​</a></h2><p>You now know how to write the simplest module, but <code>printk</code>, this &quot;console output,&quot; is actually a deep-sea leviathan — far more complex than it appears.</p><h3 id="_4-6-1-log-levels-what-is-kern-info" tabindex="-1">4.6.1 Log Levels: What is <code>KERN_INFO</code>? <a class="header-anchor" href="#_4-6-1-log-levels-what-is-kern-info" aria-label="Permalink to &quot;4.6.1 Log Levels: What is \`KERN_INFO\`?&quot;">​</a></h3><p>Looking back at the code:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">printk</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(KERN_INFO </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Hello, world</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Notice there is <strong>no comma</strong> before <code>KERN_INFO</code>. This isn&#39;t an argument; it&#39;s string concatenation. During preprocessing, it becomes a special ASCII character (Start of Header, <code>\\001</code>) plus the number <code>&quot;6&quot;</code>.</p><p>The kernel defines 8 log levels (0-7):</p><ul><li><code>KERN_EMERG &quot;0&quot;</code>: System is unusable (about to crash).</li><li><code>KERN_ALERT &quot;1&quot;</code>: Action must be taken immediately.</li><li><code>KERN_CRIT &quot;2&quot;</code>: Critical conditions.</li><li><code>KERN_ERR &quot;3&quot;</code>: Errors.</li><li><code>KERN_WARNING &quot;4&quot;</code>: Warnings.</li><li><code>KERN_NOTICE &quot;5&quot;</code>: Normal but significant.</li><li><strong><code>KERN_INFO &quot;6&quot;</code></strong>: Informational.</li><li><strong><code>KERN_DEBUG &quot;7&quot;</code></strong>: Debug-level messages.</li></ul><p>What is the point of this level? <strong>Console output control.</strong> The kernel has a parameter called <code>console_loglevel</code> (inside <code>/proc/sys/kernel/printk</code>). Only messages with a log level number <strong>less than</strong> this level will be printed to the current console terminal (the screen you&#39;re looking at).</p><ul><li>If the level is set to 4, then <code>KERN_ERR</code> (3) will print, but <code>KERN_INFO</code> (6) will not print to the screen — it will only sit in the buffer, waiting for you to view it with <code>dmesg</code>.</li><li>If you want all messages to flood the screen like <code>printf</code>, you can (temporarily) change the level to 8:<div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;echo &#39;8 4 1 7&#39; &gt; /proc/sys/kernel/printk&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div></li></ul><h3 id="_4-6-2-modern-style-the-pr-info-family" tabindex="-1">4.6.2 Modern Style: The <code>pr_info</code> Family <a class="header-anchor" href="#_4-6-2-modern-style-the-pr-info-family" aria-label="Permalink to &quot;4.6.2 Modern Style: The \`pr_info\` Family&quot;">​</a></h3><p>Although you can keep using <code>printk(KERN_INFO &quot;msg&quot;)</code>, kernel developers recommend using the more modern, convenient macro wrappers:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pr_info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Hello, world</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pr_err</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Something went wrong: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%d\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, err);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>These macros are essentially still <code>printk</code>, but they automatically handle the log level strings and are incredibly powerful when used with the <code>pr_fmt</code> macro.</p><p><strong>Standardizing your output:</strong> You can define <code>pr_fmt</code> at the very beginning of a file, so that all <code>pr_xxx</code> calls in that file will automatically carry a prefix (like the module name).</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> pr_fmt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">fmt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) KBUILD_MODNAME </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;: &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> fmt</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;linux/module.h&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ...</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pr_info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Data loaded</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 实际输出: &quot;helloworld_lkm: Data loaded&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><h3 id="_4-6-3-dynamic-debugging-dyndbg" tabindex="-1">4.6.3 Dynamic Debugging: Dyndbg <a class="header-anchor" href="#_4-6-3-dynamic-debugging-dyndbg" aria-label="Permalink to &quot;4.6.3 Dynamic Debugging: Dyndbg&quot;">​</a></h3><p>If you think <code>#ifdef DEBUG</code> is too old-school, the kernel has a magical tool called <strong>Dynamic Debug</strong>. When you enable <code>CONFIG_DYNAMIC_DEBUG</code> in your kernel configuration, you can use <code>debugfs</code> at <strong>runtime</strong> to toggle any <code>pr_debug()</code> call statement on or off, even matching by filename or function name.</p><p>For example, to print all debug messages in the <code>usb</code> core code:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;file *usb* +p&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /sys/kernel/debug/dynamic_debug/control</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This is leagues better than recompiling the module.</p><h3 id="_4-6-4-rate-limiting" tabindex="-1">4.6.4 Rate Limiting <a class="header-anchor" href="#_4-6-4-rate-limiting" aria-label="Permalink to &quot;4.6.4 Rate Limiting&quot;">​</a></h3><p>If you put a <code>printk</code> inside a high-frequency execution path (like an interrupt handler triggered 1,000 times per second), your console will be flooded, your logs will be washed away, and it might even cause system stuttering.</p><p>The kernel provides <strong>rate-limited</strong> versions of the printing macros:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pr_info_ratelimited</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;High frequency event: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%d\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, val);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This automatically limits the printing frequency (default is allowing one burst every 5 seconds) and tells you in the logs &quot;suppressed N callbacks,&quot; letting you know how many times it occurred without printing.</p><hr><h2 id="chapter-echoes" tabindex="-1">Chapter Echoes <a class="header-anchor" href="#chapter-echoes" aria-label="Permalink to &quot;Chapter Echoes&quot;">​</a></h2><p>Remember the question we posed at the beginning of this chapter: <strong>How do we dynamically grant the kernel new capabilities while keeping it stable?</strong></p><p>Now you have the answer. The LKM framework isn&#39;t just a tool for &quot;loading drivers&quot; — it&#39;s the physical embodiment of Linux&#39;s modular philosophy. Through <code>insmod</code>, we inject code into the kernel&#39;s high-privilege address space; through <code>module_init</code>, we insert initialization logic between the kernel&#39;s breaths; through <code>printk</code>, we establish a communication pipeline from the depths of the kernel to the human eye.</p><p>On the surface, we spent this chapter writing a <code>Hello, world</code>, but in reality, we were learning how to walk safely in the most heavily restricted forbidden zone. You learned why we can&#39;t use <code>printf</code>, why return values are negative, and why the Makefile has to borrow the kernel&#39;s build system.</p><p>In the next chapter, we&#39;ll dive deeper into this territory to handle more complex scenarios — inter-module dependencies, passing parameters, and how to pass real data between the kernel and user space, rather than just printing a string of characters.</p><p>When that time comes, you&#39;ll find that all the concepts we built today will come back into play in unexpected ways.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: Understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: Understanding&quot;">​</a></h3><p><strong>Question</strong>: True or False: A user-space application (like a web browser) can call kernel functions by directly accessing kernel-space memory addresses, as long as it knows the correct memory address.</p><details><summary>Answer and Explanation</summary><p><strong>Answer</strong>: False</p><p><strong>Explanation</strong>: User space and kernel space are isolated virtual address spaces. Programs running in user mode are unprivileged and cannot directly access kernel space. All requests for kernel functionality must be completed through the legitimate entry point of system calls, which handle the switch from user mode to kernel mode. Directly accessing kernel memory will result in an invalid memory access exception.</p></details><h3 id="exercise-2-application" tabindex="-1">Exercise 2: Application <a class="header-anchor" href="#exercise-2-application" aria-label="Permalink to &quot;Exercise 2: Application&quot;">​</a></h3><p><strong>Question</strong>: When writing the initialization function <code>static int __init my_init(void)</code> for a kernel module, if resource allocation fails, what value should the function return according to the kernel programming 0/-E return convention?</p><details><summary>Answer and Explanation</summary><p><strong>Answer</strong>: A negative error code (such as -ENOMEM or -1)</p><p><strong>Explanation</strong>: In kernel programming conventions, a function must return 0 on success. If an error occurs (such as out of memory, inability to acquire a lock, etc.), it must return a negative error code (for example, -ENOMEM indicates out of memory, -EINVAL indicates an invalid parameter). Returning a non-zero positive value violates convention and will cause the system to mistakenly judge it as a success.</p></details><h3 id="exercise-3-application" tabindex="-1">Exercise 3: Application <a class="header-anchor" href="#exercise-3-application" aria-label="Permalink to &quot;Exercise 3: Application&quot;">​</a></h3><p><strong>Question</strong>: Scenario application: You are writing kernel module code for a high-frequency interrupt handler. This code path might be called thousands of times per second and contains a <code>printk()</code> log output statement. To prevent this log from causing system log overflow or disk I/O overload, which macro should you use to replace the standard <code>printk()</code>?</p><details><summary>Answer and Explanation</summary><p><strong>Answer</strong>: pr_info_ratelimited() or printk_ratelimited()</p><p><strong>Explanation</strong>: A standard <code>printk</code> will create a &quot;log storm&quot; under high-frequency calls. The kernel provides a rate-limiting mechanism through the <code>pr_&lt;foo&gt;_ratelimited()</code> family of macros (like <code>pr_info_ratelimited</code>), which ensures that the same log message is only output once within a specific time window, thereby protecting system stability.</p></details><h3 id="exercise-4-thinking" tabindex="-1">Exercise 4: Thinking <a class="header-anchor" href="#exercise-4-thinking" aria-label="Permalink to &quot;Exercise 4: Thinking&quot;">​</a></h3><p><strong>Question</strong>: Thought experiment: Why can&#39;t kernel modules (LKMs) simply link and use the <code>printf()</code> function from the standard C library (glibc) like user-space programs do, and why must they use the kernel-specific <code>printk()</code>? What essential difference regarding &quot;context&quot; and &quot;dependency&quot; in operating system design does this reflect?</p><details><summary>Answer and Explanation</summary><p><strong>Answer</strong>: Primarily due to differences in the runtime environment (context) and dependent libraries. The kernel runs in an independent address space and lacks the support of user-space libraries.</p><p><strong>Explanation</strong>: The core of this question lies in understanding the fundamental differences between the kernel and user space:</p><ol><li><strong>Context environment</strong>: Kernel modules run in kernel space with the highest privilege level (Ring 0), directly managing hardware, without any runtime environment support.</li><li><strong>Library dependencies</strong>: glibc is a library that runs in user space and relies on system calls provided by the kernel to work (in fact, glibc&#39;s printf ultimately calls the write system call). The kernel exists at a lower level and cannot &quot;depend&quot; on an upper-layer library, otherwise it would create a circular dependency.</li><li><strong>Functional differences</strong>: <code>printf</code> needs to format buffers and handle stream I/O, whereas the kernel needs a lower-level logging tool that doesn&#39;t rely on complex buffering mechanisms and can safely run in interrupt context — namely, <code>printk</code>. This reflects the operating system&#39;s principle of layered design: lower-level modules cannot depend on upper-level implementations; they must be self-contained.</li></ol></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>The Linux kernel strictly divides the virtual address space into user space and kernel space through privilege levels. The former runs applications restricted to unprivileged mode and can only request services through system calls; the latter is the highest privilege mode where the core of the operating system runs, with permissions to access all memory and hardware. Understanding this boundary is a prerequisite for kernel development, because once a kernel module&#39;s code is loaded, it will run directly in this unprotected, high-privilege environment.</p><p>To solve the inefficient &quot;modify-compile-reboot&quot; endless loop of traditional kernel development, Linux introduced the Loadable Kernel Module (LKM) mechanism. LKM allows driver or feature code to be compiled into independent <code>.ko</code> files, which can be dynamically inserted or removed via the <code>insmod</code> and <code>rmmod</code> commands, extending kernel functionality without rebooting the system. This mechanism gives the Linux monolithic kernel flexibility similar to a microkernel while maintaining high performance.</p><p>Kernel modules do not have a <code>main</code> function like user-space programs; instead, they are based on an event-driven model, specifying initialization and cleanup functions through the <code>module_init</code> and <code>module_exit</code> macros. The initialization function must return <code>0</code> to indicate success (returning a negative error code on failure), and using the <code>__init</code> macro at compile time places the code into a temporary memory segment to save RAM. This design clarifies the lifecycle management of modules and is deeply tied to the kernel&#39;s build process.</p><p>Building kernel modules cannot use the standard <code>gcc</code> command; it must rely on the kernel&#39;s built-in Kbuild build system. In the Makefile, you need to define the <code>obj-m</code> variable to specify the target module and use the <code>make -C __PRESERVED_17__(PWD)</code> command to borrow the configuration and headers from the current kernel source tree for compilation. This ensures the module strictly matches the currently running kernel version, avoiding system crashes caused by interface inconsistencies.</p><p>Kernel space cannot use standard C library functions, so <code>printf</code> is replaced by <code>printk</code>, which outputs logs to the kernel buffer instead of the terminal. The <code>dmesg</code> tool can be used to read these logs, and log levels (like <code>KERN_INFO</code>) determine the display strategy of messages on the console. Proficiency in using <code>printk</code> (or the modern <code>pr_info</code>) and its log levels is a key method for developers to obtain debugging information from inside the kernel.</p>`,152)])])}const k=i(t,[["render",o]]);export{u as __pageData,k as default};
