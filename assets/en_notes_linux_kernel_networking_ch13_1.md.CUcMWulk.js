import{_ as t,o,c as n,a}from"./chunks/framework.8tiMeCK-.js";const p=JSON.parse('{"title":"Chapter 13: The Cost of Bypassing the Kernel","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch13_1.md","filePath":"en/notes/linux_kernel_networking/ch13_1.md","lastUpdated":null}'),s={name:"en/notes/linux_kernel_networking/ch13_1.md"};function i(r,e,l,h,c,d){return o(),n("div",null,[...e[0]||(e[0]=[a('<h1 id="chapter-13-the-cost-of-bypassing-the-kernel" tabindex="-1">Chapter 13: The Cost of Bypassing the Kernel <a class="header-anchor" href="#chapter-13-the-cost-of-bypassing-the-kernel" aria-label="Permalink to &quot;Chapter 13: The Cost of Bypassing the Kernel&quot;">​</a></h1><p>There is a class of problems that appear to be about &quot;network performance,&quot; but are actually about &quot;who is paying for it.&quot;</p><p>When you send data using traditional TCP/IP sockets, you might think you&#39;re simply moving data from point A to point B. In reality, you&#39;re paying for the three-way handshake, paying for context switches between kernel and user space, paying for data being copied back and forth between kernel buffers—not to mention the NIC driver that interrupts the CPU every time it receives a single packet.</p><p>For most applications, this tax is acceptable. But if you&#39;re building high-frequency trading systems, large-scale distributed storage, or million-node compute clusters, this &quot;tax&quot; isn&#39;t just expensive—it&#39;s unacceptable.</p><p>This is the moment for our main character to take the stage: <strong>RDMA (Remote Direct Memory Access)</strong>.</p><p>Its promises sound like cheating: directly accessing a remote machine&#39;s memory without CPU involvement, without kernel intervention, and without even copying data. This isn&#39;t just a protocol upgrade; it&#39;s a &quot;jailbreak&quot; from the traditional network stack.</p><p>But freedom comes at a price. RDMA discards the &quot;nanny-style services&quot; of the traditional kernel protocol stack and dumps enormous complexity—connection management, memory registration, error handling—onto the developer. If you think you can get a performance boost just by swapping out an API, you&#39;ll most likely end up with a pile of crashes and bizarre memory errors.</p><p>Our mission in this chapter is to walk you through this &quot;jailbreak&quot; path. We&#39;ll first figure out what RDMA actually is (and why it requires so much hardware support), then dive into the Linux kernel&#39;s <code>drivers/infiniband</code> directory to see how this system is built within the kernel. By the end of this chapter, you won&#39;t just be amazed by &quot;zero-copy&quot;—you&#39;ll start questioning why it took us so many years to realize this step.</p><hr><h2 id="_13-1-rdma-and-infiniband-—-an-overview" tabindex="-1">13.1 RDMA and InfiniBand — An Overview <a class="header-anchor" href="#_13-1-rdma-and-infiniband-—-an-overview" aria-label="Permalink to &quot;13.1 RDMA and InfiniBand — An Overview&quot;">​</a></h2><h3 id="_13-1-1-what-is-rdma" tabindex="-1">13.1.1 What is RDMA <a class="header-anchor" href="#_13-1-1-what-is-rdma" aria-label="Permalink to &quot;13.1.1 What is RDMA&quot;">​</a></h3><p>Before rushing into the code, let&#39;s establish the most important mental model: <strong>what exactly does RDMA do?</strong></p><p>Remote Direct Memory Access (RDMA). As the name implies, it allows one machine to directly read from and write to another machine&#39;s memory. Note the word &quot;directly&quot; here:</p><ul><li><strong>No remote CPU involvement</strong>: The remote machine&#39;s CPU doesn&#39;t even know its memory was read or written.</li><li><strong>No kernel intervention</strong>: Data bypasses the kernel protocol stack and is transferred directly between the NIC and user-space memory.</li></ul><p>To achieve this, we need a completely new set of hardware and protocol stacks. Currently, there are three mainstream network protocols that support RDMA:</p><ol><li><strong>InfiniBand (IB)</strong>: This is a brand-new network architecture designed specifically for high performance. It starts from scratch, discarding the baggage of traditional Ethernet. The specification is maintained by the InfiniBand Trade Association (IBTA), and you can look up the <em>InfiniBand Architecture Specification</em>, which is as thick as a brick.</li><li><strong>RoCE (RDMA over Converged Ethernet)</strong>: pronounced as &quot;Rocky&quot;. Since InfiniBand switches are too expensive, why not run RDMA over existing Ethernet? That&#39;s RoCE. It uses Ethernet/IP on top of the InfiniBand link layer, making it a &quot;hybrid&quot; solution. Its specification is typically an appendix to the InfiniBand specification.</li><li><strong>iWARP (Internet Wide Area RDMA Protocol)</strong>: This is another path, attempting to implement RDMA on top of the standard TCP/IP protocol stack. Maintained by the RDMA Consortium, this is your choice if you don&#39;t want to modify the underlying network infrastructure and just want to play with RDMA over wide-area networks.</li></ol><p>Despite the vast differences in the underlying physical and link layers (fiber vs. twisted pair, lossless networks vs. Ethernet), the <strong>APIs they expose are completely unified</strong>.</p><p>This unified API is called <strong>Verbs</strong>. You can think of it as the &quot;system call table&quot; of the RDMA world. Whether it&#39;s InfiniBand, RoCE, or iWARP, your client code only needs to operate the hardware through the Verbs API (i.e., the <code>ib_*</code> family of functions), and the underlying differences are abstracted away by kernel modules and drivers.</p><p>This RDMA subsystem was introduced in Linux kernel version 2.6.11. Initially, it only supported InfiniBand, and it wasn&#39;t until subsequent years that iWARP and RoCE were added. So when you see <code>include/rdma/ib_verbs.h</code> in the kernel source, don&#39;t be fooled by the <code>ib</code> (InfiniBand) in the name—it&#39;s generic.</p><blockquote><p><strong>❌ Pitfall Warning</strong> There is a legacy issue here: although the API is collectively referred to as RDMA, a massive number of kernel functions, structures, and filenames still start with <code>ib_</code> (referring to InfiniBand). For example, you&#39;ll see <code>ib_register_client</code> in the code, but it can actually register RoCE devices too. Don&#39;t get hung up on the names; just treat them as generic RDMA interfaces.</p></blockquote><p>Before diving in, here are a few trivia points about this API:</p><ul><li><strong>Mixed function styles</strong>: Some functions are inline, and some are not. This isn&#39;t some perverse taste of the designers, but a product of performance optimization. Future kernel versions might change this, so be careful when writing drivers.</li><li><strong>Who uses it</strong>: The <code>ib_verbs.h</code> header file serves three types of audiences: <ol><li><strong>RDMA stack core code</strong>: The ones responsible for maintaining order.</li><li><strong>Low-level hardware drivers</strong>: The <code>drivers/infiniband/hw</code> from various vendors (Mellanox, Intel, etc.).</li><li><strong>Upper-layer consumers</strong>: Kernel modules that use RDMA (such as NFS/RDMA, ISER, or your own custom driver).</li></ol></li></ul><p>For the rest of this book, we&#39;ll primarily play the third role: <strong>consumers</strong>. What we care about is how to put the hardware to work through the Verbs API, not how to write hardware drivers.</p><hr><h3 id="_13-1-2-what-does-the-rdma-stack-look-like-in-the-kernel" tabindex="-1">13.1.2 What Does the RDMA Stack Look Like in the Kernel <a class="header-anchor" href="#_13-1-2-what-does-the-rdma-stack-look-like-in-the-kernel" aria-label="Permalink to &quot;13.1.2 What Does the RDMA Stack Look Like in the Kernel&quot;">​</a></h3><p>If you open the kernel source, the vast majority of RDMA-related code is hidden in the <code>drivers/infiniband</code> directory. This name is also a bit misleading, because it contains both InfiniBand-specific things and logic for handling RoCE and iWARP.</p><p>Let&#39;s dismantle this like a server rack and see what modules are inside:</p><ul><li><strong><code>core/cm.c</code> (Communication Manager)</strong>: The communication manager. Just like how dating might need a matchmaker, establishing an RDMA connection between two nodes isn&#39;t as simple as saying hello—it requires negotiating parameters and exchanging keys. CM is responsible for these chores.</li><li><strong><code>core/verbs.c</code> (Kernel Verbs)</strong>: This is the core API implementation layer. Most of the <code>ib_post_send</code> functions you call can be traced back to the logic here.</li><li><strong><code>core/uverbs_*.c</code> (User Verbs)</strong>: User-space Verbs. Although we mainly focus on kernel space, a large number of RDMA applications actually run in user space. This mechanism allows user programs to interact directly with the hardware through <code>ioctl</code>, completely bypassing the kernel.</li><li><strong><code>core/mad.c</code> (Management Datagram)</strong>: Management datagrams. There are special &quot;management packets&quot; in RDMA networks, such as configuring switches or querying port states. These don&#39;t go through the data path; instead, they are handled by the MAD module.</li><li><strong><code>ulp/</code> (Upper Layer Protocols)</strong>: The upper-layer protocols. RDMA is just a transport channel; what business runs on top of it is determined here: <ul><li><strong><code>ipoib</code></strong>: IP over InfiniBand. Since we have an RDMA network, can we run regular TCP/IP applications directly on it? IPoIB is this adaptation layer; it makes an IB NIC look like a regular NIC.</li><li><strong><code>iser</code></strong>: iSCSI Extensions for RDMA. Moving the iSCSI storage protocol onto RDMA for explosive performance.</li><li><strong><code>srp</code></strong>: SCSI RDMA Protocol. Another storage protocol.</li></ul></li></ul><p>You can imagine this stack as a skyscraper. <code>core/</code> is the foundation and plumbing, <code>hw/</code> is the various utility providers (hardware drivers) plugging into it, and <code>ulp/</code> are the tenants renting space inside to open shops.</p><hr><h3 id="_13-1-3-why-bother-—-the-technical-advantages-of-rdma" tabindex="-1">13.1.3 Why Bother? — The Technical Advantages of RDMA <a class="header-anchor" href="#_13-1-3-why-bother-—-the-technical-advantages-of-rdma" aria-label="Permalink to &quot;13.1.3 Why Bother? — The Technical Advantages of RDMA&quot;">​</a></h3><p>Before diving into protocol details, we must first answer a question: <strong>why would we abandon such a mature, stable, and easy-to-use TCP/IP in favor of this complex RDMA?</strong></p><p>The answer lies in these four killer features:</p><p><strong>1. Zero-Copy</strong> In traditional network transmission, data has to endure an &quot;81-fold tribulation&quot;: user buffer -&gt; kernel socket buffer -&gt; protocol stack processing -&gt; NIC driver ring buffer -&gt; DMA to the NIC. RDMA says: stop the hassle. It allows the NIC to perform DMA operations directly on user-space memory. Data is moved only once, between &quot;local user memory&quot; and &quot;remote user memory.&quot; There are no intermediate copies.</p><p><strong>2. Kernel Bypass</strong> This is an incredibly tempting feature. When an application sends data, it doesn&#39;t need to trap into kernel mode, doesn&#39;t need to make system calls (like <code>sendmsg</code>), and doesn&#39;t need to wake up kernel softirqs to process packets. The application issues commands directly to the NIC&#39;s registers (mapped into user space), and the NIC fetches the data directly. Context switches? Non-existent.</p><p><strong>3. CPU Offload</strong> Not only is the kernel bypassed, but even the CPU&#39;s work is stolen away. RDMA NICs typically contain powerful dedicated processors. They handle the transport protocol, calculate checksums, retransmit lost packets, and even process RDMA atomic operations. For the receiver, this is a bizarre experience: memory suddenly gets overwritten, but CPU utilization doesn&#39;t budge, because it has no idea what just happened.</p><p><strong>4. Low Latency and High Bandwidth</strong> While these are hardware metrics, thanks to the architectural advantages mentioned above, RDMA&#39;s latency is absurdly low.</p><ul><li><strong>Latency</strong>: In small-message scenarios, latency can drop to a few hundred nanoseconds. Yes, nanoseconds. On Ethernet, just the protocol stack processing alone could take several microseconds.</li><li><strong>Bandwidth</strong>: InfiniBand&#39;s bandwidth scales extremely well. The same protocol technology can easily scale from 2.5 Gbps to 120 Gbps or even higher. In contrast, Ethernet standards (like 10G, 25G, 40G, 100G) often require changing physical layer technologies with each generational upgrade.</li></ul><hr><h3 id="_13-1-4-hardware-components-it-s-not-just-a-nic" tabindex="-1">13.1.4 Hardware Components: It&#39;s Not Just a NIC <a class="header-anchor" href="#_13-1-4-hardware-components-it-s-not-just-a-nic" aria-label="Permalink to &quot;13.1.4 Hardware Components: It&#39;s Not Just a NIC&quot;">​</a></h3><p>Understanding RDMA requires first understanding its underlying hardware topology, because many concepts in the software API (like LID, GID) exist to serve this hardware.</p><p>Let&#39;s look at the roles in an InfiniBand architecture:</p><p><strong>1. HCA (Host Channel Adapter)</strong> You can think of this as a &quot;super NIC.&quot; This isn&#39;t some ten-dollar Realtek network card. An HCA is an intelligent device sitting on the PCIe bus, with its own DMA engine and memory management unit. It acts as both the initiator and receiver of packets. It is responsible for executing those Verbs commands (such as send, receive, RDMA read/write).</p><p><strong>2. Switches</strong> But the switches here are quite different from Ethernet switches. InfiniBand switches are very &quot;dumb&quot; (in a good way). They don&#39;t learn MAC addresses, don&#39;t run the Spanning Tree Protocol, and don&#39;t analyze packets much. Their forwarding table is remotely configured by a god&#39;s-eye-view entity called the <strong>SM (Subnet Manager)</strong>. It only does one thing: receive a packet, look up the table, and throw it out of a specific port. This simplicity results in extremely low forwarding latency. (Note: InfiniBand doesn&#39;t support broadcast, only multicast, so switches need to know how to replicate multicast packets.)</p><p><strong>3. Routers</strong> If switches are responsible for connecting the same subnet, then routers are responsible for connecting different InfiniBand subnets. This is only used in large-scale clusters.</p><p><strong>4. Subnet</strong> A subnet is a group of connected HCA, switch, and router ports. It is the basic unit of management and addressing.</p><hr><h3 id="_13-1-5-addressing-mechanism-where-is-it" tabindex="-1">13.1.5 Addressing Mechanism: Where Is It? <a class="header-anchor" href="#_13-1-5-addressing-mechanism-where-is-it" aria-label="Permalink to &quot;13.1.5 Addressing Mechanism: Where Is It?&quot;">​</a></h3><p>In Ethernet we use MAC addresses, and at the IP layer we use IP addresses. In InfiniBand, things become more complex—and more rigorous. We have three main types of IDs:</p><p><strong>1. GUID (Globally Unique Identifier)</strong> This is the &quot;ID number&quot; assigned when the hardware leaves the factory. 64-bit, globally unique.</p><ul><li><strong>Node GUID</strong>: Every node (device) has one.</li><li><strong>Port GUID</strong>: Every port (like a specific network interface on an HCA) has one.</li><li><strong>System GUID</strong>: If a device consists of multiple chips (like a core switch), they share the same System GUID, indicating they belong to the same whole.</li></ul><p><strong>Analogy time</strong>: A GUID is like a combination of a human&#39;s fingerprint and DNA—theoretically globally unique and unchangeable.</p><p><strong>2. GID (Global IDentifier)</strong> GUIDs are for hardware; GIDs are for routing. It is usually 128-bit, generated based on the GUID plus a subnet ID. Its format is exactly the same as an IPv6 address (which is why RoCE can conveniently merge into IPv6 networks). Each port has at least one GID, stored at index 0 of the GID table.</p><p><strong>3. LID (Local IDentifier)</strong> This is the address actually used inside the packet header to run within a subnet. It&#39;s a short, 16-bit address assigned by the <strong>SM (Subnet Manager)</strong>. Why do we need LIDs when we have GUIDs? Imagine if every packet had to carry a 64-bit or 128-bit address—the lookup pressure on the switches would be enormous, and routing would be too complex. The SM assigns a short LID to each port (ranging from <code>0x0001</code> to <code>0xBFFF</code>), and switches only need to quickly forward based on this 16-bit LID.</p><ul><li><strong>Unicast LID</strong>: <code>0x0001</code> - <code>0xBFFF</code></li><li><strong>Multicast LID</strong>: <code>0xC000</code> - <code>0xFFFE</code></li></ul><hr><h3 id="_13-1-6-key-features-beyond-speed-there-s-security-and-isolation" tabindex="-1">13.1.6 Key Features: Beyond Speed, There&#39;s Security and Isolation <a class="header-anchor" href="#_13-1-6-key-features-beyond-speed-there-s-security-and-isolation" aria-label="Permalink to &quot;13.1.6 Key Features: Beyond Speed, There&#39;s Security and Isolation&quot;">​</a></h3><p>If you thought RDMA was just about speed, you&#39;re underestimating it. It also solves many enterprise-level problems:</p><p><strong>1. P_Key (Partition Key) — Virtual Isolation</strong> You have one physical switch, but you want to completely isolate the &quot;production environment&quot; from the &quot;test environment,&quot; just like two VLANs. In InfiniBand, this is called partitioning. Each port maintains a P_Key table. Each Queue Pair (QP, the core object for sending and receiving data) is associated with a P_Key. The rule is simple: <strong>communication can only occur when the P_Keys on both ends match, and at least one end has &quot;full member&quot; privileges.</strong> It&#39;s like fitting different doors with different keys.</p><p><strong>2. Q_Key (Queue Key) — The Safety Lock for UDP Mode</strong> UD (Unreliable Datagram) mode is like UDP—anyone can send packets. To prevent random spamming, InfiniBand introduces the Q_Key. A UD QP will only accept a packet if the Q_Key carried in the received packet matches its own configured Q_Key. This greatly reduces the risk of malicious interference.</p><p><strong>3. VL (Virtual Lanes) — Traffic Lanes</strong> Imagine a physical fiber as a highway. To prevent &quot;VIP&quot; packets (like storage data) from being blocked by &quot;civilian&quot; packets (like heartbeat packets), InfiniBand introduces Virtual Lanes (VL). Each physical link can be divided into multiple virtual links, and each VL has its own independent buffer. Used in conjunction with <strong>Service Level (SL)</strong>, you can tag different types of traffic and map them to different VLs, thereby achieving hardware-level QoS (Quality of Service).</p><p><strong>4. Failover — Automatic Fault Switchover</strong> An RDMA QP can be configured with two paths: a primary path and a backup path. If the primary path goes down (a fiber is unplugged, a switch crashes), the hardware automatically switches to the backup path. For upper-layer applications, as long as they handle the error codes slightly, they might not even feel the disaster that occurred underneath.</p><hr><h3 id="_13-1-7-what-does-a-packet-look-like" tabindex="-1">13.1.7 What Does a Packet Look Like <a class="header-anchor" href="#_13-1-7-what-does-a-packet-look-like" aria-label="Permalink to &quot;13.1.7 What Does a Packet Look Like&quot;">​</a></h3><p>As a low-level engineer, you&#39;ll eventually need to capture and analyze packets for troubleshooting. Even though Wireshark has plugins, knowing a bit about the headers is always good.</p><p>A standard InfiniBand packet is like a Russian nesting doll:</p><ul><li><strong>LRH (Local Routing Header, 8 bytes)</strong>: Mandatory. <ul><li>Contains the source LID and destination LID.</li><li>Contains QoS attributes (Service Level).</li><li>This is used for local routing and becomes useless once it leaves the subnet.</li></ul></li><li><strong>GRH (Global Routing Header, 40 bytes)</strong>: Optional. <ul><li>Required if it&#39;s cross-subnet transmission or multicast.</li><li>It looks exactly like an IPv6 header.</li></ul></li><li><strong>BTH (Base Transport Header, 12 bytes)</strong>: Mandatory. <ul><li>This is the transport layer header.</li><li>Contains the source QP number and destination QP number.</li><li>Contains the opcode (is this an RDMA Write or a Send?).</li><li>Contains the sequence number (used for packet ordering).</li></ul></li><li><strong>Payload</strong>: The actual data. Optional.</li><li><strong>ICRC / VCRC</strong>: Checksums. Mandatory. Guarantees data integrity.</li></ul><p>We can think of the LRH as the recipient address on an envelope (used by the local post office), the GRH as the international postal barcode (used by cross-border post offices), and the BTH as the &quot;Dear so-and-so&quot; at the top of the letter (indicating exactly which person should read it).</p><hr><h3 id="_13-1-8-management-entities-who-manages-this-network" tabindex="-1">13.1.8 Management Entities: Who Manages This Network? <a class="header-anchor" href="#_13-1-8-management-entities-who-manages-this-network" aria-label="Permalink to &quot;13.1.8 Management Entities: Who Manages This Network?&quot;">​</a></h3><p>A network this complex must have someone managing it, right? InfiniBand doesn&#39;t adopt the chaotic &quot;everyone manages themselves&quot; Ethernet model; instead, it introduces centralized managers.</p><p><strong>1. SM (Subnet Manager) — The Singular God</strong> The SM is the brain of the entire subnet. It&#39;s usually a software process running on a host, or even inside a &quot;managed switch.&quot; Its workload is heavy:</p><ul><li><strong>Topology discovery</strong>: Who is connected to whom? Were cables plugged or unplugged?</li><li><strong>Switch configuration</strong>: Pushing forwarding tables, telling switches where to send packets.</li><li><strong>Resource allocation</strong>: Assigning a LID to each port.</li><li><strong>Fault detection</strong>: If the network goes down, it&#39;s responsible for recalculating paths.</li></ul><p>To ensure high availability, you can deploy multiple SMs, but at any given moment, <strong>only one</strong> is the Master, and the others are in Standby state. If the Master goes down, they will automatically elect a new leader.</p><p><strong>2. SMA (Subnet Management Agent) — The Resident Ambassador</strong> No matter how capable the SM is, it can&#39;t physically crawl into every port. Each port has an SMA. The SMA is an agent program specifically responsible for receiving the SM&#39;s instructions (MAD packets), executing configurations, and reporting the results back to the SM.</p><p><strong>3. SA (Subnet Administrator) — The Information Query Center</strong> The SM is in charge of control; the SA is in charge of providing information. When you need to know &quot;what is the optimal path from port A to port B&quot; or &quot;how do I join this multicast group,&quot; you go ask the SA.</p><p><strong>4. CM (Communication Manager)</strong> The SM manages the network; the CM manages connections. If two nodes want to establish a reliable RDMA connection (like RC mode), they need to handshake with each other, exchanging sensitive information like QP numbers and memory keys. The CM service exists to facilitate establishing this connection state.</p><hr><p>At this point, we&#39;ve cleared all the peripheral obstacles: from API definitions and kernel code structure to hardware components and addressing mechanisms.</p><p>But I know what you care about the most right now: &quot;how do I actually send data out in code?&quot;</p><p>This brings us to the soul of RDMA — the <strong>Queue Pair (QP)</strong> and its surrounding memory management mechanisms. In the next section, we&#39;ll dive down to the data structure level and see how to create, configure, and use these resources within the kernel.</p>',81)])])}const g=t(s,[["render",i]]);export{p as __pageData,g as default};
