import{_ as e,o as i,c as n,a}from"./chunks/framework.8tiMeCK-.js";const t="/PenguinLab/assets/ch02_fig005.C9i67z1T.webp",g=JSON.parse('{"title":"2.2 Kernel Netlink Sockets","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch02_2.md","filePath":"en/notes/linux_kernel_networking/ch02_2.md","lastUpdated":null}'),l={name:"en/notes/linux_kernel_networking/ch02_2.md"};function r(h,s,p,o,k,c){return i(),n("div",null,[...s[0]||(s[0]=[a(`<h1 id="_2-2-kernel-netlink-sockets" tabindex="-1">2.2 Kernel Netlink Sockets <a class="header-anchor" href="#_2-2-kernel-netlink-sockets" aria-label="Permalink to &quot;2.2 Kernel Netlink Sockets&quot;">​</a></h1><p>In the previous section, we discussed the userspace tools—iproute2, net-tools, and the handy libnl and libmnl libraries. But on the other side of the fence, how does the <strong>kernel</strong> view all of this?</p><p>When you type <code>ip link add</code> in a terminal, what happens inside the kernel is far more complex than simply &quot;creating a virtual network interface.&quot; In this section, we dive into the kernel internals to see how this Netlink engine starts up, registers its handlers, and throws messages back to userspace.</p><hr><h3 id="creating-kernel-netlink-sockets" tabindex="-1">Creating Kernel Netlink Sockets <a class="header-anchor" href="#creating-kernel-netlink-sockets" aria-label="Permalink to &quot;Creating Kernel Netlink Sockets&quot;">​</a></h3><p>Within the kernel&#39;s Network Stack, we create several different Netlink sockets, each responsible for handling different types of messages.</p><p>For example, the <code>NETLINK_ROUTE</code> socket, which specifically handles routing and link messages, is created inside the <code>rtnetlink_net_init()</code> function. Its code is highly typical—almost a standard template for kernel Netlink initialization:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __net_init </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rtnetlink_net_init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">net</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> netlink_kernel_cfg cfg </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        .groups    </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> RTNLGRP_MAX,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        .input     </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rtnetlink_rcv,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        .cb_mutex  </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">rtnl_mutex,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        .flags     </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> NL_CFG_F_NONROOT_RECV,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    };</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    sk </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> netlink_kernel_create</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(net, NETLINK_ROUTE, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cfg);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><p><strong>But here is a frequently overlooked detail</strong>: this socket is <strong>network namespace aware</strong>.</p><p>Notice the <code>struct net *net</code> parameter in the function signature—that&#39;s the network namespace object. Inside this object, there is a member called <code>rtnl</code>, which is a pointer to this rtnetlink socket. After <code>rtnetlink_net_init()</code> calls <code>netlink_kernel_create()</code> to create the socket, it assigns the return value to <code>net-&gt;rtnl</code>.</p><p>What does this mean? It means that when you configure a network interface in Container A, the kernel only looks for this socket within the <code>struct net</code> corresponding to Container A; the host or other containers remain completely unaffected. Netlink was designed to work with network namespaces from the very beginning, not bolted on later as a patch.</p><hr><h3 id="netlink-kernel-create-the-factory-entrance" tabindex="-1"><code>netlink_kernel_create()</code>: The Factory Entrance <a class="header-anchor" href="#netlink-kernel-create-the-factory-entrance" aria-label="Permalink to &quot;\`netlink_kernel_create()\`: The Factory Entrance&quot;">​</a></h3><p>Let&#39;s zoom in and look at the parameter list of this <code>netlink_kernel_create()</code> &quot;routine&quot;:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sock </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">netlink_kernel_create</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">net</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                                   int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> unit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                                   struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> netlink_kernel_cfg </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">cfg</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>The first parameter, <code>net</code>, as we just mentioned, is the network namespace.</p><p>The second parameter, <code>unit</code>, is the <strong>Netlink protocol number</strong>.</p><ul><li>Want to handle routing? Use <code>NETLINK_ROUTE</code>.</li><li>Want to handle IPsec? Use <code>NETLINK_XFRM</code>.</li><li>Want to handle audit logs? Use <code>NETLINK_AUDIT</code>.</li></ul><p>There&#39;s a catch here: although the kernel defines over 20 protocols, the total number is capped at <strong>32</strong> (<code>MAX_LINKS</code>). This is why Generic Netlink was introduced later—the standard protocol numbers ran out, and a &quot;universal outlet&quot; was needed as a fallback. The full list of protocols is in <code>include/uapi/linux/netlink.h</code>.</p><p>The third parameter, <code>cfg</code>, is a configuration structure that tells the kernel: &quot;The socket I want must be built to these specifications.&quot;</p><hr><h3 id="the-configuration-structure-netlink-kernel-cfg" tabindex="-1">The Configuration Structure <code>netlink_kernel_cfg</code> <a class="header-anchor" href="#the-configuration-structure-netlink-kernel-cfg" aria-label="Permalink to &quot;The Configuration Structure \`netlink_kernel_cfg\`&quot;">​</a></h3><p>Let&#39;s look at the definition of this structure. Behind every field lies a set of rules:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> netlink_kernel_cfg {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    groups;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    flags;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">input)(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">skb);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mutex    </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cb_mutex;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bind)(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> group);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><h4 id="_1-groups-multicast-group-mask" tabindex="-1">1. <code>groups</code>: Multicast Group Mask <a class="header-anchor" href="#_1-groups-multicast-group-mask" aria-label="Permalink to &quot;1. \`groups\`: Multicast Group Mask&quot;">​</a></h4><p>This field specifies the multicast group (or rather, the multicast group mask). In the old days, userspace programs joined multicast groups by setting the <code>nl_groups</code> field inside <code>sockaddr_nl</code> (or by using libnl&#39;s <code>nl_join_groups()</code>). However, this approach was limited to 32 groups.</p><p><strong>The turning point</strong>: starting from kernel 2.6.14, things changed.</p><p>You can use <code>setsockopt</code> with the <code>NETLINK_ADD_MEMBERSHIP</code> / <code>NETLINK_DROP_MEMBERSHIP</code> options to join or leave a multicast group. This trick removes the 32-group limit. Under the hood, libnl&#39;s <code>nl_socket_add_memberships()</code> and <code>nl_socket_drop_memberships()</code> methods use this new mechanism.</p><h4 id="_2-flags-the-permission-control-switch" tabindex="-1">2. <code>flags</code>: The Permission Control Switch <a class="header-anchor" href="#_2-flags-the-permission-control-switch" aria-label="Permalink to &quot;2. \`flags\`: The Permission Control Switch&quot;">​</a></h4><p><code>flags</code> can be either <code>NL_CFG_F_NONROOT_RECV</code> or <code>NL_CFG_F_NONROOT_SEND</code>.</p><p>If <code>NL_CFG_F_NONROOT_RECV</code> is set, unprivileged users (non-root) are allowed to bind to a multicast group. You can see this check in the kernel&#39;s <code>netlink_bind()</code> code:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> netlink_bind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> socket </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">sock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sockaddr </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">addr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                         int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> addr_len</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (nladdr-&gt;nl_groups) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">         if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">netlink_capable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(sock, NL_CFG_F_NONROOT_RECV))</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                         return</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">EPERM;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>Without this flag set, when an unprivileged user tries to bind to a multicast group, <code>netlink_capable()</code> returns 0, and the kernel immediately throws a <code>-EPERM</code> (Permission denied).</p><p>Similarly, <code>NL_CFG_F_NONROOT_SEND</code> controls whether unprivileged users are allowed to send multicast messages.</p><h4 id="_3-input-the-soul-of-the-callback" tabindex="-1">3. <code>input</code>: The Soul of the Callback <a class="header-anchor" href="#_3-input-the-soul-of-the-callback" aria-label="Permalink to &quot;3. \`input\`: The Soul of the Callback&quot;">​</a></h4><p>This is the most critical field. If the <code>input</code> callback in <code>netlink_kernel_cfg</code> is <code>NULL</code>, this kernel socket will <strong>be unable to receive data from userspace</strong> (though it can still send data from the kernel to userspace).</p><p>For the rtnetlink socket, we specified <code>rtnetlink_rcv</code> as the callback. This means that all messages sent from userspace via the rtnetlink socket will ultimately be handed off to the <code>rtnetlink_rcv()</code> function for processing.</p><p><strong>The reverse case</strong>: for uevent (kernel event notifications), we only need one-way communication—from the kernel to userspace. So in <code>lib/kobject_uevent.c</code>, the uevent socket configuration looks like this:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> uevent_net_init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">net</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> uevent_sock </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ue_sk;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> netlink_kernel_cfg cfg </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        .groups    </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        .flags    </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> NL_CFG_F_NONROOT_RECV,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    };</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ue_sk-&gt;sk </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> netlink_kernel_create</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(net, NETLINK_KOBJECT_UEVENT, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cfg);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><p>Notice anything? There is no <code>input</code> callback. It&#39;s like a broadcast speaker that only transmits but never listens.</p><h4 id="_4-cb-mutex-the-mystery-of-the-lock" tabindex="-1">4. <code>cb_mutex</code>: The Mystery of the Lock <a class="header-anchor" href="#_4-cb-mutex-the-mystery-of-the-lock" aria-label="Permalink to &quot;4. \`cb_mutex\`: The Mystery of the Lock&quot;">​</a></h4><p><code>cb_mutex</code> is an optional mutex.</p><p>If you leave it empty, the kernel uses the default global lock <code>cb_def_mutex</code> (defined in <code>net/netlink/af_netlink.c</code>). <strong>In practice, most kernel Netlink sockets leave this field blank</strong> and simply use the default.</p><p>For example, the uevent socket (<code>NETLINK_KOBJECT_UEVENT</code>) and the audit socket (<code>NETLINK_AUDIT</code>) mentioned earlier both don&#39;t bother specifying a lock.</p><p>But rtnetlink is an <strong>exception</strong>. It uses its own lock, <code>rtnl_mutex</code>. Why? Because rtnetlink operations are too frequent and too critical—it doesn&#39;t want to contend for a lock with Netlink operations from other subsystems. Another exception is Generic Netlink, which also has its own lock, <code>genl_mutex</code>.</p><hr><h3 id="unregistration-and-lookup" tabindex="-1">Unregistration and Lookup <a class="header-anchor" href="#unregistration-and-lookup" aria-label="Permalink to &quot;Unregistration and Lookup&quot;">​</a></h3><p>When <code>netlink_kernel_create()</code> executes, it calls the <code>netlink_insert()</code> method to register itself in a global table called <code>nl_table</code>. Access to this table is protected by the read-write lock <code>nl_table_lock</code>.</p><p>Later, if someone needs to find this socket, they can use the <code>netlink_lookup()</code> method, specifying the protocol number and Port ID to locate it.</p><hr><h3 id="registering-message-handling-callbacks" tabindex="-1">Registering Message Handling Callbacks <a class="header-anchor" href="#registering-message-handling-callbacks" aria-label="Permalink to &quot;Registering Message Handling Callbacks&quot;">​</a></h3><p>Having a socket alone isn&#39;t enough; you need to tell the kernel: &quot;When you receive this type of message, call this function; when you receive that type, call that function.&quot;</p><p>This is exactly what <code>rtnl_register()</code> does. Throughout the networking kernel code, these callbacks are registered everywhere.</p><p>For example, in <code>rtnetlink_init()</code>, these message handlers are registered:</p><ul><li><code>RTM_NEWLINK</code>: Create a new link</li><li><code>RTM_DELLINK</code>: Delete a link</li><li><code>RTM_GETROUTE</code>: Dump the routing table</li></ul><p>And in <code>net/core/neighbour.c</code>, neighbor-related handlers are registered:</p><ul><li><code>RTM_NEWNEIGH</code>: Create a new neighbor</li><li><code>RTM_DELNEIGH</code>: Delete a neighbor</li><li><code>RTM_GETNEIGHTBL</code>: Dump the neighbor table</li></ul><p>(We&#39;ll dive deep into these operations in Chapters 5 and 7). Additionally, the FIB code, multicast code, and IPv6 code all register their respective callbacks.</p><p>Let&#39;s look at the prototype of <code>rtnl_register()</code>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">extern</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> rtnl_register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> protocol</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                          int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> msgtype</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                          rtnl_doit_func,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                          rtnl_dumpit_func,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                          rtnl_calcit_func);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Here is the parameter breakdown:</p><ol><li><strong>protocol</strong>: The protocol family. If not targeting a specific protocol, this is typically set to <code>PF_UNSPEC</code>. The full list of protocol families is in <code>include/linux/socket.h</code>.</li><li><strong>msgtype</strong>: The Netlink message type. For example, <code>RTM_NEWLINK</code>, <code>RTM_NEWNEIGH</code>. These are rtnetlink-specific extended types, all defined in <code>include/uapi/linux/rtnetlink.h</code>.</li><li><strong>The last three parameters</strong>: These are all callback function pointers. <ul><li><strong>doit</strong>: Used for &quot;create, delete, update&quot; operations.</li><li><strong>dumpit</strong>: Used for &quot;query/dump&quot; operations.</li><li><strong>calcit</strong>: Used to calculate the required buffer size.</li></ul></li></ol><p><strong>Usually, you only need to specify either doit or dumpit.</strong></p><p>Inside the rtnetlink module, there is a large table called <code>rtnl_msg_handlers</code>. This table is indexed by <strong>protocol number</strong>. Each entry in this table is itself a sub-table, indexed by <strong>message type</strong>. Finally, the elements of these sub-tables are <code>rtnl_link</code> structures, which hold the pointers to these three callback functions.</p><p>So, when you call <code>rtnl_register()</code>, you are essentially plugging a function pointer into a specific cell of this multi-dimensional array.</p><p>For example, in <code>net/core/rtnetlink.c</code>, there is a call like this:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rtnl_register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(PF_UNSPEC, RTM_NEWLINK, rtnl_newlink, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">NULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">NULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This line means: regardless of the protocol family (<code>PF_UNSPEC</code>), as long as a <code>RTM_NEWLINK</code> message is received, call the <code>rtnl_newlink</code> function.</p><hr><h3 id="how-the-kernel-sends-netlink-messages" tabindex="-1">How the Kernel Sends Netlink Messages <a class="header-anchor" href="#how-the-kernel-sends-netlink-messages" aria-label="Permalink to &quot;How the Kernel Sends Netlink Messages&quot;">​</a></h3><p>Conversely, what if the kernel needs to notify userspace?</p><p>Sending rtnetlink messages typically uses the <code>rtmsg_ifinfo()</code> method.</p><p>For example, when a device is brought up (<code>dev_open()</code>), the kernel creates a new link and then calls:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rtmsg_ifinfo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(RTM_NEWLINK, dev, IFF_UP</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">IFF_RUNNING);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>What happens behind the scenes? This can be broken down into four consecutive steps (see Figure 2-2):</p><ol><li><strong>Allocate space</strong>: Calls <code>nlmsg_new()</code> to allocate a properly sized <code>sk_buff</code> (the buffer used in the kernel to hold network packets).</li><li><strong>Build the header</strong>: Creates two core objects—the Netlink message header (<code>nlmsghdr</code>) and the immediately following <code>ifinfomsg</code> structure.</li><li><strong>Fill in the data</strong>: Calls <code>rtnl_fill_ifinfo()</code> to populate the specific network interface information (state, Flags, etc.).</li><li><strong>Deliver</strong>: Calls <code>rtnl_notify()</code> to send the packet. Internally, this function ultimately calls the generic <code>nlmsg_notify()</code> (defined in <code>net/netlink/af_netlink.c</code>) to actually push the packet out.</li></ol><p><img src="`+t+'" alt="figure"><em>Figure 2-2. Sending of rtnelink messages with the rtmsg_ifinfo() method</em></p><hr><p>So far, we&#39;ve been discussing the &quot;mechanism&quot;—how sockets are created and how functions are registered. But whether it&#39;s the kernel or userspace sending the data, <strong>what the packet itself looks like</strong> is the essence of the communication.</p><p>In the next section, we&#39;ll stop circling around the code and instead tear a packet open directly, staring right at the bytes inside. Welcome to the world of Netlink message headers.</p>',80)])])}const u=e(l,[["render",r]]);export{g as __pageData,u as default};
