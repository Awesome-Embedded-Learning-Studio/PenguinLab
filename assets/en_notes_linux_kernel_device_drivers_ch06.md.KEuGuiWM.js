import{_ as s,o as t,c as i,a as n}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"Chapter 6: The Cost of Exclusion: Critical Sections and Atomicity","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_device_drivers/ch06.md","filePath":"en/notes/linux_kernel_device_drivers/ch06.md","lastUpdated":null}'),a={name:"en/notes/linux_kernel_device_drivers/ch06.md"};function o(r,e,l,c,h,p){return t(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="chapter-6-the-cost-of-exclusion-critical-sections-and-atomicity" tabindex="-1">Chapter 6: The Cost of Exclusion: Critical Sections and Atomicity <a class="header-anchor" href="#chapter-6-the-cost-of-exclusion-critical-sections-and-atomicity" aria-label="Permalink to &quot;Chapter 6: The Cost of Exclusion: Critical Sections and Atomicity&quot;">​</a></h1><blockquote><p><strong>The Narrative Core of This Chapter</strong></p><p>Concurrency wasn&#39;t designed—it was a nuisance forcefully shoved into the system with the advent of multi-core CPUs and interrupts.</p><p>We will have to face the phantom of the &quot;critical section&quot; and learn to tame it with two tools that have completely different temperaments: the Mutex and the Spinlock. In this process, you&#39;ll realize the weight of the word &quot;atomicity&quot; and why kernel developers always have &quot;locking&quot; on the tip of their tongues.</p></blockquote><hr><h2 id="_6-0-chapter-prelude-when-order-begins-to-collapse" tabindex="-1">6.0 Chapter Prelude: When Order Begins to Collapse <a class="header-anchor" href="#_6-0-chapter-prelude-when-order-begins-to-collapse" aria-label="Permalink to &quot;6.0  Chapter Prelude: When Order Begins to Collapse&quot;">​</a></h2><p>There is a class of bugs that, at first glance, seems completely random and patternless—sometimes the program runs perfectly fine, and sometimes it crashes inexplicably. A reboot seems to fix it, but a while later, it breaks again. At this point, an experienced engineer will usually sigh and say, &quot;Looks like a concurrency issue.&quot;</p><p>Concurrency issues, or synchronization problems, are the most headache-inducing and fatal traps in operating system kernels.</p><p>Imagine you and another person are using the same notepad to keep accounts. Without an agreement, you both pick up a pen at the same time and write different numbers in the same row. What&#39;s the result? That row might turn into a messy scribble, or the person who wrote last overwrites the previous person&#39;s data. Either way, the ledger is wrong.</p><p>In the era of single-core CPUs, we could pretend this problem didn&#39;t exist—as long as we disabled interrupts or carefully yielded in our code, we seemed to get by peacefully. But now it&#39;s different. Today&#39;s chips are SMP (Symmetric Multiprocessing), and even single-board computers like the Raspberry Pi are multi-core. This means that at the exact same moment, there are genuinely multiple streams of code running in physical parallel on different CPU cores. Not to mention interrupt handlers that can butt in at any time.</p><p><strong>The old &quot;naive&quot; approach no longer works</strong>. You can&#39;t rely on luck. You need a strict set of rules to draw boundaries: within this code, only I can enter; everyone else (including other CPU cores and interrupts) must wait outside. This is why &quot;critical sections&quot; and &quot;mutual exclusion&quot; were born.</p><p><strong>If concurrency isn&#39;t handled properly, the consequence isn&#39;t just data corruption—it&#39;s complete system deadlock or crash.</strong></p><p>The mission of this chapter is to figure out two things:</p><ol><li><strong>Who is actually competing with whom?</strong> (Critical sections and data races)</li><li><strong>What weapons do we use to separate them?</strong> (Mutex vs. Spinlock)</li></ol><hr><h2 id="_6-1-critical-sections-exclusion-and-atomicity" tabindex="-1">6.1 Critical Sections, Exclusion, and Atomicity <a class="header-anchor" href="#_6-1-critical-sections-exclusion-and-atomicity" aria-label="Permalink to &quot;6.1  Critical Sections, Exclusion, and Atomicity&quot;">​</a></h2><h3 id="the-hidden-peril-of-concurrency-in-the-linux-kernel" tabindex="-1">The Hidden Peril of Concurrency in the Linux Kernel <a class="header-anchor" href="#the-hidden-peril-of-concurrency-in-the-linux-kernel" aria-label="Permalink to &quot;The Hidden Peril of Concurrency in the Linux Kernel&quot;">​</a></h3><p>Let&#39;s take a step back and think the problem through clearly.</p><p>Not all code needs protection. Only those code paths that <strong>access shared writable data</strong> are dangerous. We have a name for these dangerous code regions: <strong>Critical Sections</strong>.</p><p>A <strong>Data Race</strong> occurs when a critical section is executed concurrently without protection. Imagine two CPU cores executing <code>balance = balance + 1</code> at the same time. This line of code involves more than one step at the assembly level: it needs to read <code>balance</code> from memory, add 1, and write it back. If unfortunately, both CPUs read the old value (say, 100), each adds 1, and then writes back 101, the result is—an account that should have become 102 is only 101. That one dollar evaporated.</p><p>To solve this problem, we need to introduce two core concepts:</p><ol><li><strong>Mutual Exclusion</strong>: When I enter the restroom, I lock the door, and everyone else must wait outside. This is <strong>Mutual Exclusion</strong>.</li><li><strong>Atomicity</strong>: The operation is indivisible. It&#39;s either fully done or not done at all; there is no &quot;half-done and interrupted by someone else&quot; situation. This is <strong>Atomicity</strong>.</li></ol><p>In the kernel, to implement these two concepts, we primarily rely on two types of locks: <strong>Mutexes</strong> and <strong>Spinlocks</strong>.</p><hr><h3 id="mutex-or-spinlock-a-guide-for-the-indecisive" tabindex="-1">Mutex or Spinlock? A Guide for the Indecisive <a class="header-anchor" href="#mutex-or-spinlock-a-guide-for-the-indecisive" aria-label="Permalink to &quot;Mutex or Spinlock? A Guide for the Indecisive&quot;">​</a></h3><p>This is where kernel newcomers get most confused: which one should I use?</p><p>Simply put, these two guys have completely different behavioral patterns:</p><ul><li><p><strong>Mutex</strong>:</p><ul><li><strong>Behavior</strong>: If the lock is taken by someone else, you <strong>go to sleep</strong> (block) and yield the CPU. When the lock owner releases the lock, the kernel wakes you up.</li><li><strong>Use case</strong>: The critical section is relatively long, or you&#39;re doing operations that might block (like waiting for I/O).</li><li><strong>Cost</strong>: Context switch overhead (sleeping and waking up takes time).</li><li><strong>Mortal enemy</strong>: Cannot be used in interrupt context, because interrupt handlers cannot sleep.</li></ul></li><li><p><strong>Spinlock</strong>:</p><ul><li><strong>Behavior</strong>: If the lock is taken, you <strong>spin in place</strong> (busy loop), constantly asking &quot;Are we there yet? Are we there yet?&quot; until you acquire the lock.</li><li><strong>Use case</strong>: The critical section is extremely short (like changing a pointer or incrementing a counter). Also, you <strong>must use it in contexts where sleeping is not allowed</strong> (like interrupt handling or atomic contexts).</li><li><strong>Cost</strong>: If you hold the lock for too long, other CPUs spin idly, wasting CPU cycles, and can even cause system <strong>Thrashing</strong>—everyone is busy waiting for the lock, and nobody is doing real work.</li></ul></li></ul><p><strong>Summary in one sentence</strong>:</p><ul><li>Can sleep? Use <strong>Mutex</strong>.</li><li>Cannot sleep (or the time is extremely short)? Use <strong>Spinlock</strong>.</li></ul><hr><h3 id="using-mutexes" tabindex="-1">Using Mutexes <a class="header-anchor" href="#using-mutexes" aria-label="Permalink to &quot;Using Mutexes&quot;">​</a></h3><p>The Mutex is the most commonly used lock in the kernel; after all, in most cases we can comfortably run in process context.</p><h4 id="struct-mutex-and-initialization" tabindex="-1">struct mutex and Initialization <a class="header-anchor" href="#struct-mutex-and-initialization" aria-label="Permalink to &quot;struct mutex and Initialization&quot;">​</a></h4><p>The kernel uses the <code>struct mutex</code> data structure to represent a Mutex. There are usually two ways to define it:</p><p><strong>1. Static definition (compile-time):</strong></p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DEFINE_MUTEX</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(my_mutex);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This line of code defines and initializes a lock named <code>my_mutex</code>.</p><p><strong>2. Dynamic initialization (runtime):</strong></p><p>If you allocate the lock at runtime (for example, inside a <code>probe</code> function), you need to explicitly initialize it:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mutex my_mutex;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mutex_init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">my_mutex</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><h4 id="locking-and-unlocking-api" tabindex="-1">Locking and Unlocking API <a class="header-anchor" href="#locking-and-unlocking-api" aria-label="Permalink to &quot;Locking and Unlocking API&quot;">​</a></h4><p>Once you have a lock, using it is very intuitive.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 获取锁（进入临界区） */</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 如果锁不可用，当前进程会进入不可中断的睡眠 */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mutex_lock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">my_mutex</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* --- 临界区开始 --- */</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 访问共享数据，做你想做的修改 */</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* --- 临界区结束 --- */</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 释放锁（离开临界区） */</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 必须由同一个任务调用 */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mutex_unlock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">my_mutex</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><h4 id="unwritten-rules-for-using-mutexes-correctly" tabindex="-1">Unwritten Rules for Using Mutexes Correctly <a class="header-anchor" href="#unwritten-rules-for-using-mutexes-correctly" aria-label="Permalink to &quot;Unwritten Rules for Using Mutexes Correctly&quot;">​</a></h4><p>There are a few pitfalls here—step into them and you get deadlock, so please be careful:</p><p><strong>Deadlock Trap 1: Recursive Calls</strong> You absolutely cannot lock again while already holding the lock. If you try to <code>mutex_lock(&amp;my_mutex)</code> inside the critical section, you will deadlock. The kernel&#39;s Mutex <strong>does not support</strong> recursive locking. It will think it already holds the lock and then wait there until the end of time. This will genuinely blow up.</p><p><strong>Deadlock Trap 2: Out-of-Order Locking</strong> If you need to hold two locks simultaneously (say, Lock A and Lock B), <strong>you must enforce a system-wide consistent order</strong>. Everyone must take A first, then B. If one thread takes A and waits for B, while another takes B and waits for A, that&#39;s the classic <strong>AB-BA deadlock</strong>.</p><p><strong>Lock Leaks: Don&#39;t Forget to Go Home</strong> If you take a lock, you must release it. And you can&#39;t <code>return</code> inside the critical section, or jump elsewhere, otherwise the lock leaks and others will wait forever.</p><p><strong>Performance Trap: Don&#39;t Linger</strong> Although a Mutex allows sleeping, holding a lock for a long time degrades system concurrency performance. Lock granularity should be as small as possible—grab it, do your work quickly, and release it immediately.</p><hr><h3 id="using-spinlocks" tabindex="-1">Using Spinlocks <a class="header-anchor" href="#using-spinlocks" aria-label="Permalink to &quot;Using Spinlocks&quot;">​</a></h3><p>Now let&#39;s switch channels and talk about that &quot;hothead&quot;—the Spinlock.</p><p>Spinlocks are typically used in SMP systems, or single-core systems with kernel preemption enabled. Its core data structure is <code>spinlock_t</code>.</p><h4 id="basic-usage" tabindex="-1">Basic Usage <a class="header-anchor" href="#basic-usage" aria-label="Permalink to &quot;Basic Usage&quot;">​</a></h4><p>Similar to Mutexes, there are both static and dynamic initialization methods:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 静态 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DEFINE_SPINLOCK</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(my_spinlock);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 动态 */</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">spinlock_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> my_spinlock;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">spin_lock_init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">my_spinlock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>The most basic lock/unlock APIs are <code>spin_lock</code> and <code>spin_unlock</code>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">spin_lock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">my_spinlock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* --- 临界区 --- */</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 注意：这里绝对不能调用任何会引发睡眠的函数！ */</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* --- 临界区结束 --- */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">spin_unlock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">my_spinlock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>⚠️ <strong>Warning</strong> Never sleep while holding a spinlock (e.g., calling <code>kmalloc(GFP_KERNEL)</code>, <code>msleep</code>, etc.). If you do this, you will trigger one of the kernel&#39;s most famous errors: <strong>&quot;Scheduling while atomic&quot;</strong>. This means you&#39;re trying to schedule in an atomic context, and the system will directly panic or hang. This will genuinely send your blood pressure through the roof.</p><h4 id="the-tricky-case-of-handling-interrupts" tabindex="-1">The Tricky Case of Handling Interrupts <a class="header-anchor" href="#the-tricky-case-of-handling-interrupts" aria-label="Permalink to &quot;The Tricky Case of Handling Interrupts&quot;">​</a></h4><p>This is probably the most dizzying part of spinlocks.</p><p>Suppose you&#39;re holding a spinlock in process context, accessing shared data. Suddenly, an interrupt arrives, and the interrupt handler runs on the same CPU. It also tries to access this same data, so it goes for the lock too.</p><p><strong>Result</strong>: Deadlock. The interrupt handler will spin forever waiting for the lock to be released, but the lock holder (the process) was interrupted and has no chance to run, and thus no chance to release the lock. Infinite loop achieved.</p><p>To solve this problem, the kernel provides a set of APIs with the <code>irq</code> suffix. Their meaning goes beyond just locking—they also include <strong>enabling/disabling local CPU interrupts</strong>.</p><p><strong>Approach 1: <code>spin_lock_irq</code> / <code>spin_unlock_irq</code></strong></p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">spin_lock_irq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">my_spinlock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 获取锁并禁用硬件中断 */</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* --- 临界区 --- */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">spin_unlock_irq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">my_spinlock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 释放锁并恢复中断 */</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>This combination is simple and brute-force. It&#39;s suitable when you&#39;re unsure of the previous interrupt state, or when you don&#39;t care.</p><p><strong>Approach 2: <code>spin_lock_irqsave</code> / <code>spin_unlock_irqrestore</code> — The Safest Way</strong></p><p>In most cases, we should use this. It saves the current interrupt state into a flag variable and restores it upon unlocking.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> flags;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">spin_lock_irqsave</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">my_spinlock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, flags); </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 获取锁，禁用中断，并将之前的中断状态保存到 flags 中 */</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* --- 临界区 --- */</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">spin_unlock_irqrestore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">my_spinlock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, flags);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 恢复之前保存的中断状态，然后释放锁 */</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>Why is this the best? Because if the code environment where your critical section resides already requires interrupts to be disabled, using the <code>_irq</code> version might mistakenly re-enable interrupts. <code>_irqsave</code> ensures &quot;environmental restoration.&quot;</p><p><strong>Approach 3: <code>spin_lock_bh</code> / <code>spin_unlock_bh</code></strong></p><p>If you&#39;re not worried about hardware interrupts but are concerned about bottom halves—softirqs or tasklets—competing, you can use this. It disables bottom half execution but allows hardware interrupts to preempt.</p><hr><h3 id="spinlock-usage-summary-—-cheat-sheet" tabindex="-1">Spinlock Usage Summary — Cheat Sheet <a class="header-anchor" href="#spinlock-usage-summary-—-cheat-sheet" aria-label="Permalink to &quot;Spinlock Usage Summary — Cheat Sheet&quot;">​</a></h3><p>Let&#39;s organize the pile of APIs above and match them to their scenarios:</p><table tabindex="0"><thead><tr><th style="text-align:left;">Scenario</th><th style="text-align:left;">Recommended API</th><th style="text-align:left;">Characteristics</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>Simple case</strong><br>Only in process context, no data shared with interrupts/bottom halves</td><td style="text-align:left;"><code>spin_lock()</code><br><code>spin_unlock()</code></td><td style="text-align:left;">Lowest overhead. Pure spinlock.</td></tr><tr><td style="text-align:left;"><strong>Medium complexity</strong><br>Process context shares data with interrupts, don&#39;t care about saving interrupt state</td><td style="text-align:left;"><code>spin_lock_irq()</code><br><code>spin_unlock_irq()</code></td><td style="text-align:left;">Acquires lock and disables local interrupts. Simple but somewhat brute-force.</td></tr><tr><td style="text-align:left;"><strong>Safest / Complex</strong><br>Process context shares data with interrupts, needs strict state restoration</td><td style="text-align:left;"><code>spin_lock_irqsave()</code><br><code>spin_unlock_irqrestore()</code></td><td style="text-align:left;"><strong>This is the most recommended general practice</strong>. Saves and restores interrupt state, no side effects.</td></tr><tr><td style="text-align:left;"><strong>Fighting bottom halves</strong><br>Competing with Softirqs / Tasklets</td><td style="text-align:left;"><code>spin_lock_bh()</code><br><code>spin_unlock_bh()</code></td><td style="text-align:left;">Disables bottom half execution, allows hardware interrupts.</td></tr></tbody></table><p><strong>Now looking back at the question from the prelude: who is competing? Multi-core and interrupts. What do we use to separate them? If sleeping is allowed, use a Mutex to yield the CPU; if sleeping is not allowed or the time is extremely short, you have to bite the bullet and stand guard with a Spinlock.</strong></p><hr><h3 id="so-what-are-spinlocks-doing-on-a-uniprocessor-system" tabindex="-1">So, What Are Spinlocks Doing on a Uniprocessor System? <a class="header-anchor" href="#so-what-are-spinlocks-doing-on-a-uniprocessor-system" aria-label="Permalink to &quot;So, What Are Spinlocks Doing on a Uniprocessor System?&quot;">​</a></h3><p>You might ask: <strong>Do spinlocks still make sense on a uniprocessor (UP) system?</strong></p><p>That&#39;s a really good question.</p><p>Since there&#39;s only one CPU, if the lock holder is running, the waiter can&#39;t run. How can the waiter spin? It can&#39;t run at all!</p><p>The answer is: <strong>On UP systems, the &quot;spinning&quot; logic of spinlocks is optimized away</strong>. <code>spin_lock</code> is replaced with a no-op at compile time (or merely an increment to the preemption counter).</p><p><strong>However!</strong> Note the <code>irq</code> related APIs. Even on UP systems, <code>spin_lock_irqsave</code> <strong>still has meaning</strong>. Because it disables interrupts. This is exactly what we need—the only way to prevent deadlock on a uniprocessor is to ensure that when you get interrupted, the interrupt won&#39;t try to contend for the same lock.</p><p>So, there is an extremely important engineering principle here:</p><blockquote><p><strong>As a driver developer, don&#39;t worry about whether it&#39;s UP or SMP.</strong> Always write according to SMP logic, and always use standard APIs. The kernel internals will handle the details for you. On a uniprocessor, those useless spin loops will automatically disappear, leaving only the necessary interrupt-disabling logic.</p></blockquote><hr><h3 id="supplement-regarding-local-locks-in-the-5-8-kernel" tabindex="-1">Supplement: Regarding &quot;Local Locks&quot; in the 5.8 Kernel <a class="header-anchor" href="#supplement-regarding-local-locks-in-the-5-8-kernel" aria-label="Permalink to &quot;Supplement: Regarding &quot;Local Locks&quot; in the 5.8 Kernel&quot;">​</a></h3><p>That&#39;s not all. In the 5.8 kernel, the real-time Linux (PREEMPT_RT) project introduced a new term—&quot;Local Locks.&quot; This might be too far out for newcomers; just knowing it exists is enough: its main purpose is to provide optimizations for hard real-time kernels, but on non-real-time kernels, it&#39;s very helpful for lock debugging (especially when paired with tools like lockdep). If you&#39;re writing code with extremely strict real-time requirements, you can look up articles about this feature on LWN.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: Understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: Understanding&quot;">​</a></h3><p><strong>Question</strong>: Suppose your Linux driver has a global static variable <code>static int safety_count;</code>, and in the driver&#39;s <code>read</code> method, you have the following code: <code>safety_count++;</code>. If this driver runs on a multi-core system with <code>CONFIG_SMP</code> enabled and uses no locking mechanism, please explain why this could lead to data inconsistency (dirty reads or lost updates)?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Because the <code>i++</code> operation is not an atomic instruction on most processor architectures; it typically involves three steps: &quot;read-modify-write.&quot; Without lock protection, two threads might read the same old value simultaneously, each increment by one, and write it back, causing one increment to be lost, or they might read data that is only half-updated.</p><p><strong>Analysis</strong>: This tests your understanding of Critical Sections and Data Races. Although a single line of C code looks simple, the machine code generated by the compiler usually involves multiple instructions like load, inc, and store. In a multi-core environment, Thread A and Thread B might execute the load instruction simultaneously, get the same initial value, then each increment and write back, resulting in a final value that only increased by 1 instead of the expected 2. This is a classic data race caused by an unprotected critical section.</p></details><h3 id="exercise-2-application" tabindex="-1">Exercise 2: Application <a class="header-anchor" href="#exercise-2-application" aria-label="Permalink to &quot;Exercise 2: Application&quot;">​</a></h3><p><strong>Question</strong>: You are writing a block device driver and need to allocate memory and copy large amounts of user data in the process context <code>write</code> method, while also protecting a frequently accessed global linked list. Should you choose a Mutex or a Spinlock? Why?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: You should choose a Mutex.</p><p><strong>Analysis</strong>: This tests your understanding of the criteria for choosing between a Mutex and a Spinlock. The scenario involves &quot;process context&quot; and &quot;allocating memory&quot; and &quot;copying large amounts of data&quot;—all of which are potentially blocking operations. The Spinlock is designed for protecting short critical sections; while held, the thread is in a busy-wait state and cannot sleep. If you call a function that might sleep (like memory allocation) while holding a Spinlock, it will cause a kernel crash or hang. A Mutex allows the holder to sleep, making it suitable for scenarios that might take time and require blocking.</p></details><h3 id="exercise-3-application" tabindex="-1">Exercise 3: Application <a class="header-anchor" href="#exercise-3-application" aria-label="Permalink to &quot;Exercise 3: Application&quot;">​</a></h3><p><strong>Question</strong>: In a character device driver, both the driver&#39;s <code>read</code> method (process context) and the bottom-half interrupt handler (softirq context) access the same shared queue. If you can only use locking mechanisms for synchronization, which lock API should you choose to protect the critical section in the <code>read</code> method? Please write out the specific lock function calls.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: You should use <code>spin_lock_irqsave()</code> and <code>spin_unlock_irqrestore()</code> (or use <code>spin_lock_irq()</code> / <code>spin_unlock_irq()</code> if you are certain about the interrupt state).</p><p><strong>Analysis</strong>: This tests your understanding of synchronization strategies when data is shared between interrupt context and process context. Softirqs can preempt process context execution. If the process context only uses a plain <code>spin_lock()</code>, it might be interrupted by a local CPU softirq while holding the lock. If the softirq code also tries to acquire the same lock, a deadlock occurs (because the process holding the lock hasn&#39;t had a chance to release it before being interrupted). Therefore, you must use the <code>spin_lock_irq*</code> family of functions, which disable local CPU interrupts while acquiring the lock, ensuring the critical section cannot be interrupted by local interrupt handlers.</p></details><h3 id="exercise-4-thinking" tabindex="-1">Exercise 4: Thinking <a class="header-anchor" href="#exercise-4-thinking" aria-label="Permalink to &quot;Exercise 4: Thinking&quot;">​</a></h3><p><strong>Question</strong>: Suppose a real-time system has three threads: high-priority thread H, medium-priority thread M, and low-priority thread L. Thread L holds a Mutex, thread H is waiting for that lock, and thread M is currently occupying the CPU running (not waiting for the lock). What happens in this situation? How does this differ from using an RT-mutex?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Priority inversion will occur. Thread H is blocked waiting for L, but L cannot run to release the lock because of its low priority, and is instead preempted by M, which has a medium priority and doesn&#39;t need to wait for the lock. This causes H to be effectively blocked by M, behaving as if it had M&#39;s low priority. RT-mutex solves this problem through a priority inheritance mechanism: when H waits for the lock held by L, L&#39;s priority is temporarily boosted to H&#39;s level, allowing it to execute quickly and release the lock, avoiding being preempted by M for a long time.</p><p><strong>Analysis</strong>: This tests your understanding of the classic concurrency problem &quot;priority inversion&quot; and its solutions. When using a regular Mutex, the scheduler only schedules based on static priority, causing a high-priority task to be indirectly subject to a medium-priority task. The priority inheritance protocol introduced by RT-mutex ensures that a low-priority task holding a lock can obtain sufficient CPU time to complete its work when necessary, thereby eliminating unbounded priority inversion delays.</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>Concurrency issues stem from multi-core physical parallelism and interrupt preemption, causing non-atomic operations on shared data (like &quot;read-modify-write&quot;) to result in data races. The core risk lies in instruction sequences within critical sections being interfered with. Therefore, the system must introduce mutual exclusion mechanisms to draw boundaries, ensuring exclusivity when accessing shared writable state to prevent data corruption or system crashes.</p><p>The primary tools for handling concurrency are Mutexes and Spinlocks, and the choice between them depends on critical section length and context requirements. Mutexes are suitable for process contexts that allow sleeping and for longer operations, avoiding CPU waste through sleeping; Spinlocks are suitable for extremely short critical sections or contexts where sleeping is not allowed (like atomic contexts), maintaining protection of the critical section through CPU busy-waiting.</p><p>Using Mutexes requires strict adherence to rules: no recursive locking, no jumps or returns during lock holding that could lead to lock leaks, and lock holding time should be as short as possible. Additionally, when multiple locks are involved, a globally consistent locking order must be followed to prevent the classic AB-BA deadlock, because the kernel Mutex mechanism itself does not automatically detect or handle such deadlocks.</p><p>Using Spinlocks is more demanding than Mutexes, absolutely prohibiting any calls to functions that might cause sleep (like memory allocation or delays) within the critical section, otherwise it triggers a &quot;Scheduling while atomic&quot; error leading to a system crash. On uniprocessor systems, the &quot;busy-wait&quot; logic of spinning is optimized away, but its function of cooperating with interrupt control mechanisms (like <code>spin_lock_irqsave</code>) to prevent deadlocks is still preserved.</p><p>When interrupt handlers and process context have data races, you must use interrupt-controlling APIs (recommended <code>spin_lock_irqsave</code>/<code>spin_unlock_irqsave</code>). This not only acquires the lock but also safely masks local CPU hardware interrupts and saves state, preventing interrupt handlers from trying to acquire an already-held lock on the same CPU and causing a deadlock.</p><p>Remember the question from the beginning—<strong>who is actually competing with whom?</strong> The answer is <strong>multi-core and interrupts</strong>. <strong>What weapon do we use to separate them?</strong> The answer is: <strong>if sleeping is allowed, use a Mutex to yield the CPU; if sleeping is not allowed or the time is extremely short, you have to bite the bullet and stand guard with a Spinlock</strong>.</p>`,111)])])}const g=s(a,[["render",o]]);export{u as __pageData,g as default};
