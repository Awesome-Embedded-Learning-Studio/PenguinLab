import{_ as t,o,c as a,a as i}from"./chunks/framework.8tiMeCK-.js";const n="/PenguinLab/assets/ch08_fig117.CrVpdAz9.webp",p=JSON.parse('{"title":"8.2  Locking Mechanisms — Key Concepts Cheat Sheet","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch08_2.md","filePath":"en/notes/linux_kernel_debugging/ch08_2.md","lastUpdated":null}'),r={name:"en/notes/linux_kernel_debugging/ch08_2.md"};function s(l,e,c,h,d,u){return o(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="_8-2-locking-mechanisms-—-key-concepts-cheat-sheet" tabindex="-1">8.2 Locking Mechanisms — Key Concepts Cheat Sheet <a class="header-anchor" href="#_8-2-locking-mechanisms-—-key-concepts-cheat-sheet" aria-label="Permalink to &quot;8.2  Locking Mechanisms — Key Concepts Cheat Sheet&quot;">​</a></h1><p>I&#39;ve rambled on about a lot of prerequisites earlier, but to make sure we&#39;re on the same page, there are a few core principles about &quot;locks&quot; that bear repeating. Think of this as a cheat sheet — but remember, in concurrent programming, memorizing the rules and truly understanding them are two very different things. The latter requires paying some tuition in the form of debugging pain.</p><h3 id="what-is-a-critical-section" tabindex="-1">What is a Critical Section? <a class="header-anchor" href="#what-is-a-critical-section" aria-label="Permalink to &quot;What is a Critical Section?&quot;">​</a></h3><p>First, let&#39;s profile the enemy.</p><p>A <strong>critical section</strong> is a segment of a code path that can be executed concurrently by multiple execution paths (threads, processes, or even interrupt handlers), and this code reads or writes <strong>shared writable data</strong>.</p><p>As long as both conditions are met — concurrent execution + access to shared writable state — you have a critical section.</p><p>Sounds simple, right? But the devil is in the details.</p><h3 id="what-is-a-critical-section-afraid-of" tabindex="-1">What is a Critical Section Afraid Of? <a class="header-anchor" href="#what-is-a-critical-section-afraid-of" aria-label="Permalink to &quot;What is a Critical Section Afraid Of?&quot;">​</a></h3><p>Because it operates on shared writable data, a critical section is extremely fragile. It requires two different layers of protection:</p><ol><li><p><strong>Preventing Parallelism</strong> This means it must &quot;stand alone.&quot; At any given time, only one execution flow can enter this code. This is what we commonly call mutual exclusion — forcibly turning parallel execution into serial execution.</p></li><li><p><strong>Atomicity</strong> If you&#39;re running in an atomic context (such as an interrupt handler <code>ISR</code>, a softirq, or holding <code>tasklet</code>), you not only cannot be preempted, but you also cannot sleep. In this case, the critical section must execute <strong>indivisibly from start to finish</strong>. Any operation that requires waiting or sleeping is strictly contraband here.</p></li></ol><h3 id="guarding-the-critical-section-the-art-of-locking" tabindex="-1">Guarding the Critical Section: The Art of Locking <a class="header-anchor" href="#guarding-the-critical-section-the-art-of-locking" aria-label="Permalink to &quot;Guarding the Critical Section: The Art of Locking&quot;">​</a></h3><p>Since critical sections are so dangerous, how do we protect them? Step one is identification; step two is locking.</p><p><strong>Identifying the critical section is the first and most crucial step.</strong> You must scrutinize your code like a bomb disposal expert: Who will access this global variable? Will that interrupt handler touch this linked list head? Miss one, and you&#39;ve left a ticking time bomb.</p><p>Once identified, the usual protection mechanism is <strong>locking</strong> (of course, there&#39;s also the more advanced lock-free programming, but that&#39;s a high-level skill).</p><p>Here are two pitfalls that beginners fall into most often, and they deserve special attention:</p><p><strong>❌ Bad Habit 1: Only Protecting Write Operations</strong></p><p>&quot;I&#39;m only modifying the data, and you&#39;re all just reading, so it should be fine, right?&quot;</p><p>Wrong. This is a very dangerous illusion. If you only protect writes and allow concurrent read and write operations to happen simultaneously, the read operation might fetch <strong>torn dirty data</strong>. Imagine the write operation is only halfway done (for example, it has written the first 32 bits of a 64-bit variable) when the read operation rushes in and grabs this incomplete value — this is what&#39;s known as <code>torn read</code>. What you read is neither the old value nor the new value, but a meaningless garbage value.</p><p><strong>❌ Bad Habit 2: Using the Wrong Lock Object</strong></p><p>&quot;I used a lock, so why did it still blow up?&quot;</p><p>Look back at your code. When protecting the same data structure, are you using the <strong>same lock variable</strong>?</p><p>This is another fatal error: using the wrong lock, or using different locks in different code paths to protect the same resource. It&#39;s like trying to use your front door key to open the back door lock — not only won&#39;t it open, but you might break the key off inside. Protection is only valid when all places accessing that data use the <strong>exact same lock</strong>.</p><h3 id="when-protection-fails-data-races" tabindex="-1">When Protection Fails: Data Races <a class="header-anchor" href="#when-protection-fails-data-races" aria-label="Permalink to &quot;When Protection Fails: Data Races&quot;">​</a></h3><p>If you fail to protect it, or don&#39;t protect it at all, you end up with a <strong>data race</strong>.</p><p>Data races are the root of all evil in concurrent programming. In this scenario, the program&#39;s output no longer depends on code logic, but on — luck. This is known as timing dependency. This type of bug is a classic &quot;Heisenbug&quot;: when you try to observe it with a debugger, it might disappear or change its behavior, because the act of observation itself alters the execution timing.</p><p>Once this kind of bug makes it into production, reproducing it, locating it, and fixing it will make you nostalgic for the days of writing business logic.</p><p>There&#39;s a deeper story behind data races, which we&#39;ll dive into in the next subsection.</p><h3 id="when-can-you-skip-locking" tabindex="-1">When Can You Skip Locking? <a class="header-anchor" href="#when-can-you-skip-locking" aria-label="Permalink to &quot;When Can You Skip Locking?&quot;">​</a></h3><p>To save you from developing paranoia, here are a few exceptional cases where you <strong>do not</strong> need to add locks.</p><ol><li><p><strong>Local Variables</strong> If you&#39;re working with local variables, they reside on the private stack of the process (or interrupt). Since they&#39;re private, there&#39;s no sharing, making them naturally safe.</p></li><li><p><strong>Naturally Serialized Code</strong> Some code is logically guaranteed to run only once. For example, the <code>init</code> and <code>cleanup</code> functions in our kernel module — they only execute once during <code>insmod</code> and <code>rmmod</code>, so there&#39;s absolutely no concurrency risk.</p></li><li><p><strong>Truly Read-Only Data</strong> If the data is a true constant (read-only), then everyone can read it however they please. However, be careful with C&#39;s <code>const</code> keyword — sometimes it only enforces read-only behavior at compile time and doesn&#39;t guarantee the data is truly unmodifiable at runtime.</p></li><li><p><strong>Explicitly Marked Plain Accesses</strong> There&#39;s also a special case explicitly documented in the kernel regarding when plain C language memory accesses are acceptable. The documentation is located at <code>tools/memory-model/Documentation/access-marking.txt</code> in the source tree. Outside of that, exercise caution.</p></li></ol><hr><h3 id="understanding-data-races-—-diving-into-lkmm" tabindex="-1">Understanding Data Races — Diving into LKMM <a class="header-anchor" href="#understanding-data-races-—-diving-into-lkmm" aria-label="Permalink to &quot;Understanding Data Races — Diving into LKMM&quot;">​</a></h3><p>If you thought &quot;one write, one read&quot; was the full extent of data races, you might be underestimating the complexity of this issue.</p><p>As you dig deeper into concurrency topics, you&#39;ll discover a vast ocean of knowledge: memory ordering, memory barriers, lock-free programming, and more. Let&#39;s start with the most basic definition.</p><h4 id="what-exactly-is-a-data-race" tabindex="-1">What Exactly is a Data Race? <a class="header-anchor" href="#what-exactly-is-a-data-race" aria-label="Permalink to &quot;What Exactly is a Data Race?&quot;">​</a></h4><p>The Linux kernel has its own memory consistency model called <strong>LKMM (Linux Kernel Memory Model)</strong>. Under this model, the definition of a data race is extremely precise. It must simultaneously satisfy all four of the following conditions:</p><ol><li><strong>Same Address</strong>: Both accesses target the same memory location.</li><li><strong>Concurrent Execution</strong>: The two accesses occur on different threads or different CPU cores (with no guaranteed ordering).</li><li><strong>At Least One Write</strong>: At least one of the two operations is a write.</li><li><strong>At Least One &quot;Plain Access&quot;</strong>: This is the key! At least one of the two operations is a <strong>plain C-language access</strong>.</li></ol><p><strong>This last point is a blind spot that even many veterans overlook.</strong></p><p>Look at the figure below (from Marco Elver&#39;s presentation at Linux Plumbers Conference 2020). It uses a table to intuitively show what constitutes a data race:</p><p><img src="'+n+'" alt="Figure 8.2 – Screenshot from M Elver&#39;s presentation on data race detection in the kernel"></p><p><em>(Figure 8.2 – Screenshot from M Elver&#39;s presentation on data race detection in the kernel)</em></p><p>In the table, <strong>X</strong> represents a red alert (data race exists), and <strong>✅</strong> represents safe. Let&#39;s go through it row by row:</p><ul><li><p><strong>Row 1 (Race)</strong>:</p><ul><li>Thread 0: <strong>Plain read</strong> (<code>READ(x)</code>)</li><li>Thread 1: <strong>Plain write</strong> (<code>WRITE(x)</code>)</li><li><strong>Conclusion</strong>: A classic, textbook data race. One thread is reading while another is writing simultaneously, and both are plain C operations.</li></ul></li><li><p><strong>Row 2 (Race)</strong>:</p><ul><li>Thread 0: <strong>Marked read</strong> (Marked Read, such as <code>READ_ONCE(x)</code>)</li><li>Thread 1: <strong>Plain write</strong> (Plain Write)</li><li><strong>Conclusion</strong>: Still a data race! Because &quot;at least one is a plain access&quot; and a write operation occurred. Even though Thread 0 conscientiously used a marked macro, Thread 1&#39;s barbaric operation ruined everything.</li></ul></li><li><p><strong>Rows 3, 4, 5</strong>:</p><ul><li>Same principle as Row 2. As long as one side is &quot;plain&quot; and a write operation exists, a race is inevitable.</li></ul></li><li><p><strong>Rows 6, 7 (Safe)</strong>:</p><ul><li>In these two rows, both Thread 0 and Thread 1 use <strong>marked accesses</strong>.</li><li>For example, <code>READ_ONCE()</code>, <code>WRITE_ONCE()</code>, or the more advanced <code>atomic_t</code>, <code>refcount_t</code>, and the <code>smp_load_acquire()</code> family of macros.</li><li>These operations are designed to be atomic and follow LKMM&#39;s memory ordering rules.</li><li><strong>Conclusion</strong>: ✅ Safe.</li></ul></li></ul><h4 id="what-does-marked-actually-mean-here" tabindex="-1">What Does &quot;Marked&quot; Actually Mean Here? <a class="header-anchor" href="#what-does-marked-actually-mean-here" aria-label="Permalink to &quot;What Does &quot;Marked&quot; Actually Mean Here?&quot;">​</a></h4><p>Now we have to bring back that analogy — remember when we said plain C accesses are like &quot;crossing the street without looking at the traffic lights&quot;?</p><p>A &quot;marked access&quot; here is &quot;looking at the traffic lights and using the crosswalk.&quot;</p><p>But the real world is harsher than analogies. Plain C statements (like <code>int a = x;</code>) can be freely optimized by the compiler — the compiler might cache them in a register, split them into two instructions, or reorder them with other instructions. This is fine in single-threaded code, but in a multi-core concurrent environment, it&#39;s a disaster.</p><p>The purpose of macros like <code>READ_ONCE()</code> / <code>WRITE_ONCE()</code> is to:</p><ol><li><strong>Tell the compiler not to mess with it</strong>: Prevent the compiler from performing dangerous optimizations (such as merging multiple accesses or caching them in a register).</li><li><strong>Guarantee atomicity</strong>: Ensure aligned accesses complete within a single bus cycle.</li><li><strong>Make intent explicit</strong>: Tell anyone reading the code (and the KCSAN tool): &quot;I&#39;ve reached a concurrency boundary here, and I&#39;ve handled it.&quot;</li></ol><h3 id="the-deep-waters-of-knowledge" tabindex="-1">The Deep Waters of Knowledge <a class="header-anchor" href="#the-deep-waters-of-knowledge" aria-label="Permalink to &quot;The Deep Waters of Knowledge&quot;">​</a></h3><p>By this point, you should feel that the waters of concurrency run much deeper than you might have imagined.</p><p>To truly master it, you need to understand general concurrency concepts, memory ordering, memory barriers, LKMM, marked accesses, and the underlying mechanisms of lock-free programming... Each of these topics is worthy of its own book (in fact, the recommended reading <em>LKP-2</em> has dedicated chapters covering these).</p><p>Since relying purely on the human brain to wrestle with such complex rules is already proving difficult, shouldn&#39;t we find some tools to help? Absolutely. In the next section, we&#39;ll bring in the &quot;security scanner&quot; of the kernel concurrency world — KCSAN.</p>',53)])])}const m=t(r,[["render",s]]);export{p as __pageData,m as default};
