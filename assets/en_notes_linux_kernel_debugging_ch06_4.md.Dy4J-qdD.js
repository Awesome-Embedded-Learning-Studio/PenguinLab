import{_ as s,o as a,c as i,a as t}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"6.4  Decoding SLUB Debug Error Reports","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch06_4.md","filePath":"en/notes/linux_kernel_debugging/ch06_4.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_debugging/ch06_4.md"};function o(r,e,l,h,p,d){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="_6-4-decoding-slub-debug-error-reports" tabindex="-1">6.4 Decoding SLUB Debug Error Reports <a class="header-anchor" href="#_6-4-decoding-slub-debug-error-reports" aria-label="Permalink to &quot;6.4  Decoding SLUB Debug Error Reports&quot;">​</a></h1><p>Alright, now that we&#39;ve added <code>slub_debug=FZPU</code> to the kernel boot parameters and successfully triggered some bugs, let&#39;s look at what happened. As we saw at the end of the previous section, the SLUB debug mechanism did catch the culprit and spat out a rather intimidating pile of error logs.</p><p>But what exactly is that log dump trying to say?</p><p>Don&#39;t worry—that&#39;s exactly what we&#39;ll do in this section: dissect the SLUB debug output line by line, just like a forensic report. You&#39;ll find that this error report is densely packed with clues—everything from which cache had the incident, down to the exact line of code that wrote the illegal byte. It&#39;s all in there.</p><p>Since we already have the <code>run_tests</code> script handy, let&#39;s pull up a few typical test cases and examine them alongside the logs.</p><h4 id="decoding-the-out-of-bounds-write-report" tabindex="-1">Decoding the &quot;Out-of-Bounds Write&quot; Report <a class="header-anchor" href="#decoding-the-out-of-bounds-write-report" aria-label="Permalink to &quot;Decoding the &quot;Out-of-Bounds Write&quot; Report&quot;">​</a></h4><p>Let&#39;s start with test case #5.2. This is a typical <strong>Right OOB Write</strong>—meaning we wrote data past the end of a legitimate slab object. It&#39;s the one marked as <code>[V4]</code> in Table 6.2.</p><p>As soon as you execute this operation, the SLUB debug framework immediately jumps out and loudly complains. Its output looks something like this (don&#39;t be intimidated by the length; we&#39;ll break it down piece by piece):</p><blockquote><p><strong>(Figure 6.3 insertion point: Screenshot of SLUB catching a right out-of-bounds write, part 1)</strong></p></blockquote><p><strong>Line 1: Who Raised the Alarm</strong></p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BUG kmalloc-32 (Tainted: G    B      OE    ): Right Redzone overwritten</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Immediately following <code>BUG</code> is the name of the slab cache where the incident occurred—in this case, <code>kmalloc-32</code>. This makes perfect sense, since our test code dynamically allocated exactly 32 bytes (<code>kmalloc(32, ...)</code>). The subsequent <code>Right Redzone overwritten</code> is the diagnostic conclusion: <strong>Right redzone overwritten</strong>. This is very straightforward—what we did in the <code>dynamic_mem_oob_right()</code> function was exactly this: we wrote data at the 32nd byte position (the legitimate range was 0–31).</p><p>By the way, the <code>Tainted: ...</code> that appears here is the kernel&#39;s taint flag. Because we loaded an unofficial test module, the kernel considers itself &quot;tainted.&quot; This flag tells you whether the kernel is still in a &quot;trusted&quot; state.</p><p><strong>Line 2: Crime Scene Coordinates</strong></p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>INFO: 0x00000000d969b0bf-0x00000000d969b0bf</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This <code>INFO</code> line gives the start and end addresses of the corrupted memory region. You might notice that these addresses look a bit odd—they don&#39;t look like the kernel virtual addresses you normally see. That&#39;s correct: these addresses are <strong>hashed</strong>. This is a security measure to prevent attackers from inferring the memory layout directly from logs and leaking information. Don&#39;t forget, we&#39;re running these tests on a production-grade kernel.</p><p><strong>Line 3: Who is the Culprit</strong></p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>INFO: dynamic_mem_oob_right+0x39/0x9c [test_kmembugs]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This is one of the most valuable pieces of information in the entire report. It tells us exactly where in the code the erroneous access occurred. The format is the standard <code>&lt;函数&gt;+偏移/长度 [模块名]</code>. In plain English: the error occurred in the <code>dynamic_mem_oob_right()</code> function within the <code>test_kmembugs</code> module, at an offset of <code>0x39</code> bytes from the start of the function (the kernel estimates the total length of this function to be <code>0x9c</code>). (We&#39;ll cover how to use this offset to pinpoint the exact source code line in the next chapter. Just keep this skill in mind for now.)</p><p>The right side of this line usually also displays the process context—the PID and which CPU core it was running on.</p><p><strong>Line 4: The Backtrace</strong></p><p>Next comes a long kernel stack backtrace:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>kmem_cache_alloc_trace+0x40b/0x450</span></span>
<span class="line"><span>dynamic_mem_oob_right+0x39/0x9c [test_kmembugs]</span></span>
<span class="line"><span>dbgfs_run_testcase+0x4d9/0x59a [test_kmembugs]</span></span>
<span class="line"><span>vfs_write+0xca/0x2c0</span></span>
<span class="line"><span>    [...]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>There&#39;s a rule for reading stack frames: <strong>read from bottom to top</strong>, and ignore lines starting with <code>?</code>. You can clearly see the call chain: user space enters the kernel via <code>vfs_write</code>, and ultimately crashes at <code>dynamic_mem_oob_right()</code>.</p><p><strong>Line 5: Who Poisoned It</strong></p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>INFO:Freed in kvfree+0x28/0x30 [...]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>SLUB also thoughtfully (if it could put up a wanted poster) tells you who freed this memory. This information usually helps you pinpoint the culprit—because in most cases, the task that freed the memory is the same one that originally allocated it.</p><p>Below this <code>INFO:</code> line, there is usually the call stack from when the free occurred. However, note that this &quot;who freed it&quot; information isn&#39;t always accurate; sometimes it might not be available.</p><hr><p>The next part is even more interesting: a bunch of statistics and a memory snapshot.</p><blockquote><p><strong>(Figure 6.4 insertion point: Screenshot of SLUB error, part 2, showing the corrupted redzone, object memory, and padding contents)</strong></p></blockquote><p>This shows the slab&#39;s statistics, the state of the unfortunate object, and—<strong>most critically</strong>—the actual memory contents. You can see the values of the left and right redzones, the padding bytes, and the contents of the actual memory area.</p><p>Let&#39;s look back at that test code:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ch5/kmembugs_test.c</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> dynamic_mem_oob_right</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> mode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    volatile</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">kptr, ch </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *volatile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ptr;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    size_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sz </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    kptr </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kmalloc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(sz, GFP_KERNEL);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // [...]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ptr </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)kptr </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sz </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // right OOB</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // [...]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (mode </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> WRITE) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        /* 这里的 OOB 访问不会被 UBSAN 抓到，但会被 KASAN/SLUB 抓到！ */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">volatile</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)ptr </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;x&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // invalid, OOB right write</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        /* 而下面这些 OOB 访问会被 KASAN/UBSAN 抓到。</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           结论：只有基于索引的访问才会被 UBSAN 抓。 */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">        kptr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[sz] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;x&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // invalid, OOB right write</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br></div></div><p>In the two highlighted sections of our code, we deliberately wrote an illegal <code>x</code> (ASCII <code>0x78</code>). Now, going back to Figure 6.4, if you look at the erroneous write I circled in that figure, you can clearly see that <code>0x78</code>—that&#39;s exactly what our test code wrote in there. That&#39;s the smoking gun.</p><p><strong>About Poisoning</strong></p><p>You&#39;ll also see various magic numbers in the logs. This is the SLUB <strong>Poisoning</strong> mechanism at work (because we enabled the <code>P</code> flag):</p><ul><li><strong><code>0x6b</code></strong>: This is the value used to initialize legitimate slab memory areas, representing &quot;uninitialized.&quot;</li><li><strong><code>0xa5</code></strong>: This is the poison value for the end marker.</li><li><strong><code>0x5a</code></strong>: This is the poison value representing &quot;use of uninitialized memory.&quot;</li></ul><p>These numbers aren&#39;t random—they are the fingerprint database in the hands of memory detectives.</p><p><strong>The Final Part</strong></p><blockquote><p><strong>(Figure 6.5 insertion point: Screenshot of SLUB error, part 3, showing CPU registers, fix-up information, etc.)</strong></p></blockquote><p>At the end of a standard kernel bug report, you&#39;ll usually see the CPU register values.</p><p>But the bottom two lines are SLUB&#39;s masterstroke:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>FIX kmalloc-32: Restoring 0x00000000d969b0bf-0x00000000d969b0bf</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Because we enabled the <code>F</code> flag (sanity checks), the kernel doesn&#39;t just report the error—it <strong>attempts to fix</strong> the scene. It tries to restore the corrupted slab object to a &quot;correct&quot; state (for example, by filling the overwritten redzone back in). Of course, this doesn&#39;t always succeed, and such a fix-up merely prevents the kernel from crashing immediately; it doesn&#39;t mean the problem has gone away. The log also mentions <code>... not freed</code>, which is completely normal—because this report was generated while the object hadn&#39;t been freed yet (our code will call <code>kfree</code> a bit later).</p><h4 id="decoding-the-use-after-free-uaf-report" tabindex="-1">Decoding the &quot;Use-After-Free&quot; (UAF) Report <a class="header-anchor" href="#decoding-the-use-after-free-uaf-report" aria-label="Permalink to &quot;Decoding the &quot;Use-After-Free&quot; (UAF) Report&quot;">​</a></h4><p>Next, let&#39;s look at Use-After-Free (UAF), the case marked as <code>[V5]</code> in Table 6.2.</p><p>The UAF error looks like this:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BUG kmalloc-32 (Tainted: G    B      OE    ): Poison overwritten</span></span>
<span class="line"><span>--------------------------------------------</span></span>
<span class="line"><span>INFO: 0x00000000d969b0bf-0x00000000d969b0bf @offset=872. First byte 0x79 instead of 0x6b</span></span>
<span class="line"><span>INFO: Allocated in uaf+0x20/0x47 [test_kmembugs] age=5 cpu=5 pid=2306</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>The template is the same as before, but the diagnostic conclusion has changed: <strong>Poison overwritten</strong>. Why? Because in the <code>uaf()</code> test case, we first <code>kfree</code>ed this memory, and then wrote a byte into it!</p><p>The INFO line here gives the hashed address of the corrupted region, along with a very crucial piece of information: <code>offset=872</code> and <code>First byte 0x79 instead of 0x6b</code>. This means: at offset 872, where the poison value <code>0x6b</code> should have been, it is now <code>0x79</code> (which is exactly the ASCII value of the character <code>y</code> we wrote in).</p><p>The subsequent <code>INFO</code> line shows where the allocation occurred (<code>uaf+0x20/0x47</code>), along with the process PID. Below that, as usual, there is a stack backtrace, as well as the free information:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>INFO: Freed in uaf+0x34/0x47 [test_kmembugs] age=5 cpu=5 pid=2306</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Here you&#39;ll notice an interesting fact: both the allocation and the free happened in the same function, <code>uaf</code>, and even the PID didn&#39;t change. This is a classic case of self-sabotage—it allocated it, it freed it, and it corrupted it.</p><h4 id="decoding-the-double-free-report" tabindex="-1">Decoding the &quot;Double-Free&quot; Report <a class="header-anchor" href="#decoding-the-double-free-report" aria-label="Permalink to &quot;Decoding the &quot;Double-Free&quot; Report&quot;">​</a></h4><p>Finally, let&#39;s take a quick look at Double-Free, the case marked as <code>[V6]</code> in Table 6.2. The kernel reports it like this:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BUG kmalloc-32 (Tainted: G    B      OE    ): Object already free</span></span>
<span class="line"><span>--------------------------------------------</span></span>
<span class="line"><span>INFO: Allocated in double_free+0x20/0x4b [test_kmembugs] age=1 cpu=5 pid=2330</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>The conclusion is very straightforward: <strong>Object already free</strong>. The template format that follows is exactly the same as what you just learned. You can try interpreting it yourself: who did this? In which function?</p><h4 id="regarding-uninitialized-memory-reads-umr" tabindex="-1">Regarding Uninitialized Memory Reads (UMR) <a class="header-anchor" href="#regarding-uninitialized-memory-reads-umr" aria-label="Permalink to &quot;Regarding Uninitialized Memory Reads (UMR)&quot;">​</a></h4><p>We&#39;ve actually already seen how SLUB handles Uninitialized Memory Reads (UMR—the tests marked as <code>[V3]</code> and <code>[V7]</code> in Table 6.2). When you enable <code>slub_debug</code>, although it doesn&#39;t always trigger a prominent <code>BUG</code> report, if you dump that memory region, you&#39;ll see it&#39;s entirely filled with the poison value <code>0x6b</code>. This is SLUB quietly telling you: &quot;Buddy, this memory hasn&#39;t been initialized yet; whatever you&#39;re reading out of it is garbage.&quot;</p><p>At this point, you should be able to summarize a pattern: While the SLUB debug framework can catch the <strong>vast majority</strong> of corruption issues on slab memory, it seems to have no way of dealing with <strong>out-of-bounds reads</strong>. Can&#39;t remember? No problem—look back at Table 6.2. Those blank <code>[ ]</code> entries are SLUB&#39;s blind spots. If you want to catch out-of-bounds reads, you&#39;ll need to bring in the heavy artillery from the next chapter: KASAN.</p>`,61)])])}const g=s(n,[["render",o]]);export{u as __pageData,g as default};
