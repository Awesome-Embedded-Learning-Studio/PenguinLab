import{_ as t,o,c as n,a as i}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"4.9  Quick Reference","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch04_9.md","filePath":"en/notes/linux_kernel_networking/ch04_9.md","lastUpdated":null}'),s={name:"en/notes/linux_kernel_networking/ch04_9.md"};function a(r,e,c,d,h,l){return o(),n("div",null,[...e[0]||(e[0]=[i('<h1 id="_4-9-quick-reference" tabindex="-1">4.9 Quick Reference <a class="header-anchor" href="#_4-9-quick-reference" aria-label="Permalink to &quot;4.9  Quick Reference&quot;">​</a></h1><p>Now that we&#39;ve completed this journey, let&#39;s pause and pack our backpack.</p><p>In this chapter, we read through a lot of code and traced numerous paths—from how a packet is constructed, to how it is carefully fragmented, to how it navigates the kernel&#39;s forwarding path. Along the way, we encountered a multitude of function names, macro definitions, and structures—like tools scattered on the ground. Now we need to pick them up and sort them back into the toolbox.</p><p>This isn&#39;t a &quot;glossary of terms&quot; meant for passing an exam; it&#39;s a <strong>cheat sheet</strong>. Later, when you&#39;re debugging network issues, staring blankly at memory in <code>crash</code> tools, or struggling to recall &quot;what was that function that handles the fragment forwarding option called?&quot; in your code, coming back to this section will help you find that crucial clue.</p><p>Listed below are the &quot;protagonists&quot; and &quot;supporting cast&quot; that made frequent appearances in this chapter.</p><hr><h3 id="method-quick-reference" tabindex="-1">Method Quick Reference <a class="header-anchor" href="#method-quick-reference" aria-label="Permalink to &quot;Method Quick Reference&quot;">​</a></h3><p>Listed here are the most core methods of the IPv4 layer.</p><h4 id="transmit-path-from-transport-layer-to-network-layer" tabindex="-1">Transmit Path: From Transport Layer to Network Layer <a class="header-anchor" href="#transmit-path-from-transport-layer-to-network-layer" aria-label="Permalink to &quot;Transmit Path: From Transport Layer to Network Layer&quot;">​</a></h4><p>These functions are responsible for pushing data from the transport layer (L4) to the network layer (L3). Although they all share the same destination, their points of origin and methods of departure vary.</p><p><strong><code>int ip_queue_xmit(struct sk_buff *skb, struct flowi *fl);</code></strong></p><blockquote><p><strong>Used for</strong>: TCPv4 data transmission.</p></blockquote><p>This is the go-to method for TCP. It handles encapsulating TCP segments into IP packets and sending them off.</p><p><strong><code>int ip_append_data(struct sock *sk, struct flowi4 *fl4, ... );</code></strong></p><blockquote><p><strong>Used for</strong>: UDPv4 (Corked mode) and ICMPv4.</p></blockquote><p>This is a &quot;data accumulation&quot; process. When you use the <code>UDP_CORK</code> option with UDP, or when sending ICMP messages, the kernel doesn&#39;t send a packet for every tiny piece of data. Instead, it uses this method to accumulate data until an explicit send is called.</p><p><strong><code>struct sk_buff *ip_make_skb(struct sock *sk, struct flowi4 *fl4, ... );</code></strong></p><blockquote><p><strong>Historical context</strong>: Introduced in kernel 2.6.39.</p></blockquote><p><strong>Why was it introduced?</strong> To implement a lockless fast path for UDP transmission. <strong>When is it called?</strong> When you haven&#39;t set the <code>UDP_CORK</code> option, UDP prefers to call this method directly, preparing and sending the data in one shot to avoid locking overhead.</p><p><strong><code>int ip_generic_getfrag(void *from, char *to, int offset, int len, int odd, struct sk_buff *skb);</code></strong></p><blockquote><p><strong>Function</strong>: The universal &quot;mover&quot;.</p></blockquote><p>This is a generic callback function specifically responsible for copying data from user space to a specified location in an SKB. If you are writing your own protocol and don&#39;t want to reinvent the wheel, you can use it directly.</p><p><strong><code>static int icmp_glue_bits(void *from, char *to, int offset, int len, int odd, struct sk_buff *skb);</code></strong></p><blockquote><p><strong>Function</strong>: ICMPv4&#39;s dedicated <code>getfrag</code> callback.</p></blockquote><p>When the ICMP module calls <code>ip_append_data()</code>, it passes this function in as the <code>getfrag</code> parameter. At its core, it still copies data, but it incorporates ICMP-specific logic.</p><hr><h4 id="receive-and-routing-a-packet-s-destination" tabindex="-1">Receive and Routing: A Packet&#39;s Destination <a class="header-anchor" href="#receive-and-routing-a-packet-s-destination" aria-label="Permalink to &quot;Receive and Routing: A Packet&#39;s Destination&quot;">​</a></h4><p>A packet has arrived—what next? These functions answer that question.</p><p><strong><code>int ip_rcv(struct sk_buff *skb, struct net_device *dev, struct packet_type *pt, struct net_device *orig_dev);</code></strong></p><blockquote><p><strong>Identity</strong>: IPv4&#39;s &quot;head receptionist&quot;.</p></blockquote><p>This is the main receive handler for IPv4 packets. All incoming IPv4 packets make this their first stop. It performs the most basic security checks (version number, checksum) and then hands the packet off to Netfilter.</p><p><strong><code>int ip_local_deliver(struct sk_buff *skb);</code></strong></p><blockquote><p><strong>Final stop</strong>: Local upper-layer protocols.</p></blockquote><p>When a routing lookup confirms that this packet is destined for the local machine, <code>ip_rcv</code> ultimately hands the packet to this function. If necessary, it performs reassembly first, and then knocks on the door of the L4 protocol (TCP/UDP).</p><p><strong><code>int ip_forward(struct sk_buff *skb);</code></strong></p><blockquote><p><strong>Transit station</strong>: The forwarding path.</p></blockquote><p>If you have configured Linux as a router, this function is the core. It decrements the TTL by 1, updates the checksum, and then pushes the packet to the next egress.</p><p><strong><code>int ip_route_input_noref(...)</code></strong></p><blockquote><p><strong>(Though implied in the original text&#39;s logic, added here to complete the context)</strong> This is the executor of the routing lookup. Although not in the list above, it is the judge that decides whether to call <code>ip_local_deliver</code> or <code>ip_forward</code>.</p></blockquote><hr><h4 id="multicast-niche-but-important" tabindex="-1">Multicast: Niche but Important <a class="header-anchor" href="#multicast-niche-but-important" aria-label="Permalink to &quot;Multicast: Niche but Important&quot;">​</a></h4><p>Multicast has its own set of logic; while the path is similar, the handling functions are specialized.</p><p><strong><code>int ip_mr_input(struct sk_buff *skb);</code></strong></p><blockquote><p><strong>Function</strong>: Handle incoming multicast packets.</p></blockquote><p>If this is a multicast packet, the kernel calls this to decide whether to deliver the packet to the local machine or forward it to interfaces specified in the MFC table.</p><p><strong><code>static int ipmr_queue_xmit(struct net *net, struct mr_table *mrt, ...);</code></strong></p><blockquote><p><strong>Function</strong>: Multicast&#39;s dedicated send method.</p></blockquote><p><strong><code>int ip_mr_forward(struct net *net, struct mr_table *mrt, ...);</code></strong></p><blockquote><p><strong>Function</strong>: Multicast forwarding.</p></blockquote><hr><h4 id="fragmentation-cutting-and-pasting" tabindex="-1">Fragmentation: Cutting and Pasting <a class="header-anchor" href="#fragmentation-cutting-and-pasting" aria-label="Permalink to &quot;Fragmentation: Cutting and Pasting&quot;">​</a></h4><p>This is the part most prone to errors and the most demanding of attention to detail.</p><p><strong><code>int ip_fragment(struct sk_buff *skb, int (*output)(struct sk_buff *));</code></strong></p><blockquote><p><strong>Function</strong>: The master of fragmentation.</p></blockquote><p>When a packet&#39;s length exceeds the MTU and fragmentation is allowed, this function is called. It contains fast and slow path processing logic, slicing a large packet into smaller fragments that fit the MTU.</p><p><strong><code>int ip_defrag(struct sk_buff *skb, u32 user);</code></strong></p><blockquote><p><strong>Function</strong>: The master of reassembly.</p></blockquote><p>On the receiving end, it is responsible for reassembling these scattered fragments back into a complete packet based on their ID, source address, and protocol.</p><blockquote><p><strong>Parameter <code>user</code></strong>: This parameter tells us who is requesting the reassembly (e.g., the normal IP layer or Netfilter connection tracking). The full list of definitions is in the <code>ip_defrag_users</code> enum within <code>include/net/ip.h</code>.</p></blockquote><p><strong><code>bool ip_is_fragment(const struct iphdr *iph);</code></strong></p><blockquote><p><strong>Function</strong>: Identify a fragment at a glance.</p></blockquote><p>If this packet is merely a fragment (the MF flag is 1 or the FragOffset is not 0), it returns true.</p><p><strong><code>bool skb_has_frag_list(const struct sk_buff *skb);</code></strong></p><blockquote><p><strong>Function</strong>: Check the SKB&#39;s fragment linked list.</p></blockquote><p>This might be the most confusing name. An SKB has two fragmentation methods: one is the page array (<code>frags[]</code>), and the other is the SKB linked list (<code>frag_list</code>). This function checks the latter.</p><blockquote><p><strong>Historical rename</strong>: It used to be called <code>skb_has_frags()</code>, but was renamed in kernel 2.6.37. Why? Because it was too misleading, making people think it was checking the former. The improved clarity of the new name has saved the sanity of countless engineers.</p></blockquote><hr><h4 id="ip-options-those-features-dusty-with-history" tabindex="-1">IP Options: Those Features Dusty with History <a class="header-anchor" href="#ip-options-those-features-dusty-with-history" aria-label="Permalink to &quot;IP Options: Those Features Dusty with History&quot;">​</a></h4><p>Modern networks rarely use IP options, but the kernel still retains complete processing code for them.</p><p><strong><code>int ip_options_compile(struct net *net, struct ip_options *opt, struct sk_buff *skb);</code></strong></p><blockquote><p><strong>Function</strong>: Parsing.</p></blockquote><p>It parses the option byte stream in the IP header into an internal <code>ip_options</code> object that the kernel can understand.</p><p><strong><code>void ip_options_fragment(struct sk_buff *skb);</code></strong></p><blockquote><p><strong>Function</strong>: Option cleanup during fragmentation.</p></blockquote><p>When fragmentation occurs, some options (like Record Route) don&#39;t need to be copied to every fragment. This function replaces options that don&#39;t need copying with <code>IPOPT_NOOP</code>. Note that it is only called when processing the first fragment.</p><p><strong><code>void ip_options_build(struct sk_buff *skb, struct ip_options *opt, __be32 daddr, struct rtable *rt, int is_frag);</code></strong></p><blockquote><p><strong>Function</strong>: Construction.</p></blockquote><p>Writes the parsed <code>ip_options</code> object back into the IPv4 header. The parameter <code>is_frag</code> is actually 0 in all calls.</p><p><strong><code>void ip_forward_options(struct sk_buff *skb);</code></strong></p><blockquote><p><strong>Function</strong>: Handle options during forwarding.</p></blockquote><p><strong><code>ip_rcv_options(struct sk_buff *skb);</code></strong></p><blockquote><p><strong>Function</strong>: Handle options during receiving.</p></blockquote><p><strong><code>int ip_options_rcv_srr(struct sk_buff *skb);</code></strong></p><blockquote><p><strong>Function</strong>: Handle Strict Source Route.</p></blockquote><p>If the packet carries the Strict Source Route option, this function ensures the packet actually took the specified path.</p><p><strong><code>int ip_call_ra_chain(struct sk_buff *skb);</code></strong></p><blockquote><p><strong>Function</strong>: Handle the Router Alert option.</p></blockquote><p><strong><code>int ip_options_get_from_user(struct net *net, ...);</code></strong></p><blockquote><p><strong>Function</strong>: Retrieve options from user space.</p></blockquote><p>When you set <code>IP_OPTIONS</code> via the <code>setsockopt()</code> system call, the kernel uses this to bring your settings in.</p><hr><h4 id="low-level-and-auxiliary" tabindex="-1">Low-level and Auxiliary <a class="header-anchor" href="#low-level-and-auxiliary" aria-label="Permalink to &quot;Low-level and Auxiliary&quot;">​</a></h4><p><strong><code>static int raw_send_hdrinc(struct sock *sk, struct flowi4 *fl4, ...);</code></strong></p><blockquote><p><strong>Function</strong>: The Raw Socket express.</p></blockquote><p>When you use a Raw Socket and set the <code>IP_HDRINCL</code> option (meaning &quot;I will construct the IP header myself&quot;), the kernel calls this method. It directly calls <code>dst_output()</code>, bypassing much of the kernel&#39;s automatic encapsulation logic.</p><p><strong><code>int ip_decrease_ttl(struct iphdr *iph);</code></strong></p><blockquote><p><strong>Function</strong>: Decrement TTL by 1 and update the checksum.</p></blockquote><p>Don&#39;t underestimate it. It is called every time during forwarding. Because the TTL has changed, the checksum must be recalculated. It leverages the incremental update algorithm mentioned in RFC 1072, making it highly efficient.</p><p><strong><code>int ip_build_and_send_pkt(struct sk_buff *skb, struct sock *sk, ...);</code></strong></p><blockquote><p><strong>Function</strong>: Send a SYN ACK.</p></blockquote><p>TCPv4 specific. You can check out <code>tcp_v4_send_synack()</code> in <code>net/ipv4/tcp_ipv4.c</code>; that&#39;s exactly what it calls.</p><hr><h3 id="macro-quick-reference" tabindex="-1">Macro Quick Reference <a class="header-anchor" href="#macro-quick-reference" aria-label="Permalink to &quot;Macro Quick Reference&quot;">​</a></h3><p>Finally, here are a few key macros that often hide in the details of the code.</p><p><strong><code>IPCB(skb)</code></strong></p><blockquote><p><strong>Function</strong>: Get the control block.</p></blockquote><p>It returns the <code>inet_skb_parm</code> object pointed to by <code>skb-&gt;cb</code>. Hidden inside this object is the <code>ip_options</code> object we need to access.</p><p><strong><code>FRAG_CB(skb)</code></strong></p><blockquote><p><strong>Function</strong>: Fragmentation-specific control block.</p></blockquote><p>It returns the <code>ipfrag_skb_cb</code> object pointed to by <code>skb-&gt;cb</code>. This is where the fragmentation module stashes its private data inside the SKB.</p><p><strong><code>int NF_HOOK(uint8_t pf, unsigned int hook, struct sk_buff *skb, ...)</code></strong></p><blockquote><p><strong>Function</strong>: The gateway to Netfilter hooks.</p></blockquote><p>This is the core Netfilter macro. The first parameter, <code>pf</code>, is the protocol family (IPv4 is <code>NFPROTO_IPV4</code>). The second parameter is one of the five hook points (<code>PRE_ROUTING</code>, <code>LOCAL_IN</code>, etc.). If the registered hooks don&#39;t drop the packet, it calls the final <code>okfn</code> callback to continue the flow.</p><p><strong><code>int NF_HOOK_COND(..., bool cond)</code></strong></p><blockquote><p><strong>Function</strong>: Conditional Netfilter hook.</p></blockquote><p>Same as above, but with an additional boolean parameter, <code>cond</code>. It only actually calls the Netfilter hooks when <code>cond</code> is true. This is a performance optimization.</p><p><strong><code>IPOPT_COPIED(option)</code></strong></p><blockquote><p><strong>Function</strong>: Check option flag bits.</p></blockquote><p>It returns the Copied flag (the highest bit) in the option type. If it is 1, it means this option must be copied to all fragments; if it is 0, it only appears in the first fragment.</p><hr><h3 id="chapter-summary" tabindex="-1">Chapter Summary <a class="header-anchor" href="#chapter-summary" aria-label="Permalink to &quot;Chapter Summary&quot;">​</a></h3><p>With this, we have completed the full journey through the IPv4 stack.</p><p>This is not just a pile of functions and macros. Behind these APIs lie the trade-offs of network protocol design: <strong>Why is fragmentation done on the sending side while reassembly is done on the receiving side? Why are IP options barely used anymore, yet the kernel still spends significant effort to maintain compatibility? Why does the Raw Socket allow users to construct their own headers?</strong></p><p>What we built in this chapter is not just knowledge about IPv4, but a &quot;<strong>protocol implementer&#39;s</strong>&quot; perspective. You are no longer just a user calling <code>send()</code>; you see how the kernel navigates through bitstreams, walking the tightrope between security and efficiency.</p><p>In the next chapter, we will turn our attention to the &quot;brain&quot; of this massive machine—the <strong>routing subsystem</strong>. A packet not only knows how to reach its destination, but more importantly, how does it know where to go? What exactly does that routing table look like, which decides the fork in the road between <code>ip_local_deliver</code> and <code>ip_forward</code>? See you in the next chapter.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: understanding&quot;">​</a></h3><p><strong>Question</strong>: In the <code>struct iphdr</code> structure, the Internet Header Length (ihl) field occupies 4 bits. Suppose the kernel reads an IPv4 packet where the value of <code>iph-&gt;ihl</code> is 7. What is the IP header length of this packet in bytes? Does it contain IP options?</p><details><summary>Answer and Explanation</summary><p><strong>Answer</strong>: 28 bytes; it contains IP options.</p><p><strong>Explanation</strong>: According to the knowledge points, ihl represents the header length in 4-byte units. The calculation formula is: header length = ihl * 4. When ihl=7, the length is 7 * 4 = 28 bytes. The fixed part of an IPv4 header is 20 bytes (corresponding to ihl=5). Any header larger than 20 bytes implies the existence of IP options (the optional part). Since 28 &gt; 20, this packet contains 8 bytes of IP options.</p></details><h3 id="exercise-2-understanding" tabindex="-1">Exercise 2: understanding <a class="header-anchor" href="#exercise-2-understanding" aria-label="Permalink to &quot;Exercise 2: understanding&quot;">​</a></h3><p><strong>Question</strong>: When the kernel function <code>ip_rcv()</code> processes a received IPv4 packet, it first performs a series of sanity checks. When a check fails (such as the version number not being 4 or a checksum error), which statistics counter does the kernel update? (Please provide the macro definition name)</p><details><summary>Answer and Explanation</summary><p><strong>Answer</strong>: IPSTATS_MIB_INHDRERRORS</p><p><strong>Explanation</strong>: Based on the code snippet descriptions in the original chapter text, when <code>iph-&gt;ihl &lt; 5</code> or <code>iph-&gt;version != 4</code>, the code jumps to the <code>inhdr_error</code> label. The original text explicitly states: &quot;the packet is dropped and the statistics (IPSTATS_MIB_INHDRERRORS) are updated.&quot; This is used to record the number of received IP packets with header errors.</p></details><h3 id="exercise-3-application" tabindex="-1">Exercise 3: application <a class="header-anchor" href="#exercise-3-application" aria-label="Permalink to &quot;Exercise 3: application&quot;">​</a></h3><p><strong>Question</strong>: Suppose you need to develop a program that sends custom IP packets through a Raw Socket, and you do not want the packet to be fragmented by routers during transmission (e.g., for Path MTU Discovery testing). Which flag in the IPv4 header&#39;s <code>frag_off</code> field should you manually set? Please provide the constant name of this flag and its corresponding hexadecimal value.</p><details><summary>Answer and Explanation</summary><p><strong>Answer</strong>: Flag name: IP_DF (Don&#39;t Fragment flag); Hexadecimal value: 0x02.</p><p><strong>Explanation</strong>: According to the knowledge points, the upper 3 bits of the <code>frag_off</code> field are flags. Among them, IP_DF (Don&#39;t Fragment) indicates that the packet must not be fragmented. The original text states: &quot;010 is DF (Don&#39;t Fragment)&quot;. In binary, 010 corresponds to the hexadecimal value 0x02. After setting this bit, if the packet encounters a link with an MTU smaller than the packet length, the router will drop the packet and return an ICMP Fragmentation Needed message, which is exactly what the Path MTU Discovery mechanism requires.</p></details><h3 id="exercise-4-application" tabindex="-1">Exercise 4: application <a class="header-anchor" href="#exercise-4-application" aria-label="Permalink to &quot;Exercise 4: application&quot;">​</a></h3><p><strong>Question</strong>: When the kernel calls <code>ip_forward()</code> to process a packet in the forwarding path, it calls the <code>ip_decrease_ttl()</code> function. Besides decrementing the TTL field in the IPv4 header by 1, what other critical operation must this function perform to maintain the packet&#39;s validity?</p><details><summary>Answer and Explanation</summary><p><strong>Answer</strong>: Recalculate and update the IPv4 header checksum.</p><p><strong>Explanation</strong>: The IPv4 header checksum (the <code>check</code> field) covers the entire header. When the TTL field changes, the original checksum is no longer valid. According to the knowledge point definitions, the <code>ip_decrease_ttl()</code> function is responsible for &quot;decrementing the TTL in the IPv4 header by 1 and recalculating the checksum.&quot; If only the TTL is modified without updating the checksum, the next hop receiving this packet will drop it due to a checksum error.</p></details><h3 id="exercise-5-thinking" tabindex="-1">Exercise 5: thinking <a class="header-anchor" href="#exercise-5-thinking" aria-label="Permalink to &quot;Exercise 5: thinking&quot;">​</a></h3><p><strong>Question</strong>: When designing the network stack, the Linux kernel distinguishes between two main transmit helper functions, <code>ip_append_data()</code> and <code>ip_push_pending_frames()</code>, and later introduced <code>ip_make_skb()</code> for the UDP fast path. Please comparatively analyze why the &quot;lockless transmission&quot; design of <code>ip_make_skb()</code> can provide better performance than the traditional mode in a Symmetric Multiprocessing (SMP) environment? What type of overhead does this design primarily avoid in the traditional path?</p><details><summary>Answer and Explanation</summary><p><strong>Answer</strong>: <code>ip_make_skb()</code> integrates data preparation and SKB construction, allowing the packet to be built before acquiring the socket lock (or reducing the time the lock is held). The traditional mode (such as <code>ip_append_data</code> paired with <code>ip_push_pending_frames</code>) typically requires complex queue management and fragmentation calculations while holding the socket lock, leading to intense lock contention. <code>ip_make_skb()</code> aims to implement a lockless transmission fast path, primarily avoiding the overhead of heavy processing inside the socket lock (BH lock or socket lock) as well as multiple context switches, thereby improving concurrent throughput.</p><p><strong>Explanation</strong>: This is an in-depth analysis question. According to the knowledge points, <code>ip_append_data()</code> is used to prepare data but not send it, requiring coordination with <code>ip_push_pending_frames()</code>. This usually means maintaining the socket&#39;s send queue state, which involves locking. <code>ip_make_skb()</code> was introduced after kernel 2.6.39, aiming to &quot;implement a lockless transmission fast path.&quot; Its core advantage lies in decoupling or optimizing the packet construction logic from the socket&#39;s state management, allowing most of the work to be completed without lock contention. In high-concurrency UDP scenarios, reducing the hold time of the socket lock is the key to improving performance, because lock contention leads to CPU cache invalidation and process queuing.</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>Although the IPv4 header seems to be only 20 bytes, every bit is full of design pitfalls. The kernel uses <code>struct iphdr</code> to describe this structure, where the <code>ihl</code> field defines the actual length of the header in 4-byte units, allowing the IPv4 header to vary between 20 and 60 bytes to accommodate options. The Type of Service (TOS) field has evolved multiple times and is now primarily used for DSCP traffic control and ECN Explicit Congestion Notification. The fragmentation mechanism multiplexes the 16-bit <code>frag_off</code> field, using the lower 13 bits to record the offset in 8-byte units, while the upper 3 bits are reserved for the DF (Don&#39;t Fragment) and MF (More Fragments) flags. Although this bit-field multiplexing saves space, it also requires strict masking operations during parsing, otherwise it is extremely easy to be misled by magic numbers.</p><p>The receiving process of an IPv4 packet is not accomplished in a single step, but rather a relay race with checks at every level. The <code>ip_rcv</code> function plays the role of a &quot;gatekeeper,&quot; strictly responsible for verifying the header format, version number, and checksum. Once an anomaly is detected (such as <code>ihl</code> being less than 5), it drops the packet immediately. After passing security, the packet is handed over to <code>ip_rcv_finish</code>, whose core task is to perform a routing lookup. This step queries the routing subsystem, encapsulates the result in a <code>dst_entry</code>, and depending on the destination address, points the packet&#39;s <code>input</code> callback pointer to either the local delivery function <code>ip_local_deliver</code>, the forwarding function <code>ip_forward</code>, or the multicast handling function <code>ip_mr_input</code>, thereby deciding whether the packet &quot;stays&quot; or &quot;forwards.&quot;</p><p>The processing logic for multicast packets is more complex than for unicast because it needs to simultaneously determine the dual identity of &quot;receiver&quot; and &quot;forwarder.&quot; During the routing lookup phase, the kernel calls <code>ip_check_mc_rcu</code> to check if the local interface has subscribed to the multicast group, while also checking if multicast forwarding is enabled (controlled by the user-space <code>pimd</code> daemon manipulating the kernel switch <code>mc_forwarding</code>). If it is destined for the local machine, the packet enters <code>ip_local_deliver</code> and is ultimately delivered to the socket; if it needs to be forwarded, it enters <code>ip_mr_input</code>. Interestingly, even for multicast forwarding, the kernel strictly adheres to the fundamental laws of IP networking, calling <code>ip_decrease_ttl</code> to decrement the TTL and recalculate the checksum before sending, to prevent routing loops.</p><p>IP options, which rarely appear in modern networks, nonetheless have a complete compilation and processing mechanism inside the kernel. Because options can cause router performance degradation, and during fragmentation only some options (determined by the Copied Flag) are copied to subsequent fragments, handling them is extremely tedious. The kernel parses the raw byte stream into an <code>struct ip_options</code> structure via <code>ip_options_compile</code>. For options like Record Route (RR) and Timestamp, the kernel needs to dynamically fill in the current interface IP or time, which modifies the header and invalidates the checksum. Furthermore, for security reasons, the system disables Source Routing (SSRR/LSRR) options by default, preventing attackers from using them for IP spoofing or bypassing firewalls.</p><p>On the transmit path, TCP and UDP exhibit completely different personalities, using the <code>ip_queue_xmit</code> and <code>ip_append_data</code> mechanisms, respectively. As a &quot;worrywart&quot; protocol, TCP prefers to manage its own data segmentation. It usually carries an already assembled SKB downstream, requiring only a quick routing cache lookup to send. UDP, on the other hand, acts like a &quot;hands-off&quot; manager, dumping all data to the IP layer, where <code>ip_append_data</code> is responsible for page-level assembly, data merging, and header construction in kernel space, before <code>ip_push_pending_frames</code> finally sends it out uniformly. This design difference reflects the two protocols&#39; different allocations of reliability control: TCP wants to master every detail, while UDP pursues efficiency and minimalism.</p>',149)])])}const g=t(s,[["render",a]]);export{u as __pageData,g as default};
