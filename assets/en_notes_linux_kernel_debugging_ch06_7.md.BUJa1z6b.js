import{_ as t,o as i,c as s,a}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"6.7 Practical Tips for Developers","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch06_7.md","filePath":"en/notes/linux_kernel_debugging/ch06_7.md","lastUpdated":null}'),o={name:"en/notes/linux_kernel_debugging/ch06_7.md"};function n(r,e,l,h,d,c){return i(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="_6-7-practical-tips-for-developers" tabindex="-1">6.7 Practical Tips for Developers <a class="header-anchor" href="#_6-7-practical-tips-for-developers" aria-label="Permalink to &quot;6.7 Practical Tips for Developers&quot;">​</a></h1><p>Honestly, no amount of debugging tools beats writing cleaner code in the first place.</p><p>We won&#39;t discuss debugging tools in this section. Instead, let&#39;s talk about something more pragmatic—how to fundamentally prevent kernel memory issues through better coding habits. It echoes the old saying: prevention is better than cure.</p><h3 id="the-modern-approach-to-preventing-leaks-devres-api" tabindex="-1">The Modern Approach to Preventing Leaks: Devres API <a class="header-anchor" href="#the-modern-approach-to-preventing-leaks-devres-api" aria-label="Permalink to &quot;The Modern Approach to Preventing Leaks: Devres API&quot;">​</a></h3><p>If you are writing modern Linux drivers, you must know about a &quot;resource management&quot; mechanism provided by the kernel, commonly known as the <strong>devres</strong> API.</p><p>Its biggest selling point is simple: <strong>you allocate, and it frees.</strong></p><p>Although the kernel has many functions with the <code>devm_*</code> prefix, as driver authors, the two memory allocation interfaces we use most are:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">devm_kmalloc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> device </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">dev</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">size_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">gfp_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> gfp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">devm_kzalloc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> device </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">dev</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">size_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">gfp_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> gfp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>You might have noticed the emphasis on &quot;driver authors&quot; only. Just look at the function signature: the first parameter is <code>struct device *</code>. Only drivers have access to this; core kernel code or other modules don&#39;t.</p><p>The reason this mechanism is so handy is that the kernel guarantees: when a driver detaches, or when a kernel module is unloaded, the resource management framework will <strong>automatically</strong> free the memory allocated through these APIs.</p><p>This directly improves code robustness. Why? Because we are all human and we all make mistakes. And in the kernel, one of the easiest mistakes to make—especially when handling various error return paths—is forgetting to free memory.</p><h4 id="lesser-known-facts-about-the-devres-api" tabindex="-1">Lesser-Known Facts About the Devres API <a class="header-anchor" href="#lesser-known-facts-about-the-devres-api" aria-label="Permalink to &quot;Lesser-Known Facts About the Devres API&quot;">​</a></h4><p>While it feels like magic, there are a few things you should keep in mind when using it:</p><ol><li><p><strong>Don&#39;t blindly replace everything</strong>. Don&#39;t assume <code>devm_kzalloc</code> is inherently superior and replace every <code>kmalloc</code> in your code with it. This &quot;automatically managed&quot; memory is <strong>only suitable for use during driver initialization and <code>probe()</code> phases</strong>. Why? Because its lifecycle is tied to the device. If you use it in a regular function that gets called frequently, the memory won&#39;t be freed until the device is unplugged. That&#39;s not a leak; that&#39;s a vulnerability.</p></li><li><p><strong>Prefer <code>devm_kzalloc</code></strong>. It not only allocates memory but also zeroes it out. This saves you from an entire class of rookie mistakes: Uninitialized Memory Reads (UMR). Statistics show that in kernel 5.10.60, <code>devm_kzalloc</code> was called over 5,000 times—developers know a good deal when they see one.</p></li><li><p><strong>Standard parameters</strong>. The second parameter is the size in bytes, and the third is the GFP flag (e.g., <code>GFP_KERNEL</code>), which is no different from a standard <code>kmalloc</code>. The only new face is the first parameter, <code>dev</code>, which points to your device structure.</p></li><li><p><strong>Don&#39;t manually free, unless...</strong>. Once you use this API, stop worrying about <code>kfree</code>. Of course, the kernel does provide <code>devm_kfree()</code> for manual freeing. But if you find yourself needing to manually free it before the driver disconnects, it usually means you&#39;re using it in the wrong place—if you&#39;re going to manage it manually, why use <code>devm</code> in the first place?</p></li><li><p><strong>GPL&#39;s revenge</strong>. This API is only exported to modules that comply with the GPL license. The kernel community&#39;s little agenda—you know how it is.</p></li></ol><h3 id="a-few-more-fatal-mistakes-to-avoid" tabindex="-1">A Few More &quot;Fatal Mistakes&quot; to Avoid <a class="header-anchor" href="#a-few-more-fatal-mistakes-to-avoid" aria-label="Permalink to &quot;A Few More &quot;Fatal Mistakes&quot; to Avoid&quot;">​</a></h3><p>Beyond using the right tools, some pitfalls are purely human error. Here are a few of the most common memory-related bugs you&#39;ll encounter during development. We recommend keeping this page in mind during Code Review.</p><ul><li><p><strong>Wrong GFP flags</strong>. This is the most classic way to crash: calling <code>GFP_KERNEL</code> in an atomic context (e.g., inside an Interrupt Handler, or while holding a spinlock). This puts the kernel to sleep, causing the system to hang or crash instantly. Here, you must use <code>GFP_ATOMIC</code>. <em>History repeats itself. Here is a patch example that serves as a painful lesson: <a href="https://lore.kernel.org/lkml/1420845382-25815-1-git-send-email-khoroshilov@ispras.ru/" target="_blank" rel="noreferrer">https://lore.kernel.org/lkml/1420845382-25815-1-git-send-email-khoroshilov@ispras.ru/</a></em></p></li><li><p><strong>Mismatched allocation and freeing</strong>. Never use <code>vfree</code> to free memory allocated with <code>kmalloc</code>, and vice versa. That&#39;s like using a hammer to drive a screw—the kernel will ruthlessly throw an error at you.</p></li><li><p><strong>Not checking return values</strong>. Memory allocation can fail. If <code>kmalloc</code> returns <code>NULL</code> and you immediately dereference it, that&#39;s a Kernel Panic. Even if it feels a bit verbose, always write:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">unlikely</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">p)) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 处理错误 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div></li><li><p><strong>Redundant <code>if</code> checks</strong>. Fans of defensive programming often write:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (p)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    kfree</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Actually, <code>kfree(NULL)</code> is safe; the kernel already handles this. Writing it this way is harmless but redundant. Conversely, some people assume <code>kfree(p)</code> sets <code>p</code> to <code>NULL</code>. This is an <strong>illusion</strong>. <code>kfree</code> does not modify the pointer variable&#39;s value, so if you intend to use this pointer later, you should manually set it to NULL. Otherwise, you&#39;ll end up accessing freed memory (a dangling pointer).</p></li><li><p><strong>Ignoring &quot;internal fragmentation&quot;</strong>. When you request 4097 bytes, the Slab Allocator will typically give you 8192 bytes (usually a power of two or aligned to a specific cache line). This means you&#39;re wasting nearly half of the memory. How can you verify this? Use the <code>ksize()</code> API.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">p </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> kmalloc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4097</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, GFP_KERNEL);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">n </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ksize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // n 很可能是 8192</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>At this point, you should ask yourself: can you optimize the data structure to round up cleanly, like requesting exactly 4096 bytes? Or use <code>slabinfo -L</code> to check the system&#39;s overall fragmentation situation.</p></li></ul><hr><h3 id="the-ultimate-tool-showdown" tabindex="-1">The Ultimate Tool Showdown <a class="header-anchor" href="#the-ultimate-tool-showdown" aria-label="Permalink to &quot;The Ultimate Tool Showdown&quot;">​</a></h3><p>Remember the test cases we ran in the previous chapter and this one? Now is the time to connect all the dots.</p><p>The table below (Table 6.4) is an enhanced version of our previous table, with a new rightmost column added—the performance of the <strong>SLUB Debug Framework</strong>.</p><p><strong>Table 6.4 — The Ultimate Showdown of Common Memory Defect Detection Methods</strong></p><p><em>(This corresponds to the content of Table 6.4 in the original text)</em></p><p>This table covers:</p><ul><li><strong>Plain kernel</strong>: Nothing enabled, running bare.</li><li><strong>Compiler warnings</strong>: Relying on GCC&#39;s <code>-Wall</code>.</li><li><strong>KASAN</strong>: Kernel Address Sanitizer.</li><li><strong>UBSAN</strong>: Undefined Behavior Sanitizer.</li><li><strong>SLUB Debug</strong>: The focus of this chapter, a debug kernel with <code>slub_debug</code> enabled.</li></ul><p>Let&#39;s quickly recap the patterns behind the table:</p><ul><li><strong>KASAN is the most comprehensive guardian</strong>. It can catch almost all Out-Of-Bounds (OOB) accesses, whether on global variables, the stack, or dynamically allocated memory. In contrast, UBSAN is powerless against OOB on dynamic memory (slab).</li><li><strong>UBSAN&#39;s specialty</strong>. It can&#39;t catch memory overflows, but it&#39;s an expert at catching &quot;Undefined Behavior (UB)&quot; (like test case 8.x). This is exactly KASAN&#39;s blind spot.</li><li><strong>The ones that got away</strong>. Neither KASAN nor UBSAN can catch the first three categories of issues: <strong>Uninitialized Memory Reads (UMR)</strong>, <strong>Use-After-Return (UAR)</strong>, and <strong>Memory Leaks</strong>. UMR can still be somewhat flagged by compiler warnings and static analysis tools (like cppcheck).</li><li><strong>SLUB Debug&#39;s home turf</strong>. It excels at catching slab-layer memory corruption, but is helpless against anything outside of that.</li><li><strong>Kmemleak&#39;s signature move</strong>. It is specifically designed to tackle &quot;leaks.&quot; As long as memory allocated via <code>kmalloc</code>, <code>vmalloc</code>, or <code>kmem_cache_alloc</code> isn&#39;t freed, it will dig it up for you.</li></ul><h4 id="a-few-additional-notes" tabindex="-1">A Few Additional Notes <a class="header-anchor" href="#a-few-additional-notes" aria-label="Permalink to &quot;A Few Additional Notes&quot;">​</a></h4><p>The footnotes in the table are worth a closer look:</p><ul><li><strong>[V1]</strong>: If the system encounters an Oops (crash) or hang, even if it seems fine afterward, the kernel is actually in an unstable state. Don&#39;t push your luck.</li><li><strong>[S1]</strong>: When SLUB debugging is enabled (<code>slub_debug=FZPU</code>), it can simultaneously catch &quot;write overflow&quot; and &quot;write overflow (underflow).&quot; But like UBSAN, it only catches this when accessed via an <strong>incorrect array index</strong>. If you write directly via an <strong>incorrect pointer offset</strong>, it might miss it. Furthermore, it usually only catches &quot;write&quot; overflows—it ignores &quot;read&quot; overflows.</li></ul><hr><h3 id="chapter-echoes" tabindex="-1">Chapter Echoes <a class="header-anchor" href="#chapter-echoes" aria-label="Permalink to &quot;Chapter Echoes&quot;">​</a></h3><p>With this, we have finally completed the grand puzzle of catching kernel memory defects.</p><p>Think back on what we actually did in this chapter (and the previous one): we built a cognitive kaleidoscope. Every tool—KASAN, UBSAN, SLUB Debug, Kmemleak—is a facet of the lens. Looking at any single facet alone only reveals a distorted, partial truth; only by overlapping them can you see the complete picture of kernel memory errors.</p><p>Along the way, you&#39;ll discover one fact: <strong>there is no silver bullet</strong>. KASAN is powerful, but it has performance overhead and can&#39;t see UMR; kmemleak is clever, but it has false positives and can only detect leaks. As a kernel developer, your value lies not in memorizing which command to run, but in <strong>knowing exactly which scalpel to pick up the instant symptoms appear to dissect the problem</strong>.</p><p>Don&#39;t forget the fear we mentioned at the beginning of this chapter—<strong>memory leaks</strong>. Through <code>kmemleak</code>, you now have the ability to find things in the dark expanse of memory. And don&#39;t forget <code>devm_kzalloc</code>, which puts you on a safe starting line from the very beginning.</p><p>In the next chapter, we will pull our gaze back from the micro-battlefield of &quot;memory&quot; and look at the macro-picture when the entire system crashes: <strong>Kernel Oops</strong>. When the kernel finally gives up and spits out a pile of hex codes, how do we as developers read its dying words? That is an even more hardcore detective game.</p><p>Are you ready? See you in the next chapter.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: Understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: Understanding&quot;">​</a></h3><p><strong>Question</strong>: While investigating a kernel crash log, you find a SLUB Allocator error report: &#39;BUG kmalloc-32: Right Redzone overwritten&#39;. Please explain what type of memory access issue this error specifically indicates, and state what the Magic Value filled in the Red Zone is during SLUB debugging.</p><details><summary>Answer &amp; Analysis</summary><p><strong>Answer</strong>: This error indicates a buffer overflow, specifically a write out-of-bounds (right/forward overflow), where the program wrote past the end of the allocated object and intruded into the red zone. The red zone&#39;s magic value is 0x5a (corresponding to the character &#39;Z&#39;, i.e., POISON_INUSE).</p><p><strong>Analysis</strong>: &#39;Right Redzone overwritten&#39; is a type of error reported by the SLUB debugging mechanism. When Red Zone (the Z flag) is enabled, the allocator inserts padding areas before and after the object&#39;s memory. If the values in this area are found to be modified during freeing or checking, it means the code performed an out-of-bounds write. According to the kernel&#39;s definitions, SLUB uses POISON_INUSE (0x5a) to fill these padding areas. If it detects that the value is no longer 0x5a, it triggers this error.</p></details><h3 id="exercise-2-application" tabindex="-1">Exercise 2: Application <a class="header-anchor" href="#exercise-2-application" aria-label="Permalink to &quot;Exercise 2: Application&quot;">​</a></h3><p><strong>Question</strong>: You need to enable the following features via the kernel boot parameter <code>slub_debug</code>: automatically filling freed memory with a specific value (to detect UAF), and performing metadata validation during allocation/freeing. Please provide the correct parameter format, and state what the original fill value (in hexadecimal) of the corrupted memory area is when a Use-After-Free (UAF) write operation occurs.</p><details><summary>Answer &amp; Analysis</summary><p><strong>Answer</strong>: Parameter format: <code>slub_debug=PF</code>. The corrupted fill value is 0x6b.</p><p><strong>Analysis</strong>: According to the <code>slub_debug</code> parameter documentation, &#39;P&#39; stands for Poisoning, which means filling with a specific value; &#39;F&#39; stands for Sanity checks. Once the &#39;P&#39; flag is enabled, objects are filled when freed or before initialization. Based on the kernel&#39;s definitions, the Poison value used for Use-After-Free detection (POISON_FREE) is 0x6b (corresponding to ASCII &#39;k&#39;). When a UAF write occurs, SLUB will detect that the value at that location is no longer 0x6b and will report &#39;Poison overwritten&#39;.</p></details><h3 id="exercise-3-application" tabindex="-1">Exercise 3: Application <a class="header-anchor" href="#exercise-3-application" aria-label="Permalink to &quot;Exercise 3: Application&quot;">​</a></h3><p><strong>Question</strong>: While analyzing a driver module&#39;s memory usage efficiency, you use the <code>slabinfo</code> tool and find that although the <code>kmalloc-192</code> cache is active, it has high internal fragmentation. Suppose the code calls <code>kmalloc(140)</code>, and <code>ksize()</code> returns 192. What is this extra memory (52 bytes) called in slabinfo? When writing kernel code to release resources, which coding style should you adopt to avoid duplicating release logic across multiple error-handling paths?</p><details><summary>Answer &amp; Analysis</summary><p><strong>Answer</strong>: This is called Internal fragmentation. You should adopt the Centralized exiting of functions coding style.</p><p><strong>Analysis</strong>: A request for 140 bytes results in the Slab Allocator providing 192 bytes (usually a power of two or a specific object size). The extra 52 bytes are allocated to the requester but essentially wasted; this is called internal fragmentation. In terms of code maintenance, to prevent memory leaks, the kernel community recommends using <code>goto</code> labels to consolidate cleanup code at the end of the function, known as the &#39;Centralized exiting of functions&#39; style.</p></details><h3 id="exercise-4-thinking" tabindex="-1">Exercise 4: Thinking <a class="header-anchor" href="#exercise-4-thinking" aria-label="Permalink to &quot;Exercise 4: Thinking&quot;">​</a></h3><p><strong>Question</strong>: Suppose you are a kernel developer maintaining a PCI device driver. The driver allocates a large amount of memory in the <code>probe</code> function via <code>kmalloc</code> for device register mapping, but forgets to free it in the <code>remove</code> function due to an early return, causing a memory leak. In addition to manually fixing the <code>goto</code> label, you decide to refactor the code to leverage modern kernel APIs to completely eliminate such human errors. Which API mechanism would you use? How does it work? Also, if you want to detect this type of leak, besides code review, which configuration option (CONFIG_...) can you use at runtime to help discover it?</p><details><summary>Answer &amp; Analysis</summary><p><strong>Answer</strong>: You should use Resource-managed memory allocation (Devres API), such as <code>devm_kmalloc</code>. It ties the memory lifecycle to the device, automatically freeing it when the device detaches or the driver is unloaded. For runtime detection, you can use <code>CONFIG_DEBUG_KMEMLEAK</code>.</p><p><strong>Analysis</strong>: This is a comprehensive thinking question. For the common resource leak problem in drivers, the Devres API (like <code>devm_kmalloc</code>) is the best solution. It leverages the device model core to automatically manage resources, eliminating the need to manually write <code>kfree</code>. As a debugging tool, <code>kmemleak</code> (enabled via <code>CONFIG_DEBUG_KMEMLEAK</code>) can discover dynamically allocated memory that is no longer referenced but not freed by scanning memory and pointer references. This is perfect for verifying the fix during the testing phase.</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>This chapter focused on how to use the Linux kernel&#39;s built-in lightweight tools, SLUB and kmemleak, to track down &quot;silent&quot; and hard-to-reproduce memory corruption and leak issues. As the modern kernel&#39;s default Slab Allocator, SLUB can enforce &quot;poisoning&quot; and &quot;red zone&quot; monitoring on memory by enabling <code>CONFIG_SLUB_DEBUG</code> and using the <code>slub_debug</code> kernel parameter (e.g., <code>FZPU</code>). This converts random bugs like Uninitialized Memory Reads (UMR), Use-After-Free (UAF), Out-Of-Bounds (OOB) writes, and double frees into deterministic error reports at runtime.</p><p>The core mechanism of SLUB debugging lies in using specific magic numbers (like <code>0x6b</code> representing uninitialized or freed memory) to fill the areas surrounding objects. Once a program illegally reads or writes these &quot;landmines,&quot; the kernel immediately catches it and prints detailed stack traces, helping developers pinpoint the culprit at the source code level. Compared to the more powerful but heavily overhead-inducing KASAN, SLUB provides a more flexible &quot;on-demand&quot; debugging strategy, capable of targeting specific caches for checks while catching most memory corruption behaviors without severely degrading system performance.</p><p>For memory leak issues, the guide introduced kmemleak, a scan-based detection tool. It discovers leaks by periodically traversing kernel memory to find allocated memory blocks that have no pointer references. The key to using kmemleak lies in correctly configuring the environment (ensuring the <code>kmemleak=on</code> boot parameter is passed) and mastering the standard troubleshooting workflow (trigger the leak -&gt; manual scan -&gt; view the report -&gt; clear the records), leaving invisible memory black holes that &quot;only allocate, never free&quot; nowhere to hide.</p><p>Beyond tool usage, the guide also introduced userspace helper tools like slabinfo and slabratetop for monitoring slab cache usage and allocation rates. By analyzing the statistics output by these tools, developers can quickly identify which kernel objects are consuming too much memory in the system, or which caches have abnormal allocation frequencies. Combined with mechanisms like kprobe to trace specific call paths, this enables &quot;X-ray vision&quot; and precise analysis of kernel memory behavior.</p>`,58)])])}const g=t(o,[["render",n]]);export{u as __pageData,g as default};
