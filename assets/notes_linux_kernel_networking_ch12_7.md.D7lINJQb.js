import{_ as a,o as s,c as n,a as i}from"./chunks/framework.8tiMeCK-.js";const g=JSON.parse('{"title":"12.7  High Throughput (802.11n) —— 高速公路的入场券","description":"","frontmatter":{},"headers":[],"relativePath":"notes/linux_kernel_networking/ch12_7.md","filePath":"notes/linux_kernel_networking/ch12_7.md","lastUpdated":null}'),t={name:"notes/linux_kernel_networking/ch12_7.md"};function o(r,e,l,c,p,d){return s(),n("div",null,[...e[0]||(e[0]=[i(`<h1 id="_12-7-high-throughput-802-11n-——-高速公路的入场券" tabindex="-1">12.7 High Throughput (802.11n) —— 高速公路的入场券 <a class="header-anchor" href="#_12-7-high-throughput-802-11n-——-高速公路的入场券" aria-label="Permalink to &quot;12.7  High Throughput (802.11n) —— 高速公路的入场券&quot;">​</a></h1><p>上一节我们聊完 <code>mac80211</code> 的骨架和肌肉时，我提到了无线世界不仅需要「通」，更需要「快」。 如果你用过 2005 年之前的 Wi-Fi，你应该记得那种感觉：连上是连上了，传个大文件能把人急死。 问题不在于信号强弱，而在于物理规则的天花板。</p><p>这就到了 802.11n 登场的时候了。 这一节，我们要看它是如何打破这个天花板的。</p><h3 id="_1-提速的代价-mimo-和多天线" tabindex="-1">1. 提速的代价：MIMO 和多天线 <a class="header-anchor" href="#_1-提速的代价-mimo-和多天线" aria-label="Permalink to &quot;1. 提速的代价：MIMO 和多天线&quot;">​</a></h3><p>在 802.11g 刚获批没多久，IEEE 里面就成立了一个「高吞吐量任务组」（TGn），目标很明确——把无线网搞成光纤的速度。 这一搞就是好几年，直到 2009 年才正式定稿。</p><p>但在标准正式发布之前，厂商早就按捺不住了。 Broadcom 开了个先河：他们在 2003 年就发布了基于 802.11g 草案的芯片。 既然有先例，到了 2005 年，市面上就开始出现基于 802.11n 草案的设备了。 Intel 的 Santa Rosa 平台里的 Intel WiFi Link 5000 系列，以及 4965AGN，都算是那个时代的「超前体验」。 Atheros 和 Ralink 也没闲着。 到了 2007 年 6 月，Wi-Fi 联盟甚至开始给这些草案 2.0 的设备发证书。 可以说，这是一场「草台班子」先把产品做出来，标准组织在后头追的游戏。</p><h4 id="频段与-mimo" tabindex="-1">频段与 MIMO <a class="header-anchor" href="#频段与-mimo" aria-label="Permalink to &quot;频段与 MIMO&quot;">​</a></h4><p>802.11n 和前辈们最大的不同在于它的野心——它同时霸占了 2.4 GHz 和 5 GHz 两个频段。 相比之下，11b/g 只能在 2.4 GHz 这个拥堵的菜市场里挤，11a 虽然在 5 GHz，但兼容性一般。</p><p>但光换频段是不够的。 802.11n 引入了一个核心杀手锏：<strong>MIMO（Multiple Input, Multiple Output，多输入多输出）</strong>。</p><p>这个技术的原理其实并不复杂，但效果拔群： 它在 AP 和客户端两边都装上了多根天线。 这就像以前是你一张嘴喊，我一只耳朵听； 现在是你三四张嘴一起喊，我三四只耳朵同时听，而且每张嘴喊的还不太一样，我这边还能通过算法把它们区分开。</p><p>这就带来了两个直接的好处：</p><ol><li><strong>距离更远</strong>：通过多天线的信号合并，可靠性大幅提升。</li><li><strong>速度更快</strong>：理论上，802.11n 的物理层速率能飙到 <strong>600 Mbps</strong>。 当然，实际上你是跑不到这个数的，毕竟还要扣除介质访问的开销、干扰、协议头……但比起以前的 54 Mbps，这已经是质的飞跃了。</li></ol><hr><h3 id="_2-打包的艺术-packet-aggregation" tabindex="-1">2. 打包的艺术：Packet Aggregation <a class="header-anchor" href="#_2-打包的艺术-packet-aggregation" aria-label="Permalink to &quot;2. 打包的艺术：Packet Aggregation&quot;">​</a></h3><p>除了在物理层上堆天线，802.11n 还在 MAC 层动了大手术。 这就像你以前寄信都是一封一封寄，信封本身就有成本；现在改成了寄一本厚书，把一百封信塞进一个包裹里一次性发走。</p><p>这个技术叫 <strong>Packet Aggregation（数据包聚合）</strong>。 它把好几个应用层的数据包塞进一个传输帧里发出去。 为了配合这个机制，802.11e 修正案引入了 <strong>Block Acknowledgment（块确认，简称 BA）</strong>。</p><p>以前发一个包要等一个 ACK，发十个包就要等十个 ACK。 有了 BA 之后，你可以发一堆包，然后对方回一个 BA 就说：「刚才那十个我都收到了」。 中间那种傻等的间隔被砍掉了，效率自然就上来了。</p><p>这里有两种聚合方式，千万别搞混：</p><ul><li><strong>A-MSDU (Aggregated MAC Service Data Unit)</strong>： 把好几个 MSDU（也就是上层下来的数据包）捏在一起，外面只包一个 MAC 头。 ⚠️ <strong>注意</strong>：在 <code>mac80211</code> 里，A-MSDU 只在接收方向支持，发送方向是不支持的。 而且它不依赖我们要讲的 Block Ack 机制。</li><li><strong>A-MPDU (Aggregated MAC Protocol Data Unit)</strong>： 这才是本节的主角。 它是把好几个已经包好 MAC 头的完整 MPDU 打包在一起。 它必须配合 Block Ack 机制使用。 我们接下来要讲的，全是关于 <strong>A-MPDU</strong> 的故事。</li></ul><hr><h3 id="_3-握手-建立-block-ack-会话" tabindex="-1">3. 握手：建立 Block Ack 会话 <a class="header-anchor" href="#_3-握手-建立-block-ack-会话" aria-label="Permalink to &quot;3. 握手：建立 Block Ack 会话&quot;">​</a></h3><p>Block Ack 不是你想开就能开的，这得先办个手续。 这个过程涉及两方：<strong>发起者</strong> 和 <strong>接收者</strong>。 每个 Block Ack 会话都绑定在一个特定的 <strong>TID（Traffic Identifier，流量标识符）</strong> 上。</p><h4 id="第一步-发起请求" tabindex="-1">第一步：发起请求 <a class="header-anchor" href="#第一步-发起请求" aria-label="Permalink to &quot;第一步：发起请求&quot;">​</a></h4><p>一切始于驱动里的速率控制算法。 比如拿 <code>ath9k</code> 驱动来说，它的 <code>ath_tx_status()</code> 函数（在 <code>drivers/net/wireless/ath/ath9k/rc.c</code> 里）会在合适的时候调用 <code>ieee80211_start_tx_ba_session()</code>。</p><p>这个函数干了两件事：</p><ol><li>把状态标记为 <code>HT_ADDBA_REQUESTED_MSK</code>（正在请求中）。</li><li>调用 <code>ieee80211_send_addba_request()</code> 发送 <strong>ADDBA Request</strong> 帧。</li></ol><p>这个请求帧里带着两个关键参数：</p><ul><li><strong>Buffer Size</strong>：重排序缓冲区的大小。 这个值是有上限的，最大不能超过 64K（见 <code>ieee80211_max_ampdu_length_exp</code>）。 这个参数塞在 <code>struct addba_req</code> 的 <code>capab</code> 成员里发出去。</li><li><strong>TID</strong>：标识这是哪一股流量。</li></ul><h4 id="第二步-倒计时与响应" tabindex="-1">第二步：倒计时与响应 <a class="header-anchor" href="#第二步-倒计时与响应" aria-label="Permalink to &quot;第二步：倒计时与响应&quot;">​</a></h4><p>请求发出去之后，倒计时就开始了。 留给对方响应的时间是 <strong>1 秒</strong>（定义里的 <code>ADDBA_RESP_INTERVAL</code>）。 这是个硬性规定。</p><p>如果在 x86_64 上跑过了 1 秒还没收到回音，<code>sta_addba_resp_timer_expired()</code> 这个回调就会被触发。 它会毫不留情地调用 <code>___ieee80211_stop_tx_ba_session()</code>，把这场会话掐死在摇篮里。</p><p>如果对方收到了 ADDBA 请求，流程是这样的：</p><ol><li><strong>先回一个 ACK</strong>： 这和所有 802.11 帧一样，规矩不能坏。</li><li><strong>处理请求</strong>： 调用 <code>ieee80211_process_addba_request()</code>。 如果一切顺利，它会把自己的状态设为 <code>HT_AGG_STATE_OPERATIONAL</code>（开始干活）。</li><li><strong>发送 ADDBA Response</strong>： 调用 <code>ieee80211_send_addba_resp()</code> 告诉对方：「成交，参数我收到了」。</li><li><strong>关掉定时器</strong>： 调用 <code>del_timer_sync()</code> 把刚才那个 1 秒的倒计时炸弹拆了。</li></ol><h4 id="第三步-发包与-bar" tabindex="-1">第三步：发包与 BAR <a class="header-anchor" href="#第三步-发包与-bar" aria-label="Permalink to &quot;第三步：发包与 BAR&quot;">​</a></h4><p>握手完成后，狂欢就开始了。 发起者会把一大包数据（包含多个 MPDU 的 A-MPDU）呼啸着发过去。 发完这一波之后，为了确认对方真的收到了，它需要发送一个 <strong>Block Ack Request (BAR)</strong>。</p><p>这是通过调用 <code>ieee80211_send_bar()</code> 方法完成的。</p><hr><h3 id="_4-bar-催命符" tabindex="-1">4. BAR：催命符 <a class="header-anchor" href="#_4-bar-催命符" aria-label="Permalink to &quot;4. BAR：催命符&quot;">​</a></h3><p><strong>Block Ack Request (BAR)</strong> 是一种控制帧。 它的子类型是 <code>IEEE80211_STYPE_BACK_REQ</code>。 你可以把它理解为发起者拍着接收者的肩膀问：「刚才发过去那一大坨，到底收齐了没？」</p><p>这个包里最重要的信息是 <strong>SSN (Start Sequence Number，起始序列号)</strong>。 它告诉接收者：「从这个序号开始的那一批包，我要确认」。 接收方收到 BAR 后，会去检查自己的 ampdu 缓冲区，如果需要重排就整理一下。</p><p>来看看 BAR 长什么样（内核里的结构体 <code>struct ieee80211_bar</code>）：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ieee80211_bar {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        __le16 frame_control;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        __le16 duration;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        __u8 </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ra</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">6</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          /* 接收者地址 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        __u8 </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ta</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">6</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          /* 发送者地址 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        __le16 control;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      /* 包含 TID 等信息 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        __le16 start_seq_num;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 起始序列号 SSN */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} __packed;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>（<code>include/linux/ieee80211.h</code>）</p><p>在这里，帧控制里的类型是 <code>IEEE80211_FTYPE_CTL</code>，子类型是 <code>IEEE80211_STYPE_BACK_REQ</code>。 <code>ra</code> 是接收者的 MAC 地址，<code>ta</code> 是发送者的。 <code>control</code> 字段里藏着 TID。 <code>start_seq_num</code> 自然就是那个 SSN。</p><hr><h3 id="_5-block-ack-两种确认方式" tabindex="-1">5. Block Ack：两种确认方式 <a class="header-anchor" href="#_5-block-ack-两种确认方式" aria-label="Permalink to &quot;5. Block Ack：两种确认方式&quot;">​</a></h3><p>收到 BAR 之后，接收方必须回一个 <strong>Block Ack (BA)</strong>。 这里有两种策略，取决于当时的硬件能力和协议配置：</p><ol><li><strong>Immediate Block Ack（立即块确认）</strong>： 收到 BAR 后，立马回 BA。 不拖泥带水，性能最好，但对处理速度要求高。</li><li><strong>Delayed Block Ack（延迟块确认）</strong>： 这种方式比较从容： <ul><li>先给 BAR 回一个普通的 ACK。</li><li>等一会儿，处理完了，再发 BA。 这适合那种软件处理能力有限、需要一点缓冲时间的场景。</li></ul></li></ol><p>当然，BA 帧本身也是需要被确认的。</p><h4 id="曲终人散-delba" tabindex="-1">曲终人散：DELBA <a class="header-anchor" href="#曲终人散-delba" aria-label="Permalink to &quot;曲终人散：DELBA&quot;">​</a></h4><p>天下没有不散的筵席。 当发起者没数据发了，或者想结束这场会话，它会调用 <code>ieee80211_send_delba()</code> 发送 <strong>DELBA 请求</strong>。 对方收到后，通过 <code>ieee80211_process_delba()</code> 处理，并把这套 Block Ack 会话拆掉。 这个 DELBA 包，谁都能发——不管是发起者还是接收者，想离场随时能提。</p><hr><h3 id="_6-限制与边界" tabindex="-1">6. 限制与边界 <a class="header-anchor" href="#_6-限制与边界" aria-label="Permalink to &quot;6. 限制与边界&quot;">​</a></h3><p>最后，有几个硬性的边界你得心里有数：</p><ul><li><strong>最大长度</strong>：A-MPDU 的最大长度是 65535 字节。</li><li><strong>模式限制</strong>：数据包聚合只支持 <strong>AP 模式</strong> 和 <strong>Managed 模式</strong>（客户端）。 如果你用 IBSS（Ad Hoc）模式，别想了，规范里压根没支持这个。</li></ul><p>这一套机制下来，无线网络才真正有了「高速」的样子。 但别忘了，这只是高速公路的规则之一。 下一节，我们将进入另一个更复杂的拓扑世界——<strong>Mesh Networking (802.11s)</strong>。 在那里，路由和传输的逻辑会纠缠在一起，形成一张更复杂的网。</p>`,56)])])}const k=a(t,[["render",o]]);export{g as __pageData,k as default};
