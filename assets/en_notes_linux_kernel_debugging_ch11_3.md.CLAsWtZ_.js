import{_ as i,o as t,c as s,a as n}from"./chunks/framework.8tiMeCK-.js";const g=JSON.parse('{"title":"11.3  Building the ARM Target System and Kernel","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch11_3.md","filePath":"en/notes/linux_kernel_debugging/ch11_3.md","lastUpdated":null}'),o={name:"en/notes/linux_kernel_debugging/ch11_3.md"};function a(l,e,r,d,h,p){return t(),s("div",null,[...e[0]||(e[0]=[n(`<h1 id="_11-3-building-the-arm-target-system-and-kernel" tabindex="-1">11.3 Building the ARM Target System and Kernel <a class="header-anchor" href="#_11-3-building-the-arm-target-system-and-kernel" aria-label="Permalink to &quot;11.3  Building the ARM Target System and Kernel&quot;">​</a></h1><p>Don&#39;t rush to flash anything just yet.</p><p>In the previous section, we clarified the difference between <strong>vmlinux</strong> (the blueprint for engineers) and <strong>bzImage</strong> (the compressed package for the machine). Now, we need to put this theory into practice by building a tangible debugging environment capable of running KGDB.</p><p>This is trickier than it sounds—you can&#39;t just grab a production kernel and start debugging it. We need a <strong>custom-built</strong> target system. Any functional Linux system, no matter how minimal, needs these four pieces of the puzzle:</p><ul><li><strong>Bootloader</strong>: Responsible for waking up the kernel. In a QEMU virtual environment, QEMU itself acts as the Bootloader, saving us this step. But on real boards (like ARM embedded devices), we typically use <strong>Das U-Boot</strong>, while on x86, the familiar <strong>GRUB</strong> is the standard.</li><li><strong>DTB (Device Tree Blob)</strong>: A required &quot;hardware manual&quot; for architectures like ARM/ARM64/PPC, telling the kernel what devices are on the board and which bus they are connected to.</li><li><strong>Kernel Image</strong>: This is the compressed package we discussed in the last section—the actual payload loaded during boot.</li><li><strong>Rootfs</strong>: The filesystem mounted after the kernel boots, containing the init process and essential tools.</li></ul><p>Only when you have all four do you have a runnable system.</p><h3 id="_11-3-1-automated-building-the-seals-project" tabindex="-1">11.3.1 Automated Building: The SEALS Project <a class="header-anchor" href="#_11-3-1-automated-building-the-seals-project" aria-label="Permalink to &quot;11.3.1  Automated Building: The SEALS Project&quot;">​</a></h3><p>Building a rootfs from scratch is a tale of woe—BusyBox configuration, library dependencies, init scripts... These aren&#39;t problems solved by writing code; they are pure engineering grunt work.</p><p>To keep us from spending half our lives on &quot;environment setup&quot; in this chapter, we choose to cheat: by using the <strong>SEALS (Simple Embedded ARM Linux System)</strong> project.</p><p>This is an open-source automated build tool designed specifically to generate &quot;minimal but functional&quot; ARM Linux systems. It handles the following for you:</p><ol><li>Kernel configuration and compilation.</li><li>DTB generation.</li><li>A minimal BusyBox-based rootfs.</li></ol><blockquote><p><strong>SEALS Default Configuration</strong></p><p>We use the SEALS <strong>ARM Versatile Express (VExpress)</strong> platform configuration by default.</p><ul><li>CPU: ARMv7 Cortex A9 multi-core.</li><li>Memory: 512 MB RAM.</li><li>Runtime: QEMU virtual machine.</li></ul></blockquote><p>The most elegant part of this approach is <strong>nested virtualization</strong>: you can run an ARM virtual machine on an x86_64 host, and debug the kernel inside that ARM VM. This means you don&#39;t need to buy any ARM development boards to complete all the experiments in this book.</p><p>Of course, Yocto and Buildroot are more powerful industrial-grade solutions, and you might even have a Raspberry Pi or BeagleBone on hand. But for the single goal of &quot;learning KGDB,&quot; SEALS is simple enough, fast enough, and requires no specific hardware.</p><hr><h4 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h4><p>Before using SEALS, your host machine needs a few things:</p><ul><li><strong>QEMU ARM emulator</strong> (<code>qemu-system-arm</code>).</li><li><strong>Cross-compilation toolchain</strong> (x86_64 to ARM32 <code>gcc</code>).</li><li>Some miscellaneous dependency libraries.</li></ul><p>Since SEALS isn&#39;t the focus of this book, we&#39;ll leave the build details to its Wiki. You need to clone the SEALS repository and set up the environment following the Wiki. Here are some signposts:</p><ul><li><strong>Project Homepage</strong>: <a href="https://github.com/kaiwan/seals" target="_blank" rel="noreferrer">https://github.com/kaiwan/seals</a></li><li><strong>Install Dependencies</strong>: <a href="https://github.com/kaiwan/seals/wiki/HOWTO-Install-required-packages-on-the-Host-for-SEALS" target="_blank" rel="noreferrer">HOWTO Install required packages</a></li><li><strong>Detailed Steps</strong>: <a href="https://github.com/kaiwan/seals/wiki/SEALs-HOWTO" target="_blank" rel="noreferrer">SEALs HOWTO</a></li></ul><p>Once configured and running, you should see an interface similar to Figures 11.4 and 11.5. If you see scrolling boot logs in the QEMU window, the foundation is laid.</p><h3 id="_11-3-2-configuring-the-kernel-enabling-kgdb" tabindex="-1">11.3.2 Configuring the Kernel: Enabling KGDB <a class="header-anchor" href="#_11-3-2-configuring-the-kernel-enabling-kgdb" aria-label="Permalink to &quot;11.3.2  Configuring the Kernel: Enabling KGDB&quot;">​</a></h3><p>With the environment ready, it&#39;s the kernel&#39;s turn.</p><p>When you enter <code>make menuconfig</code>, you need to think of yourself as a <strong>detective</strong>. You need to flip on those debugging switches you would normally never touch. We covered the basics of kernel debugging in Chapter 1; here, we perform some specific surgery for KGDB.</p><h4 id="mandatory-options" tabindex="-1">Mandatory Options <a class="header-anchor" href="#mandatory-options" aria-label="Permalink to &quot;Mandatory Options&quot;">​</a></h4><p>Whether you&#39;re using ARM or x86, to get KGDB running, the following kernel configuration options (with the <code>CONFIG_</code> prefix omitted) are <strong>mandatory</strong>:</p><ol><li><p><strong><code>DEBUG_KERNEL=y</code></strong></p><ul><li>Location: <code>Kernel hacking</code> -&gt; <code>Kernel debugging</code></li><li>This is the master switch. Selecting it reveals many of the subsequent debugging options.</li></ul></li><li><p><strong><code>DEBUG_INFO=y</code></strong> —— <strong>Most Important</strong></p><ul><li>Location: <code>Kernel hacking</code> -&gt; <code>Compile-time checks and compiler options</code> -&gt; <code>Compile the kernel with debug info</code></li><li>Function: This passes the <code>-g</code> parameter to the compiler, burning debug symbols into <code>vmlinux</code>.</li><li>Why mandatory? Without it, GDB is just a mess of cryptic addresses, and you&#39;ll have no idea what&#39;s going on. Technically it isn&#39;t &quot;required&quot; (the kernel will still run), but in practice, don&#39;t even try debugging without it.</li></ul></li><li><p><strong><code>KGDB=y</code></strong></p><ul><li>Location: <code>Kernel hacking</code> -&gt; <code>Generic Kernel Debugging Instruments</code> -&gt; <code>KGDB: kernel debugger</code></li><li>Function: This formally embeds the GDB server code into the kernel. Only with this can the kernel understand commands sent from GDB.</li><li>Principle: A <code>gdbserver</code> now runs inside the kernel, sitting there waiting (usually over a serial port or network). Once connected, it executes the GDB commands you send locally and spits the results back.</li></ul></li><li><p><strong><code>KGDB_SERIAL_CONSOLE=y</code></strong></p><ul><li>Function: Allows reusing the serial console for KGDB communication. This is the simplest and most stable connection method.</li><li>Details: This involves a kernel parameter called <code>kgdboc</code> (KGDB over console), which we&#39;ll cover in detail later when we bind it to a specific serial port (like <code>ttyS0</code>).</li></ul></li><li><p><strong><code>KGDB_HONOUR_BLOCKLIST=y</code></strong></p><ul><li><strong>Recommended to enable</strong>. It prevents you from setting breakpoints inside certain non-interruptible functions (like those on the <code>kprobe</code> blacklist), avoiding recursive traps that could deadlock the system.</li></ul></li><li><p><strong><code>MAGIC_SYSRQ=y</code></strong></p><ul><li>Location: <code>Kernel hacking</code> -&gt; <code>Generic Kernel Debugging Instruments</code> -&gt; <code>Magic SysRq key</code></li><li>Function: Allows you to intervene in the kernel at runtime via <code>/proc/sysrq-trigger</code>. Writing a <code>g</code> to it forces the kernel to stop and enter KGDB debugging mode.</li><li>Companion: You also need to set <code>/proc/sys/kernel/sysrq</code> to <code>1</code> at runtime to ensure all SysRq functions are enabled.</li></ul></li></ol><h4 id="what-s-that-thing-called-kdb" tabindex="-1">What&#39;s That Thing Called Kdb? <a class="header-anchor" href="#what-s-that-thing-called-kdb" aria-label="Permalink to &quot;What&#39;s That Thing Called Kdb?&quot;">​</a></h4><p>In this menu, you might also see <strong>Kdb</strong>.</p><p>Kdb is a <strong>command-line-only</strong> debugger. It doesn&#39;t require two machines and can be used directly over a serial port. You can inspect memory, registers, and logs, but it <strong>does not support source-level debugging</strong>.</p><p>It&#39;s like this: KGDB is a professional IDE with a GUI, while Kdb is a console with only a hex editor. We focus on KGDB in this chapter; just understand Kdb as an alternative tool.</p><h4 id="options-to-disable-pitfall-avoidance" tabindex="-1">Options to Disable (Pitfall Avoidance) <a class="header-anchor" href="#options-to-disable-pitfall-avoidance" aria-label="Permalink to &quot;Options to Disable (Pitfall Avoidance)&quot;">​</a></h4><p>Some security features will clash with software debugging breakpoints. If the following options appear in your menu, <strong>turn them off</strong>:</p><ul><li><strong><code>CONFIG_STRICT_KERNEL_RWX</code></strong></li><li><strong><code>CONFIG_STRICT_MODULE_RWX</code></strong></li></ul><p>These two options force the kernel code segment to be read-only and the data segment to be non-executable. While intended as security protection, they prevent GDB from writing software breakpoint instructions (which essentially replace an instruction with <code>INT 3</code>).</p><blockquote><p><strong>Solution</strong>: Either turn them off, or use <strong>Hardware Breakpoints</strong>. Hardware breakpoints don&#39;t modify memory; they use the CPU&#39;s debug registers, so they aren&#39;t affected by this protection. This book recommends prioritizing hardware breakpoints.</p></blockquote><h4 id="recommended-extra-options-optional-but-useful" tabindex="-1">Recommended Extra Options (Optional but Useful) <a class="header-anchor" href="#recommended-extra-options-optional-but-useful" aria-label="Permalink to &quot;Recommended Extra Options (Optional but Useful)&quot;">​</a></h4><p>If you don&#39;t mind the extra effort, the following options can improve the experience:</p><ul><li><strong><code>FRAME_POINTER=y</code></strong> (<code>Kernel hacking</code> -&gt; <code>Compile the kernel with frame pointers</code>) <ul><li>Although marked as Optional, this is very useful for stack unwinding. If your platform has <code>CONFIG_UNWINDER_ARM</code> enabled by default, this option might conflict—choose accordingly.</li></ul></li><li><strong><code>DEBUG_INFO_SPLIT=y</code></strong>: Splits the debug information into a separate <code>.dwo</code> file, reducing the vmlinux file size.</li><li><strong><code>GDB_SCRIPTS=y</code></strong>: This is fantastic. When you load vmlinux, it automatically links some Python-based GDB helper scripts (like <code>lx-symbols</code>, <code>lx-lsmod</code>). We have a dedicated section on this later.</li><li><strong><code>DEBUG_FS=y</code></strong>: Mounts the debugfs filesystem, where a lot of kernel debugging information resides.</li></ul><blockquote><p>⚠️ <strong>One Last Warning</strong></p><p>When debugging the kernel, <strong>always disable the watchdog</strong>.</p><p>If a software or hardware watchdog is running and you pause at a breakpoint for too long, the watchdog feed will timeout and the system will reboot directly. The bug you just found will vanish without a single error message.</p></blockquote><hr><h4 id="enabling-kgdb-in-seals" tabindex="-1">Enabling KGDB in SEALS <a class="header-anchor" href="#enabling-kgdb-in-seals" aria-label="Permalink to &quot;Enabling KGDB in SEALS&quot;">​</a></h4><p>If you&#39;re using SEALS, things are a bit simpler. The SEALS build scripts have a built-in KGDB switch.</p><p>Open your board&#39;s <code>build.config</code> file and find this line:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> KGDB</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build.config</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">KGDB_MODE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # make &#39;1&#39; to have qemu run with the &#39;-s -S&#39; </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            # switch (waits for client GDB to &#39;connect&#39;)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Change <code>KGDB_MODE</code> to <code>1</code>. This way, the QEMU boot script generated by SEALS will automatically add the <code>-s -S</code> parameter, causing QEMU to freeze the CPU at boot and wait patiently for GDB to connect.</p><blockquote><p><strong>Tip: GCC Plugins</strong></p><p>If asked to Enable GCC plugins? during kernel compilation, we recommend selecting <strong>No</strong>. Plugins can sometimes interfere with debug information generation or introduce weird optimizations, actually making debugging harder. Keep it simple.</p></blockquote><h3 id="_11-3-3-test-running-the-target-system" tabindex="-1">11.3.3 Test-Running the Target System <a class="header-anchor" href="#_11-3-3-test-running-the-target-system" aria-label="Permalink to &quot;11.3.3  Test-Running the Target System&quot;">​</a></h3><p>Don&#39;t take my word for it—trust the facts.</p><p>After configuring and compiling, gather all the components generated by SEALS. In the SEALS staging directory, you should find these items:</p><ul><li><strong>Kernel Images</strong>: <ul><li><code>.../linux-5.10.109/arch/arm/boot/zImage</code> (the compressed package for the Bootloader, used for booting)</li><li><code>.../linux-5.10.109/vmlinux</code> (the <strong>blueprint</strong>, with debug symbols, used by GDB)</li></ul></li><li><strong>Device Tree</strong>: <code>.../linux-5.10.109/arch/arm/boot/dts/vexpress-v2p-ca9.dtb</code></li><li><strong>Rootfs</strong>: <code>.../seals_staging_vexpress/images/rfs.img</code></li></ul><p>Now, string them together with QEMU.</p><p>If you&#39;re running QEMU manually, the command looks roughly like this (don&#39;t be intimidated—we&#39;ll break it down):</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">qemu-system-arm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -M</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vexpress-a9</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -m</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 512</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -smp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 4,sockets=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -kernel</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/seals_staging_vexpress/images/zImage</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -drive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> file=/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/seals_staging_vexpress/images/rfs.img,if=sd,format=raw</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -append</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;console=ttyAMA0 rootfstype=ext4 root=/dev/mmcblk0 init=/sbin/init&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -dtb</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/seals_staging_vexpress/images/vexpress-v2p-ca9.dtb</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -nographic</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p><strong>Parameter Breakdown (don&#39;t skip this)</strong>:</p><ul><li><code>-M vexpress-a9</code>: Specifies the emulated board model. Running <code>qemu-system-arm -M help</code> shows all boards supported by QEMU.</li><li><code>-m 512</code>: Allocates 512MB of RAM to the virtual machine.</li><li><code>-kernel ...</code>: Specifies the kernel image. Note that we provide the <strong>zImage</strong> here because we want to boot it.</li><li><code>-drive file=...,if=sd</code>: Specifies the rootfs image and tells QEMU to emulate it as an SD card.</li><li><code>-append &quot;...&quot;</code>: These are the boot parameters passed to the kernel. <ul><li><code>console=ttyAMA0</code>: Redirects console output to this virtual serial port (the standard serial port for ARM).</li><li><code>root=/dev/mmcblk0</code>: Tells the kernel that the rootfs is on the emulated SD card.</li></ul></li><li><code>-dtb ...</code>: You must specify the DTB file, otherwise the kernel won&#39;t know what hardware it&#39;s running on.</li></ul><p>If everything goes well, you&#39;ll see scrolling boot logs in the terminal, finally stopping at the BusyBox shell prompt:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[    2.345678] Freeing unused kernel memory: 204K</span></span>
<span class="line"><span>/ #</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>See that <code>/ #</code>?</p><p>Congratulations, your ARM virtual machine is alive.</p><p>Figures 11.4 and 11.5 show screenshots of this step (QEMU boot logs and shell login). If you can reproduce this interface, the &quot;target system&quot; piece is officially done.</p><p>Now we have a well-behaved virtual sheep in our hands. In the next chapter, we take a knife (GDB) to it and see how it runs on the inside.</p>`,62)])])}const u=i(o,[["render",a]]);export{g as __pageData,u as default};
