import{_ as e,o as a,c as i,a as n}from"./chunks/framework.8tiMeCK-.js";const g=JSON.parse('{"title":"14.15  宏定义与工具函数","description":"","frontmatter":{},"headers":[],"relativePath":"notes/linux_kernel_networking/ch14_15.md","filePath":"notes/linux_kernel_networking/ch14_15.md","lastUpdated":null}'),o={name:"notes/linux_kernel_networking/ch14_15.md"};function t(l,s,d,r,c,p){return a(),i("div",null,[...s[0]||(s[0]=[n(`<h1 id="_14-15-宏定义与工具函数" tabindex="-1">14.15 宏定义与工具函数 <a class="header-anchor" href="#_14-15-宏定义与工具函数" aria-label="Permalink to &quot;14.15  宏定义与工具函数&quot;">​</a></h1><hr><p>这一节虽然名字叫 &quot;Macros&quot;（宏），但核心问题只有一个：<strong>数据包是如何在内核中流动的？</strong></p><p>如果你是一名网络驱动的开发者，当你写下一行 PCI 驱动注册宏时，你可能会好奇：这行代码到底是如何与内核庞大的设备模型挂钩的？更重要的是，当数据包像幽灵一样穿过硬件到达内核时，它变成了什么？</p><p>此外，本章作为终章的压轴部分，接下来的内容将带你深入 Linux 内核网络栈的**「基石」**：<code>sk_buff</code> 和 <code>net_device</code>。</p><p>在此之前，我必须坦诚地告诉你：接下来的内容密度极高。如果你觉得之前的章节是在「讲故事」，那么附录 A 就是在「拆解引擎」。我们会把内核中最核心的两个数据结构——Socket Buffer（SKB）和网络设备——拆开来看，每一个字段、每一个指针、每一个比特位的含义都不会放过。</p><p>这不是为了让你背下来，而是为了让你在调试崩溃日志时，能像看地图一样看清内核的脉络。</p><hr><h2 id="_15-1-注册-只是个开始" tabindex="-1">15.1 注册：只是个开始 <a class="header-anchor" href="#_15-1-注册-只是个开始" aria-label="Permalink to &quot;15.1  注册：只是个开始&quot;">​</a></h2><p>写 PCI 网络驱动的时候，你做的第一件事往往不是初始化硬件，而是<strong>向内核报到</strong>。</p><p>你可以把这行宏想象成一个**「信封」**，上面写着「我负责处理这种 ID 的设备」。</p><p>但这个「信封」比喻有一个地方是错的：真正的信封寄出去就结束了，而注册宏只是故事的开端。当内核在 PCI 总线上发现匹配的设备时，它会唤醒你的驱动，但数据包的流动并不依赖驱动本身——而是依赖内核为这个设备精心准备的 <code>net_device</code> 结构，以及承载灵魂的 <code>sk_buff</code>。</p><p>回到那个「信封」：你现在应该能看出来，注册宏只是把信封投进了邮箱，真正漫长的旅程，是在内核接过信封之后才开始的。接下来，我们将深入内核，看看它到底是怎么处理这封信的。</p><hr><h2 id="附录-a-深入内核数据结构" tabindex="-1">附录 A：深入内核数据结构 <a class="header-anchor" href="#附录-a-深入内核数据结构" aria-label="Permalink to &quot;附录 A：深入内核数据结构&quot;">​</a></h2><p>接下来的内容，是整本书的**「硬核附录」**。如果你真的想理解 Linux 网络栈是如何运作的，而不是仅仅停留在调参层面，这里是必经之路。</p><p>这里涵盖了 Linux 内核网络栈中最核心的两个数据结构：<strong><code>sk_buff</code></strong>（Socket Buffer）和 <strong><code>net_device</code></strong>。</p><p>为了不让你迷失在 27 个字段的海洋里，我们不会像念说明书一样从头念到尾。相反，我们会沿着数据包在内核中的<strong>流动路径</strong>来拆解 <code>sk_buff</code>——从分配、到指针移动、再到最终释放。我们会看到哪些字段在控制数据包的生命，哪些字段在优化性能，以及哪些字段是专门为了「绕过拷贝」而设计的。</p><p>最后，我们还会简单看看 <code>net_device</code> 和 RDMA，这是连接物理世界和内核虚拟接口的桥梁。</p><hr><h3 id="a-1-sk-buff-数据包的灵魂" tabindex="-1">A.1 <code>sk_buff</code>：数据包的灵魂 <a class="header-anchor" href="#a-1-sk-buff-数据包的灵魂" aria-label="Permalink to &quot;A.1 \`sk_buff\`：数据包的灵魂&quot;">​</a></h3><p><code>sk_buff</code> 结构体代表一个<strong>网络数据包</strong>。SKB 是 Socket Buffer 的缩写。</p><p>想象一下，一个数据包可能由本地用户空间的 socket 产生（通过 HTTP 请求），也可能从物理网卡接收上来。它可能发往外部，也可能发给本机的另一个 socket。对于内核来说，无论它的来源和去向是什么，它最终都会被封装在一个 <code>sk_buff</code> 结构中，在网络栈的各层之间传递。</p><p>你可以把 <code>sk_buff</code> 想象成一个**「信封」**。</p><ul><li><strong>信封背面</strong>写着谁寄的（<code>sk</code>）、寄给谁（<code>dev</code>）、走哪条路（<code>dst</code>）。</li><li><strong>信封里面</strong>装着信的内容（<code>data</code> 指针指向的数据）。</li></ul><p>但这个「信封」比喻有一个地方是错的：真正的信封一旦封口就不能改了，而 <code>sk_buff</code> 的数据区是<strong>动态伸缩</strong>的。协议栈的每一层（TCP、IP、以太网）都会在信封里塞一层自己的「垫纸」（协议头），或者把上面那层的垫纸撕掉。<code>sk_buff</code> 最精妙的设计，就是用指针的移动来实现这种「塞垫纸」和「撕垫纸」的过程，而不是真的把数据搬来搬去。</p><p>回到那个「信封」：你现在应该能看出来，<code>data</code> 指针就是信封里当前能看到的那一页纸的顶行，而 <code>head</code> 和 <code>end</code> 就是这张纸的物理边界。如果 <code>head</code> 到了 <code>data</code> 的位置，说明没有空间写新地址了（Headroom 耗尽），这时候内核可能不得不重新找一张更大的纸（拷贝数据），这是性能杀手。</p><h4 id="_1-指针管理-俄罗斯方块" tabindex="-1">1. 指针管理：俄罗斯方块 <a class="header-anchor" href="#_1-指针管理-俄罗斯方块" aria-label="Permalink to &quot;1. 指针管理：俄罗斯方块&quot;">​</a></h4><p>这是 SKB 最精妙也最容易出错的地方。<code>head</code>、<code>data</code>、<code>tail</code>、<code>end</code> 这四个指针定义了数据包在内存中的布局。</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> transport_header;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // L4 头部位置</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> network_header;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // L3 头部位置</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mac_header;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       // L2 头部位置</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> tail;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">             // 数据的尾部</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> end;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">              // 缓冲区的尾部</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">head;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">             // 缓冲区的起始</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">data;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">             // 数据的起始</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p><strong>核心概念：线性数据区 vs. 非线性数据区</strong></p><ul><li><strong>线性数据区</strong>：从 <code>head</code> 到 <code>end</code> 的区域是分配给 SKB 的整个线性缓冲区。</li><li><strong>有效数据</strong>：从 <code>data</code> 到 <code>tail</code> 的区域是当前有效的数据载荷。</li><li><strong>协议头</strong>：<code>mac_header</code>、<code>network_header</code>、<code>transport_header</code> 是指向线性缓冲区内特定位置的指针，分别指向 L2、L3、L4 层的起始位置。</li></ul><p><strong>操作宏（修改 <code>data</code> 和 <code>tail</code> 指针）：</strong></p><ul><li><strong><code>skb_put(skb, len)</code></strong>: 在尾部<strong>增加</strong>数据。<code>tail</code> 指针下移，<code>len</code> 增加。用于接收数据或构建协议头。</li><li><strong><code>skb_push(skb, len)</code></strong>: 在头部<strong>增加</strong>数据。<code>data</code> 指针上移，<code>len</code> 增加。用于添加协议头（例如，IP 层在发送前调用此函数添加 IP 头）。</li><li><strong><code>skb_pull(skb, len)</code></strong>: 从头部<strong>移除</strong>数据。<code>data</code> 指针下移，<code>len</code> 减少。用于剥离协议头（例如，IP 层处理完后，将 <code>data</code> 指针移过 IP 头，交给 TCP 层）。</li><li><strong><code>skb_reserve(skb, len)</code></strong>: 预留头部空间。<code>data</code> 和 <code>tail</code> 指针同时下移。通常在分配 SKB 后立即调用，为了在前面留出足够的空间给下层协议添加头部。</li></ul><p><strong>Headroom 和 Tailroom</strong></p><ul><li><strong>Headroom</strong>: <code>data</code> 减去 <code>head</code>。这是预留的头部空间。</li><li><strong>Tailroom</strong>: <code>end</code> 减去 <code>tail</code>。这是预留的尾部空间。</li></ul><p>这种设计使得内核可以在<strong>不拷贝数据</strong>的情况下，高效地在各层协议之间传递数据包，只需移动指针即可。</p><h4 id="_2-机制拆解-一个包的旅程" tabindex="-1">2. 机制拆解：一个包的旅程 <a class="header-anchor" href="#_2-机制拆解-一个包的旅程" aria-label="Permalink to &quot;2. 机制拆解：一个包的旅程&quot;">​</a></h4><p>光看指针定义容易晕，我们来推演一个数据包从网卡驱动（L2）到 TCP（L4）的过程中，SKB 内部到底发生了什么。</p><p><strong>场景：主机收到一个 TCP 数据包</strong></p><ol><li><p><strong>L2 入场（驱动接收）</strong></p><ul><li>驱动分配一个 SKB，通常使用 <code>netdev_alloc_skb()</code>。这步通常还会调用 <code>skb_reserve()</code> 预留头部空间。</li><li>驱动把数据 DMA 到 <code>data</code> 位置。</li><li>此时，<code>data</code> 指向<strong>以太网头</strong>。</li></ul></li><li><p><strong>L2 -&gt; L3（网络层）</strong></p><ul><li>内核调用 <code>eth_type_trans()</code>。这个函数做了两件事： <ul><li>将 <code>skb-&gt;protocol</code> 设置为 <code>ETH_P_IP</code>（ IPv4）。</li><li>调整 <code>skb-&gt;mac_header</code> 指向 L2 头。</li><li>调用 <code>skb_pull(skb, hlen)</code> 剥去以太网头。</li></ul></li><li><strong>关键点</strong>：此时 <code>data</code> 指针跳过了以太网头，直接指向 <strong>IP 头</strong>。</li><li>这就是为什么 TCP 层不需要处理以太网头——因为内核在交货前，已经通过移动指针把“垃圾”扔掉了。</li></ul></li><li><p><strong>L3 -&gt; L4（传输层）</strong></p><ul><li>IP 层处理完（检查校验和、路由等）后，调用 <code>ip_rcv()</code>。</li><li>同样，调用 <code>skb_pull()</code> 剥去 IP 头。</li><li>此时 <code>data</code> 指针指向 <strong>TCP 头</strong>。</li><li>最后，SKB 被放入 socket 的接收队列。</li></ul></li></ol><p><strong>为什么要这样折腾？</strong> 为了效率。内核不需要复制数据，只需要移动 <code>data</code> 指针，就能改变当前层“看到”的内容。这也是为什么 SKB 结构体如此复杂——它必须精确记录每一层协议头的位置，以便在需要时（比如计算 TCP 校验和）能瞬间找回 IP 头。</p><h4 id="_3-路由与设备-信封上的地址" tabindex="-1">3. 路由与设备：信封上的地址 <a class="header-anchor" href="#_3-路由与设备-信封上的地址" aria-label="Permalink to &quot;3. 路由与设备：信封上的地址&quot;">​</a></h4><p>当内核拿到这个 SKB，它首先需要知道：这是哪来的？要去哪？</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dev;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // 关联的网卡</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> dst_entry </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">_skb_refdst;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 路由缓存</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><ul><li><strong><code>dev</code></strong>: 这张「网卡」就是数据的出入口。如果是接收包，它代表进来的接口；如果是发送包，它代表出去的接口。</li><li><strong><code>_skb_refdst</code></strong>: 这是内核的<strong>路由决策结果</strong>。内核查过一次路由表后，就把结果（下一跳、输出接口）缓存在这里。这样下次处理这个包（比如转发）时，就不用再查表了。这个字段的低几位被借用来做引用计数标记，这是内核省内存的经典技巧。</li></ul><h4 id="_4-协议私有控制区-cb" tabindex="-1">4. 协议私有控制区（<code>cb[]</code>） <a class="header-anchor" href="#_4-协议私有控制区-cb" aria-label="Permalink to &quot;4. 协议私有控制区（\`cb[]\`）&quot;">​</a></h4><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">char</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> cb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">48</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>这是一个**「控制缓冲区」**（Control Buffer）。它是内核留给各层协议存放私有信息的便签本。</p><ul><li><strong>为什么存在</strong>：为了效率。内核不想为每个协议定义单独的 SKB 变体，所以预留了这块空间。</li><li><strong>谁在用</strong>： <ul><li><strong>TCP 协议</strong>：通过宏 <code>TCP_SKB_CB(__skb)</code> 将其强制转换为 TCP 的控制结构 <code>tcp_skb_cb</code>，用来存储 TCP 序列号、确认号等信息。</li><li><strong>蓝牙协议</strong>：通过宏 <code>bt_cb(skb)</code> 转换为蓝牙的控制结构。</li></ul></li><li><strong>注意</strong>：这块区域是不透明的，一层写入后，下一层可能会覆盖或重新解释它。</li></ul><h4 id="_5-数据长度管理" tabindex="-1">5. 数据长度管理 <a class="header-anchor" href="#_5-数据长度管理" aria-label="Permalink to &quot;5. 数据长度管理&quot;">​</a></h4><p>SKB 中的长度字段比较容易混淆，需要区分清楚：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> len;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           // 数据包的总字节数</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> data_len;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // 非线性数据的长度（即分页数据）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">__u16 mac_len;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">              // MAC 头部（L2）的长度</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">__wsum csum;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                // 校验和</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><ul><li><strong><code>len</code></strong>: 整个数据包的长度（包括线性数据和非线性分页数据）。</li><li><strong><code>data_len</code></strong>: 如果数据包使用了分散/聚合，部分数据存储在单独的内存页中，<code>data_len</code> 就记录这部分的大小。如果 <code>data_len</code> 为 0，说明所有数据都是线性连续存储的。</li><li><strong><code>mac_len</code></strong>: 链路层头部的长度（例如以太网头通常是 14 字节）。</li></ul><h4 id="_6-校验和与硬件卸载" tabindex="-1">6. 校验和与硬件卸载 <a class="header-anchor" href="#_6-校验和与硬件卸载" aria-label="Permalink to &quot;6. 校验和与硬件卸载&quot;">​</a></h4><p>现代网卡很聪明，它能帮你算校验和，甚至帮你分片。</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">__u8 ip_summed:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><ul><li><strong><code>CHECKSUM_NONE</code></strong>: 硬件不支持校验，必须由软件完成。</li><li><strong><code>CHECKSUM_UNNECESSARY</code></strong>: 不需要校验（例如回环设备）。</li><li><strong><code>CHECKSUM_COMPLETE</code></strong>: 硬件已完成校验计算（接收路径）。</li><li><strong><code>CHECKSUM_PARTIAL</code></strong>: 硬件将完成校验计算（发送路径，仅计算头部）。</li></ul><h4 id="_7-分片与克隆-cloned-users" tabindex="-1">7. 分片与克隆（<code>cloned</code>, <code>users</code>） <a class="header-anchor" href="#_7-分片与克隆-cloned-users" aria-label="Permalink to &quot;7. 分片与克隆（\`cloned\`, \`users\`）&quot;">​</a></h4><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">atomic_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> users;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">__u8 cloned:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><ul><li><strong><code>users</code></strong>: SKB 的引用计数。初始化为 1。</li><li><strong><code>cloned</code></strong>: 标记该 SKB 是否是<strong>克隆</strong>出来的。</li><li><strong>机制</strong>：当内核只需要修改 SKB 的元数据（如 <code>dev</code> 指针），而不需要修改数据内容时，为了避免昂贵的内存拷贝，它会克隆 SKB 结构体本身，但共享底下的数据块。此时，原始 SKB 和克隆 SKB 的 <code>cloned</code> 标志都会被置为 1。</li></ul><h4 id="_8-非线性数据-skb-shared-info" tabindex="-1">8. 非线性数据：<code>skb_shared_info</code> <a class="header-anchor" href="#_8-非线性数据-skb-shared-info" aria-label="Permalink to &quot;8. 非线性数据：\`skb_shared_info\`&quot;">​</a></h4><p>当数据量很大（超过一页，通常 4KB）时，内核会将多余的数据存放在分散的内存页中，而不是挤在线性数据区。这些分散的页的信息就存储在 <code>skb_shared_info</code> 结构中。</p><p>它位于线性数据区的<strong>最末端</strong>（通过 <code>skb_end_pointer()</code> 访问）。</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> skb_shared_info {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> nr_frags;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            // 分散页的数量</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ... 其他字段 ...</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    skb_frag_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> frags</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[MAX_SKB_FRAGS];</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 分散页数组</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><ul><li><strong><code>nr_frags</code></strong>: 当前使用了多少个分散页。</li><li><strong><code>frags[]</code></strong>: 存储每个页的物理地址、偏移量和长度。</li><li><strong><code>frag_list</code></strong>: 另一种非线性数据形式，直接挂一个 SKB 链表（用于 IP 分片重组）。</li></ul><hr><h3 id="a-2-net-device-驻守要塞的将军" tabindex="-1">A.2 <code>net_device</code>：驻守要塞的将军 <a class="header-anchor" href="#a-2-net-device-驻守要塞的将军" aria-label="Permalink to &quot;A.2 \`net_device\`：驻守要塞的将军&quot;">​</a></h3><p>如果说 <code>sk_buff</code> 是流水的「兵」，那么 <code>net_device</code> 就是驻守要塞的「将」。</p><p><code>net_device</code> 结构体代表一个网络接口设备。它可以是物理设备（如 <code>eth0</code>），也可以是虚拟设备（如 <code>bridge0</code> 或 <code>vlan100</code>）。</p><h4 id="_1-操作集-设备的灵魂" tabindex="-1">1. 操作集：设备的灵魂 <a class="header-anchor" href="#_1-操作集-设备的灵魂" aria-label="Permalink to &quot;1. 操作集：设备的灵魂&quot;">​</a></h4><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device_ops </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">netdev_ops;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>这是 <code>net_device</code> 的<strong>灵魂</strong>。它是一组函数指针，定义了内核如何指挥这个设备。当你在用户态执行 <code>ip link set eth0 up</code> 时，内核最终调用的是这里的 <code>ndo_open()</code>。</p><ul><li><strong><code>ndo_open()</code></strong>: 启动设备。</li><li><strong><code>ndo_stop()</code></strong>: 关闭设备。</li><li><strong><code>ndo_start_xmit()</code></strong>: <strong>最重要</strong>的函数。内核网络栈调用它来发送数据包。驱动必须将数据包写入硬件发送队列。</li><li><strong><code>ndo_set_mac_address()</code></strong>: 修改 MAC 地址。</li><li><strong><code>ndo_tx_timeout()</code></strong>: 发送卡死时的看门狗回调。</li></ul><h4 id="_2-硬件特性与卸载" tabindex="-1">2. 硬件特性与卸载 <a class="header-anchor" href="#_2-硬件特性与卸载" aria-label="Permalink to &quot;2. 硬件特性与卸载&quot;">​</a></h4><p>驱动通过告诉内核它支持哪些硬件加速功能，内核才能决定是否把繁重的任务交给硬件完成。</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">netdev_features_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> features;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       // 当前激活的特性</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">netdev_features_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hw_features;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 硬件支持的特性</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><ul><li><strong><code>NETIF_F_IP_CSUM</code></strong>: 硬件支持 IPv4 校验和计算。</li><li><strong><code>NETIF_F_TSO</code></strong>: TCP Segmentation Offload。硬件负责把大的 TCP 段切分成符合 MTU 的小包。</li><li><strong><code>NETIF_F_GRO</code></strong>: Generic Receive Offload。硬件或内核在接收时将多个小包合并成大包，减少 CPU 中断。</li></ul><h4 id="_3-状态与队列" tabindex="-1">3. 状态与队列 <a class="header-anchor" href="#_3-状态与队列" aria-label="Permalink to &quot;3. 状态与队列&quot;">​</a></h4><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> state;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> netdev_queue </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">_tx;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><ul><li><strong><code>state</code></strong>: 记录链路状态（如 <code>__LINK_STATE_START</code> 启动，<code>__LINK_STATE_NOCARRIER</code> 拔网线）。</li><li><strong><code>_tx</code></strong>: 发送队列数组。现代高性能网卡通常有多个队列，以利用多核 CPU 的并行处理能力。</li></ul><hr><h3 id="a-3-rdma-remote-dma" tabindex="-1">A.3 RDMA (Remote DMA) <a class="header-anchor" href="#a-3-rdma-remote-dma" aria-label="Permalink to &quot;A.3 RDMA (Remote DMA)&quot;">​</a></h3><p>RDMA 是一种高性能网络技术，允许一台计算机直接访问另一台计算机的内存，<strong>无需远程 CPU 的参与</strong>。这极大地降低了延迟并提高了吞吐量。</p><p>你可以把 RDMA 想象成**「专线快递」**。普通的网络通信像寄信，需要邮局（远程 CPU）分拣；RDMA 则是你直接把车开进对方的仓库搬货。</p><p>但这个「专线快递」有一个前提：<strong>安全</strong>。你不能随便让人进你的仓库。</p><h4 id="_1-protection-domain-pd-安保边界" tabindex="-1">1. Protection Domain (PD) - 安保边界 <a class="header-anchor" href="#_1-protection-domain-pd-安保边界" aria-label="Permalink to &quot;1. Protection Domain (PD) - 安保边界&quot;">​</a></h4><p>PD 就像是一个<strong>会员俱乐部</strong>。所有的资源（内存区域、队列对）都必须属于同一个俱乐部（PD）才能互相访问。如果你不属于这个俱乐部，就算有地址也不让进。</p><h4 id="_2-memory-region-mr-仓库登记" tabindex="-1">2. Memory Region (MR) - 仓库登记 <a class="header-anchor" href="#_2-memory-region-mr-仓库登记" aria-label="Permalink to &quot;2. Memory Region (MR) - 仓库登记&quot;">​</a></h4><p>在 RDMA 中，你不能随便拿一块内存地址就给远程机器写。你必须先<strong>注册</strong>这块内存。</p><ul><li><strong>注册过程</strong>： <ol><li><strong>Pinning（钉住）</strong>：防止内存被换出到磁盘。</li><li><strong>翻译</strong>：将虚拟地址翻译为物理地址，并告知网卡 DMA 引擎。</li></ol></li><li><strong>Key（密钥）</strong>：注册后，你会得到两个 Key： <ul><li><strong>LKey (Local Key)</strong>：本地访问时使用。</li><li><strong>RKey (Remote Key)</strong>：远程机器访问这块内存时必须提供的凭证。</li></ul></li></ul><p>回到那个「快递」：MR 就像是你向仓库（内存）登记的货物清单。只有登记过的货物，并且持有对应 RKey 的快递员（网卡），才能搬运这批货。如果对方拿错了钥匙，或者试图搬运没登记的货物，RDMA 硬件会直接拒绝访问，保护你的内存安全。</p><h4 id="_3-queue-pair-qp-通信通道" tabindex="-1">3. Queue Pair (QP) - 通信通道 <a class="header-anchor" href="#_3-queue-pair-qp-通信通道" aria-label="Permalink to &quot;3. Queue Pair (QP) - 通信通道&quot;">​</a></h4><p>这是 RDMA 通信的<strong>核心通道</strong>。一个 QP 包含两个队列：</p><ul><li><strong>Send Queue (SQ)</strong>: 发送工作请求。</li><li><strong>Receive Queue (RQ)</strong>: 接收工作请求。</li></ul><p><strong>QP 类型</strong>：</p><ul><li><strong>RC (Reliable Connected)</strong>: 可靠连接，类似 TCP。</li><li><strong>UD (Unreliable Datagram)</strong>: 不可靠数据报，类似 UDP。</li></ul><h4 id="_4-completion-queue-cq-回执" tabindex="-1">4. Completion Queue (CQ) - 回执 <a class="header-anchor" href="#_4-completion-queue-cq-回执" aria-label="Permalink to &quot;4. Completion Queue (CQ) - 回执&quot;">​</a></h4><p>RDMA 操作是<strong>异步</strong>的。当你发起一个发送请求时，函数会立即返回。你是怎么知道发送完成了呢？靠 CQ。硬件会将完成状态写入 CQ，你可以轮询或者等待通知。</p><hr><h2 id="本章回响" tabindex="-1">本章回响 <a class="header-anchor" href="#本章回响" aria-label="Permalink to &quot;本章回响&quot;">​</a></h2><p>本章真正在做的事情，是建立<strong>数据包视角</strong>这一底层认知。</p><p>表面上我们在看一个个结构体字段，实际上我们在理解内核是如何权衡「拷贝的代价」与「指针的复杂度」的。<code>sk_buff</code> 的 <code>data</code> 指针之所以要这样跳来跳去，本质上是为了避免数据拷贝——因为拷贝内存是网络性能的头号杀手。</p><p>还记得开头那个问题吗——<strong>如何理解一个在网络栈中流动的数据包</strong>？ 现在你应该能回答了：它不是一个静止的数据块，而是一个在不同协议层之间不断变化的「视角」。L2 看到的是帧，L3 看到的是包，L4 看到的是流，而内核通过巧妙地移动 <code>sk_buff</code> 中的指针，让所有层都能高效地处理同一个物理内存中的数据。那一行注册宏，仅仅是这一切的入场券；真正的演出，是在这些结构体中上演的。</p><p>下一章（如果有的话），我们将把这些知识带回现实，去解决那些真实世界中发生的、令人抓狂的 Bug。祝你好运。</p><hr><h2 id="练习题" tabindex="-1">练习题 <a class="header-anchor" href="#练习题" aria-label="Permalink to &quot;练习题&quot;">​</a></h2><p>走到这里，机制应该已经清楚了——或者你以为清楚了。 下面几道题难度递进，建议先不看提示独立想，卡住了再翻。 第三题如果做出来了，说明你真的懂了。</p><h3 id="练习-15-1-模拟协议封装-指针演练-⭐-理解" tabindex="-1">练习 15.1 模拟协议封装（指针演练）⭐（理解） <a class="header-anchor" href="#练习-15-1-模拟协议封装-指针演练-⭐-理解" aria-label="Permalink to &quot;练习 15.1  模拟协议封装（指针演练）⭐（理解）&quot;">​</a></h3><p>假设你刚刚在内核中分配了一个空的 SKB，<code>data</code> 和 <code>tail</code> 指针重合，且已经预留了 64 字节的 Headroom。 现在需要依次构建一个 TCP 数据包（暂不填充数据，只考虑头部预留）：</p><ol><li>调用 <code>skb_push(skb, 20)</code> 预留 <strong>IP 头</strong>。</li><li>调用 <code>skb_push(skb, 20)</code> 预留 <strong>TCP 头</strong>。</li></ol><p><strong>问题</strong>：经过这两步操作后，<code>data</code> 指针相对于初始位置移动了多少字节？此时 <code>data</code> 指针指向的是哪一层协议的头部位置？</p><details><summary>答案与解析</summary><p><strong>答案</strong>：移动了 <strong>40 字节</strong>（20+20）。此时 <code>data</code> 指针指向 <strong>TCP 头</strong>（L4）的起始位置。</p><p><strong>解析</strong>： <code>skb_push</code> 的作用是让 <code>data</code> 指针向低地址移动（向前扩展），并在头部腾出空间。 先 Push 20 字节（IP 头），<code>data</code> 指针上移 20 字节。 再 Push 20 字节（TCP 头），<code>data</code> 指针继续上移 20 字节。 总共移动 40 字节。 在构建发送包时，我们通常按照从 L4 到 L2 的顺序反向 push 头部，所以此时 <code>data</code> 指向最外层（也是最先构建）的 TCP 头。等到真正发送时，驱动会再次 push 以太网头。</p></details><h3 id="练习-15-2-headroom-耗尽⭐⭐-应用" tabindex="-1">练习 15.2 Headroom 耗尽⭐⭐（应用） <a class="header-anchor" href="#练习-15-2-headroom-耗尽⭐⭐-应用" aria-label="Permalink to &quot;练习 15.2  Headroom 耗尽⭐⭐（应用）&quot;">​</a></h3><p>在构建数据包时，如果协议头过多或者预留空间不足，<code>data</code> 指针可能会撞上 <code>head</code> 指针（即 Headroom 耗尽）。</p><p><strong>问题</strong>：当内核检测到 Headroom 不足时（例如 <code>skb_push</code> 时发现 <code>data</code> 即将小于 <code>head</code>），会发生什么？这是一个简单的操作还是昂贵的操作？为什么？</p><details><summary>答案与解析</summary><p><strong>答案</strong>：内核会调用 <code>pskb_expand_head()</code> 函数。 这是一个<strong>极其昂贵（Expensive）<strong>的操作。 因为内核必须分配一块</strong>更大</strong>的新内存块，把原有数据（包括线性数据和分片数据）全部<strong>拷贝</strong>过去，然后释放旧的 SKB。这涉及到内存分配和大量的内存复制操作，会严重降低网络性能。</p></details><h3 id="练习-15-3-skb-与设备生命周期⭐⭐⭐-思考" tabindex="-1">练习 15.3 SKB 与设备生命周期⭐⭐⭐（思考） <a class="header-anchor" href="#练习-15-3-skb-与设备生命周期⭐⭐⭐-思考" aria-label="Permalink to &quot;练习 15.3  SKB 与设备生命周期⭐⭐⭐（思考）&quot;">​</a></h3><p>考虑 Netfilter 的 <code>NF_QUEUE</code> 机制：当内核将一个数据包通过 <code>NFQUEUE</code> 规则发送到用户空间的程序（如 Suricata）进行处理时，内核必须持有这个 SKB，直到用户空间处理完毕并重新注入。</p><p><strong>问题</strong>：</p><ol><li>为什么在这种情况下，内核倾向于<strong>克隆（Clone）</strong> SKB（<code>skb_clone()</code>），而不是直接引用或拷贝？</li><li>克隆出来的 SKB 和原始 SKB 在 <code>users</code> 和 <code>cloned</code> 字段上会有什么变化？</li><li>如果用户空间程序修改了数据包的内容（比如改写了 IP 地址），内核是如何处理这种“写时复制”的需求的？</li></ol><details><summary>答案与解析</summary><p><strong>答案</strong>：</p><ol><li><strong>原因</strong>：直接引用会导致竞争条件（如果驱动还在发送，用户空间同时读取会崩溃）；完全拷贝（<code>skb_copy()</code>）则开销太大（需要复制所有数据和分页）。<strong>克隆</strong>是折中方案：它只复制 <code>sk_buff</code> 结构体本身（元数据），而<strong>共享</strong>同一块数据缓冲区。这样用户空间和内核可以各自拥有独立的元数据指针，但共享数据，大大提高了性能。</li><li><strong>字段变化</strong>： <ul><li>新旧 SKB 的 <code>cloned</code> 标志位都会被置为 <strong>1</strong>。</li><li>原始 SKB 的 <code>users</code> 引用计数会增加（因为有两个结构体指向同一份数据）。</li></ul></li><li><strong>写时复制</strong>：如果用户空间程序尝试修改数据内容（如 <code>skb_store_bits()</code> 或通过 queue 修改包体），内核会检测到数据页被共享。此时，它会触发“写时复制”机制，自动复制受影响的那部分数据页到新的内存位置，从而断开共享关系，确保修改不会影响其他持有者。</li></ol><p><strong>解析</strong>： 这题考察对 SKB 设计核心的深度理解。克隆机制是 Linux 网络栈高性能的关键之一，它体现了 Linux “引用计数 + 共享数据”的典型设计哲学。</p></details><hr><h2 id="要点提炼" tabindex="-1">要点提炼 <a class="header-anchor" href="#要点提炼" aria-label="Permalink to &quot;要点提炼&quot;">​</a></h2><p><code>sk_buff</code> 是 Linux 网络栈中处理数据包的核心数据结构，其设计精髓在于通过<strong>指针操作</strong>（<code>skb_push</code>, <code>skb_pull</code>, <code>skb_reserve</code>）而非数据拷贝来实现协议层间的传递。<code>head</code>, <code>data</code>, <code>tail</code>, <code>end</code> 四个指针界定了一个动态的线性数据区，而 <code>protocol</code> 字段则充当了层间传递的“接力棒”，告诉下一层该把 <code>data</code> 指针视为哪种协议头。</p><p>Headroom（头部预留空间）是 SKB 性能优化的关键。一旦预留的空间不足，内核必须进行昂贵的<strong>数据拷贝</strong>（<code>pskb_expand_head</code>），涉及新内存分配和全量数据迁移，这是高性能网络处理中必须极力避免的陷阱。正确的预留策略（通常在分配时调用 <code>skb_reserve</code>）是驱动开发者的基本功。</p><p><code>net_device</code> 结构体作为网络接口的抽象，其核心是 <strong><code>net_device_ops</code></strong> 函数集，它定义了内核控制硬件的具体行为（如启动、停止、发送）。通过 <strong>硬件特性卸载</strong>（如 <code>NETIF_F_IP_CSUM</code>, <code>TSO</code>, <code>GRO</code>），内核可以将繁重的计算任务（校验、分片、重组）下放到网卡硬件，从而释放 CPU 算力。</p><p><strong>克隆（Clone）<strong>机制是解决多路径共享同一数据包的方案。当只需修改元数据（如 Netfilter 修改路由）时，内核使用 <code>skb_clone()</code> 仅复制 <code>sk_buff</code> 结构体，共享底层数据缓冲区，并设置 <code>cloned</code> 标志和增加 <code>users</code> 引用计数。只有当发生</strong>写操作</strong>时，内核才会触发真正的数据拷贝，即“写时复制”。</p><p>RDMA 技术通过绕过远程 CPU 实现了零拷贝网络传输。它依赖 <strong>Protection Domain (PD)</strong> 划定安全边界，<strong>Memory Region (MR)</strong> 完成内存注册（Pin 住虚拟地址并转换为物理地址供网卡访问），并使用 <strong>Local/Remote Key (LKey/RKey)</strong> 作为访问凭证，确保只有授权的操作才能读写内存，这是高性能与安全性平衡的典范。</p>`,129)])])}const k=e(o,[["render",t]]);export{g as __pageData,k as default};
