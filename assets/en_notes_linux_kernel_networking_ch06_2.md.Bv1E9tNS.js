import{_ as e,o as a,c as i,a as n}from"./chunks/framework.8tiMeCK-.js";const k=JSON.parse('{"title":"6.2  Multicast Forwarding Cache (MFC)","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch06_2.md","filePath":"en/notes/linux_kernel_networking/ch06_2.md","lastUpdated":null}'),t={name:"en/notes/linux_kernel_networking/ch06_2.md"};function l(r,s,h,o,p,c){return a(),i("div",null,[...s[0]||(s[0]=[n(`<h1 id="_6-2-multicast-forwarding-cache-mfc" tabindex="-1">6.2 Multicast Forwarding Cache (MFC) <a class="header-anchor" href="#_6-2-multicast-forwarding-cache-mfc" aria-label="Permalink to &quot;6.2  Multicast Forwarding Cache (MFC)&quot;">​</a></h1><p>In the previous section, we compared <code>mr_table</code> to a dispatch center—holding an interface table and an unresolved queue. But you might have noticed a detail: the actual &quot;forwarding decision&quot; logic hasn&#39;t appeared yet.</p><p>A dispatch center alone isn&#39;t enough; we also need a core decision-making mechanism. In multicast routing, this mechanism is the <strong>MFC (Multicast Forwarding Cache)</strong>.</p><p>This is arguably the most frequently &quot;touched&quot; data structure in the entire multicast subsystem—every incoming multicast packet has to ask it for directions.</p><hr><h2 id="the-cache-array-mfc-cache-array" tabindex="-1">The Cache Array: mfc_cache_array <a class="header-anchor" href="#the-cache-array-mfc-cache-array" aria-label="Permalink to &quot;The Cache Array: mfc_cache_array&quot;">​</a></h2><p>Let&#39;s look at the big picture first. In the kernel&#39;s eyes, the most critical part of a multicast routing table (<code>mr_table</code>) is an array:</p><ul><li><strong><code>mfc_cache_array</code></strong>: This is an array with a length of 64 (i.e., <code>MFC_LINES</code>).</li><li><strong>Contents</strong>: It holds individual <code>mfc_cache</code> objects, which are the actual routing entries.</li></ul><p>Why an array? Because speed matters. The array index is a hash value.</p><blockquote><p><strong>MFC_HASH()</strong> The kernel uses the <code>MFC_HASH</code> macro to calculate the index. It takes only two parameters: the <strong>multicast group address</strong> and the <strong>source IP address</strong>. Feed these two values in, and it calculates which slot in <code>mfc_cache_array</code> this packet should land in.</p></blockquote><p>There&#39;s a slightly confusing point here—the routing table itself.</p><p>By default, when we haven&#39;t touched any advanced policy routing, there is only <strong>one</strong> multicast routing table in the entire IPv4 network namespace (<code>net</code>).</p><ul><li>Its reference in the kernel is <code>net-&gt;ipv4.mrt</code>.</li><li>It is created during <code>ipmr_rules_init()</code>.</li><li>When you call <code>ipmr_fib_lookup()</code> to look up the table, it usually just hands you this table straightforwardly.</li></ul><p>But if you enable <code>IP_MROUTE_MULTIPLE_TABLES</code> (multicast multi-table support), things get complicated: multiple tables will exist, and <code>ipmr_fib_lookup()</code> must rely on policy rules (fib rules) to decide which table to use. However, whether it&#39;s one table or many, the place where the lookup ultimately lands is always <code>mfc_cache_array</code>.</p><hr><h2 id="the-core-structure-mfc-cache" tabindex="-1">The Core Structure: mfc_cache <a class="header-anchor" href="#the-core-structure-mfc-cache" aria-label="Permalink to &quot;The Core Structure: mfc_cache&quot;">​</a></h2><p>Now let&#39;s zoom in and look at the <code>mfc_cache</code> structure itself. It is the smallest unit of a cache entry, defined in <code>include/linux/mroute.h</code>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mfc_cache {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> list_head list;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __be32 mfc_mcastgrp;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     // 组播组地址</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __be32 mfc_origin;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       // 源地址</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    vifi_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mfc_parent;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       // 入接口（数据包从哪个虚拟接口进来的）</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mfc_flags;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           // 标志位</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    union</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // 情况 A：还没解析出来的条目</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> expires;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               // 过期时间</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff_head unresolved;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // 未解析队列（存数据包）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        } unres;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // 情况 B：已经解析出来的条目</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> last_assert;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> minvif;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> maxvif;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> bytes;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // 统计：转发的字节数</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> pkt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // 统计：转发的包数</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> wrong_if;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // 统计：接口错误的次数</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> ttls</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[MAXVIFS];</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // TTL 阈值表</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        } res;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } mfc_un;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rcu_head rcu;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br><span class="line-number">24</span><br><span class="line-number">25</span><br><span class="line-number">26</span><br><span class="line-number">27</span><br><span class="line-number">28</span><br></div></div><p>This structure has an interesting design, hiding two different faces.</p><h3 id="breaking-down-the-key-members" tabindex="-1">Breaking Down the Key Members <a class="header-anchor" href="#breaking-down-the-key-members" aria-label="Permalink to &quot;Breaking Down the Key Members&quot;">​</a></h3><p>First, let&#39;s look at the fields that exist regardless of the state:</p><ul><li><p><strong><code>mfc_mcastgrp</code></strong> &amp; <strong><code>mfc_origin</code></strong>: These are the two values we used for hashing—the <strong>group address</strong> and the <strong>source address</strong>. These two keys uniquely identify a multicast flow.</p></li><li><p><strong><code>mfc_parent</code></strong>: This is the <strong>incoming interface</strong>. Note that multicast routing is source-oriented, so we must know which virtual interface (VIF) the packet originally arrived from to prevent loops and duplicate forwarding.</p></li><li><p><strong><code>mfc_flags</code></strong>: Flags marking special attributes of this entry. There are two common ones:</p><ul><li><strong><code>MFC_STATIC</code></strong>: This is a &quot;static&quot; entry. It means this wasn&#39;t dynamically learned by a routing daemon (like <code>mrouted</code> or <code>pimd</code>), but was manually hardcoded into the kernel by an administrator.</li><li><strong><code>MFC_NOTIFY</code></strong>: This is a notification bit. If set, it means that when this route changes, the kernel needs to notify user space via <code>Netlink</code>. You can check the code in <code>rt_fill_info()</code> and <code>ipmr_get_route()</code>, where this bit is examined.</li></ul></li></ul><h3 id="two-faces-unres-and-res" tabindex="-1">Two Faces: <code>unres</code> and <code>res</code> <a class="header-anchor" href="#two-faces-unres-and-res" aria-label="Permalink to &quot;Two Faces: \`unres\` and \`res\`&quot;">​</a></h3><p>The most critical part of the structure is that <code>union</code>. It acts as the <strong>state switch</strong> for the entire MFC mechanism.</p><p>You can think of it as Schrödinger&#39;s box:</p><ul><li>If the route hasn&#39;t been resolved yet, it is <strong><code>unres</code> (unresolved)</strong>.</li><li>If the route has been determined, it is <strong><code>res</code> (resolved)</strong>.</li></ul><p><strong>State 1: Unresolved (<code>unres</code>)</strong></p><p>When a packet arrives for the first time and the kernel hasn&#39;t found a corresponding forwarding rule in the cache, this entry is in the <code>unres</code> state.</p><ul><li><strong><code>expires</code></strong>: We can&#39;t wait forever; there must be an expiration time.</li><li><strong><code>unresolved</code></strong>: This is a queue (<code>sk_buff_head</code>). The kernel temporarily stuffs those &quot;lost&quot; packets into this queue to wait for the routing daemon to rescue them.</li></ul><p><strong>State 2: Resolved (<code>res</code>)</strong></p><p>Once the routing daemon paves the way, this entry instantly switches to the <code>res</code> state. At this point, it&#39;s filled with data ready for work:</p><ul><li><strong>Statistics</strong>: <code>bytes</code>, <code>pkt</code>, <code>wrong_if</code>. These tell the administrator &quot;how well this route is doing its job.&quot;</li><li><strong><code>ttls[MAXVIFS]</code></strong>: This is a very important small array. It records the <strong>TTL threshold for each virtual interface</strong>. As we&#39;ll see when discussing forwarding later, whether a packet can be sent out from a specific interface depends entirely on comparing the packet&#39;s TTL against the value in this array.</li></ul><hr><h2 id="the-real-world-dilemma-when-the-cache-misses" tabindex="-1">The Real-World Dilemma: When the Cache Misses <a class="header-anchor" href="#the-real-world-dilemma-when-the-cache-misses" aria-label="Permalink to &quot;The Real-World Dilemma: When the Cache Misses&quot;">​</a></h2><p>Theoretically, the perfect flow is: packet arrives -&gt; look up MFC -&gt; cache hit -&gt; forward. But reality is often: packet arrives -&gt; look up MFC -&gt; <strong>cache miss</strong>.</p><p>What should the kernel do then? Simply dropping the packet obviously won&#39;t work, as the connection would never be established. This is where the <code>ipmr_cache_unresolved()</code> function comes in to handle this awkward moment.</p><p>Its logic is highly pragmatic, even a bit &quot;humble&quot;:</p><ol><li><strong>Find a slot</strong>: First, create a new (or find an existing) <strong>unresolved entry</strong> in <code>mfc_cache_array</code>.</li><li><strong>Queue up</strong>: Hang this lost packet on the entry&#39;s <code>unresolved</code> queue.</li><li><strong>Call for backup</strong>: Through the <code>mroute_sk</code> socket, send a message (<code>IGMPMSG_NOCACHE</code>) to the user-space daemon, meaning: &quot;Boss, there&#39;s a packet here that doesn&#39;t know where to go, come take a look.&quot;</li></ol><p>However, kernel memory is limited, so we can&#39;t just queue things up mindlessly.</p><blockquote><p><strong>⚠️ Pitfall Warning: Only 3 Slots Available</strong></p><p>Look at this code in <code>net/ipv4/ipmr.c</code>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ipmr_cache_unresolved</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mr_table </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">mrt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">vifi_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> vifi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">skb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ...</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 检查未解析队列的长度</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (c-&gt;mfc_un.unres.unresolved.qlen </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        kfree_skb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 队列满了，直接丢弃</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        err </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ENOBUFS;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 返回 &quot;No buffer space available&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ...</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>The logic here is strict: if there are already 3 lost packets from the same flow waiting in the queue, the moment a 4th packet arrives, the kernel <strong>drops it immediately</strong> and returns <code>-ENOBUFS</code>.</p><p>Why 3? It&#39;s a protective mechanism. If the daemon crashes or reacts too slowly, the kernel can&#39;t let an unresolved queue grow infinitely and exhaust system memory. So it only gives you a buffer of 3 packets. If you still haven&#39;t resolved the route after 3 packets, stop queuing and drop packets to stay alive.</p></blockquote><p>This is the true face of the MFC: it is both a fast lookup table and a state machine equipped with a &quot;buffer queue&quot; and an &quot;expiration mechanism.&quot;</p><p>At this point, we have dissected the two most core components of the multicast routing table—the <strong>dispatch center (<code>mr_table</code>)</strong> and the <strong>decision cache (<code>mfc_cache</code>)</strong>. But having the tables without anyone driving them isn&#39;t enough. In the next section, we&#39;ll look at the user-space role that pushes all of this forward: the <strong>multicast router</strong>.</p>`,42)])])}const u=e(t,[["render",l]]);export{k as __pageData,u as default};
