import{_ as t,o,c as i,a as n}from"./chunks/framework.8tiMeCK-.js";const p=JSON.parse('{"title":"5.5 Policy Routing: Choices Beyond the Map","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch05_5.md","filePath":"en/notes/linux_kernel_networking/ch05_5.md","lastUpdated":null}'),a={name:"en/notes/linux_kernel_networking/ch05_5.md"};function s(r,e,l,h,d,c){return o(),i("div",null,[...e[0]||(e[0]=[n('<h1 id="_5-5-policy-routing-choices-beyond-the-map" tabindex="-1">5.5 Policy Routing: Choices Beyond the Map <a class="header-anchor" href="#_5-5-policy-routing-choices-beyond-the-map" aria-label="Permalink to &quot;5.5 Policy Routing: Choices Beyond the Map&quot;">​</a></h1><p>In the previous section, we discussed how <code>fib_nh</code> acts like a dutiful guide, holding a sticky note with the outgoing interface and gateway address, directing packets on how to leave the kernel. We built a very intuitive mental model: <strong>the destination determines the route</strong>. As long as you know where you want to go (the destination IP), the routing table can tell you how to get there.</p><p>This mental model works 99% of the time. But as an engineer digging into low-level mechanisms, you might encounter that remaining 1%.</p><p>Imagine this scenario: you have a machine with two network cables plugged in.</p><ul><li>One is <code>eth0</code>, connected to the internal network, used for management, and free of charge.</li><li>The other is <code>eth1</code>, connected to the external network, billed by traffic, and very expensive.</li></ul><p>Now you&#39;re running a program that needs to download data from <code>10.0.0.1</code>. Following the usual routine, the kernel checks the routing table, finds that <code>10.0.0.1</code> is reachable via <code>eth0</code>, and sends all the traffic out that way. But you don&#39;t want that. You have your own logic: <strong>even though the destination address is the same, if this traffic is generated by a system backup, I want it to take the expensive <code>eth1</code> (because it&#39;s fast); if it&#39;s just regular browsing, then I&#39;ll use <code>eth0</code>.</strong></p><p>If we only look at the destination, traditional routing tables are helpless. They only see the target and don&#39;t ask about the origin.</p><p>This is exactly the problem that <strong>Policy Routing</strong> solves. It makes routing decisions no longer solely based on &quot;where am I going,&quot; but also on &quot;who am I,&quot; &quot;what am I doing,&quot; or even &quot;what protocol am I using.&quot; Before introducing this mechanism, let&#39;s first look at how things work in its absence—a world without Policy Routing.</p><h3 id="life-without-policy-routing-two-tables" tabindex="-1">Life Without Policy Routing: Two Tables <a class="header-anchor" href="#life-without-policy-routing-two-tables" aria-label="Permalink to &quot;Life Without Policy Routing: Two Tables&quot;">​</a></h3><p>When the kernel configuration option <code>CONFIG_IP_MULTIPLE_TABLES</code> is not enabled, the kernel&#39;s routing world is very simple: there are only two tables.</p><ol><li><p><strong>Local Table (RT_TABLE_LOCAL, ID 255)</strong> This is the kernel&#39;s &quot;private territory.&quot; It contains only routes for local IP addresses (such as <code>127.0.0.1</code> or the IP you assigned to <code>eth0</code>). This table is highly sensitive—<strong>only the kernel itself can add entries to it</strong>. If an administrator (User) tries to use <code>ip route</code> to stuff entries into the Local table, they will be rejected. This table determines &quot;which addresses belong to me.&quot;</p></li><li><p><strong>Main Table (RT_TABLE_MAIN, ID 254)</strong> This is our &quot;world map.&quot; The vast majority of routes you configure via the <code>ip route add</code> command reside in this table. It determines &quot;if an address isn&#39;t mine, where should I throw it.&quot;</p></li></ol><p>This initialization process happens in the <code>fib4_rules_init()</code> method of <code>net/ipv4/fib_frontend.c</code>.</p><blockquote><p><strong>A Historical Footnote</strong>: In kernels prior to 2.6.25, these two tables were still global variables: <code>ip_fib_local_table</code> and <code>ip_fib_main_table</code>. Back then, the code was full of logic directly accessing these two variables. Later, kernel developers realized this was too inflexible—if you wanted to add a table, you had to modify the code and recompile. So they refactored it, consolidating all table operations into the <code>fib_get_table()</code> method. Regardless of whether you have Policy Routing enabled, or how many tables you have, everyone uses <code>fib_get_table(net, table_id)</code> to get the table pointer.</p></blockquote><p>This &quot;unified access&quot; approach is like turning &quot;dedicated drawers&quot; into a &quot;numbered locker system&quot;—no matter how many lockers there are, the action of using a key to unlock them is exactly the same.</p><h3 id="when-policy-routing-is-enabled-255-maps" tabindex="-1">When Policy Routing is Enabled: 255 Maps <a class="header-anchor" href="#when-policy-routing-is-enabled-255-maps" aria-label="Permalink to &quot;When Policy Routing is Enabled: 255 Maps&quot;">​</a></h3><p>When you enable <code>CONFIG_IP_MULTIPLE_TABLES</code>, the world changes.</p><p>The kernel is no longer limited to the Local and Main tables; it supports up to <strong>255 routing tables</strong>. At boot time, three tables are initialized by default:</p><ul><li><strong>Local</strong> (255)</li><li><strong>Main</strong> (254)</li><li><strong>Default</strong> (253)</li></ul><p>(Note: Regarding the specific use of the Default table and its detailed interaction with the Policy Routing rule set <code>fib_rules</code>, we will dive deep in Chapter 6. For now, let&#39;s focus on the management mechanism of the &quot;tables&quot; themselves.)</p><p>The question now is: <strong>with so many tables, how do we put things into them?</strong></p><h3 id="the-administrator-s-control-interface-netlink-and-ioctl" tabindex="-1">The Administrator&#39;s Control Interface: Netlink and IOCTL <a class="header-anchor" href="#the-administrator-s-control-interface-netlink-and-ioctl" aria-label="Permalink to &quot;The Administrator&#39;s Control Interface: Netlink and IOCTL&quot;">​</a></h3><p>As a kernel engineer, you&#39;re certainly familiar with the <code>ip route</code> command. But do you know what it looks like in the kernel&#39;s eyes? It&#39;s a Netlink message.</p><h4 id="_1-adding-and-deleting-routes-ip-route-add-del" tabindex="-1">1. Adding and Deleting Routes: <code>ip route add/del</code> <a class="header-anchor" href="#_1-adding-and-deleting-routes-ip-route-add-del" aria-label="Permalink to &quot;1. Adding and Deleting Routes: `ip route add/del`&quot;">​</a></h4><p>When you type:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 192.168.1.0/24</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> eth0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Your userspace tool (iproute2) actually sends an <strong>RTM_NEWROUTE</strong> message to the kernel via a Netlink socket.</p><p>The kernel side catches this with the <code>inet_rtm_newroute()</code> method (located in <code>net/ipv4/fib_frontend.c</code>).</p><ul><li>It parses the parameters you brought (destination subnet, outgoing interface, priority, etc.).</li><li>It creates the corresponding <code>fib_info</code> and <code>fib_alias</code>.</li><li>It hangs them in the hash or TRIE structure of the corresponding FIB table (the Main table by default).</li></ul><p>When you type <code>ip route del ...</code>, the flow is similar, except the message type becomes <strong>RTM_DELROUTE</strong>, and the kernel hands it over to <code>inet_rtm_delroute()</code>, which is responsible for removing the corresponding entry from the FIB table.</p><p>Here is a counter-intuitive detail worth pausing to think about: <strong>A route doesn&#39;t always mean &quot;allow passage.&quot;</strong></p><p>You can configure it like this:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> prohibit</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.1.17</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> from</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.2.103</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This command adds a &quot;prohibition order&quot; to the routing table. When the kernel looks up a route and matches this entry, it not only won&#39;t forward the packet, but will drop it directly and reply with an ICMP &quot;Packet Filtered&quot; error message. This is extremely useful in firewall or policy control scenarios—the routing table itself acts as a rule set.</p><p><strong>Viewing Routes: <code>ip route show</code></strong> This corresponds to the <strong>RTM_GETROUTE</strong> message, handled by <code>inet_dump_fib()</code>.</p><ul><li>By default, <code>ip route show</code> only looks at the <strong>Main table</strong>.</li><li>If you want to see the Local table, you must explicitly specify: <code>ip route show table local</code>.</li></ul><h4 id="_2-the-old-school-approach-route-add-del" tabindex="-1">2. The Old-School Approach: <code>route add/del</code> <a class="header-anchor" href="#_2-the-old-school-approach-route-add-del" aria-label="Permalink to &quot;2. The Old-School Approach: `route add/del`&quot;">​</a></h4><p>Although the <code>ip</code> command is the current standard, the <code>route</code> command still exists. Its kernel interface is a completely different path—<strong>IOCTL</strong>.</p><ul><li><code>route add</code> sends the <code>SIOCADDRT</code> IOCTL.</li><li><code>route del</code> sends the <code>SIOCDELRT</code> IOCTL.</li></ul><p>Both IOCTLs are handled by the <code>ip_rt_ioctl()</code> method (also in <code>net/ipv4/fib_frontend.c</code>). This is an interface left over for compatibility with ancient network tools. Although functionally similar to Netlink, inside the kernel, the IOCTL processing path is typically more rigid than Netlink&#39;s.</p><h4 id="_3-dynamic-routing-protocols-bgp-ospf-etc" tabindex="-1">3. Dynamic Routing Protocols: BGP, OSPF, etc. <a class="header-anchor" href="#_3-dynamic-routing-protocols-bgp-ospf-etc" aria-label="Permalink to &quot;3. Dynamic Routing Protocols: BGP, OSPF, etc.&quot;">​</a></h4><p>Besides administrators typing commands by hand, the other main source of routing table data is <strong>routing daemons</strong>. These are heavy-duty software programs (like Quagga, Bird, Zebra) running on backbone routers. They implement complex protocols like BGP and OSPF.</p><p>These processes run in the background, chatting with neighbor routers via protocols. As soon as they detect a change in the network topology (like a fiber optic cable getting cut), they immediately call the Netlink API, flooding the kernel with <code>RTM_NEWROUTE</code> or <code>RTM_DELROUTE</code> messages to instantly update the FIB tables. <strong>To the kernel, it doesn&#39;t care whether these routes were typed in manually by an administrator or calculated by the OSPF protocol—they all ultimately end up as <code>fib_info</code> structures, hanging in the exact same tables.</strong></p><h3 id="exceptions-and-fine-tuning-returning-to-fib-exceptions" tabindex="-1">Exceptions and Fine-Tuning: Returning to FIB Exceptions <a class="header-anchor" href="#exceptions-and-fine-tuning-returning-to-fib-exceptions" aria-label="Permalink to &quot;Exceptions and Fine-Tuning: Returning to FIB Exceptions&quot;">​</a></h3><p>At the beginning of this section, we mentioned that although we are discussing &quot;table&quot;-level management, we must not forget the <strong>FIB nexthop exception</strong> from the previous section.</p><ul><li>If the next hop changed due to an ICMP Redirect.</li><li>Or if the MTU changed due to Path MTU Discovery.</li></ul><p>These changes <strong>will not</strong> touch that massive, shared FIB routing table. They will only modify the small hash table (exception table) attached to the specific <code>fib_nh</code> header. This is an excellent isolation design: <strong>don&#39;t let special cases pollute global rules.</strong> If the path discovered by PMTU were directly modified in the global Main table, then all traffic heading to that subnet might incorrectly apply an unverified MTU value, which would be a disaster. Through the exception mechanism, the kernel only makes fine-tuned adjustments on &quot;the specific flow that actually needs it.&quot;</p><h3 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h3><p>In this section, we pulled our perspective back from the microscopic <code>fib_nh</code> to the macroscopic <strong>FIB Tables</strong> architecture.</p><p>We learned:</p><ol><li><strong>Dual-Table Mode</strong>: Without Policy Routing, the kernel only recognizes the Local and Main tables.</li><li><strong>Unified Access</strong>: Through <code>fib_get_table()</code>, the kernel abstracted table operations, laying the foundation for multi-table support.</li><li><strong>User Interface</strong>: The Netlink messages (like <code>RTM_NEWROUTE</code>) behind the <code>ip route</code> command are how administrators and routing daemons manipulate the FIB.</li><li><strong>Routes as Policy</strong>: Route entries are not just for navigation; they can also be prohibitions like <code>prohibit</code>.</li></ol><p>The FIB architecture is now fairly clear: we have tables, entries, next hops, and a fine-tuning mechanism for next hops. But as we mentioned in the opening &quot;dual-NIC&quot; scenario, having tables alone isn&#39;t enough—we also need a set of <strong>rules</strong> to decide &quot;which table to check and when.&quot;</p><p>That is the main character of the next chapter—<strong>FIB Rules</strong>. That is where the true soul of Policy Routing lies.</p>',52)])])}const g=t(a,[["render",s]]);export{p as __pageData,g as default};
