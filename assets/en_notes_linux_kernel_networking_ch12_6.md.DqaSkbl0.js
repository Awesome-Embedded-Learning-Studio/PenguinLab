import{_ as t,o as s,c as i,a}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"mac80211 Implementation Details","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch12_6.md","filePath":"en/notes/linux_kernel_networking/ch12_6.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_networking/ch12_6.md"};function o(r,e,l,h,d,c){return s(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="mac80211-implementation-details" tabindex="-1">mac80211 Implementation Details <a class="header-anchor" href="#mac80211-implementation-details" aria-label="Permalink to &quot;mac80211 Implementation Details&quot;">​</a></h1><p>Now, let&#39;s shift our focus from the over-the-air protocol interactions back into the kernel. In the previous section, we watched how packets change identities and play hide-and-seek in power-save mode. But you might have had a lingering question: <strong>Who manages all this? What does the code actually look like?</strong></p><p>The Linux wireless world isn&#39;t a free-for-all. It follows a standard set of &quot;building codes&quot; called <code>mac80211</code>. Whether it&#39;s Intel&#39;s iwlwifi or Marvell&#39;s lbtf, anyone wanting to enter the Linux wireless ecosystem must play by <code>mac80211</code>&#39;s rules. In this section, we&#39;re going to pop the hood and look at the pistons and connecting rods inside. If you don&#39;t get your hands dirty with the core bones like <code>ieee80211_hw</code> and <code>sta_info</code>, your understanding of the wireless stack will always feel like looking at flowers through a glass window.</p><hr><h3 id="_12-6-core-skeleton-from-ieee80211-hw-to-driver-callbacks" tabindex="-1">12.6 Core Skeleton: From ieee80211_hw to Driver Callbacks <a class="header-anchor" href="#_12-6-core-skeleton-from-ieee80211-hw-to-driver-callbacks" aria-label="Permalink to &quot;12.6  Core Skeleton: From ieee80211_hw to Driver Callbacks&quot;">​</a></h3><p>The <code>mac80211</code> API is mind-bogglingly complex, packed with dizzying details. I&#39;m not going to dump tens of megabytes of code here (that would be pointless anyway), but I will give you the most precise treasure map so you know where the entrances are and where the traps lie.</p><p>Everything starts with a structure called <code>struct ieee80211_hw</code>. You can think of it as the <strong>&quot;hardware device&#39;s ID card.&quot;</strong></p><p>The kernel uses this structure to tell the wireless stack, &quot;Hey, here&#39;s what this NIC looks like and what tricks it can do.&quot; But there&#39;s a key design detail here: <code>ieee80211_hw</code> contains a <code>priv</code> pointer. This isn&#39;t just any pointer—it&#39;s the driver developer&#39;s private stash. This <code>priv</code> pointer is defined as <code>void *</code>, meaning the kernel doesn&#39;t care about or inspect what&#39;s stored inside. Only the driver knows; it typically points to a larger, driver-defined private structure—for example, Intel&#39;s driver calls it <code>iwl_priv</code>, while Marvell&#39;s calls it <code>lbtf_private</code>. This design cleanly separates the &quot;generic framework&quot; from the &quot;private implementation.&quot;</p><p>To get to work, the driver needs to do a few things first:</p><ol><li><strong>Allocate space</strong>: Call <code>ieee80211_alloc_hw(size_t priv_data_len, const struct ieee80211_ops *ops)</code>. This step doesn&#39;t just allocate the <code>ieee80211_hw</code>; it also allocates the amount of private space you requested via <code>priv_data_len</code> and binds the two together.</li><li><strong>Register the device</strong>: Take the allocated <code>hw</code> and call <code>ieee80211_register_hw(struct ieee80211_hw *hw)</code>. This step is like registering a household—it officially exists in the kernel from this point on.</li><li><strong>Receive data</strong>: When the hardware receives a packet, the driver typically calls <code>ieee80211_rx_irqsafe()</code> (implemented in <code>net/mac80211/rx.c</code>). Why is it called <code>irqsafe</code>? Because it&#39;s called in interrupt context, where sleeping is not allowed.</li></ol><p>That architecture diagram you saw in the docs (Figure 12-5) actually depicts this exact relationship: Between the driver layer and the <code>mac80211</code> layer, the handshake is connected entirely through the <code>ieee80211_ops</code> set of callback functions.</p><hr><h3 id="_12-6-1-driver-operations" tabindex="-1">12.6.1 Driver Operations <a class="header-anchor" href="#_12-6-1-driver-operations" aria-label="Permalink to &quot;12.6.1  Driver Operations&quot;">​</a></h3><p>The <code>ieee80211_ops</code> mentioned earlier is essentially a &quot;pledge&quot; written by the driver for the kernel. It&#39;s also a structure filled with function pointers. You don&#39;t have to fill in every single one, but a few are absolutely critical.</p><p>Let&#39;s look at a few of the most core callbacks:</p><ul><li><strong><code>tx()</code></strong>: This is the payroll department. Every time the kernel needs to send a packet, it calls this function. Under normal circumstances, it must return <code>NETDEV_TX_OK</code>, unless something goes wrong.</li><li><strong><code>start()</code></strong> and <strong><code>stop()</code></strong>: This pair is responsible for power control. <code>start()</code> activates the hardware and turns on the frame reception switch; <code>stop()</code> shuts it down, typically cutting power to the hardware completely.</li><li><strong><code>add_interface()</code></strong> and <strong><code>remove_interface()</code></strong>: These sound abstract, but they&#39;re actually quite simple: when you <code>ifconfig wlan0 up</code>, this callback gets triggered. The kernel is asking the driver, &quot;Hey, I&#39;m about to bind a virtual network device—is your hardware ready to accept traffic?&quot; <code>remove_interface</code> is the reverse operation.</li><li><strong><code>config()</code></strong>: This is the tuning knob. For example, if you want to switch channels (from CH6 to CH11), the kernel calls this function to notify the hardware to change frequencies.</li><li><strong><code>configure_filter()</code></strong>: This is the bouncer configuration. It tells the hardware, &quot;I only want these specific types of packets; don&#39;t bother me with the rest.&quot;</li></ul><hr><h3 id="_12-6-2-station-management-the-sta-info-structure" tabindex="-1">12.6.2 Station Management: The sta_info Structure <a class="header-anchor" href="#_12-6-2-station-management-the-sta-info-structure" aria-label="Permalink to &quot;12.6.2  Station Management: The sta_info Structure&quot;">​</a></h3><p>Besides the hardware itself, what&#39;s the most important thing in the wireless world? It&#39;s the <strong>&quot;people&quot;</strong> (Stations). Whether it&#39;s a connected phone or the router next door, they are all represented in the kernel as a <code>struct sta_info</code> (defined in <code>net/mac80211/sta_info.h</code>).</p><p>This structure is incredibly rich in content—practically a black box for a station:</p><ul><li><strong>Statistical counters</strong>: How many packets were received, how many were sent, how many were dropped.</li><li><strong>Flags</strong>: Is it in power-save mode? Is it authorized?</li><li><strong>ps_tx_buf</strong>: Remember the power-save buffer we talked about in the previous section? This is that array, used to store unicast packets for sleeping stations.</li><li><strong>debugfs entries</strong>: Allowing us to peek at its state from user space.</li></ul><p>How does the kernel manage hundreds or thousands of such stations? Using a hash table (<code>sta_hash</code>) and linked lists (<code>sta_list</code>). When you need to find a station, you typically use these three tools:</p><ul><li><code>sta_info_insert(struct sta_info *sta)</code>: Add a new station to the table.</li><li><code>sta_info_destroy_addr(...)</code>: Kick someone out. Remove a station by its MAC address (internally, this calls <code>__sta_info_destroy</code>).</li><li><code>sta_info_get(...)</code>: Look up a record. Given a MAC address (usually the BSSID), fish out the <code>sta_info</code> structure for that station.</li></ul><hr><h3 id="_12-6-3-rx-path-when-a-packet-flies-in" tabindex="-1">12.6.3 RX Path: When a Packet Flies In <a class="header-anchor" href="#_12-6-3-rx-path-when-a-packet-flies-in" aria-label="Permalink to &quot;12.6.3  RX Path: When a Packet Flies In&quot;">​</a></h3><p>The receive path is the busiest place in the entire stack. The main character is the <code>ieee80211_rx()</code> function (in <code>net/mac80211/rx.c</code>).</p><p>When the driver passes an SKB up, it doesn&#39;t come empty-handed. It slips a little note into the SKB&#39;s control buffer called <code>ieee80211_rx_status</code>. You can use the <code>IEEE80211_SKB_RXCB()</code> macro to pull out this note and read it. What&#39;s written on it? It describes the reception quality of the packet: Did the FCS check fail? What&#39;s the signal strength? If the flag field carries <code>RX_FLAG_FAILED_FCS_CRC</code>, congratulations—this packet is trash.</p><p>Next comes a series of pipeline operations:</p><ol><li><p><strong>Monitor mode handling</strong>: <code>ieee80211_rx_monitor()</code> takes the stage. If you have Monitor Mode enabled (for packet capturing), it will first strip off the FCS (checksum) for you and handle any potentially present <code>radiotap</code> header. <em>Note: Not all NICs support Monitor Mode.</em></p></li><li><p><strong>802.11n reordering</strong>: If you&#39;re using HT (802.11n), out-of-order packets need to be queued up. At this point, <code>ieee80211_rx_reorder_ampdu()</code> gets called in to sort the out-of-order building blocks.</p></li><li><p><strong>The actual processing</strong>: <code>__ieee80211_rx_handle_packet()</code> ultimately calls <code>ieee80211_invoke_rx_handlers()</code>.</p></li></ol><p>Here is a very elegant design pattern known as the <strong>&quot;Chain of Responsibility.&quot;</strong> The kernel has a chain of processors, and each one takes a look at the packet:</p><ul><li>If it says &quot;I don&#39;t handle this, next,&quot; it returns <code>RX_CONTINUE</code>.</li><li>If it says &quot;I&#39;m taking this packet,&quot; it returns <code>RX_QUEUED</code>.</li><li>If it says &quot;This packet is garbage,&quot; it returns <code>RX_DROP_MONITOR</code> or <code>RX_DROP_UNUSABLE</code>.</li></ul><p>Let&#39;s look at a piece of real code (<code>net/mac80211/rx.c</code>):</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ieee80211_rx_result</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ieee80211_rx_h_mgmt_check</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ieee80211_rx_data </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">rx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ieee80211_mgmt </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mgmt </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ieee80211_mgmt </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) rx-&gt;skb-&gt;data;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ieee80211_rx_status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> IEEE80211_SKB_RXCB</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(rx-&gt;skb);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        . . .</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (rx-&gt;skb-&gt;len </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 24</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> RX_DROP_MONITOR;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ieee80211_is_mgmt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(mgmt-&gt;frame_control))</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> RX_DROP_MONITOR;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               .  .  .</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br></div></div><p>What is this code doing? It&#39;s checking: if this packet claims to be a management frame but isn&#39;t even 24 bytes long (the minimum 802.11 header length), or if it&#39;s a fake management frame, it immediately returns <code>RX_DROP_MONITOR</code>—tossed into the trash. If it&#39;s a PS-Poll packet but the receiver isn&#39;t acting as an AP, that&#39;s also <code>RX_DROP_UNUSABLE</code>. This layered filtering design ensures the kernel&#39;s CPU won&#39;t be maxed out by malformed packets.</p><hr><h3 id="_12-6-4-tx-path-when-a-packet-flies-out" tabindex="-1">12.6.4 TX Path: When a Packet Flies Out <a class="header-anchor" href="#_12-6-4-tx-path-when-a-packet-flies-out" aria-label="Permalink to &quot;12.6.4  TX Path: When a Packet Flies Out&quot;">​</a></h3><p>The main character of the transmit path is <code>ieee80211_tx()</code> (in <code>net/mac80211/tx.c</code>). Its logic is very similar to RX: prepare first, then go through the pipeline.</p><ol><li><p><strong>Preparation</strong>: <code>__ieee80211_tx_prepare()</code>. Various pre-checks happen here. For example, if you&#39;re trying to send a packet smaller than 10 bytes, the kernel will think you&#39;re joking and immediately <code>dev_kfree_skb(skb)</code> drop it.</p></li><li><p><strong>Transmit processor chain</strong>: <code>invoke_tx_handlers()</code>. Just like RX, this is a row of processing functions strung together using the <code>CALL_TXH</code> macro.</p><ul><li><code>TX_CONTINUE</code>: Not my problem, pass to the next.</li><li><code>TX_QUEUED</code>: I&#39;ve taken over this packet (e.g., it entered a buffer).</li><li><code>TX_DROP</code>: This packet has issues, do not transmit.</li></ul></li></ol><p>If it gets a green light all the way, it finally calls <code>__ieee80211_tx()</code> to push the packet down to the hardware driver&#39;s <code>tx()</code> callback.</p><hr><h3 id="_12-6-5-fragmentation-fitting-an-elephant-into-a-fridge" tabindex="-1">12.6.5 Fragmentation: Fitting an Elephant into a Fridge <a class="header-anchor" href="#_12-6-5-fragmentation-fitting-an-elephant-into-a-fridge" aria-label="Permalink to &quot;12.6.5  Fragmentation: Fitting an Elephant into a Fridge&quot;">​</a></h3><p>Wireless is fast nowadays, but in the past, to ensure reliability, even elephants had to be chopped up. This is <strong>fragmentation</strong>.</p><p>The 802.11 standard dictates that every station has a fragmentation threshold. If a unicast packet exceeds this threshold, it must be sliced into smaller pieces. Each slice requires a separate acknowledgment (ACK). You can view this threshold from the command line using <code>iwconfig</code>, or change it:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iwconfig</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wlan0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> frag</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 512</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This sets the threshold to 512 bytes. The smaller the threshold, the lower the collision probability, but the higher the overhead. On the transmit side, the knife that does the chopping is the <code>ieee80211_tx_h_fragment()</code> method. On the receive side, the magic tool that reassembles the meat is the <code>ieee80211_rx_h_defragment()</code> method.</p><p><strong>But there&#39;s a massive gotcha here</strong>: Fragmentation and A-MPDU aggregation (an 802.11n technique used to boost speed) are fundamentally incompatible. Since modern 802.11n/ac speeds are so fast and airtime is so short, fragmentation is rarely seen anymore. If you enable fragmentation on a modern high-rate network, you might find that the network actually slows down, or throughput plummets because aggregation is disabled.</p><hr><h3 id="_12-6-6-debugfs-the-kernel-s-black-box" tabindex="-1">12.6.6 debugfs: The Kernel&#39;s Black Box <a class="header-anchor" href="#_12-6-6-debugfs-the-kernel-s-black-box" aria-label="Permalink to &quot;12.6.6  debugfs: The Kernel&#39;s Black Box&quot;">​</a></h3><p>The kernel doesn&#39;t want you left guessing what it&#39;s thinking, so it left a backdoor called <code>debugfs</code>. This is a virtual filesystem dedicated to spitting out debug information.</p><p>First, you need to mount it:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mount</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> debugfs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> none_debugs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /sys/kernel/debug</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>⚠️ <strong>Warning</strong> Your kernel must have been compiled with <code>CONFIG_DEBUG_FS</code> enabled, otherwise this step will throw an error and you won&#39;t be able to see any of the content below.</p><p>Assuming your physical device is <code>phy0</code>, you can go digging for treasure under <code>/sys/kernel/debug/ieee80211/phy0</code>.</p><p>Here are a few particularly useful entries:</p><ol><li><p><strong><code>total_ps_buffered</code></strong>: This is the AP&#39;s &quot;storage locker.&quot; It shows how many packets the AP has buffered for sleeping stations (PS mode), including both unicast and multicast. This number is the sum added up by <code>ieee80211_tx_h_unicast_ps_buf</code> and <code>ieee80211_tx_h_multicast_ps_buf</code>.</p></li><li><p><strong><code>statistics</code> directory</strong>: This is full of battle stats.</p><ul><li><code>frame_duplicate_count</code>: How many duplicate packets were received (someone is retransmitting).</li><li><code>transmitted_frame_count</code>: How many packets were successfully transmitted.</li><li><code>retry_count</code>: How many retries occurred. If this number spikes, your signal quality is poor or there&#39;s heavy interference.</li><li><code>fragmentation_threshold</code>: The current fragmentation threshold.</li></ul></li><li><p><strong><code>netdev:wlan0</code> directory</strong> (assuming your interface is called wlan0): Here you can see specific interface information.</p><ul><li><code>aid</code>: Association ID.</li><li><code>assoc_tries</code>: How many attempts it took to connect.</li><li><code>bssid</code>: The address of the router you&#39;re connected to.</li></ul></li><li><p><strong><code>rc/name</code></strong>: This shows the name of the rate control algorithm currently in use. Choosing the right algorithm is more important than choosing the right router.</p></li></ol><hr><h3 id="_12-6-7-wireless-modes-the-nic-s-id-card" tabindex="-1">12.6.7 Wireless Modes: The NIC&#39;s ID Card <a class="header-anchor" href="#_12-6-7-wireless-modes-the-nic-s-id-card" aria-label="Permalink to &quot;12.6.7  Wireless Modes: The NIC&#39;s ID Card&quot;">​</a></h3><p>Finally, a NIC doesn&#39;t have just one form. Depending on what you want to do, you can switch it into different modes. This depends on whether the hardware supports it (you can check the <code>wiphy</code> field in <code>interface_modes</code>, or look up your driver list on linuxwireless.org).</p><ul><li><strong>AP mode (<code>NL80211_IFTYPE_AP</code>)</strong>: The NIC becomes a router. It maintains a station list, and the network name (SSID) is defined by it.</li><li><strong>Managed mode (<code>NL80211_IFTYPE_STATION</code>)</strong>: The NIC becomes a client. This is the mode your phone uses to connect to WiFi.</li><li><strong>Monitor mode (<code>NL80211_IFTYPE_MONITOR</code>)</strong>: Sniffer mode. All packets passing through the air, whether addressed to it or not, are captured indiscriminately. This is the dependency for packet capture tools like airodump-ng. Transmitting in this mode is called <strong>injection</strong>, and the data packets are tagged with a special <code>IEEE80211_TX_CTL_INJECTED</code> mark.</li><li><strong>Ad Hoc mode (<code>NL80211_IFTYPE_ADHOC</code>)</strong>: Point-to-point, no AP, everyone is equal.</li><li><strong>WDS mode (<code>NL80211_IFTYPE_WDS</code>)</strong>: Wireless Distribution System, used for bridging.</li><li><strong>Mesh mode (<code>NL80211_IFTYPE_MESH_POINT</code>)</strong>: Mesh networking, which we&#39;ll cover in detail in the next section.</li></ul><hr><p>At this point, we&#39;ve finished looking at the skeleton and muscles of <code>mac80211</code>. But in today&#39;s wireless world, it&#39;s not just about connectivity—it&#39;s about speed. In the next section, we&#39;ll enter the <strong>802.11n expressway</strong> and see how multiple antennas (MIMO) and aggregation technologies turn wireless networks into fiber optics. The logic in that world is an entirely different beast.</p>`,61)])])}const g=t(n,[["render",o]]);export{u as __pageData,g as default};
