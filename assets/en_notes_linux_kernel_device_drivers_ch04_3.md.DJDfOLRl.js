import{_ as t,o as r,c as n,a as i}from"./chunks/framework.8tiMeCK-.js";const p=JSON.parse('{"title":"4.3 Exercises and Reflections","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_device_drivers/ch04_3.md","filePath":"en/notes/linux_kernel_device_drivers/ch04_3.md","lastUpdated":null}'),o={name:"en/notes/linux_kernel_device_drivers/ch04_3.md"};function a(s,e,d,l,c,h){return r(),n("div",null,[...e[0]||(e[0]=[i('<h1 id="_4-3-exercises-and-reflections" tabindex="-1">4.3 Exercises and Reflections <a class="header-anchor" href="#_4-3-exercises-and-reflections" aria-label="Permalink to &quot;4.3 Exercises and Reflections&quot;">​</a></h1><p>If you&#39;ve made it this far, the mechanisms should be clear—or so you might think. This section is where we put theory to the real test. The exercises below increase in difficulty. We recommend thinking through them independently before checking the hints, and only referring to the hints if you get stuck.</p><p>If some of these exercises feel a bit &quot;sadistic,&quot; don&#39;t worry—that&#39;s intentional. Driver development is a hands-on craft; reading API documentation alone will never help you write your first working driver.</p><hr><h3 id="exercise-4-1-observing-timer-interrupts" tabindex="-1">Exercise 4.1: Observing Timer Interrupts <a class="header-anchor" href="#exercise-4-1-observing-timer-interrupts" aria-label="Permalink to &quot;Exercise 4.1: Observing Timer Interrupts&quot;">​</a></h3><p><strong>Task</strong>: On an x86 system (a virtual machine is fine), verify the following phenomenon: although the count for the traditional timer interrupt (IRQ 0) remains unchanged, there is actually another periodic system interrupt continuously incrementing to maintain the time base for each CPU core.</p><p><strong>Hint</strong>: Recall the <code>proc</code> pseudo-file we looked at in the previous section. What you&#39;re looking for is right there.</p><hr><h3 id="exercise-4-2-writing-a-keylogger-driver-advanced-hands-on" tabindex="-1">Exercise 4.2: Writing a Keylogger Driver (Advanced Hands-on) <a class="header-anchor" href="#exercise-4-2-writing-a-keylogger-driver-advanced-hands-on" aria-label="Permalink to &quot;Exercise 4.2: Writing a Keylogger Driver (Advanced Hands-on)&quot;">​</a></h3><p><strong>⚠️ Warning: Ethical Hacking Only</strong> This exercise is strictly limited to security learning on your own systems. <strong>Recording keystrokes on unauthorized systems is illegal</strong>. Additionally, this exercise relies on x86-specific hardware (the i8042 controller) and typically does not work properly in virtual machines.</p><p><strong>Task</strong>: Write a simple kernel keylogger driver using the kernel&#39;s <code>misc</code> framework. By hooking into the i8042&#39;s IRQ 1 (keyboard interrupt), &quot;capture&quot; keyboard press and release events and read the key scancodes.</p><p><strong>Implementation Steps</strong>:</p><ol><li><strong>Data Buffering</strong>: Use the <code>kfifo</code> data structure in kernel space to temporarily store these scancodes.</li><li><strong>User Space Interaction</strong>: Write a user-space process (or thread) that periodically reads data from your driver&#39;s <code>kfifo</code> into a user-space buffer and writes it to a log file.</li><li><strong>Decoding</strong>: Write an application (or use another thread) to interpret these scancodes and translate them into readable characters.</li></ol><p><strong>Practical Tips</strong>:</p><ul><li><strong>Architecture Constraints</strong>: How do we ensure this driver only compiles on x86? Add <code>#ifdef CONFIG_X86</code> at the very beginning of the code.</li><li><strong>Virtual Machine Detection</strong>: How do we ensure it only runs on physical machines? We can use the <code>virt-what</code> tool in the driver loading script and refuse to execute <code>insmod</code> if it&#39;s not a physical machine.</li><li><strong>A Better Approach</strong>: Frankly, using a driver to write a keylogger is both difficult and unnecessary (unless you&#39;re writing a virus or rootkit, which is a different story entirely). Usually, querying the kernel&#39;s input subsystem event layer from the application layer via <code>evtest(1)</code> is much simpler and safer. The sole purpose of this exercise is to give you hands-on experience handling hardware interrupts and kernel buffers.</li></ul><p><strong>Reference Materials</strong>:</p><ul><li>Kernel kfifo example: <code>samples/kfifo/bytestream-example.c</code></li><li>US keyboard scancode mapping table: Philip Storr&#39;s PC book or the OSDev Wiki.</li></ul><hr><h3 id="exercise-4-3-fill-in-the-blank-—-deferral-mechanisms" tabindex="-1">Exercise 4.3: Fill in the Blank — Deferral Mechanisms <a class="header-anchor" href="#exercise-4-3-fill-in-the-blank-—-deferral-mechanisms" aria-label="Permalink to &quot;Exercise 4.3: Fill in the Blank — Deferral Mechanisms&quot;">​</a></h3><p>The kernel provides a mechanism known as &quot;deferral functionality,&quot; commonly referred to as <strong><code>__________</code></strong>. They are designed to achieve the best of both worlds:</p><ol><li><strong><code>__________________</code></strong></li><li><strong><code>__________________</code></strong></li></ol><p>A. Top Half; run the hardirq as fast as possible; immediately restore the interrupted context afterward. B. Bottom Half; allow driver authors to perform longer interrupt processing when needed, in a deferred and safe manner, while keeping the system&#39;s normal work running. C. Better Half; do more work in interrupt context to avoid paying the cost later. D. Bottom Half; run interrupt code with interrupts disabled and let it run for a long time.</p><hr><h3 id="exercise-4-4-finding-tasklets" tabindex="-1">Exercise 4.4: Finding Tasklets <a class="header-anchor" href="#exercise-4-4-finding-tasklets" aria-label="Permalink to &quot;Exercise 4.4: Finding Tasklets&quot;">​</a></h3><p><strong>Task</strong>: Using a code browsing tool (we recommend <code>cscope(1)</code> or <code>ctags</code>), find drivers in the kernel source tree that use the <code>tasklet_hi_schedule()</code> API.</p><p><strong>Reflection</strong>: Why do these drivers use high-priority tasklets? What special types of tasks are they handling?</p><hr><h3 id="exercise-4-5-ftrace-latency-measurement" tabindex="-1">Exercise 4.5: Ftrace Latency Measurement <a class="header-anchor" href="#exercise-4-5-ftrace-latency-measurement" aria-label="Permalink to &quot;Exercise 4.5: Ftrace Latency Measurement&quot;">​</a></h3><p><strong>Task</strong>: Use Ftrace&#39;s <code>irqsoff</code> latency tracer plugin to find the longest interrupt disabled time in the system.</p><p><strong>Step-by-Step Guide</strong>:</p><ol><li><strong>Kernel Configuration</strong>: You need to enable <code>CONFIG_IRQSOFF_TRACER</code>. If it&#39;s not enabled by default, look for it under <code>make menuconfig</code> in <code>Kernel hacking -&gt; Tracers</code>.</li><li><strong>Compile and Reboot</strong>: Don&#39;t forget to <code>make</code> and reboot.</li><li><strong>Lockdep</strong>: When performing this kind of measurement, we recommend disabling Lockdep to prevent its own overhead from skewing the results.</li><li><strong>Analysis</strong>: Observe the output in <code>/sys/kernel/debug/tracing/trace</code>.</li></ol><p><strong>Reference Material</strong>: Steven Rostedt&#39;s paper <em>Finding Origins of Latencies Using Ftrace</em>.</p><hr><h2 id="chapter-recap-and-summary" tabindex="-1">Chapter Recap and Summary <a class="header-anchor" href="#chapter-recap-and-summary" aria-label="Permalink to &quot;Chapter Recap and Summary&quot;">​</a></h2><p>We covered a lot of ground in this chapter.</p><p>Starting from the original hardware signal—that single wire pulling high a CPU pin—we saw how the kernel wraps it in the complex abstraction layer of the <code>irq_desc</code> array to hand you a clean <code>irqreturn_t</code> function. We discussed why doing all the work directly in a hardirq is suicidal behavior (interrupt starvation), which led us to the philosophy of Top Halves / Bottom Halves.</p><p>We learned how to use modern APIs like <code>devm_request_threaded_irq</code> to let the kernel automatically clean up the battlefield for us, and we saw how hybrid mechanisms like NAPI can save the day during network floods.</p><p>Remember the question at the beginning of this chapter about &quot;why does the device not respond even though driver registration succeeded?&quot; You should be able to answer it now: successful registration only means the kernel has claimed the device. But if the IRQ line isn&#39;t properly bound, or if the interrupt handler returns <code>IRQ_NONE</code>, the device&#39;s cries go unheard. The interrupt system is a second heartbeat alongside the kernel&#39;s own—it must be timely and deterministic, yet not so domineering that it drags the entire system down.</p><p>This is really an art of &quot;compromise.&quot; Between speed and responsiveness, between hardware reality and software ideals, the interrupt subsystem finds a delicate balancing path.</p><p>In the next chapter, we&#39;ll turn our attention to another common asynchronous mechanism in the kernel: workqueues. Unlike softirqs and tasklets, they run in process context. What does that mean? It means they can finally &quot;sleep&quot; in peace.</p><hr><h2 id="further-reading-and-resources" tabindex="-1">Further Reading and Resources <a class="header-anchor" href="#further-reading-and-resources" aria-label="Permalink to &quot;Further Reading and Resources&quot;">​</a></h2><p>If this chapter didn&#39;t melt your brain enough, or if you want to see some debates about the future, the links below are worth bookmarking.</p><h3 id="core-documentation" tabindex="-1">Core Documentation <a class="header-anchor" href="#core-documentation" aria-label="Permalink to &quot;Core Documentation&quot;">​</a></h3><ul><li><strong>Generic IRQ handling</strong>: Official kernel documentation, a must-read. <a href="https://www.kernel.org/doc/html/latest/core-api/genericirq.html" target="_blank" rel="noreferrer">Linux generic IRQ handling</a></li><li><strong>LWN Interrupt Index</strong>: LWN&#39;s interrupt topic index, covering over a decade of in-depth technical discussions. <a href="https://lwn.net/Kernel/Index/#Interrupts" target="_blank" rel="noreferrer">LWN Interrupt Index</a></li></ul><h3 id="in-depth-topics" tabindex="-1">In-Depth Topics <a class="header-anchor" href="#in-depth-topics" aria-label="Permalink to &quot;In-Depth Topics&quot;">​</a></h3><p><strong>Interrupt Trigger Modes</strong>:</p><ul><li><em>Edge Triggered versus Level Triggered interrupts</em> (2013)</li><li><em>Level-triggered versus Edge-triggered Interrupts</em> (2008)</li></ul><p><strong>NAPI and Softirq Frontiers</strong>:</p><ul><li><em>Threadable NAPI polling, softirqs, and proper fixes</em> (Jon Corbet, 2016): Discusses the combination of NAPI and threading.</li><li><em>Per-vector software-interrupt masking</em> (Jon Corbet, 2019): On the future direction of fine-grained softirq vector masking.</li></ul><p><strong>IRQ Affinity and Performance</strong>:</p><ul><li><em>IRQ Balancing</em> (ntop project)</li><li><em>Setting interrupt affinity systems</em> (RHEL8 docs)</li></ul><h3 id="debugging-and-analysis-tools" tabindex="-1">Debugging and Analysis Tools <a class="header-anchor" href="#debugging-and-analysis-tools" aria-label="Permalink to &quot;Debugging and Analysis Tools&quot;">​</a></h3><p><strong>Modern eBPF Toolchain</strong>:</p><ul><li><em>Linux bcc/eBPF tracing tools</em> (Brendan Gregg): A modern toolset for performance analysis using eBPF.</li></ul><p><strong>In-Depth Ftrace Guides</strong>:</p><ul><li><em>ftrace – Function Tracer</em> (Kernel Doc)</li><li><em>Debugging the kernel using ftrace</em> (Steven Rostedt, LWN 2009)</li><li><em>Secrets of the ftrace function tracer</em> (Steven Rostedt, LWN 2010)</li><li><em>trace-cmd: a frontend for ftrace</em> (Steven Rostedt, LWN 2010)</li></ul><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: understanding&quot;">​</a></h3><p><strong>Question</strong>: In Linux driver development, what is the fundamental difference in resource management between the traditional <code>request_irq()</code> and the modern recommended <code>devm_request_threaded_irq()</code>? Please explain in the context of &quot;managed resources.&quot;</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: <code>request_irq()</code> is the traditional registration method, requiring the driver author to explicitly call <code>free_irq()</code> when unloading the module or removing the device to free interrupt resources; otherwise, it leads to resource leaks. <code>devm_request_threaded_irq()</code>, on the other hand, is the &quot;managed&quot; version. It leverages the driver core&#39;s managed resource mechanism to automatically release the allocated IRQ upon device detach, eliminating the need for manual cleanup code, thereby preventing resource leaks and simplifying the code.</p><p><strong>Analysis</strong>: This question tests your understanding of kernel API evolution. Modern Linux kernel driver development introduced the concept of &#39;managed resources&#39; (devm_* interfaces) to reduce human error by automatically managing resource lifecycles. Understanding this helps in writing more robust and concise driver code.</p></details><h3 id="exercise-2-application" tabindex="-1">Exercise 2: application <a class="header-anchor" href="#exercise-2-application" aria-label="Permalink to &quot;Exercise 2: application&quot;">​</a></h3><p><strong>Question</strong>: Suppose you are writing a driver for a high-speed network card that supports interrupt sharing. When calling <code>request_irq()</code>, how should you set the <code>flags</code> parameter to ensure system stability and comply with shared interrupt conventions, and what must you do before returning from the interrupt handler?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: When calling <code>request_irq()</code>, you must include the <code>IRQF_SHARED</code> flag via a bitwise OR in the <code>flags</code> parameter. Additionally, as a shared interrupt handler, you must check the hardware status registers inside the function to determine whether the interrupt was actually triggered by your device; if not, you must return <code>IRQ_NONE</code> and never misidentify it.</p><p><strong>Analysis</strong>: This is an application question testing practical coding standards. 1. Shared interrupts must declare <code>IRQF_SHARED</code>, otherwise registration will fail. 2. A shared IRQ line means multiple devices share the same physical signal. When the CPU responds to an interrupt, the kernel iterates through all registered handlers on that line. If a handler cannot correctly identify non-device interrupts and return <code>IRQ_NONE</code>, it will cause an &quot;interrupt storm&quot; or erroneous device operations.</p></details><h3 id="exercise-3-understanding" tabindex="-1">Exercise 3: understanding <a class="header-anchor" href="#exercise-3-understanding" aria-label="Permalink to &quot;Exercise 3: understanding&quot;">​</a></h3><p><strong>Question</strong>: In a network device driver, an interrupt is triggered when a data packet is received. Considering that the interrupt handler runs in &quot;atomic context,&quot; if you need to allocate a 1KB buffer in the driver to store the packet data at this point, should you use <code>kmalloc()</code> or <code>kmalloc(GFP_ATOMIC)</code>? Why?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: You should use <code>kmalloc(GFP_ATOMIC)</code> (or a similar variant with the <code>__GFP_ATOMIC</code> flag).</p><p><strong>Analysis</strong>: This question tests your understanding of &quot;interrupt context&quot; constraints. The standard <code>kmalloc()</code> uses the <code>GFP_KERNEL</code> flag by default, which allows the memory allocator to put the current process to sleep to wait for page reclaim when memory is low. However, interrupt handlers run outside of non-atomic process context (hardirq context) and cannot sleep. Using the <code>GFP_ATOMIC</code> flag tells the allocator to attempt an immediate allocation; if it fails, it must immediately return NULL and must never let the calling path enter a sleeping state.</p></details><h3 id="exercise-4-thinking" tabindex="-1">Exercise 4: thinking <a class="header-anchor" href="#exercise-4-thinking" aria-label="Permalink to &quot;Exercise 4: thinking&quot;">​</a></h3><p><strong>Question</strong>: Suppose the interrupt handler you wrote is scheduled by the system as a &quot;threaded interrupt.&quot; If you execute an operation that triggers a sleep (such as calling <code>msleep()</code> or waiting on a mutex) within the handler thread, is this allowed? Please explain the mechanism in the context of the <code>IRQF_ONESHOT</code> flag.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Yes, it is allowed. The core purpose of threaded interrupts is to move heavy or blocking operations from hardirqs into a kernel thread. Kernel threads have their own process context, so they can safely sleep. The role of the <code>IRQF_ONESHOT</code> flag is to ensure that the hardware IRQ line remains masked until the threaded handler completes, preventing the interrupt from triggering infinitely again before processing is finished and the hardware interrupt source is cleared, which would cause an interrupt storm.</p><p><strong>Analysis</strong>: This question involves deep thinking about the modern interrupt handling model (Threaded IRQ). Traditional hardirqs require fast, non-blocking execution. After Linux introduced the threaded irq model, handling is split into two parts: the Primary handler (hardirq part, which only does minimal processing) and the Threaded handler (kernel thread part). <code>IRQF_ONESHOT</code> is a key flag that works with threaded interrupts; it guarantees that even if the thread is handling time-consuming or blocking operations, the underlying IRQ signal won&#39;t be re-enabled too early, thereby avoiding the problem of level-triggered interrupts repeatedly re-entering before processing is complete. Understanding this helps in reasonably splitting tasks in driver design, balancing system response speed with data processing throughput.</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>The Linux kernel shields hardware differences like x86&#39;s IO-APIC or ARM&#39;s GIC through the generic IRQ handling layer. Driver development only requires calling <code>request_irq()</code> or its modern managed version <code>devm_request_irq</code>()<code>即可将中断处理函数挂载到内核的维护的 IRQ 链表中。注册时必须提供正确的标志位（如共享中断的 `IRQF_SHARED`</code>）和用于区分设备的 <code>`dev_id`</code>，处理函数被调用时处于不可睡眠的原子上下文，必须快速完成状态确认并返回 `` <code>IRQ_HANDLED</code>. Calling any kernel function that might block or trigger scheduling is strictly prohibited.</p><p>To resolve the contradiction between hardirqs &quot;executing fast but unable to sleep&quot; and complex device processing &quot;taking a long time and potentially blocking,&quot; modern Linux drivers widely adopt the Threaded IRQ model. Through the <code>request_threaded_irq()</code> API, developers can split interrupt logic into two parts: the Primary Handler, running in atomic context, performs only the most urgent hardware acknowledgments, while the woken kernel thread possesses full process context and can safely hold mutexes, access sleepable memory, or execute time-consuming data transfer tasks.</p><p>In high-throughput scenarios (such as 10G network cards), relying solely on interrupts causes the CPU to fall into a &quot;livelock&quot; state of handling massive interrupt context switches. Therefore, network drivers introduce the NAPI (New API) hybrid mechanism. After receiving the first packet triggers an interrupt, NAPI temporarily disables the device&#39;s hardirq and switches to polling mode, using softirqs to process this batch of data centrally, and re-enables interrupts only after the buffer is cleared, thereby striking a balance between low latency and high throughput.</p><p>In addition to moving heavy work to dedicated threads, the kernel provides softirq and tasklet mechanisms for deferred execution. Softirqs are a statically compiled, high-performance mechanism commonly used for network transmit/receive, but they can preempt user processes when running extensively; Tasklets are built on top of softirqs, providing a more friendly dynamic interface and guaranteeing that the same tasklet will not execute concurrently on multiple CPUs. This simplifies concurrency control in driver development and is suitable for most non-network general-purpose driver scenarios.</p><p>When debugging interrupt issues, developers should make full use of kernel-provided tools for verification. The <code>/proc/interrupts</code> file can display the trigger count and owning device for each IRQ on each CPU core in real time; a count of 0 indicates the hardware signal is not connected. Meanwhile, kernel macros <code>in_irq()</code> and <code>might_sleep()</code> can help check context legality at the code level, preventing the erroneous use of blocking functions that are only allowed in process context within interrupt handlers.</p>',78)])])}const g=t(o,[["render",a]]);export{p as __pageData,g as default};
