import{_ as t,o as s,c as n,a as i}from"./chunks/framework.8tiMeCK-.js";const p=JSON.parse('{"title":"Chapter 2: When Userspace Meets the Kernel","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch02_1.md","filePath":"en/notes/linux_kernel_networking/ch02_1.md","lastUpdated":null}'),o={name:"en/notes/linux_kernel_networking/ch02_1.md"};function a(r,e,l,d,h,c){return s(),n("div",null,[...e[0]||(e[0]=[i(`<h1 id="chapter-2-when-userspace-meets-the-kernel" tabindex="-1">Chapter 2: When Userspace Meets the Kernel <a class="header-anchor" href="#chapter-2-when-userspace-meets-the-kernel" aria-label="Permalink to &quot;Chapter 2: When Userspace Meets the Kernel&quot;">​</a></h1><p>Imagine you are writing an application that needs to monitor network traffic changes. Whenever the kernel&#39;s routing table changes or a new network interface comes online, your program needs to know immediately.</p><p>Twenty years ago, you would probably have opened a character device file and used the archaic ioctl mechanism to poll the kernel. Those were dark days—ioctl was like a one-way tin can telephone; you shout, the kernel replies, and if you don&#39;t shout, you never know what&#39;s happening on the other side.</p><p>But modern Linux doesn&#39;t work that way. What we need is a two-way &quot;phone call&quot; channel, ideally with caller ID (multicast). That is exactly why Netlink Sockets exist.</p><p>Our mission in this chapter is to thoroughly understand this modern kernel communication mechanism. From why we abandoned ioctl, to hand-crafting Netlink message formats, to diving into the kernel code to see how it processes these requests.</p><p>This is harder than it looks—hard because it is a complete protocol, not just a simple function call. We&#39;ll start with the tools we should use in userspace to strike up this conversation.</p><hr><h2 id="_2-1-netlink-sockets-libraries" tabindex="-1">2.1 Netlink Sockets Libraries <a class="header-anchor" href="#_2-1-netlink-sockets-libraries" aria-label="Permalink to &quot;2.1 Netlink Sockets Libraries&quot;">​</a></h2><p>If you really want to hand-write every line of code from scratch, using raw <code>socket()</code> system calls to handle Netlink communication, you certainly can. But it&#39;s like writing a web server in assembly language—sure, you&#39;re a guru, but maintaining it will make you want to cry.</p><p>Standing on the shoulders of giants is always wise. When developing Netlink applications in userspace, there are two ready-made libraries worth your attention: <strong>libnl</strong> and <strong>libmnl</strong>.</p><h3 id="libnl-the-full-featured-swiss-army-knife" tabindex="-1">libnl: The Full-Featured Swiss Army Knife <a class="header-anchor" href="#libnl-the-full-featured-swiss-army-knife" aria-label="Permalink to &quot;libnl: The Full-Featured Swiss Army Knife&quot;">​</a></h3><p>First up is <strong>libnl</strong>. This is currently the most mainstream and feature-complete Netlink user library.</p><p>You can think of it as the &quot;standard library&quot; of the Netlink world. It&#39;s not just a simple wrapper around low-level system calls; it provides a complete, object-oriented API for handling Netlink communication. The famous <code>iproute2</code> package (which powers the <code>ip</code> command you use every day) relies on libnl under the hood.</p><p>This library was developed by Thomas Graf, and its structure is highly modular. In addition to the core <strong>libnl</strong> library (which handles basic socket operations, message sending/receiving, and cache management), it provides specialized sub-libraries for different Netlink protocol families, mainly including:</p><ul><li><strong>libnl-genl</strong>: For handling Generic Netlink (we&#39;ll cover this later—it&#39;s the ultimate solution for running out of protocol numbers).</li><li><strong>libnl-route</strong>: Specifically for handling network-related Netlink messages like routing and links (corresponding to <code>NETLINK_ROUTE</code>).</li><li><strong>libnl-nf</strong>: For handling Netfilter-related messages (such as firewall rules).</li></ul><p>If your project needs to handle complex network configurations—like dynamically modifying routes, managing VLANs, or interacting with the wireless subsystem—libnl is the top choice. It handles many nasty details for you, such as message assembly, attribute parsing (that complex TLV format), and receiving asynchronous notifications.</p><h3 id="libmnl-the-minimalist-s-choice" tabindex="-1">libmnl: The Minimalist&#39;s Choice <a class="header-anchor" href="#libmnl-the-minimalist-s-choice" aria-label="Permalink to &quot;libmnl: The Minimalist&#39;s Choice&quot;">​</a></h3><p>But libnl has its &quot;problem&quot;—it&#39;s too heavy. Sometimes you just want to send a simple Netlink message without linking against a bunch of libraries or dealing with dozens of objects at every turn.</p><p>This is where <strong>libmnl</strong> comes to the rescue.</p><p>libmnl was written by Pablo Neira Ayuso (who also contributed heavily to Netfilter&#39;s core), and its design philosophy is &quot;minimalism.&quot; This is a tiny library with only a few hundred lines of code, and it does exactly one thing: it lets you write as little boilerplate code as possible while retaining full control over Netlink messages.</p><p>It doesn&#39;t do caching, it doesn&#39;t build complex object trees—it just sends buffers and receives buffers. For embedded development or tools that only need to interact with the kernel a few times, libmnl is actually easier to get started with than libnl because its behavior is very direct—what you see is what you get.</p><blockquote><p><strong>Selection Advice</strong>:</p><ul><li>Building a complex network management tool like <code>Wireshark</code>? Choose <strong>libnl</strong>.</li><li>Writing a dedicated small tool to configure a specific kernel feature, where size matters? Choose <strong>libmnl</strong>.</li></ul></blockquote><hr><h3 id="the-sockaddr-nl-structure" tabindex="-1">The sockaddr_nl Structure <a class="header-anchor" href="#the-sockaddr-nl-structure" aria-label="Permalink to &quot;The sockaddr_nl Structure&quot;">​</a></h3><p>Once we&#39;ve chosen a library, and before diving into specific APIs, we need to take a look at what a Netlink &quot;phone number&quot; looks like.</p><p>In TCP/IP, we use <code>sockaddr_in</code> to represent an address (IP + port). In the Netlink world, the corresponding structure is <code>sockaddr_nl</code>. It&#39;s defined in the kernel header file <code>include/uapi/linux/netlink.h</code> and looks very plain:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sockaddr_nl {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    __kernel_sa_family_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    nl_family;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* AF_NETLINK                */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> short</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          nl_pad;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       /* zero                      */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __u32                   nl_pid;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       /* port ID                   */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __u32                   nl_groups;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* multicast groups mask     */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>There are a few fields here worth breaking down, because they determine whether a &quot;letter&quot; can actually reach its destination.</p><h4 id="_1-nl-family" tabindex="-1">1. nl_family <a class="header-anchor" href="#_1-nl-family" aria-label="Permalink to &quot;1. nl_family&quot;">​</a></h4><p>No hesitation here—fill in <strong>AF_NETLINK</strong>. This tells the kernel, &quot;I&#39;m part of the Netlink family, don&#39;t treat me as TCP/IP traffic.&quot;</p><h4 id="_2-nl-pad" tabindex="-1">2. nl_pad <a class="header-anchor" href="#_2-nl-pad" aria-label="Permalink to &quot;2. nl_pad&quot;">​</a></h4><p>Padding field. <strong>Must be 0</strong>. This is for alignment—don&#39;t worry about why, just set it to 0.</p><h4 id="_3-nl-pid-port-id" tabindex="-1">3. nl_pid (Port ID) <a class="header-anchor" href="#_3-nl-pid-port-id" aria-label="Permalink to &quot;3. nl_pid (Port ID)&quot;">​</a></h4><p>The name of this field is a bit misleading. Although it&#39;s called <code>pid</code> (Process ID), it actually represents the <strong>address of the Netlink Socket</strong>, i.e., the &quot;Port ID&quot;.</p><ul><li><strong>For the kernel</strong>: This value is usually <strong>0</strong>. When sending a message from userspace to the kernel, we set the destination address&#39;s <code>nl_pid</code> to 0.</li><li><strong>For userspace</strong>: <ul><li><strong>The simplest approach</strong>: Set it to the current process&#39;s PID (<code>getpid()</code>). This makes it obvious at a glance who is sending the message.</li><li><strong>The laziest approach</strong> (but also the most common): Just set it to 0, or leave it blank entirely, and call <code>bind()</code> directly.</li></ul></li></ul><p>What happens then? The kernel calls an internal method called <code>netlink_autobind()</code>. It attempts to assign the current thread&#39;s PID to this socket.</p><blockquote><p>⚠️ <strong>Pitfall Warning</strong> There&#39;s a trap here that many people stumble into the first time they write a multithreaded program: if you create <strong>two</strong> Netlink sockets in a single process and want both to auto-bind via <code>netlink_autobind()</code>, you&#39;re going to have trouble—because they will both be assigned the same PID.</p><p>The result? When you send a request message to the kernel and the kernel replies, it doesn&#39;t know which socket should receive it. The response either gets sent arbitrarily or only to one of them.</p><p><strong>The fix</strong>: If you open multiple Netlink sockets in a single program, you <strong>must</strong> manually assign them different <code>nl_pid</code> values. Don&#39;t be lazy.</p></blockquote><p>Netlink doesn&#39;t just serve networking. Subsystems like SELinux, the audit system, and device hotplug all use Netlink. But what we care about most is <strong>rtnetlink</strong> (Route Netlink), which is specifically designed to handle network core messages like routing, neighbor tables (ARP), and link states.</p><h4 id="_4-nl-groups" tabindex="-1">4. nl_groups <a class="header-anchor" href="#_4-nl-groups" aria-label="Permalink to &quot;4. nl_groups&quot;">​</a></h4><p>Multicast group mask. One of Netlink&#39;s strengths is its support for <strong>multicast</strong>—the kernel can broadcast events to a group of interested sockets (e.g., &quot;a network interface went down!&quot;). <code>nl_groups</code> is a bitmask used to subscribe to the event groups you&#39;re interested in. If you only want unicast communication, set this to 0.</p><hr><h3 id="userspace-packages-for-controlling-tcp-ip-networking" tabindex="-1">Userspace Packages for Controlling TCP/IP Networking <a class="header-anchor" href="#userspace-packages-for-controlling-tcp-ip-networking" aria-label="Permalink to &quot;Userspace Packages for Controlling TCP/IP Networking&quot;">​</a></h3><p>Having covered the low-level protocols and libraries, let&#39;s come back down to earth and see how the commands we type in the terminal actually map to these concepts.</p><p>Currently, there are two mainstream toolsets in the Linux world: the aging but still widely used <strong>net-tools</strong>, and the modern standard <strong>iproute2</strong>.</p><h4 id="iproute2-the-modern-standard" tabindex="-1">iproute2: The Modern Standard <a class="header-anchor" href="#iproute2-the-modern-standard" aria-label="Permalink to &quot;iproute2: The Modern Standard&quot;">​</a></h4><p>This is the toolset installed by default on most major distributions. It is built almost entirely on Netlink Sockets. When you type <code>ip addr add</code> or <code>ip route</code>, what happens behind the scenes is: the command opens a Netlink socket, constructs a <code>RTM_NEWADDR</code> or <code>RTM_NEWROUTE</code> message, sends it to the kernel, and waits for the kernel&#39;s acknowledgment.</p><p>iproute2 includes these commands you probably use every day:</p><ul><li><strong>ip</strong>: Manages routing tables, network interfaces, addresses, etc. (the all-rounder).</li><li><strong>ss</strong>: Dumps socket statistics (the replacement for <code>netstat</code>, and much faster).</li><li><strong>tc</strong>: Traffic control, used for configuring QoS and traffic shaping (essential for network performance work).</li><li><strong>bridge</strong>: Manages network bridges.</li><li><strong>lnstat</strong>: Views network statistics.</li></ul><p>Although iproute2 mostly uses Netlink, there is one exception: <code>ip tuntap</code>. This command is used to add or delete TUN/TAP virtual devices, and it hasn&#39;t fully migrated to Netlink on the kernel side yet—it still uses IOCTL. If you dig into the TUN/TAP driver code in the kernel, you&#39;ll still see a bunch of <code>ioctl</code> handler functions with no trace of rtnetlink—this is a legacy issue, and we&#39;ll mention it again later when we discuss the kernel implementation.</p><h4 id="net-tools-tears-of-a-bygone-era" tabindex="-1">net-tools: Tears of a Bygone Era <a class="header-anchor" href="#net-tools-tears-of-a-bygone-era" aria-label="Permalink to &quot;net-tools: Tears of a Bygone Era&quot;">​</a></h4><p>If you see someone still using <code>ifconfig</code>, <code>route</code>, <code>arp</code>, or <code>netstat</code>, they are using <strong>net-tools</strong>.</p><p>This toolset is based on <strong>IOCTL</strong>. Its functionality is much weaker than iproute2&#39;s, and it doesn&#39;t support many newer network features (like namespace-related operations) at all. It is now basically in &quot;maintenance mode&quot; and has even been marked as deprecated on some distributions.</p><p>Here&#39;s a very intuitive comparison: if you want to know the detailed state of a socket, <code>netstat</code> reads from the <code>/proc</code> filesystem, which is slow and inaccurate; whereas <code>ss</code> asks the kernel directly via Netlink&#39;s <code>INET_DIAG</code> mechanism, which is fast and provides real-time data.</p><p>Later in this chapter, we&#39;ll dedicate a specific section (&quot;Adding and deleting a routing entry&quot;) to demonstrate how to manually add and delete a routing table entry via Netlink, just like <code>iproute2</code> does. By then, you&#39;ll realize just how much dirty work goes on behind the scenes of that single <code>ip route add</code> command.</p><p>Alright, we&#39;ve now taken stock of the userspace tools and libraries. Next, we&#39;ll plunge our scalpel deep into the kernel to examine <strong>Kernel Netlink Sockets</strong>—how this engine processes requests from userspace and how it packages messages to send back. Understanding this step is what truly crosses the threshold into kernel development.</p>`,55)])])}const g=t(o,[["render",a]]);export{p as __pageData,g as default};
