import{_ as t,o as n,c as o,a as i}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"3.0  Preparation and Motivation for This Chapter","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch03_1.md","filePath":"en/notes/linux_kernel_debugging/ch03_1.md","lastUpdated":null}'),a={name:"en/notes/linux_kernel_debugging/ch03_1.md"};function r(s,e,l,h,c,d){return n(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="_3-0-preparation-and-motivation-for-this-chapter" tabindex="-1">3.0 Preparation and Motivation for This Chapter <a class="header-anchor" href="#_3-0-preparation-and-motivation-for-this-chapter" aria-label="Permalink to &quot;3.0  Preparation and Motivation for This Chapter&quot;">​</a></h1><p>In this chapter, we dive inside the kernel&#39;s &quot;black box.&quot;</p><p>When writing code in user space, <code>printf</code> is your right-hand tool; but inside the kernel, things get far more subtle. You are no longer dealing with a protected process, but rather an execution environment that holds the highest privileges in the system—yet is also the most fragile. A single improper memory access, a deadlock, or even a poorly timed print statement can bring the entire system down in an instant.</p><p>This is exactly what gives kernel newcomers (and even veterans) the biggest headaches: <strong>when the system crashes, how do you debug the system itself?</strong></p><p>This calls for a debugging approach that works without any third-party tool dependencies. It must be simple enough to require no complex UI, yet low-level enough to cut through layers of abstraction and reach the heart of the problem.</p><p>In the kernel world, that approach is <code>printk</code>—along with the entire logging and tracing mechanism built around it.</p><p>But this is no simple topic. Many people assume that <code>printk</code> is just the kernel version of <code>printf</code>. That understanding is correct, but only half the story. As we proceed, we will see how it builds a ring buffer under extremely constrained resources, how it filters out noise through log levels, and even how it leaves a final message via Early printk before the system completely hangs.</p><p><strong>Get your environment ready.</strong></p><p>We don&#39;t need to install anything new here. As mentioned in Chapter 1, your working environment—whether a virtual machine or a physical board—should already be in place. The code is there, the toolchain is ready, and your debugging journey begins with this section.</p><hr><h1 id="_3-1-technical-requirements" tabindex="-1">3.1 Technical Requirements <a class="header-anchor" href="#_3-1-technical-requirements" aria-label="Permalink to &quot;3.1  Technical Requirements&quot;">​</a></h1><p>If you flip back to Chapter 1, you might think I&#39;m repeating myself—but that&#39;s actually a good thing.</p><p>It means our battlefield hasn&#39;t changed, and our arsenal consists of the same familiar tools. There&#39;s no need to wrestle with setting up a cross-compiler again, and no need to deal with those hair-pulling dependency libraries. We can focus all our energy on the core issue: <strong>how to see exactly what is happening inside the kernel.</strong></p><p>To ensure we can run all the experiments in this chapter together, here is a quick checklist:</p><ol><li><p><strong>Development environment</strong>: You still need the Linux development environment set up in Chapter 1 (the host is typically a Linux distribution, and the target can be a QEMU virtual machine or an ARM board).</p></li><li><p><strong>Kernel source code</strong>: Although we will be reading code, you don&#39;t need to write a kernel from scratch. All the example code is packaged and available in this book&#39;s GitHub repository:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>https://github.com/PacktPublishing/Linux-Kernel-Debugging</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>If your local repository isn&#39;t up to date, now is a good time to <code>git pull</code>.</p></li><li><p><strong>Necessary permissions</strong>: In later sections, we will frequently read the kernel log buffer (<code>dmesg</code>) and debug filesystems (<code>debugfs</code>). This means you need <code>root</code> privileges on the host machine, or you need to have the appropriate <code>sudo</code> rules configured. Don&#39;t get stuck here—there&#39;s nothing more frustrating than typing a bunch of commands only to discover a <code>Permission denied</code>.</p></li></ol><p>Once the environment is confirmed, we officially begin.</p><p>The first question we face, and the core problem we will solve in this chapter, is: <strong>when printk is called, where exactly do those characters go?</strong></p><p>This is a seemingly simple question, but one that touches on the underlying design philosophy of the kernel. We will break it down in the next section.</p>',18)])])}const g=t(a,[["render",r]]);export{u as __pageData,g as default};
