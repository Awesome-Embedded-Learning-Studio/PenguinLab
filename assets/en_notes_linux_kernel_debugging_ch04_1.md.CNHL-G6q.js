import{_ as t,o,c as n,a as i}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse(`{"title":"Chapter 4: Seeing the Elephant Through a Needle's Eye — Kernel Probes and Dynamic Tracing","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch04_1.md","filePath":"en/notes/linux_kernel_debugging/ch04_1.md","lastUpdated":null}`),r={name:"en/notes/linux_kernel_debugging/ch04_1.md"};function s(a,e,l,h,c,d){return o(),n("div",null,[...e[0]||(e[0]=[i('<h1 id="chapter-4-seeing-the-elephant-through-a-needle-s-eye-—-kernel-probes-and-dynamic-tracing" tabindex="-1">Chapter 4: Seeing the Elephant Through a Needle&#39;s Eye — Kernel Probes and Dynamic Tracing <a class="header-anchor" href="#chapter-4-seeing-the-elephant-through-a-needle-s-eye-—-kernel-probes-and-dynamic-tracing" aria-label="Permalink to &quot;Chapter 4: Seeing the Elephant Through a Needle&#39;s Eye — Kernel Probes and Dynamic Tracing&quot;">​</a></h1><p>We are staring at a high-speed tangle of chaos.</p><p>When you try to understand a complex system like the Linux kernel, the most intuitive tool is &quot;printing.&quot; You stuff your code with <code>printf</code>, recompile, run, and observe the output. This is standard practice in user-space development. But in the kernel, it&#39;s not just cumbersome—it&#39;s a disaster.</p><p>Why? Because the kernel <strong>does not</strong> have its own runtime environment. The moment kernel code stops to print a log, the entire system—including the very subsystem responsible for receiving that log—might pause. Worse still, the bug you want to observe might occur in a delicate instant of concurrent racing, or deep within interrupt handling. If you recompile and reboot the kernel just to insert a single log line, that bug might slip away like a startled fish.</p><p>We need a capability: <strong>to insert &quot;hooks&quot; at the entry or exit of any kernel function without interrupting system operation or recompiling the kernel</strong>. When the system flows through that point, the hook triggers, records the register states, parameter values, and return values we want to know, and then lets the system continue running as if nothing happened.</p><p>This is the core mission of this chapter: building this &quot;God&#39;s-eye view&quot; observability capability.</p><p>But honestly, this path is not smooth. Old-school kernel debugging requires you to write C kernel modules and manually register probe interfaces named <code>kprobe</code>—this is known as &quot;static probes.&quot; It works, but writing them is extremely tedious, and even changing a print format requires recompiling and reloading. Modern Linux kernels offer a more elegant approach: leveraging the <code>ftrace</code> and <code>tracepoints</code> frameworks to dynamically insert probes into the kernel directly via user-space command-line tools or scripts. This is called &quot;dynamic probing,&quot; and even revolutionary technologies like <code>eBPF</code> are built on top of these underlying mechanisms.</p><p>In this chapter, we will start with the most primitive &quot;static probes,&quot; writing code by hand, filling out structures, and handling registers. Then we will gradually free our hands and experience the convenience brought by dynamic tracing and eBPF. Only after experiencing the pain firsthand will you truly understand why modern tools are designed the way they are.</p><p>Now, let&#39;s start with the most fundamental and versatile mechanism: <strong>kprobes</strong>.</p><hr><h2 id="_4-1-the-fundamental-principle-of-kprobes-operating-on-functions" tabindex="-1">4.1 The Fundamental Principle of Kprobes: Operating on Functions <a class="header-anchor" href="#_4-1-the-fundamental-principle-of-kprobes-operating-on-functions" aria-label="Permalink to &quot;4.1  The Fundamental Principle of Kprobes: Operating on Functions&quot;">​</a></h2><p>Imagine you are a privileged observer inside the system. You want to monitor every &quot;file open&quot; operation that occurs in kernel space, regardless of which process initiated it, and regardless of whether it calls <code>open()</code> or <code>openat()</code>.</p><p>With traditional debugging methods, you would find the source code of the kernel function <code>do_sys_open</code>, add a <code>printk</code> to the first line, and recompile the kernel. But this is far too clunky. Kprobes provides a lighter-weight way to intervene: it allows you to dynamically &quot;inject&quot; a breakpoint at a specific address within a kernel function.</p><p>You can think of it as the <strong>Swiss Army knife of kernel debugging</strong>.</p><p>But this knife has a special blade—it can not only cut into a function but also intercept data when the function returns. To use it well, we first need to understand exactly what it looks like.</p><h3 id="three-states-of-a-probe" tabindex="-1">Three States of a Probe <a class="header-anchor" href="#three-states-of-a-probe" aria-label="Permalink to &quot;Three States of a Probe&quot;">​</a></h3><p>Kprobes is not just a simple &quot;set a breakpoint&quot; mechanism. To make debugging more flexible, it provides three insertion points in the execution flow.</p><p>Suppose we want to probe the famous kernel function <code>do_sys_open()</code> (the kernel function that user-space ultimately lands in when calling the <code>open(2)</code> system call; for details, see the section &quot;Where System Calls Land in the Kernel&quot;). Through the kprobes infrastructure, you can attach three different types of handlers:</p><ol><li><p><strong>Pre-handler</strong>: Triggered <strong>before</strong> the first instruction of the <code>do_sys_open()</code> function executes. This is the most commonly used hook, typically employed to print function parameters (via registers), inspect the call stack, or decide at this point whether to skip the function&#39;s execution entirely.</p></li><li><p><strong>Post-handler</strong>: Triggered immediately <strong>after</strong> all instructions of the <code>do_sys_open()</code> function have finished executing and it is about to return. It is suitable for checking the side effects after the function executes, or verifying whether state has been modified.</p></li><li><p><strong>Fault-handler</strong>: This is a safety net. If a CPU exception occurs (such as a Page Fault) during the execution of the pre-handler or post-handler, or if kprobes itself encounters an issue while single-stepping an instruction, this handler will be called. Often, it&#39;s simply because your handler code accessed an illegal memory address. Without a fault-handler, the kernel might panic directly; with it, you at least have a chance to exit gracefully with an error.</p></li></ol><p>All three handlers are <strong>optional</strong>. You can set only a pre-handler, or set all three—it entirely depends on your needs.</p><h3 id="kprobe-or-kretprobe" tabindex="-1">Kprobe or Kretprobe? <a class="header-anchor" href="#kprobe-or-kretprobe" aria-label="Permalink to &quot;Kprobe or Kretprobe?&quot;">​</a></h3><p>In addition to the &quot;regular probe&quot; described above, the Linux kernel provides a special probe designed to solve a specific pain point: <strong>I want to know what this function&#39;s return value is.</strong></p><p>This is the <strong>Kretprobe (Return Probe)</strong>.</p><p>Why is it needed? Because when a function finishes executing, the CPU&#39;s instruction pointer has already returned to the caller. If you check the regular post-handler at this point, although it triggers, getting the return value often requires digging deep into the stack or registers, which is very cumbersome (and strongly tied to the CPU architecture).</p><p>Kretprobe does something clever: at the function entry, by modifying the stack frame and other means, it secretly records the return address and intercepts the control flow right before the function actually returns. This way, it can easily hand the return value over to you.</p><p>The registration APIs for these two types of probes are also separate:</p><ul><li><strong>Regular Kprobe</strong>: uses <code>register_kprobe[s]()</code> / <code>unregister_kprobe[s]()</code></li><li><strong>Kretprobe</strong>: uses <code>register_kretprobe[s]()</code> / <code>unregister_kretprobe[s]()</code></li></ul><p>We will start with the most basic <code>kprobe</code> and leave <code>kretprobe</code> for the slightly more advanced section later.</p><h3 id="the-scalpel-s-manual-struct-kprobe" tabindex="-1">The Scalpel&#39;s Manual: <code>struct kprobe</code> <a class="header-anchor" href="#the-scalpel-s-manual-struct-kprobe" aria-label="Permalink to &quot;The Scalpel&#39;s Manual: `struct kprobe`&quot;">​</a></h3><p>To actually use this in code, you need to prepare a core data structure: <code>struct kprobe</code>. Think of it as a &quot;surgical checklist&quot;—the kernel relies on this list to know where to insert the blade and what to do once it&#39;s in.</p><p>The API for registering this structure looks like this:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;linux/kprobes.h&gt;</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> register_kprobe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kprobe </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">p</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>To keep you from getting lost in a sea of structures, we will only focus on the most critical fields (you can leave the rest to their defaults):</p><ul><li><p><strong><code>const char *symbol_name</code></strong>: This is the name of the kernel function you want to &quot;operate on,&quot; such as <code>&quot;do_sys_open&quot;</code>. Under the hood, the kprobes framework calls APIs like <code>kallsyms_lookup()</code> to resolve this symbol string into a Kernel Virtual Address (KVA) and populates the internal <code>addr</code> member of the structure. <strong>Note</strong>: Not all functions can be probed. Some functions are placed on a blacklist (such as kprobes&#39; own internal functions), and probing them will cause a kernel crash or deadlock. We will discuss this in detail in the &quot;Limitations of Kprobes&quot; section later.</p></li><li><p><strong><code>kprobe_pre_handler_t pre_handler</code></strong>: This is a function pointer pointing to your pre-handler code. It will be called before the target instruction executes.</p></li><li><p><strong><code>kprobe_post_handler_t post_handler</code></strong>: Similarly, this is the function pointer for the post-handler.</p></li><li><p><strong><code>kprobe_fault_handler_t fault_handler</code></strong>: If your pre- or post-handler triggers an exception, the kernel will jump into this function. <strong>Key point</strong>: The return value of this function is very specific. Returning <code>0</code> means &quot;I can&#39;t handle this, hand it over to the kernel&#39;s default exception handling mechanism&quot; (this is the usual case); returning <code>1</code> means &quot;It&#39;s fixed, the error has been resolved, continue execution&quot; (this is rare and requires you to truly know what you are doing, such as manually fixing a page table).</p></li></ul><blockquote><p><strong>Here&#39;s an advanced trick</strong></p><p><code>kprobe</code> can be attached not only at the beginning of a function but also at <strong>any arbitrary offset within the function</strong>. You simply need to set the <code>offset</code> member of <code>struct kprobe</code>. This is extremely useful when debugging complex assembly code blocks or when you only care about the logic in the latter half of a function. However, on CISC architectures (like x86), if you set the offset incorrectly and land directly in the middle of an instruction, the CPU will throw an error immediately. It&#39;s like sticking a needle into a heart right at the moment it beats—proceed at your own risk.</p></blockquote><p>When you&#39;re done, don&#39;t forget one thing: <strong>cleanup</strong>.</p><p>When your module is unloaded, you must call the unregister function to remove the probe:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> unregister_kprobe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kprobe </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">p</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>If you forget this step, the consequences are severe. The next time any code flows through that address, the kernel will try to trigger a probe callback that no longer exists. The result? A kernel bug, or even a direct crash. This is a classic resource leak scenario, except what&#39;s leaking isn&#39;t memory, but &quot;control flow hijack points.&quot;</p><h3 id="how-does-it-work-a-glimpse-beyond-the-black-box" tabindex="-1">How Does It Work? (A Glimpse Beyond the Black Box) <a class="header-anchor" href="#how-does-it-work-a-glimpse-beyond-the-black-box" aria-label="Permalink to &quot;How Does It Work? (A Glimpse Beyond the Black Box)&quot;">​</a></h3><p>You might be wondering: how is injecting a breakpoint into a running kernel actually implemented? Does it stop all CPUs? Or is it some kind of black magic?</p><p>To tell you the truth, the implementation details behind this—such as how to temporarily replace instructions, how to handle single-stepping, and how to ensure correctness in a multi-core environment—are extremely complex, involving architecture-specific assembly tricks and the kernel&#39;s low-level exception handling mechanisms.</p><p>If you are genuinely interested in those <strong>gory details</strong>, we highly recommend reading the official kernel documentation. It explains how kprobes leverages CPU debug registers (like DR0-DR7 on x86) and breakpoint instructions (like INT3) to achieve all of this.</p><ul><li><strong>Reference</strong>: <a href="https://www.kernel.org/doc/html/latest/trace/kprobes.html#concepts-kprobes-and-return-probes" target="_blank" rel="noreferrer">Concepts: Kprobes and Return Probes</a></li></ul><p>In this book, we will keep these black boxes closed for now and focus on <strong>how to use</strong> this powerful blade.</p><h3 id="why-go-through-all-this-trouble-from-static-to-dynamic" tabindex="-1">Why Go Through All This Trouble? (From Static to Dynamic) <a class="header-anchor" href="#why-go-through-all-this-trouble-from-static-to-dynamic" aria-label="Permalink to &quot;Why Go Through All This Trouble? (From Static to Dynamic)&quot;">​</a></h3><p>The approach we are introducing in this section—writing a kernel module, filling out <code>struct kprobe</code>, and registering handlers—is known as <strong>Static Kprobes</strong>.</p><p>It&#39;s called &quot;static&quot; because every time you want to probe a new function or change the log output format, you have to modify the C code, recompile the module, unload the old module, and load the new one.</p><p>This sounds a bit outdated, right? In this day and age, still recompiling just to take a peek at a log? Modern Linux kernels do have more advanced tricks up their sleeves.</p><p>This is <strong>Dynamic Probing</strong>, or <strong>Kprobe-based Event Tracing</strong>. It deeply integrates with the <code>ftrace</code> and <code>tracepoints</code> frameworks. You don&#39;t need to write a single line of C code; you can dynamically instrument the kernel just by writing a one-line string configuration via <code>debugfs</code>, or by using the perf tool or eBPF scripts. No compilation, no reboot—silky smooth.</p><p>But before we fly off to that modern world, let&#39;s lay a solid foundation. In the following demos, we will start with the most primitive hand-written static kprobe and step by step close in on the truth of that file open:</p><ul><li><strong>Demo 1</strong>: The simplest &quot;hardcoded&quot; probe—hardcoding the probe on the <code>open</code> system call.</li><li><strong>Demo 2</strong>: Slightly smarter—specifying the function name to probe via module parameters, eliminating the need to modify code and recompile.</li><li><strong>Demo 3</strong>: The real meat—not just &quot;seeing&quot; that a function was called, but actually <strong>extracting the filename</strong>.</li></ul><p>Ready? Let&#39;s start writing some code.</p>',53)])])}const g=t(r,[["render",s]]);export{u as __pageData,g as default};
