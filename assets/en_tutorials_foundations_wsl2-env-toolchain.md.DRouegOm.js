import{_ as s,o as a,c as n,a as i}from"./chunks/framework.BHSVNLI7.js";const u=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"en/tutorials/foundations/wsl2-env-toolchain.md","filePath":"en/tutorials/foundations/wsl2-env-toolchain.md","lastUpdated":null}'),t={name:"en/tutorials/foundations/wsl2-env-toolchain.md"};function l(r,e,o,p,h,c){return a(),n("div",null,[...e[0]||(e[0]=[i(`<h2 id="welcome-to-the-kernel" tabindex="-1">Welcome to the Kernel! <a class="header-anchor" href="#welcome-to-the-kernel" aria-label="Permalink to &quot;Welcome to the Kernel!&quot;">​</a></h2><p>This is going to be an extremely long journey. As an operating system that has evolved over decades, Linux&#39;s complexity stands as the pinnacle of software engineering in human history! A journey of a thousand miles begins with a single step. Let&#39;s start our journey with this article!</p><h2 id="what-we-ll-do" tabindex="-1">What We&#39;ll Do <a class="header-anchor" href="#what-we-ll-do" aria-label="Permalink to &quot;What We&#39;ll Do&quot;">​</a></h2><p>The author developed this tutorial based on their own learning path using WSL2. If you&#39;re using a different distribution on a real physical machine, some modifications might be necessary. The distribution running on the author&#39;s WSL2 is Arch Linux—simply out of habit! So please forgive us; if you&#39;re an Ubuntu/Debian user, you may need to adjust the commands accordingly.</p><p>We need to set up a complete Linux kernel development environment inside WSL2. Specifically, we need to confirm whether WSL2 is up to the task, then install a cross-compilation toolchain to prepare for building ARM64 kernels later. After finishing this article, we&#39;ll have a &quot;verified working&quot; development environment with the toolchain in place, ready to configure the kernel, compile it, and run it in QEMU at any time.</p><h2 id="what-to-understand" tabindex="-1">What to Understand <a class="header-anchor" href="#what-to-understand" aria-label="Permalink to &quot;What to Understand&quot;">​</a></h2><h3 id="why-wsl2" tabindex="-1">Why WSL2 <a class="header-anchor" href="#why-wsl2" aria-label="Permalink to &quot;Why WSL2&quot;">​</a></h3><p>We need to answer a question first: why not just do kernel development directly on a physical machine? The answer is simple—the ARM64 kernel we build can&#39;t run on an x86 host at all; it must execute inside an emulator (QEMU). WSL2 provides a complete Linux environment while acting as a lightweight virtual machine for Windows. This lets us enjoy the convenience of the Linux toolchain without worrying about breaking the host system—because what actually runs our compiled kernel is QEMU, which is completely isolated within WSL2. Even if the kernel crashes, a driver panics, or the rootfs gets corrupted, the worst-case scenario is just the QEMU process dying; the host machine remains completely unaffected.</p><h3 id="assessing-wsl2-capabilities" tabindex="-1">Assessing WSL2 Capabilities <a class="header-anchor" href="#assessing-wsl2-capabilities" aria-label="Permalink to &quot;Assessing WSL2 Capabilities&quot;">​</a></h3><p>Before installing anything, let&#39;s spend a few minutes figuring out exactly what we&#39;re working with. First, let&#39;s check the kernel version and basic system info:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uname</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -a</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Linux Charliechen 6.6.87.2-microsoft-standard-WSL2 #1 SMP PREEMPT_DYNAMIC ... x86_64 GNU/Linux</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Notice the <code>-microsoft-standard-WSL2</code> suffix—that&#39;s the Linux kernel bundled with WSL2, which is completely different from the ARM64 kernel we&#39;ll compile later. This kernel is just our &quot;host system&quot;; the actual target kernel we&#39;ll study and tinker with will run inside QEMU.</p><p>Next, let&#39;s look at the CPU and virtualization capabilities:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /proc/cpuinfo</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> head</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -20</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># processor    : 0</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># model name   : AMD Ryzen 7 5800H with Radeon Graphics</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># cpu cores    : 7</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ...</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>There&#39;s a key check here—whether <code>/dev/kvm</code> exists. KVM (Kernel-based Virtual Machine) is a virtualization accelerator built into the Linux kernel. If WSL2 supports KVM, QEMU can use hardware virtualization to significantly boost execution speed; if it doesn&#39;t exist, QEMU can still run using pure software emulation, but it will be noticeably slower. Starting from Windows 11 22H2, WSL2 began supporting nested virtualization, so there&#39;s a high probability that <code>/dev/kvm</code> exists:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ls</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /dev/kvm</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># /dev/kvm  ← 存在，QEMU 可以用硬件加速</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Our machine has a solid foundation—an AMD Ryzen 7 5800H, 7 cores and 14 threads, KVM available, and the parallelism for kernel compilation (<code>-j14</code>) can be fully utilized.</p><p>Let&#39;s also check the distribution info, since this directly affects package management commands:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /etc/os-release</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># NAME=&quot;Arch Linux&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># PRETTY_NAME=&quot;Arch Linux&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ID=arch</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ...</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>We&#39;re using Arch Linux (rolling release), so the package manager is <code>pacman</code> rather than <code>apt</code>. If you&#39;re using an Ubuntu/Debian-based WSL2, the software installation commands will differ later on. I&#39;ll note both syntaxes at the relevant places.</p><h3 id="cross-compilation-toolchain-building-arm-wheels-on-x86" tabindex="-1">Cross-Compilation Toolchain: Building ARM Wheels on x86 <a class="header-anchor" href="#cross-compilation-toolchain-building-arm-wheels-on-x86" aria-label="Permalink to &quot;Cross-Compilation Toolchain: Building ARM Wheels on x86&quot;">​</a></h3><p>Next, we&#39;ll install the cross-compilation toolchain. Why is it called &quot;cross&quot; compilation? Because our host machine is x86_64, but the target platform is ARM64—the compiler runs on x86, but the machine code it generates is for ARM execution. It&#39;s like manufacturing products to another country&#39;s standards in your own factory; the tools themselves need to be adapted.</p><p>Installing the toolchain is straightforward—just one command:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Arch Linux</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pacman</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -S</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> aarch64-linux-gnu-gcc</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # ARM64 交叉编译器</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Ubuntu / Debian</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gcc-aarch64-linux-gnu</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # ARM64 交叉编译器</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>After installation, let&#39;s verify it:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">aarch64-linux-gnu-gcc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --version</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># aarch64-linux-gnu-gcc (GCC) 15.2.0</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Copyright (C) 2025 Free Software Foundation, Inc.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ...</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Seeing the version number means the toolchain is in place. Now let&#39;s understand what this long name actually means—the naming convention for <code>aarch64-linux-gnu-gcc</code> is <code>&lt;架构&gt;-&lt;厂商&gt;-&lt;操作系统&gt;-&lt;ABI&gt;-&lt;工具&gt;</code>, where <code>aarch64</code> is the official architecture name for ARM64 (defined by ARM Ltd.), <code>linux</code> indicates the target system is Linux, <code>gnu</code> means it uses the GNU C Library (glibc), and the final <code>gcc</code> is our familiar C compiler. This naming convention is consistent across the entire cross-compilation toolchain. For example, <code>aarch64-linux-gnu-ld</code> is the linker, <code>aarch64-linux-gnu-objdump</code> is the disassembler, and <code>aarch64-linux-gnu-gdb</code> is the debugger—they all share the same prefix.</p><h3 id="arch-and-cross-compile-two-key-variables-in-the-kernel-build-system" tabindex="-1">ARCH and CROSS_COMPILE: Two Key Variables in the Kernel Build System <a class="header-anchor" href="#arch-and-cross-compile-two-key-variables-in-the-kernel-build-system" aria-label="Permalink to &quot;ARCH and CROSS_COMPILE: Two Key Variables in the Kernel Build System&quot;">​</a></h3><p>The kernel&#39;s build system (Kbuild) controls cross-compilation through two environment variables: <code>ARCH</code> specifies the target architecture, and <code>CROSS_COMPILE</code> specifies the toolchain prefix.</p><p>On this topic, the author recommends that anyone learning the kernel should start getting familiar with the Linux Documentation. The author is happy to excerpt content from the Documentation—after all, the authority of the official documentation always far outweighs making things up by asking an LLM to ramble, right?</p><blockquote><p>URL: <a href="https://www.kernel.org/doc/html/latest/kbuild/kbuild.html" target="_blank" rel="noreferrer">https://www.kernel.org/doc/html/latest/kbuild/kbuild.html</a></p></blockquote><p>Find the introduction to ARCH and CROSS_COMPILE.</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ARCH</span></span>
<span class="line"><span>Set ARCH to the architecture to be built.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>In most cases the name of the architecture is the same as the directory name found in the arch/ directory.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>But some architectures such as x86 and sparc have aliases.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>x86: i386 for 32 bit, x86_64 for 64 bit</span></span>
<span class="line"><span></span></span>
<span class="line"><span>parisc: parisc64 for 64 bit</span></span>
<span class="line"><span></span></span>
<span class="line"><span>sparc: sparc32 for 32 bit, sparc64 for 64 bit</span></span>
<span class="line"><span></span></span>
<span class="line"><span>CROSS_COMPILE</span></span>
<span class="line"><span>Specify an optional fixed part of the binutils filename. CROSS_COMPILE can be a part of the filename or the full path.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>CROSS_COMPILE is also used for ccache in some setups.</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br></div></div><p>Can&#39;t read the English? No problem. Keep a translation tool handy—the author will do the honors this time:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ARCH</span></span>
<span class="line"><span>将 ARCH 设置为要构建的架构。在大多数情况下，架构名称与 arch/ 目录中的目录名称相同。但某些架构如 x86 和 sparc 有别名。</span></span>
<span class="line"><span></span></span>
<span class="line"><span>- x86：32 位使用 i386，64 位使用 x86_64</span></span>
<span class="line"><span>- parisc：64 位使用 parisc64</span></span>
<span class="line"><span>- sparc：32 位使用 sparc32，64 位使用 sparc64</span></span>
<span class="line"><span></span></span>
<span class="line"><span>CROSS_COMPILE</span></span>
<span class="line"><span>- 指定 binutils 文件名的可选固定部分。CROSS_COMPILE 可以是文件名的一部分或完整路径。在某些设置中，CROSS_COMPILE 也用于 ccache。</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>There&#39;s an easy pitfall to fall into here—the value of <code>ARCH</code> doesn&#39;t always have a one-to-one correspondence with the toolchain prefix naming. For ARM64, the architecture name used by the kernel build system is <code>arm64</code>, but the toolchain prefix is <code>aarch64-linux-gnu-</code>; they are different. This is due to historical reasons. The ARM64 architecture has always been called <code>arm64</code> in the kernel community, while the toolchain side uses the <code>aarch64</code> name officially defined by ARM. The project&#39;s <a href="./scripts/linux-action-scripts.sh">linux-action-scripts.sh</a> lines 69–72 specifically handle this mapping, converting user-provided <code>aarch64</code> or <code>arm64</code> into the <code>arm64</code> that Kbuild recognizes.</p><p><code>CROSS_COMPILE</code> is even more interesting—it&#39;s not a complete command name, but a prefix. The kernel build system automatically appends suffixes like <code>gcc</code>, <code>ld</code>, and <code>objcopy</code> after this prefix to find the corresponding tools. In other words, when you set <code>CROSS_COMPILE=aarch64-linux-gnu-</code>, make will look for <code>aarch64-linux-gnu-gcc</code>, <code>aarch64-linux-gnu-ld</code>, <code>aarch64-linux-gnu-objcopy</code>, and a whole series of other tools. If <code>CROSS_COMPILE</code> is an empty string, make will directly use the local machine&#39;s <code>gcc</code>, <code>ld</code>, etc.—this is exactly why, when you forget to set this variable, the kernel gets compiled for the x86 architecture. The empty string prefix causes the build system to use the host&#39;s compiler.</p><p>A recommended practice is to export these two variables in your current shell, so you don&#39;t have to repeat them for every subsequent make command:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ARCH</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">arm64</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CROSS_COMPILE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">aarch64-linux-gnu-</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Besides the toolchain, compiling the kernel requires some auxiliary tools (bison, flex, etc.). On Ubuntu/Debian, one line does the trick:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Ubuntu / Debian, UnAuthorized Commands，这部分我没验证</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apt</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build-essential</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> libncurses-dev</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bison</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> flex</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> libssl-dev</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> libelf-dev</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Arch Linux users generally already have these tools installed through the base-devel group. If not, use <code>pacman -S base-devel</code> to fill in the gaps.</p><h2 id="try-it-yourself" tabindex="-1">Try It Yourself <a class="header-anchor" href="#try-it-yourself" aria-label="Permalink to &quot;Try It Yourself&quot;">​</a></h2><p>Here&#39;s how the author does it! Come give it a try!</p><ol><li>Run <code>uname -r</code>, <code>cat /proc/version</code>, and <code>ls /dev/kvm</code> in sequence in your WSL2 terminal, and record your environment info</li><li>Install the ARM64 cross-compiler and verify that <code>aarch64-linux-gnu-gcc --version</code> outputs a version number</li><li>Export <code>ARCH=arm64</code> and <code>CROSS_COMPILE=aarch64-linux-gnu-</code> in your terminal, then run <code>echo __PRESERVED_10__CROSS_COMPILE</code> to confirm the variables have taken effect</li><li>If you&#39;re also interested in RISC-V, you can install <code>gcc-riscv64-linux-gnu</code> along the way (Arch: <code>pacman -S riscv64-linux-gnu-gcc</code>); subsequent exercises will support multiple architectures</li></ol><h2 id="further-reading" tabindex="-1">Further Reading <a class="header-anchor" href="#further-reading" aria-label="Permalink to &quot;Further Reading&quot;">​</a></h2><ul><li><a href="https://www.kernel.org/doc/html/latest/kbuild/kbuild.html" target="_blank" rel="noreferrer">Kbuild Kernel Build System Documentation</a> — The official kernel.org definitions for variables like <code>ARCH</code> and <code>CROSS_COMPILE</code></li><li><a href="https://docs.kernel.org/admin-guide/README.html" target="_blank" rel="noreferrer">Linux Kernel Build Instructions</a> — The web version of the README in the kernel source root directory</li><li><a href="https://jensd.be/1126/linux/cross-compiling-for-arm-or-aarch64-on-debian-or-ubuntu" target="_blank" rel="noreferrer">Cross-Compiling for Debian/Ubuntu in Practice</a> — A cross-compilation tutorial for Ubuntu-based environments</li></ul>`,47)])])}const b=s(t,[["render",l]]);export{u as __pageData,b as default};
