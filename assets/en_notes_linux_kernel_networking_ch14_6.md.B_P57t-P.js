import{_ as t,o as i,c as a,a as o}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"14.6  Busy Poll Sockets","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch14_6.md","filePath":"en/notes/linux_kernel_networking/ch14_6.md","lastUpdated":null}'),s={name:"en/notes/linux_kernel_networking/ch14_6.md"};function n(r,e,l,h,c,p){return i(),a("div",null,[...e[0]||(e[0]=[o(`<h1 id="_14-6-busy-poll-sockets" tabindex="-1">14.6 Busy Poll Sockets <a class="header-anchor" href="#_14-6-busy-poll-sockets" aria-label="Permalink to &quot;14.6  Busy Poll Sockets&quot;">​</a></h1><p>In the previous section, we covered Cgroups and Namespaces, the cornerstones of containers. Now, let&#39;s shift our focus away from &quot;isolation&quot; and zero in on a topic of extreme performance.</p><p>Imagine you are writing a high-frequency trading system, or an edge gateway that requires nanosecond-level response times. Your code runs on a Xeon processor, paired with a top-tier 10GbE NIC—everything looks perfect. But when you stare at the flame graph, you&#39;ll notice a red region that sticks out like a sore thumb: <strong>context switches</strong>.</p><p>Every time a packet arrives, the NIC fires an interrupt, forcing the CPU to drop what it&#39;s doing, execute the interrupt handler, and then schedule your application to wake up. This entire flow has been optimized to the hilt in modern Linux, but for certain extreme scenarios, it is still too &quot;heavy.&quot;</p><p>Can we completely bypass interrupts and scheduling?</p><p>That&#39;s exactly what <strong>Busy Poll Sockets</strong> are all about. At its core, it&#39;s a brute-force approach that trades CPU cycles for time.</p><hr><h3 id="the-traditional-dilemma-sleeping-and-waking" tabindex="-1">The Traditional Dilemma: Sleeping and Waking <a class="header-anchor" href="#the-traditional-dilemma-sleeping-and-waking" aria-label="Permalink to &quot;The Traditional Dilemma: Sleeping and Waking&quot;">​</a></h3><p>In the traditional network stack model, when an application reads a Socket and the receive queue is empty, what does the kernel do?</p><p>In blocking mode, the application <strong>goes to sleep</strong>. It suspends itself, releasing the CPU to other processes. At this point, the application is in a waiting state.</p><p>This is elegant, right? No data, so sleep—no CPU wasted. But the cost is <strong>latency</strong>.</p><ol><li>The <strong>application</strong> finds no data and calls <code>schedule()</code> to sleep.</li><li>The <strong>NIC</strong> receives a packet and triggers a <strong>hardware interrupt</strong>.</li><li>The <strong>CPU</strong> pauses its current task and jumps to the interrupt handler.</li><li>The <strong>driver</strong> hands the packet to the protocol stack (L3) and pushes it into the Socket queue.</li><li>The <strong>protocol stack</strong> wakes up the sleeping application.</li><li>The <strong>scheduler</strong> performs a context switch and resumes the application&#39;s execution.</li></ol><p>By the time this entire &quot;combo&quot; finishes, microseconds have slipped away. For low-latency applications, this is unacceptable.</p><p>Starting with kernel 3.11 (originally called Low Latency Sockets Poll, later renamed to Busy Poll per Linus&#39;s suggestion), Linux introduced a more aggressive approach.</p><hr><h3 id="busy-poll-refusing-to-sleep" tabindex="-1">Busy Poll: Refusing to Sleep <a class="header-anchor" href="#busy-poll-refusing-to-sleep" aria-label="Permalink to &quot;Busy Poll: Refusing to Sleep&quot;">​</a></h3><p><strong>The core idea behind Busy Poll is simple: since sleeping and waking take too much time, I just won&#39;t sleep.</strong></p><p>When an application reads a Socket and finds no data, instead of blocking, it goes <strong>straight down to the driver layer</strong> to ask: &quot;Do you have any freshly received goods? If so, give them to me directly—skip the interrupt flow.&quot;</p><p>That&#39;s exactly what the <code>ndo_busy_poll</code> callback function does.</p><p>You can think of this process like this:</p><blockquote><p>The traditional model is like <strong>passively waiting for a delivery</strong>: you sleep at home (application blocks), the delivery arrives and knocks on the door (interrupt), and you wake up to answer and sign for it. Busy Poll is like <strong>proactively checking the pickup station</strong>: you run to the station (driver) every few minutes to ask, &quot;Is there a package for me?&quot; If there is, you take it immediately—no need to wait for the courier to call.</p></blockquote><h4 id="implementation-of-the-mechanism" tabindex="-1">Implementation of the Mechanism <a class="header-anchor" href="#implementation-of-the-mechanism" aria-label="Permalink to &quot;Implementation of the Mechanism&quot;">​</a></h4><p>For a NIC driver to support Busy Poll, it needs to implement the <code>ndo_busy_poll</code> callback in the <code>net_device_ops</code> structure.</p><p>Take a look at Intel&#39;s ixgbe driver (<code>drivers/net/ethernet/intel/ixgbe/ixgbe_main.c</code>); the <code>ixgbe_low_latency_recv()</code> inside is a typical implementation. This function&#39;s task is straightforward:</p><ol><li>Directly check the NIC registers or Ring Buffer.</li><li>If there are packets, extract them and pass them up to the protocol stack (L3).</li><li>It might find packets belonging to <strong>other Sockets</strong> and handle them along the way (since it&#39;s already down here, grabbing an extra one costs little).</li><li>Return the number of packets processed. If there are no packets, return 0.</li></ol><p>If the driver doesn&#39;t implement this callback, or returns 0, the kernel falls back to the traditional flow. It won&#39;t crash; it simply reverts to normal mode.</p><h4 id="one-more-detail-preventing-near-misses" tabindex="-1">One More Detail: Preventing &quot;Near Misses&quot; <a class="header-anchor" href="#one-more-detail-preventing-near-misses" aria-label="Permalink to &quot;One More Detail: Preventing &quot;Near Misses&quot;&quot;">​</a></h4><p>There is a subtle timing issue here.</p><p>Suppose the application just asked the driver &quot;Got any goods?&quot; and the driver said &quot;No&quot; and returned. <strong>A nanosecond later</strong>, the NIC receives a new packet.</p><p>Following traditional logic, the application should now go to sleep and wait for the next interrupt. But this circles right back to the &quot;high latency&quot; problem.</p><p>To avoid this awkward &quot;it arrives right after you leave&quot; scenario, Busy Poll introduces a <strong>fallback polling period</strong>.</p><p>Even if the driver returns no data the first time, the kernel doesn&#39;t give up immediately. Instead, it continues to linger in the driver layer within a <strong>configurable time window</strong>. As soon as a new packet arrives, it gets grabbed instantly.</p><p>That&#39;s why we see a parameter measured in microseconds (µs) in the configuration—it determines how long the application is willing to stubbornly wait in the driver layer for a packet.</p><hr><h3 id="comparison-two-paths-to-the-same-destination" tabindex="-1">Comparison: Two Paths to the Same Destination <a class="header-anchor" href="#comparison-two-paths-to-the-same-destination" aria-label="Permalink to &quot;Comparison: Two Paths to the Same Destination&quot;">​</a></h3><p>Let&#39;s compare the flow differences between these two modes using Figure 14-1. This will give you an intuitive feel for what the CPU is actually doing.</p><p><strong>Figure 14-1 Left: Traditional Receive Flow</strong></p><ol><li>Application checks the receive queue.</li><li>No data, block.</li><li>NIC receives a packet.</li><li>Driver hands the packet to the protocol layer.</li><li>Protocol layer/Socket <strong>wakes up</strong> the application.</li><li>Application gets the data.</li></ol><p>The key here is between steps 2 and 5: <strong>interrupt + context switch</strong>.</p><p><strong>Figure 14-1 Right: Busy Poll Receive Flow</strong></p><ol><li>Application checks the receive queue.</li><li><strong>Goes straight down to the driver layer</strong> to check for pending packets (polling begins).</li><li>Meanwhile, the NIC receives a packet.</li><li>Driver processes this pending packet.</li><li>Driver hands the packet to the protocol layer.</li><li>Application gets the data.</li></ol><p>Notice the right side:</p><ul><li><strong>No</strong> wake-up process in step 5 (the application stays awake the whole time).</li><li><strong>No</strong> interrupt handling (the application polls proactively).</li><li>Bypasses context switch and interrupt overhead.</li></ul><p>This mode can deliver latency approaching hardware limits. But the cost is: <strong>CPU utilization will shoot straight up</strong>. If many Sockets have Busy Poll enabled and they compete for polling time on the same CPU core, performance can actually degrade due to resource contention.</p><hr><h3 id="enabling-globally-using-a-sledgehammer-to-crack-a-nut" tabindex="-1">Enabling Globally: Using a Sledgehammer to Crack a Nut <a class="header-anchor" href="#enabling-globally-using-a-sledgehammer-to-crack-a-nut" aria-label="Permalink to &quot;Enabling Globally: Using a Sledgehammer to Crack a Nut&quot;">​</a></h3><p>To put all Sockets in the system into this &quot;combat mode,&quot; you can directly modify kernel parameters.</p><p>There are two main <code>/proc/sys/net/core/</code> parameters:</p><ul><li><code>busy_read</code>: Controls the Busy Poll duration (in microseconds) during the <code>read()</code> system call.</li><li><code>busy_poll</code>: Controls the Busy Poll duration during the <code>select()</code> and <code>poll()</code> system calls.</li></ul><p>By default, they are both 0, meaning disabled.</p><p>You can set them to 50 (microseconds). This is a widely recognized starting point that balances latency and CPU consumption.</p><p><strong>There&#39;s a pitfall to watch out for here</strong>:</p><ul><li>For a <strong>blocking read</strong>, <code>busy_read</code> defines how long to stubbornly poll.</li><li>For a <strong>non-blocking read</strong>, if the Socket has Busy Poll enabled, the kernel will only <strong>poll once</strong> before returning to the user. This is because the semantics of a non-blocking call are &quot;don&#39;t wait for me.&quot;</li></ul><hr><h3 id="precision-strike-enabling-on-demand" tabindex="-1">Precision Strike: Enabling On-Demand <a class="header-anchor" href="#precision-strike-enabling-on-demand" aria-label="Permalink to &quot;Precision Strike: Enabling On-Demand&quot;">​</a></h3><p>Enabling globally is a bit brute-force—like using a sledgehammer to crack a nut and turning on all the lights in the house while you&#39;re at it. A more elegant approach is to let the application specify exactly which Sockets need low latency.</p><p>We can achieve this by setting the <code>SO_BUSY_POLL</code> Socket option.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> val </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 微秒</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setsockopt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(sock, SOL_SOCKET, SO_BUSY_POLL, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">val</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">sizeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(val));</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>This step sets the <code>sk_ll_usec</code> field inside the Socket structure (<code>struct sock</code>).</p><p><strong>Configuration advice</strong>: If you use <code>SO_BUSY_POLL</code>, we recommend setting the global <code>sysctl.net.busy_read</code> to 0, leaving control entirely to the application. This way, other unrelated applications and services on the system can still follow the traditional power-saving path, rather than suffering because of your high-performance requirements.</p><hr><h3 id="tuning-and-configuration-making-it-spin-faster" tabindex="-1">Tuning and Configuration: Making It Spin Faster <a class="header-anchor" href="#tuning-and-configuration-making-it-spin-faster" aria-label="Permalink to &quot;Tuning and Configuration: Making It Spin Faster&quot;">​</a></h3><p>Enabling Busy Poll is just starting the Ferrari; to make it fast on the track, you still need to tune the chassis. Here are a few practical tips:</p><p><strong>1. Interrupt Coalescing</strong></p><p>We recommend using <code>ethtool -C</code> to increase the NIC&#39;s interrupt coalescing time (<code>rx-usecs</code>), for example, to 100. This sounds counterintuitive—we&#39;re chasing low latency, so why delay interrupts?</p><p>Because Busy Poll applications proactively fetch data, the role of interrupts is diminished. If interrupts are too frequent, they cause unnecessary context switch overhead. Lengthening the interrupt coalescing time lets the driver handle packets in batches or wait for the application to poll, which yields better results.</p><p><strong>2. The GRO/LRO Trade-off</strong></p><p>Generic Receive Offload (GRO/LRO) usually improves throughput, but it can cause packet reordering, especially when mixing bulk traffic with low-latency traffic.</p><p>You can try disabling GRO and LRO using <code>ethtool -K</code>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ethtool</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -K</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> eth0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gro</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> off</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lro</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> off</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>But this isn&#39;t a silver bullet; often, keeping them enabled works best, so you have to <strong>test</strong> this.</p><p><strong>3. Affinity and NUMA</strong></p><p>This is the most critical point.</p><ul><li>The <strong>application thread</strong> is pinned to CPU Core A.</li><li>The <strong>NIC IRQ (interrupt handling)</strong> is pinned to CPU Core B.</li></ul><p>These should be <strong>two different cores</strong>. Furthermore, both cores should ideally be on the <strong>same NUMA node</strong> as the NIC device (cross-NUMA memory access is slow).</p><p>If the application and the IRQ contend for the same core, or if they cross NUMA boundaries, latency jitter will be severe. This is especially fatal when <code>rx-usecs</code> is set very low.</p><p><strong>4. IOMMU</strong></p><p>For ultimate low latency, some veterans will suggest turning off the IOMMU (Input/Output Memory Management Unit). The IOMMU exists for security (DMA protection) and virtualization, but it adds a layer of address translation overhead. On some systems, it might be disabled by default; if it&#39;s enabled and you need extreme performance, you can try turning it off (provided you know what you&#39;re doing).</p><hr><h3 id="performance-and-trade-offs" tabindex="-1">Performance and Trade-offs <a class="header-anchor" href="#performance-and-trade-offs" aria-label="Permalink to &quot;Performance and Trade-offs&quot;">​</a></h3><p>What will you see when using Busy Poll?</p><ul><li><strong>Latency</strong> drops significantly.</li><li><strong>Jitter</strong> decreases significantly.</li><li>Transactions per second (TPS) may increase.</li></ul><p>But if you abuse it—enabling Busy Poll for thousands of Sockets—CPU utilization will explode. Because everyone is busy-waiting, the time actually spent doing useful work decreases.</p><p>Remember, this is fundamentally a trade-off that <strong>exchanges CPU resources for time</strong>. In scenarios where low latency is paramount (like HFT or telecom core networks), this deal is worth it; on a regular web server, it might be suicidal.</p><hr><p>In this section, we tore through the &quot;aggressive mode&quot; of the network stack. In the next section, we&#39;ll shift our focus back to the hardware layer and look at the <strong>PCI subsystem and Wake-on-LAN</strong>—another way to wake hardware from its sleep, only this time, it happens through an Ethernet cable.</p>`,86)])])}const g=t(s,[["render",n]]);export{u as __pageData,g as default};
