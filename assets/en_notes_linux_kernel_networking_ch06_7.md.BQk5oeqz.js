import{_ as s,o as i,c as a,a as t}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch06_7.md","filePath":"en/notes/linux_kernel_networking/ch06_7.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_networking/ch06_7.md"};function l(o,e,r,p,h,d){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h3 id="_6-7-policy-routing" tabindex="-1">6.7 Policy Routing <a class="header-anchor" href="#_6-7-policy-routing" aria-label="Permalink to &quot;6.7  Policy Routing&quot;">​</a></h3><p>In the previous section, we discussed multicast—the art of &quot;one-to-many.&quot; In this section, we pull our perspective back and re-examine that seemingly simple action: <strong>table lookup</strong>.</p><p>In traditional routing, a packet is like a passenger who only cares about the destination: regardless of who you are, where you came from, or what marks you carry, as long as you&#39;re heading to the same place, you get put on the same bus.</p><p>But the real world doesn&#39;t work that way. Sometimes we need traffic coming in over a dedicated line to take an expensive, high-quality link, while traffic from the public internet takes a cheap, congested link. Sometimes we need to split traffic based on the TOS field in the IP header. Sometimes we even need to route based on firewall marks.</p><p>This is <strong>Policy Routing</strong>. It breaks the &quot;destination is everything&quot; rule, allowing system administrators to define logic like &quot;if condition A is met, look up table B; if condition C is met, look up table C.&quot;</p><p>In this section, we focus on the IPv4 policy routing implementation (we&#39;ll tackle IPv6 in Chapter 8). To avoid confusion, we&#39;ll refer to entries in policy routing as <strong>rules</strong>, and entries in a routing table as <strong>routes</strong>. Don&#39;t mix them up: rules determine which table to look up, and the table determines where to forward.</p><hr><h4 id="managing-policy-routing" tabindex="-1">Managing Policy Routing <a class="header-anchor" href="#managing-policy-routing" aria-label="Permalink to &quot;Managing Policy Routing&quot;">​</a></h4><p>We&#39;re used to managing routes with the <code>route</code> command, but when it comes to policy routing, <code>route</code> falls short. We need the <code>ip rule</code> command from the <code>iproute2</code> package. This is a completely different management interface.</p><p>Let&#39;s see how to work with these rules.</p><p><strong>Adding a Rule</strong></p><p>Suppose we want all packets with a TOS field of <code>0x04</code> to look up table <code>252</code>. We can issue this command:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rule</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tos</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0x04</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> table</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 252</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>What happens behind the scenes when we hit Enter?</p><p>When we press Enter, the kernel calls the <code>fib_nl_newrule()</code> method in <code>net/core/fib_rules.c</code>. It inserts a new rule into our rule chain: from now on, whenever a packet arrives with a TOS matching <code>0x04</code>, skip the default <code>main</code> table and look up <code>table 252</code> instead.</p><p>Rules alone aren&#39;t enough; table <code>252</code> needs actual routes. We can add routes to table <code>252</code> like this:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> route</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> default</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> via</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.2.10</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> table</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 252</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The <code>tos</code> here is just one of many selectors. We can match on source address, destination address, input interface, firewall mark (fwmark), and more. For a full parameter list, check <code>man 8 ip-rule</code>, or flip to Table 6-1 in the &quot;Quick Reference&quot; at the end of this chapter.</p><p><strong>Deleting a Rule</strong></p><p>Done playing? Just delete it.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rule</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> del</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tos</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0x04</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> table</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 252</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Inside the kernel, <code>fib_nl_delrule()</code> takes over and removes the rule we previously added from the rule chain.</p><p><strong>Viewing Rules</strong></p><p>Want to see what custom rules are currently in the system?</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rule</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 或者</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rule</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> show</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>These two commands are essentially the same thing. They both ultimately call the <code>fib_nl_dumprule()</code> method in the kernel, dumping out all our custom rules for us to inspect.</p><p>Now that we know how to manage them, we have to peel back the kernel and see how this mechanism is actually built.</p><hr><h4 id="kernel-implementation-details" tabindex="-1">Kernel Implementation Details <a class="header-anchor" href="#kernel-implementation-details" aria-label="Permalink to &quot;Kernel Implementation Details&quot;">​</a></h4><p>There&#39;s one point here that can easily cause confusion: there are several files named <code>fib_rules.c</code> in the source tree.</p><ul><li><code>net/core/fib_rules.c</code>: This is the true core infrastructure. It&#39;s not specific to IPv4 or IPv6; it&#39;s a generic framework.</li><li><code>net/ipv4/fib_rules.c</code>: This is the IPv4 implementation of that framework.</li><li><code>net/ipv6/fib6_rules.c</code>: This is the IPv6 implementation.</li></ul><p>We&#39;ll focus on the IPv4 implementation.</p><p><strong>Core Data Structures</strong></p><p>In the IPv4 world, every rule is ultimately mapped to a <code>fib4_rule</code> structure, defined in <code>net/ipv4/fib_rules.c</code>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> fib4_rule {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> fib_rule    common;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    u8            dst_len;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    u8            src_len;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    u8            tos;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __be32            src;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __be32            srcmask;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __be32            dst;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __be32            dstmask;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#ifdef</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CONFIG_IP_ROUTE_CLASSID</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    u32            tclassid;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#endif</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p>We can see that it contains not only the source address (<code>src</code>) and destination address (<code>dst</code>), but also the TOS field. These act as &quot;filters&quot; for matching packets. If a packet&#39;s header information matches these fields, the rule is hit.</p><p><strong>Default Rules at Boot</strong></p><p>Even if we haven&#39;t done anything, our system already has three ironclad rules. During kernel boot, the <code>fib_default_rules_init()</code> method silently creates three default policies:</p><ol><li><strong>Local table (RT_TABLE_LOCAL)</strong>: Specifically handles local addresses (like 127.0.0.1) and broadcast addresses.</li><li><strong>Main table (RT_TABLE_MAIN)</strong>: This is the table we normally see with <code>ip route</code>. All unmarked, regular traffic goes here.</li><li><strong>Default table (RT_TABLE_DEFAULT)</strong>: This is the last resort. If nothing matches above, this table is used (it&#39;s usually empty, or simply forwards to a gateway).</li></ol><p><strong>Lookup Flow: Fast Path and Slow Path</strong></p><p>This is the most elegant part of the entire mechanism. When the kernel needs to find a route for a packet, it calls the <code>fib_lookup()</code> method.</p><p>Inside <code>include/net/ip_fib.h</code>, we&#39;ll see two things that look alike but are actually different <code>fib_lookup()</code> functions. If the kernel configuration doesn&#39;t have <code>CONFIG_IP_MULTIPLE_TABLES</code> enabled (meaning policy routing isn&#39;t used), it takes the simple, single-table lookup logic.</p><p>But once policy routing is enabled, things get interesting. The kernel faces two scenarios:</p><ol><li><strong>Fast path</strong>: The variable <code>net-&gt;ipv4.fib_has_custom_rules</code> is <code>false</code>. This means we&#39;ve never touched the default rules; the system is still in its factory-default state. Since the rules haven&#39;t changed, there&#39;s no need to traverse the rule chain. It directly looks up the tables in order: Local → Main → Default. This saves the overhead of traversing the rules.</li><li><strong>Slow path</strong>: Once we execute <code>ip rule add</code> or <code>ip rule del</code>, <code>fib4_rule_configure()</code> or <code>fib4_rule_delete()</code> sets <code>net-&gt;ipv4.fib_has_custom_rules</code> to <code>true</code>. At this point, the kernel knows things are no longer simple. It calls <code>_fib_lookup()</code>, which leads into <code>fib_rules_lookup()</code>.</li></ol><p>What <code>fib_rules_lookup()</code> does is tedious but necessary: it takes the packet, walks through the rule linked list from head to tail, and calls <code>fib_rule_match()</code> on each rule. If a rule matches, it looks up the routing table specified by that rule; if it doesn&#39;t match, it moves on to the next one.</p><p>This logic may seem clunky, but it&#39;s the price of flexibility. We want freedom, so we pay with CPU cycles.</p><hr><p>That wraps up the framework of policy routing. But with policy routing in place, we can do even fancier things.</p><p>In the next section, we&#39;ll discuss a more specific &quot;advanced trick&quot;—<strong>Multipath Routing</strong>. That is, when a routing table tells us &quot;there are two next hops, both are valid,&quot; how does the kernel choose? Round-robin? Weighted? This is yet another topic balancing performance and efficiency.</p>`,49)])])}const k=s(n,[["render",l]]);export{u as __pageData,k as default};
