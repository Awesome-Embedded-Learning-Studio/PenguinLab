import{_ as e,o as t,c as i,a as n}from"./chunks/framework.8tiMeCK-.js";const h=JSON.parse('{"title":"第 10 章  CPU 调度器（第 1 部分）","description":"","frontmatter":{},"headers":[],"relativePath":"notes/linux_kernel_programming/ch10.md","filePath":"notes/linux_kernel_programming/ch10.md","lastUpdated":null}'),o={name:"notes/linux_kernel_programming/ch10.md"};function a(l,s,r,p,d,c){return t(),i("div",null,[...s[0]||(s[0]=[n(`<h1 id="第-10-章-cpu-调度器-第-1-部分" tabindex="-1">第 10 章 CPU 调度器（第 1 部分） <a class="header-anchor" href="#第-10-章-cpu-调度器-第-1-部分" aria-label="Permalink to &quot;第 10 章  CPU 调度器（第 1 部分）&quot;">​</a></h1><h2 id="章节引子" tabindex="-1">章节引子 <a class="header-anchor" href="#章节引子" aria-label="Permalink to &quot;章节引子&quot;">​</a></h2><p>前两章我们在内存管理的泥潭里摸爬滚打——MGLRU、DAMON、OOM Killer，这些名字现在听起来应该既熟悉又让人头大。我们要么在动态分配内存，要么在拼命回收内存，总之，我们一直在围着内存转。</p><p>现在，让我们换个频道。</p><p>这章和下一章，我们要把注意力从内存移到 CPU 上。准确地说，是 CPU 调度——也就是操作系统决定“谁该在 CPU 上跑，谁该滚下去等待”的那套机制。这不仅是写内核和驱动必须懂的基础，作为一个系统架构师，如果你想让你的用户空间程序在高并发环境下不卡顿、不崩，你也必须理解这块东西。</p><p>我们将从零开始：首先要搞清楚内核到底在调度谁（是进程还是线程？），然后理解 POSIX 定义的那些调度策略到底是个什么玩意儿。接着，我们会拿起 <code>perf</code> 这把“手术刀”，把 CPU 的运行时剖开来看——亲眼看着线程在核心之间跳来跳去。</p><p>在掌握了这些可视化工具之后，我们会像剥洋葱一样剥开内核内部，看看 Linux 是如何通过“模块化调度类”来实现复杂的调度逻辑的。最后，我们将深入代码级细节，搞清楚调度器函数到底是谁在调用、什么时候调用，以及上下文切换到底发生了什么。</p><p>但在我们一头扎进代码之前，有一个经常被误解的事实需要澄清：CPU 是一种共享资源，但这并不意味着只有进程在抢它。Linux 有一个更深层的资源管理框架——cgroups（控制组）。如果你看过前面的图 10.1，你可能会注意到顶部有一个模糊的“cgroups”层。现在先别管它，我们会在下一章专门讲它。但你要知道，最终的调度决策，往往是调度器和 cgroups 共同作用的结果。</p><p>准备好了吗？这趟旅程可能会有点晕车，但绝对值得。</p><hr><h2 id="_10-1-cpu-调度器内核机理-第一部分-必修背景知识" tabindex="-1">10.1 CPU 调度器内核机理（第一部分）：必修背景知识 <a class="header-anchor" href="#_10-1-cpu-调度器内核机理-第一部分-必修背景知识" aria-label="Permalink to &quot;10.1  CPU 调度器内核机理（第一部分）：必修背景知识&quot;">​</a></h2><p>在看复杂的调度算法之前，我们需要先把几个最基础的概念钉死在脑子里。这些是理解后面所有逻辑的地基。</p><h3 id="_10-1-1-linux-的内核可调度实体-kse-到底是哪个" tabindex="-1">10.1.1 Linux 的内核可调度实体（KSE）到底是哪个？ <a class="header-anchor" href="#_10-1-1-linux-的内核可调度实体-kse-到底是哪个" aria-label="Permalink to &quot;10.1.1  Linux 的内核可调度实体（KSE）到底是哪个？&quot;">​</a></h3><p>我们在第 6 章讲进程和线程时提到过，每一个活着的用户模式线程，内核都会给它分配一个 <code>task_struct</code> 结构体，以及一个用户栈和一个内核栈。内核线程虽然也有 <code>task_struct</code>，但它们只有内核栈。</p><p>现在的关键问题是：<strong>当调度器说“该换人干活了”的时候，它换的到底是“谁”？</strong></p><p>在操作系统术语里，这东西叫 <strong>Kernel Schedulable Entity (KSE)</strong>。</p><p>很多人直觉上认为“进程”是调度的基本单位。但在 Linux 上，这个直觉是<strong>错的</strong>。</p><p>Linux 的 KSE 是 <strong>线程</strong>，不是进程（当然，每个进程至少包含一个线程）。调度器是以线程为粒度进行操作的。</p><p>让我们用一个假想的场景来“锚定”这个认知。</p><p>假设你的系统只有 <strong>1 个 CPU 核心</strong>，上面跑了 3 个用户空间进程：</p><ul><li><strong>P1</strong>：只有 1 个线程。</li><li><strong>P2</strong>：有 2 个线程。</li><li><strong>P3</strong>：有 5 个线程。</li></ul><p>除此之外，内核里还跑着 3 个内核线程。</p><p>那么，现在有多少个实体在抢这一个 CPU 核心？</p><p>不是 3（进程数），也不是 6（进程+内核线程数）。 答案是 <strong>1 + 2 + 5 + 3 = 11 个线程</strong>。</p><p>这 11 个线程，如果都处于“可运行”状态（也就是 Ready），它们就会在 CPU 的“运行队列”里打架。至于它们属于哪个进程，调度器并不关心。调度器眼里只有线程。</p><blockquote><p><strong>类比时刻 1：建立映射</strong> 你可以把调度器想象成一个<strong>交通指挥员</strong>。 进程就是<strong>车队</strong>，而线程是车队里的<strong>每一辆车</strong>。 调度员并不指挥“车队”走，他指挥的是“每一辆车”。一个车队可能有一辆车，也可能有十辆车，但这跟调度员没关系，他只看车。</p><p><strong>类比时刻 2：揭示距离</strong> 但这个类比在<strong>优先级</strong>上会失效。 在现实交通中，车队里的车通常是一起走的。 但在 Linux 里，同一个进程（车队）里的不同线程（车），可以一个在高速上狂奔（高优先级），另一个在路边趴窝（低优先级）。它们在调度器眼里是独立的个体，没什么“兄弟情义”。</p><p><strong>类比时刻 3：回收验证</strong> 回到那个交通指挥员。 当我们说“系统负载很高”时，实际上是指马路上有 11 辆车在抢道（11 个线程），而不是只有 3 个车队在跑。 如果 P3 车队里的 5 辆车都在疯狂踩油门，那调度员就会一直盯着这 5 辆车，而根本不管 P1 车队那辆孤零零的车。</p></blockquote><p>现在我们搞清楚了：<strong>KSE 是线程</strong>。所以接下来的讨论中，我们都会用“线程”这个词，而不是“进程”。如果你习惯看老式的 OS 教科书，它们可能还在说“进程状态机”，但现在请你在脑子里自动把它替换成“线程状态机”。</p><h3 id="_10-1-2-linux-进程-线程-状态机" tabindex="-1">10.1.2 Linux 进程（线程）状态机 <a class="header-anchor" href="#_10-1-2-linux-进程-线程-状态机" aria-label="Permalink to &quot;10.1.2  Linux 进程（线程）状态机&quot;">​</a></h3><p>每个 Linux 线程在它的生命周期里，都会在不同的状态之间流转。把这些状态画出来，就是一个状态机。</p><p>既然我们已经确立了 KSE 是线程，那我们接下来就用标准的 Linux 术语来描述这些状态。<code>ps</code> 命令通常用一个字母来表示这些状态，比如 <code>R</code>、<code>S</code>、<code>D</code> 等。</p><p><strong>核心状态列表</strong></p><ol><li><p><strong><code>TASK_RUNNING</code> (R state)</strong>：</p><ul><li><strong>别名</strong>：Ready-to-run 或 Running。</li><li><strong>含义</strong>：这是最容易被误解的状态。它并不代表“正在运行”。它代表**“正在运行”或者“准备好运行”**。</li><li><strong>关键点</strong>：只要线程在这个状态，它就在<strong>运行队列</strong>里。Linux 不会刻意区分“正在 CPU 上跑”和“在队列里排队跑”，这两者都是 <code>R</code>。它们都是候选人，随时可能上 CPU。</li></ul></li><li><p><strong><code>TASK_INTERRUPTIBLE</code> (S state)</strong>：</p><ul><li><strong>别名</strong>：可中断睡眠。</li><li><strong>含义</strong>：线程在睡觉（等待某个事件，比如网络包、磁盘 IO）。</li><li><strong>特征</strong>：虽然是“睡眠”，但它是可以被打扰的。如果这时候给它发个信号（Signal），它会被强行唤醒去处理信号。</li></ul></li><li><p><strong><code>TASK_UNINTERRUPTIBLE</code> (D state)</strong>：</p><ul><li><strong>别名</strong>：不可中断睡眠。</li><li><strong>含义</strong>：线程在睡觉，而且<strong>雷打不动</strong>。</li><li><strong>特征</strong>：通常用于极其关键的 IO 等待，这时候不能被信号干扰。如果系统中出现大量 <code>D</code> 状态的进程，通常意味着存储系统挂了或者很慢。这也是为什么 <code>ps</code> 里它显示为 <code>D</code>（Dead？不，是 Disk Wait）。</li></ul></li><li><p><strong><code>TASK_STOPPED</code> (T state)</strong>：</p><ul><li><strong>含义</strong>：线程被暂停了。通常是收到了 <code>SIGSTOP</code> 信号，或者正在被调试器跟踪。</li></ul></li><li><p><strong><code>TASK_DEAD / TASK_ZOMBIE</code> (X / Z state)</strong>：</p><ul><li><strong>含义</strong>：线程已经挂了，或者变成了僵尸。</li><li><strong>僵尸</strong>：线程执行完了，父进程还没来给它“收尸”（调用 <code>wait()</code>）。这是一种半死不活的状态，必须在系统中被清理掉。</li></ul></li></ol><p><strong>状态的动态流转</strong></p><p>当一个线程刚被 <code>fork()</code> 或 <code>pthread_create()</code> 造出来后，内核会把它设为 <code>TASK_RUNNING</code>（<code>R</code> 状态）。这时它进了运行队列，时刻准备着被调度器选中。</p><p>如果这个线程需要等待某个资源（比如读文件），它就会调用调度器相关的睡眠函数，把自己的状态从 <code>R</code> 改成 <code>S</code> 或 <code>D</code>，并从运行队列里拿出来，放到一个<strong>等待队列</strong>里。</p><p>当它等待的事件发生了（比如磁盘数据读上来了），内核会把它“唤醒”。这实际上做了两件事：</p><ol><li>把它从等待队列里摘掉。</li><li>把它重新挂回 CPU 的运行队列，状态改回 <code>TASK_RUNNING</code>。</li></ol><p>注意，被唤醒并不意味着<strong>立刻</strong>就在 CPU 上跑了。它只是又变成了 <code>R</code> 状态的候选人，得排队等调度器翻它的牌子。</p><p><strong>代码视角：<code>__state</code></strong></p><p>在代码层面，这一切是怎么实现的？答案在 <code>task_struct</code> 结构体里。</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> task_struct {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __state;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // 注意：以前叫 &#39;state&#39;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>这个成员 <code>__state</code> 就是一个整数位掩码。当它是 <code>TASK_RUNNING</code> 时，线程就在跑；当它是 <code>TASK_INTERRUPTIBLE</code> 时，线程就在睡。</p><blockquote><p><strong>⚠️ 踩坑预警</strong> 千万别以为系统里只有一个全局的运行队列，也千万别以为只有一个等待队列。 <strong>Linux 为每个 CPU 核心都维护了一个独立的运行队列。</strong> 等待队列则是到处都是，每个驱动程序、内核模块想用就能建一个。</p></blockquote><p>有了这些背景知识，我们接下来看看 Linux 具体支持哪些调度策略——也就是，它是根据什么规则从运行队列里挑人的。</p><h3 id="_10-1-3-posix-调度策略" tabindex="-1">10.1.3 POSIX 调度策略 <a class="header-anchor" href="#_10-1-3-posix-调度策略" aria-label="Permalink to &quot;10.1.3  POSIX 调度策略&quot;">​</a></h3><p>你可以把“调度策略”大致理解为“选拔人才的算法”。</p><p>Linux 并不是只有一种调度算法。POSIX 标准强制要求任何合规的操作系统至少得实现三种策略。Linux 不光实现了这三种，还加了几种自己的，并且通过<strong>模块化调度类</strong>的设计把它们完美地融合在了一起。</p><p>首先，我们需要建立一个大前提：<strong>系统里的每一个线程，在任何一个时刻，都必然绑定着一种特定的调度策略。</strong> 这个策略是可以动态改的。</p><p>下面这个表是 Linux 调度策略的速查表，建议你先扫一眼，有点印象，我们后面会挑重点细讲。</p><table tabindex="0"><thead><tr><th style="text-align:left;">调度策略</th><th style="text-align:left;">关键特性</th><th style="text-align:left;">优先级范围</th></tr></thead><tbody><tr><td style="text-align:left;"><strong><code>SCHED_OTHER</code></strong> <br> (也叫 <code>SCHED_NORMAL</code>)</td><td style="text-align:left;"><strong>默认策略</strong>。它是“普通人”的策略，追求的是<strong>公平</strong>和<strong>整体吞吐量</strong>。它属于 CFS（完全公平调度器）类。</td><td style="text-align:left;">Nice 值：<br> -20 (最高) ~ +19 (最低) <br> 默认为 0</td></tr><tr><td style="text-align:left;"><strong><code>SCHED_FIFO</code></strong></td><td style="text-align:left;"><strong>非常激进的实时策略</strong>。一旦上了 CPU，除非自己愿意下来（阻塞/退出），或者被更高优先级的线程踹下去，否则它<strong>一直占着</strong>，时间片无限大。</td><td style="text-align:left;">实时优先级：<br> 1 ~ 99 <br> 数字越大越猛</td></tr><tr><td style="text-align:left;"><strong><code>SCHED_RR</code></strong></td><td style="text-align:left;"><strong>轮转式实时策略</strong>。跟 <code>FIFO</code> 一样猛，但它有<strong>时间片</strong>（默认 100ms）。时间片用完了，如果优先级相同，就换人玩，大家轮流上。</td><td style="text-align:left;">实时优先级：<br> 1 ~ 99</td></tr><tr><td style="text-align:left;"><strong><code>SCHED_BATCH</code></strong></td><td style="text-align:left;"><strong>适合批处理任务</strong>。适合那种不跟人交互、闷头计算的程序。内核会尽量减少对它的抢占，以此来提高吞吐量。</td><td style="text-align:left;">Nice 值：-20 ~ +19</td></tr><tr><td style="text-align:left;"><strong><code>SCHED_IDLE</code></strong></td><td style="text-align:left;"><strong>极其卑微的策略</strong>。优先级比 Nice 19 还低。只有当 CPU 没事儿干的时候，才会让它跑跑。PID 0 的 <code>swapper</code> 空闲线程就是这类。</td><td style="text-align:left;">最低（比 nice 19 还低）</td></tr></tbody></table><p><strong>表 10.1：Linux（POSIX 兼容）调度策略一览</strong></p><blockquote><p><strong>⚠️ 概念区分：硬实时 vs 软实时</strong> 这里的 <code>SCHED_FIFO</code> 和 <code>SCHED_RR</code> 被称为<strong>软实时</strong>策略。Linux 作为一个通用操作系统（GPOS），默认<strong>不是</strong>硬实时操作系统（RTOS）。 “硬实时”意味着“万无一失，必须在 deadline 前做完”，而“软实时”意味着“尽量快，但不保命”。 你可以通过打补丁把 Linux 变成硬实时系统，但我们通常跑的都是通用版。</p></blockquote><p><strong>深入细节：<code>SCHED_FIFO</code> vs <code>SCHED_RR</code></strong></p><p>虽然它们都是实时策略，但有个微妙的区别，这在使用中非常关键：</p><ul><li><strong><code>SCHED_FIFO</code></strong>：<strong>没有时间片概念</strong>。如果两个 <code>FIFO</code> 线程优先级相同（比如都是 50），那么先跑的那个会一直跑，直到它主动阻塞或者被高优先级打断。另一个同优先级的线程只能干瞪眼。<strong>后果</strong>：如果你设计不好，同一个优先级下可能会有线程饿死。</li><li><strong><code>SCHED_RR</code></strong>：<strong>有时间片</strong>（Round Robin）。同优先级的线程大家轮流上，时间片到了就换下一个。这比 <code>FIFO</code> 友好一点。</li></ul><p><strong>中断的至高地位</strong></p><p>这里必须插入一个“安全刹车”机制。</p><p>不管你的线程是 <code>SCHED_FIFO</code> 优先级 99（理论上最高），<strong>硬件中断和软件中断永远比它高</strong>。中断来了，你的线程也得乖乖让路。这是系统稳定性的最后一道防线。</p><p><strong>线程优先级的层级</strong></p><p>现在我们把视角拉高，看看 Linux 整个优先级的阶梯图（见图 10.3）。</p><ol><li><strong>实时优先级 (1 ~ 99)</strong>： <ul><li>给 <code>SCHED_FIFO</code> 和 <code>SCHED_RR</code> 用的。</li><li><strong>规则</strong>：只要有一个实时线程在运行队列里，所有的 <code>SCHED_OTHER</code>（普通线程）都靠边站。它们不在一个竞技场里。</li></ul></li><li><strong>普通优先级 (Nice 值: -20 ~ +19)</strong>： <ul><li>给 <code>SCHED_OTHER</code> 用的。</li><li>Nice 值越小，优先级越高。0 是默认值。</li><li>这就是个“好人卡”系统：你越“Nice”（大度），你让 CPU 的次数就越多（优先级越低）。</li></ul></li></ol><blockquote><p><strong>⚠️ 实战经验：Autogroups 的干扰</strong> 很多现代发行版开启了 <code>CONFIG_SCHED_AUTOGROUP</code>。这会利用 cgroups 技术给终端窗口下的进程“加成”优先级。这会导致传统的 <code>nice</code> 值看起来失效了。如果你在测试时发现 nice 值不起作用，查一下这个内核选项。</p></blockquote><p>现在我们知道了有哪些策略。接下来，我们需要直观地看一看，这些策略到底是如何在 CPU 上运转的。</p><h3 id="_10-1-4-可视化调度流" tabindex="-1">10.1.4 可视化调度流 <a class="header-anchor" href="#_10-1-4-可视化调度流" aria-label="Permalink to &quot;10.1.4  可视化调度流&quot;">​</a></h3><p>多核系统让线程可以在不同的 CPU 上并行执行。这提高了吞吐量，但也带来了调试的噩梦。如果几个线程同时操作一块共享内存，谁先谁后？这很难肉眼看出来。</p><p>我们需要一种方法，能像看回放一样，看到哪个线程在哪个时刻占了哪个 CPU 核心。</p><p>这里有两个工具：一个是简单的 GUI 工具 <code>gnome-system-monitor</code>，另一个是内功深厚的 <code>perf</code>。</p><h4 id="方法一-使用-gnome-system-monitor" tabindex="-1">方法一：使用 <code>gnome-system-monitor</code> <a class="header-anchor" href="#方法一-使用-gnome-system-monitor" aria-label="Permalink to &quot;方法一：使用 \`gnome-system-monitor\`&quot;">​</a></h4><p>这是 GNOME 桌面环境自带的系统监控工具。</p><p>为了看到有趣的现象，我们需要制造点“负载”。我们将运行三个 CPU 密集型的进程，并手动把它们绑死在不同的 CPU 核心上。</p><p>这里我们要用到一个强力工具：<code>taskset</code>（属于 util-linux 包）。</p><p><strong>实验步骤</strong></p><p>假设你的系统有 6 个核心（编号 0 到 5）。</p><ol><li>在 CPU 1 上跑一个 <code>yes</code> 进程（这是一个无限循环打印 &#39;y&#39; 的程序，CPU 杀手）：<div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">taskset</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> yes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /dev/null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div></li><li>同理，在 CPU 2 和 CPU 3 上也各跑一个：<div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">taskset</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> yes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /dev/null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">taskset</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> yes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /dev/null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div></li></ol><p>现在打开 <code>gnome-system-monitor</code>，点击“资源”标签页。</p><p>你会看到类似图 10.4 的画面：CPU 2、3、4（注意 GUI 显示编号通常从 1 开始，所以对应核心 1、2、3）的负载直接拉满到 100%。这就是直观的并发和并行。</p><p>用 <code>pkill yes</code> 可以结束这场闹剧。</p><h4 id="方法二-使用-perf-工具" tabindex="-1">方法二：使用 <code>perf</code> 工具 <a class="header-anchor" href="#方法二-使用-perf-工具" aria-label="Permalink to &quot;方法二：使用 \`perf\` 工具&quot;">​</a></h4><p>如果你觉得 <code>gnome-system-monitor</code> 太玩具，那 <code>perf</code> 绝对能满足你的极客心。它是 Linux 内核自带的性能分析神器。</p><blockquote><p><strong>⚠️ 前置条件</strong><code>perf</code> 是跟内核版本强绑定的。在 Ubuntu 上你需要装 <code>linux-tools-$(uname -r)</code>。 如果你编译的是自己魔改过的内核（比如书里自带的 6.1），可能没有对应的 perf 包。建议用发行版自带的内核来做这个实验。</p></blockquote><p><strong>1. 命令行模式：<code>perf sched map</code></strong></p><p>还是刚才的实验场景。为了方便，书里提供了一个脚本 <code>ch10/concurrent_exercise/concurrency</code>，它可以一次性在所有核心上起 <code>yes</code> 进程。</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ch10/concurrent_exercise</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./concurrency</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 6</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # 在 6 个核心上各起一个 yes</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>在另一个终端窗口，开始录制调度事件：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> perf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sched</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> record</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>让它跑个十几秒，然后按 <code>Ctrl+C</code> 停止。这会生成一个名为 <code>perf.data</code> 的二进制文件。</p><p>现在，让我们把它变成人能看懂的“地图”：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> perf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sched</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> map</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mymap.txt</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>打开 <code>mymap.txt</code>，你会看到一堆字符流。这看起来像乱码，但仔细看非常有规律（参考图 10.5）。</p><ul><li>每一行代表一个时间点。</li><li>每一列代表一个 CPU 核心。</li><li>两个字符的代号代表线程。比如 <code>A0</code> 是迁移线程，<code>B0</code> 是我们的 <code>yes</code> 线程。</li></ul><p>最震撼的是，如果你用 vim 搜索 <code>B0..D0..F0...</code>，你会发现这几个字符<strong>在同一行</strong>同时出现（如图 10.6）。这证明了什么？证明了它们真的在并行运行！</p><p><strong>2. 图形化模式：<code>perf timechart</code></strong></p><p>如果字符流看着眼晕，<code>perf</code> 还能生成 SVG 矢量图，用浏览器打开，视觉效果拉满。</p><p>命令如下：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> perf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> timechart</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./perf.data</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>这会生成一个 <code>output.svg</code> 文件。用 Firefox 或 Chrome 打开它（如图 10.7 和 10.8）。</p><p>你会看到彩色的条形图。</p><ul><li><strong>蓝色</strong>代表 <code>Running</code> 状态。</li><li>你会清晰地看到我们的 <code>yes</code> 进程像长城一样铺在 CPU 核心上，没完没了地跑。</li><li>下面还穿插着其他进程，大部分是灰色的（睡眠状态）。</li></ul><p>这种可视化的能力对于分析性能瓶颈（比如为什么我的应用一秒卡顿了两次）简直是救命稻草。</p><hr><p>有了这些直观的感受和背景知识，我们现在可以潜入内核深处，看看这套复杂的机制到底是怎么设计出来的。下一节，我们将深入剖析 <strong>模块化调度类</strong>。</p><hr><h2 id="练习题" tabindex="-1">练习题 <a class="header-anchor" href="#练习题" aria-label="Permalink to &quot;练习题&quot;">​</a></h2><h3 id="练习-1-understanding" tabindex="-1">练习 1：understanding <a class="header-anchor" href="#练习-1-understanding" aria-label="Permalink to &quot;练习 1：understanding&quot;">​</a></h3><p><strong>题目</strong>：在 Linux 调度器的状态机模型中，TASK_UNINTERRUPTIBLE (D 状态) 和 TASK_INTERRUPTIBLE (S 状态) 都表示进程处于睡眠状态。请从系统管理的角度分析：为什么 Linux 需要 D 状态？如果系统出现大量处于 D 状态的进程且无法恢复，通常暗示了什么系统层面的问题？</p><details><summary>答案与解析</summary><p><strong>答案</strong>：D 状态用于处理不可中断的睡眠，通常应用于等待关键 I/O 操作（如磁盘 swap 写入）的场景，以防止进程在数据写入过程中被信号中断而导致数据损坏。如果系统出现大量且无法恢复的 D 状态进程，通常暗示底层存储子系统（如 NFS 挂载无响应、磁盘故障或由于 I/O 栈拥塞导致的“卡死”）出现了严重的阻塞。</p><p><strong>解析</strong>：本题考察对进程状态机中 S 和 D 状态区别的理解。</p><ol><li><strong>概念区分</strong>：S 状态允许被信号唤醒（如用户按 Ctrl+C），适用于大多数等待；D 状态忽略信号，必须等待等待的事件（如硬件 I/O 完成）发生。</li><li><strong>设计意图</strong>：D 状态保证了某些关键操作（如写入磁盘数据结构）的原子性，防止进程在持有锁或操作硬件关键路径时被意外终止。</li><li><strong>故障排查</strong>：D 状态通常是短暂的。如果进程永久处于 D 状态，意味着它在等待的资源永远无法就绪（例如服务器宕机导致网络 I/O 挂起），这被称为“ uninterruptible sleep deadlock”，是系统管理员需要高度警惕的故障信号。</li></ol></details><h3 id="练习-2-application" tabindex="-1">练习 2：application <a class="header-anchor" href="#练习-2-application" aria-label="Permalink to &quot;练习 2：application&quot;">​</a></h3><p><strong>题目</strong>：假设你正在开发一个高性能计算应用，其中一个线程需要进行密集的数学运算。默认情况下，该线程使用 SCHED_OTHER 策略。为了保证该线程获得最大的 CPU 时间片且尽可能减少被其他普通进程抢占，在不使用实时策略（SCHED_FIFO/RR）的前提下，你应该如何调整其 Nice 值？对应的命令是什么？</p><details><summary>答案与解析</summary><p><strong>答案</strong>：应将 Nice 值调整为 -20（最高优先级）。可以使用命令 <code>nice -n -20 ./your_program</code> 启动程序，或使用 <code>renice -n -20 -p &lt;PID&gt;</code> 修改运行中进程的 Nice 值。</p><p><strong>解析</strong>：本题考察对 SCHED_OTHER 策略中 Nice 值的实际应用。</p><ol><li><strong>策略限制</strong>：SCHED_OTHER 是 Linux 普通进程的默认策略，基于 CFS 算法。它与实时进程（优先级 1-99）不同，无法通过改变策略来抢占。</li><li><strong>Nice 值范围</strong>：-20 到 +19。-20 代表最高优先级，+19 代表最低。</li><li><strong>应用场景</strong>：对于 CPU 密集型任务，将 Nice 值设为 -20 会让 CFS 调度器在计算 vruntime（虚拟运行时间）时给予该任务更高的权重（更小的 vruntime 增长），从而使其在红黑树中更倾向于被调度，减少被其他低优先级（Nice 值大）任务抢占的频率。</li></ol></details><h3 id="练习-3-application" tabindex="-1">练习 3：application <a class="header-anchor" href="#练习-3-application" aria-label="Permalink to &quot;练习 3：application&quot;">​</a></h3><p><strong>题目</strong>：在一个单核 CPU 系统中，有两个线程：</p><ul><li>线程 A：SCHED_FIFO 策略，优先级 10，执行一个无限循环（<code>while(1);</code>）。</li><li>线程 B：SCHED_FIFO 策略，优先级 5，试图计算 1+1。</li><li>系统中还有若干中断服务程序（ISR）在运行。</li></ul><p>请问：线程 B 能有机会运行吗？中断服务程序能运行吗？请详细解释调度器的行为。</p><details><summary>答案与解析</summary><p><strong>答案</strong>：线程 B 没有机会运行（直到线程 A 主动让出 CPU 或退出）。中断服务程序（ISR）可以正常运行并抢占线程 A。</p><p><strong>解析</strong>：本题考察对 SCHED_FIFO 抢占模型和中断优先级的理解。</p><ol><li><strong>线程间调度</strong>：SCHED_FIFO 是实时抢占策略。线程 A (Prio 10) 的优先级高于线程 B (Prio 5)。根据规则，只有当高优先级任务（A）阻塞（I/O）、退出或主动放弃 CPU 时，低优先级任务（B）才能运行。由于 A 是无限循环且无阻塞，B 将永久饥饿。</li><li><strong>中断与进程的优先级</strong>：Linux 内核的中断（硬件和软中断）优先级高于所有进程（包括 SCHED_FIFO 的优先级 99）。因此，尽管 A 占据了 CPU，当硬件中断发生时（如时钟中断或网络包到达），CPU 仍然会暂停线程 A，转而去执行 ISR。ISR 执行完毕后，才会恢复线程 A 的执行。</li><li><strong>结论</strong>：B 饥饿，中断不受影响。这就是为什么编写 SCHED_FIFO 程序时必须小心，避免死循环导致系统软死机（忽略干预的话）。</li></ol></details><h3 id="练习-4-thinking" tabindex="-1">练习 4：thinking <a class="header-anchor" href="#练习-4-thinking" aria-label="Permalink to &quot;练习 4：thinking&quot;">​</a></h3><p><strong>题目</strong>：Linux 内核源码中有一个被称为“黄金法则”的调度器设计原则：<strong>“调度器代码永远不能在原子上下文（包括中断上下文）中运行。”</strong> 请思考：如果打破这个原则，允许在持有自旋锁或处于中断上下文时调用 <code>schedule()</code> 进行进程切换，会发生什么灾难性的后果？请结合中断处理和锁机制进行深度分析。</p><details><summary>答案与解析</summary><p><strong>答案</strong>：如果打破该原则，会导致“双重获取锁”或死锁，甚至导致系统彻底死锁或内核崩溃。核心原因是 <code>schedule()</code> 会触发 <code>switch_to</code>，切换到另一个可能也试图获取同一个自旋锁的进程，或者在同一 CPU 上再次触发中断。</p><p><strong>解析</strong>：本题考察对内核调度深层原理和并发控制的综合理解。</p><ol><li><strong>原子上下文的定义</strong>：原子上下文意味着内核正在执行临界区代码（持有自旋锁 <code>spinlock</code>）或正在处理中断。此时不能睡眠，不能发生进程调度。</li><li><strong>死锁场景分析（自旋锁视角）</strong>： <ul><li>假设进程 P1 在 CPU0 上持有自旋锁 L（此时禁止抢占），进入临界区。</li><li>如果允许在持有锁时调用 <code>schedule()</code>，调度器将 P1 移出运行队列，换入进程 P2。</li><li>如果 P2（在 CPU0 上运行）也试图获取锁 L，它将自旋等待 P1 释放锁。</li><li>然而，P1 只有在被重新调度回 CPU0 并执行时才能释放锁。</li><li><strong>结果</strong>：P2 等待 P1，P1 等待 P2（调度器资源），形成死锁。系统将失去响应。</li></ul></li><li><strong>中断上下文分析</strong>： <ul><li>中断处理程序（ISR）并不属于任何进程，它是“借”当前进程的栈和资源运行的。</li><li>如果 ISR 中调用 <code>schedule()</code>，内核将尝试保存“中断上下文”并恢复“进程上下文”。这不仅是逻辑混乱（谁在中断里睡觉？），而且如果中断再次发生（递归中断），内核栈可能溢出或状态完全错乱。</li></ul></li><li><strong>设计哲学</strong>：因此，Linux 强制要求所有可能引起睡眠（进而调用调度器）的函数路径都必须是非原子上下文，这是保证内核并发安全的最基本底线。</li></ol></details><hr><h2 id="要点提炼" tabindex="-1">要点提炼 <a class="header-anchor" href="#要点提炼" aria-label="Permalink to &quot;要点提炼&quot;">​</a></h2><p>Linux 的内核可调度实体（KSE）是线程而非进程，调度器以线程为粒度进行操作，同一个进程内的不同线程在调度器眼中是完全独立的个体，没有任何“兄弟情义”或必然的同步关系。</p><p>理解线程状态流转的关键在于区分 <code>TASK_RUNNING</code> 与实际执行的区别，<code>R</code> 状态仅代表线程处于运行队列中（正在执行或准备好执行），而非 <code>R</code> 状态（如 <code>S</code> 或 <code>D</code>）则意味着线程已放弃 CPU 并进入等待队列，唤醒操作仅是将其重新放回运行队列，并不代表立即获得 CPU。</p><p>Linux 将线程优先级严格划分为实时（1-99）和普通两类，只要运行队列中存在实时线程，普通线程无论 Nice 值如何都会被靠边站，且即便是最高优先级的实时线程也必须服从硬件中断和软中断的绝对权威。</p><p>虽然 <code>SCHED_FIFO</code> 和 <code>SCHED_RR</code> 均为软实时策略，但前者一旦占用 CPU 只要自身不阻塞或被更高优先级抢占就会一直运行，容易导致同优先级线程饿死，而后者引入了时间片机制，保证了同优先级线程间的公平轮转。</p><p>利用 <code>perf sched map</code> 和 <code>perf timechart</code> 等工具可以将 CPU 的运行时数据可视化为字符地图或 SVG 图谱，这不仅能直观验证多核并行性（不同核心同时运行线程），更是诊断复杂调度场景和性能瓶颈的“手术刀”。</p>`,124)])])}const u=e(o,[["render",a]]);export{h as __pageData,u as default};
