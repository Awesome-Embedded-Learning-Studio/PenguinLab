import{_ as s,o as t,c as i,a}from"./chunks/framework.8tiMeCK-.js";const k=JSON.parse('{"title":"4.1  IPv4 Header and Protocol Registration","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch04_1.md","filePath":"en/notes/linux_kernel_networking/ch04_1.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_networking/ch04_1.md"};function o(r,e,l,p,h,d){return t(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="_4-1-ipv4-header-and-protocol-registration" tabindex="-1">4.1 IPv4 Header and Protocol Registration <a class="header-anchor" href="#_4-1-ipv4-header-and-protocol-registration" aria-label="Permalink to &quot;4.1  IPv4 Header and Protocol Registration&quot;">​</a></h1><p>There is a category of problems that appear to be &quot;misconfigurations&quot; on the surface, but are actually &quot;structural misunderstandings.&quot;</p><p>The problem we tackle in this chapter is exactly that kind. You might think you know the IP header — source address, destination address, checksum, nothing more — but when you stand face-to-face with the kernel code, trying to send a crafted packet through a Raw Socket or analyzing a strange fragmentation fault in a packet capture, you&#39;ll find that the textbook&#39;s 20-byte definition hides many overlooked details.</p><p>The mission of this chapter is to lay those details bare. We will start from every single bit of the IPv4 header, dissecting this protocol header nerve by nerve like a frog in biology class. This is not only the foundation for understanding <code>tcpdump</code> output, but also a necessary prerequisite for understanding subsequent Netfilter filtering, route lookup, and fragment reassembly.</p><p>Ready? Let&#39;s start with that most familiar &quot;face.&quot;</p><hr><h3 id="ipv4-header" tabindex="-1">IPv4 Header <a class="header-anchor" href="#ipv4-header" aria-label="Permalink to &quot;IPv4 Header&quot;">​</a></h3><p>If the network is a postal system, then the IPv4 header is the envelope. But this is a very special envelope: its format is fixed, yet its length is variable; it doesn&#39;t just tell you where the letter is from and where it&#39;s going, it also tells the intermediate sorters (routers) how urgent it is (TOS), whether it can be torn apart and mailed (DF), and if it is torn apart, which piece belongs to this fragment.</p><p>In the eyes of the Linux kernel, this envelope is abstracted into a C struct <code>struct iphdr</code>. It is defined in <code>include/uapi/linux/ip.h</code>.</p><p>Don&#39;t rush to look at the fields just yet — first, look at this diagram (just get a mental impression):</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span> 0               16                              31</span></span>
<span class="line"><span> +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+</span></span>
<span class="line"><span> |Version|  IHL  |    Type of Service    |      Total Length     |</span></span>
<span class="line"><span> +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+</span></span>
<span class="line"><span> |         Identification        |Flags|      Fragment Offset    |</span></span>
<span class="line"><span> +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+</span></span>
<span class="line"><span> |  Time to Live |    Protocol    |         Header Checksum     |</span></span>
<span class="line"><span> +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+</span></span>
<span class="line"><span> |                       Source Address                          |</span></span>
<span class="line"><span> +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+</span></span>
<span class="line"><span> |                    Destination Address                       |</span></span>
<span class="line"><span> +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+</span></span>
<span class="line"><span> |                    Options (optional)                        |</span></span>
<span class="line"><span> +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br></div></div><p>This is the &quot;face&quot; the kernel sees when processing every IP packet. The code-level definition is as follows:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> iphdr {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#if</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> defined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__LITTLE_ENDIAN_BITFIELD</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __u8    ihl:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            version:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#elif</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> defined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__BIG_ENDIAN_BITFIELD</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __u8    version:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            ihl:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#else</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#error</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;Please fix &lt;asm/byteorder.h&gt;&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#endif</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __u8          tos;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __be16        tot_len;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __be16        id;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __be16        frag_off;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __u8          ttl;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __u8          protocol;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __sum16       check;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __be32        saddr;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __be32        daddr;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /*The options start here. */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br></div></div><p>You&#39;ll notice it feels a bit awkward at first glance — <code>version</code> and <code>ihl</code> are crammed into a single byte, and you have to worry about byte order. This is to squeeze every last bit of space. Let&#39;s break it down one by one.</p><h4 id="_1-version-and-header-length-version-ihl" tabindex="-1">1. Version and Header Length (version / ihl) <a class="header-anchor" href="#_1-version-and-header-length-version-ihl" aria-label="Permalink to &quot;1. Version and Header Length (version / ihl)&quot;">​</a></h4><p><strong><code>version</code></strong> is simple: it must be 4. If it isn&#39;t 4, the kernel will immediately drop the packet because it simply doesn&#39;t recognize it.</p><p><strong><code>ihl</code></strong> (Internet Header Length) is the key. It tells us exactly how long this IPv4 header is.</p><p>Here is a counterintuitive point: <strong>the IPv4 header length is not fixed</strong>.</p><p>Unlike IPv6, which is fixed at 40 bytes, the IPv4 header is a minimum of 20 bytes (when there are no options) and a maximum of 60 bytes. Because it counts in 4-byte (32-bit) units, the <code>ihl</code> field stores the &quot;number of units,&quot; not the number of bytes.</p><ul><li>Minimum 20 bytes → <code>ihl</code> = 5</li><li>Maximum 60 bytes → <code>ihl</code> = 15</li></ul><p><strong>Why design it this way?</strong> For compatibility. The IPv4 header is followed by an optional <code>options</code> field. Although rarely used today, network protocol designers back in the day thought that maybe one day we&#39;d need to stuff more routing information or security parameters into the header. This design &quot;parameterized&quot; the length — as long as we have <code>ihl</code>, the kernel can calculate where the true header ends and where the data payload begins.</p><p>Returning to the envelope analogy: <code>ihl</code> tells the sorter how many layers of &quot;envelope paper&quot; this letter has been folded into. If you tear it open without checking this, you might rip the actual letter inside.</p><h4 id="_2-type-of-service-and-congestion-notification-tos-dscp-ecn" tabindex="-1">2. Type of Service and Congestion Notification (tos / DSCP / ECN) <a class="header-anchor" href="#_2-type-of-service-and-congestion-notification-tos-dscp-ecn" aria-label="Permalink to &quot;2. Type of Service and Congestion Notification (tos / DSCP / ECN)&quot;">​</a></h4><p>The 8 bits of <strong><code>tos</code></strong> (Type of Service) have undergone multiple &quot;repurposings&quot; throughout history.</p><ul><li><strong>Originally (RFC 791)</strong>: It was used for QoS. You could mark a packet as &quot;minimize delay,&quot; &quot;maximize throughput,&quot; etc., hoping routers would prioritize it. It&#39;s like stamping &quot;URGENT&quot; on an envelope.</li><li><strong>Later (RFC 2474)</strong>: People found this too coarse, so the first 6 bits were redefined as the <strong>DS Field</strong> (Differentiated Services Field), or <strong>DSCP</strong>. Modern network devices primarily look at these 6 bits for traffic control (such as QoS marking).</li><li><strong>Finally (RFC 3168)</strong>: The remaining 2 bits weren&#39;t left idle either — they were co-opted for <strong>ECN</strong> (Explicit Congestion Notification).</li></ul><p>These 2 bits (bits 6 and 7) are quite interesting. They allow routers to &quot;not fight&quot; (not drop packets) during congestion, but instead mark this bit as 1 to tell the receiver: &quot;Hey, it was a bit congested just now, slow down your sending.&quot; This is an important mechanism in modern networks for combating congestion.</p><h4 id="_3-total-length-tot-len" tabindex="-1">3. Total Length (tot_len) <a class="header-anchor" href="#_3-total-length-tot-len" aria-label="Permalink to &quot;3. Total Length (tot_len)&quot;">​</a></h4><p><strong><code>tot_len</code></strong> is the length of the entire IP packet, including both header and data. It is 16 bits, meaning a maximum of 64KB.</p><p>There&#39;s a pitfall here: <strong>the Ethernet MTU is typically 1500 bytes</strong>. If you send a 3000-byte IP packet, it must be sliced into fragments at the link layer. However, <code>tot_len</code> records the total length <em>before</em> slicing. When the receiver sees these fragments, it relies entirely on this total length to determine whether reassembly is complete.</p><h4 id="_4-identification-and-fragmentation-id-frag-off" tabindex="-1">4. Identification and Fragmentation (id / frag_off) <a class="header-anchor" href="#_4-identification-and-fragmentation-id-frag-off" aria-label="Permalink to &quot;4. Identification and Fragmentation (id / frag_off)&quot;">​</a></h4><p>This is one of the most troublesome parts of IPv4.</p><p><strong><code>id</code></strong> (Identification) is a 16-bit ID. When an IP packet is sliced into multiple fragments, all fragments must share the same <code>id</code>. When the receiver reassembles them, it&#39;s like a jigsaw puzzle — first find all fragments with the same <code>id</code>, then piece them together in order.</p><p><strong><code>frag_off</code></strong> (Fragment Offset) is even more extreme — it has to store both the offset and the flag bits, all crammed into 16 bits:</p><ul><li><strong>Low 13 bits</strong>: The offset. Note that the unit is <strong>8 bytes</strong>. So the offset of the first fragment is 0. If the second fragment starts at byte 1400, the value here is 1400 / 8 = 175.</li><li><strong>High 3 bits</strong>: Flag bits.</li></ul><p>These flag bits are defined as macros in the kernel:</p><ul><li><strong>IP_MF (More Fragments, 0x01)</strong>: Value is 001. Means &quot;there are more siblings.&quot; Except for the last fragment, all other fragments must have this bit set to 1.</li><li><strong>IP_DF (Don&#39;t Fragment, 0x02)</strong>: Value is 010. Means &quot;don&#39;t slice me!&quot; If a packet has the DF flag set but encounters a link with an MTU that is too small along the way, the router will directly drop it and send back an ICMP &quot;Fragmentation Needed&quot; message. This is the cornerstone of <strong>PMTU (Path MTU Discovery)</strong>.</li><li><strong>IP_CE (Congestion, 0x04)</strong>: Value is 100. This is a congestion flag reserved for ECN.</li></ul><blockquote><p><strong>⚠️ Pitfall Warning</strong> Many beginners see a <code>frag_off</code> value of 8192 (decimal) when capturing packets and assume the offset is huge, but this is actually because they&#39;re looking at the DF flag bit (0x2000). When examining fragmentation, always remember to apply a mask operation to strip the high 3 bits and only look at the low 13 bits.</p></blockquote><h4 id="_5-time-to-live-ttl" tabindex="-1">5. Time to Live (ttl) <a class="header-anchor" href="#_5-time-to-live-ttl" aria-label="Permalink to &quot;5. Time to Live (ttl)&quot;">​</a></h4><p><strong><code>ttl</code></strong> (Time To Live) is essentially a &quot;countdown timer.&quot; It is decremented by 1 every time it passes through a router. When it reaches 0, the packet is destroyed.</p><p>This exists to prevent &quot;zombie packets&quot; caused by routing loops from circulating in the network forever. You&#39;ll encounter an interesting ICMP error called <code>ICMP_TIME_EXCEEDED</code> — when you use <code>traceroute</code>, it works by intentionally crafting packets with incrementally increasing TTLs to probe the path.</p><h4 id="_6-protocol-and-checksum-protocol-check" tabindex="-1">6. Protocol and Checksum (protocol / check) <a class="header-anchor" href="#_6-protocol-and-checksum-protocol-check" aria-label="Permalink to &quot;6. Protocol and Checksum (protocol / check)&quot;">​</a></h4><p><strong><code>protocol</code></strong> tells the kernel what is inside this IP packet.</p><ul><li><code>IPPROTO_TCP</code> (6): TCP</li><li><code>IPPROTO_UDP</code> (17): UDP</li><li><code>IPPROTO_ICMP</code> (1): ICMP</li><li><code>IPPROTO_ICMPV6</code> (58): ICMPv6</li><li>...and dozens of others, defined in <code>include/uapi/linux/in.h</code>.</li></ul><p><strong><code>check</code></strong> is the checksum. Note that it <strong>only verifies the header</strong>. If even a single bit is flipped during transmission, the checksum won&#39;t match, and the kernel will drop the packet directly. There&#39;s a detail here: because the TTL changes with every hop, routers must recalculate the checksum when forwarding.</p><h4 id="_7-addresses-saddr-daddr" tabindex="-1">7. Addresses (saddr / daddr) <a class="header-anchor" href="#_7-addresses-saddr-daddr" aria-label="Permalink to &quot;7. Addresses (saddr / daddr)&quot;">​</a></h4><p><strong>32-bit source and destination addresses</strong>. This is the most fundamental routing basis at the network layer.</p><hr><h3 id="protocol-registration-how-does-the-kernel-claim-ipv4-packets" tabindex="-1">Protocol Registration: How Does the Kernel Claim IPv4 Packets? <a class="header-anchor" href="#protocol-registration-how-does-the-kernel-claim-ipv4-packets" aria-label="Permalink to &quot;Protocol Registration: How Does the Kernel Claim IPv4 Packets?&quot;">​</a></h3><p>Now that we&#39;ve seen what the header looks like, we need to take a step back and ask a more fundamental question:</p><p>When a NIC receives a data frame, pulls it up from the DMA buffer, and hands it to the kernel, how does the kernel know this is an IPv4 packet and not ARP or IPv6?</p><p>The answer lies in the Ethernet header&#39;s <code>ethertype</code> field. IPv4&#39;s type is <code>ETH_P_IP</code>.</p><p>The kernel needs a mechanism to bind the number <code>ETH_P_IP</code> to the &quot;IPv4 handler function.&quot; This is exactly what <code>struct packet_type</code> does.</p><p>Here is the real &quot;registration&quot; code, defined in <code>net/ipv4/af_inet.c</code>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> packet_type ip_packet_type __read_mostly </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .type </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> cpu_to_be16</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ETH_P_IP),</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 0x0800</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .func </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ip_rcv,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 // 处理函数指针</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>How does this take effect? When the IPv4 stack initializes:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __init </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">inet_init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ...</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  dev_add_pack</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ip_packet_type);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>The <strong><code>inet_init</code></strong> function does something extremely important: it hangs <code>ip_packet_type</code> onto the kernel&#39;s global protocol processing list (typically the <code>ptype_base</code> hash table).</p><p>From then on, for every packet coming in from the NIC, the kernel will take a look at its Ethernet type.</p><ul><li>If it is <code>ETH_P_IP</code>, the kernel will find <code>ip_packet_type</code> and then call <code>ip_packet_type.func</code> — which is <strong><code>ip_rcv</code></strong>.</li></ul><p><strong>This is the starting point of the IPv4 story.</strong> <code>ip_rcv</code> is like the &quot;customs&quot; of the IPv4 kingdom — all packets must pass through this checkpoint first. It is responsible for verification (is the Version 4? Is the Checksum correct?), distribution (is it for me? Does it need to be forwarded?), and only if everything goes smoothly will it hand the packet to the next stop.</p><p>In the next section, we will stand at the <code>ip_rcv</code> entry point and see how the IPv4 receive path actually runs.</p>`,61)])])}const u=s(n,[["render",o]]);export{k as __pageData,u as default};
