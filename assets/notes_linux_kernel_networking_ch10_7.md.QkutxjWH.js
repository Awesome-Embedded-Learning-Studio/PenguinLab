import{_ as a,o as r,c as n,a as t}from"./chunks/framework.8tiMeCK-.js";const T=JSON.parse('{"title":"10.7  NAT Traversal in IPsec","description":"","frontmatter":{},"headers":[],"relativePath":"notes/linux_kernel_networking/ch10_7.md","filePath":"notes/linux_kernel_networking/ch10_7.md","lastUpdated":null}'),s={name:"notes/linux_kernel_networking/ch10_7.md"};function o(p,e,l,i,c,P){return r(),n("div",null,[...e[0]||(e[0]=[t('<h1 id="_10-7-nat-traversal-in-ipsec" tabindex="-1">10.7 NAT Traversal in IPsec <a class="header-anchor" href="#_10-7-nat-traversal-in-ipsec" aria-label="Permalink to &quot;10.7  NAT Traversal in IPsec&quot;">​</a></h1><p>上一节我们还在享受 <code>xfrm_lookup()</code> 带来的「确定感」——策略匹配了，状态找到了，加密完成了，包发出去了。一切看起来都很美好。</p><p>但现实世界喜欢在这种时候给你一巴掌。</p><p>想象一下：你在家里搭建了一个 IPsec VPN，连接到公司的服务器。在家里，你是 NAT 之后的一台内网机（比如 <code>192.168.1.5</code>），你的路由器有一个公网 IP。当你兴致勃勃地试图建立隧道时，你发现隧道起不来，或者起来了之后马上就断。</p><p>为什么？因为在中间那个负责「翻译」地址的 NAT 设备眼里，你的 ESP 包简直就是天书。</p><hr><h3 id="nat-设备为什么不放过-esp" tabindex="-1">NAT 设备为什么不放过 ESP <a class="header-anchor" href="#nat-设备为什么不放过-esp" aria-label="Permalink to &quot;NAT 设备为什么不放过 ESP&quot;">​</a></h3><p>让我们站在 NAT 设备的视角看问题。NAT 的本职工作很单纯：改写源/目的 IP 地址（有时还有端口），然后修正 TCP 或 UDP 头部的校验和。</p><p>这里的关键词是<strong>修正校验和</strong>。</p><p>传输层（TCP/UDP）计算校验和时，会把 IP 头部的源地址和目的地址也算进去（这叫 Pseudo-Header）。所以，一旦 NAT 改了 IP 地址，它必须同步更新 TCP/UDP 的校验和，否则接收方算出来的数对不上，直接把包扔了。</p><p>在传输模式下，ESP 会把整个 TCP/UDP 头部加密。</p><p>这就尴尬了。</p><p>NAT 设备看着一个 ESP 包：</p><ol><li>它想改 IP 地址。</li><li>它想更新 TCP/UDP 校验和。</li><li>但是 TCP/UDP 头被加密了，它看不懂，更没法算校验和。</li></ol><p><strong>结果</strong>：NAT 设备只能做两件事：要么把包原样转发（但这通常不行，因为内网 IP 出不去），要么——绝大多数情况——直接把包丢弃。这就导致 IPsec 在 NAT 环境下彻底失效。</p><p>但也不是所有协议都有这个问题。比如 SCTP，它的校验和就不覆盖 IP 头部，所以它对 NAT 的变化是「免疫」的。可惜，我们日常用的主要是 TCP 和 UDP。</p><p>为了解决这个问题，IETF 搞了一个标准（RFC 3948），名字起得很直白——<strong>UDP Encapsulation of IPsec ESP Packets</strong>。这就是我们常说的 NAT-T（NAT Traversal）。</p><p>它的核心思想很简单：既然你不认 ESP，那我就把它伪装成你最爱的 UDP。</p><hr><h3 id="nat-t-的游戏规则" tabindex="-1">NAT-T 的游戏规则 <a class="header-anchor" href="#nat-t-的游戏规则" aria-label="Permalink to &quot;NAT-T 的游戏规则&quot;">​</a></h3><p>在深入代码之前，先划几个重点，免得你踩坑：</p><ol><li><strong>只救 ESP，不救 AH</strong>：AH 协议本身会对 IP 头部做完整性校验，一旦 NAT 改了 IP 地址，AH 校验就会失败。所以 NAT-T 只对 ESP 有效。如果你还在用 AH，在 NAT 环境下基本无解。</li><li><strong>拒绝手动密钥</strong>：NAT-T 无法配合手动密钥使用。它必须依赖 IKEv1 或 IKEv2 的协商机制。为什么？因为 NAT-T 的开启需要双方握手确认，这个过程是 IKE 协议的一部分。</li><li><strong>软件层面的妥协</strong>：虽然 NAT-T 是 IPsec 的事，但其实很多 P2P 应用（尤其是 VoIP）也要处理类似的问题。像 STUN、TURN、ICE 这些协议，都是为了解决「在内网里怎么建立连接」的问题。甚至 strongSwan 还实现了类似 TURN 的中继服务，让两个都在 NAT 后面的客户端能通过第三方建立连接。</li></ol><hr><h3 id="⚙️-nat-t-的运作模式" tabindex="-1">⚙️ NAT-T 的运作模式 <a class="header-anchor" href="#⚙️-nat-t-的运作模式" aria-label="Permalink to &quot;⚙️  NAT-T 的运作模式&quot;">​</a></h3><p>NAT-T 的目标很明确：在 IP 头部和 ESP 头部之间，硬塞进去一个 UDP 头部。</p><p>这样，NAT 设备看到的就是一个标准的 UDP 数据包，它能改 IP，能改端口，能算 UDP 校验和，大家皆大欢喜。</p><h4 id="_1-开启与协商" tabindex="-1">1. 开启与协商 <a class="header-anchor" href="#_1-开启与协商" aria-label="Permalink to &quot;1. 开启与协商&quot;">​</a></h4><p>这一步是用户空间的活，但内核得配合。</p><p>在旧一点的软件里（比如 Openswan），你得在 <code>/etc/ipsec.conf</code> 里显式地写一句 <code>nat_traversal=yes</code> 才能激活这个功能。而在 strongSwan 的 IKEv2 守护进程 Charon 里，NAT-T 是默认开启且不能关闭的——作者显然认为现在的网络环境里，NAT 是常态，不支持 NAT-T 就是自绝于人民。</p><p>协商流程大致是这样的：</p><ul><li><p><strong>第一步：确认能力</strong> 在 IKEv1 的主模式阶段，双方会交换 ISAKMP 消息。如果支持 NAT-T，会在 Vendor ID 字段里带上特定的标记（IKEv1）。在 IKEv2 里，这已经是标准的一部分了，不用特意声明。</p></li><li><p><strong>第二步：探测 NAT</strong> 双方互发 NAT-D（NAT Discovery）载荷。这其实是一个探测包：我知道我自己的 IP，我知道我看到的你的 IP，如果这两个值不匹配，或者中间经过的 IP 地址发生了变化，我们就知道中间有 NAT 设备在「搞鬼」。</p></li></ul><p>一旦确认路径上有 NAT，双方就会立刻切换到 NAT-T 模式。</p><h4 id="_2-udp-封装结构" tabindex="-1">2. UDP 封装结构 <a class="header-anchor" href="#_2-udp-封装结构" aria-label="Permalink to &quot;2. UDP 封装结构&quot;">​</a></h4><p>切换模式后，原本长这样的 ESP 包：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[ IP Header ] [ ESP Header ] [ Encrypted Data ] [ ESP Trailer ] [ ICV ]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>变成了这样：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[ IP Header ] [ UDP Header ] [ ESP Header ] [ Encrypted Data ] [ ESP Trailer ] [ ICV ]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><strong>关键细节</strong>：</p><ul><li><strong>端口号</strong>：UDP 头部的源端口和目的端口都是 <strong>4500</strong>。这是 IANA 规定的 NAT-T 专用端口。</li><li><strong>保活机制</strong>：NAT 映射是有超时时间的。如果长时间没有流量，NAT 设备会把映射关系删掉。为了防止这一点，NAT-T 要求每隔 20 秒发一次 Keep-Alive 包。 <ul><li><strong>保活包长什么样</strong>：它就是一个发送到 UDP 4500 端口的包，载荷只有一个字节，值为 <code>0xFF</code>。收到这个包，内核就知道这是在「刷存在」，不需要解密，也不需要往上层递送。</li></ul></li></ul><h4 id="_3-内核态的接收路径-xfrm4-udp-encap-rcv" tabindex="-1">3. 内核态的接收路径（<code>xfrm4_udp_encap_rcv</code>） <a class="header-anchor" href="#_3-内核态的接收路径-xfrm4-udp-encap-rcv" aria-label="Permalink to &quot;3. 内核态的接收路径（`xfrm4_udp_encap_rcv`）&quot;">​</a></h4><p>当这个「穿了马甲」的 UDP 包到达接收方内核时，会发生什么？</p><p>我们以前看 IPsec 接收路径时，数据包是直接从 IP 层（<code>ip_protocol</code> 注册的回调）进入 ESP 处理逻辑的。但现在不行了，因为 IP 层看到的是 UDP 协议，内核会把它当成普通 UDP 包交给 UDP 协议栈处理。</p><p>UDP 协议栈收到包后，会检查这个包是否是「封装」包。</p><p>在 <code>net/ipv4/xfrm4_input.c</code> 里，有一个专门处理这种情况的函数：<strong><code>xfrm4_udp_encap_rcv()</code></strong>。</p><p>它的逻辑其实很直白（B 模式）：</p><ol><li><strong>拦截</strong>：UDP 协议栈在处理数据之前，先调用这个函数。</li><li><strong>剥离马甲</strong>：函数检查 UDP 端口是否为 4500，然后检查 UDP 载荷。 <ul><li>如果载荷是 <code>0xFF</code>（Keep-Alive），直接丢弃，任务完成。</li><li>如果是封装的 ESP 包，它会调用 <code>xfrm4_rcv_encap()</code>。</li></ul></li><li><strong>移交 ESP</strong>：<code>xfrm4_rcv_encap()</code> 会把 UDP 头部剥离，把指向 ESP 头部的指针传给正常的 IPsec 接收路径（<code>xfrm_input()</code>）。</li></ol><p>对于内核来说，剥离 UDP 头之后的包，和正常的 ESP 包没有任何区别。剩下的解密、验证、重组流程，全部复用我们上一节讲的逻辑。</p><hr><p><strong>回到那个「翻译官」的类比</strong>：</p><p>你可以把 NAT-T 想象成给一份加密文件套上一个标准的快递信封。邮局（NAT）只看信封上的地址（IP 和 UDP 端口），它不管信封里装的是啥，只要地址对，它就负责转运。只有当信封到了目的地，收件人（内核）才会撕开信封，拿出里面的加密文件去处理。</p><p>这个信封（UDP 头）存在唯一的目的，就是为了骗过路上的邮局。这虽然有点「曲线救国」，但在现有 IPv4 地址枯竭、NAT 无处不在的现实下，这是最实用的解法。</p><hr><p>走到这里，IPsec 的核心机制——从底层的 XFRM 框架，到加密算法的集成，再到策略与状态的博弈，以及最后为了穿过 NAT 而不得不做的 UDP 封装——我们就都串起来了。</p><p>这不仅仅是一堆协议和代码，这是一整套为了在不安全的网络上建立安全通道而演化出来的复杂工程。哪怕像 NAT 这样「破坏」互联网端到端原则的设计，IPsec 也能通过加一层封装来适配它。这种妥协与适应，或许才是工程世界的常态。</p>',54)])])}const h=a(s,[["render",o]]);export{T as __pageData,h as default};
