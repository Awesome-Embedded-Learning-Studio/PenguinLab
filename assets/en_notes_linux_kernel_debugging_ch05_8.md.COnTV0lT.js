import{_ as t}from"./chunks/ch05_fig058.BsyWoTmX.js";import{_ as o,o as n,c as i,a}from"./chunks/framework.8tiMeCK-.js";const g=JSON.parse('{"title":"5.8  Catching Memory Defects in the Kernel — Comparison and Notes (Part 1)","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch05_8.md","filePath":"en/notes/linux_kernel_debugging/ch05_8.md","lastUpdated":null}'),r={name:"en/notes/linux_kernel_debugging/ch05_8.md"};function s(l,e,c,h,d,p){return n(),i("div",null,[...e[0]||(e[0]=[a('<h1 id="_5-8-catching-memory-defects-in-the-kernel-—-comparison-and-notes-part-1" tabindex="-1">5.8 Catching Memory Defects in the Kernel — Comparison and Notes (Part 1) <a class="header-anchor" href="#_5-8-catching-memory-defects-in-the-kernel-—-comparison-and-notes-part-1" aria-label="Permalink to &quot;5.8  Catching Memory Defects in the Kernel — Comparison and Notes (Part 1)&quot;">​</a></h1><p>Now it&#39;s time for a retrospective.</p><p>The previous sections felt like testing blades in an armory: swinging KASAN for a few cuts, thrusting with UBSAN, and finally picking up the new sword that is Clang. These scattered attempts give you a sense that &quot;this thing is sharp,&quot; but when you hit the real battlefield—facing a mysteriously crashing production kernel—you need to know the range and blind spots of every weapon in your hands.</p><p>To save you from fumbling when the real deal arrives, let&#39;s take some time to lay out these test results side by side.</p><p>That&#39;s exactly why we have the large table below.</p><p><img src="'+t+'" alt="Table 5.5"></p><p><strong>Table 5.5 — Summary of common memory defects and the detection capabilities of different technologies</strong></p><p>The various footnotes in the table (like <code>[C1]</code>, <code>[K1]</code>, <code>[U1]</code>, etc.) have actually been explained in detail in previous chapters. Now, let&#39;s look past the dense grid of &quot;Yes&quot; and &quot;No&quot; entries and try to distill a few core patterns:</p><ul><li><p><strong>KASAN is nearly an &quot;all-rounder&quot;</strong>: For out-of-bounds (OOB) accesses on global (static) memory, stack memory, and dynamic memory, KASAN catches almost all of them. In contrast, UBSAN falls short when it comes to OOB accesses on dynamic heap memory—just look at test cases 4.x and 5.x in the table, where it is completely blind.</p></li><li><p><strong>KASAN has its blind spots</strong>: It cannot see pure &quot;undefined behavior&quot; (UB, i.e., the 8.x test cases). But this is exactly UBSAN&#39;s home turf, where it can catch the vast majority of such hidden dangers.</p></li><li><p><strong>Some bugs slip past everyone</strong>: The first three test cases—Uninitialized Memory Read (UMR), Use-After-Return (UAR), and memory leaks—leave both KASAN and UBSAN helpless. For UMR, you have to rely on compiler warnings (cranking up <code>-Wall</code>) or static analyzers (like cppcheck) to save the day. As for that tricky UAR bug, we&#39;ll save it for Chapter 12, <em>Additional Kernel Debugging Methods</em>, where we&#39;ll dedicate a section, <em>Using cppcheck and checkpatch.pl for Static Analysis</em>, to demonstrate how to catch it.</p></li><li><p><strong>Memory leaks require a specialist</strong>: The <code>kmemleak</code> kernel infrastructure is built specifically for this purpose; it can track memory leaks allocated via <code>k{m|z}alloc()</code>, <code>vmalloc()</code>, or <code>kmem_cache_alloc()</code> (and related interfaces).</p></li></ul><hr><h3 id="a-few-additional-notes-on-the-table" tabindex="-1">A Few Additional Notes on the Table <a class="header-anchor" href="#a-few-additional-notes-on-the-table" aria-label="Permalink to &quot;A Few Additional Notes on the Table&quot;">​</a></h3><p>Looking at this table, a few details are worth pausing to ponder:</p><ul><li><p><strong>Meaning of <code>[V1]</code></strong>: The table lists this as &quot;Various.&quot; This is actually the most terrifying scenario—the system might just throw an Oops, freeze and hang completely, or even appear totally unscathed on the surface. <strong>Don&#39;t be fooled by appearances.</strong> Once there&#39;s a bug in the kernel logic, the system is untrustworthy, even if it happens to be running fine right now. This &quot;looks okay&quot; state is often a ticking time bomb.</p></li><li><p><strong>Meaning of <code>[V2]</code></strong>: This isn&#39;t expanded upon in the table, but I must give a quick heads-up. We&#39;ll save the full explanation of this detail for the next chapter in the section, <em>Running SLUB Test Cases on a Kernel with slub_debug Disabled</em>. There&#39;s a plot twist there that&#39;s worth looking forward to.</p></li></ul><hr><h3 id="a-kasan-alternative-—-kfence-kernel-electric-fence" tabindex="-1">A KASAN Alternative — KFENCE (Kernel Electric-Fence) <a class="header-anchor" href="#a-kasan-alternative-—-kfence-kernel-electric-fence" aria-label="Permalink to &quot;A KASAN Alternative — KFENCE (Kernel Electric-Fence)&quot;">​</a></h3><p>When it comes to memory detection, you might be thinking to yourself: KASAN is great, but that performance overhead... I really wouldn&#39;t dare enable it on a machine running a production environment. Is there a way to enjoy the benefits of detection without worrying about dragging the machine down?</p><p>This is where <strong>KFENCE (Kernel Electric-Fence)</strong> comes in.</p><p>This is a very new tool in the Linux kernel (introduced in version 5.12, and at the time of writing, it&#39;s considered quite cutting-edge).</p><p>KFENCE has a clear positioning: a <strong>low-overhead, sampling-based memory safety error detector</strong>. It specifically watches for three notorious errors on heap memory: use-after-free, invalid free, and out-of-bounds access. It currently supports x86 and ARM64 architectures, and can hook into the kernel&#39;s SLAB and SLUB Allocators.</p><p>You might ask: <strong>Since we have KASAN, why bother with KFENCE?</strong></p><p>That&#39;s exactly the right question. Although their functionalities overlap, their design philosophies are completely different:</p><ol><li><strong>Use cases</strong>: KASAN&#39;s performance penalty is too large, dooming it to live as a pet in development/debugging environments only; KFENCE, however, is designed for production environments, with a performance overhead so low it&#39;s nearly zero.</li><li><strong>How it works</strong>: To catch everything, KASAN must monitor all memory; KFENCE chooses a &quot;sampling&quot; strategy. It doesn&#39;t watch all memory, but instead performs random spot-checks. This trades a certain degree of precision for extremely low performance overhead.</li><li><strong>Time versus probability</strong>: KASAN &quot;catches all immediately,&quot; while KFENCE &quot;will catch you sooner or later.&quot; As long as the system runs long enough (or you have enough machines running), KFENCE is almost guaranteed to stumble upon that bug.</li></ol><p><strong>The conclusion is simple</strong>: On development and debugging machines, blindly go with KASAN (you can also enable KFENCE alongside it); but on production machines, KFENCE is your only choice.</p><p>To enable KFENCE, you need to set <code>CONFIG_KFENCE</code> to <code>y</code>. <em>(⚠️ Note: Because this technology is so new, the 5.10 kernel series we use in this book doesn&#39;t have this option yet. You&#39;ll need to upgrade your kernel source slightly to play with it.)</em></p><p>For more details on KFENCE—including how to fine-tune it, how to interpret its error reports, and its internal implementation principles—you can dive into the official documentation: <code>Documentation/dev-tools/kfence.rst</code>.</p><hr><h3 id="one-last-addition-—-config-fortify-source" tabindex="-1">One Last Addition — <code>CONFIG_FORTIFY_SOURCE</code> <a class="header-anchor" href="#one-last-addition-—-config-fortify-source" aria-label="Permalink to &quot;One Last Addition — `CONFIG_FORTIFY_SOURCE`&quot;">​</a></h3><p>Before wrapping up this chapter, there&#39;s one more heavy-hitting weapon to introduce.</p><p>If you upgrade your kernel to 5.18 (the latest Stable Kernel at the time of writing), you&#39;ll encounter a new, stricter <code>memcpy()</code> API family (covering <code>memcpy()</code>, <code>memmove()</code>, and <code>memset()</code>). Behind this is a kernel feature called <strong>compile-time bounds checking</strong>, which internally leverages compiler hardening capabilities.</p><p>The kernel configuration option for this feature is <code>CONFIG_FORTIFY_SOURCE</code>.</p><p>Once enabled, it helps catch a large class of typical buffer overflow defects at both compile time and runtime. It&#39;s like adding an invisible safety net around memory operation functions. If you want to understand just how hardcore this mechanism is, we highly recommend reading this LWN article: <em>Strict memcpy() bounds checking for the kernel</em>.</p><hr><h3 id="chapter-echoes" tabindex="-1">Chapter Echoes <a class="header-anchor" href="#chapter-echoes" aria-label="Permalink to &quot;Chapter Echoes&quot;">​</a></h3><p>Writing up to this point, our journey in this chapter has actually covered the most core paths of kernel memory debugging.</p><p>Do you still remember the questions we posed at the beginning of this chapter?</p><ul><li>Why are memory bugs so hard to find?</li><li>Without rebooting the machine or destroying the crime scene, how much can we actually do?</li></ul><p>Now you have the answers in your hands.</p><p>Although C gives us the power to manipulate everything, the price of that power is that we must manage memory ourselves—and one slip plunges us into the abyss of undefined behavior. In this chapter, we didn&#39;t just meet the various enemies you might encounter—from simple out-of-bounds accesses to bizarre undefined behavior—more importantly, we equipped an entire cabinet of weapons to deal with them.</p><p>This isn&#39;t just a simple laundry list of KASAN and UBSAN. <strong>We&#39;ve established a layered defense mindset</strong>: using KUnit for automated regression, Debugfs scripts for manual probing, UBSAN to patch up logic holes, and finally switching to the Clang compiler to leverage its sharper diagnostic capabilities.</p><p>That big table (Table 5.5) isn&#39;t the finish line; it&#39;s merely an intermediate checkpoint—in the next chapter, we&#39;ll fill in data for even more tools, continuing to refine this battle map. And tools like KFENCE and <code>FORTIFY_SOURCE</code> show the direction of the future: even under the harsh constraints of a production environment, we still have ways to catch those hidden defects.</p><p>Reaching this point, congratulations—you&#39;ve completed this long and crucial first part: the foundational training for catching kernel-space memory bugs. <strong>Don&#39;t rush to turn the page; take some time to digest this.</strong> When you feel the sword in your hand is familiar enough, the next chapter is waiting for you, where we&#39;ll complete the remaining puzzle pieces of catching kernel memory defects.</p>',41)])])}const f=o(r,[["render",s]]);export{g as __pageData,f as default};
