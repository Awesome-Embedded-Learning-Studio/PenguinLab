import{_ as t,o as s,c as o,a as n}from"./chunks/framework.8tiMeCK-.js";const g=JSON.parse('{"title":"2.4 NETLINK_ROUTE Messages: More Than Just Routing","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch02_4.md","filePath":"en/notes/linux_kernel_networking/ch02_4.md","lastUpdated":null}'),i={name:"en/notes/linux_kernel_networking/ch02_4.md"};function a(r,e,l,h,d,c){return s(),o("div",null,[...e[0]||(e[0]=[n(`<h1 id="_2-4-netlink-route-messages-more-than-just-routing" tabindex="-1">2.4 NETLINK_ROUTE Messages: More Than Just Routing <a class="header-anchor" href="#_2-4-netlink-route-messages-more-than-just-routing" aria-label="Permalink to &quot;2.4 NETLINK_ROUTE Messages: More Than Just Routing&quot;">​</a></h1><p>In the previous section, we spent our time deep in the Generic Netlink mechanism, tearing apart message headers, TLVs, and validation policies. Now that the foundation is laid, it&#39;s time to shift our focus back to the networking subsystem itself and see how the &quot;veteran&quot; Netlink protocol families actually work.</p><p>The heavyweight among them is undoubtedly <code>NETLINK_ROUTE</code>.</p><p>Don&#39;t let the name fool you—although it&#39;s called Route Netlink, it handles far more than just routing tables. It acts as the &quot;general manager&quot; of the entire network configuration, overseeing network interfaces, addresses, neighbor tables, and traffic control.</p><p>Specifically, the <code>NETLINK_ROUTE</code> message families are divided into several branches by function:</p><ul><li><strong>LINK</strong>: Manages network interfaces, such as bringing <code>eth0</code> up or down, or renaming it.</li><li><strong>ADDR</strong>: Manages the addition and deletion of IP addresses.</li><li><strong>ROUTE</strong>: The actual routing messages, managing the FIB (Forwarding Information Base).</li><li><strong>NEIGH</strong>: Manages the neighbor subsystem, which means the ARP and ND (Neighbor Discovery) tables.</li><li><strong>RULE</strong>: Manages policy routing rules.</li><li><strong>QDISC</strong>, <strong>TCLASS</strong>, <strong>ACTION</strong>: This area relates to QoS (traffic control), covering queuing disciplines, traffic classification, and action handling.</li><li><strong>NEIGHTBL</strong>: Configuration of the neighbor table itself.</li><li><strong>ADDRLABEL</strong>: Address labels (typically used for IPv6).</li></ul><h3 id="mapping-crud-to-message-types" tabindex="-1">Mapping CRUD to Message Types <a class="header-anchor" href="#mapping-crud-to-message-types" aria-label="Permalink to &quot;Mapping CRUD to Message Types&quot;">​</a></h3><p>Regardless of the family above, their message type design follows a very intuitive CRUD (Create, Read, Update, Delete) logic.</p><p>For most objects (such as routes, addresses, or neighbors), there are only three operations:</p><ol><li><strong>Create</strong>: Corresponds to the <code>RTM_NEWXXX</code> message (e.g., <code>RTM_NEWROUTE</code>).</li><li><strong>Delete</strong>: Corresponds to the <code>RTM_DELXXX</code> message (e.g., <code>RTM_DELROUTE</code>).</li><li><strong>Query</strong>: Corresponds to the <code>RTM_GETXXX</code> message (e.g., <code>RTM_GETROUTE</code>).</li></ol><p>This is just like standard database operations.</p><p>However, there is one exception: <strong>LINK (network interfaces)</strong>. Configuring a network interface is sometimes more complex than simple &quot;create, delete, query&quot;—you might just want to modify a specific parameter (like the MTU) rather than tearing down and recreating the interface. Therefore, on top of the three standard messages, the Link family has an extra message dedicated to <strong>modification</strong>: <code>RTM_SETLINK</code>.</p><h3 id="when-operations-fail-the-nlmsgerr-structure" tabindex="-1">When Operations Fail: The nlmsgerr Structure <a class="header-anchor" href="#when-operations-fail-the-nlmsgerr-structure" aria-label="Permalink to &quot;When Operations Fail: The nlmsgerr Structure&quot;">​</a></h3><p>When writing userspace programs, what we fear most isn&#39;t rejection—it&#39;s silence. You send a request, the kernel doesn&#39;t respond, and you have no idea whether it never arrived or failed during processing.</p><p>The designers of the Netlink protocol clearly considered this. They designed a standard error reporting mechanism that works universally, whether you&#39;re communicating over standard Netlink or Generic Netlink.</p><p>All of this is wrapped up in an <code>nlmsgerr</code> structure:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> nlmsgerr {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        error;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           /* 负数表示标准 errno 错误码，0 表示成功/ACK */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> nlmsghdr msg;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        /* 触发错误的原始请求消息头 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>You can think of this structure as a &quot;return receipt.&quot;</p><p>When the message you sent has a problem—for example, if <code>nlmsg_type</code> contains a value the kernel doesn&#39;t recognize—the kernel doesn&#39;t just sit there. It sends back a Netlink message. The message header type of this reply is <code>NLMSG_ERROR</code>, immediately followed by the <code>nlmsgerr</code> structure above.</p><p>At this point, an interesting behavior occurs:</p><p>If the <code>error</code> field is not 0 (for example, <code>-EOPNOTSUPP</code>), this &quot;return receipt&quot; will also have the <strong>header of the original message you sent</strong> appended to it (that is, the <code>msg</code> field).</p><p><strong>Why do this?</strong></p><p>Consider a scenario where you&#39;re handling multi-threaded or asynchronous communication and have fired off several requests simultaneously. When one of them returns an error, how do you know which request failed?</p><p>The kernel thoughtfully attaches your original request header verbatim. You simply compare the sequence number in that header to immediately match it up and identify which operation went wrong.</p><p>Figure 2-4 shows the memory layout of this error message:</p><blockquote><p><strong>[Figure 2-4: Netlink Error Message Layout]</strong></p><ul><li><strong>Netlink Header (type = NLMSG_ERROR)</strong></li><li><strong>Error Code (int)</strong> : e.g., <code>-EINVAL</code> or <code>-EOPNOTSUPP</code></li><li><strong>Original Request Header</strong> : the <code>nlmsghdr</code> of the original request (present only when error != 0)</li><li><strong>Original Payload</strong> : (Note: in practice, the code usually only appends the header, not the entire payload, unless there are special debugging requirements)</li></ul></blockquote><h3 id="the-nuances-of-the-ack-mechanism" tabindex="-1">The Nuances of the ACK Mechanism <a class="header-anchor" href="#the-nuances-of-the-ack-mechanism" aria-label="Permalink to &quot;The Nuances of the ACK Mechanism&quot;">​</a></h3><p>Sometimes we send a request not to modify configuration, but simply to confirm &quot;did you get this?&quot; This is where the ACK (Acknowledgment) mechanism comes in.</p><p>Requesting an ACK is simple: add <code>NLM_F_ACK</code> to the flags of the outgoing message.</p><p>After receiving it, if the kernel processes it without issues, it will send back a message of type <code>NLMSG_ERROR</code>. But note, there&#39;s a counter-intuitive design here:</p><p><strong>Even on success (ACK), the message type the kernel replies with is <code>NLMSG_ERROR</code>.</strong></p><p>The difference lies in the <code>error</code> field of the <code>nlmsgerr</code> structure:</p><ul><li>If <code>error</code> is <strong>0</strong>, this is not a &quot;return receipt&quot; but a &quot;delivery confirmation.&quot;</li><li>In this case, the <code>msg</code> field (the original request header) is <strong>not</strong> appended to the reply—because it succeeded, the kernel figures there&#39;s no need to send your garbage back to you.</li></ul><p>If you want to dig into the details, you can look at the <code>netlink_ack()</code> function in <code>net/netlink/af_netlink.c</code>. Those few lines of code spell out the logic above perfectly clearly.</p><h3 id="where-we-are-now" tabindex="-1">Where We Are Now <a class="header-anchor" href="#where-we-are-now" aria-label="Permalink to &quot;Where We Are Now&quot;">​</a></h3><p>We now have a list of <code>NETLINK_ROUTE</code> message types (LINK, ADDR, ROUTE...) in hand, and a mental model for error handling and ACKs.</p><p>But this is just the &quot;manual.&quot; Next, we&#39;ll move into the practical portion and use these tools to manipulate the routing table (FIB) inside the kernel. We&#39;ll see exactly which TLV attributes need to be nested like Russian dolls inside the message body when we push a route using a single <code>RTM_NEWROUTE</code> message.</p>`,37)])])}const p=t(i,[["render",a]]);export{g as __pageData,p as default};
