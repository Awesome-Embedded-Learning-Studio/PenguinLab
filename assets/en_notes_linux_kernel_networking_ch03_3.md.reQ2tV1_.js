import{_ as t,o,c as i,a}from"./chunks/framework.8tiMeCK-.js";const g=JSON.parse('{"title":"3.3 Quick Reference and Practical Supplements","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch03_3.md","filePath":"en/notes/linux_kernel_networking/ch03_3.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_networking/ch03_3.md"};function s(r,e,l,d,c,h){return o(),i("div",null,[...e[0]||(e[0]=[a('<h1 id="_3-3-quick-reference-and-practical-supplements" tabindex="-1">3.3 Quick Reference and Practical Supplements <a class="header-anchor" href="#_3-3-quick-reference-and-practical-supplements" aria-label="Permalink to &quot;3.3 Quick Reference and Practical Supplements&quot;">​</a></h1><p>In this chapter, we broke down the internal workings of ICMPv4 and ICMPv6, from initialization flows to packet transmission and reception, covering a significant amount of kernel code. Now it&#39;s time to consolidate these scattered details into a quick reference, and along the way, cover two &quot;corners&quot; that are easily overlooked in engineering practice.</p><p>This section might look like an &quot;appendix,&quot; but I strongly recommend not treating it merely as a dictionary. The procfs parameters and iptables REJECT target mentioned later directly determine whether your production firewall&#39;s &quot;rejection&quot; behavior is a polite wave or an angry silence.</p><hr><h3 id="📚-core-method-quick-reference" tabindex="-1">📚 Core Method Quick Reference <a class="header-anchor" href="#📚-core-method-quick-reference" aria-label="Permalink to &quot;📚 Core Method Quick Reference&quot;">​</a></h3><p>We repeatedly encountered these functions during our code walkthrough. They are the &quot;limbs&quot; of the ICMP subsystem.</p><h4 id="reception-handling" tabindex="-1">Reception Handling <a class="header-anchor" href="#reception-handling" aria-label="Permalink to &quot;Reception Handling&quot;">​</a></h4><p><strong><code>int icmp_rcv(struct sk_buff *skb);</code></strong></p><blockquote><p><strong>Location</strong>: <code>net/ipv4/icmp.c</code></p></blockquote><p>This is the main entry function for ICMPv4. When an IP packet is stripped of its IP header and the protocol number is found to be <code>IPPROTO_ICMP</code> (1), the kernel jumps directly to this function. It handles checksum verification, packet dispatch, and triggers the generation of corresponding ICMP replies (such as Echo Reply).</p><h4 id="transmission-handling" tabindex="-1">Transmission Handling <a class="header-anchor" href="#transmission-handling" aria-label="Permalink to &quot;Transmission Handling&quot;">​</a></h4><p><strong><code>void icmp_send(struct sk_buff *skb_in, int type, int code, __be32 info);</code></strong></p><blockquote><p><strong>Location</strong>: <code>net/ipv4/icmp.c</code></p></blockquote><p>The core function for the kernel to send ICMPv4 <strong>error messages</strong> (such as Destination Unreachable) outward.</p><ul><li><code>skb_in</code>: The original packet (provoking SKB) that <strong>triggered the error</strong>. The kernel extracts the IP header from this packet for diagnostics and embeds it into the data portion of the newly generated ICMP error packet.</li><li><code>type</code> / <code>code</code>: The type and code of the ICMP message.</li><li><code>info</code>: Typically used to pass the MTU (in the Fragmentation Needed scenario) or a pointer to the offending field.</li></ul><p><strong><code>void icmpv6_send(struct sk_buff *skb, u8 type, u8 code, __u32 info);</code></strong></p><blockquote><p><strong>Location</strong>: <code>net/ipv6/icmp.c</code></p></blockquote><p>The ICMPv6 version of the send function. Although the name is similar, its internal implementation calls <code>ip6_append_data</code> and <code>ip6_push_pending_frames</code>, following the IPv6 transmission path.</p><p><strong><code>void icmpv6_param_prob(struct sk_buff *skb, u8 code, int pos);</code></strong></p><blockquote><p><strong>Location</strong>: <code>net/ipv6/icmp.c</code></p></blockquote><p>This is a wrapper function specifically for sending ICMPv6 Parameter Problem messages. Its purpose is to save you the trouble of manually filling in the <code>ICMPV6_PARAMPROB</code> type, and it automatically frees <code>skb</code> after transmission (because the parameter issue is severe enough that the packet cannot be processed and must be discarded).</p><h4 id="utility-helpers" tabindex="-1">Utility Helpers <a class="header-anchor" href="#utility-helpers" aria-label="Permalink to &quot;Utility Helpers&quot;">​</a></h4><p><strong><code>struct icmp6hdr *icmp6_hdr(const struct sk_buff *skb);</code></strong></p><blockquote><p><strong>Location</strong>: <code>include/linux/icmpv6.h</code></p></blockquote><p>A simple inline helper function used to safely obtain the <code>icmp6hdr</code> pointer from <code>skb</code>. In IPv6 extension header processing, the SKB pointers might keep moving, and using this macro prevents errors in pointer arithmetic.</p><hr><h3 id="📋-key-data-table-error-code-dictionary" tabindex="-1">📋 Key Data Table: Error Code Dictionary <a class="header-anchor" href="#📋-key-data-table-error-code-dictionary" aria-label="Permalink to &quot;📋 Key Data Table: Error Code Dictionary&quot;">​</a></h3><p>ICMP &quot;error&quot; messages are primarily distinguished by Type (major category) and Code (minor category). The following tables are the &quot;dictionary&quot; you need to constantly refer to during packet capture analysis.</p><h4 id="_1-icmpv4-destination-unreachable" tabindex="-1">1. ICMPv4 Destination Unreachable <a class="header-anchor" href="#_1-icmpv4-destination-unreachable" aria-label="Permalink to &quot;1. ICMPv4 Destination Unreachable&quot;">​</a></h4><p>This is the most common category of errors. When a router cannot find a route, or a host port is not open, you will receive these.</p><table tabindex="0"><thead><tr><th style="text-align:left;">Code</th><th style="text-align:left;">Kernel Symbol</th><th style="text-align:left;">Human-Readable Meaning</th></tr></thead><tbody><tr><td style="text-align:left;">0</td><td style="text-align:left;"><code>ICMP_NET_UNREACH</code></td><td style="text-align:left;">Network <strong>unreachable</strong> (no such subnet in the routing table).</td></tr><tr><td style="text-align:left;">1</td><td style="text-align:left;"><code>ICMP_HOST_UNREACH</code></td><td style="text-align:left;">Host <strong>unreachable</strong> (ARP resolution failed, or the host is down).</td></tr><tr><td style="text-align:left;">2</td><td style="text-align:left;"><code>ICMP_PROT_UNREACH</code></td><td style="text-align:left;">Protocol <strong>unreachable</strong> (e.g., you sent TCP, but the target machine&#39;s kernel doesn&#39;t have the TCP module loaded).</td></tr><tr><td style="text-align:left;">3</td><td style="text-align:left;"><code>ICMP_PORT_UNREACH</code></td><td style="text-align:left;">Port <strong>unreachable</strong> (most common, sending UDP to a port that isn&#39;t listening).</td></tr><tr><td style="text-align:left;">4</td><td style="text-align:left;"><code>ICMP_FRAG_NEEDED</code></td><td style="text-align:left;"><strong>Fragmentation Needed</strong>, but the DF flag is set (the key packet for PMTU Discovery).</td></tr><tr><td style="text-align:left;">5</td><td style="text-align:left;"><code>ICMP_SR_FAILED</code></td><td style="text-align:left;"><strong>Source Route Failed</strong> (rarely used, indicating an issue with the specified source route path).</td></tr><tr><td style="text-align:left;">9</td><td style="text-align:left;"><code>ICMP_NET_ANO</code></td><td style="text-align:left;">Network <strong>administratively prohibited</strong> (the firewall decided you shouldn&#39;t go here).</td></tr><tr><td style="text-align:left;">10</td><td style="text-align:left;"><code>ICMP_HOST_ANO</code></td><td style="text-align:left;">Host <strong>administratively prohibited</strong> (the firewall considers this host protected).</td></tr><tr><td style="text-align:left;">13</td><td style="text-align:left;"><code>ICMP_PKT_FILTERED</code></td><td style="text-align:left;">Packet <strong>filtered</strong> (usually generated by a firewall).</td></tr></tbody></table><p><em>(Note: Some rarely seen TOS-related and Historical codes are omitted)</em></p><h4 id="_2-icmpv4-redirect" tabindex="-1">2. ICMPv4 Redirect <a class="header-anchor" href="#_2-icmpv4-redirect" aria-label="Permalink to &quot;2. ICMPv4 Redirect&quot;">​</a></h4><p>Host A wants to send data to Host C and must go through Router R. But Router R realizes that Host A could actually send directly to Host B, who would then forward it, or R knows a shorter route. In this case, R will send a Redirect to A.</p><table tabindex="0"><thead><tr><th style="text-align:left;">Code</th><th style="text-align:left;">Kernel Symbol</th><th style="text-align:left;">Meaning</th></tr></thead><tbody><tr><td style="text-align:left;">0</td><td style="text-align:left;"><code>ICMP_REDIR_NET</code></td><td style="text-align:left;">Redirect <strong>Network</strong>.</td></tr><tr><td style="text-align:left;">1</td><td style="text-align:left;"><code>ICMP_REDIR_HOST</code></td><td style="text-align:left;">Redirect <strong>Host</strong>.</td></tr><tr><td style="text-align:left;">2</td><td style="text-align:left;"><code>ICMP_REDIR_NETTOS</code></td><td style="text-align:left;">Redirect <strong>Network (based on TOS)</strong>.</td></tr><tr><td style="text-align:left;">3</td><td style="text-align:left;"><code>ICMP_REDIR_HOSTTOS</code></td><td style="text-align:left;">Redirect <strong>Host (based on TOS)</strong>.</td></tr></tbody></table><h4 id="_3-icmpv4-time-exceeded" tabindex="-1">3. ICMPv4 Time Exceeded <a class="header-anchor" href="#_3-icmpv4-time-exceeded" aria-label="Permalink to &quot;3. ICMPv4 Time Exceeded&quot;">​</a></h4><table tabindex="0"><thead><tr><th style="text-align:left;">Code</th><th style="text-align:left;">Kernel Symbol</th><th style="text-align:left;">Meaning</th></tr></thead><tbody><tr><td style="text-align:left;">0</td><td style="text-align:left;"><code>ICMP_EXC_TTL</code></td><td style="text-align:left;"><strong>TTL Exceeded</strong> (the core of how Traceroute works).</td></tr><tr><td style="text-align:left;">1</td><td style="text-align:left;"><code>ICMP_EXC_FRAGTIME</code></td><td style="text-align:left;"><strong>Fragment Reassembly Time Exceeded</strong> (took too long to collect all fragments, discarded).</td></tr></tbody></table><h4 id="_4-icmpv6-destination-unreachable" tabindex="-1">4. ICMPv6 Destination Unreachable <a class="header-anchor" href="#_4-icmpv6-destination-unreachable" aria-label="Permalink to &quot;4. ICMPv6 Destination Unreachable&quot;">​</a></h4><p>IPv6&#39;s definitions are slightly different. Note that in IPv6, <code>ICMPV6_PKT_TOOBIG</code> is an independent Type, not a Code.</p><table tabindex="0"><thead><tr><th style="text-align:left;">Code</th><th style="text-align:left;">Kernel Symbol</th><th style="text-align:left;">Meaning</th></tr></thead><tbody><tr><td style="text-align:left;">0</td><td style="text-align:left;"><code>ICMPV6_NOROUTE</code></td><td style="text-align:left;"><strong>No Route</strong> to destination.</td></tr><tr><td style="text-align:left;">1</td><td style="text-align:left;"><code>ICMPV6_ADM_PROHIBITED</code></td><td style="text-align:left;"><strong>Administratively Prohibited</strong> (forbidden by admin).</td></tr><tr><td style="text-align:left;">3</td><td style="text-align:left;"><code>ICMPV6_ADDR_UNREACH</code></td><td style="text-align:left;"><strong>Address Unreachable</strong> (usually means neighbor unreachable, NDP failed).</td></tr><tr><td style="text-align:left;">4</td><td style="text-align:left;"><code>ICMPV6_PORT_UNREACH</code></td><td style="text-align:left;"><strong>Port Unreachable</strong>.</td></tr></tbody></table><h4 id="_5-icmpv6-parameter-problem" tabindex="-1">5. ICMPv6 Parameter Problem <a class="header-anchor" href="#_5-icmpv6-parameter-problem" aria-label="Permalink to &quot;5. ICMPv6 Parameter Problem&quot;">​</a></h4><table tabindex="0"><thead><tr><th style="text-align:left;">Code</th><th style="text-align:left;">Kernel Symbol</th><th style="text-align:left;">Meaning</th></tr></thead><tbody><tr><td style="text-align:left;">0</td><td style="text-align:left;"><code>ICMPV6_HDR_FIELD</code></td><td style="text-align:left;"><strong>Erroneous Header Field</strong> (unrecognized field).</td></tr><tr><td style="text-align:left;">1</td><td style="text-align:left;"><code>ICMPV6_UNK_NEXTHDR</code></td><td style="text-align:left;"><strong>Unrecognized Next Header</strong> (extension header chain is broken).</td></tr><tr><td style="text-align:left;">2</td><td style="text-align:left;"><code>ICMPV6_UNK_OPTION</code></td><td style="text-align:left;"><strong>Unrecognized IPv6 Option</strong>.</td></tr></tbody></table><hr><h3 id="⚙️-procfs-interfaces-controlling-icmp-from-user-space" tabindex="-1">⚙️ procfs Interfaces: Controlling ICMP from User Space <a class="header-anchor" href="#⚙️-procfs-interfaces-controlling-icmp-from-user-space" aria-label="Permalink to &quot;⚙️ procfs Interfaces: Controlling ICMP from User Space&quot;">​</a></h3><p>Linux allows you to adjust ICMP behavior via <code>/proc/sys</code> without recompiling the kernel. These variables all belong to the <code>netns_ipv4</code> structure and are isolated per network namespace.</p><h4 id="key-configuration-items" tabindex="-1">Key Configuration Items <a class="header-anchor" href="#key-configuration-items" aria-label="Permalink to &quot;Key Configuration Items&quot;">​</a></h4><p><strong>1. <code>icmp_echo_ignore_all</code></strong></p><ul><li><strong>Path</strong>: <code>/proc/sys/net/ipv4/icmp_echo_ignore_all</code></li><li><strong>Default</strong>: <code>0</code> (disabled)</li><li><strong>Purpose</strong>: When set to 1, the kernel will <strong>ignore all</strong> ICMP Echo Requests (Ping requests). <ul><li><em>Scenario</em>: This is the first step to &quot;stealth mode.&quot; Note that this only ignores Pings; it doesn&#39;t mean the host is unreachable.</li></ul></li></ul><p><strong>2. <code>icmp_echo_ignore_broadcasts</code></strong></p><ul><li><strong>Path</strong>: <code>/proc/sys/net/ipv4/icmp_echo_ignore_broadcasts</code></li><li><strong>Default</strong>: <code>1</code> (enabled)</li><li><strong>Purpose</strong>: When set to 1, ignores Pings or Timestamp requests sent to broadcast/multicast addresses. <ul><li><em>Scenario</em>: <strong>Must remain enabled</strong>. If a virus on your network is frantically Pinging the broadcast address, turning this off will directly cause a network storm.</li></ul></li></ul><p><strong>3. <code>icmp_ratelimit</code></strong></p><ul><li><strong>Path</strong>: <code>/proc/sys/net/ipv4/icmp_ratelimit</code></li><li><strong>Default</strong>: <code>1 * HZ</code> (i.e., 1 second, 1000ms)</li><li><strong>Purpose</strong>: Limits the sending rate of specific ICMP error messages (defined by <code>icmp_ratemask</code>). The unit is milliseconds. 0 means no limit. <ul><li><em>Warning</em>: If set to 0, an attacker can forge massive amounts of traffic to trigger your ICMP error replies, turning you into an accomplice in a reflection/amplification attack (although ICMP packets are small, they are still enough to congest a link).</li></ul></li></ul><p><strong>4. <code>icmp_ratemask</code></strong></p><ul><li><strong>Path</strong>: <code>/proc/sys/net/ipv4/icmp_ratemask</code></li><li><strong>Default</strong>: <code>0x1818</code> (binary <code>0001 1000 0001 1000</code>)</li><li><strong>Purpose</strong>: A bitmask that determines which ICMP Types need to be rate-limited. Each bit corresponds to an ICMP Type. <ul><li>For example, if you want to limit Destination Unreachable (Type 3) and Time Exceeded (Type 11), you need to set the corresponding bits. The default value primarily targets error messages.</li></ul></li></ul><p><strong>5. <code>icmp_errors_use_inbound_ifaddr</code></strong></p><ul><li><strong>Path</strong>: <code>/proc/sys/net/ipv4/icmp_errors_use_inbound_ifaddr</code></li><li><strong>Default</strong>: <code>0</code></li><li><strong>Purpose</strong>: Determines how the source IP address is selected when sending ICMP error messages. <ul><li><code>0</code> (default): Uses the primary address of the <strong>outbound</strong> interface.</li><li><code>1</code>: Uses the primary address of the <strong>inbound</strong> interface (i.e., the interface that received the triggering packet).</li><li><em>Gotcha</em>: In multi-homed hosts or complex asymmetric routing scenarios, if you select the wrong source address, the error packet received by the peer might be discarded as a forged packet, or the peer&#39;s reply might get lost again. Usually, keeping the default is fine unless your network topology is very special.</li></ul></li></ul><hr><h3 id="🛡️-active-rejection-using-iptables-ip6tables-reject" tabindex="-1">🛡️ Active Rejection: Using iptables/ip6tables REJECT <a class="header-anchor" href="#🛡️-active-rejection-using-iptables-ip6tables-reject" aria-label="Permalink to &quot;🛡️ Active Rejection: Using iptables/ip6tables REJECT&quot;">​</a></h3><p>There is an iron rule for firewalls: <strong>REJECT is better than DROP</strong>.</p><ul><li><strong>DROP</strong>: Your machine simply discards the packet, and the client will keep waiting until it times out. The user experience is poor, and it&#39;s hard for an attacker to tell if they were blocked by a firewall or if the machine is down.</li><li><strong>REJECT</strong>: Your machine politely sends back an ICMP error packet (&quot;Host Unreachable&quot; or &quot;Port Unreachable&quot;). The client immediately knows &quot;this path is blocked&quot; and stops trying.</li></ul><p>However, the details of this mechanism differ slightly between IPv4 and IPv6.</p><h4 id="ipv4-iptables" tabindex="-1">IPv4 (iptables) <a class="header-anchor" href="#ipv4-iptables" aria-label="Permalink to &quot;IPv4 (iptables)&quot;">​</a></h4><p>The <code>ipt_REJECT</code> module allows you to specify what type of ICMP error to return.</p><p><strong>Most common example</strong>: Deny access and inform the peer &quot;administratively prohibited.&quot;</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iptables</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -A</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> INPUT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -j</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> REJECT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --reject-with</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> icmp-host-prohibited</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><strong><code>--reject-with</code> optional parameters</strong>:</p><table tabindex="0"><thead><tr><th style="text-align:left;">Parameter</th><th style="text-align:left;">ICMP Message Sent</th><th style="text-align:left;">Meaning</th></tr></thead><tbody><tr><td style="text-align:left;"><code>icmp-net-unreachable</code></td><td style="text-align:left;"><code>ICMP_NET_UNREACH</code></td><td style="text-align:left;">Network unreachable.</td></tr><tr><td style="text-align:left;"><code>icmp-host-unreachable</code></td><td style="text-align:left;"><code>ICMP_HOST_UNREACH</code></td><td style="text-align:left;">Host unreachable.</td></tr><tr><td style="text-align:left;"><code>icmp-port-unreachable</code></td><td style="text-align:left;"><code>ICMP_PORT_UNREACH</code></td><td style="text-align:left;">Port unreachable (this is the default behavior).</td></tr><tr><td style="text-align:left;"><code>icmp-proto-unreachable</code></td><td style="text-align:left;"><code>ICMP_PROT_UNREACH</code></td><td style="text-align:left;">Protocol unreachable.</td></tr><tr><td style="text-align:left;"><code>icmp-net-prohibited</code></td><td style="text-align:left;"><code>ICMP_NET_ANO</code></td><td style="text-align:left;">Network prohibited.</td></tr><tr><td style="text-align:left;"><code>icmp-host-prohibited</code></td><td style="text-align:left;"><code>ICMP_HOST_ANO</code></td><td style="text-align:left;"><strong>Host prohibited</strong> (commonly used).</td></tr><tr><td style="text-align:left;"><code>icmp-admin-prohibited</code></td><td style="text-align:left;"><code>ICMP_PKT_FILTERED</code></td><td style="text-align:left;">Administratively filtered (explicitly informs that it&#39;s a filtering action).</td></tr></tbody></table><ul><li><em>Extra bonus</em>: <code>--reject-with tcp-reset</code>. If this is a TCP packet, the kernel will forge a TCP RST packet back instead of using ICMP. This is very effective for breaking stale connections stuck in the <code>SYN_SENT</code> state.</li></ul><h4 id="ipv6-ip6tables" tabindex="-1">IPv6 (ip6tables) <a class="header-anchor" href="#ipv6-ip6tables" aria-label="Permalink to &quot;IPv6 (ip6tables)&quot;">​</a></h4><p>Under IPv6, <code>ip6t_REJECT</code> works similarly, but the available error types are fewer (because IPv6&#39;s error types are inherently streamlined).</p><p><strong>Example</strong>: Reject ICMPv6 requests from a certain IPv6 subnet.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip6tables</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -A</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> INPUT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 2001::/64</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ICMPv6</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -j</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> REJECT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --reject-with</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> icmp6-adm-prohibited</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><strong><code>--reject-with</code> optional parameters</strong>:</p><table tabindex="0"><thead><tr><th style="text-align:left;">Parameter</th><th style="text-align:left;">ICMPv6 Message Sent</th></tr></thead><tbody><tr><td style="text-align:left;"><code>no-route</code> / <code>icmp6-no-route</code></td><td style="text-align:left;"><code>ICMPV6_NOROUTE</code></td></tr><tr><td style="text-align:left;"><code>adm-prohibited</code> / <code>icmp6-adm-prohibited</code></td><td style="text-align:left;"><code>ICMPV6_ADM_PROHIBITED</code></td></tr><tr><td style="text-align:left;"><code>addr-unreach</code> / <code>icmp6-addr-unreachable</code></td><td style="text-align:left;"><code>ICMPV6_ADDR_UNREACH</code></td></tr><tr><td style="text-align:left;"><code>port-unreach</code> / <code>icmp6-port-unreachable</code></td><td style="text-align:left;"><code>ICMPV6_PORT_UNREACH</code></td></tr></tbody></table><hr><h3 id="chapter-echoes" tabindex="-1">Chapter Echoes <a class="header-anchor" href="#chapter-echoes" aria-label="Permalink to &quot;Chapter Echoes&quot;">​</a></h3><p>ICMP is often misunderstood. Many people equate it with &quot;Ping&quot; and even consider it a security risk, wishing to block it entirely on the first line of their firewall.</p><p>But through the source code walkthrough in this chapter, you should have built a new understanding: <strong>ICMP is the maintenance crew of the IP protocol</strong>. Without ICMP error messages, IP routing becomes a one-way blind delivery—packets are sent out with no receiver, or sent down the wrong path, and the sender never knows. PMTU Discovery breaks (causing large packets to pass but with poor performance), redirects fail (causing paths to remain suboptimal), and connectivity testing becomes &quot;blind men feeling an elephant.&quot;</p><p>Those intuitions we mentioned at the beginning of the chapter—that Ping just tests connectivity, and firewalls should block Ping—now seem too crude.</p><ul><li>Ping tests more than just connectivity; it also includes routing integrity and the efficiency of name resolution (if NDP is involved in an IPv6 environment).</li><li>Firewalls shouldn&#39;t blindly DROP all ICMP, but rather apply fine-grained control (allow Type 3 Code 4 for MTU Discovery, allow Type 11 for Traceroute), and prioritize using the REJECT target over DROP.</li></ul><p>By understanding ICMP&#39;s transmission and reception mechanisms, data structures, and the <code>inet</code> protocol registration flow, you&#39;ve mastered the &quot;stethoscope&quot; for debugging network layer issues. When a network problem occurs, don&#39;t just stare at the TCP three-way handshake; shift your perspective down to L3 and ask yourself: <strong>Did ICMP report an error? What error did it report?</strong> The truth is often hidden in that error packet.</p><p>Next, we will formally enter the core of the IPv4 network layer—those core engines responsible for routing, fragmentation, and forwarding. Now that we&#39;ve figured out the &quot;control signals,&quot; it&#39;s time to see how the &quot;data traffic&quot; is scheduled and transferred.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: Understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: Understanding&quot;">​</a></h3><p><strong>Question</strong>: During the Linux kernel initialization phase, the <code>icmp_sk_init()</code> function creates a kernel socket (<code>net-&gt;ipv4.icmp_sk[i]</code>) for each CPU. What is the protocol type (<code>protocol</code>) specified when creating these sockets? What is their primary purpose?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: The protocol type is <code>IPPROTO_ICMP</code> (value 1). These sockets are used by the kernel to send ICMP error messages (such as Destination Unreachable, Time Exceeded) or reply messages (such as Echo Reply). They belong to Raw Sockets, are not used for direct user-space communication, and are for internal use within the kernel network stack.</p><p><strong>Analysis</strong>: Based on the code snippet of <code>icmp_sk_init</code> in the text: <code>err = inet_ctl_sock_create(&amp;sk, PF_INET, SOCK_RAW, IPPROTO_ICMP, net);</code> It can be seen that the third parameter <code>IPPROTO_ICMP</code> specifies the protocol type. These sockets are described in the text as &quot;kernel ICMP socket for sending ICMP messages&quot; and are used in <code>icmp_push_reply()</code> to construct and send kernel-generated ICMP packets.</p></details><h3 id="exercise-2-application" tabindex="-1">Exercise 2: Application <a class="header-anchor" href="#exercise-2-application" aria-label="Permalink to &quot;Exercise 2: Application&quot;">​</a></h3><p><strong>Question</strong>: Suppose your host, acting as a router, is forwarding a packet. If the packet&#39;s length (1500 bytes) exceeds the next hop&#39;s link MTU (1400 bytes), and the IP header has the DF (Don&#39;t Fragment) flag set. What kind of ICMP message will the kernel return to the sender? Which function in the kernel code is responsible for sending this message, and what does its <code>info</code> parameter represent?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: It returns an ICMP &quot;Destination Unreachable&quot; (Type 3) message with code &quot;Fragmentation Needed&quot; (Code 4). The sending function is <code>icmp_send()</code> (called within <code>ip_forward</code>). The <code>info</code> parameter (corresponding to the next hop&#39;s MTU value) will be set to 1400 (i.e., <code>dst_mtu(&amp;rt-&gt;dst)</code>).</p><p><strong>Analysis</strong>: This is a typical MTU Discovery scenario. Based on the code snippet of the <code>ip_forward</code> function in the text: <code>icmp_send(skb, ICMP_DEST_UNREACH, ICMP_FRAG_NEEDED, htonl(dst_mtu(&amp;rt-&gt;dst)));</code> When the packet length is greater than the MTU and the DF flag is set, the kernel triggers this logic. The fourth parameter <code>info</code> of the <code>icmp_send</code> function is used in this scenario to inform the peer of the required MTU size, thereby implementing Path MTU Discovery.</p></details><h3 id="exercise-3-thinking" tabindex="-1">Exercise 3: Thinking <a class="header-anchor" href="#exercise-3-thinking" aria-label="Permalink to &quot;Exercise 3: Thinking&quot;">​</a></h3><p><strong>Question</strong>: When the <code>icmp_rcv()</code> function processes a received ICMPv4 packet, if the checksum check fails, the kernel usually discards the packet. Please think about and analyze: why does the kernel only update the statistical counter and discard the packet when the checksum fails, instead of returning a negative error value (like -EINVAL)?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Because if the protocol handling mechanism in <code>ip_local_deliver_finish()</code> receives a negative return value, it will attempt to reprocess the packet (e.g., possibly considering it a transport layer issue, etc.). However, a checksum error in the ICMP packet itself indicates that the packet is corrupted, and there is no need for recovery or reprocessing. The only correct action is to discard it. Returning 0 indicates that the protocol stack has finished processing (even if the processing method was to discard), avoiding needless system overhead.</p><p><strong>Analysis</strong>: As mentioned in the text: <code>The icmp_rcv() method does not return an error in this case... when a protocol handler returns a negative error, another attempt to process the packet is performed, and it is not needed in this case.</code> This is part of the robustness design of the network stack. ICMP is primarily a control message, and a checksum error means the content is untrustworthy and must be silently discarded to prevent operations based on erroneous packets. If an error were returned, it might trigger upper-layer retransmissions or other side effects, which is unsafe.</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>ICMP is an indispensable &quot;nervous system&quot; in IP networks. It goes beyond the scope of a simple Ping tool, serving as the exception handling and feedback mechanism for the network layer. Its core value lies in breaking the black-box nature of IP&#39;s &quot;best-effort&quot; delivery. By reporting errors (such as Destination Unreachable, Time Exceeded) and transmitting diagnostic information, it allows the sender to perceive network state and make adjustments (like PMTU Discovery), acting as the cornerstone of maintaining network debuggability and robustness.</p><p>The kernel&#39;s core mechanism for handling ICMP relies on efficient dispatch and parallel architectures. For the receive path, the kernel uses table lookups or jump dispatching to hand off different types of packets (like Echo, Time Exceeded, Unreachable) to specific handlers. For the send path, to avoid lock contention in multi-core systems, the Linux kernel adopts a design that creates independent Raw Sockets for each CPU, ensuring that control messages can be efficiently generated and sent even under heavy load.</p><p>Although ICMPv4 and ICMPv6 have similar names, there are fundamental differences in their responsibilities. ICMPv4 is primarily used to assist IPv4 with error reporting; whereas ICMPv6 holds a very high status in the IPv6 architecture. It not only inherits the error reporting function but also completely takes over the responsibilities of ARP (Address Resolution) and IGMP (Multicast Management), becoming the unified carrier for Neighbor Discovery (ND) and Multicast Listener Discovery (MLD), and is the absolute core of IPv6 network operations.</p><p>To prevent network storms, ICMP transmission is subject to strict rate limiting. The kernel rate-limits error messages by default to avoid congestion collapse caused by &quot;avalanche&quot; feedback triggered by network failures. However, there are two key exceptions to this rule: the &quot;Packet Too Big&quot; messages required for PMTU Discovery and messages sent to the loopback device must be sent immediately, ensuring timely Path MTU updates and smooth local communication.</p><p>In engineering practice, security policies should favor &quot;reject&quot; over &quot;drop.&quot; Blindly using a firewall to DROP all ICMP will cause MTU Discovery to fail (leading to large packet transmission stalls) and Traceroute to break; a wiser approach is to use the iptables REJECT target or fine-grained procfs configurations (like <code>icmp_echo_ignore_all</code>) to hide specific host information while allowing necessary error messages (like Fragmentation Needed) to pass through, ensuring network connectivity.</p>',100)])])}const u=t(n,[["render",s]]);export{g as __pageData,u as default};
