import{_ as s,o as t,c as i,a as n}from"./chunks/framework.8tiMeCK-.js";const a="/PenguinLab/assets/ch06_fig067.DaWjFOTl.webp",o="/PenguinLab/assets/ch06_fig068.DtmVjMz1.webp",b=JSON.parse('{"title":"6.3  Running and Analyzing SLUB Test Cases","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch06_3.md","filePath":"en/notes/linux_kernel_debugging/ch06_3.md","lastUpdated":null}'),r={name:"en/notes/linux_kernel_debugging/ch06_3.md"};function l(d,e,h,c,u,p){return t(),i("div",null,[...e[0]||(e[0]=[n('<p>Now we reach the validation phase—we throw those problematic test modules into the kernel and see if the security checkpoint we just configured can actually catch the vulnerabilities.</p><hr><h1 id="_6-3-running-and-analyzing-slub-test-cases" tabindex="-1">6.3 Running and Analyzing SLUB Test Cases <a class="header-anchor" href="#_6-3-running-and-analyzing-slub-test-cases" aria-label="Permalink to &quot;6.3  Running and Analyzing SLUB Test Cases&quot;">​</a></h1><p>All the test code is packed into a single module: <code>ch5/kmembugs_test/kmembugs_test.c</code> (don&#39;t forget its partner, <code>debugfs_kmembugs.c</code>). Since this section focuses on SLUB, we&#39;ll only run the cases related to slab memory.</p><p>To clearly see the difference, we&#39;ll compare two extreme environments:</p><ol><li><strong>Debugging completely disabled</strong>: <code>slub_debug=-</code></li><li><strong>SLUB debugging fully enabled</strong>: <code>slub_debug=FZPU</code></li></ol><p>You might ask, why run both? Why not just enable debugging and call it a day?</p><p>The reason is simple: real-world distribution kernels (like the Ubuntu 20.04 LTS used here) have <code>CONFIG_SLUB_DEBUG=y</code> enabled by default, but that doesn&#39;t mean all debugging features are actually running. We need to understand the difference between &quot;having the debugging framework&quot; and &quot;actively debugging with it.&quot;</p><p>There&#39;s also a practical reason: if we test with the debug kernel we compiled earlier with KASAN and UBSAN, those two will jump in and report the errors first—leaving us unable to see SLUB&#39;s own reports. So here, we use a clean production kernel (<code>5.10.60-prod01</code>) to let SLUB take center stage.</p><hr><h3 id="scenario-1-slub-debugging-disabled-slub-debug" tabindex="-1">Scenario 1: SLUB Debugging Disabled (<code>slub_debug=-</code>) <a class="header-anchor" href="#scenario-1-slub-debugging-disabled-slub-debug" aria-label="Permalink to &quot;Scenario 1: SLUB Debugging Disabled (`slub_debug=-`)&quot;">​</a></h3><p>First, let&#39;s remove the safety net and see what running bare looks like. This corresponds to the third column in Table 6.2, as well as the rows marked <code>[V1]</code>, <code>[V2]</code>, and <code>[V3]</code>.</p><p>The results are exactly as dismal as you&#39;d expect:</p><ul><li><p><strong>No response at all</strong>: When <code>slub_debug=-</code>, no memory bugs are caught—including our first three tests (UMR uninitialized memory read, UAR use-after-free, and memory leak), which all slip through the net.</p></li><li><p><strong><code>[V1]</code> unpredictable behavior</strong>: The system might directly Oops, freeze up, or appear completely fine—but that last outcome is the most terrifying. Once the kernel is corrupted, it&#39;s corrupted, even if it hasn&#39;t crashed yet.</p></li><li><p><strong><code>[V2]</code> the fate of double-free</strong>: For the double-free defect, an ordinary vanilla kernel or production kernel will actually react. It might trigger a segmentation fault and throw an error like this:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>kernel BUG at mm/slub.c:305!</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Here, the instruction pointer register (RIP on x86_64) points right at the <code>kfree()</code> API.</p><p>This error message is quite interesting. I specifically looked up the source code for mainline kernel version 5.10.60 (using Bootlin&#39;s source browser, which is incredibly smooth): <a href="https://elixir.bootlin.com/linux/v5.10.60/source/mm/slub.c" target="_blank" rel="noreferrer"><code>mm/slub.c:305</code></a>.</p><p>Take a look:</p><p><img src="'+a+'" alt="Figure 6.1 – Bootlin source code screenshot"></p><p>Right on the money. The bug triggers exactly at line 305. This shows that even a plain vanilla kernel retains a last shred of sanity—it can detect this &quot;raw&quot; double-free and recognize it as memory corruption.</p></li><li><p><strong><code>[V3]</code> UMR remains invisible</strong>: As for uninitialized memory reads (UMR) on slab memory, in a production kernel with <code>slub_debug=-</code>, it still flies under the radar. The region returned by kmalloc might appear to be initialized to <code>0x0</code>, but that&#39;s just an illusion—you&#39;re reading dirty data, and the system isn&#39;t warning you.</p></li></ul><hr><h3 id="scenario-2-slub-debugging-enabled-slub-debug-fzpu" tabindex="-1">Scenario 2: SLUB Debugging Enabled (<code>slub_debug=FZPU</code>) <a class="header-anchor" href="#scenario-2-slub-debugging-enabled-slub-debug-fzpu" aria-label="Permalink to &quot;Scenario 2: SLUB Debugging Enabled (`slub_debug=FZPU`)&quot;">​</a></h3><p>Alright, now let&#39;s pull the safety net back up. We need to reboot the system and change the kernel parameter to <code>slub_debug=FZPU</code> in GRUB.</p><p>This corresponds to the fourth column in Table 6.2.</p><p>If you&#39;re working through VirtualBox or directly on physical hardware, editing the GRUB boot entry looks something like this:</p><p><img src="'+o+`" alt="Figure 6.2 – GRUB edit menu"></p><p>After making the change and rebooting into the system, the first step is always to confirm that the kernel actually received your instruction. Don&#39;t be lazy—this is a must-check:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dmesg</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Kernel command line&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[    0.094445] Kernel command line: BOOT_IMAGE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/boot/vmlinuz-5.11.0-40-generic</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> root</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">UUID</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">...</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ro</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> quiet</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> splash</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> slub_debug=FZPU</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>See the <code>slub_debug=FZPU</code> at the end? (Of course, running <code>cat /proc/cmdline</code> shows the same result.)</p><p>Alright, everything is in place now. Let&#39;s throw the test modules back in.</p><p>This time, the situation completely changes. Look back at Table 6.2 at the rows marked <code>[V4]</code>:</p><p>The SLUB debugging mechanism successfully catches <strong>out-of-bounds writes</strong> (OOB)—whether overflowing upward or downward, neither can escape detection.</p><p>But there&#39;s a subtle point worth noting—if you look closely at the test results, you&#39;ll notice that SLUB&#39;s behavior is somewhat similar to UBSAN&#39;s: it seems only sensitive to &quot;incorrect index out-of-bounds&quot; access. When the out-of-bounds access happens through pointer arithmetic, it might miss it. Additionally, for <strong>out-of-bounds reads</strong> (OOB Read), SLUB also seems powerless under this configuration.</p><p>This is perfectly normal. No single tool offers a god&#39;s-eye view; they all have their own blind spots.</p><p>Now, SLUB has caught the culprits and spit out a pile of error logs. Next, we need to learn a core skill: how to read this SLUB error report like a detective.</p>`,29)])])}const m=s(r,[["render",l]]);export{b as __pageData,m as default};
