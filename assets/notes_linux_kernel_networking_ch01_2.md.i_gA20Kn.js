import{_ as i,o as a,c as n,a as e}from"./chunks/framework.8tiMeCK-.js";const k=JSON.parse('{"title":"1.2  网络设备（The Network Device）","description":"","frontmatter":{},"headers":[],"relativePath":"notes/linux_kernel_networking/ch01_2.md","filePath":"notes/linux_kernel_networking/ch01_2.md","lastUpdated":null}'),l={name:"notes/linux_kernel_networking/ch01_2.md"};function t(p,s,r,o,h,d){return a(),n("div",null,[...s[0]||(s[0]=[e(`<h1 id="_1-2-网络设备-the-network-device" tabindex="-1">1.2 网络设备（The Network Device） <a class="header-anchor" href="#_1-2-网络设备-the-network-device" aria-label="Permalink to &quot;1.2  网络设备（The Network Device）&quot;">​</a></h1><p>让我们把视线落到底层，也就是 Layer 2（L2）——链路层。</p><p>网络设备驱动就住在这里。虽然这本书的主角是内核网络协议栈，而不是教你写驱动（那本身就需要另一本厚书），但为了真正搞懂数据包是怎么进出的，我们必须先认识一下驱动和协议栈之间的「握手人」：<code>net_device</code> 结构体。</p><p>这就好比你要在高速公路上开车，得先懂收费站是怎么运作的——哪怕你不是收费员。</p><h3 id="net-device-网卡的「身份证」" tabindex="-1"><code>net_device</code>：网卡的「身份证」 <a class="header-anchor" href="#net-device-网卡的「身份证」" aria-label="Permalink to &quot;\`net_device\`：网卡的「身份证」&quot;">​</a></h3><p>在内核眼里，一张网卡并不存在物理实体的感觉，它就是一个巨大的结构体实例——<code>net_device</code>。</p><p>这个结构体里装着这张网卡的所有「身家性命」：</p><ul><li><strong>硬件 IRQ 号</strong>：中断请求线，CPU 靠它知道网卡有活干了。</li><li><strong>MTU（最大传输单元）</strong>：以太网默认是 1500 字节。超过这个数，数据包就要被切分（Fragmentation）。</li><li><strong>MAC 地址</strong>：网卡的物理身份证，48位。</li><li><strong>设备名称</strong>：<code>eth0</code>、<code>wlan0</code> 这些名字就在这里定义。</li><li><strong>标志位</strong>：网卡是 UP 还是 DOWN？是不是 RUNNING？</li><li><strong>组播地址列表</strong>。</li><li><strong>混杂模式计数器</strong>：后面马上讲到，这是抓包工具的关键。</li><li><strong>硬件特性</strong>：比如是否支持 GSO、GRO 这些卸载功能。</li><li><strong><code>net_device_ops</code> 回调函数集</strong>：这是网卡的操作手册，包含了打开设备、停止设备、发送数据、更改 MTU 等函数指针。</li><li><strong>ethtool 回调</strong>：这就是为什么你在命令行敲 <code>ethtool eth0</code> 能看到一堆寄存器信息的原因。</li><li><strong>多队列支持</strong>：现在的万兆网卡都有多个 Tx/Rx 队列。</li><li><strong>时间戳</strong>：记录最后一次收发包的时间。</li></ul><p>我们先看一眼内核定义的片段，感受一下它的画风：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            irq;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            /* 中断号                */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device_ops </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">netdev_ops;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 操作方法集           */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            mtu;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            /* 最大传输单元           */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            promiscuity;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 混杂模式计数器         */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">           *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dev_addr;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      /* 硬件地址 (MAC)         */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><blockquote><p><strong>注</strong>：我在附录 A 里详细解剖了这个庞然大物。当你读源码卡壳的时候，不妨回去翻翻。</p></blockquote><p>这里有个特别有趣且容易被忽略的细节：<strong>混杂模式计数器</strong>。</p><p>为什么它是个 <code>int</code> 计数器，而不是一个 <code>bool</code> 标志位？</p><p>如果是布尔值，那当一个抓包工具（比如 <code>tcpdump</code>）打开时，标志位置 1；此时如果另一个抓包工具（比如 <code>wireshark</code>）也打开了，它想关闭时把标志位置回 0，结果第一个工具还在跑，网卡却退出了混杂模式——这就乱了。</p><p>用计数器就能完美解决这个问题：</p><ol><li><code>tcpdump</code> 启动，计数器 +1（变为 1），网卡进入混杂模式，开始接收所有经过线路的数据包（不管是不是发给本机的）。</li><li><code>wireshark</code> 启动，计数器 +1（变为 2）。</li><li><code>tcpdump</code> 退出，计数器 -1（变为 1）。网卡依然是混杂模式，因为 <code>wireshark</code> 还在用。</li><li><code>wireshark</code> 退出，计数器 -1（变为 0）。此时内核确认没人偷窥了，网卡退出混杂模式，恢复正常工作。</li></ol><p>这个设计虽然简单，但它是处理「多用户共享资源状态」的经典教科书案例。</p><hr><h3 id="性能的救赎-napi-new-api" tabindex="-1">性能的救赎：NAPI（New API） <a class="header-anchor" href="#性能的救赎-napi-new-api" aria-label="Permalink to &quot;性能的救赎：NAPI（New API）&quot;">​</a></h3><p>在浏览内核网络核心代码时，你会频繁撞见一个缩写——<strong>NAPI</strong>。</p><p>它是现代网络驱动不可或缺的一部分。要理解它，得先看看旧世界是什么样的。</p><p><strong>旧方案：中断驱动</strong> 早期的网卡驱动工作模式很简单：来一个包，发一个中断。 包来了 → 触发中断 → CPU 停下手里的活，保存上下文 → 跑到中断处理函数 → 拿包 → 恢复上下文。 平时上网没问题，但如果你遭遇了 DDoS 攻击，或者是在处理海量小包流量，CPU 就会崩溃。 每秒几十万个中断意味着什么？意味着 CPU 光是用来「进场」和「退场」（保存/恢复寄存器和上下文）就耗尽了算力，根本没时间干正事。这在操作系统里叫 <strong>中断活锁</strong>。</p><p><strong>新方案：NAPI（混合模式）</strong> 为了解决这个问题，内核开发者引入了 NAPI（New API）。它的核心思想是：<strong>根据负载情况动态切换策略</strong>。</p><ul><li><strong>低负载时</strong>：依然使用中断。没有包就不打扰 CPU，省电且响应快。</li><li><strong>高负载时</strong>：切换到<strong>轮询</strong>。中断触发后，驱动会暂时关闭该中断，然后告诉内核「我现在有一堆包，你自己定期来轮询我拿，别等下一个中断了」。</li></ul><p>这样就把「处理 N 个包需要 N 次中断上下文切换」变成了「处理 N 个包只需要 1 次中断 + 轮询」。性能提升立竿见影。</p><p>不过，世间安得双全法。轮询虽然吞吐量大，但延迟会增加——因为 CPU 不会立刻响应，而是等轮询周期到了才去拿包。</p><p>对于那些对延迟极其敏感、且愿意为此挥霍 CPU 资源的应用（比如高频交易），Linux 从内核 3.11 开始引入了 <strong>Busy Polling on Sockets</strong>（套接字忙轮询）。这属于更偏门的优化，我们留到第 14 章「Busy Poll Sockets」一节再细聊。</p><p>好了，现在我们对网卡这个「出口」已经有了足够的了解。接下来，我们来看看真正的重头戏：<strong>数据包是如何在内核中穿行的</strong>。</p><hr><h3 id="数据包的旅程-收与发" tabindex="-1">数据包的旅程：收与发 <a class="header-anchor" href="#数据包的旅程-收与发" aria-label="Permalink to &quot;数据包的旅程：收与发&quot;">​</a></h3><p>网络设备驱动的一生主要就忙活两件事：</p><ol><li><strong>接收</strong>：把从线路上抓到的包，层层递交给网络层（L3），最终传给传输层（L4）。</li><li><strong>发送</strong>：把本机产生的包，或者需要转发的包，塞进网卡送到线路上。</li></ol><p>听起来很简单？中间的岔路可多着呢。</p><p>对于每一个经过内核的包，无论进还是出，<strong>路由子系统</strong> 都会进行一次查表。 这次查找决定了这张「门票」到底该送去哪里：是留给本机？还是从哪个网卡转发出去？这部分极其重要，我们会在第 5 章和第 6 章深入探讨 IPv4 和 IPv6 的路由子系统。</p><p>但路由决定并不是唯一的阻碍。数据包在栈中穿行时，还会遭遇「路检」：</p><h4 id="_1-netfilter-钩子" tabindex="-1">1. Netfilter 钩子 <a class="header-anchor" href="#_1-netfilter-钩子" aria-label="Permalink to &quot;1. Netfilter 钩子&quot;">​</a></h4><p>这其实就是防火墙和 NAT 背后的机制。内核在网络栈的五个关键节点安插了「检查站」。</p><ul><li>收到的包还没查路由表前，会经过 <code>NF_INET_PRE_ROUTING</code>。</li><li>发出的包即将离开网卡前，会经过 <code>NF_INET_POST_ROUTING</code>。</li></ul><p>这些检查站由 <code>NF_HOOK()</code> 宏触发。如果你的 iptables 规则说这包不合法，回调函数会返回 <code>NF_DROP</code>，数据包当场就会被丢弃，内核甚至懒得给它写讣告。如果是 <code>NF_ACCEPT</code>，那就放行，继续下一站。 关于 Netfilter、连接跟踪以及 iptables 的底层机制，第 9 章会详细拆解。</p><h4 id="_2-ipsec" tabindex="-1">2. IPsec <a class="header-anchor" href="#_2-ipsec" aria-label="Permalink to &quot;2. IPsec&quot;">​</a></h4><p>如果数据包匹配了 IPsec 策略，它还会被送去加密或解密。IPsec 提供了网络层的安全（用 ESP 或 AH 协议）。 虽然 IPv6 规范强制要求支持 IPsec，IPv4 是可选的，但 Linux 在两者上都实现了完整支持。IPsec 有两种模式：</p><ul><li><strong>传输模式</strong>：只加密载荷。</li><li><strong>隧道模式</strong>：把整个原包加密并套在一个新 IP 包里（VPN 的常用方式）。 IPsec 和 NAT 经常打架（因为加密后改不了地址），所以又有了一种「NAT 穿透」方案。这些都留给第 10 章。</li></ul><h4 id="_3-ttl-生存时间" tabindex="-1">3. TTL 生存时间 <a class="header-anchor" href="#_3-ttl-生存时间" aria-label="Permalink to &quot;3. TTL 生存时间&quot;">​</a></h4><p>如果一个包是被转发的，它的 IPv4 头部里有个叫 <code>TTL</code>（Time To Live）的字段。每经过一个路由器，这个值就减 1。 当它减到 0 时，路由器会直接丢弃这个包，并回送一个 ICMP 「Time Exceeded」 报文。这能防止因为路由环路导致数据包在网络里长生不老。 同样的，每改一次 TTL，还得把 IPv4 的校验和 重算一遍，挺费工夫。 在 IPv6 里，这个字段改名叫 <code>Hop Limit</code>（跳数限制），意思一样，名字更文雅一点。</p><p>此外，数据包的旅途还充满了变数：</p><ul><li><strong>分片与重组</strong>：大包（超过 MTU）会被切碎，接收端还得拼起来。这在第 4 章讲。</li><li><strong>组播</strong>：包不是发给一个人的，是发给一群人的。这涉及 IGMP 协议和组播路由守护进程（像 <code>pimd</code>），这通常比单播要复杂得多，第 6 章会聊到。</li></ul><p>为了在这么多复杂的岔路中还能有条不紊，内核必须有一个统一的方式来描述和管理这些数据包。这个核心数据结构，就是传说中的 <strong>SKB</strong>。</p><hr><h3 id="核心-socket-buffer-sk-buff" tabindex="-1">核心：Socket Buffer (<code>sk_buff</code>) <a class="header-anchor" href="#核心-socket-buffer-sk-buff" aria-label="Permalink to &quot;核心：Socket Buffer (\`sk_buff\`)&quot;">​</a></h3><p><code>sk_buff</code>（我们通常简称为 <strong>SKB</strong>）是 Linux 内核网络栈里最核心、最复杂、也最令人头秃的数据结构。</p><p>它就是数据包在内核里的「肉身」。无论这个包是刚刚被网卡驱动捞上来，还是正准备从 TCP 层发出去，它都是一个 SKB。</p><p>我们先看一眼它的定义（只列了一部分重点成员，完整版请看附录 A）：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sock             </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sk;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            /* 拥有这个包的套接字      */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device       </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dev;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           /* 关联的网卡设备          */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __u8                    pkt_type:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     /* 包类型（单播/组播/广播）*/</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __be16                  protocol;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       /* 协议类型               */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          tail;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           /* 尾部指针                */</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          end;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            /* 结束指针                */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">           *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">head,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                            *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">data;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          /* 头部和数据指针          */</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          transport_header;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* L4 头部位置          */</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          network_header;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /* L3 头部位置          */</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          mac_header;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      /* L2 头部位置          */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br></div></div><p><strong>操作 SKB 的黄金法则</strong> 千万不要试图去手动 <code>skb-&gt;data++</code> 这种直接操作指针！SKB 的内部管理有一套严格的 API：</p><ul><li>想把 <code>data</code> 指针往后挪（剥掉头部）？用 <code>skb_pull()</code> 或 <code>skb_pull_inline()</code>。</li><li>想往前挪（预留头部空间）？用 <code>skb_push()</code>。</li><li>想拿传输层（L4）头？用 <code>skb_transport_header(skb)</code>。</li><li>想拿网络层（L3）头？用 <code>skb_network_header(skb)</code>。</li><li>想拿 MAC 头？用 <code>skb_mac_header(skb)</code>。</li></ul><p>遵守这套 API 是为了管理 SKB 内部那个精妙的线性数据区和分页结构，千万别自作聪明。</p><h4 id="skb-的诞生-接收路径" tabindex="-1">SKB 的诞生（接收路径） <a class="header-anchor" href="#skb-的诞生-接收路径" aria-label="Permalink to &quot;SKB 的诞生（接收路径）&quot;">​</a></h4><p>当一个数据包从网线进来时，驱动程序会通过 <code>netdev_alloc_skb()</code>（老代码里可能用 <code>dev_alloc_skb()</code>）分配一个 SKB。</p><p>在链路层（L2），驱动会做两件重要的事：</p><ol><li><p><strong>判断类型 (<code>eth_type_trans</code>)</strong>： 这个函数会根据以太网帧头部的目标 MAC 地址，来设置 SKB 的 <code>pkt_type</code>：</p><ul><li>是发给本机的？→ <code>PACKET_HOST</code>。</li><li>是组播？→ <code>PACKET_MULTICAST</code>。</li><li>是广播？→ <code>PACKET_BROADCAST</code>。 同时，它还会读取以太网头的 Type 字段（比如 <code>0x0800</code> 是 IPv4，<code>0x86DD</code> 是 IPv6），填入 SKB 的 <code>protocol</code> 字段。</li></ul></li><li><p><strong>指针跳跃 (<code>skb_pull_inline</code>)</strong>： 这是新手最容易晕的地方。以太网帧进到内存时，<code>skb-&gt;data</code> 是指向 L2 头部的起始位置的。 但是！ 当驱动把这个包交给网络层（L3）的那一刻，内核希望 <code>skb-&gt;data</code> 指向 L3 头部（IP 头）。 所以 <code>eth_type_trans()</code> 会调用 <code>skb_pull_inline(skb, 14)</code>，把指针往后挪 14 字节（正好是以太网头 <code>ETH_HLEN</code> 的长度），跳过 L2 头。</p><p><strong>想象一下</strong>：你在剥洋葱。剥掉一层（L2），手里剩下的应该刚好是下一层（L3）。<code>skb_pull</code> 就是这个剥皮动作。</p></li></ol><p><em>(此处插入原书 Figure 1-3 示意图：一个标准的 UDPv4 数据包，从 L2 的 14 字节，到 L3 的 20 字节，再到 L4 的 8 字节)</em></p><h4 id="skb-的流浪" tabindex="-1">SKB 的流浪 <a class="header-anchor" href="#skb-的流浪" aria-label="Permalink to &quot;SKB 的流浪&quot;">​</a></h4><p>每一个 SKB 都有一个 <code>dev</code> 指针，记录了「我是谁家的」（进来的包记录输入网卡，出去的包记录输出网卡）。这很重要，因为内核需要根据这个网卡的 MTU 来决定要不要切片。</p><p>每个传输出去的 SKB 还有一个 <code>sk</code> 指针，指向产生这个包的那个 Socket。 <strong>注意</strong>：如果是转发的包，<code>sk</code> 是 <strong>NULL</strong>。因为它不是本地生的，只是个「过路客」。</p><h4 id="skb-的归宿" tabindex="-1">SKB 的归宿 <a class="header-anchor" href="#skb-的归宿" aria-label="Permalink to &quot;SKB 的归宿&quot;">​</a></h4><p>接收到的包最终会被分发到对应的协议处理函数：</p><ul><li>IPv4 的包会被扔给 <code>ip_rcv()</code>。</li><li>IPv6 的包会被扔给 <code>ipv6_rcv()</code>。</li></ul><p>这些函数是怎么注册进去的？用 <code>dev_add_pack()</code> 方法。这个我们会在 IPv4 和 IPv6 的专门章节（第 4 章和第 8 章）细看。</p><p>以 <code>ip_rcv()</code> 为例，它先做一大堆健康检查（Sanity Checks），然后——如果没被 Netfilter 的 <code>PRE_ROUTING</code> 钩子拦截——就会进入 <code>ip_rcv_finish()</code>。 在这里，内核会去查路由子系统，构建一个 <code>dst_entry</code>（目标缓存项），这个条目决定了这个包下一步往哪走。关于路由子系统，第 5 和第 6 章有硬核分析。</p><hr><h3 id="链路层的细节-以太网与邻居" tabindex="-1">链路层的细节：以太网与邻居 <a class="header-anchor" href="#链路层的细节-以太网与邻居" aria-label="Permalink to &quot;链路层的细节：以太网与邻居&quot;">​</a></h3><p>最后，我们得提一下 L2 层的那些琐碎但致命的事。</p><p><strong>MAC 地址与 ARP</strong> 每个网卡出厂时都带个 48 位的 MAC 地址，虽然你可以用 <code>ifconfig</code> 或 <code>ip</code> 命令篡改它。 当你的 Socket 想发个包给 <code>192.168.1.5</code> 时，它只知道 IP 地址。但是，以太网帧只认 MAC 地址。 这就需要一个翻译官：<strong>邻居子系统</strong>。</p><ul><li><strong>IPv4</strong> 用 <strong>ARP</strong> 协议。它会在局域网里喊一嗓子（广播）：「谁有 192.168.1.5？告诉我你的 MAC！」</li><li><strong>IPv6</strong> 用 <strong>NDISC</strong> 协议（基于 ICMPv6），原理类似，但更复杂，而且用的是组播而不是广播。</li></ul><p>这部分机制在第 7 章有专门讲解。如果你网络不通，先 ping 一下，然后再看 <code>arp -n</code>，往往就是这里出了问题。</p><p><strong>内核与用户空间的通信：Netlink</strong> 内核怎么告诉用户空间路由变了？或者用户空间的工具怎么告诉内核「我要加一条路由」？ 靠的是 <strong>Netlink Sockets</strong>。它像是一种特殊的电话线，一头连着内核，一头连着 <code>iproute2</code> 这种工具。这在第 2 章会详细讲。</p><hr><h3 id="特殊的子系统-无线、rdma-与虚拟化" tabindex="-1">特殊的子系统：无线、RDMA 与虚拟化 <a class="header-anchor" href="#特殊的子系统-无线、rdma-与虚拟化" aria-label="Permalink to &quot;特殊的子系统：无线、RDMA 与虚拟化&quot;">​</a></h3><p>Linux 网络栈的大厦里，还有几间特殊的「VIP 包厢」。</p><ol><li><p><strong>无线子系统</strong>： 这是由 <code>mac80211</code> 框架驱动的。它处理的事情普通有线网卡见都没见过：省电模式、Mesh 组网（HWMP 路由协议）、Ad-hoc 网络。 尤其是 802.11n 的块确认机制，那是提高吞吐量的关键。这些内容留到第 12 章。</p></li><li><p><strong>RDMA / InfiniBand</strong>： 在高性能计算和数据中心里，传统的 CPU 搬运数据太慢了。RDMA 允许网卡直接读写远程主机的内存，绕过 CPU。 这种技术从内核 2.6.11 开始引入，现在在大型集群里遍地都是。第 13 章会讲。</p></li><li><p><strong>虚拟化</strong>： 基于 Namespaces 的进程级虚拟化。 这不是那种 KVM 的全虚拟化，而是更轻量的「隔离」。Linux 现在有六种 Namespace。网络 Namespace 允许你在一个机器上跑多个完全独立的网络协议栈（多个 <code>lo</code> 接口，多个路由表）。 这是 Docker 等容器技术的基石。我们会在第 14 章深入讨论，还会顺带提一下 Bluetooth、PCIe 以及物联网领域的 6LoWPAN（把 IPv6 塞进 IEEE 802.15.4 这种低功耗帧里，想想都觉得挤）。</p></li></ol><hr><p>网络设备就介绍到这里。从下一节开始，我们将真正潜入代码，去看看 Linux 内核网络开发是如何进行的——那是另一个充满了 Git tree 和邮件列表江湖的地方。</p>`,82)])])}const g=i(l,[["render",t]]);export{k as __pageData,g as default};
