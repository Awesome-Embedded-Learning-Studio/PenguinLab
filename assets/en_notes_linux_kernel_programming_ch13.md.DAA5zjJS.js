import{_ as s,o as i,c as a,a as t}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"Chapter 13: Kernel Synchronization (Part 2)","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_programming/ch13.md","filePath":"en/notes/linux_kernel_programming/ch13.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_programming/ch13.md"};function o(r,e,l,h,c,d){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="chapter-13-kernel-synchronization-part-2" tabindex="-1">Chapter 13: Kernel Synchronization (Part 2) <a class="header-anchor" href="#chapter-13-kernel-synchronization-part-2" aria-label="Permalink to &quot;Chapter 13: Kernel Synchronization (Part 2)&quot;">​</a></h1><p>This is not just a tutorial on &quot;how to lock.&quot; We&#39;ve survived the foundational洗礼 of the previous chapter, and now we face the truly tricky problems in kernel synchronization—the ones that really test your understanding of the system&#39;s underlying mechanics.</p><p>If the previous chapter taught you how to &quot;follow the rules,&quot; this chapter tells you: rules are meant to be broken—or rather, redefined at a higher dimension of performance.</p><p>We are about to enter a minefield: the traps of integer overflow, &quot;lock-free&quot; programming that looks deadlocked but isn&#39;t, cache false sharing that can cause multi-core performance to plummet, and the RCU mechanism, hailed as a miracle of &quot;social engineering.&quot;</p><p>There are few &quot;taken-for-granted&quot; assumptions here. Behind every API lies precise control over hardware behavior.</p><hr><h2 id="_13-1-when-integers-become-a-battlefield-atomic-operations-and-reference-counting" tabindex="-1">13.1 When Integers Become a Battlefield: Atomic Operations and Reference Counting <a class="header-anchor" href="#_13-1-when-integers-become-a-battlefield-atomic-operations-and-reference-counting" aria-label="Permalink to &quot;13.1  When Integers Become a Battlefield: Atomic Operations and Reference Counting&quot;">​</a></h2><p>Let&#39;s start with the simplest scenario. Remember when you first wrote that simple misc character device driver? (In the companion code for <em>Linux Kernel Programming – Part 2</em>). In that driver&#39;s <code>open</code> method, you might have written something like this:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ga, gb </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* ... */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ga</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">++</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">gb</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">--</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>This looks harmless enough. But if you remember the &quot;critical section&quot; concept we repeatedly emphasized in the last chapter, you should already be breaking out in a cold sweat: <code>ga</code> and <code>gb</code> are global variables, which means <strong>shared writable state</strong>. If multiple processes try to open this device simultaneously, this code is a classic data race in the making.</p><p>In the previous chapter, we fixed this with a mutex, and then improved performance with a spinlock. The code became this:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">spin_lock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">lock1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ga</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">++</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; gb</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">--</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">spin_unlock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">lock1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>There&#39;s nothing wrong with this, but is this the finish line? Not quite.</p><p><strong>This is too heavy.</strong></p><p>Just to add 1 or subtract 1 from two integers, we have to introduce a lock and handle various edge cases that might lead to sleeping or blocking. Kernel developers realized that integer operations are so frequent (reference counting, resource statistics, state flags) that they deserve a dedicated, hardware-level atomic instruction set.</p><p>This is why <code>atomic_t</code> and the later, safer <code>refcount_t</code> came into play.</p><hr><h3 id="_13-1-1-atomic-t-and-refcount-t-the-old-trust-and-the-new-safety" tabindex="-1">13.1.1 <code>atomic_t</code> and <code>refcount_t</code>: The Old Trust and the New Safety <a class="header-anchor" href="#_13-1-1-atomic-t-and-refcount-t-the-old-trust-and-the-new-safety" aria-label="Permalink to &quot;13.1.1  \`atomic_t\` and \`refcount_t\`: The Old Trust and the New Safety&quot;">​</a></h3><p>There is a legacy issue here. Prior to kernel 4.10 (inclusive), we only had the <code>atomic_t</code> interface. It worked well, but had a fatal flaw: <strong>it is an integer, and it can overflow or underflow</strong>.</p><p>If you change a reference count from 0 to -1, or from INT_MAX to 0, the kernel might not warn you at all. The data could silently corrupt, leading to the most dreaded <strong>Use-After-Free (UAF)</strong> vulnerability.</p><p>Starting with kernel 4.11, Linux introduced a new set of interfaces: <code>refcount_t</code>.</p><p>You can think of it as a &quot;hardened&quot; version of <code>atomic_t</code>. Its design philosophy is extremely aggressive: <strong>better safe than sorry</strong>.</p><hr><h3 id="an-analogy-the-anti-jam-counter" tabindex="-1">An Analogy: The &quot;Anti-Jam&quot; Counter <a class="header-anchor" href="#an-analogy-the-anti-jam-counter" aria-label="Permalink to &quot;An Analogy: The &quot;Anti-Jam&quot; Counter&quot;">​</a></h3><p>Imagine a traditional mechanical counter (<code>atomic_t</code>). If you push it hard enough, it keeps turning, going from 9999 back to 0000, or even becoming negative (if signed). This is called <strong>wrap-around</strong>. In the programming world, this usually means disaster.</p><p><code>refcount_t</code>, on the other hand, is like a modern electronic counter with an <strong>anti-jam</strong> mechanism. If you try to dial it below 0 or above the maximum value, it <strong>locks up</strong> at a specific error value (like <code>REFCOUNT_SATURATED</code>) and loudly alarms (WARN). It will never silently wrap around.</p><hr><p>But the &quot;anti-jam&quot; analogy for <code>refcount_t</code> has one caveat: <strong>it&#39;s not just fool-proofing; it also involves memory ordering</strong>.</p><p>With traditional <code>atomic_t</code>, we mainly care that the operation itself is indivisible. But with <code>refcount_t</code>, to guarantee absolute safety in multi-core environments, the kernel enforces the ordering of memory operations. This isn&#39;t simple &quot;addition and subtraction&quot;; it&#39;s coordinating the reality perceived by multiple CPU cores.</p><p>Returning to the anti-jam counter: it doesn&#39;t just lock up; at the moment it jams, it forcibly flushes all &quot;counter caches,&quot; ensuring everyone else knows it&#39;s broken, rather than looking at an old, incorrect value and continuing to operate.</p><hr><h3 id="comparing-the-old-and-new-interfaces-a-table-to-build-intuition" tabindex="-1">Comparing the Old and New Interfaces: A Table to Build Intuition <a class="header-anchor" href="#comparing-the-old-and-new-interfaces-a-table-to-build-intuition" aria-label="Permalink to &quot;Comparing the Old and New Interfaces: A Table to Build Intuition&quot;">​</a></h3><p>Before diving into details, let&#39;s look at a table to help you quickly build intuition.</p><p>The usage of <code>atomic_t</code> and <code>refcount_t</code> is very similar, but their <strong>semantics and scope</strong> are completely different.</p><table tabindex="0"><thead><tr><th style="text-align:left;">Operation</th><th style="text-align:left;">Legacy 32-bit <code>atomic_t</code></th><th style="text-align:left;">New <code>refcount_t</code> (32/64-bit)</th></tr></thead><tbody><tr><td style="text-align:left;"><strong>Valid Range</strong></td><td style="text-align:left;">Entire <code>int</code> range</td><td style="text-align:left;"><strong>Strictly limited:</strong> <code>[1 .. INT_MAX-1]</code></td></tr><tr><td style="text-align:left;"><strong>Initialization</strong></td><td style="text-align:left;"><code>static atomic_t v = ATOMIC_INIT(1);</code></td><td style="text-align:left;"><code>static refcount_t v = REFCOUNT_INIT(1);</code></td></tr><tr><td style="text-align:left;"><strong>Read</strong></td><td style="text-align:left;"><code>int val = atomic_read(&amp;v);</code></td><td style="text-align:left;"><code>unsigned int val = refcount_read(&amp;v);</code></td></tr><tr><td style="text-align:left;"><strong>Set</strong></td><td style="text-align:left;"><code>atomic_set(&amp;v, i);</code></td><td style="text-align:left;"><code>refcount_set(&amp;v, i);</code></td></tr><tr><td style="text-align:left;"><strong>Increment</strong></td><td style="text-align:left;"><code>atomic_inc(&amp;v);</code></td><td style="text-align:left;"><code>refcount_inc(&amp;v);</code></td></tr><tr><td style="text-align:left;"><strong>Decrement</strong></td><td style="text-align:left;"><code>atomic_dec(&amp;v);</code></td><td style="text-align:left;"><code>refcount_dec(&amp;v);</code></td></tr><tr><td style="text-align:left;"><strong>Add</strong></td><td style="text-align:left;"><code>atomic_add(i, &amp;v);</code></td><td style="text-align:left;"><code>refcount_add(i, &amp;v);</code></td></tr><tr><td style="text-align:left;"><strong>Subtract</strong></td><td style="text-align:left;"><code>atomic_sub(i, &amp;v);</code></td><td style="text-align:left;"><code>refcount_sub(i, &amp;v);</code></td></tr></tbody></table><blockquote><p><strong>⚠️ Pitfall Warning</strong> Never use <code>refcount_t</code> as a general-purpose atomic integer! Its <strong>design contract</strong> dictates it can only be used between <code>[1, INT_MAX-1]</code>. If you set it to 0, or if it decrements to 0, it will trigger a <code>WARN_ONCE()</code> and saturate at a special negative value (<code>0xc0000000</code>, which is <code>REFCOUNT_SATURATED</code>). If you need a general-purpose atomic counter, stick to <code>atomic_t</code>.</p></blockquote><p><strong>Thought Exercise</strong>: Why doesn&#39;t <code>refcount_t</code> allow 0?</p><blockquote><p><strong>Answer</strong>: Because an object&#39;s lifecycle is managed by its reference count. When the count reaches 0, the object should be freed. If you can still read a refcount of 0, it means you already have a Use-After-Free logic bug. Therefore, the last person to see it at 1 is responsible for freeing the object, rather than letting it drop to 0 and linger in memory.</p></blockquote><hr><h3 id="hands-on-overflowing-a-reference-count-and-hearing-the-kernel-scream" tabindex="-1">Hands-on: Overflowing a Reference Count (and Hearing the Kernel Scream) <a class="header-anchor" href="#hands-on-overflowing-a-reference-count-and-hearing-the-kernel-scream" aria-label="Permalink to &quot;Hands-on: Overflowing a Reference Count (and Hearing the Kernel Scream)&quot;">​</a></h3><p>Talk is cheap. Let&#39;s deliberately cause an overflow and see how the <code>refcount_t</code> &quot;anti-jam&quot; mechanism works.</p><p>Here is a piece of deliberately broken code:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> refcount_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ga </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> REFCOUNT_INIT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">42</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 初始化为 42 */</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> open_miscdrv_rdwr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> inode </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">inode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> file </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">filp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 正常情况：自增 */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    refcount_inc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ga);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* ... 坏情况（通过宏控制）：故意溢出 ... */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#if</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    pr_debug(&quot;*** Bad case! About to overflow refcount var! ***\\n&quot;);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 加上一个巨大的 INT_MAX，必定溢出 */</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    refcount_add(INT_MAX, &amp;ga);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#endif</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br></div></div><p>If you change <code>#if 0</code> to <code>#if 1</code>, compile, load, and run this module, you&#39;ll see a glaring <code>WARNING</code> pop up in the system logs.</p><p>More importantly, when you check the value of <code>ga</code>, it&#39;s no longer a small wrapped-around number, but a weird constant: <code>0xc0000000</code>.</p><p>This is <code>REFCOUNT_SATURATED</code>.</p><p><strong>What does this mean?</strong> The kernel is saying: &quot;I know something went wrong, but I&#39;m pinning it to this error state to prevent it from becoming a seemingly legitimate value (like 0) that could cause subsequent code to misjudge and free memory.&quot; This is a strategy of <strong>trading availability for safety</strong>.</p><hr><h3 id="_13-1-2-the-64-bit-world-atomic64-t" tabindex="-1">13.1.2 The 64-bit World: <code>atomic64_t</code> <a class="header-anchor" href="#_13-1-2-the-64-bit-world-atomic64-t" aria-label="Permalink to &quot;13.1.2  The 64-bit World: \`atomic64_t\`&quot;">​</a></h3><p>We&#39;ve been discussing 32-bit <code>atomic_t</code> above. The world is 64-bit now, so obviously we need 64-bit atomic operations too.</p><p>There&#39;s not much to say here; it&#39;s just a matter of changing the name.</p><ul><li>Type: <code>atomic64_t</code> (which is essentially <code>atomic_long_t</code>)</li><li>API: Replace all <code>atomic_</code> prefixes with <code>atomic64_</code>.</li></ul><p>For example:</p><ul><li><code>ATOMIC_INIT(1)</code> -&gt; <code>ATOMIC64_INIT(1)</code></li><li><code>atomic_read()</code> -&gt; <code>atomic64_read()</code></li></ul><hr><h3 id="_13-1-3-rmw-not-just-read-and-write-but-read-modify-write" tabindex="-1">13.1.3 RMW: Not Just Read and Write, but &quot;Read-Modify-Write&quot; <a class="header-anchor" href="#_13-1-3-rmw-not-just-read-and-write-but-read-modify-write" aria-label="Permalink to &quot;13.1.3  RMW: Not Just Read and Write, but &quot;Read-Modify-Write&quot;&quot;">​</a></h3><p>So far, we&#39;ve only dealt with simple addition and subtraction. But in the kernel, especially when writing drivers, there&#39;s a more subtle requirement: <strong>bit manipulation</strong>.</p><p>Imagine you&#39;re operating on a hardware register (MMIO). You want to set bit 7 (the MSB) to 1 to enable a certain feature.</p><p>You might write code like this:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">u8 tmp;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">tmp </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ioread8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(CTRL_REG);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 1. 读 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">tmp </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 0x</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">80</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">             /* 2. 改 */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iowrite8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(tmp, CTRL_REG);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 3. 写 */</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>This is the classic <strong>RMW (Read-Modify-Write)</strong> sequence.</p><p><strong>But this is dangerous.</strong></p><p>If a context switch occurs between step 1 and step 3, or if another core simultaneously modifies this register, your operation might be overwritten, or you might overwrite someone else&#39;s operation. This is a <strong>data race</strong>.</p><p><strong>Solution 1: Add a lock</strong> You could wrap these three steps in a spinlock. But this is the same old story of &quot;introducing a lock for a few instructions&quot;—the overhead is too high.</p><p><strong>Solution 2: Atomic RMW Instructions</strong> Modern CPUs (x86&#39;s <code>lock</code> prefix, ARM&#39;s <code>LDXR</code>/<code>STXR</code>) provide one-stop atomic RMW instructions. The Linux kernel wraps them into extremely handy APIs.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 以前 */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">spin_lock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">lock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">tmp </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ioread8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(CTRL_REG);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">tmp </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 0x</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">80</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iowrite8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(tmp, CTRL_REG);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">spin_unlock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">lock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 现在 */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set_bit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">7</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, CTRL_REG);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 搞定 */</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><hr><h4 id="revealing-the-catch-the-lie-of-atomic-bit-operations" tabindex="-1">Revealing the Catch: The &quot;Lie&quot; of Atomic Bit Operations <a class="header-anchor" href="#revealing-the-catch-the-lie-of-atomic-bit-operations" aria-label="Permalink to &quot;Revealing the Catch: The &quot;Lie&quot; of Atomic Bit Operations&quot;">​</a></h4><p>Here I want to reveal a &quot;lie&quot; about <code>set_bit()</code>.</p><p>Although we call it &quot;atomic bit operations&quot; and it indeed gets the job done with a single assembly instruction, in a <strong>multi-core</strong> environment, it <strong>does not guarantee atomicity across cores</strong>.</p><p>Wait, what?</p><p><strong>This sounds counterintuitive, but look closely</strong>: <code>set_bit()</code> guarantees that the operation is atomic on the current CPU core. However, if CPU 0 is operating on a certain memory address while CPU 1 is simultaneously operating on the same address (whether using <code>set_bit</code> or something else), it still constitutes a race.</p><p><strong>So, the rule is</strong>:</p><ul><li>If you are operating on <strong>normal memory</strong> (RAM) and it involves multi-core concurrent access, you <strong>still need a lock</strong> to wrap <code>set_bit()</code>, or you need to use a variable with built-in lock semantics like <code>atomic_t</code>.</li><li>If you are operating on <strong>device registers</strong> (MMIO) and the hardware itself guarantees some level of atomicity (or you only care about simple scenarios that don&#39;t need locks), <code>set_bit()</code> is a godsend.</li></ul><p><strong>Why is this important?</strong> Because many beginners mistakenly assume <code>set_bit()</code> is omnipotent, thereby omitting locks that should exist in their drivers. Don&#39;t fall into this trap.</p><hr><h4 id="back-to-that-device-register-why-is-set-bit-better" tabindex="-1">Back to That Device Register: Why is <code>set_bit</code> Better? <a class="header-anchor" href="#back-to-that-device-register-why-is-set-bit-better" aria-label="Permalink to &quot;Back to That Device Register: Why is \`set_bit\` Better?&quot;">​</a></h4><p>Returning to the device example from earlier. If we just want to control a single bit, using <code>set_bit(7, CTRL_REG)</code> isn&#39;t just about saving the effort of writing lock code.</p><p><strong>It&#39;s genuinely fast.</strong></p><p>Benchmark comparisons on x86_64 show:</p><ul><li><strong>Manual locked RMW</strong>: ~125 nanoseconds.</li><li><strong><code>set_bit()</code> RMW</strong>: ~29 nanoseconds.</li></ul><p><strong>A difference of more than 4x.</strong></p><p>The reason behind this is simple: even without contention, acquiring and releasing a spinlock involves memory barriers and bus locking overhead. The <code>lock</code> instruction (the underlying implementation of <code>set_bit</code>) also locks the bus, but it only locks for that single operation and is directly optimized by hardware, resulting in extremely low overhead.</p><hr><h3 id="_13-1-4-confessions-of-a-reader-writer-lock-i-might-starve-the-writers" tabindex="-1">13.1.4 Confessions of a Reader-Writer Lock: I Might Starve the Writers <a class="header-anchor" href="#_13-1-4-confessions-of-a-reader-writer-lock-i-might-starve-the-writers" aria-label="Permalink to &quot;13.1.4  Confessions of a Reader-Writer Lock: I Might Starve the Writers&quot;">​</a></h3><p>Let&#39;s shift our perspective from &quot;bit manipulation&quot; back to &quot;large chunks of data.&quot;</p><p>Suppose you have a massive doubly linked list with thousands of nodes. You frequently need to traverse it (read) and occasionally insert or delete nodes (write).</p><p>If you use a regular <code>spinlock</code>, even if ten threads just want to &quot;look at&quot; the data, they have to queue up and enter one by one. What a waste.</p><p><strong><code>rwlock_t</code> (Reader-Writer Spinlock)</strong> was born for this.</p><p>It allows multiple readers to enter simultaneously without interfering with each other. Only when a writer comes does it need exclusive access.</p><p><strong>Sounds perfect, right?</strong></p><hr><h4 id="the-twist-the-writer-s-nightmare" tabindex="-1">The Twist: The Writer&#39;s Nightmare <a class="header-anchor" href="#the-twist-the-writer-s-nightmare" aria-label="Permalink to &quot;The Twist: The Writer&#39;s Nightmare&quot;">​</a></h4><p>But there is a subtle twist here... and it&#39;s a performance &quot;death sentence.&quot;</p><p>Imagine this scenario:</p><ol><li>Reader A acquires the read lock.</li><li>Reader B acquires the read lock.</li><li>Reader C acquires the read lock.</li><li>At this point, a writer comes along and wants to write. It must wait for A, B, and C to all release.</li><li><strong>Here&#39;s the problem</strong>: While A, B, and C aren&#39;t finished yet, readers D, E, and F arrive! Because as long as there&#39;s no writer, read locks can be issued infinitely.</li><li>The result: <strong>the writer is starved</strong>.</li></ol><p>As long as there&#39;s a steady stream of read requests, the writer might never acquire the lock. This is fatal in scenarios that are &quot;read-mostly, write-rarely&quot; but where &quot;writes must be responded to in a timely manner.&quot;</p><p><strong>And it doesn&#39;t stop there.</strong></p><p><strong>There&#39;s an even more insidious killer: cache false sharing.</strong></p><p>Even without writers, when multiple cores&#39; readers simultaneously access this linked list, although they don&#39;t compete for the lock, they <strong>compete for cache lines</strong>. The &quot;prev&quot; and &quot;next&quot; pointers inside a linked list node are in the same cache line. Core 0 reads one node, Core 1 reads another. But to maintain memory coherence, the cache line bounces back and forth (ping-pong) between these cores, causing performance to plummet.</p><p><strong>It really doesn&#39;t stop there.</strong></p><p>The modern Linux kernel community is actively working to <strong>remove</strong> <code>rwlock_t</code>.</p><p>Why? Because there is a more powerful mechanism that almost perfectly solves all the pain points mentioned above.</p><p>It is <strong>RCU (Read-Copy-Update)</strong>.</p><hr><h2 id="_13-2-cache-effects-and-false-sharing-the-invisible-killer-of-multi-core-performance" tabindex="-1">13.2 Cache Effects and False Sharing: The Invisible Killer of Multi-Core Performance <a class="header-anchor" href="#_13-2-cache-effects-and-false-sharing-the-invisible-killer-of-multi-core-performance" aria-label="Permalink to &quot;13.2  Cache Effects and False Sharing: The Invisible Killer of Multi-Core Performance&quot;">​</a></h2><p>Before formally introducing RCU, we must understand why multi-core programming is so hard.</p><p>It&#39;s not because the code is hard to write, but because <strong>the hardware is playing tricks on you</strong>.</p><hr><h3 id="_13-2-1-how-cpu-caches-work" tabindex="-1">13.2.1 How CPU Caches Work <a class="header-anchor" href="#_13-2-1-how-cpu-caches-work" aria-label="Permalink to &quot;13.2.1  How CPU Caches Work&quot;">​</a></h3><p>Modern CPUs don&#39;t read from and write to RAM directly. They read from and write to L1, L2, and L3 caches.</p><p>Here is a key concept: the <strong>cache line</strong>.</p><p>When a CPU reads data from RAM, it doesn&#39;t just read that single byte; it reads <strong>64 bytes</strong> (a typical value) in one go. This is a cache line.</p><p><strong>This is originally a good thing</strong>: leveraging the principle of &quot;spatial locality,&quot; if you access <code>myarr[0]</code>, immediately accessing <code>myarr[1]</code> through <code>myarr[63]</code> will all be in the L1 cache, incredibly fast.</p><hr><h3 id="_13-2-2-cache-coherence-the-worldview-war-of-multi-core-systems" tabindex="-1">13.2.2 Cache Coherence: The &quot;Worldview&quot; War of Multi-Core Systems <a class="header-anchor" href="#_13-2-2-cache-coherence-the-worldview-war-of-multi-core-systems" aria-label="Permalink to &quot;13.2.2  Cache Coherence: The &quot;Worldview&quot; War of Multi-Core Systems&quot;">​</a></h3><p>But in multi-core systems, this becomes a problem.</p><p><strong>Imagine this</strong>:</p><ul><li>Core 0 reads the global variable <code>N</code> (value 55) into its cache line.</li><li>Core 0 changes <code>N</code> to 41.</li><li>At this point, Core 1&#39;s cache still holds the old value 55.</li><li>Core 1 wants to add 1 to <code>N</code>.</li></ul><p>Without intervention, Core 1 would write <code>55 + 1 = 56</code> back, overwriting Core 0&#39;s modification.</p><p>To prevent this split personality, hardware must guarantee <strong>cache coherence</strong>. When Core 0 modifies <code>N</code>, it must signal Core 1 to invalidate the <code>N</code> in its cache. Core 1 must re-read the new value from RAM (or from Core 0&#39;s cache).</p><p>This &quot;invalidate-and-reread&quot; process is <strong>cache synchronization</strong>.</p><p><strong>What&#39;s the cost?</strong> Extremely expensive. It involves bus traffic, stalls, and waiting.</p><hr><h3 id="_13-2-3-false-sharing-enemies-sharing-the-same-room" tabindex="-1">13.2.3 False Sharing: Enemies Sharing the Same Room <a class="header-anchor" href="#_13-2-3-false-sharing-enemies-sharing-the-same-room" aria-label="Permalink to &quot;13.2.3  False Sharing: Enemies Sharing the Same Room&quot;">​</a></h3><p>If it were only truly shared variables (like that <code>N</code>), it would be manageable. What&#39;s most feared is <strong>false sharing</strong>.</p><p>Look at these two variables:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">u16 ax </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">u16 bx </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>They are right next to each other. The compiler will highly likely place them in the same 64-byte cache line.</p><p><strong>Then, tragedy strikes</strong>:</p><ul><li>Thread 0 frantically modifies <code>ax</code> on Core 0.</li><li>Thread 1 frantically modifies <code>bx</code> on Core 1.</li></ul><p><strong>They are clearly modifying different variables and don&#39;t need to synchronize at all!</strong></p><p>However! Because they are in the same cache line, when Core 0 modifies <code>ax</code>, the entire cache line is marked as &quot;dirty.&quot; Core 1&#39;s cache line is invalidated. When Core 1 modifies <code>bx</code>, it must first acquire ownership of this cache line, causing Core 0&#39;s to be invalidated.</p><p>These two threads are like fighting in a very small room (the cache line), even though they don&#39;t care what the other is doing.</p><p><strong>Result</strong>: The cache line frantically bounces between the two cores. This is called <strong>Cache Ping-Pong</strong>. Performance drops exponentially.</p><hr><h4 id="fixing-false-sharing" tabindex="-1">Fixing False Sharing <a class="header-anchor" href="#fixing-false-sharing" aria-label="Permalink to &quot;Fixing False Sharing&quot;">​</a></h4><p>The fix is very brute-force yet effective: <strong>artificially create distance</strong>.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">u16 ax </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">char</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> padding</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 强制换行 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">u16 bx </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Or, use GCC&#39;s <code>__attribute__((aligned(64)))</code>.</p><p><strong>Be very careful</strong>: This increases memory usage. You&#39;re padding every variable to 64 bytes. If it&#39;s just a simple counter, the overhead is negligible; but if you have an array of a million elements, your memory usage will explode.</p><hr><h2 id="_13-3-lock-free-programming-per-cpu-and-rcu" tabindex="-1">13.3 Lock-Free Programming: Per-CPU and RCU <a class="header-anchor" href="#_13-3-lock-free-programming-per-cpu-and-rcu" aria-label="Permalink to &quot;13.3  Lock-Free Programming: Per-CPU and RCU&quot;">​</a></h2><p>Since locks are so troublesome (deadlocks, priority inversion, writer starvation, cache invalidation), can we just <strong>not use locks</strong>?</p><p>Yes, but it requires greater wisdom. Here we introduce the two most important lock-free techniques in the kernel: <strong>Per-CPU variables</strong> and <strong>RCU</strong>.</p><hr><h3 id="_13-3-1-per-cpu-variables-divide-and-conquer" tabindex="-1">13.3.1 Per-CPU Variables: Divide and Conquer <a class="header-anchor" href="#_13-3-1-per-cpu-variables-divide-and-conquer" aria-label="Permalink to &quot;13.3.1  Per-CPU Variables: Divide and Conquer&quot;">​</a></h3><p>The idea behind Per-CPU variables is extremely simple: <strong>since sharing causes conflicts, let&#39;s just not share</strong>.</p><p>For each CPU core, we allocate an independent copy of the variable.</p><ul><li>CPU 0 operates on <code>pcpu_var[0]</code></li><li>CPU 1 operates on <code>pcpu_var[1]</code></li><li>...</li></ul><p>Everyone minds their own business, <strong>zero contention, no locks needed</strong>. There&#39;s not even cache false sharing (provided you haven&#39;t accidentally put two Per-CPU variables on the same line).</p><p><strong>What&#39;s the cost?</strong> Memory. If you have 64 cores, you need 64 copies. This is perfectly fine for small data structures (like counters, pointers). But for huge structures, you have to weigh the trade-offs.</p><hr><h4 id="how-to-use-per-cpu-variables" tabindex="-1">How to Use Per-CPU Variables <a class="header-anchor" href="#how-to-use-per-cpu-variables" aria-label="Permalink to &quot;How to Use Per-CPU Variables&quot;">​</a></h4><p>Never access <code>pcpu_var[cpu_id]</code> directly like an array. The kernel provides a set of macros.</p><p><strong>Static allocation</strong>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;linux/percpu.h&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">DEFINE_PER_CPU</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, my_counter);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 定义一个 Per-CPU 整数，自动初始化为 0 */</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><strong>Dynamic allocation</strong>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> my_data </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">data </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> alloc_percpu</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> my_data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* ... 用完记得 free ... */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">free_percpu</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(data);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><strong>Access</strong>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 获取当前 CPU 的副本指针 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">val </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> get_cpu_var</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(my_counter);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">val)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">++</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">put_cpu_var</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(my_counter);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 必须配对！这涉及抢占计数 */</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 或者更简单的读写（不推荐在指针上用，推荐用 get_cpu_ptr） */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">per_cpu</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(my_counter, cpu_id);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 访问指定 CPU 的副本 */</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><hr><h4 id="⚠️-pitfall-warning-the-atomicity-of-the-per-cpu-context" tabindex="-1">⚠️ Pitfall Warning: The &quot;Atomicity&quot; of the Per-CPU Context <a class="header-anchor" href="#⚠️-pitfall-warning-the-atomicity-of-the-per-cpu-context" aria-label="Permalink to &quot;⚠️ Pitfall Warning: The &quot;Atomicity&quot; of the Per-CPU Context&quot;">​</a></h4><p>When the <code>get_cpu_var()</code> macro is expanded, it calls <code>preempt_disable()</code>.</p><p>This means that between <code>get_cpu_var()</code> and <code>put_cpu_var()</code>, <strong>you are in an atomic context</strong>.</p><p><strong>Never, ever sleep here!</strong> Do not call <code>kmalloc(GFP_KERNEL)</code>, do not call <code>mutex_lock()</code>, do not call any function that might sleep.</p><p>If you call <code>vmalloc()</code> during <code>get_cpu_var()</code>, the kernel will immediately throw an error:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BUG: sleeping function called from invalid context</span></span>
<span class="line"><span>in_atomic(): 1</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p><strong>Why?</strong> Because you have disabled preemption, and the kernel scheduler can&#39;t context-switch you out. If you sleep at this point (waiting for a resource), the entire system deadlocks (because no one can switch you out to resume you later).</p><hr><h3 id="_13-3-2-rcu-read-copy-update-the-relativity-of-reads-and-writes" tabindex="-1">13.3.2 RCU (Read-Copy-Update): The &quot;Relativity&quot; of Reads and Writes <a class="header-anchor" href="#_13-3-2-rcu-read-copy-update-the-relativity-of-reads-and-writes" aria-label="Permalink to &quot;13.3.2  RCU (Read-Copy-Update): The &quot;Relativity&quot; of Reads and Writes&quot;">​</a></h3><p>If Per-CPU is &quot;splitting up,&quot; then RCU is &quot;time travel.&quot;</p><p>RCU is one of the most complex and powerful synchronization mechanisms in the Linux kernel. Its core idea is: <strong>readers use absolutely no locks, while writers update data through a &quot;copy-modify-replace&quot; routine</strong>.</p><hr><h4 id="an-intuitive-explanation-of-rcu-level-1" tabindex="-1">An Intuitive Explanation of RCU (Level 1) <a class="header-anchor" href="#an-intuitive-explanation-of-rcu-level-1" aria-label="Permalink to &quot;An Intuitive Explanation of RCU (Level 1)&quot;">​</a></h4><p>Imagine a bulletin board (shared data).</p><ul><li><strong>Readers</strong>: Just look at the bulletin board. Even if someone is putting up a new notice, readers can still look at the old one.</li><li><strong>Writers</strong>: <ol><li>Take a photo of the old notice (<strong>Copy</strong>).</li><li>Modify the content on the photo (<strong>Update</strong>).</li><li>Wait until everyone looking at the old notice is done, then post the new photo, covering the old notice (<strong>Publish</strong>).</li><li>Throw away the old notice (<strong>Reclaim</strong>).</li></ol></li></ul><p><strong>Key point</strong>: Readers never need a lock. Coordination between writers just uses a normal lock (like a spinlock).</p><p><strong>However</strong>: Before throwing away the old notice, the writer must wait. How long? <strong>Until everyone who might be looking at the old notice has finished.</strong></p><p>This waiting period is called the <strong>grace period</strong>.</p><hr><h4 id="a-glimpse-of-rcu-s-api" tabindex="-1">A Glimpse of RCU&#39;s API <a class="header-anchor" href="#a-glimpse-of-rcu-s-api" aria-label="Permalink to &quot;A Glimpse of RCU&#39;s API&quot;">​</a></h4><p>RCU&#39;s API design is extremely elegant; it leverages a &quot;social contract.&quot;</p><p><strong>Reader API</strong>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rcu_read_lock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* ... 读取共享数据 ... */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rcu_read_unlock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><strong>Please note</strong>: In non-<code>CONFIG_PREEMPT_RCU</code> kernels, these two macros might be <strong>completely empty</strong> (or just comments). How does it work? It relies on programmers honoring the contract: <strong>you must not sleep between these two macros</strong>.</p><p>If you sleep, you are a &quot;deadbeat&quot; reader, hogging the resource and doing nothing, and the writer will wait for you forever, causing the system to freeze.</p><p><strong>Writer API</strong>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 1. 复制并修改数据 (普通 C 代码) */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> new_data </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">new </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> kmalloc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(...);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">new </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">old_ptr;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">new</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">field </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> new_value;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 2. 发布新数据 */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rcu_assign_pointer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(old_ptr, new);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 原子地替换指针 */</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 3. 等待所有读者退出宽限期 */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">synchronize_rcu</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 阻塞等待 */</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 4. 安全释放旧数据 */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kfree</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(old);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><hr><h4 id="the-ghost-of-memory-barriers" tabindex="-1">The Ghost of Memory Barriers <a class="header-anchor" href="#the-ghost-of-memory-barriers" aria-label="Permalink to &quot;The Ghost of Memory Barriers&quot;">​</a></h4><p>You might be curious what <code>rcu_assign_pointer</code> and <code>rcu_dereference</code> are for.</p><p>They are wrappers for <strong>memory barriers</strong>.</p><p><code>rcu_assign_pointer</code> ensures that writing the new data completes before the pointer update. <code>rcu_dereference</code> ensures that reading the pointer completes before reading the data content.</p><p>This is crucial on certain weakly-ordered memory architectures (like Alpha, certain ARM configurations). On x86, although they are lightweight, they are absolutely necessary because they prevent the compiler from reordering optimizations.</p><hr><h3 id="_13-3-3-rcu-vs-reader-writer-lock-the-showdown" tabindex="-1">13.3.3 RCU vs. Reader-Writer Lock: The Showdown <a class="header-anchor" href="#_13-3-3-rcu-vs-reader-writer-lock-the-showdown" aria-label="Permalink to &quot;13.3.3  RCU vs. Reader-Writer Lock: The Showdown&quot;">​</a></h3><p>Let&#39;s return to the example of traversing a large linked list from the previous section.</p><p><strong>Reader-Writer Lock</strong>:</p><ul><li>Read operations: Although they can be concurrent, the lock overhead still exists.</li><li>Write operations: Extreme starvation, and severely damages cache coherence.</li></ul><p><strong>RCU</strong>:</p><ul><li>Read operations: <strong>Zero overhead</strong> (no lock instructions, no memory barriers, just adding two macro comments).</li><li>Write operations: Requires copying data (overhead), replacing the pointer (fast), waiting for the grace period (slow, but doesn&#39;t block readers).</li></ul><p><strong>Conclusion</strong>: In scenarios that are &quot;extremely read-frequent, extremely write-rare&quot; (like routing table lookups, process list traversal), <strong>RCU is the undisputed king</strong>.</p><hr><h2 id="_13-4-debugging-concurrency-issues-lockdep-and-deadlock-detection" tabindex="-1">13.4 Debugging Concurrency Issues: Lockdep and Deadlock Detection <a class="header-anchor" href="#_13-4-debugging-concurrency-issues-lockdep-and-deadlock-detection" aria-label="Permalink to &quot;13.4  Debugging Concurrency Issues: Lockdep and Deadlock Detection&quot;">​</a></h2><p>After writing so much concurrent code, it&#39;s impossible not to make mistakes. One of the greatest contributions of the Linux kernel is that it turned concurrency debugging into a &quot;science.&quot;</p><p>It is <strong>Lockdep</strong>.</p><hr><h3 id="_13-4-1-lockdep-the-runtime-mathematical-prover" tabindex="-1">13.4.1 Lockdep: The Runtime Mathematical Prover <a class="header-anchor" href="#_13-4-1-lockdep-the-runtime-mathematical-prover" aria-label="Permalink to &quot;13.4.1  Lockdep: The Runtime Mathematical Prover&quot;">​</a></h3><p>Lockdep is not just a debugger; it is a <strong>runtime lock correctness validator</strong>.</p><p>Its principle is: on every lock and unlock, it records the &quot;lock dependency relationships.&quot;</p><ul><li>If code path A takes lock X first, then lock Y.</li><li>If code path B takes lock Y first, then lock X.</li></ul><p>When both of these situations occur, <strong>Lockdep will sound the alarm</strong>.</p><p><strong>Why?</strong> Because if A and B run simultaneously, it could lead to the classic <strong>AB-BA deadlock</strong>.</p><p><strong>The most terrifying part is</strong>: Lockdep doesn&#39;t need an actual deadlock to occur to warn you. As long as it detects a <strong>logical possibility of a deadlock</strong>, it will brutally spray you with a bunch of WARNINGs when you load your module or run your tests.</p><p><strong>This is &quot;mathematical proof&quot;</strong>. It proves your code has a bug.</p><hr><h3 id="_13-4-2-what-to-do-when-you-encounter-a-lockdep-warning" tabindex="-1">13.4.2 What to Do When You Encounter a Lockdep Warning? <a class="header-anchor" href="#_13-4-2-what-to-do-when-you-encounter-a-lockdep-warning" aria-label="Permalink to &quot;13.4.2  What to Do When You Encounter a Lockdep Warning?&quot;">​</a></h3><p>Don&#39;t panic. Look at the logs.</p><p>It will tell you:</p><ol><li><strong>Which lock</strong> is involved (usually the lock&#39;s name and address).</li><li><strong>Where</strong> it was acquired (Call Trace).</li><li><strong>Where it was previously acquired</strong> (the place that caused the dependency relationship).</li></ol><p><strong>Fixing strategy</strong>: If it&#39;s an AB-BA deadlock, unify the lock order. For example, mandate: always take lock A first, then lock B.</p><p>If it&#39;s a self-deadlock (trying to recursively acquire the same non-recursive lock), check your call chain to see if you&#39;re calling a helper function within the same function that will acquire the lock again (like the <code>get_task_comm</code> and <code>task_lock</code> example mentioned earlier).</p><hr><h2 id="_13-5-chapter-echoes" tabindex="-1">13.5 Chapter Echoes <a class="header-anchor" href="#_13-5-chapter-echoes" aria-label="Permalink to &quot;13.5  Chapter Echoes&quot;">​</a></h2><p>What this chapter is really doing is building a <strong>&quot;cost-aware&quot;</strong> intuition.</p><p>On the surface, we are configuring various locks and atomic variables, but in reality, we are measuring the <strong>hardware cost</strong> of every synchronization operation.</p><ul><li>Traditional spinlocks sacrifice parallelism for safety.</li><li>Atomic integers limit their use cases for speed.</li><li>Per-CPU sacrifices memory space for lock-freedom.</li><li>RCU introduces complex copy-on-write and grace period waiting for extreme read performance.</li></ul><p>Remember that simple <code>ga++</code> from the beginning? Now you should be able to answer: if it&#39;s just a simple statistics variable, <code>atomic_inc</code> is the lightest choice; if it&#39;s an object&#39;s lifecycle count, <code>refcount_inc</code> is a life-saving talisman; if it&#39;s frantically read on a hot path, Per-CPU might be the ultimate answer for performance.</p><p><strong>There is no silver bullet in concurrent programming, only trade-offs.</strong> And the kernel engineer&#39;s responsibility is to make the most cost-effective trade-off every single time.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: Understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: Understanding&quot;">​</a></h3><p><strong>Question</strong>: In the Linux kernel, why is it recommended to use <code>refcount_t</code> instead of the traditional <code>atomic_t</code> to manage an object&#39;s reference count? Please briefly explain from the perspectives of safety and overflow handling mechanisms.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Because <code>refcount_t</code> is specifically designed for reference counting and provides protection mechanisms against overflow and underflow (saturation logic). It can detect and prevent <strong>Use-After-Free (UAF)</strong> vulnerabilities, whereas <code>atomic_t</code> is just a simple atomic integer lacking these protections.</p><p><strong>Analysis</strong>: <code>atomic_t</code> is just a regular atomic integer. If it overflows after an operation, it wraps around, which can lead to incorrect reference count judgments and trigger UAF. <code>refcount_t</code>, on the other hand, has a strict valid range ([1 .. INT_MAX-1]). Once an overflow or underflow occurs, it doesn&#39;t wrap around; instead, it saturates the value to <code>REFCOUNT_SATURATED</code> (e.g., 0xc0000000) and triggers a kernel warning via <code>WARN_ONCE()</code>, thereby exposing potentially severe security vulnerabilities early.</p></details><h3 id="exercise-2-application" tabindex="-1">Exercise 2: Application <a class="header-anchor" href="#exercise-2-application" aria-label="Permalink to &quot;Exercise 2: Application&quot;">​</a></h3><p><strong>Question</strong>: Suppose you are writing a network device driver and need to atomically set bit 3 to 1 in a Memory-Mapped I/O (MMIO) control register without affecting other bits. If the register address <code>ctrl_reg</code> is an <code>unsigned long</code> pointer, which implementation would you choose? Why?</p><p>A. <code>*ctrl_reg |= (1 &lt;&lt; 3);</code> B. <code>set_bit(3, ctrl_reg);</code></p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Option B (<code>set_bit(3, ctrl_reg);</code>)</p><p><strong>Analysis</strong>: Option A is a standard C bit operation that, when compiled, usually corresponds to three assembly instructions (read, modify, write). It is not atomic in a concurrent environment and can lead to data races. Option B uses the kernel-provided <code>set_bit</code> RMW (Read-Modify-Write) atomic bit operation API. It guarantees the atomicity of the entire &quot;read-modify-write back&quot; process, making it suitable for device registers or multi-core concurrent scenarios, and ensuring the safety and correctness of hardware state modifications.</p></details><h3 id="exercise-3-thinking" tabindex="-1">Exercise 3: Thinking <a class="header-anchor" href="#exercise-3-thinking" aria-label="Permalink to &quot;Exercise 3: Thinking&quot;">​</a></h3><p><strong>Question</strong>: In a multi-core system, CPU 0 continuously modifies the global variable <code>x</code> at address 0x1000, while CPU 1 simultaneously and continuously modifies the global variable <code>y</code> immediately following it. Given that the CPU cache line size is 64 bytes and the address of <code>x</code> is 64-byte aligned, the system throughput might significantly decrease. What is this phenomenon called? What is its root cause? How can you mitigate this problem with minimal code changes?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: 1. Phenomenon: False Sharing. 2. Root cause: <code>x</code> and <code>y</code> reside in the same cache line. The concurrent modifications by CPU 0 and CPU 1 cause this cache line to frequently invalidate between the two cores&#39; caches, resulting in Cache Bouncing. 3. Solution: Use a cache-line alignment macro (like <code>____cacheline_aligned</code> or <code>____cacheline_aligned_in_smp</code>) when defining the variables to force <code>x</code> and <code>y</code> into different cache lines.</p><p><strong>Analysis</strong>: CPU cache coherence protocols (like MESI) manage data in units of cache lines (typically 64 bytes). Even though CPU 0 and CPU 1 are operating on different variables, if these two variables are within the same cache line, the hardware considers them to be the same piece of data. When CPU 0 writes to <code>x</code>, the <code>y</code> in CPU 1&#39;s cache is also marked invalid, and vice versa. This causes the cache line to bounce back and forth between the cores like a ping-pong ball, severely wasting bus bandwidth and CPU cycles. By aligning variables to cache lines, you ensure that frequently modified independent variables exclusively occupy their own cache lines, eliminating this false sharing issue.</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>This chapter first delved into atomic operation mechanisms that are lighter than mutexes, focusing on comparing the traditional <code>atomic_t</code> with the <code>refcount_t</code> designed specifically to prevent reference count overflows. The latter uses a &quot;saturation&quot; mechanism (pinning the overflowed value to <code>REFCOUNT_SATURATED</code>) to eliminate Use-After-Free vulnerabilities caused by integer wrap-around. Although it sacrifices flexibility, it greatly improves kernel safety. At the same time, the text introduced low-level atomic bit operations (RMW); while much faster than manual locking (leveraging hardware instructions), they still need to be paired with locks when multiple cores operate on the same memory address.</p><p>Next, the tutorial revealed a hidden performance killer in multi-core programming: cache false sharing. When multiple cores frequently modify different variables located within the same cache line (typically 64 bytes), even though there is no logical contention, the hardware&#39;s need to maintain cache coherence causes the cache line to bounce frequently between cores, resulting in a performance plunge. The solution is to use compiler attributes or manual byte padding to force sensitive variables onto different cache lines, trading physical isolation for parallel efficiency.</p><p>Following that, the article introduced two advanced synchronization techniques that abandon traditional locks for extreme performance. Per-CPU variables allocate an independent data copy for each core, completely eliminating shared contention, but you must be mindful of their context&#39;s preemption-disabling characteristic (no sleeping allowed). RCU (Read-Copy-Update), through its &quot;copy-modify-replace&quot; routine, achieves zero overhead on the reader side (completely lock-free). Although it adds complexity to the writer&#39;s copy logic and grace period waiting, it is the undisputed performance king in &quot;read-mostly, write-rarely&quot; scenarios (like routing table lookups).</p><p>Finally, addressing the pain point of concurrent code being difficult to debug, this chapter emphasized the Lockdep mechanism provided by the Linux kernel. It is not just a runtime debugger, but more like a mathematical prover that can detect potential deadlock logic before a deadlock actually occurs by checking the lock dependency graph (such as AB-BA lock ordering). Making good use of Lockdep&#39;s warnings to unify lock order or check for recursive calls is a key tool for kernel developers to ensure code correctness.</p><p>In summary, the essence of kernel synchronization is making trade-offs among hardware cost, safety principles, and parallel efficiency. Whether choosing atomic RMW instructions to avoid bus locking, introducing cache padding to prevent false sharing, or adopting RCU to sacrifice write performance for read scalability, there is no universal silver bullet. Only by deeply understanding the trade-offs behind these mechanisms can you make the most cost-effective decisions in specific driver or kernel module development.</p>`,249)])])}const g=s(n,[["render",o]]);export{u as __pageData,g as default};
