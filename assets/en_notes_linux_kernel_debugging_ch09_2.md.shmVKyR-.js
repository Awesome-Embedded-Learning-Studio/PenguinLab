import{_ as t,o as s,c as a,a as i}from"./chunks/framework.8tiMeCK-.js";const p=JSON.parse('{"title":"9.2  The Panorama of Kernel Tracing Technologies","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch09_2.md","filePath":"en/notes/linux_kernel_debugging/ch09_2.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_debugging/ch09_2.md"};function o(r,e,l,h,c,d){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="_9-2-the-panorama-of-kernel-tracing-technologies" tabindex="-1">9.2 The Panorama of Kernel Tracing Technologies <a class="header-anchor" href="#_9-2-the-panorama-of-kernel-tracing-technologies" aria-label="Permalink to &quot;9.2  The Panorama of Kernel Tracing Technologies&quot;">​</a></h1><p>Don&#39;t rush to type commands just yet.</p><p>We have our tools and our directory, but before we actually get our hands dirty with <code>ftrace</code>, it&#39;s best to take a step back and get a clear picture of what we&#39;re dealing with. If you think of tracing tools as mere &quot;recorders,&quot; you&#39;ll easily get lost in the configuration options later.</p><p>The world of kernel tracing is full of acronyms and subsystems that look like a pile of scattered parts. In reality, it&#39;s a finely engineered machine. Once we understand how this machine is built—where the data comes from, how it&#39;s collected, and how it&#39;s presented—all those seemingly mysterious configuration files that follow become nothing more than logical connectors.</p><hr><h3 id="data-sources-and-frontends-—-peeling-back-the-layers-of-tools" tabindex="-1">Data Sources and Frontends — Peeling Back the Layers of Tools <a class="header-anchor" href="#data-sources-and-frontends-—-peeling-back-the-layers-of-tools" aria-label="Permalink to &quot;Data Sources and Frontends — Peeling Back the Layers of Tools&quot;">​</a></h3><p>First, a fundamental question: where exactly does the data we want to trace come from?</p><p>In the previous section, you installed <code>lttng</code> and modified kernel parameters, but you didn&#39;t see the &quot;data&quot; itself. The data is hidden in the execution details of the kernel. To capture it, we need <strong>data sources</strong>.</p><p>We&#39;re actually not unfamiliar with the most common categories of data sources in the kernel:</p><ul><li><strong>Tracepoints</strong>: These are &quot;hooks&quot; pre-planted by kernel developers. At certain key locations in the kernel (such as when the scheduler performs a context switch, or when an interrupt arrives), developers have written code to tell the system, &quot;something happened here.&quot; The list of these tracepoints sits under <code>/sys/kernel/tracing/events/</code>. <ul><li>We actually touched on this back in Chapter 4 when we tinkered with Kprobes—those were &quot;dynamic kprobes,&quot; which is equivalent to you temporarily drilling a hole in a place that didn&#39;t have a hook.</li></ul></li><li><strong>Kprobes / Uprobes</strong>: These are not just debugging tools, but also powerful data sources for tracing. Kprobes handle kernel space, while Uprobes handle user space. They can intercept the flow of execution at almost any address.</li><li><strong>LTTng Modules and USDT</strong>: LTTng has its own set of kernel modules and user space probes, designed for more efficient, lower-latency data capture.</li></ul><p>Having data sources is only the first step. Raw data is scattered throughout kernel memory, and we need an <strong>infrastructure</strong> to extract it.</p><p>This is why your system has the <code>/sys/kernel/tracing</code> directory. This is <code>tracefs</code>, the &quot;control panel&quot; of ftrace. Tools read and write these pseudo-files to tell the kernel: start recording, stop recording, only record the <code>schedule</code> function, or record with more detail.</p><p>Finally, as humans, directly reading those binary buffer files in <code>cat</code> is pure torture. So we also need <strong>frontends</strong>.</p><p>This forms the three-layer pyramid of the entire Linux tracing technology stack:</p><ol><li><strong>Bottom Layer: Data Sources</strong>. The signals generated by the kernel.</li><li><strong>Middle Layer: Infrastructure</strong>. Mechanisms like ftrace, tracefs, and perf_events, responsible for pulling signals out of the kernel and organizing them.</li><li><strong>Top Layer: Frontends/Tools</strong>. Tools like <code>trace-cmd</code> and <code>KernelShark</code>, or those <code>perf-tools</code> scripts we used in Chapter 4. They type the tedious low-level commands for you and turn the data into charts and graphs you can actually understand.</li></ol><p>There&#39;s a very important mindset shift here: <strong>these technologies are not competing with each other</strong>.</p><p>You might have previously thought, &quot;Should I use ftrace or perf?&quot; But the answer now is, &quot;Why not both?&quot; Julia Evans has a classic blog post titled &quot;Linux tracing systems &amp; how they fit together.&quot; If you haven&#39;t read it, I highly recommend searching for it. The diagram she drew explains everything clearly: modern Linux tracing technologies share code and ideas. The underlying ftrace data can be viewed through tools like perf, or through trace-cmd, or even analyzed via the LTTng ecosystem.</p><p>This is exactly the &quot;unified tracing platform&quot; vision that kernel stalwart Steven Rostedt (the original author of ftrace) has been advocating for years. He once presented a diagram (shown below) illustrating how these technologies are intricately yet harmoniously intertwined:</p><blockquote><p><em>[Figure 9.1 – Schematic of the Linux Tracing Infrastructure]</em><em>(Original book figure: Shows Tracepoints/Probes as data sources, connected through interfaces like tracefs/debugfs to underlying tools like ftrace/perf, which in turn support various frontend scripts and GUI tools above them)</em></p></blockquote><blockquote><p><em>[Figure 9.2 – The Rich Linux Tracing Ecosystem]</em><em>(Original book figure: Steven Rostedt&#39;s slide, showing how underlying technologies are reused by upper-layer tools)</em></p></blockquote><p>See? The underlying &quot;meat&quot; is the same; only the outer &quot;skin&quot; is different. This means that when we learn ftrace, we are actually learning a universal language.</p><hr><h3 id="stepping-into-ftrace-territory" tabindex="-1">Stepping into ftrace Territory <a class="header-anchor" href="#stepping-into-ftrace-territory" aria-label="Permalink to &quot;Stepping into ftrace Territory&quot;">​</a></h3><p>Alright, we&#39;ve looked at the theoretical architecture diagram. Now let&#39;s step through the door.</p><p>The star of this chapter is <strong>ftrace</strong>. It&#39;s the tracer built into the Linux kernel, and the <code>f</code> in its name originally stood for <code>function</code> (function), because it was first designed to trace kernel function call graphs. But today&#39;s ftrace goes far beyond that—it&#39;s a generic tracing engine.</p><p>Starting with Linux kernel 4.1, ftrace&#39;s standard operational interface is implemented through a virtual filesystem called <strong>tracefs</strong>.</p><p>Don&#39;t let the name intimidate you. You can think of it as a filesystem dedicated to &quot;transmitting control commands.&quot; Just as <code>/proc</code> carries process information, <code>/sys</code> carries hardware parameters, and <code>tracefs</code> carries tracing control instructions.</p><h4 id="checking-the-mount-point" tabindex="-1">Checking the Mount Point <a class="header-anchor" href="#checking-the-mount-point" aria-label="Permalink to &quot;Checking the Mount Point&quot;">​</a></h4><p>Under normal circumstances, modern Linux distributions (especially those using Systemd) will automatically mount <code>tracefs</code> for you at boot.</p><p>You can use this command to verify its existence:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mount</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;^tracefs&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>If everything is normal, you should see output similar to this:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tracefs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> on</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /sys/kernel/tracing</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> type</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tracefs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (rw,nosuid,nodev,noexec,relatime)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tracefs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> on</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /sys/kernel/debug/tracing</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> type</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tracefs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (rw,nosuid,nodev,noexec,relatime)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Notice that there are two mount points here.</p><ol><li><code>/sys/kernel/tracing</code>: This is the <strong>new standard</strong> (4.1+ kernels). Regardless of whether your <code>debugfs</code> is allowed to be mounted, this point should be present.</li><li><code>/sys/kernel/debug/tracing</code>: This is the <strong>old tradition</strong>. Previously, <code>ftrace</code> was mounted as a subtree under <code>debugfs</code>.</li></ol><h4 id="a-pitfall-in-production-environments" tabindex="-1">A Pitfall in Production Environments <a class="header-anchor" href="#a-pitfall-in-production-environments" aria-label="Permalink to &quot;A Pitfall in Production Environments&quot;">​</a></h4><p>Why maintain two mount points? It&#39;s not just for nostalgia.</p><p>Imagine you&#39;re running a production machine with strict security policies. For security reasons, a system administrator might disable mounting <code>debugfs</code> via the kernel configuration <code>CONFIG_DEBUG_FS_DISALLOW_MOUNT</code>. Because <code>debugfs</code> exposes too many kernel internals, it is indeed a risk for production machines.</p><p>However, debugging performance issues in production is a hard requirement. So, the kernel separated <code>tracefs</code> out. Even if <code>debugfs</code> is disabled, <code>/sys/kernel/tracing</code> can still be mounted independently, allowing you to access only the tracing-related interfaces without touching those sensitive kernel debugging details.</p><p>This is why in this book (assuming you&#39;re using a newer 5.10 kernel), we default to using <code>/sys/kernel/tracing</code> as our working directory.</p><p>If you&#39;re working on a very old system (older than 4.1), you might only have <code>/sys/kernel/debug/tracing</code> as an option. Although the paths differ, the file contents inside are the same. In the following sections, when we say &quot;switch to the tracing directory,&quot; our default action is:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /sys/kernel/tracing</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Alternatively, if you want to ensure you find the correct path on your current system, you can take a more brute-force approach:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /sys/kernel/debug/tracing</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 2&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/dev/null</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ||</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /sys/kernel/tracing</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This directory is the birthplace of all the magic we&#39;ll work with next. In this directory, every file is not meant for storing data, but rather acts as a switch, knob, or display on a control panel.</p><p>Ready? Hands on the keyboard. In the next section, we&#39;re going to start flipping these switches.</p>`,46)])])}const g=t(n,[["render",o]]);export{p as __pageData,g as default};
