import{_ as t,o,c as r,a as i}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"Chapter 9: Netfilter Frameworks","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch09_1.md","filePath":"en/notes/linux_kernel_networking/ch09_1.md","lastUpdated":null}'),s={name:"en/notes/linux_kernel_networking/ch09_1.md"};function a(n,e,l,c,h,d){return o(),r("div",null,[...e[0]||(e[0]=[i('<h1 id="chapter-9-netfilter-frameworks" tabindex="-1">Chapter 9: Netfilter Frameworks <a class="header-anchor" href="#chapter-9-netfilter-frameworks" aria-label="Permalink to &quot;Chapter 9: Netfilter Frameworks&quot;">​</a></h1><h3 id="chapter-intro-setting-up-checkpoints-on-the-highway" tabindex="-1">Chapter Intro: Setting Up Checkpoints on the Highway <a class="header-anchor" href="#chapter-intro-setting-up-checkpoints-on-the-highway" aria-label="Permalink to &quot;Chapter Intro: Setting Up Checkpoints on the Highway&quot;">​</a></h3><p>If you think of the Network Stack as a busy highway and packets as the vehicles traveling along it, then Netfilter is the traffic police.</p><p>But this isn&#39;t just a metaphor. In the Linux kernel&#39;s design, network traffic truly does fly by—the NIC receives an interrupt, the kernel allocates a <code>sk_buff</code>, the protocol stack passes it upward, and finally frees the memory. This process is astonishingly fast. But if you are a &quot;traffic cop&quot; wanting to pull over a vehicle to check its credentials, or even reroute it entirely, where should you intervene?</p><p>Early kernel designs didn&#39;t reserve this many &quot;checkpoints.&quot; If you wanted to filter packets or perform NAT (Network Address Translation), you had to modify the core protocol stack code, hardcoding the logic directly into the processing flow. This was a terrible way to extend the kernel: with every new feature, the kernel grew a little fatter, and it became error-prone.</p><p>Netfilter emerged to solve this problem. Instead of modifying the main road, it hangs &quot;hooks&quot; at five key locations along it. Any kernel module can register callback functions on these hooks. When a packet passes by, the kernel pauses and asks, &quot;Does anyone here want to process this packet?&quot;</p><p>In this chapter, we dive deep into this exact mechanism. We will see how it embeds itself into every corner of the Network Stack, and how it supports functionalities we use every day like iptables, connection tracking, and NAT.</p><h3 id="netfilter-frameworks" tabindex="-1">Netfilter Frameworks <a class="header-anchor" href="#netfilter-frameworks" aria-label="Permalink to &quot;Netfilter Frameworks&quot;">​</a></h3><p>Now let&#39;s zoom out a bit. Before diving into the specific code implementation, let&#39;s get a clear view of the big picture.</p><p>The Netfilter subsystem is not just a firewall; it is a low-level traffic interception framework. Built on top of it, the kernel implements a series of features that are indispensable in network engineering:</p><ul><li><strong>Packet Selection</strong>: This is the core of iptables, determining which packets get pulled out for processing.</li><li><strong>Packet Filtering</strong>: What we commonly refer to as firewall rules—let this one through, drop that one.</li><li><strong>Network Address Translation (NAT)</strong>: Modifying a packet&#39;s IP or port, which is the foundation for allowing a LAN to share a single public IP for internet access.</li><li><strong>Packet Mangling</strong>: Modifying packet header contents before or after routing (such as adding a specific mark).</li><li><strong>Connection Tracking</strong>: One of the most core mechanisms, through which the kernel remembers &quot;this is an already established connection.&quot; Both NAT and stateful firewalls rely entirely on it.</li><li><strong>Network Accounting</strong>: Collecting traffic data for billing or monitoring purposes.</li></ul><p>Our task in this chapter is to figure out exactly how these functionalities run while hooked into the Netfilter framework.</p><h4 id="three-major-frameworks-based-on-netfilter" tabindex="-1">Three Major Frameworks Based on Netfilter <a class="header-anchor" href="#three-major-frameworks-based-on-netfilter" aria-label="Permalink to &quot;Three Major Frameworks Based on Netfilter&quot;">​</a></h4><p>In the kernel source&#39;s <code>net/netfilter</code> directory, alongside the core framework, lie several well-known projects. If you&#39;ve ever maintained production servers, you&#39;ve most likely encountered LVS or IPVS—or at the very least, the traffic you&#39;ve handled has flowed through them.</p><h5 id="_1-ipvs-transport-layer-load-balancing" tabindex="-1">1. IPVS: Transport Layer Load Balancing <a class="header-anchor" href="#_1-ipvs-transport-layer-load-balancing" aria-label="Permalink to &quot;1. IPVS: Transport Layer Load Balancing&quot;">​</a></h5><p><strong>IPVS (IP Virtual Server)</strong> is a transport layer load balancing solution built on Netfilter. The code is located at <code>net/netfilter/ipvs</code>.</p><p>When you have only one entry server but ten web servers running behind it, you need to distribute incoming traffic evenly across those ten servers. IPVS intercepts traffic on Netfilter&#39;s hooks, modifies the destination address based on a scheduling algorithm (such as round-robin or least-connections), and forwards the packets to the real backend servers.</p><p>Historical note: Support for IPv4 IPVS has existed in the kernel for a very long time. IPv6 IPVS support, however, was merged in version 2.6.28, primarily developed by Google&#39;s Julius Volz and Vince Busam. If you want to dive deeper into the LVS (Linux Virtual Server) architecture, you can refer to <code>www.linuxvirtualserver.org</code>.</p><h5 id="_2-ip-sets-managing-massive-numbers-of-ip-addresses" tabindex="-1">2. IP Sets: Managing Massive Numbers of IP Addresses <a class="header-anchor" href="#_2-ip-sets-managing-massive-numbers-of-ip-addresses" aria-label="Permalink to &quot;2. IP Sets: Managing Massive Numbers of IP Addresses&quot;">​</a></h5><p>If you&#39;ve ever written firewall rules, you might have encountered this pain point: if you want to block 10,000 IP addresses, you have to write 10,000 iptables rules. This is a performance disaster because every packet has to traverse all 10,000 rules.</p><p><strong>IP sets</strong> were born to solve this problem. It consists of two parts:</p><ul><li><strong>Kernel module</strong>: <code>net/netfilter/ipset</code></li><li><strong>Userspace tool</strong>: <code>ipset</code></li></ul><p>IP sets allow you to maintain a Set in the kernel that can contain a bunch of IP addresses, port numbers, or even network segments. In iptables, you only need to reference the name of this set, and the kernel will efficiently look it up using a hash table or red-black tree. This framework was developed by Jozsef Kadlecsik. For details, see <code>http://ipset.netfilter.org</code>.</p><h5 id="_3-iptables-the-most-familiar-old-companion" tabindex="-1">3. iptables: The Most Familiar Old Companion <a class="header-anchor" href="#_3-iptables-the-most-familiar-old-companion" aria-label="Permalink to &quot;3. iptables: The Most Familiar Old Companion&quot;">​</a></h5><p>When talking about Netfilter, we must mention <strong>iptables</strong>. It is probably the most popular Linux firewall tool in history.</p><p>It&#39;s crucial to understand this relationship: <strong>Netfilter is the framework inside the kernel, while iptables is the userspace frontend tool</strong>. You use the iptables command to write rules, and iptables tells the kernel about these rules through the <code>setsockopt()</code> system call. The Netfilter framework inside the kernel then uses these rules to match packets.</p><p>iptables provides a complete set of management layer functionalities:</p><ul><li>Adding or deleting rules</li><li>Displaying statistics</li><li>Managing different tables</li><li>Zeroing counters</li></ul><p>Depending on the protocol, there are several parallel implementations inside the kernel:</p><ul><li><strong>iptables</strong>: For IPv4 (code in <code>net/ipv4/netfilter/ip_tables.c</code>)</li><li><strong>ip6tables</strong>: For IPv6 (code in <code>net/ipv6/netfilter/ip6_tables.c</code>)</li><li><strong>arptables</strong>: For the ARP protocol (code in <code>net/ipv4/netfilter/arp_tables.c</code>)</li><li><strong>ebtables</strong>: For Ethernet bridging (Layer 2 firewall, code in <code>net/bridge/netfilter/ebtables.c</code>)</li></ul><p>In userspace, the corresponding command-line tools are <code>iptables</code> and <code>ip6tables</code>. They similarly communicate with the kernel through <code>setsockopt()</code> and <code>getsockopt()</code>.</p><h4 id="next-generation-technologies-xtables2-and-nftables" tabindex="-1">Next-Generation Technologies: xtables2 and nftables <a class="header-anchor" href="#next-generation-technologies-xtables2-and-nftables" aria-label="Permalink to &quot;Next-Generation Technologies: xtables2 and nftables&quot;">​</a></h4><p>Although iptables is a classic, this old interface has its breaking points—such as when you try to insert tens of thousands of rules. To solve this problem, the kernel community began exploring new architectures.</p><p>There are two directions here worth our attention.</p><p><strong>1. xtables2</strong> This is a project primarily developed by Jan Engelhardt (still in progress as of this writing). Its core improvement is discarding the aging <code>setsockopt()</code> interface in favor of using a <strong>Netlink</strong> message-based interface to communicate with the kernel.</p><p>There is a technical detail here worth pondering: when passing rules via the old <code>setsockopt</code> approach, it often required copying the entire rule table from userspace to kernel space, and then parsing it. When the number of rules reaches tens of thousands, the overhead of this &quot;bulk copy&quot; is staggering. xtables2, using Netlink&#39;s message-based mechanism, enables incremental updates—transmitting only the changed parts. This makes it much more graceful when dealing with large-scale rule sets. For project details, see <code>http://xtables.de</code>.</p><p><strong>2. nftables</strong> This is the true &quot;successor,&quot; aimed at completely replacing iptables. The most core design philosophy of nftables is:</p><ul><li><strong>Unified Implementation</strong>: No longer distinguishing between iptables, ip6tables, arptables, and ebtables; all protocol processing logic lives in a single unified codebase.</li><li><strong>Virtual Machine</strong>: It introduces a simple virtual machine to execute rules, making rule expression more powerful and flexible.</li></ul><p>The introduction of a virtual machine is not for showing off, but to solve the efficiency problems brought about by flexibility. Traditional iptables rules are hardcoded matching functions, whereas nftables allows users to write assembly-like instructions in userspace, which the kernel executes through a tiny interpreter. This means you can combine very complex logic without needing to modify the kernel code every time.</p><p>This project was first proposed by Patrick McHardy at the 2008 Netfilter Workshop. The kernel infrastructure and userspace tools were primarily developed by Patrick McHardy and Pablo Neira Ayuso. If you want to read a forward-looking technical analysis, check out the classic article <em>&quot;Nftables: a new packet filtering engine&quot;</em> (LWN, <code>http://lwn.net/Articles/324989/</code>).</p><hr><h4 id="extensions-and-ecosystem" tabindex="-1">Extensions and Ecosystem <a class="header-anchor" href="#extensions-and-ecosystem" aria-label="Permalink to &quot;Extensions and Ecosystem&quot;">​</a></h4><p>The charm of Netfilter lies in its extensibility. Beyond the core frameworks mentioned above, there are a massive number of kernel modules (under <code>net/netfilter</code>) that extend its functionality. We won&#39;t list all of these modules one by one in this book—after all, managing and using these rules is more of a system administrator&#39;s daily work, not a required course for kernel developers.</p><p>If you are interested in specific extension modules (such as conntrack helpers, specific NAT modules, etc.), the best references besides the source code itself are the official Netfilter project website: <strong>www.netfilter.org</strong>.</p><p>We&#39;ve now seen the big picture, but this is only the map from a userspace perspective. Next, we&#39;ll dive into the kernel and see exactly where these hooks are placed.</p>',45)])])}const f=t(s,[["render",a]]);export{u as __pageData,f as default};
