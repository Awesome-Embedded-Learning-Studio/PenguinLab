import{_ as i,o as e,c as a,a as n}from"./chunks/framework.8tiMeCK-.js";const g=JSON.parse('{"title":"11.3  搭建 ARM 目标系统与内核","description":"","frontmatter":{},"headers":[],"relativePath":"notes/linux_kernel_debugging/ch11_3.md","filePath":"notes/linux_kernel_debugging/ch11_3.md","lastUpdated":null}'),l={name:"notes/linux_kernel_debugging/ch11_3.md"};function t(o,s,r,p,d,c){return e(),a("div",null,[...s[0]||(s[0]=[n(`<h1 id="_11-3-搭建-arm-目标系统与内核" tabindex="-1">11.3 搭建 ARM 目标系统与内核 <a class="header-anchor" href="#_11-3-搭建-arm-目标系统与内核" aria-label="Permalink to &quot;11.3  搭建 ARM 目标系统与内核&quot;">​</a></h1><p>先别急着烧录。</p><p>上一节我们搞清楚了 <strong>vmlinux</strong>（给工程师的蓝图）和 <strong>bzImage</strong>（给机器的压缩包）的区别。现在，我们要把这个理论落地，搭建一个实体的、能跑 KGDB 的调试环境。</p><p>这事儿比想象中麻烦——你不能随便抓一个生产环境跑着的内核就开始调试。我们需要一个<strong>定制构建</strong>的目标系统。任何能干活的 Linux 系统，哪怕再简陋，也得凑齐这几块拼图：</p><ul><li><strong>Bootloader</strong>：负责把内核唤醒。在 QEMU 虚拟环境里，QEMU 自己就充当了 Bootloader，省去了这一步；但如果你在真板子上（比如 ARM 嵌入式设备），通常会用 <strong>Das U-Boot</strong>，x86 上则是熟悉的 <strong>GRUB</strong>。</li><li><strong>DTB（设备树二进制）</strong>：ARM/ARM64/PPC 这类架构必需的「硬件说明书」，告诉内核板子上有哪些设备、接在哪条总线上。</li><li><strong>内核镜像</strong>：这就是我们上一节说的压缩包，系统启动时真正加载的东西。</li><li><strong>根文件系统</strong>：内核启动后挂载的文件系统，里面有 init 进程和必要的工具。</li></ul><p>凑齐这四个，你才算有了一个能跑的系统。</p><h3 id="_11-3-1-自动化构建-seals-项目" tabindex="-1">11.3.1 自动化构建：SEALS 项目 <a class="header-anchor" href="#_11-3-1-自动化构建-seals-项目" aria-label="Permalink to &quot;11.3.1  自动化构建：SEALS 项目&quot;">​</a></h3><p>搭建根文件系统这事儿，说多了都是泪—— BusyBox 配置、库文件依赖、init 脚本……这都不是写代码能解决的，是纯工程体力活。</p><p>为了不让我们在「配置环境」这这一章耗尽半生，我们选择作弊：用 <strong>SEALS (Simple Embedded ARM Linux System)</strong> 项目。</p><p>这是一个开源的自动化构建工具，专门用来生成「简陋但能用」的 ARM Linux 系统。它会帮你搞定：</p><ol><li>内核配置与编译。</li><li>DTB 生成。</li><li>基于 BusyBox 的最小根文件系统。</li></ol><blockquote><p><strong>SEALS 的默认配置</strong></p><p>我们默认使用 SEALS 的 <strong>ARM Versatile Express (VExpress)</strong> 平台配置。</p><ul><li>CPU：ARMv7 Cortex A9 多核。</li><li>内存：512 MB RAM。</li><li>运行环境：QEMU 虚拟机。</li></ul></blockquote><p>这种方式最妙的地方在于<strong>嵌套虚拟化</strong>：你可以在 x86_64 机器上跑一个 ARM 虚拟机，在这个 ARM 虚拟机里调试内核。这意味着你不需要买任何 ARM 开发板，就能完成本书的所有实验。</p><p>当然，Yocto 和 Buildroot 是更强大的工业级方案，甚至你手头可能有现成的树莓派或 BeagleBone。但对于「学 KGDB」这个单一目标，SEALS 足够简单、足够快，而且不需要任何特定硬件。</p><hr><h4 id="前置准备" tabindex="-1">前置准备 <a class="header-anchor" href="#前置准备" aria-label="Permalink to &quot;前置准备&quot;">​</a></h4><p>用 SEALS 之前，你的宿主机得准备几样东西：</p><ul><li><strong>QEMU ARM 模拟器</strong>（<code>qemu-system-arm</code>）。</li><li><strong>交叉编译工具链</strong>（x86_64 到 ARM32 的 <code>gcc</code>）。</li><li>一些杂项依赖库。</li></ul><p>因为 SEALS 不是本书的重点，我们就把构建细节留给它的 Wiki。你需要把 SEALS 项目克隆下来，按照 Wiki 配置好环境。这里只给个路标：</p><ul><li><strong>项目主页</strong>：<a href="https://github.com/kaiwan/seals" target="_blank" rel="noreferrer">https://github.com/kaiwan/seals</a></li><li><strong>安装依赖</strong>：<a href="https://github.com/kaiwan/seals/wiki/HOWTO-Install-required-packages-on-the-Host-for-SEALS" target="_blank" rel="noreferrer">HOWTO Install required packages</a></li><li><strong>详细步骤</strong>：<a href="https://github.com/kaiwan/seals/wiki/SEALs-HOWTO" target="_blank" rel="noreferrer">SEALs HOWTO</a></li></ul><p>配置完环境跑起来，你应该能看到类似图 11.4 和 11.5 的界面。如果看到 QEMU 窗口里滚动的启动日志，说明地基打好了。</p><h3 id="_11-3-2-配置内核-开启-kgdb" tabindex="-1">11.3.2 配置内核：开启 KGDB <a class="header-anchor" href="#_11-3-2-配置内核-开启-kgdb" aria-label="Permalink to &quot;11.3.2  配置内核：开启 KGDB&quot;">​</a></h3><p>环境有了，现在轮到内核了。</p><p>当你进入 <code>make menuconfig</code> 时，你要把自己当成一个<strong>侦探</strong>。你需要打开那些平时绝对不会碰的调试开关。我们已经在第 1 章讲过内核调试的基础，这里我们要针对 KGDB 做一些特定的手术。</p><h4 id="必须开启的选项" tabindex="-1">必须开启的选项 <a class="header-anchor" href="#必须开启的选项" aria-label="Permalink to &quot;必须开启的选项&quot;">​</a></h4><p>不管你用 ARM 还是 x86，要想让 KGDB 跑起来，这几个内核配置项（<code>CONFIG_</code> 前缀省略）是<strong>强制</strong>的：</p><ol><li><p><strong><code>DEBUG_KERNEL=y</code></strong></p><ul><li>位置：<code>Kernel hacking</code> -&gt; <code>Kernel debugging</code></li><li>这其实是总开关，选了后面很多调试选项才会出现。</li></ul></li><li><p><strong><code>DEBUG_INFO=y</code></strong> —— <strong>最重要</strong></p><ul><li>位置：<code>Kernel hacking</code> -&gt; <code>Compile-time checks and compiler options</code> -&gt; <code>Compile the kernel with debug info</code></li><li>作用：这就给编译器加 <code>-g</code> 参数，把调试符号烙印进 <code>vmlinux</code>。</li><li>为什么强制？没有它，GDB 就是一堆乱码地址，你根本看不懂在干嘛。技术上它不是「必须」的（内核也能跑），但实战中没它就别玩调试。</li></ul></li><li><p><strong><code>KGDB=y</code></strong></p><ul><li>位置：<code>Kernel hacking</code> -&gt; <code>Generic Kernel Debugging Instruments</code> -&gt; <code>KGDB: kernel debugger</code></li><li>作用：正式把 GDB 服务端代码塞进内核。有了它，内核才能听懂 GDB 发过来的命令。</li><li>原理：内核里现在运行着一个 <code>gdbserver</code>，它坐在那边等着（通常通过串口或网络），一旦连上，你发给它的 GDB 命令它就在本地执行，把结果吐回来。</li></ul></li><li><p><strong><code>KGDB_SERIAL_CONSOLE=y</code></strong></p><ul><li>作用：允许复用串口控制台来做 KGDB 通信。这是最简单、最稳定的连接方式。</li><li>细节：这里涉及到一个叫 <code>kgdboc</code>（KGDB over console）的内核参数，我们后面会细讲怎么用它来绑定具体的串口（如 <code>ttyS0</code>）。</li></ul></li><li><p><strong><code>KGDB_HONOUR_BLOCKLIST=y</code></strong></p><ul><li><strong>建议开启</strong>。它防止你在某些不能打断的函数（比如 <code>kprobe</code> 黑名单上的函数）里设断点，避免递归陷阱导致系统直接死锁。</li></ul></li><li><p><strong><code>MAGIC_SYSRQ=y</code></strong></p><ul><li>位置：<code>Kernel hacking</code> -&gt; <code>Generic Kernel Debugging Instruments</code> -&gt; <code>Magic SysRq key</code></li><li>作用：允许你在系统运行时，通过 <code>/proc/sysrq-trigger</code> 介入内核。往里面写个 <code>g</code>，就能强制内核停下来进入 KGDB 调试模式。</li><li>配合：你还需要在运行时把 <code>/proc/sys/kernel/sysrq</code> 设为 <code>1</code>，确保所有 SysRq 功能开启。</li></ul></li></ol><h4 id="那个叫-kdb-的东西是什么" tabindex="-1">那个叫 Kdb 的东西是什么？ <a class="header-anchor" href="#那个叫-kdb-的东西是什么" aria-label="Permalink to &quot;那个叫 Kdb 的东西是什么？&quot;">​</a></h4><p>在这个菜单里，你可能还会看到 <strong>Kdb</strong>。</p><p>Kdb 是一个<strong>只有命令行</strong>的调试器。它不需要两台机器，直接在串口上就能用。你可以看内存、看寄存器、看日志，但它<strong>不支持源码级调试</strong>。</p><p>这就好比：KGDB 是带图形界面的专业 IDE，Kdb 是只有 hex 编辑器的控制台。我们这一章专注于 KGDB，Kdb 作为一个备选手段了解一下就行。</p><h4 id="需要关闭的选项-避坑" tabindex="-1">需要关闭的选项（避坑） <a class="header-anchor" href="#需要关闭的选项-避坑" aria-label="Permalink to &quot;需要关闭的选项（避坑）&quot;">​</a></h4><p>有些安全特性会跟调试软件断点打架。如果以下选项出现在你的菜单里，<strong>把它们关掉</strong>：</p><ul><li><strong><code>CONFIG_STRICT_KERNEL_RWX</code></strong></li><li><strong><code>CONFIG_STRICT_MODULE_RWX</code></strong></li></ul><p>这俩选项强制内核代码段只读、数据段不可执行。这本意是安全防护，但它会阻止 GDB 写入软件断点指令（本质上是把某条指令替换成 <code>INT 3</code>）。</p><blockquote><p><strong>解决办法</strong>：要么关掉它们，要么使用<strong>硬件断点</strong>（Hardware Breakpoints）。硬件断点不修改内存，用的是 CPU 的调试寄存器，所以不怕这种保护。本书推荐优先使用硬件断点。</p></blockquote><h4 id="推荐的额外选项-可选但很有用" tabindex="-1">推荐的额外选项（可选但很有用） <a class="header-anchor" href="#推荐的额外选项-可选但很有用" aria-label="Permalink to &quot;推荐的额外选项（可选但很有用）&quot;">​</a></h4><p>如果不嫌麻烦，以下选项能提升体验：</p><ul><li><strong><code>FRAME_POINTER=y</code></strong>（<code>Kernel hacking</code> -&gt; <code>Compile the kernel with frame pointers</code>） <ul><li>虽然标了 Optional，但在栈回溯时非常有用。如果你的平台默认开了 <code>CONFIG_UNWINDER_ARM</code>，那这个选项可能冲突，看情况选择。</li></ul></li><li><strong><code>DEBUG_INFO_SPLIT=y</code></strong>：把调试信息拆分成 <code>.dwo</code> 文件，减少 vmlinux 体积。</li><li><strong><code>GDB_SCRIPTS=y</code></strong>：这个很赞。它会在你加载 vmlinux 时，自动链接一些 Python 写的 GDB 辅助脚本（比如 <code>lx-symbols</code>, <code>lx-lsmod</code>）。后面有一节专门讲这个。</li><li><strong><code>DEBUG_FS=y</code></strong>：挂载 debugfs 文件系统，很多内核调试信息都在这。</li></ul><blockquote><p>⚠️ <strong>最后一个警告</strong></p><p>在调试内核时，<strong>一定要关掉看门狗</strong>。</p><p>软件或硬件看门狗如果开着，你一旦在断点处停久一点，喂狗超时，系统就会直接重启。你刚找到的 bug 就这样消失了，连个报错都看不到。</p></blockquote><hr><h4 id="在-seals-里开启-kgdb" tabindex="-1">在 SEALS 里开启 KGDB <a class="header-anchor" href="#在-seals-里开启-kgdb" aria-label="Permalink to &quot;在 SEALS 里开启 KGDB&quot;">​</a></h4><p>如果你用 SEALS，事情会简单点。SEALS 的构建脚本里有预留的 KGDB 开关。</p><p>打开你板子的 <code>build.config</code> 文件，找到这一行：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> KGDB</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build.config</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">KGDB_MODE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # make &#39;1&#39; to have qemu run with the &#39;-s -S&#39; </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            # switch (waits for client GDB to &#39;connect&#39;)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>把 <code>KGDB_MODE</code> 改成 <code>1</code>。这样 SEALS 生成的 QEMU 启动脚本就会自动加上 <code>-s -S</code> 参数，让 QEMU 在启动时就冻结 CPU，老实等着 GDB 连上来。</p><blockquote><p><strong>Tip：GCC Plugins</strong></p><p>编译内核时如果问你 Enable GCC plugins? 建议选 <strong>No</strong>。 插件有时会干扰调试信息的生成，或者引入一些奇怪的优化，反而增加调试难度。保持简单。</p></blockquote><h3 id="_11-3-3-试运行目标系统" tabindex="-1">11.3.3 试运行目标系统 <a class="header-anchor" href="#_11-3-3-试运行目标系统" aria-label="Permalink to &quot;11.3.3  试运行目标系统&quot;">​</a></h3><p>别信我的，信事实。</p><p>配置完、编译完，把 SEALS 生成的所有组件凑齐。在 SEALS 的 staging 目录里，你应该能找到这些东西：</p><ul><li><strong>内核镜像</strong>： <ul><li><code>.../linux-5.10.109/arch/arm/boot/zImage</code>（给 Bootloader 的压缩包，启动用）</li><li><code>.../linux-5.10.109/vmlinux</code>（<strong>蓝图</strong>，带调试符号，给 GDB 用）</li></ul></li><li><strong>设备树</strong>：<code>.../linux-5.10.109/arch/arm/boot/dts/vexpress-v2p-ca9.dtb</code></li><li><strong>根文件系统</strong>：<code>.../seals_staging_vexpress/images/rfs.img</code></li></ul><p>现在，用 QEMU 把它们串起来。</p><p>如果你是手动跑 QEMU，命令大概长这样（不要被吓跑，我们拆开看）：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">qemu-system-arm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -M</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vexpress-a9</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -m</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 512</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -smp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 4,sockets=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -kernel</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/seals_staging_vexpress/images/zImage</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -drive</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> file=/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/seals_staging_vexpress/images/rfs.img,if=sd,format=raw</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -append</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;console=ttyAMA0 rootfstype=ext4 root=/dev/mmcblk0 init=/sbin/init&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -dtb</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">..</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/seals_staging_vexpress/images/vexpress-v2p-ca9.dtb</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -nographic</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p><strong>参数解读（别跳过）</strong>：</p><ul><li><code>-M vexpress-a9</code>：指定模拟的板子型号。运行 <code>qemu-system-arm -M help</code> 可以看 QEMU 支持的所有板子。</li><li><code>-m 512</code>：给虚拟机分配 512MB 内存。</li><li><code>-kernel ...</code>：指定内核镜像。注意这里给的是 <strong>zImage</strong>，因为是要启动它。</li><li><code>-drive file=...,if=sd</code>：指定根文件系统镜像，并告诉 QEMU 把它模拟成 SD 卡。</li><li><code>-append &quot;...&quot;</code>：这是传给内核的启动参数。 <ul><li><code>console=ttyAMA0</code>：把控制台输出重定向到这个虚拟串口（ARM 的标准串口）。</li><li><code>root=/dev/mmcblk0</code>：告诉内核根文件系统在模拟的 SD 卡上。</li></ul></li><li><code>-dtb ...</code>：必须指定 DTB 文件，否则内核不知道自己长啥样。</li></ul><p>如果一切正常，你会在终端看到滚动的启动 log，最后停在 BusyBox 的 shell 提示符上：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[    2.345678] Freeing unused kernel memory: 204K</span></span>
<span class="line"><span>/ #</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>看到那个 <code>/ #</code> 了吗？</p><p>恭喜，你的 ARM 虚拟机活过来了。</p><p>图 11.4 和 11.5 展示了这一步的截图（QEMU 启动日志和 shell 登录）。如果你能复现这个界面，说明「目标系统」这块就已经搞定了。</p><p>现在我们手里有了一只听话的虚拟羊，下一章，我们要拿刀（GDB）切开它看看里面是怎么跑的。</p>`,62)])])}const k=i(l,[["render",t]]);export{g as __pageData,k as default};
