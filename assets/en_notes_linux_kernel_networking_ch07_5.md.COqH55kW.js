import{_ as t,o as s,c as o,a as i}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"7.5  Quick Reference","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch07_5.md","filePath":"en/notes/linux_kernel_networking/ch07_5.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_networking/ch07_5.md"};function a(r,e,c,h,l,d){return s(),o("div",null,[...e[0]||(e[0]=[i(`<h1 id="_7-5-quick-reference" tabindex="-1">7.5 Quick Reference <a class="header-anchor" href="#_7-5-quick-reference" aria-label="Permalink to &quot;7.5  Quick Reference&quot;">​</a></h1><p>At this point, we have dissected the skeleton (core structures), blood (protocol interactions), and muscles (state machine) of the neighbor subsystem.</p><p>Now, it&#39;s time to set aside the scalpel and open up a heavy anatomy atlas.</p><p>This section isn&#39;t meant to be &quot;read&quot;—it&#39;s meant to be &quot;consulted.&quot; We lay out the important methods, macros, and structures discussed in this chapter so you don&#39;t have to scour the kernel source code later for that one line of invocation. You&#39;ll find that many names that flashed by in the code flow now have a clear place here.</p><blockquote><p><strong>Note</strong></p><p>The core neighbor code hides in:</p><ul><li><code>net/core/neighbour.c</code></li><li><code>include/net/neighbour.h</code></li><li><code>include/uapi/linux/neighbour.h</code></li></ul><p>The ARP (IPv4) nest is in:</p><ul><li><code>net/ipv4/arp.c</code></li><li><code>include/net/arp.h</code></li><li><code>include/uapi/linux/if_arp.h</code></li></ul><p>The NDISC (IPv6) turf is in:</p><ul><li><code>net/ipv6/ndisc.c</code></li><li><code>include/net/ndisc.h</code></li></ul></blockquote><hr><h3 id="methods" tabindex="-1">Methods <a class="header-anchor" href="#methods" aria-label="Permalink to &quot;Methods&quot;">​</a></h3><p>We start with the core methods—the heartbeat of the neighbor subsystem.</p><p><strong>Table Management and Initialization</strong></p><p><code>void neigh_table_init(struct neigh_table *tbl)</code></p><blockquote><p>This is the first key to starting the engine. It calls <code>neigh_table_init_no_netlink()</code> to complete all initialization work for the neighbor table, then conveniently hangs this table (<code>tbl</code>) on the global neighbor table list (<code>neigh_tables</code>). Without this step, the kernel doesn&#39;t even know this table exists.</p></blockquote><p><code>void neigh_table_init_no_netlink(struct neigh_table *tbl)</code></p><blockquote><p>The grunt worker of <code>neigh_table_init</code>. It handles the dirty work—allocating memory, initializing the hash table, setting parameters—but it doesn&#39;t hang itself on the global list. That&#39;s the older brother&#39;s (<code>neigh_table_init</code>) job.</p></blockquote><p><code>int neigh_table_clear(struct neigh_table *tbl)</code></p><blockquote><p>This is the cleaning lady. When a neighbor table is no longer needed (e.g., during module unloading), this method is responsible for releasing all associated resources.</p></blockquote><p><strong>Birth and Death of Neighbor Entries</strong></p><p><code>struct neighbour *neigh_alloc(struct neigh_table *tbl, struct net_device *dev)</code></p><blockquote><p>Allocates a new <code>neighbour</code> object. Don&#39;t assume this is just a simple <code>kzalloc</code>—internally, it keeps an eye on <code>gc_thresh</code>&#39;s mood. If the table is too crowded, it might trigger a round of garbage collection before allocating memory for you. We discussed this mechanism in detail in the &quot;Creation and Release&quot; section.</p></blockquote><p><code>struct neighbour *__neigh_create(struct neigh_table *tbl, const void *pkey, struct net_device *dev, bool want_ref)</code></p><blockquote><p>The true creator. <code>neigh_alloc</code> only gives you an empty shell, but <code>__neigh_create</code> uses the <code>pkey</code> (L3 address) and <code>dev</code> (device) to fully construct the neighbor object and insert it into the hash table. If you pass in <code>want_ref=true</code>, it thoughtfully increments the reference count to prevent it from being reclaimed by someone else before you can use it.</p></blockquote><p><code>struct neighbour *__neigh_lookup(struct neigh_table *tbl, const void *pkey, struct net_device *dev, int creat)</code></p><blockquote><p>The detective. It checks the hash table to see if the neighbor corresponding to <code>pkey</code> exists. The <code>creat</code> parameter determines what to do if it&#39;s not found: if it&#39;s 1, it directly calls <code>neigh_create</code> to fabricate one on the spot; if it&#39;s 0, it throws its hands up and returns NULL.</p></blockquote><p><strong>Conversations with User Space</strong></p><p><code>int neigh_add(struct sk_buff *skb, struct nlmsghdr *nlh, void *arg)</code></p><blockquote><p>The Netlink service receptionist. When you type <code>ip neigh add ...</code>, the kernel eventually ends up here. It processes <code>RTM_NEWNEIGH</code> messages, turning user space&#39;s intentions (like adding a static ARP entry) into kernel data structures.</p></blockquote><p><code>int neigh_delete(struct sk_buff *skb, struct nlmsghdr *nlh, void *arg)</code></p><blockquote><p>Same as above, but it handles <code>RTM_DELNEIGH</code>—corresponding to when you type <code>ip neigh del ...</code>.</p></blockquote><p><strong>Garbage Collection and Timers</strong></p><p><code>int neigh_forced_gc(struct neigh_table *tbl)</code></p><blockquote><p>The forced eviction team. This is a <strong>synchronous</strong> garbage collection method. It ruthlessly kicks out all neighbor entries that are not in a permanent state (<code>NUD_PERMANENT</code>) and have a reference count of 1.</p><p>Its process is decisive: it first sets the neighbor&#39;s <code>dead</code> flag to 1, then calls <code>neigh_cleanup_and_release()</code> to send it on its way. Remember what we mentioned earlier? When memory pressure reaches a certain point, <code>neigh_alloc</code> wakes up this eviction team to make room. If it successfully tears down at least one entry, it returns 1; otherwise, it returns 0.</p></blockquote><p><code>void neigh_periodic_work(struct work_struct *work)</code></p><blockquote><p>This is an <strong>asynchronous</strong> cleaner that periodically does the chores. Unlike <code>forced_gc</code>, it&#39;s not as violent, gently cleaning up expired entries.</p></blockquote><p><code>static void neigh_timer_handler(unsigned long arg)</code></p><blockquote><p>Every neighbor entry has its own countdown timer. This timer handler is the reaction to hearing the alarm—usually used to detect if the neighbor has gone down (e.g., transitioning from <code>REACHABLE</code> to <code>STALE</code>, or starting to send probe packets).</p></blockquote><p><strong>Sending and Probing Logic</strong></p><p><code>void neigh_probe(struct neighbour *neigh)</code></p><blockquote><p>&quot;Hello, is anyone there?&quot; This method pulls a data packet from the neighbor&#39;s <code>arp_queue</code> queue (if there is one), then calls the corresponding <code>solicit()</code> method (like ARP&#39;s <code>arp_solicit</code>) to send a probe. It also takes the opportunity to increment the probe counter and free the packet used for probing.</p></blockquote><p><code>neigh_hh_init(struct neighbour *n, struct dst_entry *dst)</code></p><blockquote><p>For performance, the kernel needs to cache L2 headers. This method initializes the neighbor&#39;s (<code>n</code>) hardware header cache (<code>hh_cache</code>) based on the routing cache entry (<code>dst</code>). With this, subsequent packet transmissions don&#39;t need to calculate the header every time—a simple memcpy does the job.</p></blockquote><p><code>static int neigh_blackhole(struct neighbour *neigh, struct sk_buff *skb)</code></p><blockquote><p>The black hole. This isn&#39;t an astrophysical black hole, but a network dead end. It directly drops the packet and returns <code>-ENETDOWN</code> (network unreachable). This is usually used as a fallback strategy for the neighbor&#39;s <code>output</code> callback—when everything else fails, at least it prevents a kernel panic.</p></blockquote><hr><h3 id="arp-specific-methods" tabindex="-1">ARP-Specific Methods <a class="header-anchor" href="#arp-specific-methods" aria-label="Permalink to &quot;ARP-Specific Methods&quot;">​</a></h3><p>Below are the &quot;dialects&quot; unique to the IPv4 realm.</p><p><code>void __init arp_init(void)</code></p><blockquote><p>The ARP module&#39;s <code>main()</code> function. It is called during system startup and does a series of odd jobs:</p><ol><li>Initializes the ARP table (<code>arp_tbl</code>).</li><li>Registers <code>arp_rcv</code>, telling the kernel &quot;hand me any received ARP packets.&quot;</li><li>Creates various entries under <code>/proc</code>.</li><li>Registers sysctl parameters (the switches you see under <code>/proc/sys/net/ipv4/</code>).</li><li>Registers the network device notifier <code>arp_netdev_event</code> so that ARP can react when NIC state changes.</li></ol></blockquote><p><code>int arp_rcv(struct sk_buff *skb, struct net_device *dev, struct packet_type *pt, struct net_device *orig_dev)</code></p><blockquote><p>The packet reception entry point. Although logically we talk about &quot;handling ARP requests,&quot; in the kernel, everything starts with <code>arp_rcv</code> catching an Ethernet frame of type <code>0x0806</code>. It handles some validity checks (like whether the packet length is sufficient) and then hands it off to <code>arp_process</code> for processing.</p></blockquote><p><code>int arp_process(struct sk_buff *skb)</code></p><blockquote><p>The real brain. This is what we spent a lot of time dissecting earlier. It parses the ARP header and decides whether to send a reply or update the local cache. The stories happening here include: passive learning, handling ARP requests, handling ARP replies, and triggering <code>neigh_update</code>.</p></blockquote><p><code>int arp_constructor(struct neighbour *neigh)</code></p><blockquote><p>The constructor. When a new neighbor entry is created (specifically for the ARP table), the kernel calls this function to initialize the ARP-specific parts, such as setting the appropriate <code>neigh_ops</code> callbacks.</p></blockquote><p><code>void arp_solicit(struct neighbour *neigh, struct sk_buff *skb)</code></p><blockquote><p>&quot;I&#39;ll go ask.&quot; This method is responsible for sending an ARP request (<code>ARPOP_REQUEST</code>). Before sending, it does a bunch of mental preparation (checking various flags, deciding whether to use unicast or broadcast probes), and finally calls <code>arp_send</code> to throw the packet out.</p></blockquote><p><code>void arp_send(...)</code></p><blockquote><p>A convenience wrapper function. It calls <code>arp_create</code> to craft an ARP packet based on parameters like IP address and MAC address, then calls <code>arp_xmit</code> to send it.</p></blockquote><p><code>void arp_xmit(struct sk_buff *skb)</code></p><blockquote><p>The final kick. It calls <code>NF_HOOK</code> (Netfilter hooks), and if the firewall lets it through, hands it to the <code>dev_queue_xmit</code> driver for transmission.</p></blockquote><p><code>struct arphdr *arp_hdr(const struct sk_buff *skb)</code></p><blockquote><p>The lazy macro. It helps you calculate the offset and directly hands you the ARP header pointer (<code>struct arphdr</code>) from within <code>skb</code>. Stop calculating <code>skb-&gt;data + sizeof(struct ethhdr)</code> yourself—use this.</p></blockquote><p><code>int arp_mc_map(__be32 addr, u8 *haddr, struct net_device *dev, int dir)</code></p><blockquote><p>Multicast address translation. Mapping an IP address to a multicast MAC address isn&#39;t a simple arithmetic problem. For Ethernet, it calls <code>ip_eth_mc_map</code>; for InfiniBand, it calls something else. This function is that interpreter.</p></blockquote><p><code>int arp_fwd_proxy(...) / int arp_fwd_pvlan(...)</code></p><blockquote><p>The judge. These are used to determine whether the current device should enable <strong>Proxy ARP</strong> or <strong>Proxy ARP PVLAN</strong>. We mentioned proxy NDP in the NDISC section, and ARP has similar logic here—the router answers on behalf of the target host, saying &quot;I am them.&quot;</p></blockquote><hr><h3 id="ndisc-specific-methods" tabindex="-1">NDISC-Specific Methods <a class="header-anchor" href="#ndisc-specific-methods" aria-label="Permalink to &quot;NDISC-Specific Methods&quot;">​</a></h3><p>Moving to IPv6, things get slightly more modern.</p><p><code>int ndisc_rcv(struct sk_buff *skb)</code></p><blockquote><p>The NDISC main entry point. Although it&#39;s called <code>ndisc</code>, it actually sits on top of the ICMPv6 protocol. When you receive an ICMPv6 packet of type Neighbor Solicitation (NS) or Neighbor Advertisement (NA), <code>icmpv6_rcv</code> hands the packet over to <code>ndisc_rcv</code>.</p></blockquote><p><code>static void ndisc_recv_ns(struct sk_buff *skb)</code></p><blockquote><p>Specifically handles NS (Neighbour Solicitation) packets. The logic here corresponds to the &quot;handle request&quot; branch in ARP&#39;s <code>arp_process</code>, but it&#39;s much more complex—it also has to consider DAD (Duplicate Address Detection).</p></blockquote><p><code>static void ndisc_recv_na(struct sk_buff *skb)</code></p><blockquote><p>Specifically handles NA (Neighbour Advertisement) packets. It receives the neighbor&#39;s reply and updates the state machine.</p></blockquote><p><code>static void ndisc_recv_rs(struct sk_buff *skb)</code></p><blockquote><p>Handles RS (Router Solicitation). This is the host shouting &quot;Are there any routers? I need to get online.&quot;</p></blockquote><p><code>static void ndisc_router_discovery(struct sk_buff *skb)</code></p><blockquote><p>Handles RA (Router Advertisement). The router says &quot;I&#39;m here, and here&#39;s how I&#39;m configured...&quot; This is where those parameters are processed.</p></blockquote><p><code>int ndisc_constructor(struct neighbour *neigh)</code></p><blockquote><p>Corresponds to ARP&#39;s <code>arp_constructor</code>. The callback when initializing an IPv6 neighbor entry, setting the operation set specific to <code>ndisc</code>.</p></blockquote><p><code>void ndisc_solicit(struct neighbour *neigh, struct sk_buff *skb)</code></p><blockquote><p>&quot;I&#39;ll go ask (IPv6 version).&quot; It calls <code>ndisc_send_ns</code> to send a Neighbor Solicitation.</p></blockquote><p><code>int ndisc_mc_map(const struct in6_addr *addr, char *buf, struct net_device *dev, int dir)</code></p><blockquote><p>Same as <code>arp_mc_map</code>, but for IPv6 multicast addresses. On Ethernet, it calls <code>ipv6_eth_mc_map</code>.</p></blockquote><hr><h3 id="macros" tabindex="-1">Macros <a class="header-anchor" href="#macros" aria-label="Permalink to &quot;Macros&quot;">​</a></h3><p>The kernel loves using macros to encapsulate those tedious but logically fixed checks. Below are the ones you&#39;ll frequently run into when reading the neighbor subsystem code.</p><p><strong>ARP Behavior Control</strong></p><p>These macros are usually used to check the switch states under <code>/proc/sys/net/ipv4/conf/</code>.</p><p><code>IN_DEV_PROXY_ARP(in_dev)</code></p><blockquote><p>Checks if <code>proxy_arp</code> is enabled. It doesn&#39;t just look at the specific device&#39;s configuration; it also checks the settings of <code>all</code> and <code>default</code>.</p></blockquote><p><code>IN_DEV_ARPFILTER(in_dev)</code></p><blockquote><p>Checks <code>arp_filter</code>. This switch determines whether the kernel strictly filters ARP responses based on subnet matching. When enabled, if the requested ARP address is not within the receiving interface&#39;s subnet, the kernel might pretend it didn&#39;t hear it.</p></blockquote><p><code>IN_DEV_ARP_ACCEPT(in_dev)</code></p><blockquote><p>Checks <code>arp_accept</code>. This determines whether the kernel accepts &quot;gratuitous ARP&quot; (unsolicited advertisements sent without being asked). It&#39;s typically used in load balancing or high-availability failover scenarios.</p></blockquote><p><code>IN_DEV_ARP_IGNORE(in_dev)</code></p><blockquote><p>A very decisive macro. It returns the value of <code>arp_ignore</code>. This value controls the kernel&#39;s response level to ARP requests:</p><ul><li>0: Respond to any IP owned by the local machine, regardless of the interface (default).</li><li>1: Respond only if the target IP is configured on the receiving interface (preventing multi-interface confusion).</li><li>Higher values are stricter, even to the point of not responding at all.</li></ul></blockquote><p><code>IN_DEV_ARP_ANNOUNCE(in_dev)</code></p><blockquote><p>Corresponds to <code>arp_announce</code>. It controls how we choose the source IP address when sending ARP requests:</p><ul><li>0: Use any address on the local machine&#39;s interfaces.</li><li>1: Try to use an address in the subnet where this network interface resides.</li><li>2: Always use the primary address of this interface. This is to avoid being blacklisted by neighbors in complex routing environments because of a weird source IP.</li></ul></blockquote><p><code>IN_DEV_SHARED_MEDIA(in_dev)</code></p><blockquote><p>Checks <code>shared_media</code>. If enabled, the kernel assumes different media types (like Ethernet and PPP) share the same IP space, which affects the subnet mask calculation logic.</p></blockquote><p><strong>Generic Operations</strong></p><p><code>neigh_hold()</code></p><blockquote><p>The &quot;hold on tight&quot; macro. It increments the neighbor object&#39;s reference count (<code>refcnt</code>). This prevents the neighbor from being suddenly freed by someone else while you&#39;re operating on it. This is crucial in concurrent environments.</p></blockquote><hr><h3 id="neigh-statistics-structure" tabindex="-1"><code>neigh_statistics</code> Structure <a class="header-anchor" href="#neigh-statistics-structure" aria-label="Permalink to &quot;\`neigh_statistics\` Structure&quot;">​</a></h3><p>Finally, let&#39;s look at the neighbor subsystem&#39;s &quot;scoreboard.&quot;</p><p>As we mentioned at the beginning of this chapter, both ARP and NDISC export statistics via procfs (in <code>/proc/net/stat/arp_cache</code> and <code>/proc/net/stat/ndisc_cache</code>, respectively). This data is read from the <code>neigh_statistics</code> structure inside the kernel.</p><p>Let&#39;s see what these fields represent and where in the kernel they get incremented.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> neigh_statistics {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> allocs;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           /* 已分配的邻居数量           */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> destroys;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">         /* 已销毁的邻居数量           */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hash_grows;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       /* 哈希表扩容次数             */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> res_failed;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       /* 解析失败的次数             */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> lookups;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          /* 查询次数                   */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> hits;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">             /* 查询命中次数（在 lookups 中）*/</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rcv_probes_mcast;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 接收到的多播探测 (IPv6)    */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rcv_probes_ucast;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 接收到的单播探测 (IPv6)    */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> periodic_gc_runs;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 周期性 GC 执行次数         */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> forced_gc_runs;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   /* 强制 GC 执行次数           */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> unres_discards;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   /* 因解析失败而丢弃的包数     */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p><strong>Field Details</strong></p><ul><li><p><strong>allocs</strong>: Incremented by one every time <code>neigh_alloc()</code> successfully allocates. If this value skyrockets, it means there are a massive number of new hosts communicating wildly on your network, or someone is launching an attack.</p></li><li><p><strong>destroys</strong>: Incremented by one each time <code>neigh_destroy()</code> is called. Under normal circumstances, it should maintain some kind of dynamic balance with <code>allocs</code>.</p></li><li><p><strong>hash_grows</strong>: When the hash table gets too crowded (linked lists are too long), the kernel calls <code>neigh_hash_grow()</code> to expand. This number records the expansion count. If it&#39;s high, it indicates your network environment is very large or active.</p></li><li><p><strong>res_failed</strong>: Resolution failures. <code>neigh_invalidate()</code> increments this count when it completely gives up on a neighbor.</p></li><li><p><strong>lookups</strong> / <strong>hits</strong>: This is a pair of performance metrics. <code>lookups</code> is the total number of calls to <code>neigh_lookup</code>, and <code>hits</code> is the number of times it was found directly in the hash table. The closer <code>hits / lookups</code> is to 1, the higher the cache hit rate, and the smoother the network.</p></li><li><p><strong>rcv_probes_mcast / ucast</strong>: These two are IPv6-specific. They record how many NS (Neighbor Solicitation) messages were received. Separating unicast and multicast statistics helps you troubleshoot network conditions—for example, if they&#39;re all unicast probes with no replies, the network might be unidirectionally blocked.</p></li><li><p><strong>periodic_gc_runs / forced_gc_runs</strong>: Garbage collection activity. If <code>forced_gc_runs</code> is high, it means memory pressure is severe, and the kernel is continuously and violently cleaning up to make room.</p></li><li><p><strong>unres_discards</strong>: The most tragic statistic. When <code>__neigh_event_send</code> finds that a neighbor hasn&#39;t been resolved, and the queue is full or the packet wasn&#39;t meant to wait anyway, it can only drop the packet and record it here.</p></li></ul><hr><h3 id="_7-6-chapter-echoes" tabindex="-1">7.6 Chapter Echoes <a class="header-anchor" href="#_7-6-chapter-echoes" aria-label="Permalink to &quot;7.6  Chapter Echoes&quot;">​</a></h3><p>Finishing this chapter, the &quot;mysterious veil&quot; beneath the L3 layer in the Linux network stack has finally been lifted.</p><p>Many network tutorials will tell you that &quot;networks use IP addresses for routing,&quot; which is true. But they usually forget one crucial thing: <strong>on that final hop, the IP address is actually useless</strong>. NICs don&#39;t recognize IPs at all; they only recognize MACs.</p><p><strong>The neighbor subsystem is the bridge that translates &quot;address languages.&quot;</strong></p><p>The first understanding we built in this chapter is the <strong>cost of &quot;discovery.&quot;</strong> When you ping an IP, you&#39;re actually having two conversations: one is a broadcast asking &quot;who has this IP?&quot;, and the other is a unicast saying &quot;I want to send data to you.&quot; If this step goes wrong, the symptom is &quot;network unreachable,&quot; but underneath, it might just be that an entry in the ARP table has aged out.</p><p>The second understanding is the <strong>fragility of &quot;trust.&quot;</strong> As we saw when discussing NUD (Neighbor Unreachability Detection), the kernel must constantly suspect whether a neighbor is still alive. From <code>REACHABLE</code> to <code>STALE</code>, and then to <code>PROBE</code>, this state machine is like a paranoid gatekeeper. This paranoia is necessary—because in a LAN, unplugging a cable doesn&#39;t require submitting a request, and plugging one in doesn&#39;t require saying hello. The kernel can only rely on continuous probing to maintain consistency with reality.</p><p>Remember the question from the beginning—why is IPv6&#39;s NDISC so much more complex than IPv4&#39;s ARP, even though they do the same thing? The answer is now clear: <strong>because ARP was designed for a simple interconnected network, while NDISC was designed for a complex network full of routers, auto-configuration, and security concerns.</strong> Every Flag in NDISC (Router, Override, Solicited) is essentially there to patch the various security and logic vulnerabilities exposed by the ARP protocol in the early Internet.</p><p>And ultimately, all these complex mechanisms—queries, caching, timeouts, probes, garbage collection—are encapsulated in those two simplest system calls: <code>connect()</code> and <code>sendmsg()</code>. As a user, you just write down the destination IP, and all the dirty work is silently done behind the scenes by the massive and precise mechanism described in this chapter.</p><p>Only by understanding the neighbor subsystem do you truly understand how a &quot;LAN&quot; operates in the kernel&#39;s eyes.</p><p>In the next chapter, we&#39;ll turn our gaze further away—<strong>routing</strong>. If the neighbor subsystem solves the question of &quot;who is the next hop?&quot;, then the routing system solves the question of &quot;which direction should we go?&quot; That is the true decision center of the network stack.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: Understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: Understanding&quot;">​</a></h3><p><strong>Question</strong>: During the initialization of the Linux kernel&#39;s neighbor subsystem, creating a new neighbor entry triggers a synchronous garbage collection mechanism. Under what condition regarding the current number of entries in the neighbor table will the kernel forcibly trigger synchronous garbage collection (<code>neigh_forced_gc</code>)?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: When the number of neighbor entries is greater than gc_thresh3 (default 1024), or when the number of entries is greater than gc_thresh2 (default 512) and the time since the last flush exceeds 5 seconds.</p><p><strong>Analysis</strong>: Based on the logic in the <code>neigh_alloc()</code> method, the kernel checks the entry count before allocating a new neighbor. The code logic is: <code>if (entries &gt;= tbl-&gt;gc_thresh3 || (entries &gt;= tbl-&gt;gc_thresh2 &amp;&amp; time_after(now, tbl-&gt;last_flush + 5 * HZ)))</code>. If the entry count still exceeds gc_thresh3 after triggering garbage collection, the allocation fails. This ensures that the neighbor table can automatically clean up old entries under memory pressure, while also limiting the performance overhead caused by frequent scans.</p></details><h3 id="exercise-2-understanding" tabindex="-1">Exercise 2: Understanding <a class="header-anchor" href="#exercise-2-understanding" aria-label="Permalink to &quot;Exercise 2: Understanding&quot;">​</a></h3><p><strong>Question</strong>: The kernel uses the <code>struct neighbour</code> structure to manage neighbor nodes. When a packet needs to be sent but the neighbor&#39;s link-layer address (such as a MAC address) has not yet been resolved, in which member queue of the structure are the pending packets temporarily stored? Once resolution is complete, in which member does the kernel cache the resolved L2 header to accelerate the encapsulation of subsequent packets?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Packets are temporarily stored in <code>arp_queue</code>; the L2 header is cached in <code>hh</code> (hh_cache).</p><p><strong>Analysis</strong>: During address resolution (usually in the NUD_INCOMPLETE state), the neighbor entry uses <code>arp_queue</code> (an SKB queue) to cache packets waiting to be sent, preventing packet loss. Once resolution succeeds, the kernel stores the constructed L2 header in <code>hh</code> (<code>struct hh_cache</code>). On subsequent transmissions, the kernel can directly copy this header without needing to re-resolve or look it up, significantly improving forwarding performance.</p></details><h3 id="exercise-3-application" tabindex="-1">Exercise 3: Application <a class="header-anchor" href="#exercise-3-application" aria-label="Permalink to &quot;Exercise 3: Application&quot;">​</a></h3><p><strong>Question</strong>: Suppose you are troubleshooting a complex network fault: Server A has two interfaces, eth0 (192.168.1.10/24) and eth1 (192.168.2.10/24). You enable Proxy ARP on eth0 to allow Server B (192.168.1.20) to access Server C (192.168.2.20). At this point, a large number of ARP requests might cause Server A&#39;s ARP table to rapidly expand. To reduce the impact of ARP processing on normal traffic, what mechanism (involving proxy_timer and proxy_queue) does the kernel use to optimize Proxy ARP handling?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: The kernel uses a delayed processing mechanism, caching Proxy ARP request packets in <code>proxy_queue</code> and utilizing <code>proxy_timer</code> to batch-process them after a random delay (up to proxy_delay).</p><p><strong>Analysis</strong>: In a Proxy ARP scenario, a host might receive a massive number of ARP requests. Processing every request immediately could consume significant CPU resources and lead to queue overflow. The kernel implements a delay strategy through <code>neigh_proxy_process()</code> and <code>proxy_timer</code>: request packets are placed into <code>proxy_queue</code>, and replies are sent after waiting for a random amount of time. This mechanism gives the real IP owner (the host that actually owns the IP) a chance to respond first, while also smoothing out the Proxy ARP processing load. This is critical in high-traffic gateway or load-balancing scenarios.</p></details><h3 id="exercise-4-thinking" tabindex="-1">Exercise 4: Thinking <a class="header-anchor" href="#exercise-4-thinking" aria-label="Permalink to &quot;Exercise 4: Thinking&quot;">​</a></h3><p><strong>Question</strong>: In the IPv6 Neighbor Discovery Protocol (NDISC), when a host configures a new IPv6 address, it must perform &quot;Duplicate Address Detection&quot; (DAD). In the kernel, once the DAD process starts, the IPv6 address is assigned a specific state flag (such as IFA_F_TENTATIVE). If a data packet must be sent before DAD completes (i.e., while the address is still in the Tentative state) and the kernel has &quot;Optimistic DAD&quot; enabled, what is the fundamental difference in the kernel&#39;s behavior compared to when this feature is not enabled?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: When Optimistic DAD is not enabled, a Tentative state address cannot be used for communication, and packet transmission is blocked or restricted; when Optimistic DAD (RFC 4429) is enabled, the kernel allows using the Tentative address as a source address to send packets before DAD completes. Although this violates strict duplicate-free guarantees, it significantly reduces connection establishment delay during network startup.</p><p><strong>Analysis</strong>: This question tests a deep understanding of the IPv6 address state machine and performance optimizations. Standard DAD requires waiting for verification to pass before using the address, which introduces a bidirectional delay in connection establishment (especially when RTT is high). <code>addrconf_dad_start()</code> sets the <code>IFA_F_TENTATIVE</code> flag. Optimistic DAD assumes the probability of conflict is extremely low, thereby taking the risk of using the address early. This requires special handling in the protocol stack (such as restricting responses to neighbor solicitations) and represents an engineering trade-off of the &quot;reliability first&quot; principle, reflecting the kernel&#39;s design considerations in high-performance scenarios.</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>The Linux kernel uniformly manages the IPv4 ARP and IPv6 NDISC protocols through the neighbor subsystem, with the core responsibility of dynamically resolving Layer 3 IP addresses into Layer 2 MAC addresses. To balance resolution efficiency with system resources, the kernel uses a hash table to store neighbor entries and sets <code>gc_thresh</code> thresholds (soft limits trigger garbage collection, hard limits directly reject new connections) to prevent table overflow. When creating a neighbor entry, the kernel calls a specific constructor based on the protocol type (IPv4/IPv6), automatically identifies and handles special addresses like broadcast and multicast that don&#39;t need resolution, and works with the <code>neigh_ops</code> interface to adapt the sending behavior of different network device types (such as Ethernet or point-to-point devices).</p><p>On the sending path (like <code>ip_finish_output2</code>), packets query the neighbor table. If an entry doesn&#39;t exist or the state is unreachable, the kernel doesn&#39;t just drop the packet; it temporarily stores it in the <code>arp_queue</code> queue and triggers the resolution mechanism. For IPv4, this is achieved through broadcast or unicast ARP Requests; for IPv6, it&#39;s achieved through ICMPv6 Neighbor Solicitation (NS) messages. To optimize the network environment, the kernel carefully selects the source IP based on the <code>arp_announce</code> parameter when sending requests, and supports sending unicast probes first to reduce broadcast traffic. On the receiving path, the kernel utilizes a &quot;passive learning&quot; mechanism—whether it receives a request or a response, it casually records the sender&#39;s MAC address to update the neighbor table.</p><p>IPv6&#39;s NDISC protocol is more rigorous than ARP. It implements multiple functions including router discovery, prefix discovery, and address resolution through ICMPv6. To guarantee address uniqueness, IPv6 strictly requires performing Duplicate Address Detection (DAD) when configuring an address (i.e., marking it as Tentative state), sending a special NS message with an empty source address to confirm there are no conflicts before officially enabling it. NDISC&#39;s Neighbor Advertisement (NA) messages carry flags like Router, Solicited, and Override, which not only communicate resolution results but also precisely control neighbor cache state update strategies (such as whether to forcibly overwrite old caches), thereby building a neighbor discovery system that is more secure and controllable than plain ARP.</p><p>To cope with unreliable network environments, the neighbor subsystem introduces a comprehensive state machine (NUD states) to manage the lifecycle of entries. Entries transition from the initial <code>INCOMPLETE</code> state to the <code>REACHABLE</code> state after resolution. As time passes and they go unused, they age to the <code>STALE</code>, <code>DELAY</code>, or even <code>PROBE</code> states. The kernel decides whether to send data directly or perform address verification first based on these states. This &quot;trust but verify&quot; strategy, combined with timers and random timeout mechanisms, ensures that in highly concurrent and dynamically changing network topologies, link-layer mappings can remain efficient while also failing fast and recovering quickly, preventing the creation of network black holes.</p>`,142)])])}const g=t(n,[["render",a]]);export{u as __pageData,g as default};
