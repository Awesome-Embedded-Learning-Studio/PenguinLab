import{_ as t,o as a,c as s,a as i}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"9.9  Ftrace in Action: From Stack Overflow Monitoring to Android Debugging","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch09_9.md","filePath":"en/notes/linux_kernel_debugging/ch09_9.md","lastUpdated":null}'),o={name:"en/notes/linux_kernel_debugging/ch09_9.md"};function n(r,e,l,c,d,h){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="_9-9-ftrace-in-action-from-stack-overflow-monitoring-to-android-debugging" tabindex="-1">9.9 Ftrace in Action: From Stack Overflow Monitoring to Android Debugging <a class="header-anchor" href="#_9-9-ftrace-in-action-from-stack-overflow-monitoring-to-android-debugging" aria-label="Permalink to &quot;9.9  Ftrace in Action: From Stack Overflow Monitoring to Android Debugging&quot;">​</a></h1><p>Let&#39;s set the Instances topic aside for now.</p><p>With all these tools at our disposal, how do we actually use them? Ftrace is like a Swiss Army knife—so feature-rich that you might find yourself holding the blade, unsure where to cut. In this section, we&#39;ll look at a few real-world &quot;slaughter&quot; scenes—not actual slaughters, but Bug slaughters.</p><p>We&#39;ll start with a subtle but deadly kernel issue: stack overflows.</p><hr><h2 id="monitoring-kernel-stack-usage-—-ftrace-s-lie-detector" tabindex="-1">Monitoring Kernel Stack Usage — Ftrace&#39;s &quot;Lie Detector&quot; <a class="header-anchor" href="#monitoring-kernel-stack-usage-—-ftrace-s-lie-detector" aria-label="Permalink to &quot;Monitoring Kernel Stack Usage — Ftrace&#39;s &quot;Lie Detector&quot;&quot;">​</a></h2><p>You probably know that every live thread has two stacks: a user-space stack and a kernel-space stack.</p><p>The user-space stack is large and &quot;soft&quot;—it grows dynamically. On a typical Linux distribution, its upper limit (<code>RLIMIT_STACK</code>) is usually 8 MB. If you run out of space, the kernel automatically expands it until it hits this limit.</p><p>But the kernel-space stack is an entirely different beast.</p><p>It is fixed, rigid, and very small.</p><ul><li>On 32-bit systems, it&#39;s typically only <strong>8 KB</strong>.</li><li>On 64-bit systems, it&#39;s typically <strong>16 KB</strong>.</li></ul><p>This leads to a serious consequence: <strong>overflow</strong>. If your kernel code recurses too deeply or allocates overly large local variables, the kernel stack will overflow. This isn&#39;t as simple as &quot;throwing an exception&quot;—it usually means the system locks up instantly or triggers a Kernel Panic. It&#39;s the kind of bug that has you staring at the console at 3 AM, questioning your life choices.</p><blockquote><p>⚠️ <strong>Configuration Tip</strong></p><p>There are two kernel options that can slightly ease this pain:</p><ul><li><code>CONFIG_VMAP_STACK</code>: When enabled, the kernel stack is allocated from the <code>vmalloc</code> region. This allows the kernel to set up a &quot;guard page.&quot; If a stack overflow touches this guard page, the kernel triggers an Oops and gracefully kills the process instead of taking down the entire machine.</li><li><code>CONFIG_THREAD_INFO_IN_TASK</code>: This further mitigates the cascading effects of a stack overflow.</li></ul><p>If you&#39;re compiling a kernel, make sure to enable these.</p></blockquote><p>Because the kernel stack is so fragile, <strong>monitoring how much of it is used at runtime</strong> becomes an incredibly valuable debugging technique. Ftrace comes with a dedicated tool for exactly this—the <strong>Stack Tracer</strong>.</p><p>To use it, ensure your kernel configuration includes <code>CONFIG_STACK_TRACER=y</code> (it&#39;s usually enabled by default). It isn&#39;t directly controlled by <code>tracefs</code>, but rather toggled via the <code>/proc</code> pseudo-file <code>/proc/sys/kernel/stack_tracer_enabled</code>. It is disabled by default.</p><p>Let&#39;s do a hands-on exercise. We&#39;ll enable the ftrace stack tracer, run a sampling pass, and see which kernel functions are the &quot;stack-eating monsters&quot; (note that this requires root privileges):</p><h3 id="step-1-—-enable-the-stack-tracer" tabindex="-1">Step 1 — Enable the Stack Tracer <a class="header-anchor" href="#step-1-—-enable-the-stack-tracer" aria-label="Permalink to &quot;Step 1 — Enable the Stack Tracer&quot;">​</a></h3><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /proc/sys/kernel/stack_tracer_enabled</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><h3 id="step-2-—-run-a-sampling-pass" tabindex="-1">Step 2 — Run a Sampling Pass <a class="header-anchor" href="#step-2-—-run-a-sampling-pass" aria-label="Permalink to &quot;Step 2 — Run a Sampling Pass&quot;">​</a></h3><p>We need a script to help with this. I wrote a simple script, <code>ch9/ftrace/ftrc_1s.sh</code>, that enables ftrace and records kernel activity over a 1-second window. During this window, we&#39;ll try our best to trigger deep stack usage.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /sys/kernel/tracing</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">...</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/ch9/ftrace/ftrc_1s.sh</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[...]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><h3 id="step-3-—-check-the-maximum-stack-depth-and-details" tabindex="-1">Step 3 — Check the Maximum Stack Depth and Details <a class="header-anchor" href="#step-3-—-check-the-maximum-stack-depth-and-details" aria-label="Permalink to &quot;Step 3 — Check the Maximum Stack Depth and Details&quot;">​</a></h3><p>Now the data is recorded. We mainly care about two files:</p><ul><li><code>stack_max_size</code>: The maximum stack usage observed since system boot (or since the last reset).</li><li><code>stack_trace</code>: The detailed call stack from the moment that maximum depth was recorded.</li></ul><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stack_max_size</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stack_trace</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>The screenshot below shows the result of one run. In this example, the maximum kernel stack usage exceeded 4000 bytes (nearly 4 KB, meaning half the stack space on a 32-bit system is already gone).</p><blockquote><p><em>(Insert Figure 9.19 screenshot description here: showing stack_max_size as 4160 bytes, along with the detailed function call stack below it)</em></p></blockquote><p>If you see this number approaching 8 KB (32-bit) or 16 KB (64-bit), your heart rate should increase. That means you&#39;re only a few steps away from a crash.</p><p>For more details on the Stack Tracer, check out the official kernel documentation: <code>Documentation/trace/ftrace.html</code>.</p><hr><h2 id="how-android-uses-ftrace-to-debug-system-issues" tabindex="-1">How Android Uses Ftrace to Debug System Issues <a class="header-anchor" href="#how-android-uses-ftrace-to-debug-system-issues" aria-label="Permalink to &quot;How Android Uses Ftrace to Debug System Issues&quot;">​</a></h2><p>Whether a tool is useful depends on the battlefield.</p><p>The Android Open Source Project (AOSP) doesn&#39;t just use Ftrace—they practically treat it as a nuclear weapon. Internally, Android development wraps a layer around Ftrace, but also allows direct usage.</p><p>There&#39;s a particularly hardcore quote in the AOSP documentation that I recommend memorizing:</p><blockquote><p>&quot;However, every single difficult performance bug in 2015 and 2016 was ultimately root-caused using dynamic ftrace.&quot;</p></blockquote><p>That&#39;s a bold claim, but if you&#39;ve seen the complexity of Android devices, you&#39;ll understand it&#39;s not an exaggeration.</p><p>The documentation specifically highlights a few killer use cases for Ftrace:</p><ol><li><p><strong>Debugging Uninterruptible Sleep</strong> Why is this hard to debug? Because when a process is asleep, its logs might stop too. But with Ftrace&#39;s <code>function_graph</code> or <code>function</code> tracer combined with filters, you can capture a kernel stack snapshot <strong>every single time</strong> the code enters the <code>uninterruptible_sleep</code> function. It&#39;s like taking an EKG of a comatose patient every minute.</p></li><li><p><strong>Confirming When a Driver &quot;Hogs&quot; the CPU</strong> Drivers might disable interrupts or preemption for extended periods to maintain &quot;atomicity.&quot; This might seem safe in single-threaded code, but it&#39;s a disaster on multi-core systems.</p><p>Remember the &quot;latency tracers&quot; we mentioned in the previous section?</p><ul><li><code>irqsoff</code></li><li><code>preemptoff</code></li><li><code>preemptirqsoff</code></li></ul><p>The AOSP documentation calls them out directly: these tools are primarily used to <strong>confirm</strong> whether a driver is keeping interrupts or preemption disabled for too long.</p></li></ol><h3 id="real-world-case-post-camera-jank" tabindex="-1">Real-World Case: Post-Camera Jank <a class="header-anchor" href="#real-world-case-post-camera-jank" aria-label="Permalink to &quot;Real-World Case: Post-Camera Jank&quot;">​</a></h3><p>The documentation provides a real example involving a Pixel XL phone. <strong>Symptom</strong>: After taking an HDR photo, immediately rotating the viewfinder caused noticeable UI jank. <strong>Method</strong>: They tracked it down using Ftrace. Through the trace, they discovered that a critical path was blocked by a long-running operation, causing response latency. (See: <a href="https://source.android.com/devices/tech/debug/ftrace" target="_blank" rel="noreferrer">https://source.android.com/devices/tech/debug/ftrace</a>)</p><p>Beyond this example, the AOSP documentation summarizes several common patterns:</p><ul><li><strong>Driver Misbehavior</strong>: A driver keeps hardware interrupts (IRQs) or preemption disabled for too long, slowing down system responsiveness.</li><li><strong>Excessive Softirq Duration</strong>: Softirqs disable kernel preemption. If softirq handling takes too long, other tasks can&#39;t be scheduled in, and the system feels like it&#39;s frozen.</li></ul><p>This is fascinating stuff. If you&#39;re writing embedded Linux or Android drivers and don&#39;t understand Ftrace, you&#39;re essentially driving blind.</p><hr><h2 id="netflix-s-cloud-practice-a-preview" tabindex="-1">Netflix&#39;s Cloud Practice: A Preview <a class="header-anchor" href="#netflix-s-cloud-practice-a-preview" aria-label="Permalink to &quot;Netflix&#39;s Cloud Practice: A Preview&quot;">​</a></h2><p>This &quot;relentless pursuit&quot; mentality isn&#39;t limited to the mobile world.</p><p>In the cloud, Ftrace is also a stabilizing force. As a preview of an even more exciting real-world case: using the <strong>perf-tools</strong> scripts (the Ftrace-based frontend toolkit written by Brendan Gregg that we mentioned earlier) to debug a database disk I/O issue on Netflix Linux instances.</p><p>We&#39;ll dedicate a future section (&quot;Investigating a database disk I/O issue on Netflix cloud instances with perf-tools&quot;) to cover this.</p><p>There, the battlefield shifts from phones to cloud servers, and the weapon of choice shifts from raw Ftrace to more user-friendly scripts—but the core logic remains the same: <strong>making the invisible visible</strong>.</p>`,49)])])}const g=t(o,[["render",n]]);export{u as __pageData,g as default};
