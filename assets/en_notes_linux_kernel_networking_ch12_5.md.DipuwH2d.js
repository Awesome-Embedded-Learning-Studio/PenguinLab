import{_ as t,o as n,c as a,a as o}from"./chunks/framework.8tiMeCK-.js";const p=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch12_5.md","filePath":"en/notes/linux_kernel_networking/ch12_5.md","lastUpdated":null}'),i={name:"en/notes/linux_kernel_networking/ch12_5.md"};function s(r,e,h,c,l,d){return n(),a("div",null,[...e[0]||(e[0]=[o('<h2 id="_12-5-mac-layer-management-entity-mlme" tabindex="-1">12.5 MAC Layer Management Entity (MLME) <a class="header-anchor" href="#_12-5-mac-layer-management-entity-mlme" aria-label="Permalink to &quot;12.5  MAC Layer Management Entity (MLME)&quot;">​</a></h2><p>In the previous section, we talked about &quot;sleeping&quot; — how a client sleeps and how an AP keeps packets on its behalf. But this comes with a prerequisite: before going to sleep, the client must first make sure it has found the right &quot;pillow&quot; (AP), and that this &quot;pillow&quot; actually allows it to rest its head.</p><p>Establishing this trust relationship, along with its day-to-day maintenance (like switching to a different WiFi network, or moving around a room and switching to an AP with a stronger signal), falls outside the scope of data transmission. Instead, it belongs to the <strong>management layer</strong>.</p><p>In the grand architecture of 802.11, management tasks are divided among three pillars: the Physical Layer Management Entity (PLME), the Station Management Entity (SME), and the <strong>MAC Layer Management Entity (MLME)</strong>, which is our main focus here.</p><p>In this section, we shift our gaze away from moving data packets and look at how the MLME handles chores like <strong>scanning</strong>, <strong>authentication</strong>, <strong>association</strong>, and <strong>roaming</strong> — much like a busy switchboard operator.</p><hr><h3 id="_12-5-1-scanning-searching-for-signals-in-the-air" tabindex="-1">12.5.1 Scanning: Searching for Signals in the Air <a class="header-anchor" href="#_12-5-1-scanning-searching-for-signals-in-the-air" aria-label="Permalink to &quot;12.5.1  Scanning: Searching for Signals in the Air&quot;">​</a></h3><p>Imagine walking into a deep forest with a radio, wanting to know if there are any radio broadcasts. You have two ways to find out:</p><p>One is to <strong>listen quietly</strong>. You tune to a frequency, listen for any sound, and if there&#39;s nothing, you move to the next one. The other is to <strong>shout loudly</strong>. You tune to a frequency, shout &quot;Is anyone there?&quot; into the microphone, and if someone hears you, they&#39;ll reply &quot;Yes.&quot;</p><p>In wireless networks, this is the difference between <strong>Passive Scanning</strong> and <strong>Active Scanning</strong>.</p><h4 id="passive-scanning" tabindex="-1">Passive Scanning <a class="header-anchor" href="#passive-scanning" aria-label="Permalink to &quot;Passive Scanning&quot;">​</a></h4><p>In certain frequency bands — especially the higher 5GHz bands (where radar is frequently present) — regulations prohibit you from transmitting arbitrarily. You can&#39;t just send a Probe Request right off the bat; that&#39;s illegal. You must keep your mouth shut and keep your ears open.</p><p>In <code>mac80211</code>, when the scan request flags include <code>IEEE80211_CHAN_PASSIVE_SCAN</code>, the driver enters this &quot;silent listening&quot; mode. The station hops from channel to channel (Channel Hopping), staying on each channel for a short while, waiting to receive Beacon frames. Because Beacons are the periodic &quot;heartbeats&quot; broadcast by an AP, as long as the AP is present, you will eventually hear it.</p><h4 id="active-scanning" tabindex="-1">Active Scanning <a class="header-anchor" href="#active-scanning" aria-label="Permalink to &quot;Active Scanning&quot;">​</a></h4><p>In ordinary 2.4GHz/5GHz bands like those in homes or offices, time is money. Waiting for Beacon periods is too slow, so we usually take the initiative.</p><p>The logic behind active scanning is simple, even a bit blunt:</p><ol><li>Hop to a channel.</li><li>Shout: &quot;Who is an AP?&quot; (Send a <strong>Probe Request</strong> frame).</li><li>Listen for a second to see who shouts back (Receive a <strong>Probe Response</strong>).</li><li>If nobody answers, switch to the next channel and repeat the process.</li></ol><p>In the kernel code, this sequence of actions is triggered by the <code>ieee80211_request_scan()</code> method. The actual &quot;shouting&quot; action is specifically carried out by <code>ieee80211_send_probe_req()</code>. This function constructs a management frame with its frame control subtype set to <code>IEEE80211_STYPE_PROBE_REQ</code>.</p><p>During this process, <strong>switching channels</strong> is a technical task. It&#39;s not as simple as changing a variable; you have to tell the hardware to adjust the radio frequency. This is done by calling the <code>ieee80211_hw_config()</code> method, passing in the parameter <code>IEEE80211_CONF_CHANGE_CHANNEL</code>.</p><p>⚠️ <strong>Here is an easily confused concept</strong> In upper-layer applications (like <code>wpa_supplicant</code>), we often say &quot;Channel 6,&quot; but in the eyes of hardware drivers, it only knows &quot;Frequency 2.437 GHz.&quot; This is a one-to-one mapping relationship. The kernel has a dedicated function, <code>ieee80211_channel_to_frequency()</code> (located in <code>net/wireless/util.c</code>), responsible for translating human-readable channel numbers into frequencies that the hardware understands. If you are writing a driver and directly stuff Channel 6 into a register, the hardware will just stare at you blankly.</p><hr><h3 id="_12-5-2-authentication-the-initial-handshake" tabindex="-1">12.5.2 Authentication: The Initial Handshake <a class="header-anchor" href="#_12-5-2-authentication-the-initial-handshake" aria-label="Permalink to &quot;12.5.2  Authentication: The Initial Handshake&quot;">​</a></h3><p>Now, suppose that through scanning, your client discovers an AP with a strong signal. It wants to connect to it. But before allowing it to send data, the 802.11 protocol requires a formal procedure: <strong>authentication</strong>.</p><p>Note that <strong>authentication is not encryption</strong>. The &quot;encryption key negotiation&quot; in modern WiFi networks (WPA2/WPA3) actually happens during the 4-Way Handshake phase. The &quot;authentication&quot; at the MLME level discussed here is more like &quot;knocking on the door to say hello.&quot;</p><p>In the kernel, the method responsible for this step is <code>ieee80211_send_auth()</code> (in <code>net/mac80211/util.c</code>). It sends a management frame with the subtype <code>IEEE80211_STYPE_AUTH</code>.</p><h4 id="open-system-authentication" tabindex="-1">Open System Authentication <a class="header-anchor" href="#open-system-authentication" aria-label="Permalink to &quot;Open System Authentication&quot;">​</a></h4><p>The original 802.11 standard defined two authentication algorithms. One of them, which is mandatory to support, is called <strong>Open System Authentication</strong>, corresponding to the macro <code>WLAN_AUTH_OPEN</code>.</p><p>This is arguably the most hypocritical security mechanism in the world. Its flow goes like this:</p><ol><li>Client: &quot;I want to authenticate.&quot;</li><li>AP: &quot;Granted.&quot; Yes, as long as you are a human (or a machine) sending a request, the AP will let you through. This is the so-called &quot;Null Authentication.&quot; Although its security value is exactly zero, it is a mandatory step in the protocol flow. Even if you are going to use WPA2 encryption later, you must first go through this &quot;Open System Authentication&quot; to advance the state machine.</li></ol><h4 id="shared-key-authentication" tabindex="-1">Shared Key Authentication <a class="header-anchor" href="#shared-key-authentication" aria-label="Permalink to &quot;Shared Key Authentication&quot;">​</a></h4><p>The other ancient method is <strong>Shared Key Authentication</strong>, corresponding to the macro <code>WLAN_AUTH_SHARED_KEY</code>. This involves WEP (Wired Equivalent Privacy), an encryption standard that has been proven to be not only terrible but also full of holes. The general flow is that the AP sends a challenge text, the client encrypts it back with the WEP key, and the AP verifies its correctness. Because WEP has been completely obsoleted, this authentication method in modern drivers is more of a &quot;I know it exists, but I try not to touch it.&quot;</p><hr><h3 id="_12-5-3-association-establishing-a-formal-connection" tabindex="-1">12.5.3 Association: Establishing a Formal Connection <a class="header-anchor" href="#_12-5-3-association-establishing-a-formal-connection" aria-label="Permalink to &quot;12.5.3  Association: Establishing a Formal Connection&quot;">​</a></h3><p>After authentication passes, the client and the AP can be considered &quot;acquainted.&quot; But to actually transmit data, the client must initiate <strong>Association</strong>.</p><p>You can think of &quot;authentication&quot; as shaking hands when meeting, and &quot;association&quot; as taking a seat and registering. Only after a successful association will the AP allocate resources for you, add you to its internal tables, and tell the switch that this MAC address is on its wireless port.</p><p>This step is completed by the <code>ieee80211_send_assoc()</code> method (in <code>net/mac80211/mlme.c</code>). It sends a management frame with the subtype <code>IEEE80211_STYPE_ASSOC_REQ</code>. This frame contains a lot of key information: which rates the client supports, which 802.11n/ac/ax features it supports, whether it wants to save power, and so on. Upon receiving this, the AP replies with a <code>Association Response</code>. If it carries a status code of <code>0</code>, it means success.</p><hr><h3 id="_12-5-4-roaming-switching-to-another-ap-and-continuing" tabindex="-1">12.5.4 Roaming: Switching to Another AP and Continuing <a class="header-anchor" href="#_12-5-4-roaming-switching-to-another-ap-and-continuing" aria-label="Permalink to &quot;12.5.4  Roaming: Switching to Another AP and Continuing&quot;">​</a></h3><p>As a user, you&#39;ve definitely encountered this: you&#39;re connected to Router A in the living room, you walk to the bedroom, the signal weakens, but the system automatically switches to Router B (or another band of the same router), and the network barely skips a beat.</p><p>This is <strong>Roaming</strong>.</p><p>Roaming essentially happens between two APs (belonging to the same ESS, i.e., the same SSID). When your client finds that the signal quality of the current AP has degraded to an intolerable level, or when another AP with a better signal appears nearby, it decides to &quot;jump ship.&quot;</p><p>This process is called <strong>Reassociation</strong>.</p><p>In the kernel, the <code>ieee80211_send_assoc()</code> function is reused. Why? Because &quot;association&quot; and &quot;reassociation&quot; share 99% of the same content — both need to send capability sets and wait for a reply. The only difference is that the frame subtype is <code>IEEE80211_STYPE_REASSOC_REQ</code>, and in this request frame, the client includes an extra piece of information: <strong>what the AP I was previously connected to is called (BSSID)</strong>.</p><p>This information is very important. It&#39;s just like telling your new employer: &quot;I just resigned from my previous employer, here is my resignation letter.&quot; The new AP will take this address, look it up in its tables, and finding that it&#39;s a sibling in the same network, will go to the old AP to synchronize your cached data (like the packets that weren&#39;t picked up while you were sleeping), achieving a seamless handover.</p><p>If all goes well, the new AP will reply with a <code>Reassociation Response</code> and conveniently assign a new <strong>AID (Association ID)</strong>. Once you get this AID, you have officially settled into this new territory.</p><hr><p>In this section, we pulled up a chair and watched a play unfold. We watched how a client scans channels like a radar, how it goes through &quot;open authentication&quot; completely unguarded, and how it officially takes a seat to associate. This is the daily routine of the MLME as a management layer — tedious, but indispensable.</p><p>Now, we have the foundation for data transmission (TX/RX), as well as lifecycle management (Power Save, MLME). Next, we will dive into the kernel implementation details of <code>mac80211</code> and look at the <strong>core data structures</strong> that support the entire wireless framework. If we don&#39;t look at what <code>ieee80211_hw</code> and <code>sta_info</code> look like, our understanding of the Linux wireless stack will always be obscured by a veil.</p>',48)])])}const g=t(i,[["render",s]]);export{p as __pageData,g as default};
