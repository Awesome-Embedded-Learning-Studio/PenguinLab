import{_ as e,o as i,c as a,a as t}from"./chunks/framework.8tiMeCK-.js";const k=JSON.parse('{"title":"Chapter 11: CPU Scheduler (Part 2)","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_programming/ch11.md","filePath":"en/notes/linux_kernel_programming/ch11.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_programming/ch11.md"};function r(l,s,o,h,p,d){return i(),a("div",null,[...s[0]||(s[0]=[t(`<h1 id="chapter-11-cpu-scheduler-part-2" tabindex="-1">Chapter 11: CPU Scheduler (Part 2) <a class="header-anchor" href="#chapter-11-cpu-scheduler-part-2" aria-label="Permalink to &quot;Chapter 11: CPU Scheduler (Part 2)&quot;">​</a></h1><blockquote><p><strong>Introduction: The Illusion of Control</strong></p><p>If you share a Linux server with nine other people, you might assume CPU time is fairly distributed—everyone gets an equal slice, no interference. But this is an illusion. The kernel&#39;s CFS scheduler tries its best, but &quot;completely fair&quot; only exists in a vacuum.</p><p>Reality is messier: if one person writes a program that frantically forks a bunch of child threads, each devouring CPU, that server effectively becomes their personal property, and for you, it might as well be frozen. The kernel needs a more powerful, more forceful mechanism to break this deadlock—it can&#39;t just &quot;try to schedule fairly,&quot; it must be able to &quot;hard-limit&quot; usage.</p><p>That&#39;s the story we&#39;re telling in this chapter: from pinning a thread to a specific CPU core (affinity), to locking a group of threads into a resource management cage, to transforming an ordinary Linux system into a hard real-time system. We&#39;re moving from &quot;letting the kernel decide&quot; to &quot;making the kernel obey.&quot;</p></blockquote><hr><p>This is our second journey into the topic of the Linux kernel CPU (or rather, task) scheduler. In the previous chapter (Chapter 10), we laid the groundwork: we figured out who the actual schedulable unit is (it&#39;s threads, not processes), saw through POSIX scheduling policies, and even used tools like <code>perf</code> to witness the scheduler in action. We also learned how to query a thread&#39;s scheduling policy and priority, and dug into the modular design inside the scheduler.</p><p>Now that we have these cards in hand, we can raise the stakes. In this chapter, we&#39;ll dive into the following topics:</p><ul><li><strong>Understanding, querying, and setting CPU affinity masks</strong></li><li><strong>Querying and setting thread scheduling policies and priorities</strong></li><li><strong>Introduction to cgroups (Control Groups)</strong></li><li><strong>Running Linux as an RTOS—A getting started guide</strong></li><li><strong>Other miscellaneous scheduling-related topics</strong></li></ul><p>This chapter builds directly on the previous one, so we strongly recommend that you finish Chapter 10 before turning this page.</p><hr><h2 id="technical-prerequisites" tabindex="-1">Technical Prerequisites <a class="header-anchor" href="#technical-prerequisites" aria-label="Permalink to &quot;Technical Prerequisites&quot;">​</a></h2><p>We assume you&#39;ve set up your kernel workspace (see the online chapters of this book for details) and have a virtual machine running Ubuntu 22.04 LTS (or newer, or the latest Fedora) with all the necessary packages installed. If you haven&#39;t done this yet, now is a good time.</p><p>Additionally, for the best experience, we recommend cloning this book&#39;s companion GitHub repository and following along with the code.</p><p>The repository URL is here: <code>https://github.com/PacktPublishing/Linux-Kernel-Programming_2E</code></p><hr><h2 id="understanding-querying-and-setting-cpu-affinity-masks" tabindex="-1">Understanding, Querying, and Setting CPU Affinity Masks <a class="header-anchor" href="#understanding-querying-and-setting-cpu-affinity-masks" aria-label="Permalink to &quot;Understanding, Querying, and Setting CPU Affinity Masks&quot;">​</a></h2><p><code>task_struct</code>—the root data structure for a thread (or task), packed with dozens of member variables—has several attributes directly tied to scheduling: priority (both nice values and real-time priority), the scheduling class structure pointer, the run queue the thread is on (if any), and so on. (By the way, we covered the details of <code>task_struct</code> back in Chapter 6 when we discussed processes and threads).</p><p>Among these attributes, one member is particularly important: the <strong>CPU affinity bitmask</strong> (the actual structure member is <code>cpumask_t *cpus_ptr</code>. Interestingly, prior to kernel 5.3, it was called <code>cpus_allowed</code>; it was later renamed in this commit: <code>https://github.com/torvalds/linux/commit/3bd3706251ee8ab67e69d9340ac2abdca217e733</code>).</p><p>As the name suggests, this bitmask is a string of bits used to indicate which CPU cores this thread (the entity represented by <code>task_struct</code>) is allowed to run on. A diagram makes this most intuitive: suppose we have an 8-core system, a typical CPU affinity mask might look like this:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>       7     6     5     4     3     2     1     0      &lt;- CPU 核心编号</span></span>
<span class="line"><span>       ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━</span></span>
<span class="line"><span>       0     0     1     1     1     1     1     1      &lt;- 亲和性位</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>In the example above, each cell represents a CPU core. The first row is the core number, and the row below shows the corresponding bit value: if it&#39;s 0, the thread cannot run on that core; if it&#39;s 1, it can.</p><p>So, if the mask value is <code>0x3f</code> (binary <code>0011 1111</code>), it means this thread can be scheduled on CPUs 0 through 5, but will never appear on cores 6 and 7.</p><p>By default, all mask bits are set to 1. In other words, by default, a thread can run wherever it wants. This makes sense—for example, on a box where the OS sees 8 cores, every living thread has a default CPU affinity mask of binary <code>1111 1111</code> (which is hexadecimal <code>0xff</code>).</p><p>Since this mask lives inside <code>task_struct</code>, it tells us: <strong>CPU affinity is per-thread</strong>. This is also easy to understand—after all, the schedulable entities in the Linux kernel are threads.</p><p>At runtime, the scheduler decides which core a thread ultimately lands on. If you think about it, this is implicit by design: each CPU core is associated with a run queue. Every runnable thread queues up in some CPU&#39;s run queue; thus it becomes eligible to run, and by default, it runs on the CPU represented by that queue.</p><p>Of course, the scheduler has a &quot;load balancing&quot; component that will migrate threads to other CPU cores (i.e., other run queues) when necessary (the kernel threads doing this work are called <code>migration/n</code>, where n is the core number).</p><p>The kernel exposes a set of APIs to user space (system calls, specifically <code>sched_{s,g}etaffinity(2)</code>, along with their pthread wrapper library functions) that allow applications to &quot;affinitize&quot; threads (or multiple threads) to specific CPU cores on demand.</p><p>We can do the same thing from kernel space, setting the affinity for any given kernel thread. For example, if you set the CPU affinity mask to binary <code>1000 0001</code> (which is hexadecimal <code>0x81</code>), it means this thread can only run on core 7 and core 0 (remember, core numbering starts at 0).</p><p><strong>（Insert Figure 11.1 image here: Diagram of CPU affinity bitmask）</strong></p><p>Although technically you can arbitrarily modify a thread&#39;s CPU affinity mask, we strongly advise against doing so carelessly. The kernel scheduler subsystem is well aware of the CPU topology (or domains) and can achieve the best system load balancing.</p><p>That being said, in certain specific scenarios, explicitly setting CPU affinity does have benefits:</p><ol><li><strong>Reducing cache invalidation</strong>: Ensuring a thread always runs on the same core can significantly reduce cache data bouncing, which is crucial for performance. (We&#39;ll dive into CPU caches in Chapter 13 when discussing kernel synchronization).</li><li><strong>Eliminating migration overhead</strong>: This directly removes the cost of a thread migrating back and forth between different cores.</li><li><strong>Implementing CPU isolation</strong>: This is a strategy that exclusively dedicates a core to a specific thread by explicitly preventing other threads from running on it. This trick is commonly used in time-sensitive real-time systems.</li></ol><p>The first two points usually only apply to extreme corner cases. The third point, CPU isolation, is typically a technique used in systems with extreme real-time requirements; while the cost is non-trivial, it&#39;s worth it in that context. (By the way, this used to be implemented via the <code>isolcpus=</code> kernel parameter; that&#39;s now deprecated, and everyone has switched to using the cpusets cgroup controller).</p><p>Now that we understand the theory, let&#39;s write a user-space C program to actually query and modify a thread&#39;s CPU affinity mask.</p><hr><h3 id="querying-and-setting-a-thread-s-cpu-affinity-mask" tabindex="-1">Querying and Setting a Thread&#39;s CPU Affinity Mask <a class="header-anchor" href="#querying-and-setting-a-thread-s-cpu-affinity-mask" aria-label="Permalink to &quot;Querying and Setting a Thread&#39;s CPU Affinity Mask&quot;">​</a></h3><p>Talk is cheap. We provide a small user-space C program to query and set the CPU affinity mask of a user-space process (actually a thread). Querying the mask relies on the <code>sched_getaffinity()</code> system call, and setting it relies on its counterpart—<code>sched_setaffinity()</code>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> _GNU_SOURCE</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;sched.h&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> sched_getaffinity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">pid_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> pid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">size_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> cpusetsize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cpu_set_t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">mask</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> sched_setaffinity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">pid_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> pid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">size_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> cpusetsize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> cpu_set_t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">mask</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>This uses a dedicated data type called <code>cpu_set_t</code>, which represents that CPU affinity bitmask (the third parameter of the two functions above). There&#39;s a catch to this: its size is dynamically allocated, depending on how many CPU cores the system actually has.</p><p>This CPU mask (of type <code>cpu_set_t</code>) must be initialized to zero before use. The <code>CPU_ZERO()</code> macro does exactly this (there are a few similar helper macros; we recommend checking the man page for <code>CPU_SET(3)</code>).</p><p>The second parameter of both system calls above is the size of the CPU mask (we just use sizeof). The first parameter is the PID of the target process (or thread)—whose internals you want to query or set.</p><p>Looking at the code alone might not give you a feel for it, so let&#39;s run the example code (available on GitHub: <code>ch11/cpu_affinity</code>).</p><p>Here&#39;s what it looks like running on a native Linux machine with 12 cores:</p><p><strong>（Insert Figure 11.2 image here: Demo program displaying the calling process&#39;s CPU affinity mask）</strong></p><p>In this example, we ran the program without arguments. In this mode, it queries its own CPU affinity mask. We print out the bits of the mask: as you can clearly see in the figure above (Figure 11.2), the output is binary <code>1111 1111 1111</code> (equal to hexadecimal <code>0xfff</code>). This shows that, by default, the process is eligible to run on all 12 cores of the system!</p><p>Internally, the program calls the <code>popen()</code> library API, which in turn calls the <code>nproc</code> utility to detect the number of cores. But note that <code>nproc</code> returns the number of cores <strong>available to the current process</strong>; this might be less than the actual number of cores (online and offline), though usually they are the same. There are several ways the number of available cores can change, the most &quot;orthodox&quot; being through the cgroup cpuset resource controller (we&#39;ll cover cgroups later in this chapter).</p><p>The core code for querying looks like this (source file is <code>ch11/cpu_affinity/userspc_cpuaffinity.c</code>):</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> query_cpu_affinity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">pid_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> pid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    cpu_set_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cpumask;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    CPU_ZERO</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cpumask);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sched_getaffinity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(pid, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">sizeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cpu_set_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cpumask) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        perror</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sched_getaffinity() failed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    disp_cpumask</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(pid, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cpumask, numcores);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p><code>disp_cpumask()</code> is responsible for drawing the bitmask (we&#39;ll leave that for you to look at yourself).</p><p>If you pass some arguments to this program—the first argument being the PID of the process (or thread), and the second being the CPU bitmask (in hexadecimal format)—it will attempt to change that process&#39;s affinity mask to the value you passed.</p><p>Of course, to modify someone else&#39;s mask, you must own that process, or have root privileges (more precisely, you need the <code>CAP_SYS_NICE</code> capability).</p><p>A quick demo: in Figure 11.3, <code>nproc</code> tells us there are 12 cores. Then, we run our program to query and set the shell (bash) process&#39;s CPU affinity mask. Suppose on this 12-core laptop, bash&#39;s affinity mask starts out as <code>0xfff</code> (binary <code>1111 1111 1111</code>), which is normal; then, we change it to <code>0xdae</code> (binary <code>1101 1010 1110</code>), and query it again to verify:</p><p><strong>（Insert Figure 11.3 image here: Demo program querying and setting bash&#39;s CPU affinity mask to 0xdae）</strong></p><p>This gets interesting. First, the program correctly detected that the number of available cores is 12. Then it queried the (default) mask of the bash process (we passed its PID as the first argument), showing <code>0xfff</code>, flawlessly.</p><p>Immediately after, because we passed a second argument—the mask value we wanted to set (<code>0xdae</code>)—the program did exactly that, setting bash&#39;s mask to <code>0xdae</code>.</p><p>Now here&#39;s the catch: the terminal window we&#39;re currently in <em>is</em> this bash process. If you run <code>nproc</code> again now, you&#39;ll find it shows 8 instead of 12! This makes perfect sense: the bash process can now only see 8 CPU cores. (Because our program didn&#39;t restore the mask when it exited).</p><p>The relevant code for setting the CPU affinity mask is as follows:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ch11/cpu_affinity/userspc_cpuaffinity.c</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> set_cpu_affinity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">pid_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> pid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> bitmask</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    cpu_set_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cpumask;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    printf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Setting CPU affinity mask for PID </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> now...</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, pid);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    CPU_ZERO</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cpumask);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 遍历给定的位掩码，按需设置 CPU 位 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (i</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; i</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;sizeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; i</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">++</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        /* printf(&quot;bit %d: %d\\n&quot;, i, (bitmask &gt;&gt; i) &amp; 1); */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ((bitmask </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">            CPU_SET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(i, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cpumask);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sched_setaffinity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(pid, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">sizeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cpu_set_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cpumask) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        perror</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sched_setaffinity() failed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    disp_cpumask</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(pid, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cpumask, numcores);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br><span class="line-number">24</span><br></div></div><p>In the code snippet above, you can see we first set up the bitmask of <code>cpu_set_t</code> (by looping through each bit; as you know, the expression <code>(bitmask &gt;&gt; i) &amp; 1</code> is used to test if bit i is 1), and then call the <code>sched_setaffinity()</code> system call to set the new mask for the specified pid.</p><p><strong>（Insert image here: Tip icon）</strong></p><p><strong>⚠️ Note</strong> Something very important, and entirely correct: <strong>anyone can query a task&#39;s CPU affinity mask</strong>, but <strong>if you don&#39;t own that task, don&#39;t have root privileges, or lack the CAP_SYS_NICE capability, you cannot set it</strong>.</p><hr><h3 id="using-the-taskset-tool-for-cpu-affinity" tabindex="-1">Using the taskset Tool for CPU Affinity <a class="header-anchor" href="#using-the-taskset-tool-for-cpu-affinity" aria-label="Permalink to &quot;Using the taskset Tool for CPU Affinity&quot;">​</a></h3><p>Just as we used the convenient user-space tool <code>chrt</code> in the previous chapter to query (or set) a process&#39;s (or thread&#39;s) scheduling policy and priority, you can also use the <code>taskset</code> user-space tool to query or modify a given process&#39;s (or thread&#39;s) CPU affinity mask.</p><p>Here are two simple examples; note that these examples are run on an x86_64 Linux virtual machine with 6 cores:</p><ol><li><strong>Querying the CPU affinity mask of systemd (PID 1):</strong></li></ol><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> taskset</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pid</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;s current affinity mask: 3f</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">$</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Think about it: <code>0x3f</code> in binary is <code>0011 1111</code>, which represents that the process/thread (systemd in this case) can run on all 6 cores.</p><ol start="2"><li><strong>Running the compiler under taskset&#39;s protection, ensuring GCC—and its child processes (assembler and linker)—only run on the first two cores.</strong></li></ol><p>The first argument to <code>taskset</code> is the CPU affinity mask (<code>03</code> is binary <code>0011</code>):</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> taskset</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 03</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gcc</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> userspc_cpuaffinity.c</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -o</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> userspc_cpuaffinity</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -Wall</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Done.</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Done. For full usage details, check the man page for <code>taskset(1)</code>. (By the way, as mentioned in the previous chapter, the <code>schedtool(8)</code> tool can also handle setting a given thread/process&#39;s CPU affinity bitmask on the fly).</p><hr><h3 id="setting-cpu-affinity-masks-on-kernel-threads" tabindex="-1">Setting CPU Affinity Masks on Kernel Threads <a class="header-anchor" href="#setting-cpu-affinity-masks-on-kernel-threads" aria-label="Permalink to &quot;Setting CPU Affinity Masks on Kernel Threads&quot;">​</a></h3><p>As an interesting example, suppose we want to demonstrate a synchronization technique called &quot;Per-CPU variables&quot; (we will indeed learn about this in Chapter 13, and get hands-on in the &quot;Per-CPU—A Kernel Module Example&quot; section). We need to create two kernel threads (kthreads) and ensure they run on different CPU cores.</p><p>To do this, we must explicitly set the CPU affinity masks of these two kernel threads to be different and non-overlapping (for simplicity, we&#39;ll set the first kthread&#39;s mask to 0 (core 0 only), and the second to 1 (core 1 only), guaranteeing they run on cores 0 and 1 respectively).</p><p>But there&#39;s a catch... we&#39;ll get into that in the next section.</p><hr><h3 id="working-around-unexported-symbol-availability" tabindex="-1">Working Around Unexported Symbol Availability <a class="header-anchor" href="#working-around-unexported-symbol-availability" aria-label="Permalink to &quot;Working Around Unexported Symbol Availability&quot;">​</a></h3><p>The problem is that setting CPU affinity from within a module right now is, frankly, a hack. We&#39;ll show it here, but we absolutely do not recommend it for production environments.</p><p>The reason is that the kernel API we need to set the CPU affinity mask—<code>sched_setaffinity()</code>—exists, but it is <strong>unexported</strong>. As we learned in the earlier chapters on writing modules, an out-of-tree module (like ours) can only call exported functions (and data). So what do we do?</p><p>For many years (I did this in the first edition of this book too!), the &quot;standard&quot; approach used by module developers was to call an existing convenience routine, <code>kallsyms_lookup_name()</code>, to look up any given symbol in the kernel and get its (kernel virtual) address.</p><p>Once you have the address, any decent C programmer can use it as a function pointer and call it however they want. This amounts to &quot;cracking&quot; the restriction—the restriction of only being able to call exported functions! (It&#39;s a slick trick, but kernel veterans would probably frown upon seeing this.)</p><p>Indeed, but starting with kernel version 5.7, the community decided it was time to stop this (foolish) abuse and simply <strong>unexported</strong> <code>kallsyms_lookup_name()</code> (and the similar <code>kallsyms_on_each_symbol()</code>)! (The short commit ID is <code>0bd476e6c671</code>; you can take a look).</p><p>So now what? Don&#39;t panic, as long as you have root privileges, we can always find the address of any kernel symbol via the <code>/proc/kallsyms</code> pseudo-file (this is also for security reasons). Moreover, modern kernels usually have Kernel Address Space Layout Randomization (KASLR) enabled, meaning this value changes on every boot and can&#39;t be hardcoded (which is also good for security).</p><p>So, we wrote a small wrapper script to do this (the code is here: <code>ch13/3_lockfree/percpu/run</code>; yes, this code actually belongs to Chapter 13), and then pass the retrieved address (the address of <code>sched_setaffinity()</code> found via <code>/proc/kallsyms</code>) as a parameter to the module (<code>ch13/3_lockfree/percpu/percpu_var.c</code>).</p><p>Once the module receives the address, it uses it as a function pointer and can successfully call it. Phew!</p><p>The function signature of <code>sched_setaffinity()</code> looks like this:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">long</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> sched_setaffinity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">pid_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> pid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cpumask </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">new_mask</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Here is a small piece of the key code—we use the passed-in (via a module parameter named <code>func_ptr</code>) <code>sched_setaffinity()</code> function pointer to set our desired CPU mask:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ch13/3_lockfree/percpu/percpu_var.c</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[ … ]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> func_ptr;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">module_param</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(func_ptr, ulong, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">schedsa_ptr)(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">pid_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cpumask </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[ … ]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 设置函数指针</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">schedsa_ptr </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">pid_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> pid, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cpumask </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))func_ptr;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[ … ]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * !HACK! sched_setaffinity() 没导出，我们不能直接调它。</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * 所以我们通过函数指针来Invoke它</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ret </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">schedsa_ptr)(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mask);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 0 =&gt; 针对自己</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[ … ]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br></div></div><p>Frankly, this method of &quot;cracking&quot; kernel addresses is unconventional and somewhat controversial, and rather crude from an engineering standpoint, but it does work—especially in demo and experimental scenarios. However, one thing must be kept in mind: this hack is built on unexported interfaces and is a &quot;wild&quot; approach. In serious production environments, or when your code needs long-term maintenance, please avoid relying on such fragile tricks. There&#39;s usually a reason the kernel didn&#39;t export a function, and bypassing these restrictions is like walking a tightrope—you might reach the end, but you do so at your own risk.</p><p>Alright, enough with this &quot;hacker&quot; stuff; let&#39;s get back on track. Now that you know how to query/modify a (kernel) thread&#39;s CPU affinity mask, let&#39;s move to the next logical step: how to programmatically query/modify a thread&#39;s scheduling policy and priority! We&#39;ll dive into the details in the next section.</p><hr><h2 id="querying-and-setting-thread-scheduling-policies-and-priorities" tabindex="-1">Querying and Setting Thread Scheduling Policies and Priorities <a class="header-anchor" href="#querying-and-setting-thread-scheduling-policies-and-priorities" aria-label="Permalink to &quot;Querying and Setting Thread Scheduling Policies and Priorities&quot;">​</a></h2><p>In the &quot;Thread Priorities&quot; section of Chapter 10 (CPU Scheduler—Part 1), you learned how to query the scheduling policy and priority of any given thread using the <code>chrt</code> tool (we even demonstrated a simple Bash script to do this). We mentioned there that <code>chrt</code> internally calls the <code>sched_getattr()</code> system call.</p><p>Very similarly, setting the scheduling policy and priority can be done via the <code>chrt</code> tool (convenient for use in scripts, for example), or via the <code>sched_setattr()</code> system call in a (user-space) C application. Additionally, the kernel exposes other APIs: <code>sched_{g,s}etscheduler()</code> and their pthread library wrapper APIs <code>pthread_{g,s}etschedparam()</code> (since these are all user-space APIs, we&#39;ll leave the specific details and usage to you to explore in their man pages).</p><hr><h3 id="setting-policies-and-priorities-from-within-the-kernel—for-kernel-threads" tabindex="-1">Setting Policies and Priorities from Within the Kernel—For Kernel Threads <a class="header-anchor" href="#setting-policies-and-priorities-from-within-the-kernel—for-kernel-threads" aria-label="Permalink to &quot;Setting Policies and Priorities from Within the Kernel—For Kernel Threads&quot;">​</a></h3><p>As you know, the kernel itself is neither a process nor a thread. That said, the Linux kernel does support multithreading, and it does have threads, known as <strong>kernel threads</strong> (kthreads). Like their user-space counterparts, kernel threads can be created on demand (by the core kernel, device drivers, or kernel modules; the kernel exposes APIs for this).</p><p>They are schedulable entities (KSEs, after all!), and of course, each kernel thread has its own <code>task_struct</code> and kernel-mode stack; therefore, just like regular threads, they compete for CPU resources, and their scheduling policies and priorities can also be queried or set programmatically as needed.</p><p><strong>（Insert image here: Linux Kernel Programming Part 2 free ebook ad）</strong></p><p><strong>（Insert image here: Link about kthread naming）</strong></p><p>Which brings us to the point: in user space, the modern recommended system calls for querying and setting thread scheduling attributes are <code>sched_getattr()</code> and <code>sched_setattr()</code>, respectively. In earlier years, the <code>sched_{g|s}et_scheduler()</code> pair was used.</p><p>The current <code>sched_{g|s}etattr()</code> system call takes a pointer to a <code>struct sched_attr</code>, which contains all the details you might need; check the man page for specifics (<code>https://man7.org/linux/man-pages/man2/sched_setattr.2.html</code>).</p><p>So, following the modern approach, one might assume we&#39;d use the kernel implementations of these system calls to do similar work inside the kernel. Not so fast; the kernel community considered the old design—which allowed users (applications) and module developers to happily call these APIs, casually fill in a SCHED_FIFO policy, and conveniently supply a (real-time) priority they thought was reasonable—<strong>fundamentally flawed</strong>.</p><p>Why? Because it easily leads to disaster: for example, two or more SCHED_FIFO threads with the same priority, or using &quot;randomly&quot; chosen priority values—picked without any real thought. This directly messes up CPU scheduling, which in turn messes up resource management.</p><p>Therefore, starting with kernel 5.9, the community made the following changes (allow me to quote the commit directly, as it&#39;s the best way to convey the message); this is part of commit <code>https://github.com/torvalds/linux/commit/7318d4cc14c8c8a5dde2b0b72ea50fd2545f0b7a</code>:</p><blockquote><p>...</p><p>Therefore exposing the priority field is pointless; the kernel is incapable of setting a sensible value, as it lacks the system knowledge required to do so.</p><p>Take <code>sched_setschedule() / sched_setattr()</code> away from modules and replace it with:</p><ul><li><code>sched_set_fifo(p);</code> to create a FIFO task (priority 50)</li><li><code>sched_set_fifo_low(p);</code> to create a task that is higher than NORMAL, which ends up being a FIFO task with priority 1.</li><li><code>sched_set_normal(p, nice);</code> to (re)set the task to normal.</li></ul><p>This prevents the proliferation of randomly chosen, meaningless priorities, which don&#39;t serve any real purpose anyway.</p><p>The system administrator/integrator, the people who have insight into the actual system design and requirements (userspace), can set appropriate priorities when required...</p><p>...</p></blockquote><p>Aha; so, dear module authors, we now have to use these three APIs when setting up FIFO threads in the kernel—<code>sched_set_fifo()</code>, <code>sched_set_fifo_low()</code>, and <code>sched_set_normal()</code>.</p><p>As the commit above says, we trust administrators and/or user-space developers to write user programs and provide correct, meaningful real-time priority values; the kernel (or modules) shouldn&#39;t question these decisions—it just executes them (again, this is the &quot;provide mechanism, not policy&quot; design principle at work).</p><p>The first two APIs:</p><ul><li>Are wrappers around the kernel&#39;s <code>sched_setscheduler_nocheck()</code> function;</li><li>Set the thread&#39;s scheduling policy to <code>SCHED_FIFO</code>;</li><li>Set the thread&#39;s (real-time) priority to <code>MAX_RT_PRIO/2</code> (i.e., 50) and 1, respectively.</li></ul><p>And <code>sched_set_normal()</code>:</p><ul><li>Is a wrapper around <code>sched_setattr_nocheck()</code>;</li><li>Sets the thread&#39;s scheduling policy to <code>SCHED_NORMAL</code> (same as <code>SCHED_OTHER</code>, meaning non-real-time, driven by the Completely Fair Scheduler (CFS));</li><li>Sets the thread&#39;s nice value to the second parameter.</li></ul><p><strong>（Insert image here: Note about _nocheck）</strong></p><p>Here, the <code>*_nocheck()</code> suffix means the kernel doesn&#39;t even bother checking whether the process context running these APIs has sufficient permissions; it just lets it through. (See the comment here: <code>https://elixir.bootlin.com/linux/v6.1.25/source/kernel/sched/core.c#L7742</code>).</p><p>Additionally, all three of these APIs are GPL-exported, meaning only modules released under the GNU GPL license can use them.</p><hr><h3 id="a-real-world-example—threaded-interrupt-handlers" tabindex="-1">A Real-World Example—Threaded Interrupt Handlers <a class="header-anchor" href="#a-real-world-example—threaded-interrupt-handlers" aria-label="Permalink to &quot;A Real-World Example—Threaded Interrupt Handlers&quot;">​</a></h3><p>A classic use case where the kernel uses kernel threads—which is actually very common—is <strong>Threaded Interrupts</strong> (work queues are another example). In this case, the kernel must create a dedicated kernel thread and set it to the <code>SCHED_FIFO</code> (soft) real-time scheduling policy with a real-time priority of 50 (a middle-ground value) to properly handle the so-called threaded interrupt.</p><p>Let&#39;s look at the relevant code path: <code>https://elixir.bootlin.com/linux/v6.1.25/source/kernel/irq/manage.c#L1448</code></p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setup_irq_thread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> irqaction </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> irq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">bool</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> secondary</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> task_struct </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">t;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">secondary) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        t </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> kthread_create</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(irq_thread, new, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;irq/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, irq, new-&gt;name);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        t </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> kthread_create</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(irq_thread, new, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;irq/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">-s-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, irq, new-&gt;name);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    [ … ]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><p>The <code>kthread_create()</code> macro is responsible for creating the kernel thread. Now, the kernel-specific API <code>irq_thread()</code> (called as the thread function via the <code>kthread_create()</code> macro) will, during its execution, set the scheduling policy and priority correctly. The code path is here: <code>https://elixir.bootlin.com/linux/v6.1.25/source/kernel/irq/manage.c#L1286</code></p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* Interrupt handler thread */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> irq_thread</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> callback_head on_exit_work;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> irqaction </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">action </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> data;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    [ ... ]</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    sched_set_fifo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(current);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    [ ... ]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>See that! Notice the call to <code>sched_set_fifo()</code>; as we&#39;ve seen, it sets this kernel thread (the caller, referenced by <code>current</code>) to use the <code>SCHED_FIFO</code> policy with a priority of 50. Done.</p><p><strong>（Insert image here: Tip about why IRQ threads use FIFO 50）</strong></p><p>Understanding scheduling isn&#39;t enough—what if one of those ten people launches a fork bomb? Or what if you just want to gracefully limit the resource usage of a compilation task? That&#39;s when we need to bring out the heavy artillery—<strong>cgroups</strong>. We&#39;ll get to that in the next section.</p><hr><h2 id="introduction-to-cgroups-control-groups" tabindex="-1">Introduction to cgroups (Control Groups) <a class="header-anchor" href="#introduction-to-cgroups-control-groups" aria-label="Permalink to &quot;Introduction to cgroups (Control Groups)&quot;">​</a></h2><p>In the distant past, the kernel community wrestled with a rather tricky problem: although scheduling algorithms and their implementations—the early 2.6.0 O(1) scheduler, and the slightly later (2.6.23) Completely Fair Scheduler (CFS)—promised so-called &quot;completely fair&quot; scheduling, this wasn&#39;t truly &quot;completely fair&quot; in any real sense!</p><p>Think about it for a moment: suppose you and 9 other people are logged into a Linux server at the same time. All else being equal, CPU time would be distributed (more or less) fairly among the ten of you; of course, you also know that it&#39;s not &quot;people&quot; that actually run on the processor and consume memory, but the processes and threads running on your behalf.</p><p>Let&#39;s assume for now that it&#39;s still (roughly) fair. But suppose you—one of those ten people—wrote a user-space program that recklessly forks a bunch of new threads in a loop, each frantically consuming CPU (perhaps generously allocating memory on the side)! The CPU bandwidth allocation (even via CFS) would no longer be truly fair; your account would effectively monopolize the CPU (and perhaps other system resources, like memory and I/O)!</p><p>What was urgently needed was a general-purpose solution that could precisely and effectively manage CPU (and other resource) bandwidth, throttling it (preventing further use) once a specified limit was reached.</p><p>Many patch proposals were submitted at the time, discussed, and then tossed into the trash. Eventually, engineers from Google, IBM, and other companies stepped up and submitted a set of patches that brought the modern version of the control groups (cgroups) solution into the Linux kernel (this traces back to version 2.6.24, October 2007. The original conception and implementation were done by Google&#39;s Paul Menage and Rohit Seth in 2006).</p><p>Simply put, <strong>cgroups</strong> is a kernel feature that allows system administrators (or anyone with root privileges) to elegantly perform bandwidth allocation and fine-grained resource management for various system resources or <strong>controllers</strong> (as they are called in cgroup terminology).</p><p>Note: using cgroups isn&#39;t just for managing processors (CPU bandwidth); it can also manage memory and block I/O bandwidth (and much more), allowing you to partition, allocate, and monitor them with precision based on your project or product needs.</p><p>Returning to our earlier example—ten people on a Linux system—if all processes are placed in the same cgroup and the CPU controller is enabled for that cgroup, it will actually achieve fair CPU distribution when facing CPU contention!</p><p>Alternatively, as a system administrator, you can get fancier: you can slice the system into several cgroups—one for compiling projects (like a Yocto build), one for the web browser, one for virtual machines, and so on—and then fine-tune and allocate resources (CPU, memory, I/O) to each cgroup as needed!</p><p>In fact, almost all modern distributions do exactly this, thanks to the powerful systemd framework (we&#39;ll cover this in detail shortly); embedded Linux typically does this too, including Android.</p><p>Alright, now you&#39;re interested! How do you enable this cgroups feature? Simple—it&#39;s a kernel feature that you can enable (or disable) in the usual way when configuring the kernel: <code>make menuconfig</code>, and then enter <code>General setup | Control Group support</code>.</p><p>You can try grepping your kernel config file for <code>CGROUP</code>; if needed, modify the config, rebuild the kernel, reboot with the new kernel, and test it. (We covered kernel configuration and building in detail in Chapters 2 and 3).</p><p><strong>（Insert image here: Tip that cgroups are enabled by default under systemd）</strong></p><p>The good news is: on any modern Linux system running the systemd init framework, cgroups are enabled by default. As just mentioned, you can check which cgroup controllers are enabled by grepping the kernel config file, and modify them as needed; on desktop and server-grade systems, you usually don&#39;t need to touch this.</p><p>Since its debut in 2.6.24, cgroups has evolved just like all other kernel features. Recently, the improved cgroup feature has become incompatible with the old version, leading to a new cgroup design and release called <strong>cgroups v2</strong> (or simply cgroups2—the maintainer is Tejun Heo); it was declared production-ready in the 4.5 kernel series (the old one is now referred to as cgroups v1, or the legacy cgroup implementation).</p><p>Note that, as of this writing, both versions coexist, albeit with some limitations; many applications and frameworks still use the old cgroups v1 and haven&#39;t migrated to v2. But this is changing; even if it&#39;s not fully rolled out yet, cgroups2 will become the de facto standard, so you should plan to use it.</p><p>In our coverage for this chapter, we&#39;ll focus almost exclusively on using the modern version, cgroups v2. The best documentation is the official kernel documentation; here it is for version 6.1: <code>https://www.kernel.org/doc/html/v6.1/admin-guide/cgroup-v2.html</code>. (By the way, the documentation for the latest kernel version is always here: <code>https://docs.kernel.org/admin-guide/cgroup-v2.html</code>).</p><p><strong>（Insert image here: Doc links about v1 vs v2）</strong></p><hr><h3 id="cgroup-controllers" tabindex="-1">cgroup Controllers <a class="header-anchor" href="#cgroup-controllers" aria-label="Permalink to &quot;cgroup Controllers&quot;">​</a></h3><p>A <strong>cgroup controller</strong> is the underlying kernel component responsible for distributing a specific resource (such as CPU cycles, memory, and I/O bandwidth, etc.) within a cgroup hierarchy (a cgroup and its descendants). You can think of it as a kind of &quot;resource limiter&quot; for a given cgroup hierarchy.</p><p>The man page for <code>cgroups(7)</code> details the interfaces for the various available (resource) controllers (sometimes called subsystems). The controllers typically available under cgroups v2 are as follows (Table 11.1 shows the v2 stuff; the original v1 implementations of many controllers trace back to 2.6.24):</p><table tabindex="0"><thead><tr><th style="text-align:left;"><strong>Cgroups v2 Controller Name</strong></th><th style="text-align:left;"><strong>What it Controls (or Limits or Regulates)</strong></th><th style="text-align:left;"><strong>Kernel Version When Enabled</strong></th></tr></thead><tbody><tr><td style="text-align:left;"><strong>cpu</strong></td><td style="text-align:left;">CPU bandwidth (cycles)</td><td style="text-align:left;">4.15</td></tr><tr><td style="text-align:left;"><strong>cpuset</strong></td><td style="text-align:left;">CPU affinity and memory node placement (especially useful for large NUMA systems)</td><td style="text-align:left;">5.0</td></tr><tr><td style="text-align:left;"><strong>memory</strong></td><td style="text-align:left;">Memory (RAM) usage</td><td style="text-align:left;">4.5</td></tr><tr><td style="text-align:left;"><strong>io</strong></td><td style="text-align:left;">I/O resource allocation</td><td style="text-align:left;">4.5</td></tr><tr><td style="text-align:left;"><strong>pids</strong></td><td style="text-align:left;">Hard limit on the number of processes in a cgroup</td><td style="text-align:left;">4.5</td></tr><tr><td style="text-align:left;"><strong>devices</strong></td><td style="text-align:left;">Creation and access of device files (only via cgroup BPF programs)</td><td style="text-align:left;">4.15</td></tr><tr><td style="text-align:left;"><strong>rdma</strong></td><td style="text-align:left;">Allocation and accounting of Remote Direct Memory Access (RDMA) resources</td><td style="text-align:left;">4.11</td></tr><tr><td style="text-align:left;"><strong>hugetlb</strong></td><td style="text-align:left;">Limits HugeTLB (large page) usage per cgroup</td><td style="text-align:left;">5.6</td></tr><tr><td style="text-align:left;"><strong>misc</strong></td><td style="text-align:left;">Various miscellaneous items; see documentation</td><td style="text-align:left;">5.13</td></tr></tbody></table><p><em>Table 11.1: Summary of cgroups v2 controllers available on a modern Linux system</em></p><p>We recommend interested readers to read the official kernel documentation and man pages mentioned above for details; for example, the <strong>PIDS controller</strong> is very useful for preventing fork bombs, as it allows you to limit how many processes a cgroup or its descendants can fork. (A fork bomb is a stupid but deadly DoS attack that frantically calls the <code>fork()</code> system call in an infinite loop!)</p><p>Next, a very important point: how are kernel cgroups exposed (or interfaced) to user space? Ah, it&#39;s the old Linux trick: control groups are exposed through a specially constructed <strong>synthetic or pseudo-filesystem</strong>! This is the cgroup filesystem, typically mounted at <code>/sys/fs/cgroup</code>.</p><p>In cgroups v2, the filesystem type is now called <code>cgroup2</code> (you can simply run <code>mount | grep cgroup</code> to see it). There&#39;s a lot of good stuff to explore in there; we&#39;ll see more as we continue...</p><p>Let&#39;s start with this question: how do I know which controllers are enabled on my system (actually, in the kernel)? Simple:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /sys/fs/cgroup/cgroup.controllers</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cpuset</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cpu</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> io</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> memory</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> hugetlb</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pids</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> rdma</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> misc</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Obviously, it displays a space-separated list of available controllers (I ran this command on an x86_64 Fedora 38 VM. Also note that using <code>/proc/cgroups</code> to peek at controllers is cgroups v1 compatible; don&#39;t expect it to work for v2). The exact controllers you see here depend on how the kernel is configured.</p><p>In cgroups v2, all controllers are mounted in a <strong>single hierarchy (or tree)</strong>. This differs from cgroups v1, which allowed multiple controllers to be hung under multiple hierarchies or groups.</p><p>The modern init framework <strong>systemd</strong> uses both v1 and v2 cgroups. In fact, it&#39;s systemd that automatically mounts the cgroups v2 filesystem at boot (right at <code>/sys/fs/cgroup/</code>).</p><hr><h3 id="exploring-the-cgroups-v2-hierarchy" tabindex="-1">Exploring the cgroups v2 Hierarchy <a class="header-anchor" href="#exploring-the-cgroups-v2-hierarchy" aria-label="Permalink to &quot;Exploring the cgroups v2 Hierarchy&quot;">​</a></h3><p>Looking under the mount point of the cgroups (v2) pseudo-filesystem—always <code>/sys/fs/cgroup</code> by default—staring at all those pseudo-files (and folders), you might feel a bit overwhelmed (go ahead, take a peek at Figure 11.4); in this section, we&#39;ll explore some of the more interesting and useful nooks and crannies!</p><p>First, let&#39;s confirm where the cgroups v2 hierarchy is mounted:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mount</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cgroup2</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cgroup2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> on</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /sys/fs/cgroup</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> type</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cgroup2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (rw,nosuid,nodev,noexec,relatime,nsdelegate,memory_recursiveprot)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Clearly, as expected, under <code>/sys/fs/cgroup</code>. (Curious about what that bunch of mount options in the parentheses means? The documentation is here: <code>https://www.kernel.org/doc/html/v6.1/admin-guide/cgroup-v2.html#mounting</code>).</p><p><strong>（Insert image here: Tip that older distros might not have controllers）</strong></p><p>Alright, let&#39;s start exploring!</p><p>In this segment, I&#39;m working on an x86_64 Fedora 38 virtual machine, and I&#39;ve built and booted a custom 6.1.25 kernel. Let&#39;s first get the big picture:</p><p><strong>（Insert Figure 11.4 image here: Root directory of the cgroups v2 hierarchy）</strong></p><p>At the root cgroup location—<code>/sys/fs/cgroup</code>—you can see several files and folders (needless to say, these are volatile pseudo-file objects; mounted in RAM via sysfs).</p><p>First:</p><ul><li>The &quot;regular&quot; files you see—like <code>cgroup.controllers</code>, <code>cpu.pressure</code>, and so on—are cgroup2 <strong>interface files</strong>.</li><li>These files are further divided into <strong>core interfaces</strong> and <strong>controller interfaces</strong>; all <code>cgroup.*</code> files are core interface files, <code>cpu.*</code> is a CPU controller interface file, <code>memory.*</code> is for the memory controller, and so on.</li><li>The folders you see represent—finally!—<strong>control groups</strong> (cgroups)! Among these many folders, you&#39;ll find that not all of them are constrained (or rather, not all of them are &quot;populated&quot;). You might wonder who created them; the short answer (at least for the ones that exist by default) is systemd; we&#39;ll cover this in detail shortly.</li></ul><hr><h4 id="enabling-or-disabling-controllers" tabindex="-1">Enabling or Disabling Controllers <a class="header-anchor" href="#enabling-or-disabling-controllers" aria-label="Permalink to &quot;Enabling or Disabling Controllers&quot;">​</a></h4><p>Let&#39;s first look at a key core interface file: <code>cgroup.controllers</code>. We briefly mentioned this in the previous section. Its contents are the list of controllers available to this cgroup; for the root cgroup, this means which controllers are configured in the kernel. As we just saw, for modern distributions, it&#39;s typically something like this: <code>cpuset cpu io memory hugetlb pids rdma misc</code>.</p><p>Be careful here: a controller appearing in this list doesn&#39;t mean it&#39;s already <strong>enabled</strong> in this cgroup hierarchy; in fact, by default, none of them are enabled!</p><p>Enabling a controller means that the constraints on its target resource distribution will take effect on <strong>direct children</strong>. To enable a controller, you write the string <code>+&lt;controller-name&gt;</code> to the <code>cgroup.subtree_control</code> pseudo-file (conversely, write <code>-&lt;controller-name&gt;</code> to disable it).</p><p>So, for example, to enable the CPU and I/O controllers but disable the memory controller (on the current cgroup, thus letting it take effect in its descendants, i.e., the levels below), you would do this (with root privileges):</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;+cpu +io -memory&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cgroup.subtree_control</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>So now we know that the <code>cgroup.subtree_control</code> file produces a space-separated list of controllers that are enabled for resource distribution from the current cgroup down to its children.</p><p>The kernel documentation puts it this way (<code>https://docs.kernel.org/admin-guide/cgroup-v2.html</code>):</p><blockquote><p><strong>Top-down Constraint</strong> Resources are distributed top-down and a cgroup can only distribute resources</p></blockquote>`,183)])])}const u=e(n,[["render",r]]);export{k as __pageData,u as default};
