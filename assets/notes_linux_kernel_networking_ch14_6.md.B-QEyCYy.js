import{_ as o,o as l,c as e,a as i}from"./chunks/framework.8tiMeCK-.js";const g=JSON.parse('{"title":"14.6  Busy Poll Sockets","description":"","frontmatter":{},"headers":[],"relativePath":"notes/linux_kernel_networking/ch14_6.md","filePath":"notes/linux_kernel_networking/ch14_6.md","lastUpdated":null}'),t={name:"notes/linux_kernel_networking/ch14_6.md"};function n(a,s,r,p,c,h){return l(),e("div",null,[...s[0]||(s[0]=[i(`<h1 id="_14-6-busy-poll-sockets" tabindex="-1">14.6 Busy Poll Sockets <a class="header-anchor" href="#_14-6-busy-poll-sockets" aria-label="Permalink to &quot;14.6  Busy Poll Sockets&quot;">​</a></h1><p>上一节我们聊完了 Cgroups 和 Namespaces，这两个是容器的基石。现在我们把目光从「隔离」移开，聚焦到一个极致性能的话题上。</p><p>想象你正在编写一个高频交易系统，或者一个需要纳秒级响应的边缘网关。你的代码跑在 Xeon 上，网卡是顶级的万兆卡，一切看起来都很完美。但当你盯着火焰图看时，你会发现有一块红色的区域让你如鲠在喉——<strong>上下文切换</strong>。</p><p>每一次数据包到达，网卡都要发起中断，CPU 被迫停下手中的活，去执行中断处理程序，然后调度你的应用醒来。这一套流程在现代 Linux 里已经优化到了极致，但对于某些极端场景来说，它还是太「重」了。</p><p>能不能彻底跳过中断和调度？</p><p>这就是本节要讲的 <strong>Busy Poll Sockets</strong>（忙轮询套接字）。它本质上是一种「以 CPU 换时间」的暴力美学。</p><hr><h3 id="传统的困境-睡觉与唤醒" tabindex="-1">传统的困境：睡觉与唤醒 <a class="header-anchor" href="#传统的困境-睡觉与唤醒" aria-label="Permalink to &quot;传统的困境：睡觉与唤醒&quot;">​</a></h3><p>在传统的网络栈模型里，当应用去读取一个 Socket 时，如果接收队列是空的，内核会怎么做？</p><p>如果是阻塞模式，应用会<strong>去睡觉</strong>。它把自己挂起，释放 CPU 给其他进程。此时，应用处于等待状态。</p><p>这很优雅，对吧？没数据就睡，不浪费 CPU。但代价是：<strong>延迟</strong>。</p><ol><li><strong>应用</strong>发现没数据，调用 <code>schedule()</code> 进入睡眠。</li><li><strong>网卡</strong>收到数据包，触发 <strong>硬件中断</strong>。</li><li><strong>CPU</strong> 停下当前任务，跑去跑中断处理程序。</li><li><strong>驱动</strong>把数据包送到协议栈（L3），推入 Socket 队列。</li><li><strong>协议栈</strong>唤醒睡眠的应用。</li><li><strong>调度器</strong>进行上下文切换，恢复应用的执行。</li></ol><p>这一套「组合拳」打下来，微秒就溜走了。对于低延迟应用来说，这是不可接受的。</p><p>从内核 3.11 开始（最初叫 Low Latency Sockets Poll，后来按 Linus 的建议改成了 Busy Poll），Linux 提供了一种更激进的方式。</p><hr><h3 id="busy-poll-拒绝睡觉" tabindex="-1">Busy Poll：拒绝睡觉 <a class="header-anchor" href="#busy-poll-拒绝睡觉" aria-label="Permalink to &quot;Busy Poll：拒绝睡觉&quot;">​</a></h3><p><strong>Busy Poll 的核心思想很简单：既然睡觉和唤醒太费时间，那我不睡了。</strong></p><p>当应用读取 Socket 发现没数据时，它不阻塞，而是直接<strong>主动下到驱动层</strong>去问：「你手里现在有没有刚收到的货？有的话直接给我，别走中断流程了。」</p><p>这就是 <code>ndo_busy_poll</code> 回调函数做的事。</p><p>你可以把这个过程理解为：</p><blockquote><p>传统模式是<strong>被动等快递</strong>：你在家里睡觉（应用阻塞），快递到了敲门（中断），你醒来开门签收。 Busy Poll 是<strong>主动去驿站查</strong>：你每隔几分钟就跑去驿站（驱动）问：「有我的件吗？」有就直接拿走，不用等快递员打电话。</p></blockquote><h4 id="机制的实现" tabindex="-1">机制的实现 <a class="header-anchor" href="#机制的实现" aria-label="Permalink to &quot;机制的实现&quot;">​</a></h4><p>为了让一个网卡驱动支持 Busy Poll，它需要在 <code>net_device_ops</code> 结构体里实现 <code>ndo_busy_poll</code> 回调。</p><p>去看看 Intel 的 ixgbe 驱动（<code>drivers/net/ethernet/intel/ixgbe/ixgbe_main.c</code>），里面的 <code>ixgbe_low_latency_recv()</code> 就是典型实现。这个函数的任务很明确：</p><ol><li>直接去网卡寄存器或 Ring Buffer 里检查。</li><li>如果有包，取出并送上协议栈（L3）。</li><li>它可能会发现属于<strong>其他 Socket</strong> 的包，顺手也处理了（既然已经下来了，多拿一个也是拿）。</li><li>返回处理的包数量。如果没有包，返回 0。</li></ol><p>如果驱动没实现这个回调，或者返回 0，内核就会按传统流程走，不会崩溃，只是退回到普通模式。</p><h4 id="还有一个细节-防止「擦肩而过」" tabindex="-1">还有一个细节：防止「擦肩而过」 <a class="header-anchor" href="#还有一个细节-防止「擦肩而过」" aria-label="Permalink to &quot;还有一个细节：防止「擦肩而过」&quot;">​</a></h4><p>这里有一个微妙的时序问题。</p><p>假设应用刚去问过驱动「有货吗」，驱动说「没有」并返回了。<strong>就在这一纳秒之后</strong>，网卡收到了新包。</p><p>按照传统逻辑，应用这会儿该去睡觉了，然后等待下一次中断。但这样又绕回了「高延迟」的老路。</p><p>为了避免这种「刚走就来」的尴尬，Busy Poll 引入了一个<strong>保底轮询期</strong>。</p><p>即使驱动第一次返回没数据，内核也不会立刻放弃，而是会在一段<strong>可配置的时间窗口</strong>内，继续在驱动层徘徊。新包一到达，立刻被抓取。</p><p>这就是为什么我们在配置里会看到一个以微秒（µs）为单位的参数——它决定了应用愿意为了等这个包，在驱动层死磕多久。</p><hr><h3 id="对比-两种路径的殊途同归" tabindex="-1">对比：两种路径的殊途同归 <a class="header-anchor" href="#对比-两种路径的殊途同归" aria-label="Permalink to &quot;对比：两种路径的殊途同归&quot;">​</a></h3><p>让我们通过图 14-1 来对比一下这两种模式的流程差异，这能帮你直观地感受 CPU 到底在干什么。</p><p><strong>图 14-1 左侧：传统接收流</strong></p><ol><li>应用检查接收队列。</li><li>没数据，阻塞（Block）。</li><li>网卡收到包。</li><li>驱动把包交给协议层。</li><li>协议层/Socket <strong>唤醒</strong>应用。</li><li>应用拿到数据。</li></ol><p>这里的关键是步骤 2 到 5 之间：<strong>中断 + 上下文切换</strong>。</p><p><strong>图 14-1 右侧：Busy Poll 接收流</strong></p><ol><li>应用检查接收队列。</li><li><strong>直接下到驱动层</strong>检查是否有挂起的包（轮询开始）。</li><li>与此同时，网卡收到包。</li><li>驱动处理这个挂起的包。</li><li>驱动把包交给协议层。</li><li>应用拿到数据。</li></ol><p>注意看右侧：</p><ul><li><strong>没有</strong>步骤 5 的唤醒过程（应用一直醒着）。</li><li><strong>没有</strong>中断处理（应用主动轮询）。</li><li>绕过了上下文切换和中断开销。</li></ul><p>这种模式能提供接近硬件极限的延迟。但代价是：<strong>CPU 占用率会直线上升</strong>。如果很多 Socket 都开 Busy Poll，它们在同一个 CPU 核心上抢着轮询，反而会因为资源竞争导致性能下降。</p><hr><h3 id="全局开启-大炮打蚊子" tabindex="-1">全局开启：大炮打蚊子 <a class="header-anchor" href="#全局开启-大炮打蚊子" aria-label="Permalink to &quot;全局开启：大炮打蚊子&quot;">​</a></h3><p>想让系统里的所有 Socket 都进入这种「战斗模式」，你可以直接改内核参数。</p><p>有两个主要的 <code>/proc/sys/net/core/</code> 参数：</p><ul><li><code>busy_read</code>：控制 <code>read()</code> 系统调用时的 Busy Poll 时长（单位：微秒）。</li><li><code>busy_poll</code>：控制 <code>select()</code> 和 <code>poll()</code> 系统调用时的 Busy Poll 时长。</li></ul><p>默认它们都是 0，也就是关闭。</p><p>你可以把它们设为 50（微秒）。这是一个比较公认的起点，能兼顾延迟和 CPU 消耗。</p><p><strong>这里有个坑需要注意</strong>：</p><ul><li>对于<strong>阻塞读</strong>（blocking read），<code>busy_read</code> 定义了死磕的时间。</li><li>对于<strong>非阻塞读</strong>（non-blocking read），如果 Socket 开了 Busy Poll，内核只会<strong>轮询一次</strong>，然后就返回给用户了。因为非阻塞调用的语义就是「别等我」。</li></ul><hr><h3 id="精准打击-按需开启" tabindex="-1">精准打击：按需开启 <a class="header-anchor" href="#精准打击-按需开启" aria-label="Permalink to &quot;精准打击：按需开启&quot;">​</a></h3><p>全局开启有点暴力，就像为了杀鸡用了牛刀，还把全屋的灯都打开。更优雅的做法是让应用自己指定哪些 Socket 需要低延迟。</p><p>这可以通过设置 <code>SO_BUSY_POLL</code> 这个 Socket 选项来实现。</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> val </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 微秒</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setsockopt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(sock, SOL_SOCKET, SO_BUSY_POLL, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">val</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">sizeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(val));</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>这一步设置的是 Socket 结构体（<code>struct sock</code>）里的 <code>sk_ll_usec</code> 字段。</p><p><strong>配置建议</strong>： 如果使用 <code>SO_BUSY_POLL</code>，建议把全局的 <code>sysctl.net.busy_read</code> 设为 0，完全交给应用自己控制。这样，系统里其他无关的应用和服务还能走传统的节能路径，不会因为你的高性能需求而遭殃。</p><hr><h3 id="调优与配置-让它转得更快" tabindex="-1">调优与配置：让它转得更快 <a class="header-anchor" href="#调优与配置-让它转得更快" aria-label="Permalink to &quot;调优与配置：让它转得更快&quot;">​</a></h3><p>开了 Busy Poll 只是把法拉利发动了，要想在赛道上跑得快，还得做些底盘调教。这里有几个实战经验：</p><p><strong>1. 中断合并</strong></p><p>推荐用 <code>ethtool -C</code> 把网卡的中断合并时间（<code>rx-usecs</code>）调大一点，比如 100。 这听起来很反直觉——我们在追求低延迟，为什么要推迟中断？</p><p>因为 Busy Poll 的应用会主动去抓数据，中断的作用被削弱了。如果中断太频繁，反而会造成不必要的上下文切换开销。把中断合并时间拉长，让驱动批量处理或者等应用来轮询，效果更好。</p><p><strong>2. GRO/LRO 的取舍</strong></p><p>通用接收卸载（GRO/LRO）通常能提升吞吐量，但可能会造成包乱序，尤其是在混合了大批量流量和低延迟流量的情况下。</p><p>你可以尝试用 <code>ethtool -K</code> 关掉 GRO 和 LRO：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ethtool</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -K</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> eth0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gro</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> off</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lro</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> off</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>但这并不是银弹，很多时候保持开启反而效果最好，所以这个得<strong>测</strong>。</p><p><strong>3. 亲和性与 NUMA</strong></p><p>这是最关键的一点。</p><ul><li><strong>应用线程</strong>绑在 CPU Core A。</li><li>**网卡 IRQ（中断处理）**绑在 CPU Core B。</li></ul><p>这应该是<strong>两个不同的核心</strong>。 而且，这两个核心最好和网卡设备在<strong>同一个 NUMA 节点</strong>上（跨 NUMA 访问内存很慢）。</p><p>如果应用和 IRQ 抢同一个核，或者跨 NUMA 访问，延迟波动会非常大。尤其是当 <code>rx-usecs</code> 设置得很低的时候，这种竞争会极其致命。</p><p><strong>4. IOMMU</strong></p><p>为了追求极致的低延迟，有些老司机会建议关掉 IOMMU（Input/Output Memory Management Unit）。 IOMMU 是为了安全（DMA 保护）和虚拟化存在的，但它增加了一层地址翻译开销。在某些系统上，它可能是默认关闭的；如果开了且你需要极致性能，可以尝试关掉它（前提是你知道你在干什么）。</p><hr><h3 id="性能与权衡" tabindex="-1">性能与权衡 <a class="header-anchor" href="#性能与权衡" aria-label="Permalink to &quot;性能与权衡&quot;">​</a></h3><p>用了 Busy Poll，你会看到什么？</p><ul><li><strong>延迟</strong>显著降低。</li><li><strong>抖动</strong>显著减少。</li><li>每秒事务处理量（TPS）可能会提升。</li></ul><p>但如果你滥用它——给几千个 Socket 都开了 Busy Poll——CPU 占用率会爆炸。因为大家都在空转轮询，真正干活的时间变少了。</p><p>记住，这本质上是一种<strong>用 CPU 资源换取时间</strong>的 trade-off。在低延迟至关重要的场景（如 HFT、电信核心网）里，这笔买卖是划算的；在普通的 Web 服务器上，这可能是自杀。</p><hr><p>本节我们把网络栈的「激进模式」扒了一遍。下一节，我们将把视线移回硬件层，看看 <strong>PCI 子系统和 Wake-on-LAN</strong>，那是另一种把硬件从睡眠中唤醒的方式——只不过这一次，是通过网线。</p>`,86)])])}const k=o(t,[["render",n]]);export{g as __pageData,k as default};
