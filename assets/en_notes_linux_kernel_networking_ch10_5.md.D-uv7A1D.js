import{_ as i,o as e,c as a,a as n}from"./chunks/framework.8tiMeCK-.js";const d=JSON.parse('{"title":"10.5 Receiving an IPsec Packet (Transport Mode)","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch10_5.md","filePath":"en/notes/linux_kernel_networking/ch10_5.md","lastUpdated":null}'),t={name:"en/notes/linux_kernel_networking/ch10_5.md"};function p(l,s,r,h,o,c){return e(),a("div",null,[...s[0]||(s[0]=[n(`<h1 id="_10-5-receiving-an-ipsec-packet-transport-mode" tabindex="-1">10.5 Receiving an IPsec Packet (Transport Mode) <a class="header-anchor" href="#_10-5-receiving-an-ipsec-packet-transport-mode" aria-label="Permalink to &quot;10.5 Receiving an IPsec Packet (Transport Mode)&quot;">​</a></h1><p>In the previous section, we paused at the end of initialization at <code>xfrm4_rcv()</code>—the receive entry point that the ESP protocol registered with the kernel. The pipeline is laid out, so what kind of journey does a packet actually take when it flows in?</p><p>Let&#39;s assume this is an IPv4 ESP transport mode packet destined for the local machine.</p><h3 id="_10-5-receiving-an-ipsec-packet-transport-mode-1" tabindex="-1">10.5. Receiving an IPsec Packet (Transport Mode) <a class="header-anchor" href="#_10-5-receiving-an-ipsec-packet-transport-mode-1" aria-label="Permalink to &quot;10.5. Receiving an IPsec Packet (Transport Mode)&quot;">​</a></h3><p>As we mentioned in the previous section, <code>xfrm4_rcv()</code> is registered as the shared entry point for ESP, AH, and even IPCOMP protocols. This is a clever design: since the very first thing they all do after entering is table lookup and replay attack verification, it makes sense to consolidate this dirty work in one place.</p><p>So, when this ESP packet arrives at <code>ip_local_deliver_finish()</code>, the kernel sees that the protocol field in the IP header is 50 (ESP) and directly calls <code>xfrm4_rcv()</code>. This function does nothing else but hands the packet straight to the generic XFRM processing center—<code>xfrm_input()</code>.</p><p>This is the place where the &quot;encryption shell&quot; truly gets cracked open.</p><h4 id="step-1-look-up-the-key-in-the-sad" tabindex="-1">Step 1: Look Up the Key in the SAD <a class="header-anchor" href="#step-1-look-up-the-key-in-the-sad" aria-label="Permalink to &quot;Step 1: Look Up the Key in the SAD&quot;">​</a></h4><p>After <code>xfrm_input()</code> gets the packet, the first thing it does isn&#39;t rushing to decrypt. Instead, it verifies one thing: <strong>have we agreed with the peer on how to decrypt this packet?</strong></p><p>This requires a lookup in the Security Association Database (SAD). The lookup keys are the SPI (Security Parameter Index), destination address, and protocol number. If the lookup fails, it means the packet was either misdirected or blindly sent, so it gets dropped immediately:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 在 state_byspi 哈希表中执行查找 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> xfrm_state_lookup</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(net, skb</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">mark</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, daddr, spi, nexthdr, family);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 如果查找失败，静默丢弃数据包 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> NULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    XFRM_INC_STATS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(net, LINUX_MIB_XFRMINNOSTATES);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    xfrm_audit_state_notfound</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb, family, spi, seq);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    goto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> drop;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>If the corresponding SA (<code>x == NULL</code>) isn&#39;t found in this step, the kernel silently drops the packet and increments a counter, <code>XFRMINNOSTATES</code>. This kind of &quot;silent failure&quot; is common at the network layer, because you can&#39;t send an ICMP error message back for an unknown encrypted packet—you have no idea who sent it, and you can&#39;t decrypt its contents.</p><h4 id="step-2-protocol-specific-decryption-callback" tabindex="-1">Step 2: Protocol-Specific Decryption Callback <a class="header-anchor" href="#step-2-protocol-specific-decryption-callback" aria-label="Permalink to &quot;Step 2: Protocol-Specific Decryption Callback&quot;">​</a></h4><p>If we&#39;re lucky and the SA is found, the <code>x</code> pointer will point to a valid <code>xfrm_state</code> structure. Next, the kernel calls the <code>.input</code> callback function registered by the protocol associated with this SA (e.g., ESP).</p><p>For the IPv4 ESP scenario we&#39;re discussing, this <code>x-&gt;type</code> is the <code>esp_type</code> registered in the previous section, and its <code>.input</code> callback points to <code>esp_input()</code>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 调用对应协议的 input 方法（这里是 esp_input），</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   返回值 nexthdr 是加密前原始数据的协议号 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">nexthdr </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x, skb);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><code>esp_input()</code> calls the Crypto API to perform decryption and authentication (AEAD operations). If the cryptographic verification fails, the packet is dropped; if it succeeds, the ESP header and ESP trailer are stripped away, leaving behind the original IP payload.</p><p>This function returns something important: <code>nexthdr</code>. This is the protocol number of the original packet inside, after stripping off the ESP encapsulation (for example, TCP is 6, UDP is 17).</p><p>The kernel carefully stores this protocol number and tucks it into the control buffer (<code>cb</code>) of <code>skb</code>. Because the data area of <code>skb</code> is now decrypted, but the original IP header hasn&#39;t been updated yet, we need to remember it for when we fix the IP header later:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 将原始协议号保存在 SKB 的控制缓冲区中，</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> * 稍后修改 IP 头时会用到 */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">XFRM_MODE_SKB_CB</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">protocol </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> nexthdr;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><h4 id="step-3-cleanup-and-disguise-transport-finish" tabindex="-1">Step 3: Cleanup and Disguise (Transport Finish) <a class="header-anchor" href="#step-3-cleanup-and-disguise-transport-finish" aria-label="Permalink to &quot;Step 3: Cleanup and Disguise (Transport Finish)&quot;">​</a></h4><p>Decryption is done, and now we&#39;re holding a &quot;restored&quot; packet: its data portion is in plaintext, but its IP header still says &quot;protocol=ESP&quot; (50). This obviously won&#39;t work—the transport layer (L4) would be completely confused if it received this packet.</p><p>So, <code>xfrm_input()</code> finally calls <code>xfrm4_transport_finish()</code> to perform the final cosmetic surgery.</p><p>Let&#39;s see what this function does:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> xfrm4_transport_finish</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">skb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> iphdr </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">iph </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ip_hdr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 此时 iph-&gt;protocol 还是 50 (ESP)，</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     * 我们要把它改成解密后原始包的协议号（如 TCP/UDP），</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     * 这样 L4 协议栈才能认领这个包 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    iph-&gt;protocol </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> XFRM_MODE_SKB_CB</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb)-&gt;protocol;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 调整 skb 的指针，因为解密后包的长度可能变了，</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     * 需要让 IP 头部的指针回到正确的位置 */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    __skb_push</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb, skb-&gt;data </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> skb_network_header</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb));</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 更新 IP 头部的总长度字段 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    iph-&gt;tot_len </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> htons</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb-&gt;len);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 重新计算 IP 头部校验和，因为我们改了 protocol 和 tot_len */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    ip_send_check</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(iph);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br></div></div><p>This section is critical. You can think of it like unboxing a package: <code>esp_input</code> is responsible for taking the items (data) out of the box, while <code>xfrm4_transport_finish</code> is responsible for changing the information on the shipping label (IP header) back to what the original product looked like.</p><p>Modifying the IP header is mandatory, because this packet is about to re-enter the subsequent stages of the network stack. If we don&#39;t change it, the kernel will think this is still an ESP packet and keep sending it to the encryption engine, resulting in an infinite loop.</p><h4 id="step-4-reinject-into-the-protocol-stack" tabindex="-1">Step 4: Reinject into the Protocol Stack <a class="header-anchor" href="#step-4-reinject-into-the-protocol-stack" aria-label="Permalink to &quot;Step 4: Reinject into the Protocol Stack&quot;">​</a></h4><p>After the disguise is complete, this packet is logically a normal, unencrypted IP packet. To let it finish the rest of its journey (reaching the TCP or UDP socket), the kernel throws it back into the receive path of the protocol stack.</p><p>This step is implemented via a Netfilter hook:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 调用 netfilter 的 PRE_ROUTING hook，</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     * 然后通过 xfrm4_rcv_encap_finish 继续流转 */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    NF_HOOK</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(NFPROTO_IPV4, NF_INET_PRE_ROUTING, skb, skb</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">dev</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">NULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            xfrm4_rcv_encap_finish);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p><code>xfrm4_rcv_encap_finish()</code> ultimately calls <code>ip_local_deliver()</code>. At this point, the protocol field in the IP header is already TCP or UDP, and the kernel handles it just like a normal packet freshly arrived from the NIC, passing it to the upper-layer protocol.</p><p>With this, the receive journey of an IPsec transport mode packet truly comes to an end.</p><p>From the outside, it looks like an ESP packet; once inside <code>xfrm_input()</code>, it gets looked up and decrypted; when it comes out, it has changed its appearance and turned into a normal IP packet, silently blending into the regular data flow. This is the essence of transport mode—<strong>it doesn&#39;t alter the original routing path, it simply quietly hardens the transmission process</strong>.</p>`,34)])])}const u=i(t,[["render",p]]);export{d as __pageData,u as default};
