import{_ as t,o as s,c as i,a}from"./chunks/framework.8tiMeCK-.js";const k=JSON.parse('{"title":"1.2  The Network Device","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch01_2.md","filePath":"en/notes/linux_kernel_networking/ch01_2.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_networking/ch01_2.md"};function o(r,e,l,h,d,p){return s(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="_1-2-the-network-device" tabindex="-1">1.2 The Network Device <a class="header-anchor" href="#_1-2-the-network-device" aria-label="Permalink to &quot;1.2  The Network Device&quot;">​</a></h1><p>Let&#39;s shift our focus down to the bottom layer — Layer 2 (L2), the data link layer.</p><p>This is where the Network Device Driver lives. Although the main character of this book is the kernel Network Stack rather than teaching you how to write drivers (which would require a whole separate thick book), to truly understand how packets enter and leave, we must first meet the &quot;handshaker&quot; between the driver and the stack: the <code>net_device</code> structure.</p><p>It&#39;s like driving on a highway — you need to understand how toll booths work, even if you&#39;re not the toll collector.</p><h3 id="net-device-the-nic-s-id-card" tabindex="-1"><code>net_device</code>: The NIC&#39;s &quot;ID Card&quot; <a class="header-anchor" href="#net-device-the-nic-s-id-card" aria-label="Permalink to &quot;\`net_device\`: The NIC&#39;s &quot;ID Card&quot;&quot;">​</a></h3><p>In the kernel&#39;s eyes, a NIC doesn&#39;t feel like a physical entity; it&#39;s simply a massive structure instance — <code>net_device</code>.</p><p>This structure holds everything about the NIC&#39;s &quot;life and soul&quot;:</p><ul><li><strong>Hardware IRQ number</strong>: The interrupt request line the CPU relies on to know the NIC has work to do.</li><li><strong>MTU (Maximum Transmission Unit)</strong>: The Ethernet default is 1500 bytes. Packets exceeding this must be fragmented.</li><li><strong>MAC address</strong>: The NIC&#39;s physical ID, 48 bits long.</li><li><strong>Device name</strong>: Names like <code>eth0</code> and <code>wlan0</code> are defined here.</li><li><strong>Flags</strong>: Is the NIC UP or DOWN? Is it RUNNING?</li><li><strong>Multicast address list</strong>.</li><li><strong>Promiscuous mode counter</strong>: We&#39;ll cover this shortly; it&#39;s key to packet capture tools.</li><li><strong>Hardware features</strong>: Such as whether it supports offloading features like GSO and GRO.</li><li><strong><code>net_device_ops</code> callback set</strong>: The NIC&#39;s operation manual, containing function pointers for opening the device, stopping the device, transmitting data, changing the MTU, and so on.</li><li><strong>ethtool callbacks</strong>: This is why typing <code>ethtool eth0</code> on the command line shows you a bunch of register information.</li><li><strong>Multi-queue support</strong>: Modern 10-Gigabit NICs have multiple Tx/Rx queues.</li><li><strong>Timestamps</strong>: Recording the time of the last packet transmission or reception.</li></ul><p>Let&#39;s take a quick look at a snippet of the kernel definition to get a feel for its style:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            irq;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            /* 中断号                */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device_ops </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">netdev_ops;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* 操作方法集           */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            mtu;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            /* 最大传输单元           */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            promiscuity;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 混杂模式计数器         */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">           *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dev_addr;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      /* 硬件地址 (MAC)         */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><blockquote><p><strong>Note</strong>: I dissect this behemoth in detail in Appendix A. When you get stuck reading the source code, it&#39;s worth flipping back to it.</p></blockquote><p>Here&#39;s a particularly interesting and easily overlooked detail: the <strong>promiscuous mode counter</strong>.</p><p>Why is it an <code>int</code> counter rather than a <code>bool</code> flag?</p><p>If it were a boolean, when a packet capture tool (like <code>tcpdump</code>) is opened, the flag is set to 1. If another capture tool (like <code>wireshark</code>) is then opened and later closed, it would set the flag back to 0. The result? The first tool is still running, but the NIC has exited promiscuous mode — total chaos.</p><p>Using a counter perfectly solves this problem:</p><ol><li><code>tcpdump</code> starts, counter +1 (becomes 1), NIC enters promiscuous mode and starts receiving all packets passing on the wire (regardless of whether they are destined for the local machine).</li><li><code>wireshark</code> starts, counter +1 (becomes 2).</li><li><code>tcpdump</code> exits, counter -1 (becomes 1). The NIC remains in promiscuous mode because <code>wireshark</code> is still using it.</li><li><code>wireshark</code> exits, counter -1 (becomes 0). At this point, the kernel confirms no one is eavesdropping, and the NIC exits promiscuous mode and resumes normal operation.</li></ol><p>Although simple, this design is a classic textbook case for handling &quot;shared resource state among multiple users.&quot;</p><hr><h3 id="a-performance-savior-napi-new-api" tabindex="-1">A Performance Savior: NAPI (New API) <a class="header-anchor" href="#a-performance-savior-napi-new-api" aria-label="Permalink to &quot;A Performance Savior: NAPI (New API)&quot;">​</a></h3><p>While browsing the kernel&#39;s core networking code, you&#39;ll frequently run into an acronym — <strong>NAPI</strong>.</p><p>It&#39;s an indispensable part of modern network drivers. To understand it, we need to look at what the old world looked like.</p><p><strong>The Old Approach: Interrupt-Driven</strong> Early NIC drivers had a very simple working model: one packet arrives, one interrupt is fired. Packet arrives → interrupt triggers → CPU stops what it&#39;s doing, saves context → jumps to the interrupt handler → grabs the packet → restores context. This is fine for everyday web browsing, but if you&#39;re hit with a DDoS attack or processing massive amounts of small-packet traffic, the CPU will collapse. What does hundreds of thousands of interrupts per second mean? It means the CPU exhausts its computing power just &quot;entering&quot; and &quot;exiting&quot; (saving/restoring registers and context), leaving no time to do actual work. In operating systems, this is called <strong>interrupt livelock</strong>.</p><p><strong>The New Approach: NAPI (Hybrid Mode)</strong> To solve this problem, kernel developers introduced NAPI (New API). Its core idea is to <strong>dynamically switch strategies based on the load</strong>.</p><ul><li><strong>Under low load</strong>: It still uses interrupts. No packets means no bothering the CPU, saving power and ensuring fast response times.</li><li><strong>Under high load</strong>: It switches to <strong>polling</strong>. After the initial interrupt triggers, the driver temporarily disables that interrupt and tells the kernel, &quot;I have a bunch of packets now. Come poll me periodically to get them, don&#39;t wait for the next interrupt.&quot;</li></ul><p>This changes &quot;processing N packets requires N interrupt context switches&quot; into &quot;processing N packets requires only 1 interrupt + polling.&quot; The performance improvement is immediate.</p><p>However, you can&#39;t have the best of both worlds. Although polling offers high throughput, it increases latency — because the CPU doesn&#39;t respond immediately, but waits until the polling cycle arrives to fetch the packets.</p><p>For applications that are extremely latency-sensitive and willing to splurge CPU resources to achieve it (like high-frequency trading), Linux introduced <strong>Busy Polling on Sockets</strong> starting from kernel 3.11. This is a more niche optimization, which we&#39;ll save for the &quot;Busy Poll Sockets&quot; section in Chapter 14.</p><p>Alright, now we have a sufficient understanding of the NIC as an &quot;exit.&quot; Next, let&#39;s look at the real main event: <strong>how packets travel through the kernel</strong>.</p><hr><h3 id="the-packet-s-journey-receive-and-transmit" tabindex="-1">The Packet&#39;s Journey: Receive and Transmit <a class="header-anchor" href="#the-packet-s-journey-receive-and-transmit" aria-label="Permalink to &quot;The Packet&#39;s Journey: Receive and Transmit&quot;">​</a></h3><p>The entire life of a network device driver is basically spent on two things:</p><ol><li><strong>Receiving</strong>: Taking packets captured from the wire and passing them up layer by layer to the network layer (L3), and ultimately to the transport layer (L4).</li><li><strong>Transmitting</strong>: Taking packets generated locally or needing to be forwarded, and pushing them into the NIC to be sent out on the wire.</li></ol><p>Sounds simple? There are plenty of detours along the way.</p><p>For every packet passing through the kernel, whether inbound or outbound, the <strong>routing subsystem</strong> performs a table lookup. This lookup determines where this &quot;ticket&quot; should actually go: should it be kept for the local machine, or forwarded out through which NIC? This part is extremely important, and we&#39;ll dive deep into the IPv4 and IPv6 routing subsystems in Chapters 5 and 6.</p><p>But the routing decision isn&#39;t the only hurdle. As packets travel through the stack, they also encounter &quot;roadblocks&quot;:</p><h4 id="_1-netfilter-hooks" tabindex="-1">1. Netfilter Hooks <a class="header-anchor" href="#_1-netfilter-hooks" aria-label="Permalink to &quot;1. Netfilter Hooks&quot;">​</a></h4><p>This is the mechanism behind firewalls and NAT. The kernel sets up &quot;checkpoints&quot; at five critical nodes in the network stack.</p><ul><li>Before a received packet even hits the routing table, it passes through <code>NF_INET_PRE_ROUTING</code>.</li><li>Before a transmitted packet leaves the NIC, it passes through <code>NF_INET_POST_ROUTING</code>.</li></ul><p>These checkpoints are triggered by the <code>NF_HOOK()</code> macro. If your iptables rules say this packet is illegitimate, the callback function returns <code>NF_DROP</code>, and the packet is dropped on the spot — the kernel doesn&#39;t even bother to write an obituary for it. If it&#39;s <code>NF_ACCEPT</code>, it&#39;s let through to the next stop. We&#39;ll break down the underlying mechanisms of Netfilter, connection tracking, and iptables in detail in Chapter 9.</p><h4 id="_2-ipsec" tabindex="-1">2. IPsec <a class="header-anchor" href="#_2-ipsec" aria-label="Permalink to &quot;2. IPsec&quot;">​</a></h4><p>If a packet matches an IPsec policy, it gets sent off for encryption or decryption. IPsec provides network-layer security (using the ESP or AH protocols). Although the IPv6 specification mandates IPsec support while it&#39;s optional for IPv4, Linux has full support for both. IPsec has two modes:</p><ul><li><strong>Transport mode</strong>: Only the payload is encrypted.</li><li><strong>Tunnel mode</strong>: The entire original packet is encrypted and wrapped in a new IP packet (the common method for VPNs). IPsec and NAT often clash (because you can&#39;t modify the address after encryption), leading to a &quot;NAT Traversal&quot; solution. We&#39;ll leave all of this for Chapter 10.</li></ul><h4 id="_3-ttl-time-to-live" tabindex="-1">3. TTL (Time To Live) <a class="header-anchor" href="#_3-ttl-time-to-live" aria-label="Permalink to &quot;3. TTL (Time To Live)&quot;">​</a></h4><p>If a packet is being forwarded, its IPv4 header contains a field called <code>TTL</code> (Time To Live). Every time it passes through a router, this value is decremented by 1. When it reaches 0, the router drops the packet immediately and sends back an ICMP &quot;Time Exceeded&quot; message. This prevents packets from living forever in the network due to routing loops. Similarly, every time the TTL is modified, the IPv4 header checksum must be recalculated, which is quite a bit of work. In IPv6, this field was renamed to <code>Hop Limit</code> (Hop Limit) — same meaning, just a more elegant name.</p><p>In addition, the packet&#39;s journey is full of variables:</p><ul><li><strong>Fragmentation and reassembly</strong>: Large packets (exceeding the MTU) get chopped up, and the receiving end has to piece them back together. We&#39;ll cover this in Chapter 4.</li><li><strong>Multicast</strong>: The packet isn&#39;t sent to one person, but to a group. This involves the IGMP protocol and multicast routing daemons (like <code>pimd</code>), which is typically much more complex than unicast. We&#39;ll touch on this in Chapter 6.</li></ul><p>To maintain order amidst all these complex detours, the kernel must have a unified way to describe and manage these packets. This core data structure is the legendary <strong>SKB</strong>.</p><hr><h3 id="the-core-socket-buffer-sk-buff" tabindex="-1">The Core: Socket Buffer (<code>sk_buff</code>) <a class="header-anchor" href="#the-core-socket-buffer-sk-buff" aria-label="Permalink to &quot;The Core: Socket Buffer (\`sk_buff\`)&quot;">​</a></h3><p><code>sk_buff</code> (which we usually just call <strong>SKB</strong>) is the most core, most complex, and most headache-inducing data structure in the Linux kernel Network Stack.</p><p>It is the physical &quot;body&quot; of the packet inside the kernel. Whether the packet was just fished out of the wire by the NIC driver, or is about to be sent out from the TCP layer, it is an SKB.</p><p>Let&#39;s take a look at its definition (only listing key members; see Appendix A for the full version):</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sock             </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sk;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            /* 拥有这个包的套接字      */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device       </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dev;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           /* 关联的网卡设备          */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __u8                    pkt_type:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     /* 包类型（单播/组播/广播）*/</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __be16                  protocol;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       /* 协议类型               */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          tail;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           /* 尾部指针                */</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          end;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            /* 结束指针                */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">           *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">head,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                            *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">data;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          /* 头部和数据指针          */</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          transport_header;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> /* L4 头部位置          */</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          network_header;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /* L3 头部位置          */</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    sk_buff_data_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          mac_header;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      /* L2 头部位置          */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    . . .</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br></div></div><p><strong>The Golden Rule of SKB Manipulation</strong> Never try to manually <code>skb-&gt;data++</code> or directly manipulate pointers! SKB&#39;s internal management relies on a strict set of APIs:</p><ul><li>Want to move the <code>data</code> pointer forward (strip the header)? Use <code>skb_pull()</code> or <code>skb_pull_inline()</code>.</li><li>Want to move it backward (reserve header space)? Use <code>skb_push()</code>.</li><li>Want to get the transport layer (L4) header? Use <code>skb_transport_header(skb)</code>.</li><li>Want to get the network layer (L3) header? Use <code>skb_network_header(skb)</code>.</li><li>Want to get the MAC header? Use <code>skb_mac_header(skb)</code>.</li></ul><p>Following this API is crucial for managing the exquisite linear data area and paged structure inside the SKB — don&#39;t try to be too clever.</p><h4 id="the-birth-of-an-skb-receive-path" tabindex="-1">The Birth of an SKB (Receive Path) <a class="header-anchor" href="#the-birth-of-an-skb-receive-path" aria-label="Permalink to &quot;The Birth of an SKB (Receive Path)&quot;">​</a></h4><p>When a packet comes in from the Ethernet cable, the driver allocates an SKB via <code>netdev_alloc_skb()</code> (older code might use <code>dev_alloc_skb()</code>).</p><p>At the data link layer (L2), the driver does two important things:</p><ol><li><p><strong>Determine the type (<code>eth_type_trans</code>)</strong>: This function sets the SKB&#39;s <code>pkt_type</code> based on the destination MAC address in the Ethernet frame header:</p><ul><li>Destined for the local machine? → <code>PACKET_HOST</code>.</li><li>Multicast? → <code>PACKET_MULTICAST</code>.</li><li>Broadcast? → <code>PACKET_BROADCAST</code>. At the same time, it reads the Type field from the Ethernet header (e.g., <code>0x0800</code> for IPv4, <code>0x86DD</code> for IPv6) and fills it into the SKB&#39;s <code>protocol</code> field.</li></ul></li><li><p><strong>Pointer jumping (<code>skb_pull_inline</code>)</strong>: This is the part where beginners get dizzy most easily. When an Ethernet frame enters memory, <code>skb-&gt;data</code> points to the start of the L2 header. However! The moment the driver hands this packet over to the network layer (L3), the kernel expects <code>skb-&gt;data</code> to point to the L3 header (the IP header). So <code>eth_type_trans()</code> calls <code>skb_pull_inline(skb, 14)</code> to move the pointer forward by 14 bytes (exactly the length of the Ethernet header <code>ETH_HLEN</code>), skipping the L2 header.</p><p><strong>Imagine this</strong>: You&#39;re peeling an onion. Peel off one layer (L2), and what&#39;s left in your hand should be exactly the next layer (L3). <code>skb_pull</code> is exactly this peeling action.</p></li></ol><p><em>(Insert original book Figure 1-3 diagram here: a standard UDPv4 packet, from the 14 bytes of L2, to the 20 bytes of L3, and then the 8 bytes of L4)</em></p><h4 id="the-wandering-skb" tabindex="-1">The Wandering SKB <a class="header-anchor" href="#the-wandering-skb" aria-label="Permalink to &quot;The Wandering SKB&quot;">​</a></h4><p>Every SKB has a <code>dev</code> pointer that records &quot;whose side it&#39;s on&quot; (incoming packets record the input NIC, outgoing packets record the output NIC). This is important because the kernel needs to decide whether to slice the packet based on this NIC&#39;s MTU.</p><p>Every transmitted SKB also has a <code>sk</code> pointer pointing to the Socket that produced this packet. <strong>Note</strong>: For forwarded packets, <code>sk</code> is <strong>NULL</strong>. Because it wasn&#39;t generated locally; it&#39;s just a &quot;passerby.&quot;</p><h4 id="the-destination-of-the-skb" tabindex="-1">The Destination of the SKB <a class="header-anchor" href="#the-destination-of-the-skb" aria-label="Permalink to &quot;The Destination of the SKB&quot;">​</a></h4><p>Received packets are ultimately dispatched to their corresponding protocol handler functions:</p><ul><li>IPv4 packets are handed off to <code>ip_rcv()</code>.</li><li>IPv6 packets are handed off to <code>ipv6_rcv()</code>.</li></ul><p>How are these functions registered? Using the <code>dev_add_pack()</code> method. We&#39;ll take a closer look at this in the dedicated chapters for IPv4 and IPv6 (Chapters 4 and 8).</p><p>Taking <code>ip_rcv()</code> as an example, it first performs a bunch of sanity checks, and then — if it isn&#39;t intercepted by Netfilter&#39;s <code>PRE_ROUTING</code> hook — it enters <code>ip_rcv_finish()</code>. Here, the kernel queries the routing subsystem and builds a <code>dst_entry</code> (destination cache entry), which determines where this packet goes next. For a hardcore analysis of the routing subsystem, check out Chapters 5 and 6.</p><hr><h3 id="data-link-layer-details-ethernet-and-neighbors" tabindex="-1">Data Link Layer Details: Ethernet and Neighbors <a class="header-anchor" href="#data-link-layer-details-ethernet-and-neighbors" aria-label="Permalink to &quot;Data Link Layer Details: Ethernet and Neighbors&quot;">​</a></h3><p>Finally, we need to mention those trivial but critical matters in the L2 layer.</p><p><strong>MAC Addresses and ARP</strong> Every NIC comes from the factory with a 48-bit MAC address, although you can tamper with it using the <code>ifconfig</code> or <code>ip</code> commands. When your Socket wants to send a packet to <code>192.168.1.5</code>, it only knows the IP address. However, Ethernet frames only recognize MAC addresses. This calls for an interpreter: the <strong>Neighbor Subsystem</strong>.</p><ul><li><strong>IPv4</strong> uses the <strong>ARP</strong> protocol. It shouts out in the local network (broadcast): &quot;Who has 192.168.1.5? Tell me your MAC!&quot;</li><li><strong>IPv6</strong> uses the <strong>NDISC</strong> protocol (based on ICMPv6). The principle is similar, but it&#39;s more complex and uses multicast instead of broadcast.</li></ul><p>This mechanism is explained in detail in Chapter 7. If your network is down, try pinging first, and then check <code>arp -n</code> — the problem is often right here.</p><p><strong>Kernel-to-Userspace Communication: Netlink</strong> How does the kernel tell userspace that a route has changed? Or how do userspace tools tell the kernel, &quot;I want to add a route&quot;? Through <strong>Netlink Sockets</strong>. It acts like a special phone line, with one end connected to the kernel and the other to tools like <code>iproute2</code>. We&#39;ll cover this in detail in Chapter 2.</p><hr><h3 id="special-subsystems-wireless-rdma-and-virtualization" tabindex="-1">Special Subsystems: Wireless, RDMA, and Virtualization <a class="header-anchor" href="#special-subsystems-wireless-rdma-and-virtualization" aria-label="Permalink to &quot;Special Subsystems: Wireless, RDMA, and Virtualization&quot;">​</a></h3><p>In the grand edifice of the Linux Network Stack, there are also a few special &quot;VIP suites.&quot;</p><ol><li><p><strong>Wireless Subsystem</strong>: This is driven by the <code>mac80211</code> framework. It handles things that ordinary wired NICs have never seen: power-saving modes, Mesh networking (HWMP routing protocol), and Ad-hoc networks. In particular, the 802.11n Block Ack mechanism is key to improving throughput. We&#39;ll save this content for Chapter 12.</p></li><li><p><strong>RDMA / InfiniBand</strong>: In high-performance computing and data centers, traditional CPU data transfer is too slow. RDMA allows NICs to directly read and write remote host memory, bypassing the CPU. This technology was introduced starting from kernel 2.6.11 and is now ubiquitous in large clusters. We&#39;ll cover it in Chapter 13.</p></li><li><p><strong>Virtualization</strong>: Process-level virtualization based on Namespaces. This isn&#39;t full virtualization like KVM, but a lighter-weight &quot;isolation.&quot; Linux currently has six types of Namespaces. Network Namespaces allow you to run multiple completely independent network stacks on a single machine (multiple <code>lo</code> interfaces, multiple routing tables). This is the cornerstone of container technologies like Docker. We&#39;ll dive deep into this in Chapter 14, and we&#39;ll also touch on Bluetooth, PCIe, and 6LoWPAN in the IoT domain (stuffing IPv6 into low-power frames like IEEE 802.15.4 — just thinking about it feels cramped).</p></li></ol><hr><p>That concludes our introduction to network devices. Starting from the next section, we will truly dive into the code and see how Linux kernel network development is done — that&#39;s another world full of Git trees and mailing list politics.</p>`,82)])])}const u=t(n,[["render",o]]);export{k as __pageData,u as default};
