import{_ as s,o as i,c as a,a as t}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"8.3  Catching Concurrency Bugs with KCSAN","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch08_3.md","filePath":"en/notes/linux_kernel_debugging/ch08_3.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_debugging/ch08_3.md"};function o(r,e,l,h,c,p){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="_8-3-catching-concurrency-bugs-with-kcsan" tabindex="-1">8.3 Catching Concurrency Bugs with KCSAN <a class="header-anchor" href="#_8-3-catching-concurrency-bugs-with-kcsan" aria-label="Permalink to &quot;8.3  Catching Concurrency Bugs with KCSAN&quot;">​</a></h1><p>Since relying on the human brain to wrestle with the sheer complexity of LKMM is becoming impractical, we had better find a tool to help. Entering the stage for this section is the &quot;security scanner&quot; of kernel concurrency: <strong>KCSAN</strong>.</p><h3 id="what-is-kcsan" tabindex="-1">What is KCSAN <a class="header-anchor" href="#what-is-kcsan" aria-label="Permalink to &quot;What is KCSAN&quot;">​</a></h3><p>KCSAN (Kernel Concurrency Sanitizer) is a runtime framework for dynamically detecting data races. It was merged into the mainline kernel in version 5.8 in August 2020. Currently, its primary battleground is the x86_64 architecture, with ARM64 support arriving much later (stabilized only in kernel 5.17 in March 2022).</p><blockquote><p><strong>If you haven&#39;t read the definition of &quot;data race&quot; in the previous section, we strongly recommend going back and reviewing it now. Otherwise, the following discussion will lack a proper foundation.</strong></p></blockquote><h4 id="how-kcsan-works-the-extremely-simplified-version" tabindex="-1">How KCSAN Works (The Extremely Simplified Version) <a class="header-anchor" href="#how-kcsan-works-the-extremely-simplified-version" aria-label="Permalink to &quot;How KCSAN Works (The Extremely Simplified Version)&quot;">​</a></h4><p>KCSAN has only one core mission: <strong>discover and report data races</strong>.</p><p>To achieve this, it makes a default assumption: <strong>all aligned writes no larger than the processor word size are atomic</strong> (regardless of whether you marked them with <code>WRITE_ONCE</code>). Under this assumption, KCSAN only needs to watch for one specific pattern: an <strong>unmarked plain read</strong> racing against <strong>any write to the same address</strong>.</p><p>Sounds a bit loose? Yes. If those write operations are also plain C writes (no locks, no markers), this is absolutely a bug under the strict LKMM definition, but KCSAN in its default configuration might let it slide. This is a compromise made to reduce the false positive rate (we will dive into this later in the configuration section).</p><h4 id="syzbot-the-tireless-robot" tabindex="-1">Syzbot: The Tireless Robot <a class="header-anchor" href="#syzbot-the-tireless-robot" aria-label="Permalink to &quot;Syzbot: The Tireless Robot&quot;">​</a></h4><p>KCSAN is essentially an automated robot. Paired with <strong>Syzbot</strong> (the syzkaller robot), it can continuously scan the mainline kernel.</p><p>Syzbot specializes in fuzzing; it feeds the kernel all sorts of bizarre system call sequences, trying to force hidden bugs out into the open. KCSAN, meanwhile, stands guard in the background. Once Syzbot&#39;s operations trigger a concurrent access conflict, KCSAN logs it.</p><p>This work has been running since October 2019 and is still going strong today. You can see its track record here: <code>https://syzkaller.appspot.com/upstream?manager=ci2-upstream-kcsan-gce</code></p><h4 id="the-key-technology-here-soft-watchpoints" tabindex="-1">The Key Technology Here: Soft Watchpoints <a class="header-anchor" href="#the-key-technology-here-soft-watchpoints" aria-label="Permalink to &quot;The Key Technology Here: Soft Watchpoints&quot;">​</a></h4><p>The most frustrating part of concurrency bugs is their <strong>Heisenbug</strong> nature—when you try to debug them, they disappear. To catch these bugs that depend on subtle timing, KCSAN must artificially introduce a bit of &quot;perturbation&quot; in the code execution path.</p><p>It works through <strong>compiler instrumentation</strong> and a <strong>soft watchpoint</strong> mechanism:</p><ol><li><strong>Setting the point</strong>: KCSAN sets a soft watchpoint on a memory address.</li><li><strong>Intentional delay</strong>: When code accesses this address, KCSAN intentionally pauses it for a brief moment (this time is configurable; it defaults to 80 microseconds in task context and 20 microseconds in interrupt context).</li><li><strong>Closing the net</strong>: During this delay, if another thread or interrupt also accesses this address, both watchpoints trigger simultaneously.</li></ol><p>KCSAN checks the nature of these two accesses. If they meet the conditions for a data race (e.g., one is an unmarked read and the other is a write), it reports immediately.</p><p>It tells you what happened, whether the data changed (old value vs. new value), and most importantly, it provides <strong>call stack traces for both sides</strong>, letting you see exactly how these two ghosts collided.</p><blockquote><p><strong>Only unmarked accesses take the bait</strong> If your access is marked with the <code>READ_ONCE()</code>, <code>WRITE_ONCE()</code>, or <code>atomic_*</code> macros, KCSAN will not set a watchpoint there. It assumes that since you marked it, you know what you are doing.</p></blockquote><h3 id="configuring-your-kernel-to-enable-kcsan" tabindex="-1">Configuring Your Kernel to Enable KCSAN <a class="header-anchor" href="#configuring-your-kernel-to-enable-kcsan" aria-label="Permalink to &quot;Configuring Your Kernel to Enable KCSAN&quot;">​</a></h3><p>Talk is cheap. To get KCSAN up and running, you need to reconfigure and recompile the kernel.</p><h4 id="prerequisites-checklist" tabindex="-1">Prerequisites Checklist <a class="header-anchor" href="#prerequisites-checklist" aria-label="Permalink to &quot;Prerequisites Checklist&quot;">​</a></h4><p>This isn&#39;t something you can just toggle on; it has several hard requirements:</p><ul><li><strong>Architecture</strong>: Currently primarily supports x86_64. ARM64 support is newer (kernel 5.17+).</li><li><strong>Kernel version</strong>: x86_64 requires at least 5.8 (August 2020); ARM64 requires at least 5.17.</li><li><strong>Compiler</strong>: GCC or Clang must be version 11 or higher. The kernel config option <code>CONFIG_HAVE_KCSAN_COMPILER</code> is responsible for checking this.</li><li><strong>Debug switch</strong>: You must enable <code>CONFIG_DEBUG_KERNEL=y</code>. Note that this merely makes the debug menu visible; it does not automatically select specific tools for you.</li><li><strong>Mutual exclusivity</strong>: <strong>KASAN and KCSAN are fundamentally incompatible</strong>. You can only choose one; they cannot be enabled simultaneously. The reason is simple: both perform massive amounts of instrumentation, and combining them will blow up.</li><li><strong>KCOV conflict</strong>: If compiling with Clang, KCSAN and KCOV (code coverage tool) also cannot be enabled at the same time.</li></ul><p>When you select <code>CONFIG_KCSAN=y</code>, it automatically selects <code>CONFIG_STACKTRACE</code>, because reporting bugs requires printing detailed call stacks.</p><h4 id="enabling-kcsan" tabindex="-1">Enabling KCSAN <a class="header-anchor" href="#enabling-kcsan" aria-label="Permalink to &quot;Enabling KCSAN&quot;">​</a></h4><p>In <code>make menuconfig</code>, you can find it via the following path: <code>Kernel hacking</code> -&gt; <code>Generic Kernel Debugging Instruments</code> -&gt; <code>KCSAN: dynamic data race detector</code></p><p>If you don&#39;t see it in the menu at all, don&#39;t curse just yet—check whether all the dependency conditions above are met. The easiest way is to set this up in a fairly recent x86_64 Ubuntu virtual machine (e.g., 21.10).</p><p>After entering the KCSAN submenu, you will see a bunch of parameters. The defaults are generally conservative and suitable for daily use. Table 8.1 lists some key parameters (for the sake of brevity, we won&#39;t paste a screenshot here, but will describe the core logic in text):</p><p><strong>CONFIG_KCSAN_ASSUME_PLAIN_WRITES_ATOMIC</strong></p><ul><li><strong>Default</strong>: <code>y</code></li><li><strong>Effect</strong>: Assumes aligned plain writes are atomic. This is the source of much confusion for beginners, and we will specifically address it when we step into the pitfalls later.</li></ul><p><strong>CONFIG_KCSAN_REPORT_VALUE_CHANGE_ONLY</strong></p><ul><li><strong>Default</strong>: <code>y</code></li><li><strong>Effect</strong>: Only reports when the race actually causes the data to change. This filters out some harmless, theoretical races.</li></ul><p><strong>CONFIG_KCSAN_SKIP_WATCH</strong></p><ul><li><strong>Default</strong>: <code>4000</code></li><li><strong>Effect</strong>: This is the ultimate knob affecting performance. It means that out of every 4000 memory accesses, KCSAN will only sample one. The smaller you set this, the more accurately it catches bugs, but the more severe the system stuttering becomes.</li></ul><h3 id="hands-on-catching-a-simple-data-race" tabindex="-1">Hands-on: Catching a Simple Data Race <a class="header-anchor" href="#hands-on-catching-a-simple-data-race" aria-label="Permalink to &quot;Hands-on: Catching a Simple Data Race&quot;">​</a></h3><p>Enough theory. Let&#39;s get our hands dirty.</p><p>We wrote a simple kernel module <code>ch8/kcsan_datarace</code> to demonstrate this mechanism. The code initializes two workqueues, running <code>do_the_work1</code> and <code>do_the_work2</code> respectively. If we enable <code>race_2plain_w=y</code> via a module parameter, these two functions will frantically read and write the same global variable <code>gctx-&gt;data</code>, with <strong>absolutely no lock protection</strong>.</p><p>This is textbook data race code:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ch8/kcsan_datarace.c</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> do_the_work1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> work_struct </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">work1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i; u64 bogus </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 32000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    PRINT_CTX</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (race_2plain_w) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        pr_info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;data race: 2 plain writes:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (i</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; i</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">iter1; i</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">++</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            gctx-&gt;data </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (u64)bogus </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i; </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">             /* 无保护的普通写操作 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> do_the_work2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> work_struct </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">work2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i; u64 bogus </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 98000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    PRINT_CTX</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (race_2plain_w) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        pr_info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;data race: 2 plain writes:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (i</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; i</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">iter2; i</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">++</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">           gctx-&gt;data </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (u64)gctx-&gt;y </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i; </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">             /* 无保护的普通写操作 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br><span class="line-number">24</span><br></div></div><h4 id="the-first-pitfall-why-isn-t-it-reporting" tabindex="-1">The First Pitfall: Why Isn&#39;t It Reporting? <a class="header-anchor" href="#the-first-pitfall-why-isn-t-it-reporting" aria-label="Permalink to &quot;The First Pitfall: Why Isn&#39;t It Reporting?&quot;">​</a></h4><p>You insert the module and stare at the logs—KCSAN makes <strong>not a peep</strong>.</p><p>This is counterintuitive. Two threads are frantically writing to the same address without locks, and it doesn&#39;t report? Is KCSAN just slacking off?</p><p>No. Remember that <code>CONFIG_KCSAN_ASSUME_PLAIN_WRITES_ATOMIC</code> config option from earlier? It defaults to <code>y</code>.</p><p>Under this default assumption, KCSAN considers aligned plain writes to be atomic. Since they are &quot;atomic,&quot; a write-write collision, while not conforming to the strict LKMM definition, is not considered a mandatory error in KCSAN&#39;s &quot;relaxed mode&quot; (especially if <code>CONFIG_KCSAN_REPORT_VALUE_CHANGE_ONLY</code> is also on and the data doesn&#39;t look corrupted).</p><h4 id="fixing-the-configuration-to-reproduce-the-bug" tabindex="-1">Fixing the Configuration to Reproduce the Bug <a class="header-anchor" href="#fixing-the-configuration-to-reproduce-the-bug" aria-label="Permalink to &quot;Fixing the Configuration to Reproduce the Bug&quot;">​</a></h4><p>Now, let&#39;s play the strict engineer. Go back to the kernel source directory, run <code>make menuconfig</code>, find <code>KCSAN_ASSUME_PLAIN_WRITES_ATOMIC</code>, and <strong>turn it off</strong> (set to <code>n</code>).</p><p>This means: KCSAN, stop making assumptions. Any unmarked concurrent write that collides is a bug.</p><p>Recompile the kernel and reboot.</p><p>Insert our test module again. This time, the console immediately explodes with a report (Figure 8.5 is a screenshot of it):</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BUG: KCSAN: data-race in do_the_work1 / do_the_work2</span></span>
<span class="line"><span></span></span>
<span class="line"><span>write to 0xffff9fc3cc9e3238 of 8 bytes by task kworker/0:1 on cpu 0:</span></span>
<span class="line"><span> do_the_work1+0x...</span></span>
<span class="line"><span> process_one_work+0x...</span></span>
<span class="line"><span> ...</span></span>
<span class="line"><span></span></span>
<span class="line"><span>write to 0xffff9fc3cc9e3238 of 8 bytes by task kworker/1:2 on cpu 1:</span></span>
<span class="line"><span> do_the_work2+0x...</span></span>
<span class="line"><span> process_one_work+0x...</span></span>
<span class="line"><span> ...</span></span>
<span class="line"><span>Reported by Kernel Concurrency Sanitizer on:</span></span>
<span class="line"><span>...</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><h4 id="how-to-read-this-report" tabindex="-1">How to Read This Report <a class="header-anchor" href="#how-to-read-this-report" aria-label="Permalink to &quot;How to Read This Report&quot;">​</a></h4><p>KCSAN&#39;s report is actually quite structured. Let&#39;s break it down:</p><ol><li><p><strong>First line (red header)</strong>: <code>BUG: KCSAN: data-race in func_x / func_y</code> This tells you directly: <code>func_x</code> and <code>func_y</code> are clashing. In our example, these are <code>do_the_work1</code> and <code>do_the_work2</code>, but it will often show the underlying wrapper functions (like <code>process_one_work</code>, because workqueues dispatch tasks through this function).</p></li><li><p><strong>Access info</strong>: <code>write to 0xffff... of 8 bytes by task &lt;PID&gt; on cpu &lt;ID&gt;</code> This line tells you:</p><ul><li>Whether it&#39;s a read or a write (here it is <code>write</code>).</li><li>Whether it&#39;s marked (if it were <code>read/write (marked)</code>, it would mean something like <code>READ_ONCE</code> was used; here it is unmarked).</li><li>The kernel virtual address (confirm if it&#39;s your global variable).</li><li>Who did it (which task, which CPU).</li></ul></li><li><p><strong>Call stack</strong>: Immediately following the access info is the complete stack trace. This is what you need the most; follow the stack upwards to locate the exact line of code.</p></li></ol><h3 id="the-runtime-statistics-game" tabindex="-1">The Runtime Statistics Game <a class="header-anchor" href="#the-runtime-statistics-game" aria-label="Permalink to &quot;The Runtime Statistics Game&quot;">​</a></h3><p>Since KCSAN is based on statistical sampling, catching a bug involves a bit of luck.</p><p>To verify this, the original author wrote a simple Shell script <code>tester.sh</code> that tests how many runs are roughly needed to trigger a report by varying the loop count.</p><p>The conclusion is intuitive: the more loops and the denser the accesses, the higher the probability of triggering a KCSAN watchpoint. However, due to the <code>CONFIG_KCSAN_REPORT_ONCE_IN_MS</code> limit (default 3000ms, meaning it only reports once every 3 seconds), you might find that even after many runs, only the first occurrence is logged.</p><p>Additionally, the kernel comes with a more professional test module <code>CONFIG_KCSAN_TEST</code>. If enabled, it compiles a <code>kcsan-test.ko</code>. This module uses the KUnit and Torture frameworks to intentionally create all sorts of tricky concurrent scenarios to stress the system. A single run takes about 7 minutes; if you run it without a panic, it means your KCSAN environment is basically stable.</p><h3 id="runtime-control-the-debugfs-interface" tabindex="-1">Runtime Control: The debugfs Interface <a class="header-anchor" href="#runtime-control-the-debugfs-interface" aria-label="Permalink to &quot;Runtime Control: The debugfs Interface&quot;">​</a></h3><p>Besides compile-time configuration, you can also interact with KCSAN at runtime via debugfs (root privileges required, of course):</p><p>File path: <code>/sys/kernel/debug/kcsan</code></p><ul><li><strong>echo &quot;on&quot; &gt; .../kcsan</strong>: Enable KCSAN (it is on by default).</li><li><strong>echo &quot;off&quot; &gt; .../kcsan</strong>: Disable KCSAN. If you feel the current performance overhead is too high, you can temporarily turn it off.</li><li><strong>cat .../kcsan</strong>: View current statistics (e.g., how many accesses have been checked, how many races have been caught).</li></ul><h3 id="the-right-attitude-toward-reports-don-t-rush-to-judgment" tabindex="-1">The Right Attitude Toward Reports: Don&#39;t Rush to Judgment <a class="header-anchor" href="#the-right-attitude-toward-reports-don-t-rush-to-judgment" aria-label="Permalink to &quot;The Right Attitude Toward Reports: Don&#39;t Rush to Judgment&quot;">​</a></h3><p>This is the most important piece of advice.</p><p>When you see KCSAN report a data race, <strong>do not</strong> reflexively slap a <code>READ_ONCE()</code> or <code>WRITE_ONCE()</code> on it, thinking &quot;as long as the warning goes away, we&#39;re good.&quot;</p><h4 id="why-should-you-hold-back" tabindex="-1">Why Should You Hold Back? <a class="header-anchor" href="#why-should-you-hold-back" aria-label="Permalink to &quot;Why Should You Hold Back?&quot;">​</a></h4><p>This approach masks the problem rather than solving it.</p><p>KCSAN&#39;s design philosophy is: <strong>accesses to shared variables should not race in the first place</strong>. If you access shared data without a lock, that is a logic bug. If you simply mark the access as <code>READ_ONCE</code>, you are just telling KCSAN &quot;shut up, I know this is a race,&quot; but the risk of data inconsistency remains. The correct approach should be:</p><ol><li>Add lock protection.</li><li>Or use atomic operations.</li><li>Or use lock-free techniques.</li></ol><h4 id="when-can-you-use-data-race" tabindex="-1">When Can You Use <code>data_race()</code>? <a class="header-anchor" href="#when-can-you-use-data-race" aria-label="Permalink to &quot;When Can You Use \`data_race()\`?&quot;">​</a></h4><p>Of course, there are exceptions to everything. If you are indeed writing statistics code, diagnostics code, or some logic that intentionally sacrifices consistency for performance (e.g., reading an unprotected statistics counter where it doesn&#39;t matter if you get 100 or 101), then you can use the <code>data_race()</code> macro.</p><p>It explicitly tells KCSAN (and anyone reading the code): <strong>I know there is a race here, but it is benign, so leave it alone</strong>.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 示例：内核 fork.c 中的代码</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 这是一个防 fork 炸弹的检查，稍微不准一点（竞态）也没关系 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">data_race</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(nr_threads </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> max_threads))</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    goto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> bad_fork_cleanup_count;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Additionally, if you feel a specific function is too hot and the instrumentation is causing a performance drop, you can mark the entire function with the <code>__no_kcsan</code> attribute, though this is usually done to isolate known false positives or third-party code.</p><h3 id="kcsan-s-unique-skill-advisory-lock-detection" tabindex="-1">KCSAN&#39;s Unique Skill: Advisory Lock Detection <a class="header-anchor" href="#kcsan-s-unique-skill-advisory-lock-detection" aria-label="Permalink to &quot;KCSAN&#39;s Unique Skill: Advisory Lock Detection&quot;">​</a></h3><p>Traditional lock debugging tools (like Lockdep) mainly detect issues like deadlocks and lock dependency errors. But they have a blind spot: <strong>Advisory Locks</strong>.</p><p>If you hold a lock but forget to use it to protect access to a shared variable, the access will still succeed (because the kernel doesn&#39;t enforce physical memory isolation), but it will cause a data race. Lockdep is powerless here because it only checks if your lock acquisition order is correct.</p><p>KCSAN is extremely powerful in this regard. Through the <code>ASSERT_EXCLUSIVE*()</code> macro, it can verify whether a piece of code is truly holding a lock exclusively when accessing memory. If a concurrent write occurs without holding a lock, KCSAN will still catch you. This is why we say KCSAN can discover deep-seated concurrency issues through compiler instrumentation without relying on lock semantics.</p><h3 id="chapter-summary-from-tools-to-practice" tabindex="-1">Chapter Summary: From Tools to Practice <a class="header-anchor" href="#chapter-summary-from-tools-to-practice" aria-label="Permalink to &quot;Chapter Summary: From Tools to Practice&quot;">​</a></h3><p>At this point, our understanding of KCSAN should be quite comprehensive. It is not just an error-reporting tool, but a mentor that helps us understand kernel concurrency semantics.</p><p>Now that we have mastered how to discover concurrency bugs, the next section will move into real-world practice. We will analyze several real kernel bugs caused by lock deficiencies. We will see how these lurking crises were introduced in the code of core developers, and how they were ultimately fixed.</p>`,83)])])}const k=s(n,[["render",o]]);export{u as __pageData,k as default};
