import{_ as i,o as e,c as a,a as t}from"./chunks/framework.8tiMeCK-.js";const c=JSON.parse('{"title":"4.2 Receiving IPv4 Packets","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch04_2.md","filePath":"en/notes/linux_kernel_networking/ch04_2.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_networking/ch04_2.md"};function h(l,s,r,p,o,k){return e(),a("div",null,[...s[0]||(s[0]=[t(`<h1 id="_4-2-receiving-ipv4-packets" tabindex="-1">4.2 Receiving IPv4 Packets <a class="header-anchor" href="#_4-2-receiving-ipv4-packets" aria-label="Permalink to &quot;4.2 Receiving IPv4 Packets&quot;">​</a></h1><p>In the previous section, we covered the &quot;registration&quot; process of IPv4 — the kernel hangs <code>ip_packet_type</code> on the global protocol list and sets <code>ip_rcv()</code> as the callback function. When the NIC delivers a packet and the Ethernet type is <code>0x0800</code>, the kernel heads straight for this function.</p><p>There is a very intuitive assumption here: since this is a &quot;receive function,&quot; it should be responsible for unpacking the packet, processing it, and passing it up the stack, right?</p><p><strong>Exactly the opposite.</strong></p><p>The true identity of <code>ip_rcv()</code> is more like a <strong>gatekeeper</strong>. It doesn&#39;t care whether the payload is TCP or UDP; it cares about exactly one thing: is this a valid IPv4 packet? If yes, let it through; if no, drop it. The real &quot;processing&quot; work is actually handed off to the next runner in the relay, <code>ip_rcv_finish()</code>. And sandwiched between these two runners is an extremely critical checkpoint — Netfilter hooks.</p><h3 id="entering-the-reception-hall" tabindex="-1">Entering the Reception Hall <a class="header-anchor" href="#entering-the-reception-hall" aria-label="Permalink to &quot;Entering the Reception Hall&quot;">​</a></h3><p>Let&#39;s look at the specific workflow of this &quot;gatekeeper.&quot;</p><p>First, the function signature:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ip_rcv</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">skb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">dev</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">           struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> packet_type </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">pt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">orig_dev</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> iphdr </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">iph;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>When <code>ip_rcv()</code> gets the <code>skb</code>, the network layer has just stripped off the Ethernet header. Now, <code>skb-&gt;data</code> points to the IPv4 header. First, we need to extract the header and see if it looks &quot;normal.&quot;</p><h4 id="step-1-header-format-sanity-checks" tabindex="-1">Step 1: Header Format Sanity Checks <a class="header-anchor" href="#step-1-header-format-sanity-checks" aria-label="Permalink to &quot;Step 1: Header Format Sanity Checks&quot;">​</a></h4><p>The IPv4 header has a strict format. The RFC specifies that the header must be at least 20 bytes, and the version number must be 4.</p><p>In the kernel, the <code>ihl</code> field of <code>struct iphdr</code> represents the header length, but there&#39;s a catch — its unit is not bytes, but <strong>4 bytes (32 bits)</strong>. So, for a standard 20-byte header, <code>ihl</code> should be 5 (5 x 4 = 20).</p><p>If someone sends a packet where <code>ihl</code> is less than 5, it means the packet can&#39;t even piece together a basic header; or if <code>version</code> is not 4 — for example, this is an IPv6 packet that ended up in the IPv4 slot. For these malformed packets, the kernel&#39;s attitude is straightforward: drop them immediately and update the <code>IPSTATS_MIB_INHDRERRORS</code> statistic.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 提取 IP 头部 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    iph </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ip_hdr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 检查：头部长度够不够？版本号对不对？ */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (iph</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ihl </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 5</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ||</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> iph</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">version </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        goto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> inhdr_error;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><h4 id="step-2-checksum" tabindex="-1">Step 2: Checksum <a class="header-anchor" href="#step-2-checksum" aria-label="Permalink to &quot;Step 2: Checksum&quot;">​</a></h4><p>After passing the format check, the next step is the &quot;anti-counterfeit mark&quot; — the checksum. Per RFC 1122 requirements, a host <strong>must</strong> perform a header checksum on every received datagram and silently drop packets that fail the check. There is no need to send an error message back, because it would be useless anyway — the link layer has a problem of its own.</p><p>The kernel uses <code>ip_fast_csum()</code> to do this. Note that this calculation applies <strong>only to the IP header</strong>, not the subsequent data payload.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 校验和计算：失败则返回非 0 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">unlikely</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip_fast_csum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((u8 </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">iph</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, iph</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ihl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)))</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        goto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> inhdr_error;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><h4 id="step-3-let-it-through-or-intercept-it" tabindex="-1">Step 3: Let It Through or Intercept It? <a class="header-anchor" href="#step-3-let-it-through-or-intercept-it" aria-label="Permalink to &quot;Step 3: Let It Through or Intercept It?&quot;">​</a></h4><p>If the packet is healthy, <code>ip_rcv()</code>&#39;s job is basically done. Next, it calls the <code>NF_HOOK</code> macro.</p><p>This line of code is the bridge connecting &quot;security check&quot; and &quot;sorting&quot;:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> NF_HOOK</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(NFPROTO_IPV4, NF_INET_PRE_ROUTING, skb, dev, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">NULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                   ip_rcv_finish);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Let&#39;s expand on this a bit, because <code>NF_HOOK</code> is everywhere in the network stack.</p><p><strong>Netfilter Hooks</strong>: You can think of this as a &quot;security checkpoint socket&quot; inside the kernel. The kernel allows you to insert your own code at five key points of a packet&#39;s journey (such as iptables, nf_conntrack). We are at the <strong><code>NF_INET_PRE_ROUTING</code></strong> hook point here — meaning the packet has just entered the stack and no routing decision has been made yet.</p><p>The logic of <code>NF_HOOK</code> is simple:</p><ol><li>Check if anyone has registered a callback function at this point.</li><li>If so, call them.</li><li>If no one has registered, or if the registered callback returns &quot;accept&quot; (<code>NF_ACCEPT</code>), continue by calling the function specified by the last argument — which, in this case, is <code>ip_rcv_finish()</code>.</li></ol><p>If the callback function returns &quot;drop&quot; (<code>NF_DROP</code>), the packet&#39;s life ends here, and <code>ip_rcv_finish()</code> will never be called. There is also a return value called <code>NF_STOLEN</code>, which means the hook function &quot;stole&quot; the packet, and the subsequent legitimate path is no longer executed.</p><p>For the sake of a smooth explanation, let&#39;s temporarily assume there are no Netfilter rules interfering, the packet gets a green light all the way, and goes straight into <code>ip_rcv_finish()</code>.</p><h3 id="the-real-work-begins-ip-rcv-finish" tabindex="-1">The Real Work Begins: ip_rcv_finish() <a class="header-anchor" href="#the-real-work-begins-ip-rcv-finish" aria-label="Permalink to &quot;The Real Work Begins: ip_rcv_finish()&quot;">​</a></h3><p><code>ip_rcv</code> only &quot;looks,&quot; while <code>ip_rcv_finish</code> actually &quot;does.&quot;</p><p>The core task of this section is to determine the fate of this packet: <strong>keep it for ourselves, or forward it for someone else?</strong> The sole basis for this answer is the <strong>routing table</strong>. Before the routing lookup, all we have is an SKB; after the lookup, the SKB will have a crucial new attachment — <strong><code>dst_entry</code></strong>.</p><p>The code logic looks like this:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ip_rcv_finish</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">skb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> iphdr </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">iph </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ip_hdr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rtable </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">rt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // rtable 是路由表项的结构体</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 如果 SKB 上还没挂 路由结果，就去查一下表 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">skb_dst</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb)) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> err </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ip_route_input_noref</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb, iph-&gt;daddr, iph-&gt;saddr,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                                       iph-&gt;tos, skb-&gt;dev);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">unlikely</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(err)) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            /* 这里有个特殊情况：Reverse Path Filter (RPF) */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (err </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">EXDEV)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">                NET_INC_STATS_BH</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">dev_net</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb-&gt;dev), LINUX_MIB_IPRPFILTER);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            goto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> drop;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* ... 省略了统计更新 ... */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> dst_input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br></div></div><h4 id="routing-lookup-determining-fate" tabindex="-1">Routing Lookup: Determining Fate <a class="header-anchor" href="#routing-lookup-determining-fate" aria-label="Permalink to &quot;Routing Lookup: Determining Fate&quot;">​</a></h4><p><strong><code>ip_route_input_noref()</code></strong> is the core function for routing queries. It takes three key pieces of information to ask the routing subsystem:</p><ol><li>Destination address (<code>daddr</code>)</li><li>Source address (<code>saddr</code>)</li><li>Type of Service (<code>tos</code>)</li></ol><p>After consulting the table, the routing subsystem binds a <code>dst</code> object to this SKB. This <code>dst</code> object is extremely critical; it not only contains next-hop information, but more importantly, it <strong>carries an action directive</strong>.</p><p>This directive is hidden in the <code>dst-&gt;input</code> callback function pointer:</p><ul><li><strong>If it&#39;s destined for the local machine</strong>: The lookup result sets <code>dst-&gt;input</code> to <strong><code>ip_local_deliver()</code></strong>. This means: &quot;This one is ours, send it up to the transport layer.&quot;</li><li><strong>If it needs to be forwarded</strong>: The lookup result sets <code>dst-&gt;input</code> to <strong><code>ip_forward()</code></strong>. This means: &quot;This is just passing through, help send it next door.&quot;</li><li><strong>If it&#39;s multicast</strong> (special scenario): Under certain conditions, it will be set to <strong><code>ip_mr_input()</code></strong>, entering the multicast handling logic.</li></ul><p>The final <code>dst_input(skb)</code> actually just executes that function pointer:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> inline</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> dst_input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">skb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> skb_dst</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb)-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>This is a classic use of C polymorphism — the routing table queries for &quot;data,&quot; but what it returns is &quot;code.&quot;</p><h4 id="that-weird-exdev-rpf" tabindex="-1">That Weird -EXDEV (RPF) <a class="header-anchor" href="#that-weird-exdev-rpf" aria-label="Permalink to &quot;That Weird -EXDEV (RPF)&quot;">​</a></h4><p>In the code above, there is a check for <code>err == -EXDEV</code>. This is actually an error from a security mechanism called <strong>Reverse Path Filter (RPF)</strong>.</p><p>Its logic goes like this:</p><blockquote><p>&quot;This packet claims it was sent from IP A and came in via interface eth0. But if I check my own routing table, to send a packet to IP A, I should be going out through eth1. Since the incoming path and the return path don&#39;t match, there&#39;s something wrong with this packet (it might be a spoofed-source attack), so drop it!&quot;</p></blockquote><p>If you have RPF enabled (usually configured in procfs), the kernel will return an <code>-EXDEV</code> error during this table lookup and increment the <code>LINUX_MIB_IPRPFILTER</code> counter.</p><h3 id="handling-ip-options" tabindex="-1">Handling IP Options <a class="header-anchor" href="#handling-ip-options" aria-label="Permalink to &quot;Handling IP Options&quot;">​</a></h3><p>Inside <code>ip_rcv_finish</code>, there is also an easily overlooked piece of logic that handles the <strong>options</strong> in the IPv4 header.</p><p>Remember the <code>ihl</code> field in the header? Normally it&#39;s 5 (20 bytes). If it&#39;s greater than 5, it means there are additional option fields following it (such as Record Route, Timestamp, etc.).</p><p>Although IP options are rarely used in modern networks (because they cause router performance degradation), the kernel still has to support them:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 如果头部长度大于 5，说明有选项，需要解析 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (iph</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ihl </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 5</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ip_rcv_options</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(skb))</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        goto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> drop;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><code>ip_rcv_options()</code> reads out these options and performs necessary processing (such as updating routing records). If the option format is wrong, it will return non-zero here, causing the packet to be dropped.</p><h3 id="the-dust-settles" tabindex="-1">The Dust Settles <a class="header-anchor" href="#the-dust-settles" aria-label="Permalink to &quot;The Dust Settles&quot;">​</a></h3><p>After going through all the checks above:</p><ol><li>The header format is fine.</li><li>The checksum passed.</li><li>The route was found (deciding whether to receive locally or forward).</li><li>The IP options (if any) have been processed.</li></ol><p>At this point, the packet is finally handed off to the next stop.</p><ul><li>If it&#39;s <strong>local delivery</strong>: It enters <code>ip_local_deliver()</code>, where fragment reassembly is handled (if applicable), and finally the TCP or UDP data is thrown to the transport layer.</li><li>If it&#39;s <strong>forwarding</strong>: It enters <code>ip_forward()</code>, the TTL is decremented by 1, the checksum is recalculated, and then a table lookup sends it out through the egress NIC.</li><li>If it&#39;s <strong>multicast</strong>: It enters <code>ip_mr_input()</code>, handed over to the Multicast Forwarding Cache (MFC) for processing.</li></ul><p>It looks like a lot of steps, but under a gigabit NIC driver, this entire process runs in microseconds. Every packet is like a part on an assembly line, strictly passing through every checkpoint seamlessly. If any link <code>goto drop</code>, this packet simply vanishes from the world, leaving only the cold counter increments in <code>/proc/net/snmp</code>.</p><p>In the next section, we&#39;ll look at how packets judged to be &quot;forwarded&quot; go through TTL decay, checksum recalculation, and are finally sent out of this host.</p>`,61)])])}const g=i(n,[["render",h]]);export{c as __pageData,g as default};
