import{_ as s,o as t,c as a,a as i}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch08_3.md","filePath":"en/notes/linux_kernel_networking/ch08_3.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_networking/ch08_3.md"};function o(r,e,l,h,d,p){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h2 id="_8-3-ipv6-header" tabindex="-1">8.3 IPv6 Header <a class="header-anchor" href="#_8-3-ipv6-header" aria-label="Permalink to &quot;8.3  IPv6 Header&quot;">​</a></h2><p>In the previous section, we solved the &quot;where to&quot; problem — we got the address and joined the Solicited-Node multicast group. Now, it&#39;s time to tackle the &quot;how to get there&quot; problem. But before we actually take a step, we need to understand what an IPv6 packet looks like.</p><p>This isn&#39;t just a matter of looking at a diagram and filling in the blanks. The IPv6 header design wasn&#39;t merely about accommodating those 128-bit extra-long addresses; it was a major &quot;surgery&quot; to shed the historical baggage of IPv4. Some features were removed, some were moved out, and others took on forms you might not expect.</p><h3 id="removing-the-safety-net" tabindex="-1">Removing the Safety Net <a class="header-anchor" href="#removing-the-safety-net" aria-label="Permalink to &quot;Removing the Safety Net&quot;">​</a></h3><p>Every IPv6 packet opens with the IPv6 header. This header has a <strong>fixed length of 40 bytes</strong>.</p><p>Pause for a moment and appreciate that word &quot;fixed.&quot; In the IPv4 era, the header length was variable (because of Options), so an <code>IHL</code> (Internet Header Length) field was mandatory to tell the kernel &quot;how long this header is.&quot; With IPv6, that field is gone. 40 bytes, set in stone.</p><p>There&#39;s an even more radical change: the IPv6 header has <strong>no Checksum</strong>.</p><p>In IPv4, every time a packet passed through a router, the TTL was decremented by 1, and the router had to faithfully recalculate the checksum for the entire header. Back when CPUs were single-core and clocked at a few hundred megahertz, this was a non-trivial overhead. IPv6 simply chopped this overhead away: the checksum responsibility was offloaded to Layer 2 (the link layer, e.g., CRC) and Layer 4 (the transport layer, e.g., TCP/UDP checksums).</p><p>This has a direct consequence: when a router forwards an IPv6 packet, modifying the <code>hop_limit</code> (the equivalent of IPv4&#39;s TTL) does not require recalculating a checksum. On a pure software router, this translates to a very real performance boost.</p><p>Of course, there&#39;s a side effect: in IPv4, UDP could set its checksum to 0 to mean &quot;no checksum.&quot; But in IPv6, except for a very small number of special tunneling scenarios (RFC 6935), UDP <strong>must</strong> have checksums enabled. Because if the data gets corrupted in transit, there&#39;s no safety net to catch it.</p><h3 id="header-structure-the-split-traffic-class" tabindex="-1">Header Structure: The Split Traffic Class <a class="header-anchor" href="#header-structure-the-split-traffic-class" aria-label="Permalink to &quot;Header Structure: The Split Traffic Class&quot;">​</a></h3><p>Let&#39;s look at the definition in the kernel. There&#39;s a subtle detail here that&#39;s easy to miss on the first read:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ipv6hdr {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#if</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> defined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__LITTLE_ENDIAN_BITFIELD</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        __u8                    priority:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                                version:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#elif</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> defined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__BIG_ENDIAN_BITFIELD</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        __u8                    version:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                                priority:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#else</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#error</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;Please fix &lt;asm/byteorder.h&gt;&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#endif</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        __u8                    </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">flow_lbl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">];</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        __be16                  payload_len;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        __u8                    nexthdr;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        __u8                    hop_limit;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  in6_addr        saddr;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  in6_addr        daddr;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br></div></div><p>(include/uapi/linux/ipv6.h)</p><p>Notice the first byte. The standard document (RFC 2460) specifies 4 bits of <code>Version</code> + 8 bits of <code>Traffic Class</code> + 20 bits of <code>Flow Label</code>.</p><p>But in Linux&#39;s implementation, this first byte is split into <code>priority:4</code> and <code>version:4</code>. Where did the remaining 4 bits of <code>Traffic Class</code> go? They were stuffed into the first bit of the <code>flow_lbl</code> array.</p><p>In other words, it takes the <code>priority</code> in the code plus the upper 4 bits of <code>flow_lbl[0]</code> to piece together a complete 8-bit <code>Traffic Class</code>. This is a classic bitfield operation done to save memory width.</p><p>Let&#39;s break down each field in this structure and see what role it plays in the IPv6 world:</p><ul><li><p><strong><code>version</code> (4 bit)</strong>: Must be 6. Nothing more to say — it&#39;s the protocol&#39;s ID card.</p></li><li><p><strong><code>priority</code> (4 bit) / <code>Traffic Class</code> (8 bit)</strong>: This is the Traffic Class. Although RFC 2460 defined this field, it didn&#39;t specify what each value means — that part was left for the DiffServ (QoS) mechanisms to define. The kernel code keeping the <code>priority</code> name actually has a bit of a legacy flavor to it.</p></li><li><p><strong><code>flow_lbl</code> (20 bit)</strong>: <strong>Flow Label</strong>. This is a field that was marked as &quot;experimental&quot; back in the RFC 2460 days and didn&#39;t get a clear specification until RFC 6437 in 2011. Its purpose is to tag a series of packets belonging to the same &quot;flow.&quot; When a router sees this label, it can theoretically skip looking up the 5-tuple and do fast forwarding directly based on the label. RFC 6437 even suggests using it to detect address spoofing. However, in today&#39;s general internet, you rarely see it deployed at scale.</p></li><li><p><strong><code>payload_len</code> (16 bit)</strong>: Payload Length. This <strong>does not include</strong> the fixed 40-byte header. 16 bits means the maximum length is 65,535 bytes. If this is exceeded (e.g., with Jumbo Frames), this field must be set to 0, and the Jumbo Payload option in the Hop-by-Hop extension header takes over. We&#39;ll dive into this in the next section.</p></li><li><p><strong><code>nexthdr</code> (8 bit)</strong>: <strong>Next Header</strong>. This is an extremely critical design. If there are no extension headers, it&#39;s the upper-layer protocol number: <code>IPPROTO_TCP</code> (6), <code>IPPROTO_UDP</code> (17), and so on. If there are extension headers, it points to the type of the extension header immediately following the IPv6 header. It&#39;s like the <code>next</code> pointer in a linked list node.</p></li><li><p><strong><code>hop_limit</code> (8 bit)</strong>: Hop Limit. Decremented by 1 at each router. When it hits 0, the packet is dropped, and an ICMPv6 Time Exceeded message is sent back. This is the mechanism that prevents packets from getting lost and looping forever in the network.</p></li><li><p><strong><code>saddr</code> / <code>daddr</code> (128 bit)</strong>: Source Address and Destination Address. There&#39;s a small detail here: if a <strong>Routing Header</strong> (routing extension header) is used, the <code>daddr</code> might not be the final destination, but rather the &quot;next hop&quot; along the path. It&#39;s like writing a shipping label where the recipient isn&#39;t your friend, but some transit station.</p></li></ul><h3 id="saying-goodbye-to-ipv4" tabindex="-1">Saying Goodbye to IPv4 <a class="header-anchor" href="#saying-goodbye-to-ipv4" aria-label="Permalink to &quot;Saying Goodbye to IPv4&quot;">​</a></h3><p>You&#39;ll notice that a lot of IPv4&#39;s baggage is missing here: no Header Length, no Checksum, no Options.</p><p>IPv4&#39;s Options mechanism was criticized for a long time due to performance issues (causing variable-length headers that are hard to accelerate in hardware). IPv6 completely replaced it with <strong>Extension Headers</strong> — a more elegant and flexible &quot;chained&quot; approach.</p><p>But we won&#39;t rush into the details of extension headers just yet. For now, staring at this fixed 40-byte structure, you should be able to feel a shift in design philosophy: <strong>from a complex variable-length structure, back to a fixed, fast, and predictable simple skeleton</strong>. For the purity of these 40 bytes, IPv6 was even willing to push the &quot;checksum&quot; responsibility onto others.</p><p>In the next section, we&#39;ll attach the &quot;limbs&quot; to this simple skeleton — those functionally diverse, end-to-end linked Extension Headers. Then you&#39;ll discover how the tiny <code>nexthdr</code> field single-handedly supports the extensibility of the entire network stack.</p>`,24)])])}const k=s(n,[["render",o]]);export{u as __pageData,k as default};
