import{_ as t,o,c as n,a as i}from"./chunks/framework.8tiMeCK-.js";const g=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch02_7.md","filePath":"en/notes/linux_kernel_networking/ch02_7.md","lastUpdated":null}'),s={name:"en/notes/linux_kernel_networking/ch02_7.md"};function r(a,e,l,c,d,u){return o(),n("div",null,[...e[0]||(e[0]=[i('<h2 id="_2-7-quick-reference" tabindex="-1">2.7 Quick Reference <a class="header-anchor" href="#_2-7-quick-reference" aria-label="Permalink to &quot;2.7  Quick Reference&quot;">​</a></h2><p>The most frustrating part of writing code isn&#39;t not understanding the theory—it&#39;s knowing the theory but forgetting that damn function&#39;s name, or what parameters it actually takes.</p><p>In this chapter, we dissected the skeleton of Netlink, the muscle of Generic Netlink, and even traced the veins of RTNL. Now, as we return to our editors ready to type that first line of code, we need a &quot;cheat sheet&quot;—not some hollow document that just lists function names, but a quick reference that tells you &quot;what dirty work this function actually does.&quot;</p><p>I&#39;ve put together the core APIs we interact with most in this chapter (and across the kernel source tree). Rather than flipping through hundreds of lines of <code>include/linux/netlink.h</code>, just look here.</p><p>Note: This isn&#39;t a cold, dry API manual. I&#39;ve annotated the easily overlooked behavioral details right alongside them—like whether it actually validates the length for you, or whether it automatically sends an ACK.</p><hr><h3 id="_2-7-1-core-netlink-helper-functions" tabindex="-1">2.7.1 Core Netlink Helper Functions <a class="header-anchor" href="#_2-7-1-core-netlink-helper-functions" aria-label="Permalink to &quot;2.7.1  Core Netlink Helper Functions&quot;">​</a></h3><p>These are the infrastructure functions for Netlink communication, handling the common logic from socket creation to message encapsulation.</p><p><strong><code>struct sock *netlink_kernel_create(struct net *net, int unit, struct netlink_kernel_cfg *cfg)</code></strong></p><ul><li><strong>Purpose</strong>: Creates a Netlink socket in kernel space. This is the starting point for all kernel-side Netlink communication.</li><li><strong>Parameters</strong>: <code>unit</code> is the protocol number (e.g., <code>NETLINK_GENERIC</code>), and <code>cfg</code> is the configuration structure (where you plug in your callback functions).</li><li><strong>Details</strong>: Returns a <code>sock</code> pointer on success, or NULL on failure. When it fails, it&#39;s usually due to a protocol number conflict or insufficient memory.</li></ul><p><strong><code>int netlink_rcv_skb(struct sk_buff *skb, int (*cb)(struct sk_buff *, struct nlmsghdr *))</code></strong></p><ul><li><strong>Purpose</strong>: The &quot;standard butler&quot; for receiving Netlink messages. It gets called inside your input callback.</li><li><strong>Why we need it</strong>: It handles all the tedious dirty work for you—checking if the message header length is out of bounds (<code>NLMSG_HDRLEN</code>), skipping control messages (like <code>NLMSG_ERROR</code>), and handling the <code>NLM_F_ACK</code> flag (if the user requests a reply, it automatically calls <code>netlink_ack()</code> to send an error packet).</li><li><strong>Your job</strong>: You just need to stuff your actual business logic into the <code>cb</code> callback function you pass to it. If <code>cb</code> returns a non-zero value, <code>netlink_rcv_skb</code> considers it an error and sends an ERROR message back.</li></ul><p><strong><code>struct sk_buff *nlmsg_new(size_t payload, gfp_t flags)</code></strong></p><ul><li><strong>Purpose</strong>: Allocates a new Netlink message buffer (<code>sk_buff</code>).</li><li><strong>Details</strong>: Internally, it calls <code>alloc_skb()</code>. If you specify <code>payload</code> as 0, it&#39;s smart enough to allocate at least an aligned message header length (<code>NLMSG_HDRLEN</code>). This prevents you from corrupting memory before you even fill in the message header.</li></ul><p><strong><code>void *nlmsg_put(struct sk_buff *skb, u32 portid, u32 seq, int type, int len, int flags)</code></strong></p><ul><li><strong>Purpose</strong>: Constructs a Netlink message header and places it into the data area of <code>skb</code>.</li><li><strong>Details</strong>: It calls <code>__nlmsg_put()</code>. If there isn&#39;t enough space (insufficient tail room in <code>skb</code>), it returns NULL and discards <code>skb</code>.</li><li><strong>Tip</strong>: Always check the return value! If it returns NULL, it means the <code>skb</code> you allocated is too small. Don&#39;t keep writing—just throw away <code>skb</code> and start over.</li></ul><p><strong><code>struct nlmsghdr *nlmsg_hdr(const struct sk_buff *skb)</code></strong></p><ul><li><strong>Purpose</strong>: Gets the Netlink message header pointer from a <code>skb</code>.</li><li><strong>Details</strong>: Essentially, it just returns <code>skb-&gt;data</code> with a type cast. Don&#39;t let the macro scare you; it just saves you a few lines of explicit type casting.</li></ul><p><strong><code>struct netlink_sock *nlk_sk(struct sock *sk)</code></strong></p><ul><li><strong>Purpose</strong>: Gets the Netlink-specific <code>netlink_sock</code> structure from the generic <code>sock</code> structure.</li><li><strong>Scenario</strong>: Use this macro when you need to access Netlink-specific fields (like <code>nl_pid</code> or <code>groups</code>). Defined in <code>net/netlink/af_netlink.h</code>.</li></ul><hr><h3 id="_2-7-2-rtnetlink-specific-interfaces" tabindex="-1">2.7.2 RTNetlink-Specific Interfaces <a class="header-anchor" href="#_2-7-2-rtnetlink-specific-interfaces" aria-label="Permalink to &quot;2.7.2  RTNetlink-Specific Interfaces&quot;">​</a></h3><p>Routing Netlink is the most heavily used Netlink consumer. Here is a set of APIs we frequently interact with, primarily used for manipulating network interfaces, routing tables, and IP addresses.</p><p><strong><code>int rtnl_register(int protocol, int msgtype, rtnl_doit_func doit, rtnl_dumpit_func dumpit, rtnl_calcit_func calcit)</code></strong></p><ul><li><strong>Purpose</strong>: Binds a specific RTNetlink message type (e.g., <code>RTM_NEWLINK</code>) to your handler function.</li><li><strong>Parameters</strong>: <ul><li><code>doit</code>: Handles operations on a single object (create/delete/modify).</li><li><code>dumpit</code>: Handles list dumps (like <code>ip link show</code>).</li><li><code>calcit</code>: Calculates the buffer size needed for the dump (optional).</li></ul></li><li><strong>Details</strong>: If you provide neither <code>doit</code> nor <code>dumpit</code>, the kernel will reject the request outright.</li></ul><p><strong><code>static int rtnetlink_rcv_msg(struct sk_buff *skb, struct nlmsghdr *nlh)</code></strong></p><ul><li><strong>Purpose</strong>: The RTNetlink message dispatcher. It parses the message type, looks up the previously registered <code>doit</code> or <code>dumpit</code> callbacks, and invokes them.</li><li><strong>Details</strong>: You typically don&#39;t call this directly when writing drivers. It serves as the <code>rcv</code> callback for the RTNetlink socket, dispatching messages to the correct handler functions for you.</li></ul><p><strong><code>static int rtnl_fill_ifinfo(struct sk_buff *skb, struct net_device *dev, int type, u32 pid, u32 seq, u32 change, unsigned int flags, u32 ext_filter_mask)</code></strong></p><ul><li><strong>Purpose</strong>: Fills network interface (<code>net_device</code>) information into a Netlink message.</li><li><strong>Details</strong>: It does two things: first puts down an <code>nlmsghdr</code>, immediately followed by an <code>ifinfomsg</code> structure. If you want to send a &quot;link up&quot; or &quot;IP changed&quot; notification, this function is your go-to.</li></ul><p><strong><code>void rtnl_notify(struct sk_buff *skb, struct net *net, u32 pid, u32 group, struct nlmsghdr *nlh, gfp_t flags)</code></strong></p><ul><li><strong>Purpose</strong>: Sends an RTNetlink message to user space.</li><li><strong>Scenario</strong>: When network state changes in the kernel (e.g., unplugging a cable, changing an IP), the kernel uses this function to notify user space processes subscribed to that <code>group</code> (like NetworkManager).</li></ul><hr><h3 id="_2-7-3-generic-netlink-registration-and-management" tabindex="-1">2.7.3 Generic Netlink Registration and Management <a class="header-anchor" href="#_2-7-3-generic-netlink-registration-and-management" aria-label="Permalink to &quot;2.7.3  Generic Netlink Registration and Management&quot;">​</a></h3><p>The Generic Netlink API is a bit more complex than standard Netlink because it adds an extra layer of &quot;family management&quot; logic.</p><p><strong><code>int genl_register_family(struct genl_family *family)</code></strong></p><ul><li><strong>Purpose</strong>: Registers a new Generic Netlink family.</li><li><strong>Details</strong>: It validates the family&#39;s integrity and allocates a unique family ID. If you set <code>family-&gt;id</code> to <code>GENL_ID_GENERATE</code>, the kernel will automatically generate an ID for you.</li><li><strong>Note</strong>: Names must be unique. Registering the same name twice will result in an immediate error.</li></ul><p><strong><code>int genl_register_family_with_ops(struct genl_family *family, struct genl_ops *ops, size_t n_ops)</code></strong></p><ul><li><strong>Purpose</strong>: The atomic version—simultaneously registers a family and its set of operations (<code>ops</code>).</li><li><strong>Why it&#39;s recommended</strong>: This is the most commonly used API. It&#39;s equivalent to calling <code>genl_register_family()</code> and then looping through to call <code>genl_register_ops()</code>. If something goes wrong midway, it cleans up the half-registered family for you, leaving no garbage behind.</li><li><strong>Constraint</strong>: Each <code>ops</code> must contain at least one of <code>doit</code> or <code>dumpit</code>, otherwise it directly returns <code>-EINVAL</code>.</li></ul><p><strong><code>int genl_register_ops(struct genl_family *family, struct genl_ops *ops)</code></strong></p><ul><li><strong>Purpose</strong>: Adds a specific command operation to an already registered family.</li><li><strong>Constraint</strong>: The same command ID (<code>cmd</code>) cannot be registered twice.</li></ul><p><strong><code>void genl_unregister_mc_group(struct genl_family *family, struct genl_multicast_group *grp)</code></strong></p><ul><li><strong>Purpose</strong>: Unregisters a multicast group.</li><li><strong>Details</strong>: Unregistering a family automatically cleans up all its groups, so you don&#39;t need to manually unregister them one by one when your driver exits. But if you want to dynamically add or remove groups, this function comes in handy.</li></ul><p><strong><code>void *genlmsg_put(struct sk_buff *skb, u32 portid, u32 seq, struct genl_family *family, int flags, u8 cmd)</code></strong></p><ul><li><strong>Purpose</strong>: Adds the Generic Netlink header to a <code>skb</code>.</li><li><strong>Details</strong>: It actually calls <code>nlmsg_put()</code> to add the Netlink header, and then immediately fills in a <code>genlmsghdr</code> (which contains your <code>cmd</code>).</li><li><strong>Return</strong>: Returns a pointer to the payload area after the Generic Netlink header. You&#39;ll typically use this return value as the starting position for filling in attributes (<code>nla_put</code>).</li></ul><hr><h3 id="_2-7-4-memory-and-locking" tabindex="-1">2.7.4 Memory and Locking <a class="header-anchor" href="#_2-7-4-memory-and-locking" aria-label="Permalink to &quot;2.7.4  Memory and Locking&quot;">​</a></h3><p><strong><code>struct sk_buff *netlink_alloc_skb(struct sock *ssk, unsigned int size, u32 dst_portid, gfp_t gfp_mask)</code></strong></p><ul><li><strong>Purpose</strong>: Allocates a <code>sk_buff</code> specifically for Netlink.</li><li><strong>Scenario</strong>: Primarily used for advanced optimization scenarios like <code>NETLINK_MMAP</code> (Memory-Mapped I/O), which is rare in ordinary driver development. If you aren&#39;t using mmap, just stick with <code>nlmsg_new</code>.</li></ul><p><strong><code>void genl_lock(void)</code> / <code>void genl_unlock(void)</code></strong></p><ul><li><strong>Purpose</strong>: Operate on the Generic Netlink global mutex (<code>genl_mutex</code>).</li><li><strong>Scenario</strong>: Protects the family registration/unregistration process. In most cases, the kernel-internal <code>genl_register_*</code> family of functions already handles the locking for you. Unless you&#39;re writing extremely low-level concurrent logic, don&#39;t touch this.</li></ul><hr><h2 id="chapter-echoes" tabindex="-1">Chapter Echoes <a class="header-anchor" href="#chapter-echoes" aria-label="Permalink to &quot;Chapter Echoes&quot;">​</a></h2><p>We spent an entire chapter dissecting Netlink, which might seem a bit lengthy—after all, on the surface it&#39;s just a &quot;mechanism for the kernel and user space to chat.&quot;</p><p>But there&#39;s a subtle turning point here: the boundary between the modern Linux kernel and user space is becoming increasingly &quot;thin.&quot; In the past, we sent rigid commands via <code>ioctl</code>; now, through Netlink (especially Generic Netlink), the kernel and user space act more like two equal processes collaborating through a carefully designed message protocol. You can not only send down commands but also receive event notifications; you can not only query a single object but also dump an entire database.</p><p>This &quot;bidirectional, asynchronous, extensible&quot; communication model is key to understanding the modern Linux network stack. Without Netlink, the <code>ip</code> command couldn&#39;t exist, <code>iw</code> couldn&#39;t control complex wireless drivers, and even container technology would struggle immensely when performing network isolation.</p><p>Remember the question we raised at the beginning of this chapter—why don&#39;t we use <code>ioctl</code> anymore? Now the answer is clear: because what we need is a <strong>dynamic, future-proof interface</strong>, not just a bunch of static command codes.</p><p>In the next chapter, we&#39;ll temporarily leave the control plane and explore the other extreme of the data plane: when a network failure occurs, who delivers the news? The answer is the ICMP protocol. We&#39;ll see how the kernel generates ICMP messages and how these error packets traverse the protocol stack to ultimately reach the application.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: Understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: Understanding&quot;">​</a></h3><p><strong>Question</strong>: When developing user space network tools, would you choose the IOCTL-based net-tools package (like <code>ifconfig</code>) or the Netlink socket-based iproute2 package (like <code>ip</code>)? Please list two main technical advantages of Netlink sockets over IOCTL.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: We should choose iproute2 (Netlink). Advantage 1: Netlink supports the kernel proactively sending asynchronous messages to user space, whereas IOCTL can only handle synchronous requests initiated by user space. Advantage 2: Netlink is built on the socket mechanism, supports multicast, and doesn&#39;t require complex IOCTL command number definitions.</p><p><strong>Analysis</strong>: This chapter explicitly points out that Netlink was created to replace the clunky communication method of IOCTL. The main flaw of IOCTL is that it is synchronous, and the kernel cannot initiate communication on its own (for example, it cannot proactively notify user space that a network interface has gone down). Netlink solves this problem. Furthermore, Netlink leverages the standard socket API, making bidirectional communication and multicast handling much simpler and more flexible.</p></details><h3 id="exercise-2-application" tabindex="-1">Exercise 2: Application <a class="header-anchor" href="#exercise-2-application" aria-label="Permalink to &quot;Exercise 2: Application&quot;">​</a></h3><p><strong>Question</strong>: Suppose you need to write a user space daemon to monitor IPv4 routing table changes in a Linux system (e.g., receiving a notification when an administrator executes <code>ip route add</code>). Based on this chapter&#39;s description of rtnetlink and Netlink message handling, which specific Netlink protocol family must this daemon bind to, and which multicast group must it join to receive these asynchronous events?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Protocol family: NETLINK_ROUTE (or AF_NETLINK paired with protocol type NETLINK_ROUTE) Multicast group: RTNLGRP_IPV4_ROUTE</p><p><strong>Analysis</strong>: Based on this chapter&#39;s content, routing and link-related messages belong to the rtnetlink protocol family (NETLINK_ROUTE). To receive specific asynchronous events, a user space socket needs not only to create a NETLINK_ROUTE type socket but also to join the corresponding multicast group via <code>bind()</code>. As mentioned in the text, when a new routing entry is inserted, the kernel calls <code>rtmsg_fib()</code> via <code>rtnl_notify()</code> to notify all listeners registered to the <code>RTNLGRP_IPV4_ROUTE</code> group.</p></details><h3 id="exercise-3-thinking" tabindex="-1">Exercise 3: Thinking <a class="header-anchor" href="#exercise-3-thinking" aria-label="Permalink to &quot;Exercise 3: Thinking&quot;">​</a></h3><p><strong>Question</strong>: When designing a custom Netlink protocol, the number of standard Netlink protocols is limited to 32 (MAX_LINKS). To overcome this limitation and support communication for more dynamic kernel subsystems, Linux introduced Generic Netlink (genl). Briefly describe how Generic Netlink uses multiplexing techniques to break through this quantity limit, and provide an example of an actual subsystem that uses it.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Principle: Generic Netlink itself only occupies one standard Netlink protocol number (NETLINK_GENERIC), acting as a multiplexer. It introduces the concept of a <code>genl_family</code> (family), allowing different subsystems (like nl80211) to register as different families. By using the family ID to distinguish messages from different subsystems on the same Netlink socket, it theoretically supports hundreds or even thousands of different protocols. Example: nl80211 (used for wireless subsystem configuration, utilized by the <code>iw</code> tool).</p><p><strong>Analysis</strong>: This question tests a deep understanding of the Netlink architecture. Standard Netlink is similar to a flat channel with a limited number of slots (32). Generic Netlink dedicates one of these channels to act as a &quot;head manager&quot; (Multiplexer), supporting an arbitrary number of communication types within this channel through a &quot;sub-channel&quot; approach. This is the core design philosophy behind the Generic Netlink protocol discussed in this chapter, and it&#39;s the reason why modern subsystems like wireless drivers and task statistics tend to use it.</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>Netlink sockets are the core mechanism for communication between the modern Linux kernel and user space, completely replacing the outdated <code>ioctl</code> approach. By supporting bidirectional asynchronous communication and multicast mechanisms, Netlink not only allows user space to actively query kernel state (such as routing tables and network interface info) but, more critically, enables the kernel to proactively &quot;push&quot; notifications to subscribed user processes when events occur (such as network interfaces going up or down, or routing changes). This event-driven model is the foundation for the efficient operation of modern network management tools like <code>iproute2</code>.</p><p>Developing Netlink applications in user space typically relies on the <strong>libnl</strong> or <strong>libmnl</strong> libraries to avoid dealing directly with tedious system calls and buffer operations. The foundation of this communication is the address structure <code>sockaddr_nl</code>, where the <code>nl_pid</code> field acts as the &quot;port number&quot; concept: set it to 0 when sending to the kernel, but be careful of conflicts in multi-threaded programs using auto-binding, because multiple sockets within the same process that don&#39;t manually specify different <code>nl_pid</code> values will lead to receive confusion.</p><p>The core of kernel Netlink message handling lies in the <strong>message dispatch and callback registration mechanism</strong>. The <code>input</code> callback function specified when creating a socket via <code>netlink_kernel_create</code> (like rtnetlink&#39;s <code>rtnetlink_rcv</code>) serves as the main entry point, while specific message types (like <code>RTM_NEWLINK</code>) register their handler functions into the kernel&#39;s multi-dimensional lookup table via <code>rtnl_register()</code>. When a message arrives, the kernel dispatches it to the corresponding <code>doit</code> (for single operations) or <code>dumpit</code> (for bulk dumps) function based on the protocol number and message type.</p><p>The Netlink protocol uses a <strong>&quot;header + TLV attributes&quot;</strong> binary format to ensure extreme extensibility. The message header <code>nlmsghdr</code> contains the length, type, and sequence number, followed by a payload using TLV (Type-Length-Value) encoding that supports nesting to build complex data structures. The kernel strictly validates incoming attributes for type and length through a predefined <code>nla_policy</code> policy array, and only requests that pass validation are executed, thereby ensuring kernel security.</p><p>To address the scarcity of Netlink protocol number resources (only 32 available), the kernel introduced <strong>Generic Netlink</strong> as a universal multiplexing solution. It occupies only a single protocol number (<code>NETLINK_GENERIC</code>) and uses a &quot;main service desk&quot; called <code>nlctrl</code> to dynamically allocate and manage an unlimited number of sub-families (like <code>nl80211</code>), achieving runtime extension based on string naming. This mechanism allows developers to implement specific kernel module communication interfaces by registering custom <code>genl_family</code> and <code>genl_ops</code> without modifying the core kernel code.</p>',75)])])}const p=t(s,[["render",r]]);export{g as __pageData,p as default};
