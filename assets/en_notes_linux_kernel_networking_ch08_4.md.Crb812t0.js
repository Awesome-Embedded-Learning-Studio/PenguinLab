import{_ as t,o as s,c as i,a as n}from"./chunks/framework.8tiMeCK-.js";const g=JSON.parse('{"title":"8.4  Extension Headers — Infinite Extension via Chaining","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch08_4.md","filePath":"en/notes/linux_kernel_networking/ch08_4.md","lastUpdated":null}'),a={name:"en/notes/linux_kernel_networking/ch08_4.md"};function o(r,e,h,l,p,d){return s(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="_8-4-extension-headers-—-infinite-extension-via-chaining" tabindex="-1">8.4 Extension Headers — Infinite Extension via Chaining <a class="header-anchor" href="#_8-4-extension-headers-—-infinite-extension-via-chaining" aria-label="Permalink to &quot;8.4  Extension Headers — Infinite Extension via Chaining&quot;">​</a></h1><p>In the previous section, we spent a good while looking at IPv6&#39;s fixed 40-byte header. It&#39;s clean and pure, shedding all unnecessary baggage.</p><p>But you probably have a question: <strong>What if certain optional features absolutely must be added?</strong> For example, encryption, fragmentation, or route recording?</p><p>In the IPv4 era, this problem was solved with &quot;Options&quot;—stuffing a bunch of variable-length fields after the main header. This was the root of all evil: variable-length headers made hardware acceleration painful, forcing routers to peel open the entire packet just to parse a few option fields that might or might not be there.</p><p>IPv6&#39;s answer is: <strong>Don&#39;t put it inside; put it outside.</strong></p><p>These are <strong>Extension Headers</strong>. They are one of the most elegant parts of the IPv6 design: they turn the protocol into a chain.</p><h3 id="the-secret-of-the-chain-nexthdr" tabindex="-1">The Secret of the Chain: <code>nexthdr</code> <a class="header-anchor" href="#the-secret-of-the-chain-nexthdr" aria-label="Permalink to &quot;The Secret of the Chain: \`nexthdr\`&quot;">​</a></h3><p>Remember the <code>nexthdr</code> field we mentioned in the previous section?</p><p>I called it IPv6&#39;s &quot;Rosetta Stone.&quot; Now let&#39;s see how this stone turns.</p><p>The core logic of extension headers is <strong>chaining</strong>. In the IPv6 main header, <code>nexthdr</code> doesn&#39;t point to TCP or UDP—it points to <strong>whoever is sitting right behind the IPv6 header</strong>.</p><p>It&#39;s like a treasure hunt:</p><ol><li>You open the IPv6 box, and inside is a note saying <code>nexthdr = 43</code> (Routing Header).</li><li>You find the Routing Header box, open it, and inside is another note saying <code>nexthdr = 17</code> (UDP).</li><li>You then go grab the UDP box.</li></ol><p>This is why every extension header must have a <code>Next Header</code> field. It&#39;s the key to the next box. Only at the very last extension header does <code>Next Header</code> point to the real star of the show—like TCP, UDP, or ICMPv6.</p><p>The greatest benefit of this design is: <strong>intermediate routers only need to look at the IPv6 header</strong>. Unless they encounter a special Hop-by-Hop header, routers don&#39;t care how many extension headers are chained behind it—they simply forward based on the IPv6 header. This represents a qualitative leap in forwarding performance compared to IPv4 Options.</p><h3 id="the-rules-order-in-the-chain" tabindex="-1">The Rules: Order in the Chain <a class="header-anchor" href="#the-rules-order-in-the-chain" aria-label="Permalink to &quot;The Rules: Order in the Chain&quot;">​</a></h3><p>Although extension headers are flexible, the chain is not chaotic. There are a few ironclad rules we must follow:</p><ol><li><strong>Strict sequential processing</strong>: We can&#39;t look at Fragment before Routing. We must process them in the exact order they appear in the packet.</li><li><strong>Each extension header typically appears only once</strong>: The exception is <strong>Destination Options</strong>, which is allowed to appear twice (we&#39;ll explain why later).</li><li><strong>8-byte alignment</strong>: For memory access efficiency, the length of all extension headers must be a multiple of 8. What if it falls short? <strong>Padding</strong>.</li></ol><p>RFC 2460 provides a &quot;recommended order.&quot; While not mandatory, it&#39;s best for everyone to follow it, or things will get messy:</p><ol><li><strong>IPv6 Header</strong></li><li><strong>Hop-by-Hop Options</strong> (this must be first, immediately after the IPv6 header)</li><li><strong>Destination Options</strong> (first occurrence, for routers to read)</li><li><strong>Routing Header</strong></li><li><strong>Fragment Header</strong></li><li><strong>Authentication Header</strong></li><li><strong>Encapsulating Security Payload Header</strong></li><li><strong>Destination Options</strong> (second occurrence, for the destination host to read)</li><li><strong>Upper-layer header</strong> (TCP/UDP/etc)</li></ol><h3 id="when-the-chain-breaks-handling-unknown-protocols" tabindex="-1">When the Chain Breaks: Handling Unknown Protocols <a class="header-anchor" href="#when-the-chain-breaks-handling-unknown-protocols" aria-label="Permalink to &quot;When the Chain Breaks: Handling Unknown Protocols&quot;">​</a></h3><p>What if, while walking the chain, we suddenly encounter an unrecognized <code>nexthdr</code> value?</p><p>For example, a certain kernel version doesn&#39;t yet support a new protocol, or a malicious packet deliberately stuffs in a weird number.</p><p>We can&#39;t just drop it, nor can we force our way through. The correct approach is to <strong>file a complaint with the sender</strong>.</p><p>The kernel calls <code>icmpv6_param_prob()</code> to send back an ICMPv6 <strong>Parameter Problem</strong> message, with the error code set to <code>ICMPV6_UNK_NEXTHDR</code> (unknown Next Header).</p><blockquote><p><strong>Analogy time</strong>: Imagine receiving a package. You open a box inside, and it says &quot;Please forward to Department XX,&quot; but you&#39;ve never heard of that department. At this point, you can only return the package to the sender with a note: &quot;We don&#39;t have that department here.&quot; That&#39;s exactly what ICMPv6 does.</p></blockquote><h3 id="diving-into-the-four-core-extension-headers" tabindex="-1">Diving into the Four Core Extension Headers <a class="header-anchor" href="#diving-into-the-four-core-extension-headers" aria-label="Permalink to &quot;Diving into the Four Core Extension Headers&quot;">​</a></h3><p>The Linux kernel defines all extension header types using constants (see Table 8-2 at the end of this book). Each extension header (except Hop-by-Hop) registers its own processing callback via <code>inet6_add_protocol()</code>.</p><p>Let&#39;s talk about the most commonly used ones.</p><h4 id="_1-hop-by-hop-options" tabindex="-1">1. Hop-by-Hop Options <a class="header-anchor" href="#_1-hop-by-hop-options" aria-label="Permalink to &quot;1. Hop-by-Hop Options&quot;">​</a></h4><p>This is the only privileged class.</p><p>It must sit immediately after the IPv6 header, and it is the only one that <strong>forces every router along the path to process it</strong>. Because of this, it must be used with extreme caution—once abused, routing efficiency across the entire internet would be dragged down.</p><p>Its privileged status means it can&#39;t go through the normal registration process. In the kernel, it has a dedicated <code>ipv6_parse_hopopts()</code> method for handling, which is called directly within <code>ipv6_rcv()</code>.</p><p>It contains a set of options in TLV (Type-Length-Value) format. Here are a few typical examples:</p><ul><li><strong>Router Alert (IPV6_TLV_ROUTERALERT)</strong>: Tells the router, &quot;Hey, you need to take a look at this packet.&quot; Primarily used for RSVP (Resource Reservation) or multicast packets.</li><li><strong>Jumbo (IPV6_TLV_JUMBO)</strong>: A normal IPv6 payload has a maximum size of 65535 bytes. If we need to transmit &quot;Jumbograms&quot; exceeding 2^16 bytes, we need this option to extend the length field.</li><li><strong>Pad1 / PadN</strong>: &quot;Scrap paper&quot; used purely for padding to meet the 8-byte alignment. <code>Pad1</code> takes up 1 byte, and <code>PadN</code> takes up N bytes.</li></ul><h4 id="_2-routing-header" tabindex="-1">2. Routing Header <a class="header-anchor" href="#_2-routing-header" aria-label="Permalink to &quot;2. Routing Header&quot;">​</a></h4><p>This is the successor to IPv4 <strong>Source Routing</strong>.</p><p>If we want a packet to &quot;pass through Router X, then come to me,&quot; we use this header. It allows the sender to specify one or more intermediate addresses that must be traversed.</p><blockquote><p><strong>Echoes of history</strong>: In the IPv4 era, this feature was directly blocked by many networks due to security risks (it could be used for IP spoofing attacks). In IPv6, although it still exists, many strict network operators will still drop packets containing a Routing Header by default.</p></blockquote><h4 id="_3-fragment-header" tabindex="-1">3. Fragment Header <a class="header-anchor" href="#_3-fragment-header" aria-label="Permalink to &quot;3. Fragment Header&quot;">​</a></h4><p>This is the core of the IPv6 fragmentation mechanism.</p><p>In IPv4, if an intermediate router found that the interface MTU was too small, it could fragment the packet. This was the &quot;fallback plan when Path MTU Discovery (PMTUD) fails.&quot;</p><p><strong>But in IPv6, this fallback plan was removed.</strong> IPv6 dictates: <strong>intermediate routers are strictly forbidden from fragmenting</strong>. Fragmentation can only happen at the <strong>source host</strong>.</p><p>This means if a source host sends a 5000-byte packet, and an intermediate router has an MTU of only 1500, the router won&#39;t fragment it. Instead, it drops the packet and sends an ICMPv6 <strong>Packet Too Big</strong> message back to the source host. Upon receiving this, the source host has no choice but to shrink the packet size and retransmit.</p><p>This is IPv6&#39;s PMTUD mechanism—a &quot;tough&quot; negotiation.</p><p>The kernel implements fragmentation in the <code>ip6_fragment()</code> method, while reassembly happens in <code>net/ipv6/reassembly.c</code>.</p><p>Here is a kernel code snippet that registers the Fragment protocol handler, showing how it hooks into the chain:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> inet6_protocol frag_protocol </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .handler     </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ipv6_frag_rcv,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .flags       </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> INET6_PROTO_NOPOLICY,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __init </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ipv6_frag_init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ret;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 注册 IPPROTO_FRAGMENT (44) 对应的处理函数</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ret </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> inet6_add_protocol</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">frag_protocol, IPPROTO_FRAGMENT);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><p>(net/ipv6/reassembly.c)</p><p>When a packet&#39;s <code>nexthdr</code> is 44, the kernel knows: &quot;Oh, a fragment header follows; I need to collect all the fragments before passing them up.&quot;</p><h4 id="_4-destination-options" tabindex="-1">4. Destination Options <a class="header-anchor" href="#_4-destination-options" aria-label="Permalink to &quot;4. Destination Options&quot;">​</a></h4><p>This is the only extension header allowed to appear twice. Why? Because it has two completely different use cases:</p><ol><li><strong>Appearing before the Routing Header</strong>: At this point, its information is meant for those &quot;transit routers&quot; specified in the Routing Header.</li><li><strong>Appearing after the Routing Header (or when there is no Routing Header)</strong>: At this point, its information is meant for the <strong>final destination host</strong>.</li></ol><hr><h2 id="ipv6-initialization-starting-from-the-ethernet-type" tabindex="-1">IPv6 Initialization: Starting from the Ethernet Type <a class="header-anchor" href="#ipv6-initialization-starting-from-the-ethernet-type" aria-label="Permalink to &quot;IPv6 Initialization: Starting from the Ethernet Type&quot;">​</a></h2><p>We&#39;ve covered so many structures; now let&#39;s see how this system boots up in the kernel.</p><p>Everything starts with <code>inet6_init()</code>. This function acts as the commander-in-chief of the IPv6 subsystem. It initializes procfs, registers the TCPv6/UDPv6 protocol handlers, and starts the Neighbor Discovery and routing subsystems.</p><p>But the most critical step is telling the network core: <strong>&quot;Hand over any frame with an Ethernet type of 0x86DD to me.&quot;</strong></p><p>This is accomplished via <code>dev_add_pack()</code>, almost exactly like the IPv4 approach:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> packet_type ipv6_packet_type __read_mostly </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .type </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> cpu_to_be16</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ETH_P_IPV6),</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 0x86DD</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .func </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ipv6_rcv,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                // 处理回调</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __init </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ipv6_packet_init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    dev_add_pack</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ipv6_packet_type);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>(net/ipv6/af_inet6.c)</p><p>From this point on, whenever the NIC receives an Ethernet frame whose Type field is <code>0x86DD</code>, the kernel jumps directly to the <code>ipv6_rcv()</code> function.</p><p><code>ipv6_rcv()</code> is the entry point for the entire IPv6 receive path. Here, the kernel checks the version number, performs checksums (if any), and then follows the chain linked by <code>nexthdr</code> that we discussed earlier, unwrapping the packet step by step, ultimately delivering the data to the upper-layer protocols.</p><hr><blockquote><p><strong>Foreshadowing for this section</strong></p><p>Up to now, all the problems we&#39;ve tackled (fragmentation, routing) actually assume a premise: <strong>your address is already configured</strong>.</p><p>But IPv6 addresses are 128 bits long—nobody wants to type them in by hand. How do they end up on your machine? Is it DHCP? Or some kind of magic?</p><p>In the next section, we&#39;ll dive into IPv6&#39;s most representative feature: <strong>Autoconfiguration</strong>. We&#39;ll discover that even without a DHCP server, Linux can conjure up a globally unique address out of thin air.</p></blockquote>`,64)])])}const k=t(a,[["render",o]]);export{g as __pageData,k as default};
