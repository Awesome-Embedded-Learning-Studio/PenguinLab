import{_ as i,o as a,c as n,a as e}from"./chunks/framework.8tiMeCK-.js";const c=JSON.parse('{"title":"8.4  扩展头部——链接式的无限扩展","description":"","frontmatter":{},"headers":[],"relativePath":"notes/linux_kernel_networking/ch08_4.md","filePath":"notes/linux_kernel_networking/ch08_4.md","lastUpdated":null}'),t={name:"notes/linux_kernel_networking/ch08_4.md"};function p(r,s,l,o,h,d){return a(),n("div",null,[...s[0]||(s[0]=[e(`<h1 id="_8-4-扩展头部——链接式的无限扩展" tabindex="-1">8.4 扩展头部——链接式的无限扩展 <a class="header-anchor" href="#_8-4-扩展头部——链接式的无限扩展" aria-label="Permalink to &quot;8.4  扩展头部——链接式的无限扩展&quot;">​</a></h1><p>在上一节，我们盯着 IPv6 那个只有 40 字节的固定头部看了半天。它干净、纯粹，把所有不必要的负担都甩掉了。</p><p>但你心里肯定有个疑问：<strong>如果有些可选功能必须得加怎么办？</strong> 比如加密、分片，或者是路由记录？</p><p>在 IPv4 时代，这个问题是通过「Options」解决的——也就是在主头部后面塞上一堆不定长的字段。这是个万恶之源：变长头部让硬件加速痛苦不堪，路由器不得不为了解析那几个可能存在的选项字段，把整个包剥开来看。</p><p>IPv6 的回答是：<strong>别放在里面，把它放在外面。</strong></p><p>这就是 <strong>Extension Headers（扩展头部）</strong>。它是 IPv6 设计中最精妙的部分之一：它把协议变成了一条链。</p><h3 id="链条的秘密-nexthdr" tabindex="-1">链条的秘密：<code>nexthdr</code> <a class="header-anchor" href="#链条的秘密-nexthdr" aria-label="Permalink to &quot;链条的秘密：\`nexthdr\`&quot;">​</a></h3><p>还记得我们在上一节提到的 <code>nexthdr</code> 字段吗？</p><p>当时我说它是 IPv6 的「罗塞塔石碑」。现在我们就来看看这块石头是怎么转动的。</p><p>扩展头部的核心逻辑是<strong>链接</strong>。在 IPv6 主头里，<code>nexthdr</code> 指向的不是 TCP 或 UDP，而是<strong>紧跟在 IPv6 头部后面的那个头部是谁</strong>。</p><p>这就像玩寻宝游戏：</p><ol><li>你打开 IPv6 的盒子，里面有一张纸条写着 <code>nexthdr = 43</code>（Routing Header）。</li><li>你去找 Routing Header 的盒子，打开它，里面又有一张纸条写着 <code>nexthdr = 17</code>（UDP）。</li><li>你再去拿 UDP 的盒子。</li></ol><p>这就是为什么每个扩展头部都必须有一个 <code>Next Header</code> 字段。它就是指向下一个盒子的钥匙。只有到了最后一个扩展头，<code>Next Header</code> 才会指向真正的主角——比如 TCP、UDP 或 ICMPv6。</p><p>这种设计带来的最大好处是：<strong>中间的路由器只需要看 IPv6 头就够了</strong>。除非遇到了特殊的 Hop-by-Hop 头部，否则路由器根本不关心后面链了多少扩展头，直接根据 IPv6 头部转发。这让转发性能相比于 IPv4 的 Options 有了质的飞跃。</p><h3 id="规则-链条的秩序" tabindex="-1">规则：链条的秩序 <a class="header-anchor" href="#规则-链条的秩序" aria-label="Permalink to &quot;规则：链条的秩序&quot;">​</a></h3><p>虽然扩展头部是自由的，但链条并不是混乱的。有几条铁律你必须遵守：</p><ol><li><strong>顺序必须严格执行</strong>：你不能先看 Fragment 再看 Routing，包里什么顺序排列，你就必须按什么顺序处理。</li><li><strong>每个扩展头通常只能出现一次</strong>：除了 <strong>Destination Options</strong>（目的选项头），它允许出现两次（原因后面讲）。</li><li><strong>8 字节对齐</strong>：这是为了内存访问效率，所有扩展头的长度必须是 8 的倍数。不够怎么办？<strong>填（Padding）</strong>。</li></ol><p>RFC 2460 给出了一个「推荐顺序」，虽然不是强制的，但大家最好都照着做，否则世界会乱套：</p><ol><li><strong>IPv6 Header</strong></li><li><strong>Hop-by-Hop Options</strong>（这个必须是老大，紧挨着 IPv6 头）</li><li><strong>Destination Options</strong>（第一次出现，给路由看的）</li><li><strong>Routing Header</strong></li><li><strong>Fragment Header</strong></li><li><strong>Authentication Header</strong></li><li><strong>Encapsulating Security Payload Header</strong></li><li><strong>Destination Options</strong>（第二次出现，给目标主机看的）</li><li><strong>Upper-layer header</strong> (TCP/UDP/etc)</li></ol><h3 id="当链条断裂-未知协议处理" tabindex="-1">当链条断裂：未知协议处理 <a class="header-anchor" href="#当链条断裂-未知协议处理" aria-label="Permalink to &quot;当链条断裂：未知协议处理&quot;">​</a></h3><p>如果在解链的过程中，你突然遇到了一个不认识的 <code>nexthdr</code> 值怎么办？</p><p>比如内核某个版本还没支持某种新协议，或者恶意包故意塞了个奇怪的数字。</p><p>这时候不能直接丢弃，也不能强行往下走。正确的做法是<strong>向发送方投诉</strong>。</p><p>内核会调用 <code>icmpv6_param_prob()</code> 发回一个 ICMPv6 的 <strong>Parameter Problem</strong> 消息，错误码设为 <code>ICMPV6_UNK_NEXTHDR</code>（unknown Next Header）。</p><blockquote><p><strong>类比时刻</strong>：想象你收到一个快递包裹，打开一层盒子，里面写着「请转交给 XX 部门」，但你根本没听说过这个部门。这时候你只能把包裹退回给发件人，并附上一张纸条：「我们这儿没这个地儿」。这就是 ICMPv6 做的事。</p></blockquote><h3 id="深入四种核心扩展头" tabindex="-1">深入四种核心扩展头 <a class="header-anchor" href="#深入四种核心扩展头" aria-label="Permalink to &quot;深入四种核心扩展头&quot;">​</a></h3><p>Linux 内核用常量定义了所有扩展头的类型（见本书末尾的表 8-2）。每个扩展头（除了 Hop-by-Hop）都通过 <code>inet6_add_protocol()</code> 注册了自己的处理回调。</p><p>我们来聊聊最常用的这几种。</p><h4 id="_1-hop-by-hop-options-逐跳选项头" tabindex="-1">1. Hop-by-Hop Options（逐跳选项头） <a class="header-anchor" href="#_1-hop-by-hop-options-逐跳选项头" aria-label="Permalink to &quot;1. Hop-by-Hop Options（逐跳选项头）&quot;">​</a></h4><p>这是唯一的特权阶级。</p><p>只有它必须紧挨着 IPv6 头，也只有它<strong>强迫路径上的每一个路由器都必须处理</strong>。正因为如此，它用起来得非常小心——一旦滥用，整个互联网的路由效率都会被拖垮。</p><p>它的特权决定了它不能走普通的注册流程。在内核中，它有一个专门的 <code>ipv6_parse_hopopts()</code> 方法来处理，这个方法是在 <code>ipv6_rcv()</code> 里直接调用的。</p><p>它里面包含了一堆 TLV（Type-Length-Value）格式的选项。这里有几个典型的例子：</p><ul><li><strong>Router Alert (IPV6_TLV_ROUTERALERT)</strong>：告诉路由器「嘿，这个包你得看一眼」。主要用于 RSVP（资源预留）或组播数据包。</li><li><strong>Jumbo (IPV6_TLV_JUMBO)</strong>：普通的 IPv6 载荷最大是 65535 字节。如果你需要传输「巨型帧」（Jumbograms），超过 2^16 字节，就需要这个选项来扩展长度字段。</li><li><strong>Pad1 / PadN</strong>：就是填充用的「废纸」，为了凑齐 8 字节对齐。<code>Pad1</code> 占 1 字节，<code>PadN</code> 占 N 字节。</li></ul><h4 id="_2-routing-header-路由头" tabindex="-1">2. Routing Header（路由头） <a class="header-anchor" href="#_2-routing-header-路由头" aria-label="Permalink to &quot;2. Routing Header（路由头）&quot;">​</a></h4><p>这是 IPv4 <strong>Source Routing</strong> 的继任者。</p><p>如果你想让数据包「途经 X 路由器，再到我」，你就用这个头。它允许发送端指定一个或多个中间必须经过的地址。</p><blockquote><p><strong>历史的回响</strong>：这东西在 IPv4 时代因为安全风险（可以被用来进行 IP 欺骗攻击）而被很多网络直接封禁。在 IPv6 里，虽然它还在，但很多严谨的运维依然会默认丢弃包含 Routing Header 的包。</p></blockquote><h4 id="_3-fragment-header-分片头" tabindex="-1">3. Fragment Header（分片头） <a class="header-anchor" href="#_3-fragment-header-分片头" aria-label="Permalink to &quot;3. Fragment Header（分片头）&quot;">​</a></h4><p>这是 IPv6 分片机制的核心。</p><p>在 IPv4 里，中间路由器如果发现接口 MTU 太小，是可以把包分片的。这叫「Path MTU Discovery (PMTUD) 失败后的保底方案」。</p><p><strong>但在 IPv6 里，这个保底方案被移除了。</strong> IPv6 规定：<strong>中间路由器绝对禁止分片</strong>。分片只能发生在<strong>源主机</strong>。</p><p>这意味着如果源主机发了一个 5000 字节的包，结果中间有个路由器的 MTU 只有 1500，路由器不会分片，而是直接把包扔掉，并给源主机发回一个 ICMPv6 <strong>Packet Too Big</strong> 消息。源主机收到后，不得不缩小包的大小重发。</p><p>这就是 IPv6 的 PMTUD 机制——它是一种「强硬」的协商。</p><p>内核实现分片的是 <code>ip6_fragment()</code> 方法，而重组则在 <code>net/ipv6/reassembly.c</code> 里。</p><p>这里有一个内核注册 Fragment 协议处理器的代码片段，展示了它是如何挂接到链条上的：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> inet6_protocol frag_protocol </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .handler     </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ipv6_frag_rcv,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .flags       </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> INET6_PROTO_NOPOLICY,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __init </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ipv6_frag_init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ret;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 注册 IPPROTO_FRAGMENT (44) 对应的处理函数</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ret </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> inet6_add_protocol</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">frag_protocol, IPPROTO_FRAGMENT);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><p>(net/ipv6/reassembly.c)</p><p>当一个包的 <code>nexthdr</code> 是 44 时，内核就会知道：「哦，后面跟的是分片头，我得把碎片收齐了再往上交」。</p><h4 id="_4-destination-options-目的选项头" tabindex="-1">4. Destination Options（目的选项头） <a class="header-anchor" href="#_4-destination-options-目的选项头" aria-label="Permalink to &quot;4. Destination Options（目的选项头）&quot;">​</a></h4><p>这是唯一允许出现两次的扩展头。为什么？因为它有两个完全不同的应用场景：</p><ol><li><strong>出现在 Routing Header 之前</strong>：这时候，它的信息是给 Routing Header 里指定的那些「中转路由器」看的。</li><li><strong>出现在 Routing Header 之后（或者没有 Routing Header）</strong>：这时候，它的信息是给<strong>最终目的地主机</strong>看的。</li></ol><hr><h2 id="ipv6-初始化-从以太网类型开始" tabindex="-1">IPv6 初始化：从以太网类型开始 <a class="header-anchor" href="#ipv6-初始化-从以太网类型开始" aria-label="Permalink to &quot;IPv6 初始化：从以太网类型开始&quot;">​</a></h2><p>我们讲了这么多结构，现在来看看这套系统是如何在内核里启动的。</p><p>一切的起点是 <code>inet6_init()</code>。这个函数就像是 IPv6 子系统的总指挥，它初始化了 procfs、注册了 TCPv6/UDPv6 的协议处理函数、启动了邻居发现和路由子系统。</p><p>但最关键的一步，是告诉网络核心：<strong>「收到以太网类型为 0x86DD 的帧，交给我」</strong>。</p><p>这是通过 <code>dev_add_pack()</code> 完成的，和 IPv4 的做法几乎一模一样：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> packet_type ipv6_packet_type __read_mostly </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .type </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> cpu_to_be16</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ETH_P_IPV6),</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 0x86DD</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .func </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ipv6_rcv,</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                // 处理回调</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __init </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ipv6_packet_init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    dev_add_pack</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ipv6_packet_type);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>(net/ipv6/af_inet6.c)</p><p>从此以后，只要网卡收到了一个以太网帧，其 Type 字段是 <code>0x86DD</code>，内核就会直接跳转到 <code>ipv6_rcv()</code> 函数。</p><p><code>ipv6_rcv()</code> 就是整个 IPv6 接收路径的入口。在这里，内核会开始检查版本号、校验（如果有的话），然后顺着我们在前面讲的那条由 <code>nexthdr</code> 串起来的链条，一步步解包，最终把数据送到上层协议的手里。</p><hr><blockquote><p><strong>这一节的伏笔</strong></p><p>到现在为止，我们处理的所有问题（分片、路由）其实都假设了一个前提：<strong>你的地址已经配置好了</strong>。</p><p>但 IPv6 的地址是 128 位的，没人愿意手敲。它是怎么跑到你机器上去的？是 DHCP 嘛？还是什么魔法？</p><p>下一节，我们将进入 IPv6 最具代表性的特性：<strong>Autoconfiguration（自动配置）</strong>。你会发现，即使没有 DHCP 服务器，Linux 也能凭空变出一个全球唯一的地址来。</p></blockquote>`,64)])])}const g=i(t,[["render",p]]);export{c as __pageData,g as default};
