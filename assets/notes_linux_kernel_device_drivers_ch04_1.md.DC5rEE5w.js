import{_ as i,o as a,c as n,a as e}from"./chunks/framework.8tiMeCK-.js";const c=JSON.parse('{"title":"第 4 章  硬件中断与内核的应答之道","description":"","frontmatter":{},"headers":[],"relativePath":"notes/linux_kernel_device_drivers/ch04_1.md","filePath":"notes/linux_kernel_device_drivers/ch04_1.md","lastUpdated":null}'),l={name:"notes/linux_kernel_device_drivers/ch04_1.md"};function p(t,s,h,r,k,d){return a(),n("div",null,[...s[0]||(s[0]=[e(`<h1 id="第-4-章-硬件中断与内核的应答之道" tabindex="-1">第 4 章 硬件中断与内核的应答之道 <a class="header-anchor" href="#第-4-章-硬件中断与内核的应答之道" aria-label="Permalink to &quot;第 4 章  硬件中断与内核的应答之道&quot;">​</a></h1><p>想象这样一个场景。</p><p>你正在聚精会神地写代码，或者正准备端起早就凉透的咖啡喝一口。突然，门铃响了。</p><p>那一瞬间，你的大脑必须立刻做出反应：把咖啡放下（保存现场），站起来（切换上下文），去开门（处理中断）。不管你当时正在做什么，门铃这种「硬中断」拥有最高优先级——它强迫你立刻做出响应。</p><p>操作系统里的硬件中断（Hardware Interrupt）就是这么回事。</p><p>但事情比这稍微复杂一点。</p><p>如果你的门铃每秒钟响一次，你还能写代码吗？或者更糟，门铃坏了，一直在响，你会不会疯掉？这就是我们在驱动开发中每天都在面对的现实：外设（键盘、鼠标、网卡、磁盘控制器）时刻都在向 CPU 发送信号。CPU 如果每次都亲自下场处理，什么都干不了。</p><p>这就引出了本章的核心议题：<strong>如何优雅地处理被打断的生活？</strong></p><p>早期的操作系统很简单，CPU 听到铃声就跑过去，处理完再回来。但在现代高性能系统中，这种「老实人」策略行不通了。我们需要分层处理：有人负责应答（上半部/Hard IRQ），有人负责干脏活累活（下半部/Softirq/Threaded IRQ）。</p><p>本章的任务，就是搞清楚这套机制是怎么搭起来的。</p><hr><h2 id="_4-1-硬件中断与内核的处理流" tabindex="-1">4.1 硬件中断与内核的处理流 <a class="header-anchor" href="#_4-1-硬件中断与内核的处理流" aria-label="Permalink to &quot;4.1  硬件中断与内核的处理流&quot;">​</a></h2><p>先别急着写代码。在动手分配 IRQ 之前，我们需要先把这个「门铃」的布线图搞清楚。</p><h3 id="从硬件电信号到内核抽象" tabindex="-1">从硬件电信号到内核抽象 <a class="header-anchor" href="#从硬件电信号到内核抽象" aria-label="Permalink to &quot;从硬件电信号到内核抽象&quot;">​</a></h3><p>这一切始于设备上的一根物理导线。</p><p>当设备需要服务时（比如网卡收到了一个包），它会在这根特定的物理线上拉高电压（或者拉低，取决于电平逻辑）。这根线最终连到了主板上的一块芯片——在 x86 上叫 <strong>IO-APIC</strong>，在 ARM 上通常是 <strong>GIC</strong>（Generic Interrupt Controller）。这块芯片就像一个公司的总机秘书，它负责汇总所有打进来的电话，然后决定转接给哪条 CPU 线路。</p><p>这里有一个抽象层需要跨越。</p><p>对于 CPU 来说，它不知道「网卡」或者「键盘」是什么。它只知道「第 24 号中断线」触发了。这个数字，我们称之为 <strong>IRQ（Interrupt ReQuest）</strong>。</p><p>你可以把 IRQ 理解为硬件中断的<strong>虚拟身份证号</strong>。内核维护了一个从 IRQ 号到具体设备处理函数的映射表。当总机（APIC/GIC）告诉 CPU &quot;嘿，24 号线有活&quot; 时，CPU 会查表，找到对应的处理函数跳过去执行。</p><h3 id="通用-irq-处理层" tabindex="-1">通用 IRQ 处理层 <a class="header-anchor" href="#通用-irq-处理层" aria-label="Permalink to &quot;通用 IRQ 处理层&quot;">​</a></h3><p>但「查表」这个动作其实很麻烦。</p><p>不同的中断控制器（PIC, IO-APIC, GIC）操作方式完全不同。如果驱动程序每次都要去写底层寄存器配置，那内核代码就会乱成一锅粥。</p><p>为了屏蔽这些硬件差异，Linux 内核发明了 <strong>Generic IRQ handling layer（通用 IRQ 处理层）</strong>。</p><p>这一层的存在非常关键。它就像一个适配器。上层的驱动程序只需要调用标准的 API（比如「给我分配 24 号中断」），底层的通用层负责把这个请求翻译成给中断控制器的具体指令。这一层还得负责处理各种棘手的情况：比如两个设备共用一根线（共享中断 IRQ），或者中断处理函数正在执行时又来了一个同样的中断（中断屏蔽）。</p><p>现在，我们建立了一个完整的心理模型：外设拉线 $\\rightarrow$ 中断控制器汇总 $\\rightarrow$ CPU 捕获 $\\rightarrow$ 内核通用层分发 $\\rightarrow$ 驱动处理函数执行。</p><p>接下来的问题就是：作为驱动作者，我们怎么把自己挂到这条链上？</p><hr><h3 id="分配硬件-irq" tabindex="-1">分配硬件 IRQ <a class="header-anchor" href="#分配硬件-irq" aria-label="Permalink to &quot;分配硬件 IRQ&quot;">​</a></h3><p>要接收中断，你得先向内核「预订」这个号码。</p><p>这不仅仅是注册一个回调函数那么简单。内核在这个过程中要做很多后台工作：检查这个 IRQ 线是不是已经被占用了？是不是允许共享？对应的设备结构体 <code>dev</code> 是什么？中断触发方式是电平触发还是边沿触发？</p><p>所有的这些配置，最终都收敛到一个古老的 API 上：<code>request_irq()</code>。</p><p>虽然现代代码更推荐用它的「托管版本」（我们后面会讲），但理解 <code>request_irq()</code> 是基础。它的原型长这样：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> inline</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __must_check</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">request_irq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> irq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">irq_handler_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> handler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> flags</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">dev</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>别被参数吓到了，它们都有明确的职责：</p><ul><li><code>irq</code>: 你要申请的硬件中断号。这通常来自设备树或者是 <code>platform_get_irq()</code> 的返回值。</li><li><code>handler</code>: 你的中断处理函数。这是核心，等下细讲。</li><li><code>flags</code>: 标志位。这很关键，它定义了中断的行为（比如是快速中断还是共享中断）。</li><li><code>name</code>: 传递给 <code>/proc/interrupts</code> 显示的字符串。起个好名字，调试时你会感谢自己的。</li><li><code>dev</code>: 一个指向设备特定数据的指针。<strong>如果设置了 <code>IRQF_SHARED</code> 标志，这个参数就至关重要</strong>，因为它用来区分共享同一根 IRQ 线的不同设备。</li></ul><p>如果分配成功，返回 0。失败的话，返回一个负的错误码（比如 <code>-EBUSY</code> 表示这根线被人占用了，而且对方不愿意共享）。</p><h3 id="实现中断处理程序" tabindex="-1">实现中断处理程序 <a class="header-anchor" href="#实现中断处理程序" aria-label="Permalink to &quot;实现中断处理程序&quot;">​</a></h3><p>现在来看看 <code>handler</code>。</p><p>当中断真的发生时，内核会暂停当前的进程，保存上下文，然后跳转到这个函数执行。这意味着你的代码运行在一个非常特殊的环境里：<strong>中断上下文</strong>。</p><p>这里有一件极其重要的事情要印在脑子里：</p><blockquote><p><strong>在中断处理程序里，绝不能睡眠。</strong></p></blockquote><p>你不能调用任何会阻塞的函数，不能申请带 <code>GFP_KERNEL</code> 标志的内存（要用 <code>GFP_ATOMIC</code>），也不能使用互斥锁。你的动作必须快如闪电，处理完最紧急的状态确认，然后立刻退出。</p><p>标准的处理函数签名是这样的：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> irqreturn_t</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> my_isr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> irq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">dev_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 1. 检查设备状态，确认是不是我们的设备发出的中断 */</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 2. 如果是，清除设备寄存器里的中断 pending 位 */</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 3. 执行最小限度的处理工作 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> IRQ_HANDLED;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 或者 IRQ_NONE</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>注意返回值类型 <code>irqreturn_t</code>。</p><ul><li>返回 <code>IRQ_HANDLED</code>：表示你的设备确实触发了中断，并且你已经处理了。</li><li>返回 <code>IRQ_NONE</code>：表示这个中断不是你的设备触发的（这在共享中断的场景下很常见）。</li></ul><h3 id="使用线程化中断模型" tabindex="-1">使用线程化中断模型 <a class="header-anchor" href="#使用线程化中断模型" aria-label="Permalink to &quot;使用线程化中断模型&quot;">​</a></h3><p>「不能睡眠」这个限制太痛苦了。</p><p>有些硬件的中断处理逻辑真的很复杂，可能需要通过慢速的总线（比如 I2C）去读数据，或者需要加锁访问临界区。如果在硬中断里干这些，系统延迟会爆炸。</p><p>为了解决这个问题，现代 Linux 内核引入了 <strong>Threaded IRQ（线程化中断）</strong> 模型。</p><p>这是一种非常聪明的妥协。它把中断处理拆成了两个阶段：</p><ol><li><strong>Primary Handler (Hardirq)</strong>: 就像传统的硬中断，仍然在原子上下文里运行。但它的任务被极度简化了——<strong>只做最紧急的事，比如确认硬件状态、屏蔽中断源</strong>。然后，它不返回 <code>IRQ_HANDLED</code>，而是唤醒一个内核线程。</li><li><strong>Threaded Handler</strong>: 这是一个运行在内核线程上下文里的函数。在这个线程里，<strong>你可以睡眠，可以使用互斥锁，可以做所有进程上下文能做的事</strong>。</li></ol><p>要使用这个模型，你需要用 <code>request_threaded_irq()</code> API：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> request_threaded_irq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> irq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">irq_handler_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> handler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">                         irq_handler_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> thread_fn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                         unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> flags</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                         const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">dev</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>这里的关键是 <code>thread_fn</code>。</p><ul><li>如果你提供了 <code>thread_fn</code>，内核会在中断发生时，先执行 <code>handler</code>（如果非空），然后强制唤醒 <code>thread_fn</code> 对应的内核线程。</li><li>如果你把 <code>handler</code> 设为 <code>NULL</code>，内核会提供一个默认的 <code>handler</code>，它的唯一作用就是唤醒你的 <code>thread_fn</code>。</li></ul><p>这非常符合现代驱动的需求：把紧急的应答（Hardirq）和繁重的处理逻辑彻底解耦。</p><h3 id="启用与禁用-irq" tabindex="-1">启用与禁用 IRQ <a class="header-anchor" href="#启用与禁用-irq" aria-label="Permalink to &quot;启用与禁用 IRQ&quot;">​</a></h3><p>有时候，你需要在代码里暂时关掉中断。</p><p>也许你在重新配置设备，也许你在处理一个竞态条件。内核提供了几个不同的 API，它们的作用域完全不同，千万别搞混了。</p><p>最底层的是 <strong><code>local_irq_disable()</code></strong> 和 <strong><code>local_irq_enable()</code></strong>。 这俩是「核武器」级别的。它们会禁止<strong>当前 CPU 核心</strong>上的<strong>所有</strong>中断。除了不可屏蔽中断（NMI），什么都进不来。这通常用于内核核心代码的保护临界区，驱动代码极少需要用到这个。</p><p>如果你只是想关掉<strong>你当前设备这一条</strong> IRQ 线，应该用 <strong><code>disable_irq()</code></strong> 和 <strong><code>enable_irq()</code></strong>。 <code>disable_irq(n)</code> 会禁止 IRQ 号为 <code>n</code> 的中断在整个系统中传递（如果它是共享的，连累其他设备也一起被禁了）。此外，它还会等待当前正在执行的中断处理程序跑完。 如果你不想等待，可以用 <code>disable_irq_nosync()</code>，它会立即返回，但可能导致正在处理的中断还没结束就被新的逻辑打断，这是很危险的。</p><h3 id="查看已分配的-irq-线路" tabindex="-1">查看已分配的 IRQ 线路 <a class="header-anchor" href="#查看已分配的-irq-线路" aria-label="Permalink to &quot;查看已分配的 IRQ 线路&quot;">​</a></h3><p>我们不是在瞎写代码。系统里发生了什么，是有迹可循的。</p><p>你可以随时查看 <code>/proc/interrupts</code> 文件。这是一张实时的「战报表」：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /proc/interrupts</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">            CPU0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">       CPU1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">       CPU2</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">       CPU3</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  0:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">         46</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">   IO-APIC</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">   2-edge</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      timer</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  1:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          3</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">   IO-APIC</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">   1-edge</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      i8042</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  24:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">     10234</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">       5601</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">       4200</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">       8991</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">   PCI-MSI</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 524288-edge</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  eth0</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">...</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>每一行代表一个 IRQ。</p><ul><li>第一列是 IRQ 号。</li><li>后面几列是各个 CPU 核心上的中断计数器（每次中断发生，计数+1）。</li><li>再后面是中断控制器类型和设备名称。</li></ul><p>如果你怀疑你的中断丢失了，或者没有被触发，第一件事就是看这个文件。如果计数器一直是 0，说明硬件根本没拉线，或者线接错了。</p><hr><h2 id="_4-2-理解并使用上半部与下半部" tabindex="-1">4.2 理解并使用上半部与下半部 <a class="header-anchor" href="#_4-2-理解并使用上半部与下半部" aria-label="Permalink to &quot;4.2  理解并使用上半部与下半部&quot;">​</a></h2><p>现在我们把视角从微观的 API 拉开，看看策略。</p><p>正如我们前面提到的，中断处理程序有两大天敌：</p><ol><li><strong>它太慢了</strong>：处理太慢会丢后续的中断。</li><li><strong>它太急了</strong>：不能睡眠，干不了重活。</li></ol><p>为了调和这对矛盾，Linux 社区在几十年的演化中，沉淀出了一套标准的分治策略：<strong>Top Halves（上半部）</strong> 和 <strong>Bottom Halves（下半部）</strong>。</p><p>这不仅仅是 Linux 的专利，几乎所有现代操作系统都在做类似的事。但在 Linux 里，实现方式极其丰富：Softirq、Tasklet、Workqueue、Threaded IRQ。这里我们重点前两种。</p><h3 id="软中断与内核线程" tabindex="-1">软中断与内核线程 <a class="header-anchor" href="#软中断与内核线程" aria-label="Permalink to &quot;软中断与内核线程&quot;">​</a></h3><p>最底层的机制是 <strong>Softirq（软中断）</strong>。</p><p>软中断是在编译内核时静态定义的向量。你不能在驱动模块里动态注册一个新的软中断类型。内核预定义了几个常用的，比如 <code>HI_SOFTIRQ</code>（高优先级）、<code>NET_TX_SOFTIRQ</code>（网络发包）、<code>NET_RX_SOFTIRQ</code>（网络收包）等。</p><p>软中断的执行时机非常微妙。它通常会在硬中断处理程序返回之前被检查。如果发现有挂起的软中断，内核会立刻找一个时间点去执行它。</p><p>但这里有个坑。</p><p>如果系统里网络流量巨大，软中断就会疯狂触发。此时 CPU 忙于处理软中断，用户进程完全得不到调度机会，看起来系统就像死机了一样。这就是传说中的 <strong><code>softirqd</code></strong> 爆炸。</p><p>为了解决这个问题，内核创造了 <code>ksoftirqd</code> 这个内核线程。当软中断处理过于频繁时，内核会把这些工作甩锅给 <code>ksoftirqd</code> 线程，让它作为普通的进程去调度，防止饿死用户进程。</p><p>你可以通过 <code>/proc/softirqs</code> 看到各个软中断的统计情况：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /proc/softirqs</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">                    CPU0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">       CPU1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">          HI:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          0</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">       TIMER:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    2831401</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    2703456</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">      NET_TX:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        123</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">         89</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">      NET_RX:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    1234567</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    9876543</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">       BLOCK:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      45000</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      32000</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><h3 id="使用-tasklet" tabindex="-1">使用 Tasklet <a class="header-anchor" href="#使用-tasklet" aria-label="Permalink to &quot;使用 Tasklet&quot;">​</a></h3><p>对于驱动开发者来说，Softirq 太底层了，也太危险（没有锁保护，容易 SMP 竞态）。我们需要一个更友好的接口。</p><p>这就是 <strong>Tasklet</strong>。</p><p>Tasklet 本质上是建立在 Softirq 之上的一种动态机制（具体用的是 <code>HI_SOFTIRQ</code> 或 <code>TASKLET_SOFTIRQ</code>）。它允许你在运行时注册，并且保证：<strong>同一个 tasklet 永远不会同时在两个 CPU 上运行</strong>。</p><p>这大大简化了并发控制。</p><p>使用 Tasklet 通常分两步：</p><p><strong>第一步：定义并初始化 tasklet 结构体。</strong></p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;linux/interrupt.h&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 定义 tasklet 处理函数 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> my_do_tasklet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 定义 tasklet 结构体并静态初始化 */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">DECLARE_TASKLET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(my_tasklet, my_do_tasklet, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>如果你需要动态分配（比如在 <code>probe</code> 函数里），可以用 <code>tasklet_init</code>：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> tasklet_struct my_tasklet;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tasklet_init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">my_tasklet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, my_do_tasklet, (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> long</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p><strong>第二步：调度它。</strong></p><p>当你需要把重活扔给下半部时，只需要在中断处理程序里调用：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tasklet_schedule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">my_tasklet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>这就像是在老板（CPU）的耳朵边小声说：「这事儿我现在标记下来了，你稍后有空找那个叫 my_tasklet 的家伙处理。」 内核会在随后的某个时刻（通常是开中断后不久）执行 <code>my_do_tasklet</code>。</p><p>这种「异步调度」的感觉，是理解整个 Linux 中断子系统的钥匙。</p><hr><h2 id="_4-3-几个遗留问题的解答" tabindex="-1">4.3 几个遗留问题的解答 <a class="header-anchor" href="#_4-3-几个遗留问题的解答" aria-label="Permalink to &quot;4.3  几个遗留问题的解答&quot;">​</a></h2><p>到这里，关于中断的核心机制已经讲得差不多了。但在实际动手前，还有几个常让人困惑的细节需要澄清。</p><h3 id="边沿触发-vs-电平触发" tabindex="-1">边沿触发 vs 电平触发 <a class="header-anchor" href="#边沿触发-vs-电平触发" aria-label="Permalink to &quot;边沿触发 vs 电平触发&quot;">​</a></h3><p>这是硬件层面的一个经典分歧。</p><ul><li><strong>电平触发</strong>：只要中断线保持高电平，中断就会一直被触发。这通常用于总线共享的场景。这意味着处理程序<strong>必须</strong>在处理完之后把电平拉低（或者操作硬件让设备拉低），否则会死循环进入中断。</li><li><strong>边沿触发</strong>：只有当电平从低变高的那一瞬间（上升沿）触发一次。之后不管你保持高电平多久，它都不会再触发，除非你拉低再拉高。</li></ul><p>边沿触发相对简单，不太容易丢中断（如果只是瞬间脉冲），但如果不小心处理中断太快，可能会错过第二次脉冲。电平触发则很稳健，但如果硬件没处理好，就会变成「中断风暴”。</p><h3 id="中断上下文指南-什么该做-什么不该做" tabindex="-1">中断上下文指南：什么该做，什么不该做 <a class="header-anchor" href="#中断上下文指南-什么该做-什么不该做" aria-label="Permalink to &quot;中断上下文指南：什么该做，什么不该做&quot;">​</a></h3><p>这里有一份简单的生存法则。</p><p><strong>在硬中断上下文里：</strong></p><ul><li>✅ 可以：修改寄存器、使用 <code>spinlock</code>（不能睡眠的那种）、读写 <code>per-cpu</code> 变量。</li><li>❌ 禁止：调用 <code>kmalloc(GFP_KERNEL)</code>、调用 <code>mutex_lock()</code>、调用任何可能引起 <code>schedule()</code> 的函数。</li></ul><p>怎么检查？内核提供了一个宏 <code>might_sleep()</code>。如果你在不能睡眠的地方调用了可能睡眠的函数，内核配置了 <code>DEBUG_ATOMIC_SLEEP</code> 的话，系统会直接 panic 并打印堆栈。相信我，这比以后莫名其妙死机要好得多。</p><h3 id="中断掩码-默认行为与控制" tabindex="-1">中断掩码：默认行为与控制 <a class="header-anchor" href="#中断掩码-默认行为与控制" aria-label="Permalink to &quot;中断掩码：默认行为与控制&quot;">​</a></h3><p>当你进入一个中断处理程序时，内核为了防止重入，默认会屏蔽<strong>当前这条 IRQ 线</strong>在<strong>所有 CPU</strong> 上的中断。</p><p>注意这个细节：它在所有 CPU 上屏蔽了这条线。 为什么？因为如果你在 CPU0 上处理这个中断，CPU1 又收到了同一个设备的中断，而且它们共享数据，那你就得加锁。为了简化这种复杂度，内核默认采取了「独占」策略。</p><p>但这会影响性能。如果你确定你的处理很快，而且你能通过自旋锁保护好数据，你可以使用 <code>IRQF_DISABLED</code> 标志（这个现在很少用了，因为现在的内核默认行为已经很复杂了），或者在处理函数内部手动调用 <code>irq_set_affinity</code> 来调整。</p><h3 id="中断栈-内核维护独立的栈吗" tabindex="-1">中断栈：内核维护独立的栈吗？ <a class="header-anchor" href="#中断栈-内核维护独立的栈吗" aria-label="Permalink to &quot;中断栈：内核维护独立的栈吗？&quot;">​</a></h3><p>这是个很深的问题。</p><p>在早期的 Linux 里，中断是直接借用当前进程的内核栈的。这意味着如果你的进程栈本身就很深（比如复杂的系统调用嵌套），突然来了个中断，栈可能就溢出了。</p><p>现在的内核（绝大多数架构）<strong>为每个 CPU 都分配了一个独立的 <code>irq_stack</code></strong>。 当中断发生时，CPU 会切换到这个专门的栈上运行。这大大增加了系统的健壮性，也允许我们在中断里做稍微多一点一点的事情（但也只是多一点而已）。</p><hr><p>好了，理论铺垫得差不多了。现在我们手里有了「门铃」（硬件中断）、有了「秘书」（通用 IRQ 层），也有了「办事员」（处理函数）。</p><p>在下一节，我们将把这些理论应用到真正的代码中，看看一个现代驱动是如何通过托管资源 API 来优雅地管理这一切的。</p>`,123)])])}const g=i(l,[["render",p]]);export{c as __pageData,g as default};
