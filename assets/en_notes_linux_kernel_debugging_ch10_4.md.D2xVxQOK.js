import{_ as s,o as a,c as t,a as n}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"10.4  Detecting Deadlocks and CPU Stalls in the Kernel","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch10_4.md","filePath":"en/notes/linux_kernel_debugging/ch10_4.md","lastUpdated":null}'),i={name:"en/notes/linux_kernel_debugging/ch10_4.md"};function o(r,e,l,h,d,p){return a(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="_10-4-detecting-deadlocks-and-cpu-stalls-in-the-kernel" tabindex="-1">10.4 Detecting Deadlocks and CPU Stalls in the Kernel <a class="header-anchor" href="#_10-4-detecting-deadlocks-and-cpu-stalls-in-the-kernel" aria-label="Permalink to &quot;10.4  Detecting Deadlocks and CPU Stalls in the Kernel&quot;">​</a></h1><p>At the end of the previous section, we talked about the &quot;red line&quot; in the Panic handler: don&#39;t make it too complex, or you won&#39;t even be able to leave a &quot;dying message.&quot; But sometimes, the kernel dies in a more insidious way—it doesn&#39;t crash immediately or scream for help; it just suddenly goes silent.</p><p>This is the &quot;hung&quot; crime scene we&#39;ll tackle in this section.</p><h3 id="what-exactly-is-a-deadlock" tabindex="-1">What Exactly is a Deadlock? <a class="header-anchor" href="#what-exactly-is-a-deadlock" aria-label="Permalink to &quot;What Exactly is a Deadlock?&quot;">​</a></h3><p>The meaning of a deadlock is straightforward: the system or a specific CPU core enters a state of long unresponsiveness. This is more troublesome than a direct Panic because it might happen in the dead of night in a production environment—the machine appears to be running, but it has long been frozen.</p><p>To catch these ghost-like failures, we need more advanced monitoring mechanisms. Before diving into the specific kernel detection mechanisms, let&#39;s take some time to clarify the underlying &quot;watchdog&quot; concept—it&#39;s the physical foundation for all detection mechanisms.</p><h4 id="a-brief-note-on-watchdogs" tabindex="-1">A Brief Note on Watchdogs <a class="header-anchor" href="#a-brief-note-on-watchdogs" aria-label="Permalink to &quot;A Brief Note on Watchdogs&quot;">​</a></h4><p>A watchdog is essentially a monitoring program. Its logic is very simple: I send you a heartbeat (ping) at regular intervals; if you don&#39;t respond within the specified time, I consider you dead and forcibly reboot the system.</p><p>In the Linux ecosystem, watchdogs come in a few forms:</p><ol><li><strong>Hardware Watchdog</strong>: This is an independent chip or module soldered directly onto the board. It&#39;s connected to the system&#39;s reset circuit, and once triggered, it physically pulls the reset pin. Driving this usually depends heavily on the specific board, and the kernel provides a generic framework to make it easier for driver developers to interface with all sorts of messy hardware chips.</li><li><strong>Software Watchdog</strong>: This is the <code>softdog</code> driver in the kernel. It&#39;s not as brutal as hardware, but it&#39;s sufficient in many scenarios. Its location in the kernel configuration is <code>Device Drivers | Watchdog Timer Support</code>, and the corresponding config option is <code>CONFIG_SOFT_WATCHDOG</code>.</li></ol><p>However, a kernel module alone isn&#39;t enough. We usually need a userspace daemon to work with it. This daemon&#39;s job is to periodically feed the dog (usually by writing something to <code>/dev/watchdog</code>, or issuing an ioctl). If this daemon stops working because the system is hung, the watchdog feeding times out, and the system is rebooted.</p><p>In many production kernels (like the custom 5.10.60 kernel we build in this book), we compile <code>softdog</code> as a module:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CONFIG_SOFT_WATCHDOG=m</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Once loaded, you&#39;ll see a module named <code>softdog</code> in the system.</p><h4 id="hands-on-running-softdog-and-the-userspace-watchdog" tabindex="-1">Hands-on: Running softdog and the Userspace Watchdog <a class="header-anchor" href="#hands-on-running-softdog-and-the-userspace-watchdog" aria-label="Permalink to &quot;Hands-on: Running softdog and the Userspace Watchdog&quot;">​</a></h4><p>Talk is cheap. Let&#39;s fire up these components in an x86_64 Ubuntu VM and see for ourselves.</p><p>First, load the kernel&#39;s <code>softdog</code> module, then manually start the userspace <code>watchdog</code> daemon (for demonstration purposes, I&#39;ll run it in <code>--verbose</code> mode):</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> modprobe</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> softdog</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> watchdog</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --verbose</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[...]</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">watchdog:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> String</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;watchdog-device&#39;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> found</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> as</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;/dev/watchdog&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">watchdog:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Variable</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;realtime&#39;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> found</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> as</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;yes&#39;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">watchdog:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Integer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;priority&#39;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> found</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[1]+  Done                    watchdog --verbose</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>Now let&#39;s confirm they&#39;re actually running:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ps</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> watch</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    111</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ?</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        00:00:00</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> watchdogd</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  10106</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ?</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        00:00:00</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> watchdog</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>There are two lines here: The first line, <code>watchdogd</code>, is a kernel thread (part of the softdog driver). The second line, <code>watchdog</code>, is the userspace daemon we just started.</p><p>By the way, if your system uses systemd, it has built-in watchdog functionality as well (configured in <code>/etc/systemd/system.conf</code>).</p><p><strong>⚠️ Debugging Warning</strong>: Watchdogs are great on servers, but they&#39;re a <strong>nightmare when debugging the kernel</strong>. Imagine you&#39;re single-stepping code in an interactive debugger like KGDB, and because you&#39;re stepping too slowly, you trigger the watchdog and the system just reboots on you—who could stand that? So, when you&#39;re about to do deep kernel debugging, remember to turn these watchdogs off.</p><p>Alright, now that we&#39;ve laid the watchdog foundation, we can look at how the kernel leverages this mechanism (especially NMI) to build more advanced detectors—specifically for hard and soft lockups.</p><hr><h3 id="using-the-kernel-s-hard-and-soft-lockup-detectors" tabindex="-1">Using the Kernel&#39;s Hard and Soft Lockup Detectors <a class="header-anchor" href="#using-the-kernel-s-hard-and-soft-lockup-detectors" aria-label="Permalink to &quot;Using the Kernel&#39;s Hard and Soft Lockup Detectors&quot;">​</a></h3><p>Software (and even hardware) is imperfect. I&#39;d bet you&#39;ve encountered that kind of &quot;mysterious hang&quot;: the system doesn&#39;t Panic, the logs aren&#39;t updating, the mouse won&#39;t move, the keyboard is unresponsive, and the screen is frozen.</p><p>This is a Lockup.</p><p>The kernel has ways to detect these kinds of problems. The watchdog we just mentioned plays a key role here. The kernel uses the <strong>NMI Watchdog</strong> (Non-Maskable Interrupt Watchdog) and the <strong>perf subsystem</strong> to implement hard and soft lockup detection.</p><p>The relevant config options are hidden in the <code>Kernel hacking | Debug Oops, Lockups and Hangs</code> menu. Looking at Figure 10.8 (assuming you can see that pile of config options), you might ask: since you said this is a &quot;production kernel,&quot; why aren&#39;t <code>panic_on_oops</code> or <code>panic on soft/hard lockup</code> selected?</p><p><strong>That&#39;s a good question.</strong> The &quot;production kernel&quot; in this book is primarily for demonstration purposes; it&#39;s not actually running business workloads in a server room. If you&#39;re building a real product, whether to enable automatic reboot is an architectural decision: <strong>When the system hangs, do you want it to reboot immediately to heal itself, or do you want it to stay frozen in place so you can investigate the corpse?</strong> If it&#39;s the former, turn on the <code>panic_on</code> related options and pair them with the <code>panic=n</code> boot parameter (which means automatically reboot <em>n</em> seconds after a Panic).</p><p>In our configuration, the detection functionality is enabled, but it won&#39;t immediately Panic. Table 10.3 summarizes the key configurations, boot parameters, and sysctl controls involved here.</p><p>You can use <code>sysctl</code> to check your current system&#39;s settings (note that <code>nmi_watchdog</code> corresponds to hard lockups, <code>soft_watchdog</code> corresponds to soft lockups, and does <strong>not</strong> refer to the <code>softdog</code> module):</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sysctl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -a</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> watchdog</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.nmi_watchdog</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.soft_watchdog</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.watchdog</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.watchdog_cpumask</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> 0-5</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.watchdog_thresh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 10</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>In this example, <code>nmi_watchdog</code> is 0 because this is a VM, which typically lacks hardware watchdog support. <code>soft_watchdog</code>, however, is always available.</p><p>Now, let&#39;s clarify what these terms actually mean.</p><hr><h4 id="what-is-a-soft-lockup" tabindex="-1">What is a Soft Lockup? <a class="header-anchor" href="#what-is-a-soft-lockup" aria-label="Permalink to &quot;What is a Soft Lockup?&quot;">​</a></h4><p>A soft lockup is a specific type of kernel bug.</p><p>Imagine a task getting stuck in an infinite loop in kernel mode, or refusing to leave the CPU for some reason, and doing so for a long time. The result is that other tasks have no chance to be scheduled and run on that CPU core. This is a soft lockup.</p><p><strong>Time Limit</strong>: The default timeout for a soft lockup is <strong>20 seconds</strong>. How is this value calculated? It&#39;s twice the <code>watchdog_thresh</code> value. The hard lockup timeout is the <code>watchdog_thresh</code> value itself, defaulting to <strong>10 seconds</strong>.</p><p>You can view and modify this value:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /proc/sys/kernel/watchdog_thresh</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">10</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>If you want to change it to 5 seconds (meaning a soft lockup triggers at 10 seconds), just write an integer to it. Writing 0 disables the detection.</p><p><strong>What Happens When a Soft Lockup is Detected?</strong></p><p>This depends on a few config options:</p><ol><li>If the <code>kernel.softlockup_panic</code> sysctl is 1, or if the <code>softlockup_panic=1</code> boot parameter is present, the kernel will directly <strong>Panic</strong>.</li><li>If Panic is not enabled (the default), the kernel will print a warning message and dump the stack of the stuck task.</li></ol><p><strong>⚠️ Note</strong>: In the second case (warning only, no Panic), the buggy task that caused the problem will continue to sit there and hog the CPU. The system will not recover automatically.</p><hr><h4 id="hands-on-triggering-a-soft-lockup-on-x86-64" tabindex="-1">Hands-on: Triggering a Soft Lockup on x86_64 <a class="header-anchor" href="#hands-on-triggering-a-soft-lockup-on-x86-64" aria-label="Permalink to &quot;Hands-on: Triggering a Soft Lockup on x86_64&quot;">​</a></h4><p>Reading the definition alone doesn&#39;t give you a real feel for it, so let&#39;s artificially create a disaster.</p><p>The idea is simple: pick an unlucky CPU core, put it into kernel mode, and then run a highly CPU-intensive infinite loop on that core.</p><p>I modified a demo module from the book <em>Linux Kernel Programming Part 2</em> (<code>kthread_simple</code>) and added some malicious code to it. The specific code is in the <code>ch10/kthread_stuck</code> directory.</p><p>Load this module (without parameters, it defaults to testing a soft lockup):</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[ ... 模块加载过程 ... ]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>After running for more than 20 seconds, the kernel&#39;s soft lockup watchdog will catch on and immediately spit out a <code>BUG()</code> message! Looking at Figure 10.9, you&#39;ll see the console flooded with <code>BUG: soft lockup ...</code> type <code>KERN_EMERG</code> level messages.</p><p>Besides that prominent BUG alert, the watchdog will also call routines like <code>dump_stack()</code> to dump the entire current state:</p><ul><li>The list of modules in memory</li><li>Context information</li><li>CPU register snapshots</li><li>Machine instructions</li><li>Most importantly: the kernel-mode call stack</li></ul><p>If you used our handy <code>PRINT_CTX()</code> macro, you&#39;ll see output similar to this:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>002) [lkd/kt_stuck]:3530   |  .N.1   /* simple_kthread() */</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Notice that <code>.N.1</code> format string. The first character is <code>.</code> (a dot), which means <strong>hardware interrupts are enabled</strong>.</p><p>This matches our expectation: in this test, we used a regular <code>spin_lock()</code> and didn&#39;t disable interrupts. This stands in stark contrast to the hard lockup we&#39;ll discuss next.</p><p><strong>Don&#39;t Forget the Spinlock!</strong></p><p>You might ask: since it&#39;s an infinite loop, why bother acquiring a spinlock? This is the essence of the test.</p><p>A spinlock—especially the <code>spin_lock_irqsave()</code> variant—doesn&#39;t just spin-wait; it also <strong>disables hardware interrupts</strong>. Disabling interrupts has a side effect: it also disables kernel preemption. This means that once you&#39;re in, basically no one can interrupt you (except for NMI).</p><p>This is exactly the condition needed to simulate a <strong>hard lockup</strong>.</p><p>But there&#39;s an interesting contradiction here: <strong>Since interrupts are disabled, how can the kernel watchdog detect that it&#39;s stuck?</strong></p><p>The answer lies in NMI (Non-Maskable Interrupt). The very definition of NMI is &quot;even if interrupts are disabled, I&#39;m coming in.&quot; It uses hardware performance counters to periodically check whether the CPU is still ticking. So, even if you disable all interrupts in your code and enter an infinite loop, NMI can still break down the door and catch you slacking.</p><p>(Of course, as we emphasized in Chapter 8, you must be quick when holding a spinlock. We&#39;re deliberately doing the exact opposite here purely for educational demonstration.)</p><p>If you want to dive into the soft lockup detection source code, you can check out the <code>watchdog_timer_fn()</code> function in <code>kernel/watchdog.c</code>.</p><p>Also, if you try to <code>rmmod</code> this malicious module without properly sending a signal to let the kernel thread exit first, after about 2 minutes, the <code>rmmod</code> process itself will be detected as a &quot;Hung Task.&quot; We&#39;ll cover this detection mechanism shortly.</p><hr><h4 id="what-is-a-hard-lockup" tabindex="-1">What is a Hard Lockup? <a class="header-anchor" href="#what-is-a-hard-lockup" aria-label="Permalink to &quot;What is a Hard Lockup?&quot;">​</a></h4><p>A hard lockup is more severe than a soft lockup.</p><p>If a soft lockup is &quot;I won&#39;t let anyone else run, but I can still respond to interrupts,&quot; then a hard lockup is &quot;shutting the door completely.&quot; A CPU core gets stuck in an infinite loop in kernel mode and <strong>has interrupts disabled</strong>. This means that on this core, not even hardware interrupts can be handled.</p><p><strong>Time Limit</strong>: The default timeout for a hard lockup is 10 seconds (<code>watchdog_thresh</code>).</p><p><strong>What Happens When a Hard Lockup is Detected?</strong></p><p>Again, there are two scenarios:</p><ol><li><strong>Panic</strong>: If the <code>nmi_watchdog=1</code> boot parameter is set, or if <code>kernel.hardlockup_panic</code> is 1, the kernel will directly Panic.</li><li><strong>Warning</strong>: By default, it will only print a warning and a stack trace. If the <code>hardlockup_all_cpu_backtrace=1</code> boot parameter is present, it will also print stack traces for all CPUs.</li></ol><p>Similarly, if it doesn&#39;t Panic, the buggy code will continue to freeze that core.</p><hr><h4 id="rcu-and-cpu-stalls" tabindex="-1">RCU and CPU Stalls <a class="header-anchor" href="#rcu-and-cpu-stalls" aria-label="Permalink to &quot;RCU and CPU Stalls&quot;">​</a></h4><p>Another common source of &quot;hangs&quot; comes from the RCU (Read-Copy Update) mechanism.</p><p>You may have heard of RCU; it&#39;s a powerful lockless synchronization mechanism in the kernel. But when using RCU, if a CPU falls into a prolonged state of disabled interrupts or unpreemptibility, it will cause an <strong>RCU CPU Stall</strong>.</p><p>It&#39;s like the RCU mechanism is waiting for you to finish reading data, but you just won&#39;t let go, so it times out and sounds the alarm.</p><p><strong>Quickly Understanding the Core Logic of RCU</strong>:</p><p>Imagine several readers (R1, R2, R3) are reading a piece of shared data. Along comes a writer. The writer doesn&#39;t modify the data directly; instead, it makes a copy, modifies the copy, and then atomically points the pointer to the new data. What happens to the old data? It can only be freed after all the readers who started reading (R1, R2, R3) have confirmed they are done.</p><p><strong>How do we confirm the readers are done?</strong> RCU&#39;s implementation approach is: wait for all current readers to <strong>voluntarily yield the CPU</strong> (e.g., by calling the scheduler). The writer sets a grace period, which defaults to a lengthy 60 seconds (<code>CONFIG_RCU_CPU_STALL_TIMEOUT</code>). If even after that much time the readers haven&#39;t finished, or if the CPU just isn&#39;t scheduling, the kernel will print an RCU Stall warning.</p><hr><h4 id="hands-on-triggering-a-hard-lockup-rcu-stall" tabindex="-1">Hands-on: Triggering a Hard Lockup / RCU Stall <a class="header-anchor" href="#hands-on-triggering-a-hard-lockup-rcu-stall" aria-label="Permalink to &quot;Hands-on: Triggering a Hard Lockup / RCU Stall&quot;">​</a></h4><p>This step is a bit more troublesome than the soft lockup, as the conditions are stricter:</p><ol><li><strong>Must be physical hardware</strong>: Hard lockup detection relies on NMI, which VMs usually don&#39;t have.</li><li><strong>Enable NMI</strong>: You need to add <code>nmi_watchdog=1</code> to the boot parameters.</li><li><strong>Configuration check</strong>: <code>CONFIG_RCU_CPU_STALL_TIMEOUT</code> needs to be between 3 and 300.</li></ol><p>Once configured, you should be able to see this with <code>sysctl</code>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># sysctl -a | grep watchdog</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.nmi_watchdog</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.soft_watchdog</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">...</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.watchdog_thresh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 10</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Now, let&#39;s load that malicious module <code>ch10/kthread_stuck</code> again, this time passing the parameter <code>lockup_type=2</code>. This parameter makes the kernel thread call <code>spin_lock_irq()</code> while holding the spinlock, <strong>disabling both interrupts and preemption</strong>, and then entering an infinite loop.</p><p>After a while, the kernel logs will explode. You might see backtraces generated by NMI interrupts, or RCU CPU Stall warnings.</p><p>The logs will look roughly like this:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>rcu:  INFO: rcu_sched detected stalls on CPUs/tasks:</span></span>
<span class="line"><span>rcu:         3-...0: (1 GPs behind) idle=462/1/0x4000000000000000 softirq=60126/60127 fqs=6463</span></span>
<span class="line"><span>(detected by 2, t=15003 jiffies, g=127897, q=1345272)</span></span>
<span class="line"><span>Sending NMI from CPU 2 to CPUs 3:</span></span>
<span class="line"><span>NMI backtrace for cpu 3</span></span>
<span class="line"><span>CPU: 3 PID: 16351 Comm: lkd/kt_stuck Tainted: P        W  OEL 5.13.0-37-generic #42~20.04.1-Ubuntu</span></span>
<span class="line"><span>[...]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>This is the RCU mechanism roaring after discovering that a CPU has been stuck with interrupts disabled for too long.</p><p>You can use the <code>kernel.panic_on_rcu_stall</code> sysctl to make the kernel Panic directly in this situation.</p><p>Finally, the kernel actually provides a more professional test module, <code>test_lockup</code> (<code>CONFIG_TEST_LOCKUP</code>), specifically for testing these detection mechanisms. It&#39;s much more comprehensive than our homemade module.</p><p>Table 10.4 summarizes all the key parameters and configurations related to hard and soft lockups.</p><hr><h3 id="using-the-kernel-s-hung-task-and-workqueue-stall-detectors" tabindex="-1">Using the Kernel&#39;s Hung Task and Workqueue Stall Detectors <a class="header-anchor" href="#using-the-kernel-s-hung-task-and-workqueue-stall-detectors" aria-label="Permalink to &quot;Using the Kernel&#39;s Hung Task and Workqueue Stall Detectors&quot;">​</a></h3><p>Besides CPU infinite loops, another common type of &quot;hang&quot; is a <strong>stuck task</strong>.</p><p>For example, a process enters <code>TASK_UNINTERRUPTIBLE</code> (D state) and refuses to wake up from its sleep, persisting for more than the default <strong>120 seconds</strong>. This is a Hung Task.</p><h4 id="configuring-hung-task-detection" tabindex="-1">Configuring Hung Task Detection <a class="header-anchor" href="#configuring-hung-task-detection" aria-label="Permalink to &quot;Configuring Hung Task Detection&quot;">​</a></h4><p>Still in the <code>Kernel hacking | Debug Oops, Lockups and Hangs</code> menu:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[*] Detect Hung Tasks</span></span>
<span class="line"><span>(120) Default timeout for hung task detection (in seconds)</span></span>
<span class="line"><span>[ ]   Panic (Reboot) On Hung Tasks</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Once enabled, the kernel will periodically scan all tasks to see if anyone is in the D state and has timed out.</p><p>The relevant parameters are as follows:</p><ul><li><code>CONFIG_DEFAULT_HUNG_TASK_TIMEOUT</code>: Compile-time configuration for the timeout.</li><li><code>kernel.hung_task_timeout_secs</code>: Runtime sysctl; set to 0 to disable.</li><li><code>kernel.hung_task_panic</code>: Whether to Panic when detected.</li></ul><p><strong>⚠️ Debugging Pitfall</strong>: We mentioned earlier that if you try to <code>rmmod</code> that malicious module without first sending a signal to stop the kernel thread, the <code>rmmod</code> process itself might get stuck (because it&#39;s waiting for the module&#39;s reference count to drop to zero), and then get caught red-handed by the Hung Task detector. This is a classic pitfall: the tool used to debug a deadlock gets deadlocked itself.</p><p>Besides stuck tasks, the kernel&#39;s Workqueues can also stall. If a work item sits in the queue for too long without executing, it will also trigger an alarm.</p><hr><h3 id="chapter-echoes" tabindex="-1">Chapter Echoes <a class="header-anchor" href="#chapter-echoes" aria-label="Permalink to &quot;Chapter Echoes&quot;">​</a></h3><p>In this chapter, we built a complete &quot;fault monitoring system.&quot;</p><p>From the most brutal <strong>Panic</strong>, to the <strong>Hard Lockup</strong> detection that uses NMI to penetrate defenses, to the <strong>Hung Task</strong> detection that discovers tasks suffocating in their sleep. Essentially, we are installing more and more sensors into the kernel.</p><p>Do you remember the question we posed in the chapter introduction—<strong>The system didn&#39;t crash, but it&#39;s hung. What do we do?</strong> Now the answer is clear:</p><ul><li>If the CPU is running wild without scheduling, it&#39;s a <strong>Soft Lockup</strong>;</li><li>If the CPU has interrupts disabled and isn&#39;t responding, it&#39;s a <strong>Hard Lockup</strong>;</li><li>If a task is deadlocked waiting for a resource and won&#39;t wake up, it&#39;s a <strong>Hung Task</strong>;</li><li>If the RCU grace period can&#39;t complete, it&#39;s an <strong>RCU Stall</strong>.</li></ul><p>In the next chapter, we&#39;ll shift our focus from &quot;the kernel dying on its own&quot; to &quot;the kernel being killed by external forces&quot;—namely, hardware failures, memory corruption, and the ultimate means of debugging these nuclear-level problems.</p>`,121)])])}const g=s(i,[["render",o]]);export{u as __pageData,g as default};
