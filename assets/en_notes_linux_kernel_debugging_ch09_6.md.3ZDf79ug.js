import{_ as i,o as t,c as s,a}from"./chunks/framework.8tiMeCK-.js";const n="/PenguinLab/assets/ch09_fig144.BMcDWh_k.webp",o="/PenguinLab/assets/ch09_fig145.-9lLrn5j.webp",r="/PenguinLab/assets/ch09_fig146.C99f7qvO.webp",h="/PenguinLab/assets/ch09_fig147.BA3Q6nq1.webp",l="/PenguinLab/assets/ch09_fig148.BzIpyvol.webp",y=JSON.parse('{"title":"9.6 Hands-on: Tracing a Single Ping Request with Raw ftrace","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch09_6.md","filePath":"en/notes/linux_kernel_debugging/ch09_6.md","lastUpdated":null}'),c={name:"en/notes/linux_kernel_debugging/ch09_6.md"};function p(d,e,g,u,k,m){return t(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="_9-6-hands-on-tracing-a-single-ping-request-with-raw-ftrace" tabindex="-1">9.6 Hands-on: Tracing a Single Ping Request with Raw ftrace <a class="header-anchor" href="#_9-6-hands-on-tracing-a-single-ping-request-with-raw-ftrace" aria-label="Permalink to &quot;9.6 Hands-on: Tracing a Single Ping Request with Raw ftrace&quot;">​</a></h1><p>Alright, the toolbox is open. We now have the ability to configure the kernel, simple tracing methods at our disposal, and a whole bunch of advanced filtering techniques—Glob, index, blacklist, and commands.</p><p>Theorizing on paper is no fun anymore.</p><p>Now, we&#39;re going to throw this knowledge into a real-world scenario. We&#39;re going to fire a Ping packet at the kernel:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ping</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> packtpub.com</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Then, using the ftrace techniques we just learned, we&#39;ll fully reproduce everything that happened inside the kernel in that instant—from the construction of the ICMP packet, to the layer-by-layer encapsulation in the Network Stack, all the way down to the driver transmitting it.</p><hr><h3 id="strategy-the-art-of-function-graph-and-filtering" tabindex="-1">Strategy: The Art of function_graph and Filtering <a class="header-anchor" href="#strategy-the-art-of-function-graph-and-filtering" aria-label="Permalink to &quot;Strategy: The Art of function_graph and Filtering&quot;">​</a></h3><p>In this hands-on exercise, we&#39;ll use the <code>function_graph</code> tracer from start to finish.</p><p>Why? Because we need to see the &quot;flow.&quot; The previous <code>function</code> tracer only gives us a flat list of functions, like looking at a pile of scattered Lego bricks; whereas <code>function_graph</code> assembles those bricks into a castle, letting us see who called whom and how deep the call hierarchy goes.</p><p>But the networking subsystem is massive. Without filtering, the log will explode in a matter of milliseconds. Our script (<code>ch9/ftrace/ping_ftrace.sh</code>) provides two filtering approaches:</p><ol><li><strong>Via <code>available_filter_functions</code></strong> (this is the &quot;index filtering method&quot; we covered in detail earlier)</li><li><strong>Via the <code>set_event</code> interface</strong></li></ol><p>These two are mutually exclusive. In the script, we use a variable called <code>FILTER_VIA_AVAIL_FUNCS</code> as a toggle. It defaults to <code>1</code>, which is the first method. There&#39;s a good reason for this: while the first method is slightly more tedious to configure (you have to handle indices), it provides the most comprehensive view—it&#39;s not just about &quot;picking a few functions to watch,&quot; but rather &quot;pulling up the entire networking subsystem&#39;s call chain while keeping only the relevant parts.&quot;</p><p>Let&#39;s look at the core logic in the script:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ch9/ftrace/ping_ftrace.sh</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">FILTER_VIA_AVAIL_FUNCS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">1</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;[+] Function filtering:&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [ \${FILTER_VIA_AVAIL_FUNCS} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-eq</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ] ; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">then</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # 这种方法快，而且细节保留得完美！</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    filterfunc_idx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> read</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> write</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> net</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> packet_</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sock</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sk_</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tcp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> udp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> skb</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    netdev</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    netif_</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> napi</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> icmp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;^ip_&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;xmit$&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev_</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> qdisc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [...]</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">fi</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>The <code>filterfunc_idx()</code> called here is the magic tool we implemented in the previous section—index-based filtering. It throws these keywords (<code>sock</code>, <code>skb</code>, <code>icmp</code>, etc.) into <code>available_filter_functions</code> to filter line numbers, then writes those line numbers back to <code>set_ftrace_filter</code>.</p><p>At the same time, we didn&#39;t forget about cleanup. The script also uses <code>filterfunc_remove()</code> to ensure certain noise functions we don&#39;t care about are stripped out.</p><p>Additionally, the script pays special attention to the network driver module. In this example, we&#39;re using the <code>e1000</code> driver:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">KMOD</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">e1000</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;[+] module filtering (for \${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">KMOD</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">})&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> lsmod</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">grep</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> \${KMOD} ; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">then</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;[+] setting filter command: :mod:\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">KMOD</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}&quot;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;:mod:\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">KMOD</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set_ftrace_filter</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">fi</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>This step is crucial. If you only trace the protocol stack without tracing the driver, you&#39;ll only see the packet handed off at the doorstep, but not how it actually gets transmitted.</p><hr><h3 id="the-chicken-or-the-egg-the-challenge-of-precise-capture" tabindex="-1">The Chicken or the Egg: The Challenge of Precise Capture <a class="header-anchor" href="#the-chicken-or-the-egg-the-challenge-of-precise-capture" aria-label="Permalink to &quot;The Chicken or the Egg: The Challenge of Precise Capture&quot;">​</a></h3><p>So far, everything looks straightforward. But there&#39;s a tricky problem hiding in the details.</p><p>We only want to trace that <code>ping</code> process, and only the activity on the CPU core where it runs. To achieve this, we need two things:</p><ol><li>Tell ftrace which PID to record (via <code>set_event_pid</code>).</li><li>Tell ftrace which CPU to monitor (via <code>tracing_cpumask</code>). We use <code>taskset</code> to pin ping to a specific core.</li></ol><p><strong>But here&#39;s the problem</strong>: how do we get the PID?</p><p>You have to let the process start running before you can get its PID. But if you let it run, it might finish its job before you even finish configuring ftrace. This is a classic race condition—the chicken or the egg?</p><p>If you just run <code>ping</code> in the background and grab its PID, by the time you write that PID into <code>set_event_pid</code>, that ICMP packet might have long been sent, and your trace log will contain nothing but thin air.</p><p>To solve this problem, our script (<code>ping_ftrace.sh</code>) uses a slightly more complex &quot;wrapper&quot; trick.</p><p>We wrote a helper script called <code>runner</code>. The main script and <code>runner</code> perform a synchronization handshake that goes like this:</p><ol><li>The main script starts <code>runner</code> in the background and grabs its PID.</li><li><code>runner</code> doesn&#39;t immediately execute <code>ping</code>; instead, it <strong>waits there</strong>.</li><li>The main script takes that PID, configures ftrace with it, and gets everything ready.</li><li>The main script creates a &quot;trigger file.&quot;</li><li><code>runner</code> sees the trigger file appear, knows &quot;the stage is set,&quot; and immediately executes <code>exec ping ...</code>.</li></ol><p>Here&#39;s a key technical detail: <code>runner</code> uses <code>exec</code> to launch ping. In Unix, <code>exec</code> replaces the current process&#39;s memory image with a new process, but <strong>keeps the PID unchanged</strong>.</p><p>This means the PID of <code>runner</code> is the exact same as the PID of <code>ping</code>. When we configure ftrace in step 2, we use the PID of <code>runner</code>, but what actually gets traced is <code>ping</code>—because they share the same PID.</p><p>This isn&#39;t just a trick; it&#39;s an engineering solution that turns a synchronization problem into a deterministic workflow.</p><p>Once <code>ping</code> finishes running, the main script saves the report.</p><p>If you feel like this process is &quot;too convoluted&quot;—you&#39;re right, it absolutely is. To trace a single simple command, we wrote all this synchronization logic. This is exactly why we&#39;ll introduce <code>trace-cmd</code> in the next chapter: it takes care of all this dirty work for us.</p><hr><h3 id="let-s-go-looking-at-the-results" tabindex="-1">Let&#39;s Go! Looking at the Results <a class="header-anchor" href="#let-s-go-looking-at-the-results" aria-label="Permalink to &quot;Let&#39;s Go! Looking at the Results&quot;">​</a></h3><p>Let&#39;s run the script.</p><p>Figure 9.11 shows the moment the script runs. You can see the script outputting various logs, ping doing its job, and finally a prompt indicating the report has been generated.</p><p><img src="`+n+'" alt="figure"></p><p><em>Figure 9.11 – Our raw ftrace script tracing a single ping</em></p><p>Notice the size of the generated report—only 272 KB.</p><p>If you didn&#39;t apply any filtering, a single ping could produce a trace of tens of MB. This shows our filtering strategy worked: we chopped out the irrelevant noise and kept only the essence of the network stack.</p><p>You can find this report in the source code at <code>ch9/ftrace/ping_ftrace_report.txt</code>. Of course, your results might vary slightly depending on your kernel version and hardware.</p><p>Even though this report is trimmed down, it&#39;s still quite long. What should we be looking for? Here are a few key &quot;landmarks&quot;:</p><ol><li><code>__sys_socket()</code>: Where the Ping program creates the Socket.</li><li><strong>Transmit path</strong>: Usually called tx. Look for <code>sock_sendmsg()</code>, which is the starting point for sending a packet.</li><li><strong>Receive path</strong>: Usually called rx. Look for <code>net_rx_action()</code>.</li></ol><hr><h3 id="diving-into-the-transmit-path" tabindex="-1">Diving into the Transmit Path <a class="header-anchor" href="#diving-into-the-transmit-path" aria-label="Permalink to &quot;Diving into the Transmit Path&quot;">​</a></h3><p>Let&#39;s zoom in on the transmit path. When ping decides to send a packet, the kernel walks through this chain of functions (top to bottom):</p><ul><li><code>sock_sendmsg()</code></li><li><code>inet_sendmsg()</code></li><li><code>udp_sendmsg()</code> (Does Ping use the UDP protocol? No, Ping uses ICMP, but in the Linux kernel, certain paths might reuse UDP&#39;s transmit logic or similar encapsulation, or this is a general reference to network transmission in the original text. <em>Note: Strictly speaking, Ping uses a RAW socket to send ICMP, but we&#39;re following the original text&#39;s logic here</em> -&gt; The original text mentions functions like <code>udp_sendmsg</code>, which are typical stops along the network stack&#39;s transmit path)</li><li><code>ip_send_skb()</code></li><li><code>ip_output()</code></li><li><code>dev_queue_xmit()</code></li><li><code>dev_hard_start_xmit()</code></li></ul><p>This last function is the gateway to the driver. In this example, it calls <code>e1000_xmit_frame()</code> to hand the packet off to the actual network hardware.</p><p>Look at Figure 9.12, which is a screenshot from the real trace report. You can clearly see <code>sock_sendmsg</code> and <code>inet_sendmsg</code> highlighted.</p><p><img src="'+o+'" alt="figure"></p><p><em>Figure 9.12 – Trace snippet of the transmit path (upper half)</em></p><p>Scrolling down a bit further, Figure 9.13 shows the deeper levels of the call chain.</p><p><img src="'+r+'" alt="figure"></p><p><em>Figure 9.13 – Trace snippet of the transmit path (lower half, diving into the UDP/IP layer and driver layer)</em></p><p>See that indentation?</p><p>That&#39;s not just formatting; that&#39;s <strong>the passage of time</strong>. Every indentation level is a function call; every indentation step back is a return.</p><p>This is the charm of <code>function_graph</code>. It&#39;s not just a log; it&#39;s a movie that you can replay.</p><hr><h3 id="diving-into-the-receive-path" tabindex="-1">Diving into the Receive Path <a class="header-anchor" href="#diving-into-the-receive-path" aria-label="Permalink to &quot;Diving into the Receive Path&quot;">​</a></h3><p>After the packet is sent out, it has to come back.</p><p>The receive path is typically triggered by an Interrupt, and then the Softirq <code>NET_RX_SOFTIRQ</code> takes over. You should be able to find these landmark functions in the trace:</p><ul><li><code>net_rx_action()</code>: This is the main processing loop for softirqs.</li><li><code>__netif_receive_skb()</code>: Passing the packet up the protocol stack.</li><li><code>ip_rcv()</code>: IP layer processing.</li><li><code>udp_rcv()</code> or <code>icmp_rcv()</code>: Protocol layer dispatch.</li><li><code>sock_recvmsg()</code>: The data finally arrives in user space.</li></ul><p>Look at Figure 9.14, which shows the beginning of the receive path.</p><p><img src="'+h+'" alt="figure"></p><p><em>Figure 9.14 – Trace snippet of the receive path</em></p><p>And Figure 9.15 shows how the packet is ultimately delivered to the Socket layer.</p><p><img src="'+l+'" alt="figure"></p><p><em>Figure 9.15 – The final stage of the receive path</em></p><h3 id="validating-our-intuition" tabindex="-1">Validating Our Intuition <a class="header-anchor" href="#validating-our-intuition" aria-label="Permalink to &quot;Validating Our Intuition&quot;">​</a></h3><p>Honestly, the first time I saw this output, it felt magical.</p><p>Normally, when we write code, <code>socket()</code> -&gt; <code>send()</code> -&gt; <code>recv()</code>, it feels like we&#39;re calling black-box APIs. But now, through ftrace, that black box has been forcibly pried open.</p><p>You can see how <code>inet_sendmsg</code> translates a generic request into a UDP/IP request; you can see how <code>dev_queue_xmit</code> queues up to wait for the network card to be free; you can see how, when a hardware interrupt occurs, the kernel pauses the current process and jumps to <code>net_rx_action</code> to handle the packet.</p><p>On the surface, this section is about Ping, but what we&#39;re actually doing is validating our design intuition for the kernel&#39;s network stack. If you find that the code&#39;s execution path doesn&#39;t match what&#39;s in your head, it means your mental model needs adjusting—and ftrace is the tool to do exactly that.</p><p>Now, we&#39;ve completed this task using the most primitive tools available (shell scripts + tracefs). You must have felt it: <strong>it works, but it&#39;s incredibly tedious</strong>.</p><p>In the next section, we&#39;ll introduce <code>trace-cmd</code>. Let&#39;s see how it condenses this complex workflow of &quot;synchronization, filtering, capturing, and formatting&quot; into a single command.</p>',79)])])}const b=i(c,[["render",p]]);export{y as __pageData,b as default};
