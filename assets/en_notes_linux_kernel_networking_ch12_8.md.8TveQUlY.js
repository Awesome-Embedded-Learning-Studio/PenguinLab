import{_ as t,o as s,c as o,a}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch12_8.md","filePath":"en/notes/linux_kernel_networking/ch12_8.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_networking/ch12_8.md"};function i(r,e,h,l,d,p){return s(),o("div",null,[...e[0]||(e[0]=[a(`<h3 id="_12-8-mesh-networking-802-11s" tabindex="-1">12.8 Mesh Networking (802.11s) <a class="header-anchor" href="#_12-8-mesh-networking-802-11s" aria-label="Permalink to &quot;12.8  Mesh Networking (802.11s)&quot;">​</a></h3><p>In the previous section, we were marveling at high-speed &quot;point-to-point&quot; transmission mechanisms like A-MPDU and Block Ack, thinking this was the ultimate destination for wireless networking.</p><p>But reality hits fast—once you step out of that room with its几十-meter coverage range, or enter a harsh environment without wiring, that so-called &quot;high-speed&quot; AP becomes useless. You face a more primitive, thorny problem: <strong>how do we make wireless signals relay hop-by-hop to their destination?</strong></p><p>This is exactly what 802.11s sets out to solve. It is no longer satisfied with the centralized &quot;star&quot; topology; instead, it builds a truly decentralized &quot;mesh&quot; world.</p><hr><h4 id="from-ad-hoc-to-mesh-topology-evolution" tabindex="-1">From Ad Hoc to Mesh: Topology Evolution <a class="header-anchor" href="#from-ad-hoc-to-mesh-topology-evolution" aria-label="Permalink to &quot;From Ad Hoc to Mesh: Topology Evolution&quot;">​</a></h4><p>Before 802.11s, if you didn&#39;t want to use an AP, your only option was IBSS (also known as Ad Hoc mode). But Ad Hoc has a fatal weakness: it requires &quot;full mesh&quot; connectivity. It&#39;s like a group of people shouting in a dark room—everyone must be able to hear everyone else directly to ensure network connectivity. This is almost impossible in the real world. Once a node moves into a corner or gets blocked by a wall, the network fragments.</p><p>Mesh emerged to solve this exact &quot;fragility.&quot;</p><p>You can think of a Mesh as a mature community gossip network:</p><ul><li>In <strong>Full Mesh</strong>, everyone is friends with everyone else, and gossip spreads across the network instantly (this is the ideal state).</li><li>In <strong>Partial Mesh</strong> (more common), you only know a few neighbors, but you trust them to pass the word along.</li></ul><p>Back in Linux kernel 2.6.26 (2008), the <code>open80211s</code> project pushed draft 802.11s support into the wireless stack. This was thanks to sponsorship from OLPC (One Laptop Per Child) and various vendors, as well as the early code written by Luis Carlos Cobo and Javier Cardona at Cozybit. They made Linux the first general-purpose operating system to natively support Mesh at the kernel level.</p><hr><h4 id="hwmp-protocol-the-layer-2-routing-heart" tabindex="-1">HWMP Protocol: The Layer 2 Routing Heart <a class="header-anchor" href="#hwmp-protocol-the-layer-2-routing-heart" aria-label="Permalink to &quot;HWMP Protocol: The Layer 2 Routing Heart&quot;">​</a></h4><p>Since it&#39;s a &quot;mesh,&quot; a packet traveling from A to C might need to pass through B. How does B know where to forward the packet? Normally, we&#39;d rely on IP routing (Layer 3), but 802.11s is a Layer 2 protocol—it doesn&#39;t care about IP at all. Instead, it invented its own routing protocol: <strong>HWMP (Hybrid Wireless Mesh Protocol)</strong>.</p><p><strong>Why &quot;Hybrid&quot;?</strong> Because it combines two radically different routing strategies, oscillating between &quot;lazy&quot; and &quot;proactive&quot; approaches:</p><ol><li><p><strong>On-Demand Routing</strong> — &quot;Find it when you need it&quot;</p><ul><li>It doesn&#39;t maintain a routing table during idle times. Only when you need to send data to someone and realize you have no route (<code>mesh_path_lookup</code> fails) does it initiate a <strong>PREQ (Path Request)</strong>.</li><li>It&#39;s like needing to borrow money from someone, suddenly realizing you don&#39;t have their number, and standing at the village entrance shouting: &quot;I&#39;m looking for Zhang San! Does anyone know where Zhang San is?&quot;</li><li>The PREQ broadcasts throughout the Mesh (<code>IEEE80211_STYPE_ACTION</code>). Every node it passes through notes &quot;who is looking for Zhang San,&quot; until the shout finally reaches Zhang San&#39;s ears.</li></ul></li><li><p><strong>Proactive Routing</strong> — &quot;Tree-based broadcasting&quot;</p><ul><li>Some nodes are &quot;busy hubs&quot; (traffic aggregation points) that everyone wants to reach. Having everyone do on-demand lookups for them is too slow.</li><li>This node crowns itself the <strong>Root Mesh Point</strong> and periodically sends out a <strong>RANN (Root Announcement)</strong>: &quot;I am the root, connect to me.&quot;</li><li>Upon receiving this, other nodes proactively establish a &quot;tree-shaped&quot; path pointing to the root.</li></ul></li></ol><hr><h4 id="path-discovery-and-caching-the-preq-prep-duet" tabindex="-1">Path Discovery and Caching: The PREQ/PREP Duet <a class="header-anchor" href="#path-discovery-and-caching-the-preq-prep-duet" aria-label="Permalink to &quot;Path Discovery and Caching: The PREQ/PREP Duet&quot;">​</a></h4><p>Let&#39;s zoom in and see how a packet finds its way through the Mesh.</p><p><strong>Step 1: Lost and Shouting (PREQ)</strong> Suppose node A wants to send data to node D. A first checks its <code>mesh_path</code> table (defined in <code>net/mac80211/mesh.h</code>).</p><ul><li><strong>No route?</strong> Triggers <code>hwmp_preq_frame_process()</code> and sends a <strong>PREQ</strong> broadcast packet.</li><li>At this point, A can&#39;t just stand idle—data still needs to flow. A stuffs these unsent packets into a buffer called <code>frame_queue</code> (every <code>mesh_path</code> object has one).</li><li>Note that this buffer is very small, holding a maximum of only <strong>10</strong> packets (<code>MESH_FRAME_QUEUE_LEN</code>). If it fills up, additional packets are dropped—nothing more can be done.</li></ul><p><strong>Step 2: Response and Return (PREP)</strong> The PREQ hops along until it finally reaches D. D receives it and realizes: &quot;Hey, they&#39;re looking for me.&quot; It then unicasts a <strong>PREP (Path Reply)</strong> back along the reverse path the PREQ took. The PREP carries path information and makes its way back to A. Upon receiving the PREP, A finally establishes a complete route to D (stored in the <code>mesh_path</code> structure).</p><p><strong>Step 3: Releasing the Backlog</strong> Remember those packets stuffed into <code>frame_queue</code> earlier? As soon as the route is established, <code>mesh_path_tx_pending()</code> is called, flushing those 10 backlogged packets out all at once along the newly born route. It&#39;s like a newly unclogged pipe flowing freely again.</p><p><strong>Step 4: If the Route Breaks (PERR)</strong> Wireless environments are turbulent. An intermediate node B might lose power or suffer interference. B will emit a <strong>PERR (Path Error)</strong>, notifying everyone: &quot;Stop sending stuff my way, the road ahead is broken.&quot; Upon receiving the PERR, A might have to start over and send another PREQ.</p><hr><h4 id="the-cost-of-routing-airtime-metric" tabindex="-1">The Cost of Routing: Airtime Metric <a class="header-anchor" href="#the-cost-of-routing-airtime-metric" aria-label="Permalink to &quot;The Cost of Routing: Airtime Metric&quot;">​</a></h4><p>You might ask: if there are two paths to D, which one does HWMP choose? It doesn&#39;t just look at the lowest hop count; it looks at the <strong>Airtime Metric</strong>.</p><p>This is a highly pragmatic metric. It calculates: <strong>roughly how many microseconds of channel time will it cost to transmit a packet over this link?</strong> The calculation logic lives in <code>airtime_link_metric_get()</code> and looks roughly like this:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>开销 = (数据包长度 + 固定开销) / 传输速率</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><ul><li>If your signal is good (high rate), the overhead is small, and the Metric is low (better).</li><li>If your signal is poor (low rate, high packet error rate), the overhead is large, and the Metric is high (worse).</li></ul><p>Therefore, HWMP tends to choose the path that &quot;takes an extra hop but is fast on every hop,&quot; rather than the path that &quot;only takes two hops but crawls like a snail on each one.&quot;</p><hr><h4 id="hands-on-building-a-mesh-with-iw" tabindex="-1">Hands-on: Building a Mesh with <code>iw</code> <a class="header-anchor" href="#hands-on-building-a-mesh-with-iw" aria-label="Permalink to &quot;Hands-on: Building a Mesh with \`iw\`&quot;">​</a></h4><p>With all that said, how do we turn a wireless NIC into a Mesh node on Linux?</p><p>First, you need hardware that supports Mesh mode (many older NICs, or drivers with incomplete firmware support, will trip you up here). Then, forget the antiquated <code>iwconfig</code> and bring out the modern standard tool: <strong><code>iw</code></strong>.</p><p><strong>1. Create the Mesh Interface</strong></p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 把 wlan0 设置为 Mesh Point 模式</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iw</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wlan0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> type</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mesh</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 或者简写为 mp</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iw</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wlan0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> type</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mp</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p><strong>2. Join the Mesh Network</strong> Just like setting an SSID for Wi-Fi, a Mesh network also has an ID.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iw</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wlan0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mesh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> join</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;my-mesh-ID&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>As long as two or more nodes execute the same <code>mesh join</code> command (and are within RF range), they will start handshaking, exchanging PREQ/PREP, and establishing routes.</p><p><strong>3. Verify the Status</strong> Now you can look down on the network like a god:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 查看有哪些邻居（1-hop邻居）</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iw</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wlan0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> station</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dump</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 查看路由表（整个 Mesh 的路径）</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iw</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wlan0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mpath</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dump</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>In the <code>mpath dump</code> output, you can see the <code>NEXT_HOP</code> (next hop) and <code>METRIC</code> (cost). This is the direct result of the HWMP algorithm&#39;s hard work.</p><hr><h4 id="every-rose-has-its-thorn-the-cost-of-mesh" tabindex="-1">Every Rose Has Its Thorn: The Cost of Mesh <a class="header-anchor" href="#every-rose-has-its-thorn-the-cost-of-mesh" aria-label="Permalink to &quot;Every Rose Has Its Thorn: The Cost of Mesh&quot;">​</a></h4><p>Mesh sounds beautiful (wide coverage, strong self-healing, no wiring required), but don&#39;t rush to throw away your Ethernet cables—it has obvious side effects:</p><ol><li><strong>Broadcast Storms</strong>: HWMP&#39;s PREQs are broadcast, and route maintenance involves broadcasting too. In large Mesh networks, management frames can consume more than half the bandwidth, leaving your user data to scrape by in the cracks.</li><li><strong>Performance Bottlenecks</strong>: Every hop introduces latency and throughput degradation. Traveling 5 hops from A to B, your gigabit NIC might only achieve 100 Mbps speeds, and you&#39;ll have to endure extremely high ping values.</li><li><strong>Driver Hell</strong>: Although the kernel supports it, many vendors&#39; wireless drivers simply haven&#39;t implemented the Mesh callback functions, or their implementations are buggy. You might find that your NIC can connect to an AP just fine, but entering Mesh mode causes a kernel panic.</li></ol><hr><h4 id="summary-and-preview" tabindex="-1">Summary and Preview <a class="header-anchor" href="#summary-and-preview" aria-label="Permalink to &quot;Summary and Preview&quot;">​</a></h4><p>In this section, we stepped out of the comfort zone of a single AP and entered a more complex, dynamic distributed world. We evolved from the &quot;full-mesh kindergarten&quot; of IBSS to the &quot;multi-hop mature society&quot; of Mesh. We saw how HWMP uses PREQ/PREP to perform routing tricks at Layer 2, and how Linux encapsulates these complex protocols into a few simple commands via <code>mac80211</code> and <code>iw</code>.</p><p>But whether wired or wireless, Linux&#39;s network stack ultimately runs on hardware. In the next chapter, we will shift our gaze from the radio waves in the air to a higher-performance, more expensive hardware world—<strong>InfiniBand and RDMA</strong>. There, latency will be measured in microseconds, and the kernel&#39;s role will undergo a massive transformation.</p>`,51)])])}const g=t(n,[["render",i]]);export{u as __pageData,g as default};
