import{_ as s,o as t,c as a,a as i}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"6. Kernel Mechanism Essentials — Processes and Threads","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_programming/ch06.md","filePath":"en/notes/linux_kernel_programming/ch06.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_programming/ch06.md"};function r(o,e,l,h,c,p){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="_6-kernel-mechanism-essentials-—-processes-and-threads" tabindex="-1">6. Kernel Mechanism Essentials — Processes and Threads <a class="header-anchor" href="#_6-kernel-mechanism-essentials-—-processes-and-threads" aria-label="Permalink to &quot;6. Kernel Mechanism Essentials — Processes and Threads&quot;">​</a></h1><p>In the last chapter, we got our feet wet with kernel modules. By now, you should be able to write some simple code inside the kernel. But that&#39;s just the tip of the iceberg. The Linux kernel is massive, complex, and profound. If we want to navigate it freely, knowing only <code>printk</code> is far from enough.</p><p>In this chapter, we will truly open the door to the kernel&#39;s internal mechanisms. The topic we are going to discuss is <strong>how processes and threads are actually managed inside the kernel</strong>. Why is this important? Because if you don&#39;t understand what &quot;processes&quot; and &quot;threads&quot; look like through the kernel&#39;s eyes, you won&#39;t be able to grasp the memory management discussions in the next chapter, let alone write efficient drivers. We will see how those concepts you take for granted in user space—like stacks, heaps, or even the fact that &quot;I am running&quot;—are completely reconstructed from the kernel&#39;s perspective.</p><p>In fact, there is a fundamental difference between writing kernel code and writing user-space code: in user space, you are the master of your process; in kernel space, you are doing work in a <strong>borrowed context</strong>. Figuring out whose context you are &quot;borrowing,&quot; and what limitations that context imposes, is the core mission of this chapter.</p><p>To explain this clearly, I&#39;ve split this content into two chapters. This chapter focuses on the <strong>architecture of processes and threads</strong>, and the next chapter will dive into the <strong>internals of memory management</strong>. Of course, real kernel development experience is scattered throughout the entire book—like CPU scheduling, synchronization primitives, and so on—but those are stories for later.</p><p>The core questions we will tackle in this chapter include:</p><ul><li>On whose &quot;turf&quot; is kernel code actually running? (Process context vs. Interrupt context)</li><li>What does a process&#39;s Virtual Address Space (VAS) actually look like?</li><li>How does the kernel organize these processes, threads, and their stacks?</li><li>How do we find and manipulate that ultimate task-describing structure in the kernel — <code>task_struct</code>?</li><li>How do we iterate over all tasks in the system?</li></ul><p>Ready? Let&#39;s get our VM (Virtual Machine) ready first. Make sure you have set up the environment following the steps in the Online Chapter. If not, go back and get it done—the upcoming content is best verified by typing it in yourself.</p><hr><h2 id="_6-1-understanding-process-and-interrupt-contexts" tabindex="-1">6.1 Understanding Process and Interrupt Contexts <a class="header-anchor" href="#_6-1-understanding-process-and-interrupt-contexts" aria-label="Permalink to &quot;6.1 Understanding Process and Interrupt Contexts&quot;">​</a></h2><p>Back in Chapter 4, when we wrote our first kernel module, we briefly touched on kernel architecture. Now, it&#39;s time to lay this topic out in full.</p><p>Modern CPUs typically have the concept of privilege levels. For example, x86 has 4 Rings (Ring 0 being the most privileged, Ring 3 the least), ARM-32 has 7 modes, and ARM64 has Exception Levels (EL0-EL3). But regardless of the architecture, modern operating systems simplify this into <strong>two levels</strong> in practice: <strong>privileged</strong> (kernel mode) and <strong>unprivileged</strong> (user mode).</p><p>This is crucial.</p><p>What comes next might challenge your intuition a bit: Linux is a <strong>monolithic kernel</strong>. The literal meaning of this is &quot;one giant stone.&quot; What does this mean? It means that when your process initiates a system call, there is no mysterious &quot;kernel process&quot; that jumps out to do the work for you.</p><p><strong>The truth is: that process itself switches into kernel mode and personally executes the kernel code.</strong></p><p>So, we say that kernel code executes in <strong>process context</strong>. This isn&#39;t just terminology; it&#39;s the foundation for understanding kernel behavior. The vast majority of driver code, exception handling (like page faults), and even parts of the scheduler, all run in this context.</p><p>But you might ask: aside from process context, is there another way for kernel code to live?</p><p>Of course. Imagine you are deeply immersed in the joy of coding, and suddenly your network card receives a packet. A hardware interrupt signal is instantly triggered. No matter what the CPU is doing at that moment (even if it&#39;s executing kernel code), it must immediately stop, save its current state, and jump to execute the Interrupt Service Routine (ISR). The code executing at this moment is running in <strong>interrupt context</strong>.</p><p><strong>These are two completely different worldviews:</strong></p><ol><li><strong>Process context</strong>: Actively initiated by a process (via system call or exception), and synchronous. Essentially, &quot;I&quot; am still doing the work, just with elevated privileges.</li><li><strong>Interrupt context</strong>: Asynchronously triggered by hardware. At this point, you are not any process; you are the interrupt itself. You cannot sleep, you cannot block, and you must finish fast.</li></ol><p>Figure 6.1 illustrates this conceptual view: user-space threads enter the kernel via system calls; meanwhile, pure kernel threads (the kind that don&#39;t need user space) are silently doing their work; and interrupts are the uninvited guests that interrupt everything at any moment.</p><blockquote><p><strong>Mode A: SICP Tense Mode</strong> The distinction here is not an academic game; it is the line between life and death. In process context, you can allocate memory, wait for locks, and sleep. In interrupt context, if you dare to sleep, the system crashes immediately.</p><p>Why? Because if you trigger the scheduler while sleeping, how does the scheduler know who to wake up? You aren&#39;t even a &quot;process.&quot;</p><p>We&#39;ll teach you how to determine which context you&#39;re in later. But keep this intuition in mind for now.</p></blockquote><hr><h2 id="_6-2-understanding-the-basics-of-process-virtual-address-space-vas" tabindex="-1">6.2 Understanding the Basics of Process Virtual Address Space (VAS) <a class="header-anchor" href="#_6-2-understanding-the-basics-of-process-virtual-address-space-vas" aria-label="Permalink to &quot;6.2 Understanding the Basics of Process Virtual Address Space (VAS)&quot;">​</a></h2><p>Before diving into the kernel, we need to review the process&#39;s &quot;home&quot;—the Virtual Address Space (VAS). There is an iron rule here: <strong>memory is sandboxed</strong>. A process believes it has the entire memory space to itself; looking &quot;outside&quot; is impossible.</p><p>The user-space VAS is divided into several homogeneous regions, which we call <strong>segments</strong> or, more technically, <strong>mappings</strong>—because they are essentially pieced together by the kernel via the <code>mmap()</code> system call.</p><p>Figure 6.2 shows a minimal set of a standard Linux user-space process VAS. Let&#39;s do a quick pass from low addresses to high addresses:</p><h3 id="text-segment-code-segment" tabindex="-1">Text Segment (Code Segment) <a class="header-anchor" href="#text-segment-code-segment" aria-label="Permalink to &quot;Text Segment (Code Segment)&quot;">​</a></h3><p>This is where machine instructions reside. The Instruction Pointer (IP/PC) dances here. It is read-only (r-x). Note that the Text segment does not start at address 0. The page near address 0 is the famous &quot;null pointer trap page,&quot; specifically designed to catch NULL pointer accesses.</p><h3 id="data-segment" tabindex="-1">Data Segment <a class="header-anchor" href="#data-segment" aria-label="Permalink to &quot;Data Segment&quot;">​</a></h3><p>Immediately following the Text segment. It stores global and static variables (rw-). In reality, it is split into three parts:</p><ol><li><strong>Initialized Data</strong>: Initialized global/static variables.</li><li><strong>Uninitialized Data (BSS)</strong>: Uninitialized global/static variables, automatically zeroed at runtime.</li><li><strong>Heap</strong>: The home of <code>malloc()</code>. But note that in modern glibc, only requests smaller than 128KB (<code>MMAP_THRESHOLD</code>) are allocated from the heap. Larger chunks of memory are handled separately via <code>mmap()</code>, known as <strong>anonymous mapping</strong>. The heap is dynamic, and it is the only segment that grows toward <strong>higher addresses</strong>. The legal address boundary at the top of the heap is called the <strong>Program Break</strong> (which you can check with <code>sbrk(0)</code>).</li></ol><h3 id="shared-libraries" tabindex="-1">Shared Libraries <a class="header-anchor" href="#shared-libraries" aria-label="Permalink to &quot;Shared Libraries&quot;">​</a></h3><p>All dynamically linked shared libraries (.so) are mapped into a region somewhere between the heap and the stack.</p><h3 id="stack" tabindex="-1">Stack <a class="header-anchor" href="#stack" aria-label="Permalink to &quot;Stack&quot;">​</a></h3><p>This is the LIFO (Last-In, First-Out) world. Function calls, parameter passing, and local variables all live here. On modern CPUs (x86, ARM), the stack <strong>grows downward</strong>, which is known as a <strong>Fully Descending Stack</strong>.</p><p>Here is an interesting detail: although logically we say &quot;a stack frame is allocated for each function call,&quot; the actual physical operation isn&#39;t that complex—the Stack Pointer (SP) moves, and the stack frame appears; when returning, it just pops back. This design is incredibly fast.</p><p><strong>Regarding threads:</strong> A process has at least one thread (the <code>main()</code> thread). If there are multiple threads (thrd2, thrd3 in Figure 6.2), they share almost everything in the process&#39;s VAS—except the stack. <strong>Each thread has its own independent private stack</strong>. The main thread&#39;s stack is at the very top of the VAS, while the stacks of other threads sprout up in the &quot;shared region&quot; between the heap and the stack.</p><blockquote><p><strong>Analogy Recall: Back to the &quot;Sandbox&quot;</strong> We can imagine a process&#39;s VAS as a <strong>courtyard house</strong> (Sandbox).</p><ul><li><strong>Text/Data</strong> are the foundation and load-bearing walls; once built, they don&#39;t move.</li><li><strong>Heap</strong> is a temporary shed you build in the yard, which can continuously expand toward the center of the yard (higher addresses).</li><li><strong>Stack</strong> is a basket hanging from the ceiling. The more things you put in, the heavier the basket gets, and the longer the rope extends (growing toward lower addresses).</li><li><strong>Threads</strong>: If they are a family living in the courtyard house, everyone shares the kitchen and bathroom, but each person has their own <strong>private stash (stack)</strong> hidden in their own suitcase. You can&#39;t go rummaging through someone else&#39;s suitcase, or things will get messy.</li></ul><p>This analogy helps you understand why in multi-threaded programming, local variables are safe (in their own baskets), while global variables require caution (everyone can see them).</p></blockquote><hr><h2 id="_6-3-organizing-processes-threads-and-stacks-—-user-space-and-kernel-space" tabindex="-1">6.3 Organizing Processes, Threads, and Stacks — User Space and Kernel Space <a class="header-anchor" href="#_6-3-organizing-processes-threads-and-stacks-—-user-space-and-kernel-space" aria-label="Permalink to &quot;6.3 Organizing Processes, Threads, and Stacks — User Space and Kernel Space&quot;">​</a></h2><p>The traditional UNIX philosophy is &quot;everything is a process.&quot; But in the eyes of the modern kernel, <strong>threads are the basic unit of scheduling</strong>. The Linux kernel does not distinguish between processes and threads; a thread is simply &quot;a process that shares certain resources.&quot; Every thread—whether in user space or in the kernel—corresponds to a kernel metadata structure: <strong><code>task_struct</code></strong>. We will discuss this structure in detail later.</p><p>Here is a key point that is easily overlooked: <strong>we need to prepare a stack for each thread at every privilege level.</strong></p><p>Linux has two privilege levels: user mode and kernel mode. This means that <strong>every living user-space thread actually has two stacks:</strong></p><ol><li><strong>User-space stack</strong>: Used when running user code. The local variables you define in C live here.</li><li><strong>Kernel-space stack</strong>: Once you trap into the kernel via a system call, or trigger an exception (like a page fault), the CPU switches to kernel mode, and the Stack Pointer (SP) instantly points to this kernel stack. All kernel function calls and local variables within the kernel reside on this tiny kernel stack.</li></ol><p><strong>The only exception is kernel threads</strong>. They are born in the kernel and only see kernel space, so they <strong>only have a kernel stack</strong> and no user stack.</p><blockquote><p><strong>⚠️ Pitfall Warning</strong> Don&#39;t assume the kernel stack is large. On 32-bit systems, it&#39;s usually only 2 pages (8KB); on 64-bit systems, it&#39;s only 4 pages (16KB). Defining a <code>int huge_array[4096]</code> in a kernel module? You might blow the stack right out, leading to a hard-to-debug crash. Later on, when we need large chunks of memory in the kernel, we must allocate them dynamically (kmalloc/vmalloc). Never try to be cheap by putting them on the stack.</p></blockquote><p><strong>One more detail: Hardware IRQ Stacks</strong> When a hardware interrupt occurs, the CPU doesn&#39;t necessarily use the interrupted process&#39;s kernel stack (that&#39;s too small—what if the interrupt handler is also greedy?). Many architectures (including x86_64) prepare a separate <strong>IRQ stack</strong> for each CPU core, dedicated to handling interrupts. This prevents the interrupt handler from trampling an innocent process&#39;s kernel stack.</p><hr><h2 id="_6-4-inspecting-the-user-stack-and-kernel-stack" tabindex="-1">6.4 Inspecting the User Stack and Kernel Stack <a class="header-anchor" href="#_6-4-inspecting-the-user-stack-and-kernel-stack" aria-label="Permalink to &quot;6.4 Inspecting the User Stack and Kernel Stack&quot;">​</a></h2><p>Stacks are your lifeline when debugging. If you want to figure out &quot;how did the program get here?&quot; or &quot;why is it stuck here?&quot;, looking at the stack is almost the only way. But as we just mentioned, each thread has two stacks—how do we look at them?</p><h3 id="inspecting-the-kernel-stack-proc-pid-stack" tabindex="-1">Inspecting the Kernel Stack: <code>/proc/PID/stack</code> <a class="header-anchor" href="#inspecting-the-kernel-stack-proc-pid-stack" aria-label="Permalink to &quot;Inspecting the Kernel Stack: \`/proc/PID/stack\`&quot;">​</a></h3><p>Good news: the kernel has made this part very user-friendly. As long as you have root privileges, you can directly read <code>/proc/PID/stack</code> to get the current kernel stack trace of that process.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /proc/2549/stack</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] do_wait+0x184/0x340</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] kernel_wait4+0xaf/0x150</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] __do_sys_wait4+0x89/0xa0</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] __x64_sys_wait4+0x1e/0x30</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] do_syscall_64+0x5c/0x90</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] entry_SYSCALL_64_after_hwframe+0x63/0xcd</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>How do you read it?</p><ul><li><strong>From bottom to top</strong>. The very bottom is the entry point (<code>entry_SYSCALL_64...</code>), and the very top is the function currently executing (<code>do_wait</code>).</li><li><code>do_wait+0x184/0x340</code> means: currently stopped at an offset of <code>0x184</code> bytes inside this function, and the total length of the function is <code>0x340</code> bytes.</li><li><code>[&lt;0&gt;]</code> was originally the code segment address, but to prevent hackers from leaking kernel layout information through these details (KASLR), modern kernels usually zero it out.</li></ul><p>Look, our Bash process is currently hanging out in the kernel, dumbly waiting for a child process to finish via the <code>wait4</code> system call.</p><h3 id="inspecting-the-user-stack-gdb-magic" tabindex="-1">Inspecting the User Stack: GDB Magic <a class="header-anchor" href="#inspecting-the-user-stack-gdb-magic" aria-label="Permalink to &quot;Inspecting the User Stack: GDB Magic&quot;">​</a></h3><p>Inspecting the user stack is actually a bit more troublesome. The most universal way is to use GDB. For convenience, you can write a simple script <code>ch6/ustack</code>, the core of which is calling GDB&#39;s <code>bt</code> command.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gdb</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -ex</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;set pagination 0&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -ex</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;thread apply all bt&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --batch</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $PID</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>If we throw this at our Bash process from earlier, you&#39;ll see output similar to this:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Thread </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (process </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2549</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">):</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">#</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  0x</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">00007fadd3109c3a</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> in __GI___wait4 ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">#</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  0x</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0000555b98cd4f03</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> in </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">#</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  0x</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0000555b98cd6373</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> in </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">wait_for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">...</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Likewise, read this from bottom to top. The <code>#0</code> at the very top is the current frame.</p><h3 id="ebpf-modern-magic" tabindex="-1">eBPF: Modern Magic <a class="header-anchor" href="#ebpf-modern-magic" aria-label="Permalink to &quot;eBPF: Modern Magic&quot;">​</a></h3><p>If you feel the above approach is too &quot;traditional,&quot; let&#39;s look at modern weaponry — <strong>eBPF</strong>.</p><p>Using <strong><code>stackcount</code></strong> from the <strong>BCC (BPF Compiler Collection)</strong> toolkit, we can do even cooler things: <strong>trace both the kernel stack and the user stack simultaneously</strong>.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 举个栗子，我们想看 ping 程序在干嘛</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stackcount-bpfcc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  \`</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pgrep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ping\`</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  --delimited</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  ping_echo_send</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>This tool will attach itself to the Ping process like handing out flyers. Once it calls <code>ping_echo_send</code>, it captures and prints both the kernel and user call stacks at that exact moment. The <code>--delimited</code> parameter draws a <code>--</code> divider line in the output, with the kernel on the left and the user on the right.</p><blockquote><p><strong>Feynman Flow Mode</strong> Honestly, the first time I saw the output from tools like eBPF, my reaction was &quot;I don&#39;t fully get it, but it&#39;s awesome.&quot; You don&#39;t need to master all the details of BCC right now, but you need to build an intuition: Modern kernel debugging is no longer just staring at <code>/proc</code> files; it&#39;s about using instrumentation to make the kernel &quot;spill its guts&quot; on critical paths for you to see.</p><p>This is a God&#39;s-eye view. We used to guess what happened; now we watch it happen in real time.</p></blockquote><hr><h2 id="_6-5-the-kernel-task-structure-task-struct" tabindex="-1">6.5 The Kernel Task Structure: <code>task_struct</code> <a class="header-anchor" href="#_6-5-the-kernel-task-structure-task-struct" aria-label="Permalink to &quot;6.5 The Kernel Task Structure: \`task_struct\`&quot;">​</a></h2><p>Alright, now let&#39;s go back to that big family portrait (Figure 6.3/6.5). Besides code and data, the most core things in kernel space are those individual <strong><code>task_struct</code></strong> structures.</p><p>This is the &quot;ID card&quot; in the kernel&#39;s eyes. Every living thread—whether user-space or kernel—has a corresponding <code>task_struct</code>. It stores everything: the PID, memory descriptors, open files, signal handling, scheduling information... even whether this thread has been sleeping a lot lately.</p><p>This structure is defined in <code>include/linux/sched.h</code>. It is absurdly large (13KB+ on x86_64 in kernel 6.1).</p><blockquote><p><strong>SICP Tense Mode</strong> This is not just a data structure. This is the operating system&#39;s abstraction of the real world. When we write <code>current-&gt;pid</code> in our code, we are essentially asking the kernel: &quot;Who is the soul currently running on the CPU?&quot;</p></blockquote><hr><h2 id="_6-6-accessing-the-task-structure-via-the-current-macro" tabindex="-1">6.6 Accessing the Task Structure via the <code>current</code> Macro <a class="header-anchor" href="#_6-6-accessing-the-task-structure-via-the-current-macro" aria-label="Permalink to &quot;6.6 Accessing the Task Structure via the \`current\` Macro&quot;">​</a></h2><p>Since there are hundreds or thousands of <code>task_struct</code> hanging on the kernel&#39;s linked lists, when my kernel code is running, how do I know &quot;who am I&quot;?</p><p>The kernel developers are a clever bunch; they came up with a macro: <strong><code>current</code></strong>.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;linux/sched.h&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">current</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pid;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // 拿到当前进程（线程）的 PID</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">current</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">comm;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 拿到当前进程的名字（去掉路径的）</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>You can think of <code>current</code> like the <code>this</code> pointer in C++, but it points to the <code>task_struct</code> of the thread currently executing kernel code.</p><p>Its implementation is highly architecture-dependent:</p><ul><li><strong>x86_64</strong>: Leverages per-CPU variables, making it extremely fast with no locks needed.</li><li><strong>ARM64 / PowerPC</strong>: Even dedicates a general-purpose register to store this thing, making it a single-instruction lookup.</li></ul><h3 id="determining-context-who-am-i-where-am-i" tabindex="-1">Determining Context: Who am I? Where am I? <a class="header-anchor" href="#determining-context-who-am-i-where-am-i" aria-label="Permalink to &quot;Determining Context: Who am I? Where am I?&quot;">​</a></h3><p>We repeatedly emphasized earlier that you must not sleep in atomic context. So how do you know if you&#39;re in atomic context? The kernel provides an incredibly handy macro:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;linux/preempt.h&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">in_task</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 这是进程上下文，通常可以睡觉</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    foo</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 这是中断上下文（或持有自旋锁），绝对不能睡！</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    bar</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><blockquote><p><strong>⚠️ Pitfall Warning</strong><code>in_task()</code> returning true doesn&#39;t necessarily mean you can sleep! If you are in process context but holding a <strong>spinlock</strong>, you are still part of an atomic context. Calling <code>msleep()</code> then? You&#39;re just asking to deadlock.</p><p>Remember a simple rule: don&#39;t sleep while holding a lock, and don&#39;t sleep in an interrupt.</p></blockquote><h3 id="hands-on-the-current-affairs-module" tabindex="-1">Hands-on: The <code>current_affairs</code> Module <a class="header-anchor" href="#hands-on-the-current-affairs-module" aria-label="Permalink to &quot;Hands-on: The \`current_affairs\` Module&quot;">​</a></h3><p>Let&#39;s write a module to test the waters. We&#39;ll print the current context information when the module initializes (<code>init</code>) and exits (<code>cleanup</code>).</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 代码节选自 ch6/current_affairs/current_affairs.c */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> show_ctx</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">nm</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ... 头文件包含 ...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">likely</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">in_task</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">())) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        pr_info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Running in process context ::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">                &quot; name        : </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">                &quot; PID         : </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%6d\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">                &quot; TGID        : </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%6d\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">                &quot; UID         : </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%6u\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">                &quot; EUID        : </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%6u</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> root)</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                // ... 打印指针地址 ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                current-&gt;comm,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">                task_pid_nr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(current),</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 推荐使用 helper 宏</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">                task_tgid_nr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(current),</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                );</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        pr_alert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Whoa! running in interrupt context! Should NOT happen here</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br></div></div><p>Here we use a common micro-optimization macro, <code>likely()</code>. It tells the compiler: &quot;This condition is highly likely to be true, so optimize the assembly code in that direction.&quot; You&#39;ll see this everywhere in the kernel.</p><p>When you <code>insmod</code> this module, guess who <code>current</code> points to?</p><p><strong>It&#39;s the <code>insmod</code> process itself!</strong></p><blockquote><p><strong>Callback: Linux is a Monolithic Kernel</strong> Remember what we said in the chapter intro? &quot;You are in a borrowed context.&quot; When you type <code>insmod module.ko</code> in the terminal, the <code>insmod</code> process initiates a system call, and the kernel switches to privileged mode. There is no &quot;kernel daemon&quot; taking over your module. <strong>It is you (that <code>insmod</code> process) executing the module&#39;s <code>init</code> function in kernel mode.</strong></p><p>This is the essence of a monolithic kernel. (A microkernel is the model where you send messages to a &quot;server process&quot; to get things done.)</p></blockquote><hr><h2 id="_6-7-traversing-the-kernel-s-task-list" tabindex="-1">6.7 Traversing the Kernel&#39;s Task List <a class="header-anchor" href="#_6-7-traversing-the-kernel-s-task-list" aria-label="Permalink to &quot;6.7 Traversing the Kernel&#39;s Task List&quot;">​</a></h2><p>Since all <code>task_struct</code> are strung together on a massive doubly-linked circular list, can we look at them one by one, like flipping through a roster? Of course we can.</p><p>The kernel provides a macro called <code>for_each_process()</code>.</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#include</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &lt;linux/sched/signal.h&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> task_struct </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">p;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">for_each_process</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // p 指向每一个进程的 task_struct</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 注意：它只遍历每个进程的主线程</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>But there&#39;s a catch: <code>for_each_process</code> technically only iterates over the &quot;leader&quot; (main thread) of each process group. If you want to find <strong>all threads</strong> in the system (including those underlings in multi-threaded processes), you need a more powerful macro:</p><ul><li><strong>Older kernels (&lt; 6.6)</strong>: <code>do_each_thread(p, t) { ... } while_each_thread(p, t);</code></li><li><strong>Newer kernels (&gt;= 6.6)</strong>: <code>for_each_process_thread(p, t);</code></li></ul><p>We wrote a demo module <code>ch6/foreach/thrd_showall</code>. When you insert it and look at the output in <code>dmesg</code>, you&#39;ll see a densely packed task list.</p><p>In the output of Figure 6.12, there is a very interesting column: <strong>TGID vs PID</strong>.</p><ul><li><strong>PID</strong>: In the kernel&#39;s eyes, this is the Thread ID. Every thread is different.</li><li><strong>TGID</strong>: Thread Group ID. This is what we call the &quot;Process ID&quot; in user space.</li></ul><p>If a process is single-threaded, PID == TGID. If it is multi-threaded, the main thread&#39;s PID == TGID, while the other threads have different PIDs, but their TGID all equal the main thread&#39;s PID.</p><p>You can verify this with the <code>ps -LA</code> command:</p><ul><li>The first column, <code>PID</code>, is actually the TGID.</li><li>The second column, <code>LWP</code> (Light-Weight Process), is the PID in the kernel.</li></ul><hr><h3 id="chapter-summary" tabindex="-1">Chapter Summary <a class="header-anchor" href="#chapter-summary" aria-label="Permalink to &quot;Chapter Summary&quot;">​</a></h3><p>In this chapter, we built a complete map: from CPU privilege levels, to a process&#39;s Virtual Address Space (heap, stack, code segment), to how the kernel manages hundreds or thousands of threads using <code>task_struct</code> and doubly-linked lists, and how we locate ourselves in this massive web using the <code>current</code> macro.</p><p>In particular, the <strong>distinction between process context and interrupt context</strong>, along with the monolithic kernel characteristic that <strong>&quot;user-space processes execute kernel code themselves&quot;</strong>, forms the foundation for understanding all subsequent driver models. Without understanding this, when you write code like &quot;calling <code>copy_from_user</code> while holding a spinlock,&quot; you&#39;ll just be left staring blindly at a system crash.</p><p>In the next chapter, we will dive into another dimension of this memory — <strong>memory management</strong>. You&#39;ll discover how the structures we discussed today (task_struct, mm_struct) actually mount to physical memory pages.</p><p>It&#39;s a long journey, but now you have the map.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: Understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: Understanding&quot;">​</a></h3><p><strong>Question</strong>: In Linux kernel programming, why are functions that might sleep (like <code>kmalloc(GFP_KERNEL)</code> or <code>down()</code>) strictly prohibited in interrupt context? What does this have to do with process scheduling?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Because interrupt context is not associated with any specific Process Descriptor, and there is no &quot;returnable&quot; process for the scheduler to switch to. If you sleep in an interrupt, the scheduler cannot find the correct process to resume execution, leading to a system deadlock or crash.</p><p><strong>Analysis</strong>: Tests the understanding of the essential difference between Process Context and Interrupt Context.</p><ol><li><strong>Process context</strong>: Triggered by a system call or exception, attached to a specific process (the <code>current</code> pointer is valid). If it sleeps, the scheduler can suspend the current process and wake it up later to resume.</li><li><strong>Interrupt context</strong>: Triggered asynchronously by hardware, not part of any specific process. The interrupt handler interrupted a running process (whether in user mode or kernel mode).</li></ol><p>If the interrupt handler attempts to sleep, the kernel scheduler will try to switch the CPU to another process. However, since the interrupt was not initiated by a &quot;process,&quot; the scheduler cannot return the CPU to the state it was in when the interrupt occurred (because not enough &quot;process context&quot; information was saved to allow such a return). Therefore, interrupt handlers must execute quickly and cannot block or sleep.</p></details><h3 id="exercise-2-application" tabindex="-1">Exercise 2: Application <a class="header-anchor" href="#exercise-2-application" aria-label="Permalink to &quot;Exercise 2: Application&quot;">​</a></h3><p><strong>Question</strong>: On a running Linux system, you have written a kernel module. In one of its functions, you need to determine whether the current code is in &quot;atomic context.&quot; Which kernel macro would you use to make this determination? Please write the name of the macro and briefly describe two typical scenarios where its return value is true.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Use the <code>in_atomic()</code> macro.</p><p>Two typical scenarios:</p><ol><li>Executing within a hardware interrupt handler or softirq.</li><li>In process context while holding a spinlock.</li></ol><p><strong>Analysis</strong>: Tests the practical application of kernel context detection tools.</p><ul><li><strong><code>in_atomic()</code></strong>: This macro is used to detect whether code is in an atomic context, meaning whether process scheduling is allowed.</li><li><strong>Scenario 1 (Interrupt Context)</strong>: When handling a hardware interrupt, sleeping is obviously not allowed, and <code>in_atomic()</code> returns true.</li><li><strong>Scenario 2 (Spinlock)</strong>: In process context, if the code already holds a spinlock, it has entered an atomic critical section. Although it belongs to process context, sleeping while holding a spinlock can lead to deadlocks, so <code>in_atomic()</code> also returns true.</li></ul><p>Note: <code>in_interrupt()</code> can also be used to detect interrupt context, but <code>in_atomic()</code> has broader coverage, including situations where a spinlock is held.</p></details><h3 id="exercise-3-application" tabindex="-1">Exercise 3: Application <a class="header-anchor" href="#exercise-3-application" aria-label="Permalink to &quot;Exercise 3: Application&quot;">​</a></h3><p><strong>Question</strong>: Suppose you are writing a kernel driver for a 64-bit system. In your driver, you define a local array <code>char buf[4096]</code>. Although it compiles successfully, the system occasionally crashes (Kernel Panic) under heavy load. Based on your knowledge of &quot;kernel stack size,&quot; analyze what the most likely cause of the crash is. Why don&#39;t we usually need to worry about this issue in user-space programming?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: The cause of the crash is a <strong>kernel stack overflow</strong>.</p><p>The kernel stack is very small (usually only 16KB on 64-bit systems) and fixed in size. Every level deeper the function call chain goes, and every bit larger the local variables are, consumes this limited stack space. If the call chain is deep enough, a 4096-byte local array can easily eat up 1/4 to 1/2 of the stack space, causing a stack overflow that overwrites the adjacent <code>thread_info</code> or other critical data, triggering a Panic.</p><p>In user space, however, the stack space is dynamically allocated, typically defaulting to around 8MB, and can be adjusted via <code>ulimit</code>. The space is very generous, making stack overflows caused by local arrays extremely rare.</p><p><strong>Analysis</strong>: Tests practical engineering awareness of the differences between the kernel stack and the user stack.</p><ol><li><strong>Kernel stack limits</strong>: Each thread&#39;s kernel stack usually has only 2 pages (32-bit) or 4 pages (64-bit), i.e., 8KB or 16KB. This must accommodate all function stack frames along the entire kernel call path.</li><li><strong>Risk</strong>: Defining large local variables in the kernel is extremely dangerous. Under heavy system load, interrupt handling and nested driver calls can be quite deep, instantly exhausting stack space.</li><li><strong>User stack comparison</strong>: The user-space stack resides in the VAS&#39;s Stack Segment. Not only is the space large, but it can also grow dynamically. The Linux kernel not only provides the space but also handles expansion via the <code>guard page</code> mechanism when the stack approaches its limit. Unless recursion goes out of control, overflows are generally unlikely.</li><li><strong>Best practice</strong>: When large chunks of memory are needed in the kernel, <code>kmalloc()</code> or heap allocation should be used, rather than local arrays.</li></ol></details><h3 id="exercise-4-thinking" tabindex="-1">Exercise 4: Thinking <a class="header-anchor" href="#exercise-4-thinking" aria-label="Permalink to &quot;Exercise 4: Thinking&quot;">​</a></h3><p><strong>Question</strong>: Thought exercise: To defend against ROP (Return-Oriented Programming) attacks, the Linux kernel introduced &quot;user-space shadow stacks.&quot; However, this feature (as mentioned in the relevant chapter) explicitly states that it targets &quot;user space.&quot; Please analyze: why doesn&#39;t the kernel usually need the same hardware-level shadow stack to protect its own return addresses? Or more deeply, if kernel code is exploited, would this hardware protection mechanism still be effective?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: The kernel usually doesn&#39;t need a separate hardware shadow stack, and hardware protection mechanisms often become ineffective under the kernel&#39;s highest privilege level.</p><p><strong>Analysis reasons:</strong></p><ol><li><strong>Ring 0 privileges</strong>: The kernel runs at the highest privilege level. If an attacker can already control kernel execution flow (hijacking the return address), it means the attacker has already gained Ring 0 privileges. An attacker with the highest privileges can disable the shadow stack feature (e.g., by modifying the CR4 register) or directly read/write the Model-Specific Registers (MSRs) that control the shadow stack&#39;s memory model, thereby bypassing the protection.</li><li><strong>Performance and overhead</strong>: The kernel is extremely sensitive to performance. Enabling shadow stacks brings significant context-switching overhead and interrupt handling latency.</li><li><strong>Existing mechanisms</strong>: The kernel primarily relies on strict code review, compile-time checks using <code>__builtin_return_address</code> (like STACKPROTECTOR), and memory partitioning to mitigate stack overflow issues, rather than relying on runtime hardware locking of return addresses.</li></ol><p><strong>Analysis</strong>: This is a comprehensive thought exercise involving security mechanisms and privilege models.</p><ul><li><strong>Principle of shadow stacks</strong>: It stores return addresses separately in a protected, read-only memory area. When a function returns, the CPU compares the return address on the normal stack with the one in the shadow stack. If they don&#39;t match, an exception is triggered.</li><li><strong>Applicability to user space</strong>: User programs run at a low privilege level and cannot tamper with the shadow stack&#39;s configuration or memory, making this hardware-enforced protection highly effective.</li><li><strong>The paradox in kernel space</strong>: The cornerstone of security mechanisms is &quot;high privilege protects low privilege.&quot; But if an attacker breaches the kernel, they possess the highest privileges. Who supervises the one with the highest privileges? <ul><li>If an attacker can overwrite the return address on the kernel stack, a kernel write vulnerability already exists. The attacker could just as easily build a ROP chain to execute instructions that disable shadow stack protection while overwriting the return address.</li><li>Therefore, kernel security focuses more on <strong>preventing unintended memory writes</strong> (like using SMEP/SMAP to prevent the kernel from executing user-space code) rather than solely protecting return addresses from being modified.</li></ul></li></ul></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>Linux uses a monolithic kernel architecture, which means that when a system call or exception occurs, it is not some mysterious &quot;kernel process&quot; that takes over the CPU. Instead, the current process <strong>actively switches to a privileged level (kernel mode)</strong> and borrows its own context to execute kernel code. Understanding this is crucial because kernel code almost always runs in a &quot;borrowed context,&quot; and we must constantly be vigilant about the current execution environment (process context or interrupt context), as the latter strictly prohibits sleeping or blocking operations.</p><p>The kernel manages all scheduling entities through a massive doubly-linked list and the core structure <strong><code>task_struct</code></strong>. In the eyes of the Linux kernel, <strong>threads are the basic unit of scheduling</strong>, and what we call a &quot;process&quot; is essentially a group of threads that share specific resources (like a memory address space). Whether it&#39;s a user-space thread or a pure kernel thread, it is described by an independent <code>task_struct</code> in the kernel. This means the kernel doesn&#39;t specifically distinguish between processes and threads; it only sees the individual as a scheduling entity.</p><p>Due to the division of privilege levels, every active user-space thread actually possesses <strong>two stacks</strong>: a <strong>user stack</strong> for running user code (dynamically allocated via <code>malloc</code>, etc.), and a <strong>kernel stack</strong> used when trapping into the kernel (usually very small, only 16KB on x86_64). When writing kernel modules, developers must constantly be wary of the kernel stack&#39;s size limit. Defining large arrays on the stack is strictly forbidden, otherwise it can easily lead to stack overflows and system crashes. Large chunks of memory must be obtained through dynamic allocation.</p><p>A process&#39;s Virtual Address Space (VAS) is strictly sandboxed, divided into regions like the code segment, data segment, heap, and stack. The heap grows toward higher addresses, while the stack (Fully Descending Stack) grows toward lower addresses. For multi-threaded programs, all threads share the process&#39;s VAS (code segment, global variables, etc.), but <strong>each thread has its own independent user stack area</strong>. This is the root cause of why local variables are safe in multi-threaded programming, while global variables require synchronization protection.</p><p>Locating &quot;who am I&quot; in kernel code is achieved through the macro <code>current</code>, which points to the <code>task_struct</code> of the task currently running on the CPU. Developers can use helper macros like <code>in_task()</code> to determine whether they are in process context or interrupt context: in process context, you can usually request resources or briefly sleep, but in interrupt context or while holding a spinlock, any operation that might cause blocking will trigger a system crash. Additionally, traversing all tasks in the system requires the <code>for_each_process_thread</code> macro, and you need to distinguish between the thread ID (PID) in kernel concepts and the process ID (TGID) as seen in user space.</p>`,134)])])}const k=s(n,[["render",r]]);export{u as __pageData,k as default};
