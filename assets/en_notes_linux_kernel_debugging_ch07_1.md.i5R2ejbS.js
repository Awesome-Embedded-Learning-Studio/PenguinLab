import{_ as t,o as a,c as o,a as n}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"Chapter 7: When the Kernel Roars","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch07_1.md","filePath":"en/notes/linux_kernel_debugging/ch07_1.md","lastUpdated":null}'),s={name:"en/notes/linux_kernel_debugging/ch07_1.md"};function i(r,e,l,h,c,p){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="chapter-7-when-the-kernel-roars" tabindex="-1">Chapter 7: When the Kernel Roars <a class="header-anchor" href="#chapter-7-when-the-kernel-roars" aria-label="Permalink to &quot;Chapter 7: When the Kernel Roars&quot;">​</a></h1><p>There is a class of problems that can instantly send a chill down a systems engineer&#39;s spine.</p><p>It&#39;s not high CPU usage, nor is it a memory leak—those are annoying, but at least the system is still alive. You can still SSH in, still read the logs, still observe it like a doctor examining a patient.</p><p>The truly terrifying scenario is the sudden, dead silence. The cursor on the screen stops moving, heartbeat packets drop, or the monitor even goes completely black. The system isn&#39;t just broken; it&#39;s broken ugly—without even leaving a final dying word.</p><p>In this chapter, we tackle exactly this nightmare scenario: <strong>kernel panics</strong>.</p><p>When kernel code goes wrong, there is no &quot;exception handling&quot; to catch the fall. A process crash only harms itself, but a kernel crash means the collapse of the entire world. However, the kernel doesn&#39;t die quietly. It leaves behind an angry diagnostic message—a <strong>Kernel Oops</strong>.</p><p>Reading an Oops is like deciphering a killer&#39;s code left at a crime scene. It is obscure, packed with hexadecimal addresses, and littered with hardware register jargon, making you want to close the terminal at first glance. But once you master the method, this seemingly garbled text will tell you with extreme precision: which pointer was NULL, which line of assembly triggered the illegal access, and exactly which colleague&#39;s code caused the disaster.</p><p>Even better, the Linux kernel community has long embedded a complete toolchain to make life a little easier for us unlucky engineers: from <code>procmap</code> that can see through the memory layout, to <code>decodecode</code> that can act like a detective to reverse-engineer machine code back into source lines, all the way to <code>netconsole</code> that can throw logs over the network before the system completely hangs.</p><p>In this chapter, we won&#39;t just learn how to read the kernel&#39;s &quot;dying words.&quot; We will set up a hands-on environment, deliberately trigger a crash, and use these tools to dissect it clearly.</p><p>Ready to enter the ICU? Let&#39;s begin.</p><hr><h2 id="_7-1-technical-preparation" tabindex="-1">7.1 Technical Preparation <a class="header-anchor" href="#_7-1-technical-preparation" aria-label="Permalink to &quot;7.1 Technical Preparation&quot;">​</a></h2><p>Before we start crashing the kernel, we need to get our toolbox ready.</p><p>The good news is that our base workspace configuration is exactly the same as in Chapter 1. If you&#39;ve already followed the previous chapters to set up your development environment and configure the kernel source tree, you can skip this part—no need to spin up a new virtual machine or reinstall the system.</p><p>All the example code is available in this book&#39;s GitHub repository. You can clone it directly:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clone</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://github.com/PacktPublishing/Linux-Kernel-Debugging.git</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Since we are going to dive deep into the kernel&#39;s memory layout and crash scenes, there is one new tool that is very important—<strong>procmap</strong>.</p><p>You already saw <code>/proc/&lt;pid&gt;/maps</code> in Chapter 1. That&#39;s a plain text file; while it contains all the information, it&#39;s really hard to read, especially when you&#39;re staring at hundreds of lines of memory mappings—it&#39;s easy for your eyes to glaze over. <code>procmap</code> is a dedicated visualization tool that can draw a clear map of a process&#39;s address space, including both user space and kernel space. In the &quot;NULL pointer dereference&quot; and &quot;kernel-mode Oops&quot; cases we are about to handle, this map will be your navigator, helping you see at a glance exactly where a virtual address maps to.</p><p>When we analyze the NULL trap page later, you&#39;ll thank this tool—it gives you an intuitive look at how the kernel &quot;locks down&quot; the very first page at the bottom of a process&#39;s address space.</p><p>Beyond that, to ensure the following experiments run smoothly, please double-check that you have the standard compilation toolchain installed on your system. Although we&#39;ll be doing most of our work on x86_64, if you want to try a cross-platform experience—like reproducing a crash on an ARM board—make sure your cross-compilation toolchain is also ready.</p><p>Tools in place, code in place. Next up, we&#39;re going to do something mischievous—deliberately dig a pit for the kernel and watch it fall in.</p>',21)])])}const g=t(s,[["render",i]]);export{u as __pageData,g as default};
