import{_ as e}from"./chunks/ch05_fig058.BsyWoTmX.js";import{_ as i,o as a,c as t,a as n}from"./chunks/framework.8tiMeCK-.js";const l="/PenguinLab/assets/ch05_fig049.BwUfRVw0.webp",r="/PenguinLab/assets/ch05_fig050.B6RljTaB.webp",h="/PenguinLab/assets/ch05_fig051.CgIKy_qz.webp",p="/PenguinLab/assets/ch05_fig052.DorrJ7L0.webp",o="/PenguinLab/assets/ch05_fig053.BrCyg7xu.webp",d="/PenguinLab/assets/ch05_fig054.Bp_8Uknd.webp",c="/PenguinLab/assets/ch05_fig055.Bb5WNwb6.webp",k="/PenguinLab/assets/ch05_fig056.C-YX7QEA.webp",u="/PenguinLab/assets/ch05_fig057.DhcWEoHQ.webp",v=JSON.parse('{"title":"5.5  Catching Bugs with KASAN","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch05_5.md","filePath":"en/notes/linux_kernel_debugging/ch05_5.md","lastUpdated":null}'),g={name:"en/notes/linux_kernel_debugging/ch05_5.md"};function m(b,s,y,E,f,F){return a(),t("div",null,[...s[0]||(s[0]=[n(`<h1 id="_5-5-catching-bugs-with-kasan" tabindex="-1">5.5 Catching Bugs with KASAN <a class="header-anchor" href="#_5-5-catching-bugs-with-kasan" aria-label="Permalink to &quot;5.5  Catching Bugs with KASAN&quot;">​</a></h1><p>Assume you have already followed the detailed steps from the previous section to configure, compile, and successfully boot a debug kernel with KASAN enabled. In my environment—an x86_64 Ubuntu 20.04 LTS virtual machine—everything is ready to go.</p><p>Now that we have our eyes installed, we need to find something for them to look at.</p><p>To verify that KASAN is actually working, we need to execute some code that contains memory bugs (I can almost hear some veterans in the audience smirking: &quot;Ha, how hard can that be? Just grab any piece of code&quot;). True, we could easily hand-write a few test cases, but since the wheel has already been invented, why reinvent it? This is a perfect opportunity to get familiar with the kernel&#39;s testing infrastructure. In the following section, we will look at how to leverage the kernel&#39;s KUnit unit testing framework to run KASAN test cases.</p><hr><h3 id="running-kasan-tests-using-the-kernel-s-kunit-infrastructure" tabindex="-1">Running KASAN Tests Using the Kernel&#39;s KUnit Infrastructure <a class="header-anchor" href="#running-kasan-tests-using-the-kernel-s-kunit-infrastructure" aria-label="Permalink to &quot;Running KASAN Tests Using the Kernel&#39;s KUnit Infrastructure&quot;">​</a></h3><p>Since the community has already written the test cases for us, why bother writing our own? This is the beauty of open source.</p><p>The Linux kernel has evolved to become quite comprehensive, incorporating various testing infrastructures, including mature test suites. To test a certain aspect of the kernel today, you usually just need to configure the right kernel options and run the tests.</p><p>Among the kernel&#39;s many built-in testing frameworks, there are two main ones: the KUnit framework and the kselftest framework. By the way, the official kernel documentation of course has all the details. As a starting point, you can check out the &quot;Kernel Testing Guide&quot;:</p><p><code>https://www.kernel.org/doc/html/latest/dev-tools/testing-overview.html</code></p><p>It provides a rough overview of the testing frameworks and tools available in the kernel (including dynamic analysis tools).</p><p>Additionally, there are several related and useful frameworks: kernel fault injection, notifier error injection, Linux Kernel Dump Test Module (LKDTM), and so on. You can find them under the <code>Kernel hacking | Kernel Testing and Coverage</code> menu in the kernel configuration.</p><p>We don&#39;t plan to dive deep into how KUnit works here; our goal right now is simply to use it as a practical example to test KASAN. For details on how to use these testing frameworks (which prove to be very useful!), you can refer to the links in the &quot;Further reading&quot; section at the end of the chapter.</p><p>Getting back on track, to get our hands dirty and get familiar with it, let&#39;s use the kernel&#39;s KUnit (Kernel Unit Testing) framework to execute KASAN test cases.</p><p>This is actually quite simple. First, ensure your debug kernel has KUnit enabled:</p><p><code>CONFIG_KUNIT=y</code> (or <code>CONFIG_KUNIT=m</code>).</p><p>Since we want to run the KASAN test cases, we must also select the KASAN test module:</p><p><code>CONFIG_KASAN_KUNIT_TEST=m</code></p><p>The kernel code for the KASAN test module we are about to run is located at <code>lib/test_kasan.c</code>. Taking a quick glance at the code, you will see various test cases defined inside (quite a few—38 at the time of writing this):</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// lib/test_kasan.c</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kunit_suite kasan_kunit_test_suite </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .name </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;kasan&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .init </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kasan_test_init,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .test_cases </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kasan_kunit_test_cases,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .exit </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kasan_test_exit,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kunit_test_suite</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(kasan_kunit_test_suite);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>This code sets up a group of test suites to be executed. The actual test cases reside in the <code>test_cases</code> member of the <code>kunit_suite</code> structure. It is a pointer to an array of <code>kunit_case</code> structures:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kunit_case kasan_kunit_test_cases</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">[]</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    KUNIT_CASE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(kmalloc_oob_right),</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    KUNIT_CASE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(kmalloc_oob_left),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    [...]</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    KUNIT_CASE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(kmalloc_double_kzfree),</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    KUNIT_CASE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(vmalloc_oob),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>The <code>KUNIT_CASE()</code> macro is responsible for setting up an individual test case. To help you understand how it works, let&#39;s look at the code for the first test case:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// lib/test_kasan.c</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> kmalloc_oob_right</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kunit </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">test</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ptr;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    size_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> size </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 123</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ptr </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> kmalloc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(size, GFP_KERNEL);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    KUNIT_ASSERT_NOT_ERR_OR_NULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(test, ptr);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    KUNIT_EXPECT_KASAN_FAIL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(test, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ptr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[size </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> OOB_TAG_OFF] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;x&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    kfree</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ptr);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><p>Very intuitive, the actual checks happen in the <code>KUNIT_ASSERT|EXPECT_*()</code> macros you see here. The first macro asserts that the return value of the <code>kmalloc()</code> API is neither an error nor NULL. The second macro, <code>KUNIT_EXPECT_KASAN_FAIL()</code>, tells the KUnit code to expect a failure—this is a negative test case. This is exactly what we want: we expect that writing out-of-bounds to the right side of a buffer (a write overflow defect) will trigger KASAN and report a failure! If you are interested, you can dig into the implementation details of these macros.</p><p>Additionally, a very interesting point: the <code>name</code> and <code>exit</code> members in the <code>kunit_suite</code> structure specify the functions to be executed before and after each test case runs. This module leverages this mechanism to ensure that the kernel sysctl <code>kasan_multi_shot</code> is temporarily enabled, and <code>panic_on_warn</code> is set to 0 (otherwise, only the first illegal memory access would trigger a report and potentially crash the kernel!).</p><p>Finally, let&#39;s give it a try:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> uname</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -r</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">5.10.60-dbg01</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> modprobe</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test_kasan</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>This causes all test cases in the KASAN test module to be executed! Checking the kernel logs (via <code>journalctl -k</code> or <code>dmesg</code>), you will see detailed KASAN reports for each test case. Since the output is quite massive, I will only capture a portion. The first test case—<code>KUNIT_CASE(kmalloc_oob_right)</code>—caused KASAN to generate the following report (output is truncated—there is more to follow):</p><p><img src="`+l+`" alt="Figure 5.2"></p><p><strong>Figure 5.2 – Part 1 of the KUnit KASAN bug-catching example</strong></p><p>Note a few key points in the preceding figure:</p><ul><li><p>In the first two lines, KUnit displays the test title (i.e., <code># Subtest: kasan</code>) and indicates that it is about to run test cases 1..38.</p></li><li><p>As expected, KASAN successfully detected the memory defect (write overflow) and generated a report. The report starts with <code>BUG: KASAN: [...]</code>, followed by the details.</p></li><li><p>The next few lines reveal the root cause. KASAN displays the offending function in the format <code>func()+0xoff_from_func/0xsize_of_func</code>. This indicates that in a function named <code>func()</code>, the error occurred at an offset of <code>0xoff_from_func</code> bytes from the start of the function, and the kernel estimates the function length to be <code>0xsize_of_func</code> bytes. So here, the code in the <code>kmalloc_oob_right()</code> function within kernel module <code>test_kasan</code> (shown in the rightmost square brackets), at an offset of <code>0x159</code> bytes from its start (followed by an educated guess that the function is <code>0x260</code> bytes long), attempted to illegally write to a specified address. This defect, the bug, is an OOB write to a slab memory buffer, as indicated by the <code>slab-out-of-bounds</code> marker we see:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BUG: KASAN: slab-out-of-bounds in kmalloc_oob_right+0x159/0x260 [test_kasan]</span></span>
<span class="line"><span>Write of size 1 at addr ffff8880316a45fb by task kunit_try_catch/1206</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div></li><li><p>The line below shows the process context in which this happened (we will cover the meaning of the tainted flag in the next chapter):</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CPU: 2 PID: 1206  Comm: kunit_try_catch Tainted: G    O      5.10.60-dbg01 #6</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div></li><li><p>The next line shows the hardware details (you can tell this is a VM, VirtualBox).</p></li><li><p>The bulk of the output is the call stack (marked as <code>Call Trace:</code>). If you read from bottom to top (and ignore lines prefixed with <code>?</code>), you can clearly see how the control flow reached the buggy code!</p></li><li><p>The <code>Allocated by task 1206:</code> line and subsequent output show the call trace of the memory allocation code path. This is extremely helpful because it shows who originally allocated this memory buffer and where.</p></li></ul><p>The remainder of the output can be seen in the following figure:</p><p><img src="`+r+'" alt="Figure 5.3"></p><p><strong>Figure 5.3 – Part 2 of the KUnit KASAN bug-catching example</strong></p><p>Since we previously recommended enabling <code>CONFIG_PAGE_OWNER=y</code> when configuring Generic KASAN mode (see the <em>Configuring the kernel for Generic KASAN mode</em> section), the following output will also appear. It gives you insight into where the page that suffered the illegal access resides and its ownership information:</p><p><img src="'+h+`" alt="Figure 5.4"></p><p><strong>Figure 5.4 – Part 3 (and final) of the KUnit KASAN bug-catching example</strong></p><p>In the preceding figure, you can see KASAN proving its case. It displays the actual memory region where the defect occurred, and even precisely points out the offending byte (via the <code>&gt;</code> at the beginning of the line and the <code>^</code> symbol on the column!). As a side effect of this bug, the kernel has now disabled all lock debugging. Furthermore, KUnit indicates that running the first test case went smoothly: <code>ok 1 - kmalloc_oob_right</code>.</p><p>Understanding this information is crucial. It helps you peel back the layers and find the true culprit that triggered the bug. We will decode this line of output in detail in the next section!</p><hr><h3 id="decoding-the-kasan-shadow-memory-output" tabindex="-1">Decoding the KASAN Shadow Memory Output <a class="header-anchor" href="#decoding-the-kasan-shadow-memory-output" aria-label="Permalink to &quot;Decoding the KASAN Shadow Memory Output&quot;">​</a></h3><p>In Figure 5.4, you can see the KASAN shadow memory revealing the cause of the defect. Let&#39;s print out the key line—the one with the right arrow <code>&gt;</code> prefix:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&gt;ffff8880318ad980: 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 03</span></span>
<span class="line"><span>                  ^</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>These are the KASAN shadow memory bytes, where each byte represents 8 bytes of actual memory. The byte being pointed to (the <code>^</code> symbol) <code>03</code> tells us where the problem lies. So what do bytes like <code>00</code> and <code>03</code> actually mean? Here are the details:</p><ul><li><p><strong>Generic KASAN</strong> allocates one shadow byte to track 8 bytes of kernel memory (you can think of this 8-byte region as a <strong>memory granule</strong>).</p></li><li><p>A granule (8-byte region) is encoded as either: accessible, partially accessible, part of a redzone, or freed.</p></li><li><p>The rules for encoding memory granules (8-byte regions) via shadow byte tracking are as follows:</p><ul><li><strong>Shadow memory = 00</strong>: All 8 bytes are accessible (no problem).</li><li><strong>Shadow memory = N</strong> (where N can be a value between 1 and 7): The <strong>first N bytes</strong> are accessible (legal); the remaining <code>8 - N</code> bytes are legally inaccessible.</li><li><strong>Shadow memory &lt; 0</strong>: A negative value means the entire granule (8 bytes) is inaccessible. The specific (negative) values and their meanings (freed memory, redzones, etc.) are encoded in the header file <code>mm/kasan/kasan.h</code>.</li></ul></li></ul><p>Now you should understand that a shadow byte of <code>03</code> means this memory is <strong>partially accessible</strong>. The first 3 bytes (where N = 3) are legally accessible; the remaining 5 bytes (8 - 3 = 5) are not. Let&#39;s take the trouble to verify this in detail. Of course, the code line that triggered the bug is right here (located in the kernel codebase):</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// lib/test_kasan.c</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> kmalloc_oob_right</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kunit </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">test</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    [...]</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    size_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> size </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 123</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ptr </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> kmalloc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(size, GFP_KERNEL);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    [...]</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    KUNIT_EXPECT_KASAN_FAIL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(test, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ptr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[size </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> OOB_TAG_OFF] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;x&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>Now, the variable <code>size</code> is set to the value <code>123</code>, and <code>OOB_TAG_OFF</code> is 0 when <code>CONFIG_KASAN_GENERIC</code> is enabled. So, effectively, this (buggy) code is equivalent to:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ptr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">123</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;x&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The Generic KASAN memory granule size is 8 bytes. So, out of the 123 allocated bytes, the 15th memory granule (<code>8 * 15 = 120</code>) is exactly what we are targeting for the write. The following diagram clearly illustrates the memory buffer and how it gets overflowed:</p><p><img src="`+p+`" alt="Figure 5.5"></p><p><strong>Figure 5.5 – The kmalloc-allocated memory (slab) buffer overflows</strong></p><p>Look closely: on the right end, byte positions 120, 121, and 122 are legal and can be read/written, but our KUnit KASAN test case deliberately writes to byte position 123—which is 1 byte past the end of the slab buffer. This is a clear OOB write overflow violation, and KASAN caught it!</p><p>Not only that, as Figure 5.4 and Figure 5.5 clearly show, the kernel is smart enough to display the shadow value here as <code>03</code>, meaning the first 3 bytes are valid, but the remaining 5 are not—which is exactly the case!</p><p>Furthermore, the surrounding bytes are set to the value <code>0xfc</code> (see Figure 5.4). What does this mean? A quick look at the header file reveals it—it&#39;s a <strong>redzone</strong> inside the kernel SLUB object:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// mm/kasan/kasan.h</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#ifdef</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CONFIG_KASAN_GENERIC</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> KASAN_FREE_PAGE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">         0x</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">FF</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /* page was freed */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> KASAN_PAGE_REDZONE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      0x</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">FE</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /* redzone for kmalloc_large allocations */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> KASAN_KMALLOC_REDZONE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">   0x</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">FC</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /* redzone inside slub object */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> KASAN_KMALLOC_FREE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      0x</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">FB</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /* object was freed (kmem_cache_free/kfree) */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">#define</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> KASAN_KMALLOC_FREETRACK</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 0x</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">FA</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /* object was freed and has free track set */</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>Returning to our interpretation of Figure 5.4: the next line (<code>BUG: KASAN: [...]</code>) just tells you it&#39;s moving on to run the next test case... KASAN now catches the bug in the second test case (<code>KUNIT_CASE(kmalloc_oob_left)</code>). The kernel log contains the same information as the first defect: the KASAN bug summary, the <code>dump_stack()</code> output (call stack frames), who performed the allocation, page ownership information, and the memory state around the bug&#39;s access address. This continues all the way to the thirty-eighth test case—perfectly!</p><p>A quick check of the kernel log reveals exactly what we expected—the kernel&#39;s KUnit KASAN test case module caught all 38 memory-defective test cases:</p><p><img src="`+o+`" alt="Figure 5.6"></p><p><strong>Figure 5.6 – Screenshot showing the kernel&#39;s KUnit KASAN test module catching all 38 memory-defective test cases</strong></p><p>As you can clearly see from the preceding screenshot, all 38 test cases are reported as <code>ok</code> (passed).</p><p><strong>Exercise</strong> Be sure to perform the steps we just did on your machine—run the kernel&#39;s KUnit KASAN test cases. Observe the various KASAN test cases from the kernel logs and verify that they all ran correctly.</p><p>By the way, notice this:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lsmod</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">egrep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;kunit|kasan&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">test_kasan</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">             81920</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  0</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kunit</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">                  49152</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test_kasan</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>In my specific environment, you can see from the <code>lsmod</code> output that KUnit is configured as a kernel module.</p><p>You can learn how to write your own KUnit test suites. For more on using KUnit, be sure to check out the &quot;Further reading&quot; section!</p><hr><h3 id="remaining-tests-using-our-custom-buggy-kernel-module" tabindex="-1">Remaining Tests Using Our Custom Buggy Kernel Module <a class="header-anchor" href="#remaining-tests-using-our-custom-buggy-kernel-module" aria-label="Permalink to &quot;Remaining Tests Using Our Custom Buggy Kernel Module&quot;">​</a></h3><p>Did you notice that despite running all the KASAN KUnit test cases, there seem to be a few common memory defects (which we mentioned in Chapter 4 and in the <em>What&#39;s the problem with memory anyway?</em> section of this chapter) that are not covered by the KUnit test cases?</p><ul><li>Uninitialized Memory Read (UMR) bugs</li><li>Use-After-Return (UAR) bugs</li><li>Simple memory leak bugs (we will discuss memory leaks in more detail later in this chapter)</li></ul><p>So, I wrote a kernel module to execute these test cases (of course, running on a debug kernel with Generic KASAN enabled), along with a few more interesting ones. To test against KASAN, remember to boot via your custom debug kernel, which obviously needs to include <code>CONFIG_KASAN=y</code>.</p><p>Due to space constraints, I won&#39;t show the entire test module code here (please refer to this book&#39;s GitHub repository and read the comments within it—you can find it under the <code>ch5/kmembugs_test</code> folder). To give you a taste, let&#39;s look at one of the test cases and how it is invoked. Here is the code for the UAR test case:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ch5/kmembugs_test/kmembugs_test.c</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* The UAR - Use After Return - testcase */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">uar</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    volatile</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[NUM_ALLOC];</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    volatile</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    pr_info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;testcase 2: UAR:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (i</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; i</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">NUM_ALLOC</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; i</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">++</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">        name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[i] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;x&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[i] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> name;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><p>This module is designed to be loaded via a bash script named <code>load_testmod</code>, and the test cases are run interactively (via a bash wrapper script named <code>run_tests</code>). The <code>run_tests</code> script (which you must run as root) displays a menu of available tests and prompts you to enter the corresponding number to select one. You can see a screenshot of the menu in Figure 5.8 (in the next section)—so you can also see all the test cases you can try.</p><p>The script then writes this number to our debugfs pseudo-file: <code>/sys/kernel/debug/test_kmembugs/lkd_dbgfs_run_testcase</code>. The debugfs write hook function then receives this data from user space, validates it, and invokes the appropriate test case routine (via a long if-else ladder). This design allows you to test interactively and execute any test case as many times as you wish.</p><p>The following code snippet shows how our debugfs module code invokes the preceding <code>uar()</code> test case:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ch5/kmembugs_test/debugfs_kmembugs.c</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ssize_t</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> dbgfs_run_testcase</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> file </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">filp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> __user </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ubuf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                                  size_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> count</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">loff_t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">fpos</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    char</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> udata</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[MAXUPASS];</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    volatile</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">res1 </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> NULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">res2 </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> NULL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    [...]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">copy_from_user</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(udata, ubuf, count))</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">EIO;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    udata</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[count</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    pr_debug</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;testcase to run: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, udata);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* Now udata contains the data passed from userspace - the testcase # to run (as a string) */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">strncmp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(udata, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">       umr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    else</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">strncmp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(udata, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">       res1 </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> uar</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">       pr_info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;testcase 2: UAR: res1 = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\&quot;%s\\&quot;\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">res1 </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> NULL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ?</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;&lt;whoops, it&#39;s NULL; UAR!&gt;&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> :</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)res1);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">strncmp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(udata, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;3.1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">...</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br></div></div><p>Obviously, this—test case #2—is a defect, a bug. You know that local variables are only valid during their lifetime—that is, during the function&#39;s execution. This is because local (or automatic) variables are allocated on the executing process context&#39;s (kernel-mode) stack frame. Therefore, once you go out of the scope of the containing function, you must stop referencing that local variable. We (deliberately) didn&#39;t do that! We tried to retrieve it as a return value. The problem is, by that time, it&#39;s already gone...</p><p>However, before diving into running these test cases (though there&#39;s no reason not to run them right now), let&#39;s take a detour and talk about an interesting dilemma: how a known bug (like our UAR) can sometimes appear to run perfectly fine.</p><hr><h3 id="stale-stack-frames-–-trouble-in-paradise" tabindex="-1">Stale Stack Frames – Trouble in Paradise <a class="header-anchor" href="#stale-stack-frames-–-trouble-in-paradise" aria-label="Permalink to &quot;Stale Stack Frames – Trouble in Paradise&quot;">​</a></h3><p>The most magical (or rather, craziest) thing about a bug like UAR is that the code can sometimes appear to run perfectly fine! How is this possible?</p><p>Here is how it works: the memory that holds the contents of local (automatic) variables resides on the stack. While we colloquially say that stack frames are allocated on function entry and destroyed on function return (the so-called function prologue and epilogue), reality is not quite that dramatic.</p><p>The reality is that memory is typically allocated at page-level granularity. This includes memory for stack pages. Therefore, once a page of memory is allocated for the stack, it is usually enough to hold several frames (of course, this depends on the specifics). Then, when more stack memory is needed, the stack grows (by allocating more pages, growing downward because the stack grows downward). The system keeps track of where the top of the stack is by having the Stack Pointer (SP) register track this memory location. Additionally, you need to realize that the so-called &quot;top of the stack&quot; is usually the lowest valid address. Thus, when frames are allocated and/or functions are called, the value of the SP register decreases. When a function returns, the stack shrinks by adding a value to the SP register (remember, this is a downward-growing stack!). The following diagram shows what a typical kernel-mode stack looks like on a Linux system (32-bit):</p><p><img src="`+d+`" alt="Figure 5.7"></p><p><strong>Figure 5.7 – Typical kernel-mode stack diagram on 32-bit Linux; function call chain: foo1() -&gt; bar1() -&gt; foo2() -&gt; bar2()</strong></p><p>So, it is entirely possible for this to happen: a <strong>stale</strong> stack frame (and the corresponding data within it) exists below valid frames and might be successfully referenced—even later on—without the system throwing a memory error!</p><p>Study Figure 5.7 carefully. For example, we show the kernel-mode stack on a 32-bit Linux system, so the kernel stack size will be 2 pages, typically 8 KB. Now, suppose the executing process context (within the kernel) called these functions in the following order (this is the call chain, circle steps 1 through 4 in the figure):</p><p><code>foo1() --&gt; bar1() --&gt; foo2() --&gt; bar2()</code></p><p>Imagine we are now at the leaf node, which in this example is the function <code>bar2()</code>. It returns (circle step 5 in the preceding figure). This causes the SP register to increase back to the address representing the call frame of function <code>foo2()</code>. So, while the call stack memory for function <code>bar2()</code> still remains intact on the stack, it is effectively <strong>invalid</strong>. However, an erroneous (that is, buggy) access to it might still succeed.</p><p>This shouldn&#39;t happen in an ideal world, but hey, it&#39;s not a perfect world, right?! The lesson here is that we need tools—and clear thinking, which is the best tool of all—to catch tricky bugs like UAR!</p><p>Alright, back to our test cases! To run the tests, follow these steps:</p><ol><li><p>Run the following command:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">book_sr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">c</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/ch5/kmembugs_test</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div></li><li><p>Load the module:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./load_testmod</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[...]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>This should build the kernel module and load it into memory, with <code>dmesg</code> showing that the debugfs pseudo-file—<code>&lt;debugfs_mountpt&gt;/test_kmembugs/lkd_dbgfs_run_testcase</code>—has been created.</p></li><li><p>Run our bash script to perform the tests:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./run_tests</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div></li></ol><p>The following screenshot shows that our <code>test_kmembugs</code> module was indeed loaded (done via our <code>load_testmod</code> script), the menu is displayed via the <code>run_tests</code> script, and we are running test case #2—the UAR bug:</p><p><img src="`+c+'" alt="Figure 5.8"></p><p><strong>Figure 5.8 – Partial screenshot showing the build and output of our kmembugs_test LKM</strong></p><p>Here is an example screenshot of our test case framework catching a left OOB write bug access via KASAN:</p><p><img src="'+k+`" alt="Figure 5.9"></p><p><strong>Figure 5.9 – Partial screenshot showing KASAN catching a left OOB bug when writing to global memory</strong></p><p>A few things to realize:</p><ul><li><p>First, the compiler, whether GCC or Clang, is smart enough to emit warnings for these (rather obvious) bugs. Both the UAR and UMR defects are indeed caught by them (at the exact location in the code where they occur), albeit as warnings! Here is one of the warnings GCC emitted for our UAR bug:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;...&gt;/ch5/kmembugs_test/kmembugs_test.c:115:9: warning: function returns address of local variable [-Wreturn-local-addr]</span></span>
<span class="line"><span>  115 |  return (void *)name;</span></span>
<span class="line"><span>      |         ^~~~~~~~~~~~</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><strong>⚠️ This is important</strong> As a programmer, your job is to carefully heed all compiler warnings and—within human capability—fix them.</p></li><li><p>The script queries the kernel configuration file to see if your current kernel is configured with KASAN, UBSAN, and KMEMLEAK, and displays what it finds. It also shows the path to the debugfs pseudo-file where the test case number will be written (to invoke the test).</p></li></ul><p>Here is a sample run of the UAR test case:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./run_tests</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[...]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Type</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> in</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> the</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> testcase</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> number</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> to</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">2</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Running</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> testcase</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;2&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> via</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> module</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> now...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[  144.313592] testcase to run: 2</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[  144.313597] </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">test_kmembugs:uar</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(): testcase 2: UAR:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[  144.313600] testcase 2: UAR: res1 = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;whoops, it&#39;s NULL; UAR!&gt;&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><ul><li>The output in the kernel log (seen via the preceding <code>dmesg</code>) clearly states the situation: we executed the UAR test case, and neither the kernel nor KASAN caught it (if they had, the log would be full of complaints!). Our own code checks whether the variable <code>res1</code> is NULL and concludes that a UAR bug occurred. We can do this because we specifically initialized it to NULL and checked it after it was set to the string returned by the function <code>uar()</code>; otherwise, we might not have caught it.</li></ul><p>Alright, we have now done a few tests with KASAN. How is KASAN&#39;s report card looking? The next section will tell you.</p><hr><h3 id="kasan-–-summary-results-table" tabindex="-1">KASAN – Summary Results Table <a class="header-anchor" href="#kasan-–-summary-results-table" aria-label="Permalink to &quot;KASAN – Summary Results Table&quot;">​</a></h3><p>Exactly which memory corruption bugs (defects) can KASAN catch, and which can it not catch? Based on our test runs, we have summarized the results in the following table. Please study it carefully, along with the notes that follow:</p><p><img src="`+u+'" alt="Figure 5.7"><img src="'+e+`" alt="Figure 5.8"></p><p><strong>Table 5.3 – Summary of memory defects and arithmetic UB test cases caught (or not caught) by KASAN</strong></p><p>You will find explanations for the footnote symbols seen in the table (such as <code>[C1]</code>, <code>[U1]</code>, etc.) below.</p><p><strong>Test Environment</strong></p><ul><li><strong>[1] Test case numbers</strong>: Please refer to the test kernel module&#39;s source code—<code>ch5/kmembugs_test/kmembugs_test.c</code>, the debugfs entry creation and usage in <code>debugfs_kmembugs.c</code>, and the bash scripts <code>load_testmod</code> and <code>run_tests</code>, all in the same folder.</li><li><strong>[2] The compiler used here</strong> is GCC version 9.3.0 on x86_64 Ubuntu Linux. A later section - <em>Using Clang 13 on Ubuntu 21.10</em> - will cover using the Clang 13 compiler.</li><li><strong>[3]</strong> To test KASAN, I had to boot via our custom debug kernel (5.10.60-dbg01), which includes <code>CONFIG_KASAN=y</code> and <code>CONFIG_KASAN_GENERIC=y</code>. We assume the use of the Generic KASAN variant.</li><li><strong>Test cases 4.1 to 4.4</strong> operate on static (compile-time allocated global) memory as well as stack-local memory. This is why the test case numbers are 4.x in both.</li></ul><p><strong>Compiler Warnings</strong></p><ul><li><p><strong>Version</strong>: This is for GCC version 9.3.0 on x86_64 Ubuntu:</p><ul><li><p><strong>[C1]</strong> The GCC compiler reports the UMR as a warning:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>warning: &#39;&lt;var&gt;&#39; is used uninitialized in this function [-Wuninitialized]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div></li><li><p><strong>[C2]</strong> GCC reports the potential UAF defect as a warning:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>warning: function returns address of local variable [-Wreturn-local-addr]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div></li><li><p><strong>[C3]</strong> GCC (quite smartly!) catches the illegal <code>copy_[to|from]_user()</code> here. It figures out the destination size is too small:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>* In function &#39;check_copy_size&#39;,</span></span>
<span class="line"><span>    inlined from &#39;copy_from_user&#39; at ./include/linux/uaccess.h:191:6,</span></span>
<span class="line"><span>    inlined from &#39;copy_user_test&#39; at &lt;...&gt;/ch5/kmembugs_test/kmembugs_test.c:482:14:</span></span>
<span class="line"><span>./include/linux/thread_info.h:160:4: error: call to &#39;__bad_copy_to&#39; declared with attribute error: copy destination size is too small</span></span>
<span class="line"><span>  160 |    __bad_copy_to();</span></span>
<span class="line"><span>      |    ^~~~~~~~~~~~~~~</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div></li></ul></li><li><p>When <strong>using the Clang 13 compiler</strong> (which we will cover in the <em>Building the kernel and modules with Clang</em> section), the warnings are almost exactly identical to GCC. Additionally, it emits <code>variable &#39;xxx&#39; set but not used [-Wunused-but-set-variable]</code>.</p></li></ul><p>The following section dives into the details—don&#39;t miss it!</p><hr><h3 id="kasan-–-detailed-notes-on-the-table-results" tabindex="-1">KASAN – Detailed Notes on the Table Results <a class="header-anchor" href="#kasan-–-detailed-notes-on-the-table-results" aria-label="Permalink to &quot;KASAN – Detailed Notes on the Table Results&quot;">​</a></h3><p>Here is a detailed explanation of the KASAN footnote symbols (<code>[K1]</code>, <code>[K2]</code>, etc.). It is important to read through all the notes, as we mention several caveats and edge cases:</p><ul><li><p><strong>[K1]</strong> KASAN catches and reports OOB accesses on <strong>global static memory</strong> in the following format:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>global-out-of-bounds in &lt;func&gt;+0xstart/0xlen [modname]</span></span>
<span class="line"><span>Read/Write of size &lt;n&gt; at addr &lt;addr&gt; by task &lt;taskname/PID&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>The report will include either <code>Read</code> or <code>Write</code>, depending on whether a read or write bug access occurred.</p></li><li><p><strong>[K2]</strong> There are quite a few caveats to mention here:</p><ul><li><p>Out-Of-Bounds (OOB) read/write <strong>left underflow</strong> on <strong>global memory</strong> test cases are only caught when compiled with Clang version 11 or higher. GCC 10 or 11 cannot catch it either, due to how their redzones work.</p></li><li><p>KASAN only catches OOB accesses on global memory when compiled with <strong>Clang 11 and later</strong>! Therefore, in my test runs using GCC 9.3 and Clang 10, I saw it fail to catch read/write underflow (left OOB) accesses on global buffers (test cases 4.3 and 4.4)! However, for overflow defects on global memory, it does seem to catch them, though you shouldn&#39;t take this for granted... (by the way, Clang is pronounced &quot;clang&quot; and not &quot;see-lang&quot;). Furthermore, although the documentation claims support for GCC starting from version 8.3.0, in my testing, it failed to catch the read/write underflow bug test cases on (only) global memory. Be sure to read the upcoming <em>Building the kernel and modules with Clang</em> section!</p></li><li><p>However, even with GCC 9.3, depending on how its internal redzones and padding work, the <strong>first declared global variable</strong> (exactly which variable depends on how the linker lays things out) might not have a left redzone, causing left OOB bug accesses to be missed... This is why—as a silly workaround until GCC fixes this issue—we used three global arrays. In the test cases, we use the middle one as the test buffer (any one except the first will do). Hopefully, GCC will be fixed—to have proper redzones—so that all OOB accesses can be caught. In our specific test run, the left OOB bug access on global memory was indeed caught, even when compiled with GCC 9.3!</p></li><li><p>These observations, caveats, and so on, are inherently somewhat mystical at times. They might work one way on one system and quite differently on another system with a different configuration or architecture. Therefore, we strongly recommend that you test your workloads using a properly configured debug kernel combined with all the tools at your disposal (including using newer compiler technologies like Clang) and the various tools and techniques covered in this book. Yes, it&#39;s a lot of work, and yes, it&#39;s worth it!</p></li></ul></li><li><p><strong>[K3]</strong> KASAN catches and reports OOB accesses on <strong>stack-local memory</strong> in the following format:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>stack-out-of-bounds in &lt;func&gt;+0xstart/0xlen [modname]</span></span>
<span class="line"><span>Read/Write of size &lt;n&gt; at addr &lt;addr&gt; by task &lt;taskname/PID&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div></li><li><p><strong>[K4]</strong> KASAN catches and reports OOB accesses on <strong>dynamic slab memory</strong> in the following format:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BUG: KASAN: slab-out-of-bounds in &lt;func&gt;+0xstart/0xlen [modname]</span></span>
<span class="line"><span>Read/Write of size &lt;n&gt; at addr &lt;addr&gt; by task &lt;taskname/PID&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div></li><li><p><strong>[K5]</strong> KASAN catches and reports <strong>UAF</strong> defects in the following format:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BUG: KASAN: use-after-free in &lt;func&gt;+0xstart/0xlen [modname]</span></span>
<span class="line"><span>Read/Write of size &lt;n&gt; at addr &lt;addr&gt; by task &lt;taskname/PID&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div></li><li><p><strong>[K6]</strong> KASAN catches and reports <strong>double frees</strong> in the following format:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BUG: KASAN: double-free or invalid-free in &lt;func&gt;+0xstart/0xlen [modname]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div></li><li><p>In all the preceding cases, the KASAN report also details the actual violation, including the process context, the (kernel-mode stack) call trace, and the shadow memory mapping, showing which variable the OOB memory access belongs to (if applicable) and the memory state around the bug&#39;s address.</p></li></ul><p><strong>Tip – Consolidating all results into one table</strong> For your convenience, in Part 2 of this key topic (the next chapter), in the <em>Catching memory defects in the kernel – comparisons and notes (Part 2)</em> section, Table 6.4 summarizes the test case results for all the tool techniques we used in this chapter—vanilla/distro kernels, compiler warnings, KASAN, UBSAN, and SLUB debug. Essentially, it is a compilation of all findings, allowing you to quickly (and hopefully helpfully) compare them.</p><p>Did you notice that, regarding the kernel&#39;s built-in KUnit-based KASAN test cases, the <code>test_kasan</code> kernel module does <strong>not</strong> have test cases for these three memory defects—UMR, UAR, and memory leaks. Why? Simple: <strong>KASAN cannot catch these bugs</strong>. So, what can we conclude? KUnit (and other) test suites are typically run in an automated fashion, and the expected end result is that all viable test cases pass; in fact, they must pass. If the test suite included these three defects, that would be impossible to achieve, so they are not included. Now, don&#39;t get me wrong—this is just how the test suite is designed. Of course, there are definitely other means besides KASAN to catch these defects. Don&#39;t worry—we will get there, and we will catch them too.</p><p>Here, we are simply showing that KASAN by itself does not catch these specific tricky bugs. Later in the book, we will see which tools can.</p><p>By the way, KASAN is a key component in catching hard-to-find bugs through fuzzing methodologies. Syzkaller (aka syzbot)—the de facto powerful Linux kernel fuzzer—requires the kernel to be configured with KASAN! We will briefly introduce fuzzing in the <em>What is fuzzing?</em> section of Chapter 12, <em>A few More Kernel Debug Approaches</em>. Be sure to check it out.</p><p>Nice job—you now know how to leverage the power of KASAN to help catch those tricky memory bugs! Next, let&#39;s move on to using UBSAN.</p>`,127)])])}const C=i(g,[["render",m]]);export{v as __pageData,C as default};
