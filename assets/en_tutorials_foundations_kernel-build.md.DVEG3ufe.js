import{_ as t,o as a,c as i,a as o}from"./chunks/framework.BHSVNLI7.js";const p=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"en/tutorials/foundations/kernel-build.md","filePath":"en/tutorials/foundations/kernel-build.md","lastUpdated":null}'),s={name:"en/tutorials/foundations/kernel-build.md"};function n(r,e,l,d,c,h){return a(),i("div",null,[...e[0]||(e[0]=[o(`<h2 id="what-we-ll-do" tabindex="-1">What We&#39;ll Do <a class="header-anchor" href="#what-we-ll-do" aria-label="Permalink to &quot;What We&#39;ll Do&quot;">​</a></h2><p>In this section, we do exactly one thing—compile our carefully crafted mini config into a bootable ARM64 kernel. The compilation itself is just a single <code>make</code> command, but the kernel build system produces very rich output that is worth taking the time to understand. When we finish, we&#39;ll get a <code>Image</code> file—this is the ARM64 kernel boot image that QEMU will load later to boot the system.</p><h2 id="what-to-know" tabindex="-1">What to Know <a class="header-anchor" href="#what-to-know" aria-label="Permalink to &quot;What to Know&quot;">​</a></h2><h3 id="kicking-off-the-build" tabindex="-1">Kicking Off the Build <a class="header-anchor" href="#kicking-off-the-build" aria-label="Permalink to &quot;Kicking Off the Build&quot;">​</a></h3><p>Make sure you&#39;ve completed the three-step configuration process from the previous section and that <code>out/build_latest_arm64/.config</code> exists with the correct contents. Then, run the following in the <code>third_party/linux/</code> directory:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ARCH=arm64</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> CROSS_COMPILE=aarch64-linux-gnu-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">     O=../../out/build_latest_arm64</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -j$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nproc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p><code>-j$(nproc)</code> automatically uses all available CPU cores for parallel compilation. On our 14-thread machine, a mini config build takes about 3–5 minutes; if we use the 952 options in <code>defconfig</code>, that time doubles or more.</p><p>During the build, you&#39;ll see a massive amount of output scrolling rapidly across the terminal, with a short tag at the beginning of each line. If these tags look like gibberish, don&#39;t worry—that&#39;s exactly what we&#39;re about to break down.</p><h3 id="build-output-tags-cheat-sheet" tabindex="-1">Build Output Tags Cheat Sheet <a class="header-anchor" href="#build-output-tags-cheat-sheet" aria-label="Permalink to &quot;Build Output Tags Cheat Sheet&quot;">​</a></h3><p>The kernel build system defines an abbreviated tag for each type of operation, controlled by the <code>quiet_cmd_*</code> variable in <code>scripts/Makefile.*</code>. In the default mode (without the <code>V=</code> parameter), <code>make</code> only shows these abbreviations instead of the full command lines, keeping the output compact and readable.</p><p>First are the core compilation tags. <strong>CC</strong> is the one you&#39;ll see most often; it represents the C compiler translating a <code>.c</code> source file into a <code>.o</code> object file. If it&#39;s followed by <code>[M]</code>, it means the file is being compiled as a loadable module (<code>.ko</code>) rather than built into the core kernel. <strong>AS</strong> is the assembler, handling <code>.S</code> assembly source files—many critical paths on ARM64, such as boot code, interrupt handling, and context switching, are written in assembly. <strong>LD</strong> is the linker, combining multiple <code>.o</code> files into a larger object; similarly, <code>[M]</code> indicates that a module is being linked. <strong>AR</strong> is the archiver, packing a batch of <code>.o</code> files into a <code>.a</code> static library—every subdirectory in the kernel source tree ultimately produces a <code>built-in.a</code> containing all the object files from that directory that need to be built into the kernel.</p><p>Next are the host tool tags. <strong>HOSTCC</strong> and <strong>HOSTLD</strong> compile and link helper tools that run on the host machine (our x86_64 WSL2). These tools aren&#39;t for the target board; they&#39;re small utilities needed by the kernel build process itself, such as generating the kernel symbol table or processing the device tree. You might also see <strong>HOSTCXX</strong>, which compiles C++ host tools (like the Qt-based graphical configuration tool for KConfig).</p><p>Generation and checking tags also appear frequently. <strong>GEN</strong> indicates the generation of various intermediate files, such as <code>asm-offsets</code> (a constant bridge between assembly and C) and <code>autoconf.h</code> (converting <code>.config</code> into a C header file). <strong>CHK</strong> and <strong>UPD</strong> are a duo—<code>CHK</code> checks whether a file&#39;s contents need to be regenerated, and if the check finds changes, a <code>UPD</code> line follows to indicate the file was actually updated.</p><p>Device tree related tags are common on ARM platforms. <strong>DTC</strong> is the Device Tree Compiler, which compiles <code>.dts</code> source files into <code>.dtb</code> binary blobs. The device tree is the standard way to describe hardware topology on ARM platforms. While the QEMU virt machine automatically generates a device tree, the kernel also has some device tree blobs embedded at compile time.</p><p>Module related tags appear toward the final stages of compilation. <strong>MODPOST</strong> stands for module post-processing; it generates the <code>Module.symvers</code> file (recording version information for all exported symbols) and checks module symbol dependencies. <strong>SIGN</strong> signs the modules (if <code>CONFIG_MODULE_SIG</code> is enabled), and <strong>DEPMOD</strong> generates the module dependency file <code>modules.dep</code>.</p><p>If you want to see the full command line behind each tag, simply add <code>V=1</code> to the <code>make</code> command:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ARCH=arm64</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> CROSS_COMPILE=aarch64-linux-gnu-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">     O=../../out/build_latest_arm64</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> V=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -j$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nproc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>This prints the complete <code>gcc</code>/<code>ld</code> command for every step, including all compiler options, header search paths, and macro definitions. <code>V=1</code> is essential when debugging build issues.</p><h3 id="verifying-the-build-results" tabindex="-1">Verifying the Build Results <a class="header-anchor" href="#verifying-the-build-results" aria-label="Permalink to &quot;Verifying the Build Results&quot;">​</a></h3><p>After a successful build, the last line of output should look something like this:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>LD      arch/arm64/boot/Image</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The ARM64 kernel boot image is called <code>Image</code> (note that it is uncompressed) and is located under <code>arch/arm64/boot/</code> in the build output directory. We use the <code>file</code> command to verify its format:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> out/build_latest_arm64/arch/arm64/boot/Image</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Linux kernel ARM64 boot executable Image, little-endian, 4K pages</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Seeing <code>Linux kernel ARM64 boot executable Image</code> means we&#39;re on the right track. The ARM32 platform uses a different image name and format—there it&#39;s called <code>zImage</code> (self-extracting compressed image)—but the principle is the same: QEMU loads this file and jumps into it for execution.</p><p>In addition to <code>Image</code>, there&#39;s another important file in the build output directory: <code>vmlinux</code>. This is the uncompressed ELF format kernel, containing the full symbol table and debug information, which we&#39;ll need later for GDB debugging. <code>Image</code> is a pure binary image generated from <code>vmlinux</code> by stripping the ELF headers and debug information using <code>objcopy</code>. It is smaller in size and suitable for QEMU to load.</p><h3 id="output-directory-structure" tabindex="-1">Output Directory Structure <a class="header-anchor" href="#output-directory-structure" aria-label="Permalink to &quot;Output Directory Structure&quot;">​</a></h3><p>We use the <code>O=</code> parameter to place all build artifacts in the <code>out/build_latest_arm64/</code> directory, rather than scattering files throughout the source tree. The structure of this directory is essentially a mirror of the source tree—compiled <code>.o</code> artifacts from source files under <code>kernel/sched/</code> in the source tree end up under <code>out/build_latest_arm64/kernel/sched/</code>, and the boot image from <code>arch/arm64/boot/</code> in the source tree lands in <code>out/build_latest_arm64/arch/arm64/boot/</code>. The benefit of this separation is that the source tree stays clean, and switching between build outputs for different architectures won&#39;t cause any cross-contamination. It&#39;s just like when we write our own C/C++ projects and use build tools like CMake to specify a dedicated build directory, right?</p><h2 id="try-it-yourself" tabindex="-1">Try It Yourself <a class="header-anchor" href="#try-it-yourself" aria-label="Permalink to &quot;Try It Yourself&quot;">​</a></h2><ol><li>Confirm that the configuration process from the previous section is complete and that <code>out/build_latest_arm64/.config</code> exists</li><li>Run the build command and observe the various tags in the terminal output</li><li>After the build finishes, use the <code>file</code> command to verify the format of the <code>Image</code> file</li><li>Use <code>ls -lh out/build_latest_arm64/arch/arm64/boot/Image out/build_latest_arm64/vmlinux</code> to compare the sizes of the two, and understand why QEMU uses <code>Image</code> while GDB uses <code>vmlinux</code></li><li>Try a <code>V=1</code> build (you can compile just a single file: <code>make O=... arch/arm64/kernel/setup.o V=1</code>) to see what the full command line looks like</li></ol><h2 id="further-reading" tabindex="-1">Further Reading <a class="header-anchor" href="#further-reading" aria-label="Permalink to &quot;Further Reading&quot;">​</a></h2><ul><li><a href="https://docs.kernel.org/admin-guide/README.html" target="_blank" rel="noreferrer">Linux Kernel Build Instructions</a> — The official compilation guide on kernel.org</li><li><a href="https://www.kernel.org/doc/html/latest/kbuild/kbuild.html" target="_blank" rel="noreferrer">Kbuild Documentation</a> — Detailed documentation for the build system</li></ul>`,31)])])}const m=t(s,[["render",n]]);export{p as __pageData,m as default};
