import{_ as t,o,c as n,a}from"./chunks/framework.8tiMeCK-.js";const c=JSON.parse('{"title":"11.2  Understanding How KGDB Works Conceptually","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch11_2.md","filePath":"en/notes/linux_kernel_debugging/ch11_2.md","lastUpdated":null}'),s={name:"en/notes/linux_kernel_debugging/ch11_2.md"};function i(r,e,l,h,d,u){return o(),n("div",null,[...e[0]||(e[0]=[a('<h1 id="_11-2-understanding-how-kgdb-works-conceptually" tabindex="-1">11.2 Understanding How KGDB Works Conceptually <a class="header-anchor" href="#_11-2-understanding-how-kgdb-works-conceptually" aria-label="Permalink to &quot;11.2  Understanding How KGDB Works Conceptually&quot;">​</a></h1><p>In the previous section, we set up the SEALS project and got our &quot;hardware&quot; (albeit virtual) and rootfs. Now, we need to bring the real weapon—the debugger—onto the kernel battlefield.</p><p>KGDB enables <strong>source-level</strong> kernel debugging. What does this mean? It means you no longer have to stare at a wall of assembly instructions or vague Oops reports. Instead, you can stop directly at a specific line of C code, inspect variables, and step through execution.</p><p>But you might immediately realize a problem, one that is also the core design challenge of KGDB.</p><h4 id="the-pause-and-the-paradox-who-debugs-the-debugger" tabindex="-1">The Pause and the Paradox: Who Debugs the Debugger? <a class="header-anchor" href="#the-pause-and-the-paradox-who-debugs-the-debugger" aria-label="Permalink to &quot;The Pause and the Paradox: Who Debugs the Debugger?&quot;">​</a></h4><p>When we use GDB to debug a regular user-space application, GDB runs on top of the OS and can pause that unfortunate process at any time. But what if what we want to debug is the <strong>kernel itself</strong>?</p><p>When GDB pauses the kernel to execute a breakpoint instruction, the entire OS stops. The CPU is frozen in kernel mode at that exact moment. So, who runs GDB? Who handles network packets? Who responds to keyboard input?</p><p>This is a classic &quot;performing surgery on yourself&quot; problem. If you are performing an appendectomy on yourself while also holding the scalpel, it&#39;s obviously impossible.</p><p>To solve this paradox, GDB uses a <strong>client-server</strong> architecture, which requires the cooperation of two machines (or two isolated execution environments).</p><h4 id="the-two-leads-host-and-target" tabindex="-1">The Two Leads: Host and Target <a class="header-anchor" href="#the-two-leads-host-and-target" aria-label="Permalink to &quot;The Two Leads: Host and Target&quot;">​</a></h4><p>KGDB&#39;s solution is to split GDB in half, placing each piece in a different world:</p><ol><li><strong>Host</strong>: This is your everyday computer (or our x86_64 virtual machine). It runs the <strong>GDB client</strong>. This is a massive piece of software containing all the fancy symbol resolution, source code display, and TUI graphical interface features. It is the &quot;commander.&quot;</li><li><strong>Target</strong>: This is the machine we want to debug (or the QEMU-emulated ARM32 board). It runs the <strong>GDB server</strong>, which is KGDB. This is a lightweight component that resides permanently in kernel space as part of the kernel. It is the &quot;frontline scout.&quot;</li></ol><blockquote><p><strong>Analogy (Part 1)</strong></p><p>You can think of this process as the collaboration between a <strong>bomb disposal expert and a field assistant</strong>.</p><ul><li>The <strong>Host</strong> is the bomb disposal expert sitting in a safe truck, with blueprints, coffee, and a large screen (the GDB client).</li><li>The <strong>Target</strong> is the field assistant wearing a bomb suit, standing right next to the bomb (the KGDB server).</li><li>The bomb is the kernel that is about to crash.</li><li>The expert can&#39;t go up and cut the wires because they aren&#39;t on site; the assistant must follow the expert&#39;s orders on which wire to cut or which voltmeter to read, and then report the results back to the expert.</li></ul></blockquote><p>But there is a crucial distinction here: a real bomb disposal assistant has free will, whereas the KGDB server is merely a thread embedded within the kernel. When KGDB is active, the entire kernel on the Target is actually <strong>paused</strong>—only when KGDB receives a command from the Host (such as &quot;read the value of variable x&quot;) does it briefly wake up, perform the memory read, send the result back to the Host, and then put the kernel back to sleep.</p><h4 id="the-communication-line-crossing-the-isolation-barrier" tabindex="-1">The Communication Line: Crossing the Isolation Barrier <a class="header-anchor" href="#the-communication-line-crossing-the-isolation-barrier" aria-label="Permalink to &quot;The Communication Line: Crossing the Isolation Barrier&quot;">​</a></h4><p>Since they are separated, the two halves need to talk.</p><p>The GDB client and server typically communicate over a <strong>TCP/IP network</strong> (default port is 1234), though a serial console can also be used.</p><p>The process looks like this:</p><ol><li>You type <code>step</code> in GDB on the Host.</li><li>The GDB client packages this command and sends it over the network to KGDB on the Target.</li><li>KGDB on the Target receives the command, takes control of the CPU, and single-steps one instruction.</li><li>KGDB captures the resulting register state, packages it, and sends it back to the Host.</li><li>Your GDB screen updates, displaying the next line of source code.</li></ol><blockquote><p><strong>Analogy (Part 2: Revealing the Distance)</strong></p><p>Returning to the bomb disposal analogy.</p><p>There is one aspect of this analogy that is &quot;over-anthropomorphized&quot;: the assistant (KGDB) isn&#39;t actually &quot;thinking&quot; when the kernel is paused. It is more like <strong>puppet strings</strong>.</p><p>When the kernel is paused, the entire world (the Target) has hit the pause button. The only thing KGDB can do is respond to the Host&#39;s requests to read or write memory and registers. It&#39;s not like an assistant shouting into a walkie-talkie, &quot;Sir, what&#39;s the voltage?&quot;; it&#39;s more like you using remote desktop software to control a frozen computer—every mouse click (command) you make is a remote electrical signal that forces the CPU to twitch for an instant before going limp again.</p><p>This explains why the Target&#39;s screen usually &quot;freezes&quot; during debugging—because the kernel thread responsible for drawing the display never gets a chance to run.</p></blockquote><h4 id="why-not-jtag-a-brief-aside" tabindex="-1">Why Not JTAG? (A Brief Aside) <a class="header-anchor" href="#why-not-jtag-a-brief-aside" aria-label="Permalink to &quot;Why Not JTAG? (A Brief Aside)&quot;">​</a></h4><p>When it comes to embedded debugging, veterans might ask: why not use JTAG (like the BDI2000 hardware debugger)?</p><p>JTAG is indeed more hardcore. It operates directly at the chip level and can even be used before the kernel boots. Furthermore, JTAG&#39;s built-in gdbserver is usually more stable than KGDB because it doesn&#39;t depend on the kernel&#39;s own robustness.</p><p>But JTAG is expensive and requires physical wiring. KGDB&#39;s advantage is that it is <strong>purely software-based</strong>. As long as your Linux kernel can still catch its breath and run the network stack, you can use KGDB. For tinkering in virtual environments like QEMU, or doing early kernel driver development, KGDB is that readily available Swiss Army knife.</p><p>Alright, the theory is clear: the Host gives the orders, and the Target obediently follows. Now we need to transplant this mechanism onto our specific hardware platform.</p><h4 id="setting-the-stage-vmlinux-vs-bzimage" tabindex="-1">Setting the Stage: vmlinux vs. bzImage <a class="header-anchor" href="#setting-the-stage-vmlinux-vs-bzimage" aria-label="Permalink to &quot;Setting the Stage: vmlinux vs. bzImage&quot;">​</a></h4><p>Before jumping into practice, there is one last conceptual point to clear up.</p><p>When we compile the kernel under Linux, two key files are generated. You must distinguish between them, because loading the wrong one during debugging will cause things to break.</p><ol><li><strong>vmlinux</strong>: This is the <strong>uncompressed</strong> kernel image. It is a massive ELF format file containing all symbol tables. This is meant for <strong>humans and GDB</strong>.</li><li><strong>bzImage / zImage</strong>: This is the <strong>compressed</strong> kernel image (located in the <code>arch/&lt;arch&gt;/boot/</code> directory). This is meant for the <strong>Bootloader</strong> (U-Boot, GRUB). This is what actually gets loaded and executed at boot time.</li></ol><blockquote><p><strong>Analogy (Part 3: Verification)</strong></p><p>Returning to the <strong>blueprint</strong> analogy.</p><ul><li><strong>bzImage</strong> is the machine that has been compressed, packaged, and prepared for shipping to the battlefield.</li><li><strong>vmlinux</strong> is the detailed <strong>engineering blueprint</strong> showing the exact position of every single screw.</li></ul><p>When we debug with KGDB, GDB needs the <strong>blueprint</strong>. It has to know exactly which page and which line (memory address) the <code>sys_open</code> function is on in the blueprint. As for how the machine was compressed and packed into the BOOT partition, GDB doesn&#39;t care.</p><p>If you tell GDB to load <code>bzImage</code>, it&#39;s like handing an engineer a compressed ration pack and asking them to fix a machine—they can&#39;t understand the internal structure. <strong>So, always keep the uncompressed vmlinux at hand.</strong></p></blockquote><p>Having just vmlinux isn&#39;t enough. If this blueprint is an &quot;abridged&quot; version with key areas blacked out (no debug symbols), repairs are still impossible. We need to intentionally bake the symbol information into vmlinux when compiling the kernel.</p><p>That is the dirty work we will tackle in the next section: modifying the kernel configuration to enable <code>CONFIG_DEBUG_INFO</code>.</p>',32)])])}const p=t(s,[["render",i]]);export{c as __pageData,p as default};
