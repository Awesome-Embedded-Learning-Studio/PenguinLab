import{_ as t,o,c as a,a as n}from"./chunks/framework.8tiMeCK-.js";const p=JSON.parse('{"title":"8.9 Quick Reference & Kernel Fragments","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch08_9.md","filePath":"en/notes/linux_kernel_networking/ch08_9.md","lastUpdated":null}'),s={name:"en/notes/linux_kernel_networking/ch08_9.md"};function r(i,e,d,l,c,h){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="_8-9-quick-reference-kernel-fragments" tabindex="-1">8.9 Quick Reference &amp; Kernel Fragments <a class="header-anchor" href="#_8-9-quick-reference-kernel-fragments" aria-label="Permalink to &quot;8.9 Quick Reference &amp; Kernel Fragments&quot;">​</a></h1><p>Throughout this book, we often say: &quot;Don&#39;t memorize—understand the mechanisms.&quot; But before you dive deep into the kernel code, having an accurate &quot;map&quot; in hand is essential. This section is that map.</p><p>In the previous eight sections, we broke down IPv6 reception, transmission, routing, multicast, and autoconfiguration. Now, we need to gather the function call relationships scattered across the <code>net/ipv6/</code> directory. This isn&#39;t your boring API manual that just lists function prototypes—I&#39;ll try to explain the &quot;context&quot; behind these functions so you know exactly where they sit in the grand scheme of the entire flow.</p><p>Finally, we&#39;ll organize a few key constant tables and point out some easily overlooked &quot;special corners.&quot;</p><hr><h3 id="core-methods—the-ipv6-kernel-toolbox" tabindex="-1">Core Methods—The IPv6 Kernel Toolbox <a class="header-anchor" href="#core-methods—the-ipv6-kernel-toolbox" aria-label="Permalink to &quot;Core Methods—The IPv6 Kernel Toolbox&quot;">​</a></h3><p>Let&#39;s start with the kernel methods we encounter most often in the code. Some are responsible for manipulating data structures, while others trigger critical protocol actions.</p><h4 id="_1-address-checks-and-operations" tabindex="-1">1. Address Checks and Operations <a class="header-anchor" href="#_1-address-checks-and-operations" aria-label="Permalink to &quot;1. Address Checks and Operations&quot;">​</a></h4><p>Often, when you get an <code>struct in6_addr *</code>, the first thing you want to know is whether it&#39;s a &quot;valid&quot; or &quot;special&quot; address.</p><p><strong><code>bool ipv6_addr_any(const struct in6_addr *a)</code></strong></p><p>This is the most basic check. It determines whether the given address is the all-zeros address (<code>::</code>).</p><blockquote><p><strong>Why does this matter?</strong> When you initialize a socket or haven&#39;t bound to a specific address yet, the kernel often uses the all-zeros address to represent &quot;any address&quot; or &quot;unspecified.&quot; If you pass an all-zeros address during a route lookup, the routing subsystem will either crash or match a default route, depending on the context. So, asking &quot;Are you <code>any</code>?&quot; first is usually a lifesaver.</p></blockquote><p><strong><code>bool ipv6_addr_equal(const struct in6_addr *a1, const struct in6_addr *a2)</code></strong></p><p>Directly compares two <code>in6_addr</code> structures for equality. This is slightly faster than <code>memcmp</code> and has clearer semantics—it explicitly tells anyone reading the code: this is an IPv6 address comparison.</p><p><strong><code>static inline void ipv6_addr_set(struct in6_addr *addr, __be32 w1, __be32 w2, __be32 w3, __be32 w4)</code></strong></p><p>This is a &quot;manual transmission&quot; function. An <code>in6_addr</code> is essentially a 128-bit chunk of data (usually four 32-bit integers). If you want to manually assemble an address—for example, piecing together a link-local prefix—this function is used to stuff those four <code>u32</code> values in.</p><blockquote><p><strong>⚠️ Pitfall Warning</strong>: Pay attention to byte order. The <code>w1</code> through <code>w4</code> here are in network byte order (<code>__be32</code>). If you fill them directly with local integers, it usually works fine on a PC (because x86 is little-endian), but when porting to certain big-endian architectures or embedded boards, you&#39;ll end up with a reversed address.</p></blockquote><p><strong><code>bool ipv6_addr_is_multicast(const struct in6_addr *addr)</code></strong></p><p>Determines whether it&#39;s a multicast address. The implementation is very straightforward: it checks if the first 8 bits are <code>0xFF</code>. If so, it returns <code>true</code>. Don&#39;t write <code>(addr-&gt;s6_addr[0] == 0xff)</code> yourself—use this macro or function. Not only does it keep the code clean, but if the standard ever changes (however unlikely), the kernel only needs to be updated in one place.</p><hr><h4 id="_2-packet-anatomy—extracting-ipv6-information-from-an-skb" tabindex="-1">2. Packet Anatomy—Extracting IPv6 Information from an SKB <a class="header-anchor" href="#_2-packet-anatomy—extracting-ipv6-information-from-an-skb" aria-label="Permalink to &quot;2. Packet Anatomy—Extracting IPv6 Information from an SKB&quot;">​</a></h4><p>Once a packet arrives at the protocol stack, it&#39;s stuffed into an <code>struct sk_buff</code> (SKB). You need to &quot;extract&quot; the IPv6 header from the SKB.</p><p><strong><code>struct ipv6hdr *ipv6_hdr(const struct sk_buff *skb)</code></strong></p><p>This is the most common &quot;get header&quot; operation. It returns a pointer to the IPv6 header within the SKB.</p><blockquote><p><strong>Note</strong>: This function assumes the SKB&#39;s network layer header pointer (<code>skb-&gt;network_header</code>) has been correctly set. In Netfilter hooks or early stages of the receive path, this is usually safe. But if you get an SKB in an odd corner (like a driver callback), the pointer might not be set yet. Before using this, it&#39;s best to confirm that <code>skb-&gt;network_header</code> is valid.</p></blockquote><p><strong><code>bool ipv6_ext_hdr(u8 nexthdr)</code></strong></p><p>As we discussed earlier, the <code>nexthdr</code> field in IPv6 can be either an upper-layer protocol (TCP/UDP) or the next extension header. This function helps you make that determination: if the value of <code>nexthdr</code> is a known extension header type (like Hop-by-Hop, Routing, Fragment, etc.), it returns <code>true</code>.</p><p>This is extremely useful when traversing the extension header chain—you need to know when to stop (when you hit a transport layer protocol) and when to keep going (when you hit an extension header).</p><hr><h4 id="_3-protocol-registration-and-dispatch—making-the-kernel-recognize-your-protocol" tabindex="-1">3. Protocol Registration and Dispatch—Making the Kernel Recognize Your Protocol <a class="header-anchor" href="#_3-protocol-registration-and-dispatch—making-the-kernel-recognize-your-protocol" aria-label="Permalink to &quot;3. Protocol Registration and Dispatch—Making the Kernel Recognize Your Protocol&quot;">​</a></h4><p>If you want to write a new protocol (like building a layer on top of UDP yourself), or write a Netfilter module to precisely match a certain extension header, you&#39;ll run into these.</p><p><strong><code>int inet6_add_protocol(const struct inet6_protocol *prot, unsigned char protocol)</code></strong></p><p>This is the core function for registering a protocol handler with the kernel.</p><ul><li>If you&#39;re writing TCPv6, you&#39;d set <code>protocol</code> to <code>IPPROTO_TCP</code>.</li><li>If you&#39;re writing ICMPv6, it&#39;s <code>IPPROTO_ICMPV6</code>.</li><li>But interestingly, <strong>the Fragment extension header is also registered through this function</strong> (with a value of <code>NEXTHDR_FRAGMENT</code>).</li></ul><p>After calling this function, the kernel fills your handler function pointer into the global <code>inet6_protos</code> array. Later, when the IPv6 core code finishes parsing the header and finds that <code>nexthdr</code> matches your registered value, it will directly call your function. This is a classic &quot;publish-subscribe&quot; pattern.</p><hr><h4 id="_4-mld-and-multicast—when-a-host-wants-to-join" tabindex="-1">4. MLD and Multicast—When a Host Wants to Join <a class="header-anchor" href="#_4-mld-and-multicast—when-a-host-wants-to-join" aria-label="Permalink to &quot;4. MLD and Multicast—When a Host Wants to Join&quot;">​</a></h4><p>We spent a lot of time on MLDv2 earlier. The kernel logic responsible for joining a network interface to a multicast group or sending MLD reports outward lives in these functions.</p><p><strong><code>bool ipv6_is_mld(struct sk_buff *skb, int nexthdr, int offset)</code></strong></p><p>This is a very handy helper function. It checks for you: is this packet ICMPv6? And is the ICMPv6 type MLD-related? It checks for one of the following four types—returning <code>true</code> if any match:</p><ul><li><code>ICMPV6_MGM_QUERY</code> (MLD Query)</li><li><code>ICMPV6_MGM_REPORT</code> (MLDv1 Report)</li><li><code>ICMPV6_MGM_REDUCTION</code> (Leave Multicast)</li><li><code>ICMPV6_MLD2_REPORT</code> (MLDv2 Report)</li></ul><p><strong><code>int ipv6_dev_mc_inc(struct net_device *dev, const struct in6_addr *addr)</code></strong></p><p>This is the underlying implementation for &quot;making the NIC listen to multicast.&quot;</p><ul><li>When you call <code>setsockopt(..., IPV6_ADD_MEMBERSHIP, ...)</code> in user space, the kernel eventually ends up here (or at its socket variant).</li><li>It does two things: <ol><li>Sets up hardware-level filtering on the NIC so it receives frames destined for this multicast address.</li><li>Maintains a software list in the kernel&#39;s <code>inet6_dev</code> structure, recording which groups this interface belongs to.</li></ol></li></ul><p><strong><code>int ipv6_sock_mc_join(struct sock *sk, int ifindex, const struct in6_addr *addr)</code></strong></p><p>This is the kernel entry point for user space&#39;s <code>IPV6_JOIN_GROUP</code>. It not only calls the aforementioned <code>_inc</code> to update the hardware, but also hangs the socket on that group&#39;s member list, so the kernel knows who to copy multicast packets to when they arrive.</p><hr><h4 id="_5-reception-and-forwarding—a-packet-s-journey" tabindex="-1">5. Reception and Forwarding—A Packet&#39;s Journey <a class="header-anchor" href="#_5-reception-and-forwarding—a-packet-s-journey" aria-label="Permalink to &quot;5. Reception and Forwarding—A Packet&#39;s Journey&quot;">​</a></h4><p><strong><code>int ipv6_rcv(struct sk_buff *skb, struct net_device *dev, struct packet_type *pt, struct net_device *orig_dev)</code></strong></p><p>This is the &quot;main entrance&quot; of the IPv6 protocol stack. When a driver receives an IPv6 packet (Ethernet type 0x86DD) and hands it to the kernel, <code>netif_receive_skb()</code> ultimately throws the packet to <code>ipv6_rcv</code>.</p><p>Its main job is &quot;security screening&quot;:</p><ol><li>Is the version number 6?</li><li>Is the packet length less than the basic IPv6 header length?</li><li>If there&#39;s a problem, it drops the packet and sends an ICMPv6 Parameter Problem.</li><li>If everything checks out, it calls <code>ip6_rcv_finish()</code> to proceed to the next step (route lookup).</li></ol><p><strong><code>int ip6_forward(struct sk_buff *skb)</code></strong></p><p>This is the core of the forwarding path. A packet only reaches here when the machine is configured as a router (<code>/proc/sys/net/ipv6/conf/all/forwarding = 1</code>).</p><p>It does a few critical things:</p><ul><li><strong>Hop Limit check</strong>: If the TTL decrements to 0, it sends an ICMPv6 Time Exceeded and drops the packet.</li><li><strong>PMTU check</strong>: If the packet is too large and exceeds the next hop&#39;s MTU, it sends an ICMPv6 Packet Too Big and drops the packet (IPv6 intermediate routers never fragment).</li><li><strong>Send ICMPv6 Redirect</strong>: If it notices the source host made a poor routing choice (e.g., going through another interface on the local subnet would be closer), it calls <code>ndisc_send_redirect()</code> to notify the source host.</li></ul><hr><h4 id="_6-route-lookup—asking-for-directions" tabindex="-1">6. Route Lookup—Asking for Directions <a class="header-anchor" href="#_6-route-lookup—asking-for-directions" aria-label="Permalink to &quot;6. Route Lookup—Asking for Directions&quot;">​</a></h4><p><strong><code>void ip6_route_input(struct sk_buff *skb)</code></strong></p><p>This is the &quot;input route lookup.&quot; When a packet is destined for the local machine or needs to be forwarded by it, the kernel needs to know: &quot;Where exactly should this packet go?&quot; It calls the underlying <code>fib6_lookup()</code> to search the FIB (Forwarding Information Base) table and attaches the result (a <code>dst_entry</code>) to the SKB.</p><blockquote><p><strong>Note</strong>: The lookup result isn&#39;t just &quot;local delivery&quot; or &quot;forward&quot;—for forwarded packets, it also determines what the next hop&#39;s MAC address is.</p></blockquote><p><strong><code>struct dst_entry *ip6_route_output(struct net *net, const struct sock *sk, struct flowi6 *fl6)</code></strong></p><p>This is the &quot;output route lookup.&quot; When the local machine (like Apache or your client program) needs to send a packet, it asks the kernel: &quot;I want to connect to this IP—which interface should it go out from? Who is the next hop?&quot; It also calls <code>fib6_lookup()</code>, but in the context of the transmit path.</p><hr><h3 id="macros-and-constant-tables—the-common-language-of-the-protocol" tabindex="-1">Macros and Constant Tables—The Common Language of the Protocol <a class="header-anchor" href="#macros-and-constant-tables—the-common-language-of-the-protocol" aria-label="Permalink to &quot;Macros and Constant Tables—The Common Language of the Protocol&quot;">​</a></h3><p>Kernel code is full of magic numbers. To keep people from going crazy, the kernel wraps these magic numbers in macros. Below are the tables we encounter most often when dealing with IPv6 headers and multicast.</p><h4 id="table-8-2-ipv6-extension-headers-and-next-header-values" tabindex="-1">Table 8-2: IPv6 Extension Headers and Next Header Values <a class="header-anchor" href="#table-8-2-ipv6-extension-headers-and-next-header-values" aria-label="Permalink to &quot;Table 8-2: IPv6 Extension Headers and Next Header Values&quot;">​</a></h4><p>These are all possible values for the <code>nexthdr</code> field in an IPv6 header. Note that extension headers and upper-layer protocols (TCP/UDP) share this namespace.</p><table tabindex="0"><thead><tr><th style="text-align:left;">Linux Symbol</th><th style="text-align:left;">Value</th><th style="text-align:left;">Description</th></tr></thead><tbody><tr><td style="text-align:left;"><code>NEXTHDR_HOP</code></td><td style="text-align:left;">0</td><td style="text-align:left;"><strong>Hop-by-Hop Options</strong> header. Must immediately follow the IPv6 header; all routers must examine it.</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_TCP</code></td><td style="text-align:left;">6</td><td style="text-align:left;">TCP segment. An old friend.</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_UDP</code></td><td style="text-align:left;">17</td><td style="text-align:left;">UDP message. DNS and many game protocols run on this.</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_IPV6</code></td><td style="text-align:left;">41</td><td style="text-align:left;"><strong>IPv6 in IPv6</strong> (tunneling protocol). When you see this, it means you&#39;ve peeled off one layer of IPv6, only to find another IPv6 layer inside.</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_ROUTING</code></td><td style="text-align:left;">43</td><td style="text-align:left;"><strong>Routing</strong> header (source routing). Used to specify intermediate nodes that the packet must pass through.</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_FRAGMENT</code></td><td style="text-align:left;">44</td><td style="text-align:left;"><strong>Fragment</strong> header (fragmentation). IPv6 fragmentation can only be done by the source; this header records the fragmentation info.</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_GRE</code></td><td style="text-align:left;">47</td><td style="text-align:left;">GRE header. Commonly used for VPNs.</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_ESP</code></td><td style="text-align:left;">50</td><td style="text-align:left;">Encapsulating Security Payload (part of IPsec). Encrypted data.</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_AUTH</code></td><td style="text-align:left;">51</td><td style="text-align:left;">Authentication Header (another part of IPsec). Verifies data integrity.</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_ICMP</code></td><td style="text-align:left;">58</td><td style="text-align:left;"><strong>ICMP for IPv6</strong>. NDP and MLD both live here.</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_NONE</code></td><td style="text-align:left;">59</td><td style="text-align:left;"><strong>No next header</strong>. This means there&#39;s nothing after this. Used in certain options or security headers to indicate &quot;end of the line.&quot;</td></tr><tr><td style="text-align:left;"><code>NEXTHDR_DEST</code></td><td style="text-align:left;">60</td><td style="text-align:left;"><strong>Destination Options</strong> header. Information that only the final destination needs to process.</td></tr></tbody></table><h4 id="table-8-3-mldv2-multicast-address-record-types" tabindex="-1">Table 8-3: MLDv2 Multicast Address Record Types <a class="header-anchor" href="#table-8-3-mldv2-multicast-address-record-types" aria-label="Permalink to &quot;Table 8-3: MLDv2 Multicast Address Record Types&quot;">​</a></h4><p>These are the type codes used in MLDv2 Report messages, precisely describing a host&#39;s &quot;attitude&quot; toward a specific multicast group.</p><table tabindex="0"><thead><tr><th style="text-align:left;">Linux Symbol</th><th style="text-align:left;">Value</th><th style="text-align:left;">Description</th></tr></thead><tbody><tr><td style="text-align:left;"><code>MLD2_MODE_IS_INCLUDE</code></td><td style="text-align:left;">1</td><td style="text-align:left;"><strong>Include mode</strong>: I only want to receive packets from sources listed in the <em>Sources List</em>.</td></tr><tr><td style="text-align:left;"><code>MLD2_MODE_IS_EXCLUDE</code></td><td style="text-align:left;">2</td><td style="text-align:left;"><strong>Exclude mode</strong>: I want to receive all packets for this group, <strong>except</strong> those from the <em>Sources List</em>.</td></tr><tr><td style="text-align:left;"><code>MLD2_CHANGE_TO_INCLUDE</code></td><td style="text-align:left;">3</td><td style="text-align:left;">Switch to Include mode.</td></tr><tr><td style="text-align:left;"><code>MLD2_CHANGE_TO_EXCLUDE</code></td><td style="text-align:left;">4</td><td style="text-align:left;">Switch to Exclude mode.</td></tr><tr><td style="text-align:left;"><code>MLD2_ALLOW_NEW_SOURCES</code></td><td style="text-align:left;">5</td><td style="text-align:left;">Allow new sources to join (append to the list in Include mode).</td></tr><tr><td style="text-align:left;"><code>MLD2_BLOCK_OLD_SOURCES</code></td><td style="text-align:left;">6</td><td style="text-align:left;">Block old sources (remove from the list in Include mode).</td></tr></tbody></table><h4 id="table-8-4-icmpv6-parameter-problem-codes" tabindex="-1">Table 8-4: ICMPv6 Parameter Problem Codes <a class="header-anchor" href="#table-8-4-icmpv6-parameter-problem-codes" aria-label="Permalink to &quot;Table 8-4: ICMPv6 Parameter Problem Codes&quot;">​</a></h4><p>When the kernel encounters a parsing error, it sends a &quot;Parameter Problem&quot; message. This Code field tells the peer exactly what went wrong.</p><table tabindex="0"><thead><tr><th style="text-align:left;">Linux Symbol</th><th style="text-align:left;">Value</th><th style="text-align:left;">Description</th></tr></thead><tbody><tr><td style="text-align:left;"><code>ICMPV6_HDR_FIELD</code></td><td style="text-align:left;">0</td><td style="text-align:left;"><strong>Erroneous header field</strong>. There&#39;s garbage in a header field.</td></tr><tr><td style="text-align:left;"><code>ICMPV6_UNK_NEXTHDR</code></td><td style="text-align:left;">1</td><td style="text-align:left;"><strong>Unrecognized Next Header type</strong>. The kernel received a <code>nexthdr</code> type it doesn&#39;t recognize and doesn&#39;t know how to peel off the next layer.</td></tr><tr><td style="text-align:left;"><code>ICMPV6_UNK_OPTION</code></td><td style="text-align:left;">2</td><td style="text-align:left;"><strong>Unrecognized IPv6 option</strong>. Encountered an unknown Option while parsing Hop-by-Hop or Destination Options.</td></tr></tbody></table><hr><h3 id="special-addresses—those-untouchable-constants" tabindex="-1">Special Addresses—Those &quot;Untouchable&quot; Constants <a class="header-anchor" href="#special-addresses—those-untouchable-constants" aria-label="Permalink to &quot;Special Addresses—Those &quot;Untouchable&quot; Constants&quot;">​</a></h3><p>The kernel uses a few global <code>struct in6_addr</code> instances to define special addresses that appear on almost every network interface. You can reference them directly without needing to fill in arrays yourself.</p><p><strong>Note</strong>: These instances are read-only; just use them directly for comparison or assignment.</p><ul><li><strong><code>in6addr_any</code></strong>: The all-zeros address <code>::</code>. Represents &quot;any address&quot; or &quot;unspecified.&quot; Commonly used when listening on a socket.</li><li><strong><code>in6addr_loopback</code></strong>: The loopback address <code>::1</code>. Always represents the local machine.</li><li><strong><code>in6addr_linklocal_allnodes</code></strong>: The link-local all-nodes address <code>ff02::1</code>. Packets sent to this address are received by all IPv6-enabled hosts on the local link.</li><li><strong><code>in6addr_linklocal_allrouters</code></strong>: The link-local all-routers address <code>ff02::2</code>. Only routers listen to this.</li><li><strong><code>in6addr_interfacelocal_allnodes</code></strong>: Interface-local all-nodes <code>ff01::1</code>.</li><li><strong><code>in6addr_interfacelocal_allrouters</code></strong>: Interface-local all-routers <code>ff01::2</code>.</li><li><strong><code>in6addr_sitelocal_allrouters</code></strong>: Site-local all-routers <code>ff05::2</code>. Note that Site-Local addresses (fec0::/10) have been deprecated by RFC 3879, but the kernel retains these constants for compatibility.</li></ul><hr><h3 id="routing-table-management—how-user-space-commands-land-in-the-kernel" tabindex="-1">Routing Table Management—How User Space Commands Land in the Kernel <a class="header-anchor" href="#routing-table-management—how-user-space-commands-land-in-the-kernel" aria-label="Permalink to &quot;Routing Table Management—How User Space Commands Land in the Kernel&quot;">​</a></h3><p>On Linux, we generally use two tools to configure routing: the legacy <code>route</code> (net-tools) and the modern <code>iproute2</code> (the <code>ip</code> command). Although their interfaces differ, they converge at the same low-level kernel implementation.</p><h4 id="adding-deleting-routes-with-iproute2" tabindex="-1">Adding/Deleting Routes with <code>iproute2</code> <a class="header-anchor" href="#adding-deleting-routes-with-iproute2" aria-label="Permalink to &quot;Adding/Deleting Routes with ``iproute2``&quot;">​</a></h4><p>This is the recommended approach today. It communicates with the kernel via Netlink Sockets.</p><ol><li><p><strong>Add a route</strong> (<code>ip -6 route add ...</code>):</p><ul><li>User space sends a Netlink message.</li><li>On the kernel side, <code>net/ipv6/route.c</code> receives the message and calls <code>inet6_rtm_newroute()</code>.</li><li><code>inet6_rtm_newroute()</code> parses the parameters and ultimately calls <code>ip6_route_add()</code> to stuff the route entry into the FIB table.</li></ul></li><li><p><strong>Delete a route</strong> (<code>ip -6 route del ...</code>):</p><ul><li>User space sends a Netlink message.</li><li>The kernel side calls <code>inet6_rtm_delroute()</code>.</li><li>It ultimately calls <code>ip6_route_del()</code> to remove that route.</li></ul></li><li><p><strong>View routes</strong> (<code>ip -6 route show</code>):</p><ul><li>The kernel side calls <code>inet6_dump_fib()</code>, traverses the entire FIB table, and spits out each entry to user space via Netlink.</li></ul></li></ol><h4 id="using-the-route-command-the-old-school-way" tabindex="-1">Using the <code>route</code> Command (The Old-School Way) <a class="header-anchor" href="#using-the-route-command-the-old-school-way" aria-label="Permalink to &quot;Using the ``route`` Command (The Old-School Way)&quot;">​</a></h4><p>This is the classic <code>ioctl</code> approach. Although the <code>route</code> command itself is quite old, the kernel retains this code for compatibility.</p><ol><li><p><strong>Add a route</strong> (<code>route -A inet6 add ...</code>):</p><ul><li>The <code>route</code> tool calls <code>ioctl(sockfd, SIOCADDRT, &amp;rt)</code>.</li><li>The kernel&#39;s <code>ipv6_route_ioctl()</code> catches this request.</li><li>Internally, it still calls <code>ip6_route_add()</code> to do the actual work.</li></ul></li><li><p><strong>Delete a route</strong> (<code>route -A inet6 del ...</code>):</p><ul><li>Calls <code>ioctl(sockfd, SIOCDELRT, &amp;rt)</code>.</li><li><code>ipv6_route_ioctl()</code> handles it, internally calling <code>ip6_route_del()</code>.</li></ul></li></ol><blockquote><p><strong>Conclusion</strong>: No matter which tool you use, the two functions doing the heavy lifting at the very bottom are always the same. This once again illustrates the Linux kernel&#39;s layered design philosophy—user interfaces can change, but the core logic stays put.</p></blockquote><hr><h3 id="chapter-echo" tabindex="-1">Chapter Echo <a class="header-anchor" href="#chapter-echo" aria-label="Permalink to &quot;Chapter Echo&quot;">​</a></h3><p>Alright, we&#39;ve finally finished the entire IPv6 chapter—from the original 128-bit address design, to the granular Neighbor Discovery messages, to multicast source filtering, and today&#39;s quick reference.</p><p>What this chapter truly leaves you with shouldn&#39;t be the names of those dozens of functions, but rather an appreciation for IPv6&#39;s <strong>&quot;unified&quot;</strong> design philosophy. Look at it this way: ARP, RARP, ICMP Redirect, IGMP... these protocols scattered all over the place in IPv4 have been unified under the single banner of ICMPv6 in IPv6. NDP is ICMPv6, MLD is ICMPv6, even error reporting is ICMPv6. This unification makes the protocol stack leaner and more efficient, but it also makes the parsing of every ICMPv6 packet more critical—miss one type, and you might lose a key piece of neighbor information.</p><p>We spent a lot of time circling around structures like <code>inet6_dev</code>, <code>mc_list</code>, and <code>fib6_table</code> because network protocols aren&#39;t just theoretical state machines—they are real, tangible kernel memory structures. If you don&#39;t know which linked list a multicast filter hangs off of, you&#39;ll just stare blankly at packets captured by tcpdump when debugging packet loss, with no idea which <code>if</code> statement to set a breakpoint on in the kernel code.</p><p>In the next chapter, we&#39;ll cross over the protocol stack&#39;s processing layer and enter the legendary &quot;great network filter&quot;—<strong>Netfilter</strong>. That is the most treacherous stretch of the kernel networking stack, and the birthplace of magical features like firewalls, NAT, and Conntrack. You&#39;ll see how, just as a packet has finished being parsed for its IPv6 header and is about to be happily handed off to a socket, it&#39;s grabbed by the &quot;hand of God&quot; that is Netfilter, which judges whether it lives or dies.</p><p>That will be a game of interception and rewriting.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: Understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: Understanding&quot;">​</a></h3><p><strong>Question</strong>: During the IPv6 protocol stack initialization, the kernel needs to register specific protocol handling logic (such as TCPv6, UDPv6, or extension header handling) into the system. Suppose you are a kernel developer and need to write a module to handle a new IPv6 extension header (with a Next Header number of N). Which function should you call to complete the registration? What is its signature, and which global data structure in the kernel will store this handling logic?</p><details><summary>Answer &amp; Analysis</summary><p><strong>Answer</strong>: You should call the <code>inet6_add_protocol()</code> function. Its signature is <code>int inet6_add_protocol(const struct inet6_protocol *prot, unsigned char protocol)</code>. The handling logic will be stored in the global array <code>inet6_protos[]</code>.</p><p><strong>Analysis</strong>: Based on knowledge point <code>inet6_add_protocol()</code>, this is the standard kernel function for registering IPv6 protocol handlers (including transport layer protocols and extension headers). During registration, the protocol number (like IPPROTO_UDP or an extension header identifier) serves as the index, storing the <code>struct inet6_protocol</code> pointer in the <code>inet6_protos</code> array. This way, when <code>ipv6_rcv</code> parses a packet, it can find the corresponding handler callback function through the <code>nexthdr</code> field.</p></details><h3 id="exercise-2-application" tabindex="-1">Exercise 2: Application <a class="header-anchor" href="#exercise-2-application" aria-label="Permalink to &quot;Exercise 2: Application&quot;">​</a></h3><p><strong>Question</strong>: A host has just generated a link-local IPv6 address. Before passing Duplicate Address Detection (DAD), the address is in a &quot;tentative&quot; state. To verify the address&#39;s uniqueness, the Neighbor Discovery Protocol needs to use a special multicast address. If the host&#39;s link-local address is <code>fe80::2aa:ff:fe3f:4a21</code> (ignoring the <code>ff:fe</code> insertion bit), which solicited-node multicast address should it join for DAD verification? Please write out the calculation process and the result.</p><details><summary>Answer &amp; Analysis</summary><p><strong>Answer</strong>: The result is <code>ff02::1:ff3f:4a21</code>. Calculation process: take the low 24 bits of the unicast address (3f:4a:21) and prepend the prefix <code>ff02:0:0:0:0:1:ff00::/104</code>.</p><p><strong>Analysis</strong>: Based on knowledge point <code>Solicited-Node Multicast Address</code>, the solicited-node multicast address is used for Neighbor Discovery and DAD. Its generation rule is to concatenate the low 24 bits of a unicast/anycast address with the fixed prefix <code>ff02:0:0:0:0:1:ff00::/104</code>. The low 24 bits of the address in the question are <code>3f:4a:21</code>, and concatenating them yields <code>ff02::1:ff3f:4a21</code>. The host sends a Neighbor Solicitation message to this address; if another host owns that address, it will respond with a Neighbor Advertisement.</p></details><h3 id="exercise-3-thinking" tabindex="-1">Exercise 3: Thinking <a class="header-anchor" href="#exercise-3-thinking" aria-label="Permalink to &quot;Exercise 3: Thinking&quot;">​</a></h3><p><strong>Question</strong>: IPv6&#39;s &quot;autoconfiguration&quot; allows hosts to generate IP addresses statelessly. However, the standard EUI-64 interface ID generation method (based on MAC addresses) can lead to user privacy tracking. The Linux kernel provides a mechanism called &quot;Privacy Extensions&quot; to solve this problem. Analyzing this in the context of RFC 4941, why does generating IPv6 interface IDs based on MAC addresses pose a privacy risk? How does the kernel mitigate this risk through the Privacy Extensions mechanism?</p><details><summary>Answer &amp; Analysis</summary><p><strong>Answer</strong>: Generating interface IDs based on MAC addresses results in a fixed, unchanging interface ID, and MAC addresses are globally unique hardware identifiers. As long as a user connects to a network, the latter half of their IPv6 address remains constant, making cross-network user behavior tracking easy. The Privacy Extensions (RFC 4941) mechanism mitigates this risk by generating random interface IDs (instead of MAC-based ones) to create temporary addresses, and these temporary addresses periodically expire and are renewed, thereby increasing the difficulty of tracking and protecting user privacy.</p><p><strong>Analysis</strong>: This question tests a deep understanding of the &quot;Autoconfiguration&quot; and &quot;Privacy Extensions&quot; knowledge points. IPv6 stateless autoconfiguration typically combines the MAC address to generate an interface ID (like EUI-64), which is convenient for address uniqueness but also exposes the device&#39;s identity. The core of Privacy Extensions is &quot;decoupling&quot;—by randomizing the interface ID and introducing a lifetime (Preferred Lifetime), the same device uses different IP addresses across different time periods or different networks, thereby preventing persistent surveillance. This is a classic design that maintains network layer connectivity while improving user security.</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>The kernel implementation of IPv6 is built upon the <code>in6_addr</code> data structure and a brand-new address classification system. By using a union to divide the 128-bit address into 8-bit, 16-bit, and 32-bit views, the kernel can efficiently handle memory operations and bitwise calculations at different granularities. At the same time, IPv6 completely abandons broadcast in favor of three modes: unicast, anycast, and multicast. In particular, the introduction of the &quot;Solicited-Node Multicast Address&quot; maps addresses to specific <code>ff02::1:ffxx:xxxx</code> multicast groups, ensuring that the Neighbor Discovery process occurs only within a very small scope. This drastically solves the &quot;Ethernet noise&quot; problem caused by ARP broadcasts in the IPv4 era.</p><p>The protocol header design reflects IPv6&#39;s engineering philosophy of &quot;streamlined skeleton, flexible extension.&quot; The IPv6 header is fixed at 40 bytes, removing the checksum to reduce router burden, and moving all optional features out. Through the <code>nexthdr</code> field, IPv6 introduces a chained processing mechanism for &quot;extension headers&quot; (like Hop-by-Hop, Routing, Fragment), where each header is linked together like a node in a linked list. This design means intermediate routers only need to process the IPv6 main header and a very small number of hop-by-hop options, greatly improving forwarding efficiency and achieving a return from &quot;complex variable-length structures&quot; to &quot;fixed fast processing.&quot;</p><p>The Autoconfiguration mechanism gives hosts &quot;plug-and-play&quot; capabilities, allowing them to generate globally routable addresses without a DHCP server. This process is divided into four phases: first, generating a link-local address and performing Duplicate Address Detection (DAD); then sending a Router Solicitation (RS); next, receiving a Router Advertisement (RA) containing prefix information; and finally, combining the interface ID (usually MAC-based or randomly generated via Privacy Extensions) to synthesize a global unicast address. Coupled with the Valid Lifetime and Preferred Lifetime mechanisms, network administrators can achieve smooth, seamless renumbering of prefixes across the entire network by adjusting RA parameters.</p><p>In the packet receive path <code>ipv6_rcv()</code>, the kernel maintains protocol purity through strict &quot;security screening&quot; rules. The function first drops illegal packets with incorrect version numbers, loopback destination addresses, or multicast source addresses, and forcibly parses the Hop-by-Hop extension header. Afterwards, the packet passes through a Netfilter hook into <code>ip6_rcv_finish()</code>, where a route lookup (<code>fib6_lookup</code>) is performed. Based on the lookup result, the kernel decides the packet&#39;s fate: either handing it to the local protocol stack (<code>ip6_input</code>) for onion-style extension header parsing and dispatch to upper layers, or forwarding it (<code>ip6_forward</code>).</p><p>The revolution in fragmentation handling is one of the key features that distinguishes IPv6 from IPv4, but it also introduces potential MTU traps. IPv6 prohibits intermediate routers from fragmenting; all fragmentation must be done by the source host. If a smaller MTU link is encountered along the transmission path, the router will simply drop the packet and send back an ICMPv6 &quot;Packet Too Big&quot; message, forcing the source host to perform Path MTU Discovery (PMTUD). Therefore, when configuring firewalls or routes, you must ensure that ICMPv6 traffic is not blocked; otherwise, it will lead to silent connectivity failures for large packets. This is the most common failure point in IPv6 network operations.</p>',115)])])}const g=t(s,[["render",r]]);export{p as __pageData,g as default};
