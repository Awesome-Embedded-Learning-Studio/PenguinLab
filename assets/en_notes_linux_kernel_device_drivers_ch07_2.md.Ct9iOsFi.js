import{_ as s,o as t,c as a,a as i}from"./chunks/framework.8tiMeCK-.js";const k=JSON.parse('{"title":"7.2 Memory Barriers in Practice: A Careful Conversation with Hardware","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_device_drivers/ch07_2.md","filePath":"en/notes/linux_kernel_device_drivers/ch07_2.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_device_drivers/ch07_2.md"};function r(o,e,l,h,p,d){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="_7-2-memory-barriers-in-practice-a-careful-conversation-with-hardware" tabindex="-1">7.2 Memory Barriers in Practice: A Careful Conversation with Hardware <a class="header-anchor" href="#_7-2-memory-barriers-in-practice-a-careful-conversation-with-hardware" aria-label="Permalink to &quot;7.2 Memory Barriers in Practice: A Careful Conversation with Hardware&quot;">​</a></h1><p>In the previous section, we discussed how to safely manage reference counts, but in the world of kernel synchronization, beyond the lifecycle of data structures, there is another area that gives hardware driver developers headaches: <strong>communicating with the outside world</strong>.</p><p>By &quot;outside world,&quot; we mean DMA controllers and network chips.</p><p>When you offload data movement to hardware, you are essentially collaborating with an entity that has a completely different &quot;mindset.&quot; CPUs execute instructions out of order for performance, and compilers optimize instructions for efficiency, but the DMA controller on the hardware side couldn&#39;t care less. It follows a strict rule: it reads descriptors in the order they appear in memory (or according to its own defined timing).</p><p>If you assume that writing code sequentially means the hardware will receive it in that same order, you are gravely mistaken.</p><p>Here is a counterintuitive fact: <strong>the order you see in C code is not necessarily the order that occurs in memory.</strong></p><p>To understand why we need to use a seemingly &quot;low-level&quot; mechanism to solve this problem, let&#39;s look at a real driver example.</p><hr><h3 id="starting-with-a-network-driver" tabindex="-1">Starting with a Network Driver <a class="header-anchor" href="#starting-with-a-network-driver" aria-label="Permalink to &quot;Starting with a Network Driver&quot;">​</a></h3><p>Take the Realtek 8139 &quot;Fast Ethernet&quot; network driver as an example (code located at <code>drivers/net/ethernet/realtek/8139cp.c</code>). To transmit a network packet, the driver must first set up a DMA descriptor, telling the hardware: &quot;fetch data from here, here is the length, and here are the flags.&quot;</p><p>For this specific network chip, the DMA descriptor looks like this:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// drivers/net/ethernet/realtek/8139cp.c</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cp_desc {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __le32 opts1;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __le32 opts2;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    __le64 addr;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>These three fields are: option 1, option 2, and the data address. Before handing the data over to DMA, all three fields must be initialized.</p><p>Now, the question arises: why does the initialization order of these three fields matter so much?</p><hr><h3 id="why-does-order-matter" tabindex="-1">Why Does Order Matter? <a class="header-anchor" href="#why-does-order-matter" aria-label="Permalink to &quot;Why Does Order Matter?&quot;">​</a></h3><p>You can think of this DMA descriptor as a &quot;shipping manifest.&quot;</p><ol><li><strong><code>addr</code> (Address)</strong>: Tells the warehouse (DMA) which shelf to fetch the goods from.</li><li><strong><code>opts1</code> / <code>opts2</code> (Options)</strong>: Tells the warehouse how to handle this batch of goods, such as &quot;expedite,&quot; &quot;valid,&quot; or &quot;this is the final order.&quot;</li></ol><p>If you are the warehouse clerk (CPU) responsible for filling out the form, you might casually write the &quot;handling method&quot; first, then write the &quot;shelf number.&quot;</p><p>But in the real world, the DMA controller &quot;mover&quot; is extremely rigid. It might glance at the manifest in memory from time to time. The moment it sees the &quot;manifest valid&quot; flag set to 1, it will immediately grab its shovel and get to work—<strong>completely regardless of whether you&#39;ve finished writing down the shelf number or not</strong>.</p><p>If, from the DMA controller&#39;s perspective, it first sees the &quot;valid&quot; flag in <code>opts1</code>, and immediately goes to read <code>addr</code>, but due to CPU out-of-order execution or cache incoherence, it reads a stale address or 0...</p><p>The result is: <strong>the hardware starts moving the wrong memory, or directly triggers a catastrophic bus error.</strong></p><p>So here is an ironclad rule: <strong>you must write all the preparatory fields first, and only &quot;give the final stamp of approval&quot; (set the valid flag) at the very end.</strong></p><hr><h3 id="wmb-the-impenetrable-wall" tabindex="-1">wmb(): The Impenetrable Wall <a class="header-anchor" href="#wmb-the-impenetrable-wall" aria-label="Permalink to &quot;wmb(): The Impenetrable Wall&quot;">​</a></h3><p>To prevent this kind of tragedy, the kernel&#39;s DMA mapping guidelines explicitly require: <strong>when writing DMA descriptors, you must guarantee the memory write order.</strong></p><p>This is where <code>wmb()</code> (Write Memory Barrier) comes in. Its job is to drive a stake into the code, telling both the compiler and the CPU: &quot;All write operations before this point must be fully committed to memory (and visible to other devices) before any write operations after this point can execute.&quot;</p><p>Returning to our Realtek driver, let&#39;s see how it transmits a packet (the <code>cp_start_xmit</code> function).</p><p>First, prepare the data:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    len </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> skb</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">len;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    mapping </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> dma_map_single</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cp</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pdev</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">dev</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, skb</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, len,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                             PCI_DMA_TODEVICE);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><code>mapping</code> is the physical address—the &quot;shelf number&quot; we need to tell the hardware about.</p><p>Next comes the critical part, setting up the descriptor:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cp_desc </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">txd;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* [...... 省略部分代码 ......] */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 第一步：设置 opts2 和地址</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    txd</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">opts2 </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> opts2;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    txd</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">addr </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> cpu_to_le64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(mapping);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 【关键点 1】屏障！</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 确保 opts2 和 addr 已经安全写入内存，绝不能被重排到后面去</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    wmb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 第二步：组装最终的控制字（包括长度、首尾标志等）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    opts1 </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> eor </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> len </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> FirstFrag </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LastFrag;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 第三步：写入 opts1（包含让硬件开始干活的标志位）</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    txd</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">opts1 </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> cpu_to_le32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(opts1);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 【关键点 2】再来一道屏障！</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 确保 opts1 的写入（尤其是那个「有效」位）立刻对硬件可见</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    wmb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br></div></div><p>Do you see what&#39;s happening here?</p><ol><li><strong>The first <code>wmb()</code></strong>: Protects data dependencies. It ensures that the two &quot;preparatory&quot; fields, <code>opts2</code> and <code>addr</code>, land in memory first. Without this barrier, the CPU or compiler might decide &quot;it&#39;s faster to calculate opts1 first,&quot; causing the flag to be written before the address.</li><li><strong>The second <code>wmb()</code></strong>: Ensures the final command takes effect. Once this line of code executes, the hardware must immediately see this descriptor turn red (valid), thereby starting the DMA transfer.</li></ol><p>It&#39;s just like filling out a manifest: <strong>fill in all the details first, double-check them, and only then check the &quot;ship?&quot; box.</strong> The <code>wmb()</code> in the middle is the pen in your hand, forcing you to follow the order and not allowing you to check the box early just because you have fast hands.</p><hr><h3 id="the-myth-of-volatile-faq" tabindex="-1">The Myth of volatile (FAQ) <a class="header-anchor" href="#the-myth-of-volatile-faq" aria-label="Permalink to &quot;The Myth of volatile (FAQ)&quot;">​</a></h3><p>At this point, people often ask: <strong>&quot;Since the <code>volatile</code> keyword can also prevent compiler optimizations, why not use it to guarantee ordering?&quot;</strong></p><p>This is a very classic misconception.</p><p><code>volatile</code> does indeed tell the compiler: &quot;don&#39;t mess with reads and writes to this variable; always faithfully fetch the value from memory.&quot; This is useful when operating MMIO (Memory-Mapped I/O).</p><p>But in the concurrent world, <code>volatile</code> has two fatal flaws:</p><ol><li><strong>It does not guarantee atomicity</strong>: If two threads perform a <code>i++</code> on a <code>volatile int i</code> at the same time, it will still be a mess, because <code>i++</code> is a read-modify-write sequence of three instructions, and <code>volatile</code> has no jurisdiction at that level.</li><li><strong>It does not act as a memory barrier</strong>: Although the C standard dictates that accesses between <code>volatile</code> variables cannot be reordered, <strong>it has no control over non-volatile variables</strong>. In the <code>txd-&gt;addr = ...; txd-&gt;opts1 = ...;</code> example, even if <code>opts1</code> is <code>volatile</code>, the compiler and CPU might still reorder the assignment to <code>addr</code> to happen later, because they aren&#39;t in the same camp.</li></ol><p>So, don&#39;t count on <code>volatile</code> to solve synchronization problems. In the kernel, we either use locks, atomic operations, or, as shown above, we dutifully insert memory barriers.</p><hr><h3 id="beyond-locks" tabindex="-1">Beyond Locks <a class="header-anchor" href="#beyond-locks" aria-label="Permalink to &quot;Beyond Locks&quot;">​</a></h3><p>Honestly, as a driver developer, you don&#39;t need to append <code>wmb()</code> after every line of code. Most of the heavy lifting is already done for you behind the scenes by the kernel&#39;s lock APIs and primitives (like RCU).</p><p>However, when your code starts crossing that boundary—from pure software memory operations to <strong>telling hardware to do work</strong> (such as setting up DMA descriptors or triggering register commands)—you must be on high alert.</p><p>At this boundary, the CPU&#39;s assumptions (e.g., &quot;I can execute out of order&quot;) no longer hold true. Hardware is honest and rigid; it interprets your memory layout literally. At this point, explicitly adding a memory barrier is a sign of respect for the hardware, and a commitment to system stability.</p><p>The picture is clear at this point: <code>atomic_t</code> protects the <em>value</em> of data, while memory barriers protect the <em>timing</em> of data. Both are indispensable.</p><hr><h3 id="⚠️-pitfall-warning" tabindex="-1">⚠️ Pitfall Warning <a class="header-anchor" href="#⚠️-pitfall-warning" aria-label="Permalink to &quot;⚠️ Pitfall Warning&quot;">​</a></h3><blockquote><p><strong>Never take shortcuts in the middle of DMA descriptor initialization.</strong></p><p>You might think: &quot;Oh, the docs say <code>opts2</code> isn&#39;t important, so I&#39;ll just skip it,&quot; or &quot;Let me just remove the <code>wmb()</code>; I&#39;ve run it a thousand times without a crash.&quot;</p><p>Trust me, you might get lucky on x86 without issues (x86 has a strong memory model, and the hardware itself guarantees quite a bit of ordering). But once you port the code to ARM or PowerPC, or switch to a more particular network card, you will be rewarded with extremely hard-to-reproduce bugs—maybe packets going to the wrong places, maybe a kernel panic, and these bugs tend to strike at 3 AM when the load is at its highest.</p></blockquote>`,53)])])}const u=s(n,[["render",r]]);export{k as __pageData,u as default};
