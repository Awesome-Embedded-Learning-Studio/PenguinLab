import{_ as t,o,c as i,a as s}from"./chunks/framework.8tiMeCK-.js";const p=JSON.parse('{"title":"11.1 Sockets","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch11_1.md","filePath":"en/notes/linux_kernel_networking/ch11_1.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_networking/ch11_1.md"};function r(a,e,l,c,h,d){return o(),i("div",null,[...e[0]||(e[0]=[s('<h1 id="_11-1-sockets" tabindex="-1">11.1 Sockets <a class="header-anchor" href="#_11-1-sockets" aria-label="Permalink to &quot;11.1 Sockets&quot;">​</a></h1><p>A philosophical question has run throughout Unix history: &quot;everything is a file.&quot;</p><p>Following this philosophy, network communication should feel as natural as reading and writing a local file: <code>open()</code>, <code>read()</code>, <code>write()</code>, <code>close()</code>. But when you actually dive into the network subsystem implementation, you&#39;ll find that the kernel goes to extraordinarily complex lengths behind the scenes to maintain this &quot;file&quot; illusion.</p><p>What&#39;s even more confusing is that to implement this interface, the kernel maintains two structs with strikingly similar names but completely different responsibilities. Why split a simple communication endpoint in half? This isn&#39;t just historical baggage—it&#39;s the first real hurdle to understanding the entire Linux Network Stack.</p><p>In this section, we&#39;ll start by examining how this interface is designed from the user&#39;s perspective, then peel back the layer to reveal the &quot;twin&quot; puzzle hidden inside the kernel.</p><h2 id="socket-types" tabindex="-1">Socket Types <a class="header-anchor" href="#socket-types" aria-label="Permalink to &quot;Socket Types&quot;">​</a></h2><p>The Socket API supports multiple communication types. We&#39;ll briefly survey them here, focusing on the ones we&#39;ll explore in depth in this chapter.</p><p><strong>Stream Sockets (SOCK_STREAM)</strong> Think of this as <strong>making a phone call</strong>. First, you dial to establish a connection, much like TCP&#39;s three-way handshake—if the other party doesn&#39;t pick up (busy, powered off), the call won&#39;t go through. Once connected, the channel is exclusive and bidirectional. Whatever you say, the other party hears exactly that, and the voice (byte stream) arrives in order with no dropped words. TCP is the poster child for this mechanism. If you can&#39;t tolerate data corruption or out-of-order delivery, this is the safe choice.</p><p><strong>Datagram Sockets (SOCK_DGRAM)</strong> This is like <strong>sending a postcard</strong>. You drop the postcard in the mailbox and walk away. Neither you nor the post office guarantees when it will arrive, or even that it will arrive at all—it might blow away halfway, arrive three days late, or the recipient might receive two identical copies. The upside is you don&#39;t need to call ahead and ask &quot;can I send you a letter now?&quot;; you just send it. UDP follows this model. If you&#39;re writing real-time audio/video or games where you can tolerate packet loss but are extremely sensitive to latency, postcards are your go-to.</p><p>But remember, the postcard analogy has one imprecise aspect: in a highly reliable LAN environment, UDP rarely drops packets, making it look like a dependable courier service. Don&#39;t be fooled by this appearance—once you cross the public internet, it instantly reverts to being a postcard that might get lost.</p><p><strong>Raw Sockets (SOCK_RAW)</strong> This is a &quot;god mode&quot; interface. It lets you bypass the transport layer and talk directly to the IP layer. If you want to construct your own IP headers, implement your own protocol, or write a tool like <code>ping</code> (which requires ICMP access), this is what you need. Naturally, this privilege usually requires root permissions—after all, the kernel doesn&#39;t want you arbitrarily spoofing a source IP to cause trouble.</p><p><strong>Other Types</strong> Linux also supports a few more specialized types:</p><ul><li><strong>SOCK_RDM</strong>: Provides reliable message delivery, primarily used with TIPC (Transparent Inter-Process Communication).</li><li><strong>SOCK_SEQPACKET</strong>: Similar to SOCK_STREAM in being connection-oriented, but it preserves record boundaries.</li><li><strong>SOCK_DCCP</strong>: Datagram Congestion Control Protocol, a transport protocol combining characteristics of both TCP and UDP.</li><li><strong>SOCK_PACKET</strong>: Considered obsolete in the <code>AF_INET</code> protocol family.</li></ul><h2 id="core-socket-api-methods" tabindex="-1">Core Socket API Methods <a class="header-anchor" href="#core-socket-api-methods" aria-label="Permalink to &quot;Core Socket API Methods&quot;">​</a></h2><p>Userspace programmers work with these functions every day. But from the kernel&#39;s perspective, they aren&#39;t just function calls—they&#39;re entry points into a complex kernel subsystem:</p><ul><li><strong><code>socket()</code></strong>: Creates a new Socket. This is far more than simply <code>malloc</code> a block of memory; behind the scenes, it involves protocol family lookup and initialization.</li><li><strong><code>bind()</code></strong>: Gives the Socket an &quot;identity&quot; (local port and IP address).</li><li><strong><code>send()</code> / <code>recv()</code></strong>: The cornerstones of sending and receiving data.</li><li><strong><code>listen()</code></strong>: Puts the Socket into &quot;listening&quot; mode, ready to accept connection requests. Note that only &quot;phone calls&quot; (TCP) need this; &quot;postcards&quot; (UDP) don&#39;t.</li><li><strong><code>accept()</code></strong>: Pulls a connection from the wait queue and returns a new Socket descriptor. This is the most critical step on the TCP server side.</li><li><strong><code>connect()</code></strong>: Initiates a connection. For TCP, this kicks off the three-way handshake; for UDP, it simply sets a default destination address (writing a fixed recipient on the postcard), making it convenient to use <code>send()</code> directly afterward.</li></ul><p>In the kernel code (<code>net/socket.c</code>), all of these system calls ultimately converge into the <code>socketcall()</code> method for unified dispatch.</p><p>The focus of this book is <strong>kernel network implementation</strong>, not a userspace API usage guide. So we won&#39;t dive into how to write a <code>connect()</code> loop or how to handle <code>EINTR</code> error codes.</p><h2 id="what-is-a-socket-inside-the-kernel" tabindex="-1">What Is a Socket Inside the Kernel? <a class="header-anchor" href="#what-is-a-socket-inside-the-kernel" aria-label="Permalink to &quot;What Is a Socket Inside the Kernel?&quot;">​</a></h2><p>Merely knowing how to call these APIs is a far cry from truly understanding the network subsystem.</p><p>The real trouble hides inside the kernel. When you call <code>socket()</code> in userspace, the kernel doesn&#39;t just create an inode like it does when opening a regular file. Instead, it orchestrates a &quot;split&quot;: it simultaneously creates two things—<strong><code>struct socket</code></strong> and <strong><code>struct sock</code></strong>.</p><p>These two names look like twins, but they belong to entirely different species. Why design it this way? Couldn&#39;t we just merge them?</p><p>If you feel this design is a bit odd, your intuition is correct. Behind it lies the most core layering philosophy of the Linux Network Stack. In the next section, we&#39;ll dive into the source code, put this pair of &quot;twins&quot; on the dissection table, and along the way, take a look at <strong><code>struct msghdr</code></strong>—the &quot;suitcase&quot; for exchanging data between userspace and the kernel.</p>',23)])])}const g=t(n,[["render",r]]);export{p as __pageData,g as default};
