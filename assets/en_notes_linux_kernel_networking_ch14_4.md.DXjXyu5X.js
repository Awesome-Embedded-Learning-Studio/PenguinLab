import{_ as s,o as i,c as a,a as n}from"./chunks/framework.8tiMeCK-.js";const k=JSON.parse(`{"title":"14.4  Managing Network Namespaces: From a God's-Eye View to Hands-On Operations","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch14_4.md","filePath":"en/notes/linux_kernel_networking/ch14_4.md","lastUpdated":null}`),t={name:"en/notes/linux_kernel_networking/ch14_4.md"};function o(l,e,h,p,r,d){return i(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="_14-4-managing-network-namespaces-from-a-god-s-eye-view-to-hands-on-operations" tabindex="-1">14.4 Managing Network Namespaces: From a God&#39;s-Eye View to Hands-On Operations <a class="header-anchor" href="#_14-4-managing-network-namespaces-from-a-god-s-eye-view-to-hands-on-operations" aria-label="Permalink to &quot;14.4  Managing Network Namespaces: From a God&#39;s-Eye View to Hands-On Operations&quot;">​</a></h1><p>In the previous section, we traced the past and present of <code>struct net</code> from the kernel&#39;s perspective. Now, let&#39;s return to user space.</p><p>With the theory in place, it&#39;s time for hands-on practice. We don&#39;t need to write C code to call <code>unshare()</code>, though that would certainly be cool. In 99% of cases, we use the <code>ip netns</code> command provided by the <code>iproute2</code> package. It&#39;s the Swiss Army knife for manipulating network namespaces—handy and intuitive, but with a few hidden pitfalls.</p><p>In this section, we&#39;ll step on every single one of those pitfalls from the command line.</p><hr><h3 id="_4-4-1-creating-and-destroying-more-than-just-adding-a-name" tabindex="-1">4.4.1 Creating and Destroying: More Than Just Adding a Name <a class="header-anchor" href="#_4-4-1-creating-and-destroying-more-than-just-adding-a-name" aria-label="Permalink to &quot;4.4.1  Creating and Destroying: More Than Just Adding a Name&quot;">​</a></h3><p>Creating a network namespace named <code>ns1</code> is deceptively simple:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ns1</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>But there&#39;s a lot more happening behind the scenes than meets the eye. Think of it as a &quot;registering a household&quot; process.</p><p><strong>First</strong>, the system creates a file named <code>ns1</code> under <code>/var/run/netns/</code>. <strong>Immediately after</strong>, <code>ip netns</code> uses the <code>unshare()</code> system call (with the <code>CLONE_NEWNET</code> flag) to tell the kernel to create a new network namespace. <strong>Finally</strong>, and most critically, it bind mounts <code>/proc/self/ns/net</code> (the current process&#39;s network namespace handle) onto the <code>/var/run/netns/ns1</code> file just created.</p><p>Why do it this way? Here&#39;s a useful analogy:</p><blockquote><p><strong>[Analogy Intro]</strong>: You can think of the files under <code>/var/run/netns/</code> as &quot;portal anchors.&quot; The network namespaces in the kernel are data structures floating in memory. Without a file in the filesystem referencing them, the namespace would vanish like a ghost the moment the process that created it exits.</p><p><strong>[Limitation Reveal]</strong>: But the &quot;portal&quot; analogy has a limitation—it implies the file <em>is</em> the namespace. It isn&#39;t. The file is merely a handle (via bind mount) pointing to a kernel object. If you directly delete the <code>/var/run/netns/ns1</code> file (using <code>rm</code> instead of <code>ip netns del</code>), as long as there are still processes alive inside that namespace, the namespace itself won&#39;t be destroyed—you just won&#39;t be able to find the door to get back.</p></blockquote><p>With this &quot;anchor&quot; in place, even if the process that created the namespace exits, we can still &quot;return&quot; to that isolated network environment through this file.</p><p>Of course, network namespaces can be nested. You can create another <code>ns2</code> inside <code>ns1</code>, like Russian nesting dolls, where each layer has its own <code>lo</code> device and routing table.</p><hr><h4 id="destroying-a-namespace" tabindex="-1">Destroying a Namespace <a class="header-anchor" href="#destroying-a-namespace" aria-label="Permalink to &quot;Destroying a Namespace&quot;">​</a></h4><p>Deleting it is just as intuitive:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> del</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ns1</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>But there&#39;s a catch here.</p><p>If there are still living processes inside this namespace (for example, if you left a bash shell open), this command will refuse to execute. Why? Because the kernel won&#39;t tear down a house that still has &quot;residents.&quot;</p><p>Only when it confirms no processes are attached will <code>ip netns del</code> delete the <code>/var/run/netns/ns1</code> file. As the reference count drops to zero, the kernel cleans up the namespace.</p><p><strong>Something interesting happens at this point</strong>: When a namespace is destroyed, all network devices inside it are forcibly &quot;relocated&quot;—moved back to the initial default namespace, <code>init_net</code>. <strong>Exception</strong>: Devices marked with <code>NETIF_F_NETNS_LOCAL</code> (such as the lo device, VXLAN, bridges, etc.) have &quot;local residency&quot; and cannot be relocated; they are destroyed along with the namespace. We&#39;ll dive into this flag later when we discuss <code>ip link set</code>.</p><hr><h3 id="_4-4-2-viewing-monitoring-and-pid-magic-tricks" tabindex="-1">4.4.2 Viewing, Monitoring, and PID Magic Tricks <a class="header-anchor" href="#_4-4-2-viewing-monitoring-and-pid-magic-tricks" aria-label="Permalink to &quot;4.4.2  Viewing, Monitoring, and PID Magic Tricks&quot;">​</a></h3><h4 id="seeing-all-the-worlds" tabindex="-1">Seeing All the Worlds <a class="header-anchor" href="#seeing-all-the-worlds" aria-label="Permalink to &quot;Seeing All the Worlds&quot;">​</a></h4><p>List all namespaces created via <code>ip netns add</code>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The implementation behind this is quite simple: it just lists the filenames under the <code>/var/run/netns/</code> directory. What does this mean? It means if you created a namespace using a method like <code>unshare --net bash</code>, it won&#39;t appear in this list—because this method doesn&#39;t leave an &quot;anchor&quot; under <code>/var/run/netns/</code>. It&#39;s like an invisible space, perceivable only to the current process.</p><h4 id="real-time-monitoring" tabindex="-1">Real-Time Monitoring <a class="header-anchor" href="#real-time-monitoring" aria-label="Permalink to &quot;Real-Time Monitoring&quot;">​</a></h4><p>If you want to watch namespace creation and destruction from another terminal, you can use this:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> monitor</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This is implemented by monitoring the <code>/var/run/netns/</code> directory via the <code>inotify</code> mechanism. When you run <code>ip netns add ns2</code>, the monitoring terminal prints <code>add ns2</code>. When you run <code>ip netns del ns2</code>, it prints <code>delete ns2</code>.</p><p><strong>⚠️ Note</strong>: If you run the monitor before creating any namespaces, you&#39;ll get an error: <code>inotify_add_watch failed: No such file or directory</code> The reason is simple: the <code>/var/run/netns/</code> directory doesn&#39;t exist yet, so <code>inotify</code> has nowhere to attach its hooks.</p><p>Similarly, only operations performed via the <code>ip netns</code> command will trigger the monitor. If you manually write code to <code>unshare()</code>, it won&#39;t show up here—because that directory wasn&#39;t touched.</p><h4 id="hide-and-seek-between-processes-and-namespaces" tabindex="-1">Hide and Seek Between Processes and Namespaces <a class="header-anchor" href="#hide-and-seek-between-processes-and-namespaces" aria-label="Permalink to &quot;Hide and Seek Between Processes and Namespaces&quot;">​</a></h4><p><code>iproute2</code> also gives us two very useful commands for handling the relationship between processes and namespaces.</p><p><strong>1. Finding a Name for a PID</strong> You see a process (say, PID 1234) and want to know which network namespace it&#39;s currently in?</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> identify</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1234</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>What happens behind the scenes: it reads <code>/proc/1234/ns/net</code>, then iterates through the files under <code>/var/run/netns/</code>, comparing inode numbers using the <code>stat()</code> system call. Once it finds a match, it tells you the name. If there&#39;s no match (for instance, if it&#39;s an invisible space created via unshare), the command stays silent.</p><p><strong>2. Finding PIDs for a Name</strong> You want to know who&#39;s hiding inside <code>ns1</code>?</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pids</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ns1</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This is like taking roll call. It reads the inode of <code>/var/run/netns/ns1</code>, then iterates through <code>/proc/&lt;pid&gt;/ns/net</code>, comparing inode numbers one by one. Any matching PIDs get listed.</p><hr><h3 id="_4-4-3-entering-a-namespace-the-magic-of-exec" tabindex="-1">4.4.3 Entering a Namespace: The Magic of Exec <a class="header-anchor" href="#_4-4-3-entering-a-namespace-the-magic-of-exec" aria-label="Permalink to &quot;4.4.3  Entering a Namespace: The Magic of Exec&quot;">​</a></h3><p>Creation is just the first step; the real goal is to get inside and play.</p><p>The most classic operation is opening a shell in <code>ns1</code>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ns1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This <code>exec</code> command is extremely powerful. It essentially does three things:</p><ol><li>Opens the <code>/var/run/netns/ns1</code> file (obtaining the bind mount&#39;s fd).</li><li>Uses the <code>setns()</code> system call to associate the current process with the namespace pointed to by this fd.</li><li><code>fork()</code> + <code>execve()</code> the command you specified (in this case, <code>bash</code>).</li></ol><p>Once that bash starts up, you are inside <code>ns1</code>. If you run <code>ifconfig -a</code> now, you&#39;ll see a very minimalist world:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ns1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ifconfig</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -a</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">lo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">       Link</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> encap:Local</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Loopback</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">         LOOPBACK</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> MTU:65536</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Metric:1</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>(Usually just a lone lo device, and it&#39;s in the DOWN state).</p><hr><h3 id="_4-4-4-moving-devices-you-can-t-take-all-your-luggage" tabindex="-1">4.4.4 Moving Devices: You Can&#39;t Take All Your Luggage <a class="header-anchor" href="#_4-4-4-moving-devices-you-can-t-take-all-your-luggage" aria-label="Permalink to &quot;4.4.4  Moving Devices: You Can&#39;t Take All Your Luggage&quot;">​</a></h3><p>Right now, your <code>ns1</code> is empty, with nothing but lo. That&#39;s boring. We usually need to plug a physical or virtual network interface into it.</p><p>For example, moving <code>eth0</code> into <code>ns1</code>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> link</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> eth0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ns1</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The moment you run this command, your main shell will immediately lose control of <code>eth0</code>—it disappears from your view. You must <code>ip netns exec ns1</code> to look inside and find it over there.</p><p>But there&#39;s a <strong>hard limit</strong> here.</p><p>Remember the <code>net_device</code> structure we mentioned in the previous section? It has a feature flag called <code>NETIF_F_NETNS_LOCAL</code>. If this bit is set, the device has &quot;local residency&quot; and <strong>refuses to be moved</strong>.</p><p>You can check this flag using <code>ethtool</code>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ethtool</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -k</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> eth0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns-local</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">netns-local:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> off</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [fixed]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>If it&#39;s <code>on</code> (like the lo device, vxlan, pppoe, etc.), the kernel will directly return <code>-EINVAL</code> (Invalid argument) when you try to move it.</p><p>Let&#39;s see how the kernel source code rejects this:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* net/core/dev.c */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> dev_change_net_namespace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">dev</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">net</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> char</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">pat</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> err;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        ASSERT_RTNL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        /* Don&#39;t allow namespace local devices to be moved. */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        err </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">EINVAL;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (dev-&gt;features </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> NETIF_F_NETNS_LOCAL)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                goto</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> out;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        /* ... 真正的切换逻辑 ... */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        dev_net_set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(dev, net);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        /* ... */</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">out:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> err;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br></div></div><p>Those are the rules. You want to take it with you? No way.</p><p><strong>An alternative way to move</strong>: Sometimes you might not know the namespace&#39;s name, but you know a process (PID 666) is running in that space. You can also just throw the network interface at that process:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> link</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> eth1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 666</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The kernel will find the namespace where PID 666 resides via <code>get_net_ns_by_pid()</code> and stuff the interface in. This achieves the same result as <code>get_net_ns_by_fd()</code> (finding by name).</p><p><strong>The Wireless Exception</strong>: If you want to move a wireless interface (wlan0) to another namespace, <code>ip link</code> might not be enough. You&#39;ll need to use the <code>iw</code> command, because configuring wireless devices is much more complex than regular Ethernet (involving phy, etc.).</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iw</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> phy</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> phy0</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">pidNumbe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">r</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><hr><h3 id="_4-4-5-bridging-the-void-making-two-worlds-talk" tabindex="-1">4.4.5 Bridging the Void: Making Two Worlds Talk <a class="header-anchor" href="#_4-4-5-bridging-the-void-making-two-worlds-talk" aria-label="Permalink to &quot;4.4.5  Bridging the Void: Making Two Worlds Talk&quot;">​</a></h3><p>Now that we&#39;ve separated the devices, we still need them to communicate. Otherwise, what&#39;s the point of isolation? There are usually two ways for two isolated namespaces to communicate:</p><ol><li><strong>Unix Sockets</strong>: The granddaddy of inter-process communication, ignoring network isolation entirely.</li><li><strong>VETH Pairs</strong>: More like a &quot;cross-world network cable.&quot;</li></ol><p>VETH (Virtual Ethernet) always comes in pairs. The pair acts like two ends of a pipe—data going in one end comes out the other immediately. We can leave one end in <code>init_net</code> (the main world) and throw the other into <code>ns1</code>.</p><p>Suppose we have two empty namespaces, <code>ns1</code> and <code>ns2</code>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 创建空间</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ns1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ns2</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Now let&#39;s create a VETH pair in the current (main) space:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> link</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> if_one</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> type</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> veth</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> peer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> name</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> if_one_peer</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Now the main space has two new devices: <code>if_one</code> and <code>if_one_peer</code>. Next, we throw <code>if_one_peer</code> into <code>ns1</code>:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> link</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> if_one_peer</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> netns</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ns1</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Now, the main space only has <code>if_one</code>, while <code>ns1</code> has <code>if_one_peer</code>. If you then throw <code>if_one</code> into <code>ns2</code>, or configure IPs for <code>if_one</code> and the <code>if_one_peer</code> inside <code>ns1</code> and bring them up, you&#39;ve laid a direct network cable between two namespaces.</p><p>You can use <code>ifconfig</code> or <code>ip addr</code> to configure their IPs, then <code>ping</code> each other. This is the foundation of building container networks.</p><hr><h3 id="_4-4-6-conclusion-but-this-is-just-the-beginning" tabindex="-1">4.4.6 Conclusion: But This Is Just the Beginning <a class="header-anchor" href="#_4-4-6-conclusion-but-this-is-just-the-beginning" aria-label="Permalink to &quot;4.4.6  Conclusion: But This Is Just the Beginning&quot;">​</a></h3><p>In this chapter, we dissected the implementation of network namespaces: from <code>nsproxy</code> in the kernel, to <code>ip netns</code> in user space. We also saw how many compromises and changes the kernel made to achieve all of this: the new <code>CLONE_NEW*</code> flag, new system calls, the pernet_operations callback mechanism...</p><p>These things aren&#39;t magic; they are layers of abstraction stacked up by engineers to solve the age-old problem of &quot;isolation.&quot;</p><p>But this isn&#39;t everything. Namespaces solve the problem of &quot;visibility isolation,&quot; but they don&#39;t solve the problem of &quot;resource contention.&quot; If a process inside a container starts hogging the CPU, the host machine will still freeze. The solution to this problem is the star of the next chapter: <strong>Cgroups</strong>. After that, we&#39;ll see how specific network modules attach themselves to the larger Cgroups framework.</p><p>Are you ready? We&#39;re about to enter the deep waters of resource management.</p>`,90)])])}const g=s(t,[["render",o]]);export{k as __pageData,g as default};
