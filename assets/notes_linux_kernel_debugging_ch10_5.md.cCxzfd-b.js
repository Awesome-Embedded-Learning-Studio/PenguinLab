import{_ as a,o as n,c as i,a as e}from"./chunks/framework.8tiMeCK-.js";const d=JSON.parse('{"title":"10.5  利用内核的 Hung Task 与 Workqueue 停顿检测器","description":"","frontmatter":{},"headers":[],"relativePath":"notes/linux_kernel_debugging/ch10_5.md","filePath":"notes/linux_kernel_debugging/ch10_5.md","lastUpdated":null}'),p={name:"notes/linux_kernel_debugging/ch10_5.md"};function t(l,s,r,o,c,h){return n(),i("div",null,[...s[0]||(s[0]=[e(`<h1 id="_10-5-利用内核的-hung-task-与-workqueue-停顿检测器" tabindex="-1">10.5 利用内核的 Hung Task 与 Workqueue 停顿检测器 <a class="header-anchor" href="#_10-5-利用内核的-hung-task-与-workqueue-停顿检测器" aria-label="Permalink to &quot;10.5  利用内核的 Hung Task 与 Workqueue 停顿检测器&quot;">​</a></h1><p>接上一节，我们刚刚提到了系统可能会出现任务卡死的情况——也就是所谓的「Hung Task」。</p><p>但这里有一个很有意思的细节：内核是怎么知道任务卡住了？毕竟，如果 CPU 都在忙着跑死循环代码，谁还有空来当「裁判」？</p><p>答案藏在一个叫做 <code>khungtaskd</code> 的内核线程里。它是内核专门为了抓现行而养的一条「警犬」，周期性地醒来，四处巡视，看看有没有任务在 <code>D</code> 状态（不可中断睡眠）里待得太久。</p><p>现在，我们要把这个机制彻底扒开看清楚。就像 Fine-tuning 一台赛车一样，内核提供了一整套 sysctl 参数，允许你调整这台检测器的灵敏度和行为。</p><p>假设你在 x86_64 的虚拟机上，我们来看一下这些参数的默认值：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sysctl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -a</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> hung_task</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.hung_task_all_cpu_backtrace</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.hung_task_check_count</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4194304</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.hung_task_check_interval_secs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.hung_task_panic</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.hung_task_timeout_secs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 120</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.hung_task_warnings</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 10</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>这一串输出背后，每一个开关都值得深究。我们逐个拆解，看看它们怎么决定系统的生死。</p><hr><h3 id="阈值与代价-配置-hung-task-检测器" tabindex="-1">阈值与代价：配置 Hung Task 检测器 <a class="header-anchor" href="#阈值与代价-配置-hung-task-检测器" aria-label="Permalink to &quot;阈值与代价：配置 Hung Task 检测器&quot;">​</a></h3><p>以下这些参数都依赖于内核配置项 <code>CONFIG_DETECT_HUNG_TASK</code> 被开启。如果你在嵌入式设备上跑内核，这一节的内容特别关键——因为资源有限，你得在「检测准确性」和「系统开销」之间做取舍。</p><h4 id="hung-task-timeout-secs" tabindex="-1"><code>hung_task_timeout_secs</code> <a class="header-anchor" href="#hung-task-timeout-secs" aria-label="Permalink to &quot;\`hung_task_timeout_secs\`&quot;">​</a></h4><p>这是最核心的一个参数：<strong>判定「卡死」的时间标准</strong>。</p><p>当一个任务处于不可中断睡眠（Uninterruptible Sleep，也就是你在 <code>ps</code> 命令里看到的 <code>D</code> 状态）的时间超过这个秒数，内核就会判定这是一个 Hung Task，并触发警告。</p><p>默认值是 <strong>120 秒</strong>。</p><p>这是一个「定性」的参数。如果你的系统对响应时间要求极高，比如某个实时任务必须在 10ms 内响应，那你显然不能等 120 秒才报警。你可以把它调小，甚至调到 1 秒。但要注意，把调得太低可能会导致正常的 I/O 等待被误报为卡死。</p><p>它的合法范围是 <code>{0:LONG_MAX/HZ}</code>。</p><h4 id="hung-task-warnings" tabindex="-1"><code>hung_task_warnings</code> <a class="header-anchor" href="#hung-task-warnings" aria-label="Permalink to &quot;\`hung_task_warnings\`&quot;">​</a></h4><p><strong>警告次数上限</strong>。</p><p>即使检测到了卡死，内核也不一定愿意一直打印日志刷屏。这个参数定义了系统最多报告几次警告。默认值是 <strong>10</strong>。每次检测到卡死任务，这个计数器就减 1。</p><p>当它减到 0 时，内核就会闭嘴——即使系统里还有任务卡着，它也不再打印新警告了。</p><p>这就像汽车的故障灯，闪了几下之后为了不让你心烦就自动灭了。但这有一个隐患：如果是持续性的死锁，你可能只看到开头的几次日志，后面就以为系统安静了。</p><p>如果你不想让它闭嘴，可以把它设为 <strong>-1</strong>，表示允许无限次警告。</p><h4 id="hung-task-panic" tabindex="-1"><code>hung_task_panic</code> <a class="header-anchor" href="#hung-task-panic" aria-label="Permalink to &quot;\`hung_task_panic\`&quot;">​</a></h4><p><strong>是否由「警告」升级为「处决」</strong>。</p><p>默认值是 <strong>0</strong>。这意味着当检测到 Hung Task 时，内核仅仅打印一条报警信息（<code>KERN_WARNING</code>），然后让该任务继续在 <code>D</code> 状态里待着。</p><p>但如果你把它设为 <strong>1</strong>，性质就变了：一旦发现 Hung Task，内核会立刻调用 <code>panic()</code>，直接重启或停机。</p><p>这个开关通常用于高可用性（HA）集群环境：与其让服务器半死不活地卡着，不如直接重启让它恢复服务。</p><h4 id="hung-task-check-count" tabindex="-1"><code>hung_task_check_count</code> <a class="header-anchor" href="#hung-task-check-count" aria-label="Permalink to &quot;\`hung_task_check_count\`&quot;">​</a></h4><p><strong>检测范围的上限</strong>。</p><p><code>khungtaskd</code> 在扫描的时候，不会真的去遍历几万个任务——那太慢了。它只检查这个参数指定数量的任务。</p><p>这其实是一个性能优化的手段。在资源受限的嵌入式系统上，遍历任务链表本身就是一个重活。这个值允许你限制检测器的工作量。</p><p>有意思的是，这个值是架构相关的。</p><ul><li>在我的 x86_64 虚拟机上，它是 <strong>4,194,304</strong>（约 420 万）。</li><li>但在树莓派（ARM-32）上，它可能只有 <strong>32,768</strong>。</li></ul><p>这反映了不同平台对「遍历开销」的不同容忍度。</p><h4 id="hung-task-check-interval-secs" tabindex="-1"><code>hung_task_check_interval_secs</code> <a class="header-anchor" href="#hung-task-check-interval-secs" aria-label="Permalink to &quot;\`hung_task_check_interval_secs\`&quot;">​</a></h4><p><strong>检测频率</strong>。</p><p>通常这个值是 <strong>0</strong>。这意味着检测的间隔时间是由 <code>hung_task_timeout_secs</code> 决定的——也就是「一旦超时就检测」。</p><p>但如果你把它设为一个正数（比如 5），那么内核会每隔 5 秒强制扫描一次，不管有没有任务超时。这个值会覆盖掉 <code>timeout</code> 的逻辑。</p><p>合法范围是 <code>{0:LONG_MAX/HZ}</code>。一般来说，保持默认的 0 就行，让超时逻辑自然触发更合理。</p><h4 id="hung-task-all-cpu-backtrace" tabindex="-1"><code>hung_task_all_cpu_backtrace</code> <a class="header-anchor" href="#hung-task-all-cpu-backtrace" aria-label="Permalink to &quot;\`hung_task_all_cpu_backtrace\`&quot;">​</a></h4><p><strong>全场照相功能</strong>。</p><p>默认值是 <strong>0</strong>。当设为 <strong>1</strong> 时，一旦检测到 Hung Task，内核会向所有 CPU 核心发送 NMI（不可屏蔽中断），强制每颗 CPU 都打印一遍当前的堆栈回溯（stack backtrace）。</p><p>这非常暴力，但也极其有用。</p><p>想象一下，某个线程死锁了，但你不知道是谁占了锁。开启这个选项后，你不仅能看到受害者（Hung Task）的堆栈，还能看到所有其他 CPU 正在干嘛——很可能其中一个 CPU 正抓着锁不放。</p><p>这需要 <code>CONFIG_SMP</code> 和 <code>CONFIG_TRACE_IRQFLAGS_SUPPORT</code> 支持。</p><hr><h3 id="更隐蔽的死锁-workqueue-停顿检测" tabindex="-1">更隐蔽的死锁：Workqueue 停顿检测 <a class="header-anchor" href="#更隐蔽的死锁-workqueue-停顿检测" aria-label="Permalink to &quot;更隐蔽的死锁：Workqueue 停顿检测&quot;">​</a></h3><p>解决了任务卡死的问题，是不是就万事大吉了？</p><p>未必。</p><p>内核里还有一个重灾区：<strong>Workqueue（工作队列）</strong>。驱动开发者最喜欢用它来把耗时工作推迟到进程上下文里执行。内核内部维护了一堆内核线程（Worker Threads）来默默消化这些工作项。</p><p>问题来了：如果你提交的一个工作项一直躺在队列里不被执行，或者执行得太慢，这算不算故障？</p><p><strong>算，而且很致命。</strong> 这可能导致存储设备响应超时、网络丢包，或者你看着风扇狂转但系统毫无反应。</p><p>为了抓这种「怠工」行为，内核提供了 <strong>Workqueue Stall Detection</strong>（工作队列停顿检测）。</p><h4 id="启用检测-config-wq-watchdog" tabindex="-1">启用检测：CONFIG_WQ_WATCHDOG <a class="header-anchor" href="#启用检测-config-wq-watchdog" aria-label="Permalink to &quot;启用检测：CONFIG_WQ_WATCHDOG&quot;">​</a></h4><p>这需要在编译内核时开启配置项：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CONFIG_WQ_WATCHDOG = y</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>你可以在 <code>make menuconfig</code> 里找到它： <code>Kernel hacking -&gt; Debug Oops, Lockups and Hangs -&gt; Detect Workqueue Stalls</code></p><p>一旦开启，如果某个工作队列的线程池在处理工作项时卡住了，或者进度慢得离谱，内核就会打印 <code>KERN_WARN</code> 级别的报警，并附上工作队列的内部状态信息。</p><h4 id="阈值-workqueue-watchdog-thresh" tabindex="-1">阈值：workqueue.watchdog_thresh <a class="header-anchor" href="#阈值-workqueue-watchdog-thresh" aria-label="Permalink to &quot;阈值：workqueue.watchdog_thresh&quot;">​</a></h4><p>这个超时阈值默认是 <strong>30 秒</strong>。</p><p>它由内核启动参数 <code>workqueue.watchdog_thresh</code> 控制，也可以通过对应的 sysfs 文件动态修改。</p><p>如果某个工作项在这个时间里还没被执行完，就会触发报警。</p><p>如果你想关闭这个检测（比如你确实有个合法的长耗时任务），把这个值设为 <strong>0</strong> 即可。</p><hr><h3 id="故意搞破坏-实测-workqueue-stall" tabindex="-1">故意搞破坏：实测 Workqueue Stall <a class="header-anchor" href="#故意搞破坏-实测-workqueue-stall" aria-label="Permalink to &quot;故意搞破坏：实测 Workqueue Stall&quot;">​</a></h3><p>光说不练假把式。让我们亲手制造一次 Workqueue Stall，看看系统是怎么反应的。</p><p>⚠️ <strong>实验警告</strong><strong>千万别在生产环境上跑这一段代码！</strong><strong>这会让你的某个 CPU 核心瞬间 100% 也就是死循环，可能会直接把系统搞卡死。</strong><strong>请务必在有多核 CPU 的测试机上做实验，至少留一个核能维持系统响应。</strong></p><p>我们准备了一个简单的内核模块，核心逻辑就在工作队列的处理函数里。</p><p>代码逻辑很简单：我们在默认的内核工作队列（<code>system_wq</code>）里提交一个任务，然后在这个任务里故意写一个死循环，把 CPU 吃死。</p><p>来看这段代码（<code>ch10/workq_stall/workq_stall.c</code>）：</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* [ ... 省略头文件包含 ... ] */</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> workq_func</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> work_struct </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">work</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    pr_info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">: workqueue handler start</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, KBUILD_MODNAME);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 正常工作完成后，故意卡住 CPU */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    mdelay</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 模拟一点正常工作耗时</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* --- 下面是 BUG 代码 --- */</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 这是一个死循环，专门用来触发 Workqueue Stall */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        cpu_relax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 空转，吃死 CPU</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* --- BUG 代码结束 --- */</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    pr_info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">: workqueue handler done</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, KBUILD_MODNAME);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* [ ... 模块初始化代码，提交工作 ... ] */</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br></div></div><p>这里的关键是那个 <code>while(1)</code> 循环。 <code>cpu_relax()</code> 只是告诉 CPU 这里是个忙等待，但在没有其他更高优先级线程时，这个核就会一直在这里空转，永远跑不到函数结尾，也永远处理不了队列里的下一个任务。</p><p>把这个模块 <code>insmod</code> 进去，几秒钟内（取决于你的 <code>watchdog_thresh</code> 设置），你的控制台就会像疯了一样输出红色的报警信息：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BUG: workqueue lockup - pool cpu0, time=30024ms, last=42s ago!</span></span>
<span class="line"><span>...</span></span>
<span class="line"><span>WARNING: CPU: 0 PID: 1234 at kernel/workqueue.c:5806 check_flush_dependency+0x...</span></span>
<span class="line"><span>...</span></span>
<span class="line"><span>Workqueue: events workq_func</span></span>
<span class="line"><span>RIP: 0010:check_flush_dependency+0...</span></span>
<span class="line"><span>...</span></span>
<span class="line"><span>Call Trace:</span></span>
<span class="line"><span> check_flush_dependency</span></span>
<span class="line"><span> process_one_work</span></span>
<span class="line"><span> worker_thread</span></span>
<span class="line"><span> ...</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><p>这时候，如果你在另一个终端打开 <code>top</code> 或 <code>htop</code>，你会看到一个名为 <code>kworker/0:1</code> 之类的内核线程占用了 <strong>100%</strong> 的 CPU。</p><p>这正是我们预期的结果：</p><ol><li>我们把工作提交给了系统默认的工作队列（events）。</li><li>工作队列的某个内核线程（kworker）接手了这项工作。</li><li>工作函数死循环，导致这个 kworker 线程卡死。</li><li>内核的 Watchdog 检测到这个 kworker 陷入了「停顿」状态。</li><li>内核愤怒地打印出堆栈，告诉你到底是谁在捣乱（<code>workq_func</code>）。</li></ol><p>这就是 Workqueue Stall Detector 的威力——它能把那种「线程还在但就是不干活」的故障给精准定位出来。</p><hr><h3 id="本章回响" tabindex="-1">本章回响 <a class="header-anchor" href="#本章回响" aria-label="Permalink to &quot;本章回响&quot;">​</a></h3><p>这一章，我们终于凑齐了内核故障监控的最后一块拼图。</p><p>从最开始的 <strong>Kernel Panic</strong>（彻底崩溃），到 <strong>Lockup</strong>（CPU 疯跑不响应），再到现在的 <strong>Hung Task</strong>（任务睡死）和 <strong>Workqueue Stall</strong>（工作队列怠工）。我们实际上是在构建一个多维度的监控体系：</p><ul><li><strong>Panic</strong> 解决的是「内核数据结构坏了，没法继续活」的问题；</li><li><strong>Soft/Hard Lockup</strong> 解决的是「CPU 还在转，但逻辑已经卡死」的问题；</li><li><strong>Hung Task</strong> 解决的是「任务在等永远等不到的资源」的问题；</li><li><strong>Workqueue Stall</strong> 解决的是「任务队列堵住了，积压严重」的问题。</li></ul><p>还记得我们在章节引子里提到的那种情况吗？——系统看起来是 Up 的，网络还能 ping 通，但就是完全没响应。 现在的你应该知道，这大概率是 <strong>Soft Lockup</strong> 或者 <strong>Hung Task</strong>，而不是简单的 Panic。你不再是看到一个黑屏就傻眼的人了，你有工具（<code>sysctl</code>、<code>kexec</code>、<code>Magic SysRq</code>）去深入窥探内核死前的最后状态。</p><p>这非常重要。因为只有知道它是怎么死的，你才能在下一次重启前，把那个 Bug 给找出来，修好它，避免它再次发生。</p><p>下一章，我们将把这些监控手段用到极致。我们将把视角从「内核自己出错」转向「外界破坏」——也就是硬件故障、内存位翻转（Bit Flip）以及如何利用 GDB 和 Kdump 这些终极武器，对着内核的尸体进行现场解剖。那将是硬核中的硬核。</p><p>准备好了吗？我们继续。</p><hr><h2 id="练习题" tabindex="-1">练习题 <a class="header-anchor" href="#练习题" aria-label="Permalink to &quot;练习题&quot;">​</a></h2><h3 id="练习-1-understanding" tabindex="-1">练习 1：understanding <a class="header-anchor" href="#练习-1-understanding" aria-label="Permalink to &quot;练习 1：understanding&quot;">​</a></h3><p><strong>题目</strong>：在标准的内核 Panic 输出中，信息 &#39;Kernel panic - not syncing: ...&#39; 包含了 &#39;not syncing&#39; 字样。请问这个短语在内核源码中具体代表了什么操作行为？为什么在致命错误发生时要采取这种行为？</p><details><summary>答案与解析</summary><p><strong>答案</strong>：该短语表示内核故意停止将缓冲区的数据刷新（同步）到磁盘。这是因为当内核发生 Panic 时，系统已处于不稳定状态，尝试进行磁盘 I/O 操作可能会导致数据进一步损坏或错误覆盖。</p><p><strong>解析</strong>：这是一个考察对基础概念理解的问题。根据文中 &#39;Why Is the Phrase &quot;not syncing&quot; in the Kernel Panic Message?&#39; 小节的解释：当系统检测到无法恢复的错误时，内存中的文件系统缓冲区可能包含不一致的数据。如果此时强制执行磁盘同步，可能会破坏文件系统的完整性。&#39;not syncing&#39; 明确告知用户，内核为了保护数据安全，放弃了保存未写入磁盘的数据。</p></details><h3 id="练习-2-application" tabindex="-1">练习 2：application <a class="header-anchor" href="#练习-2-application" aria-label="Permalink to &quot;练习 2：application&quot;">​</a></h3><p><strong>题目</strong>：假设你正在编写一个内核模块，该模块需要在系统崩溃时执行一些紧急处理（如记录特定的硬件状态到特定寄存器），但该处理过程可能包含休眠操作。请问你应该使用 <code>atomic_notifier_chain_register()</code> 还是 <code>blocking_notifier_chain_register()</code> 来注册你的回调函数？请解释原因。</p><details><summary>答案与解析</summary><p><strong>答案</strong>：两者都不应该直接使用，或者需要非常谨慎地处理，但在架构上，Panic 发生时通常要求非阻塞操作。</p><p>更准确地说：如果必须注册到 Panic 链，你必须确保代码不包含休眠操作。因为 Panic 发生时，系统会禁用本地中断并禁止抢占，运行在原子上下文中。虽然 <code>panic_notifier_list</code> 本身是 <code>ATOMIC_NOTIFIER_HEAD</code>，但任何试图休眠的操作都可能导致系统死锁或更严重的错误。</p><p><strong>解析</strong>：本题考察应用场景分析。虽然 &#39;Application&#39; 难度通常鼓励实际编码，但这里是一个关键的陷阱题。</p><ol><li>文中明确指出 <code>panic_notifier_list</code> 是一个 &#39;Atomic&#39; 类型的通知器链（<code>ATOMIC_NOTIFIER_HEAD</code>）。</li><li>文中提到 Atomic 类型的回调运行在原子上下文，不能阻塞。</li><li>文中提到 Panic 函数内部会禁用中断并停止调度。因此，如果回调函数试图休眠，它将永远无法被唤醒，导致系统挂起。</li><li>对于 Panic 处理，最佳实践是快速且非阻塞的。</li></ol></details><h3 id="练习-3-application" tabindex="-1">练习 3：application <a class="header-anchor" href="#练习-3-application" aria-label="Permalink to &quot;练习 3：application&quot;">​</a></h3><p><strong>题目</strong>：假设你的 Linux 服务器在生产环境中偶尔发生内核 Oops，导致服务中断。为了调试，你决定在发生 Oops 时触发 Panic 并重启。同时，为了获得更详细的上下文，你希望在 Panic 时自动打印所有活动任务的回溯和内存信息。请写出需要配置的 <code>/proc/sys/kernel</code> 参数及其对应值的命令（假设使用 shell 命令行）。</p><details><summary>答案与解析</summary><p><strong>答案</strong>：配置 Oops 触发 Panic: <code>echo 1 &gt; /proc/sys/kernel/panic_on_oops</code></p><p>配置 Panic 打印任务和内存信息 (panic_print 掩码计算): <code>echo 17 &gt; /proc/sys/kernel/panic_print</code> (或者 0x11)</p><p><strong>解析</strong>：这是一道实际应用题。</p><ol><li><strong>触发 Panic</strong>: 根据知识点 <code>panic_on_oops</code>，将其设置为 1 可以让 Oops 升级为 Panic。</li><li><strong>打印信息</strong>: 根据文中 Table 10.2 (<code>panic_print</code>): <ul><li>显示任务状态 (Show all tasks info): bit 4 (值为 16)</li><li>显示内存信息 (Show memory info): bit 0 (值为 1)</li><li>为了同时获得这两个信息，需要进行按位或运算：16 | 1 = 17。</li></ul></li><li>组合起来就是设置这两个参数。</li></ol></details><h3 id="练习-4-thinking" tabindex="-1">练习 4：thinking <a class="header-anchor" href="#练习-4-thinking" aria-label="Permalink to &quot;练习 4：thinking&quot;">​</a></h3><p><strong>题目</strong>：文中提到了 &#39;Hard Lockup（硬锁）&#39; 和 &#39;Soft Lockup（软锁）&#39; 的区别，主要在于是否禁用中断以及 NMI Watchdog 的作用。请结合 <code>kdump</code> (Dump-capture kernel) 机制，分析以下场景：</p><p>如果 CPU 禁用了中断并陷入死循环，触发 NMI Watchdog 导致 Hard Lockup 警告，此时主内核已经无法正常调度运行。在这种极端情况下，<code>kdump</code> 机制是如何工作的？（注：提示关注 NMI 的作用）</p><details><summary>答案与解析</summary><p><strong>答案</strong>：即使主内核因中断被禁用而死循环（Hard Lockup），NMI（不可屏蔽中断）仍然具有比普通中断更高的优先级，能够强制打断 CPU 的执行流。NMI Watchdog 正是利用 NMI 来检测硬锁的。当配置了 <code>kdump</code> 时，触发 Panic 的路径（即使是 NMI 触发的 Panic）最终会执行 <code>kexec</code> 机制，利用 CPU 的响应在 NMI 上下文中强制加载并启动到捕获内核，从而保留主内核崩溃时的内存镜像。</p><p><strong>解析</strong>：这是一道深度思考题，需要综合 Hard Lockup、NMI 和 Kdump 的知识。</p><ol><li><strong>Hard Lockup 的本质</strong>: CPU 处于死循环且中断被禁用，普通中断（包括时钟中断）无法执行，因此无法进行任务调度，普通软件看门狗无法工作。</li><li><strong>NMI 的特殊性</strong>: NMI 是不可屏蔽中断，即使 CPU 标志寄存器中关闭了中断（IF=0），NMI 依然会被处理器响应。NMI Watchdog 利用性能计数器产生 NMI。</li><li><strong>Kdump 的介入</strong>: 当 NMI 触发 Panic 例程后，虽然主内核逻辑已死，但 Panic 处理流程（特别是 <code>kexec</code> 加载部分）利用了 NMI 的上下文或者 CPU 仍能执行低级指令的特性，将控制权移交给了预加载的捕获内核。这使得系统能越过死锁的主内核，完成内存转储。</li></ol></details><hr><h2 id="要点提炼" tabindex="-1">要点提炼 <a class="header-anchor" href="#要点提炼" aria-label="Permalink to &quot;要点提炼&quot;">​</a></h2><p>本章核心聚焦于建立内核故障的「死后验尸」与实时监控能力。首先， Panic 是内核遇到无法修复错误时的彻底放弃行为，系统会通过 <code>panic()</code> 函数停止调度并进入死循环，期间不仅会打印包含寄存器、堆栈和 KASLR 偏移量的最后遗言，还可能触发键盘灯闪烁（<code>panic_blink</code>）等视觉警报，且内核参数 <code>panic</code> 和 <code>panic_print</code> 可分别控制其重启时机和详细信息的输出粒度。</p><p>为了在系统死机时获取关键诊断信息，通过 <code>netconsole</code> 结合网络接收端（如 netcat）来远程捕获日志是标准做法，这解决了本地终端因系统僵死无法输出日志的问题。此外，利用 Magic SysRq 机制（通过 <code>/proc/sysrq-trigger</code> 或组合键）可以在系统无响应时强制触发崩溃或执行紧急操作（如同步磁盘、转储任务信息），是调试死锁或假死状态的强力后门。</p><p>针对更隐蔽的系统假死，内核提供了基于 NMI 和看门狗的 Lockup 检测机制。 Soft Lockup 指任务在内核态死循环导致其他进程无法调度（默认 20 秒触发），而 Hard Lockup 则涉及中断被关闭的极端死循环（默认 10 秒触发），这两者均可通过内核参数配置为直接触发 Panic 或仅打印警告，帮助开发者区分是调度器失效还是中断被屏蔽。</p><p>除了 CPU 层面的死锁，Hung Task 检测器专门负责发现陷入不可中断睡眠（D 状态）过久的任务（默认 120 秒），通常由等待 I/O 或锁资源导致。与此同时，Workqueue 停顿检测机制则用于监控内核工作队列中的任务是否被无限期延迟执行，防止驱动或子系统利用工作队列推迟的任务因死循环而阻塞整个队列的运行。</p><p>开发者还可以通过注册 <code>panic_notifier_list</code> 原子通知链，在内核崩溃的瞬间插入自定义的处理逻辑（如点亮故障灯或记录关键硬件状态）。但需要注意的是，这种 Panic 处理回调运行在极不稳定的原子上下文中，严禁执行任何可能引起睡眠或阻塞的操作，否则可能导致系统连崩溃转储都无法生成。</p>`,110)])])}const u=a(p,[["render",t]]);export{d as __pageData,u as default};
