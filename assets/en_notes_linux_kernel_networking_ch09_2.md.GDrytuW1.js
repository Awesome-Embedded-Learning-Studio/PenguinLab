import{_ as s,o as i,c as t,a as n}from"./chunks/framework.8tiMeCK-.js";const k=JSON.parse('{"title":"9.2  Netfilter Hooks","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch09_2.md","filePath":"en/notes/linux_kernel_networking/ch09_2.md","lastUpdated":null}'),a={name:"en/notes/linux_kernel_networking/ch09_2.md"};function o(r,e,l,h,p,c){return i(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="_9-2-netfilter-hooks" tabindex="-1">9.2 Netfilter Hooks <a class="header-anchor" href="#_9-2-netfilter-hooks" aria-label="Permalink to &quot;9.2  Netfilter Hooks&quot;">​</a></h1><p>Let&#39;s turn our focus back inside the kernel Network Stack.</p><p>In the previous section, we mentioned that the core of Netfilter is a &quot;vast net&quot; spread across the network stack. Now we need to see exactly which key stakes this net is hung on.</p><p>When we discussed the send and receive paths of IPv4 and IPv6, we actually passed by these stakes multiple times—we were just moving too fast at the time to stop and take a closer look. Netfilter defines five key hook points in the network stack. Whether for IPv4 or IPv6, the names and meanings of these hook points are unified. They form the universal skeleton for the kernel to intercept and modify traffic.</p><p>Below are the exact locations of these five hooks, each corresponding to a specific moment in a packet&#39;s life.</p><h3 id="five-checkpoints-for-packets" tabindex="-1">Five Checkpoints for Packets <a class="header-anchor" href="#five-checkpoints-for-packets" aria-label="Permalink to &quot;Five Checkpoints for Packets&quot;">​</a></h3><p>Imagine a packet as a train speeding along a railway. Netfilter&#39;s hooks are the five checkpoints on that railway.</p><p><code>NF_INET_PRE_ROUTING</code>: This is the <strong>first stop</strong> for all inbound trains.</p><ul><li><strong>Location</strong>: Inside the <code>ip_rcv()</code> method for IPv4 and the <code>ipv6_rcv()</code> method for IPv6.</li><li><strong>Timing</strong>: This is the first hook a packet hits after entering the network stack. At this point, the kernel has just received the packet and hasn&#39;t even looked up the routing table yet.</li><li><strong>Significance</strong>: Intercepting here means you get the first look at a packet before the kernel decides where to send it. If you want to build a &quot;universal&quot; packet catcher, this is the best spot—because whether the packet is destined for the local machine or being forwarded, it must pass through here first.</li></ul><p><code>NF_INET_LOCAL_IN</code>: This is a stop only for trains <strong>destined for the local machine</strong>.</p><ul><li><strong>Location</strong>: Inside the <code>ip_local_deliver()</code> method for IPv4 and the <code>ip6_input()</code> method for IPv6.</li><li><strong>Prerequisite</strong>: The packet must have first passed through <code>PRE_ROUTING</code> and a routing subsystem lookup (confirming the destination is local).</li><li><strong>Significance</strong>: Only packets truly intended for local upper-layer applications will reach here. If you only care about &quot;things sent to me from the outside,&quot; this is where you should intercept.</li></ul><p><code>NF_INET_FORWARD</code>: This is the dedicated line for <strong>transit trains</strong>.</p><ul><li><strong>Location</strong>: Inside the <code>ip_forward()</code> method for IPv4 and the <code>ip6_forward()</code> method for IPv6.</li><li><strong>Prerequisite</strong>: It also passed through <code>PRE_ROUTING</code> and a routing lookup, but the result is &quot;this packet needs to be forwarded to someone else.&quot;</li><li><strong>Significance</strong>: This is the core path for Linux acting as a router. If you want to control the machine&#39;s forwarding behavior (like a firewall rejecting forwards), this is the mandatory passage.</li></ul><p><code>NF_INET_POST_ROUTING</code>: This is the <strong>last stop</strong> for all <strong>outbound trains</strong>.</p><ul><li><strong>Location</strong>: Inside the <code>ip_output()</code> method for IPv4 and the <code>ip6_finish_output2()</code> method for IPv6.</li><li><strong>Traffic sources</strong>: The situation here is slightly more complex. Two types of trains merge here: <ol><li>Forwarded trains: They just passed through the <code>FORWARD</code> checkpoint.</li><li>Locally generated trains: They just passed through the <code>LOCAL_OUT</code> checkpoint.</li></ol></li><li><strong>Significance</strong>: No matter where the packet came from, as long as it&#39;s about to leave this machine, the last chance to intercept and modify it is right here. Source Network Address Translation (SNAT) typically happens here.</li></ul><p><code>NF_INET_LOCAL_OUT</code>: This is the <strong>starting station</strong> for <strong>locally generated</strong> trains.</p><ul><li><strong>Location</strong>: Inside the <code>__ip_local_out()</code> method for IPv4 and the <code>__ip6_local_out()</code> method for IPv6.</li><li><strong>Timing</strong>: After a locally originating packet from a process goes through a routing lookup to determine which exit to take, but before it actually enters the transmit queue.</li><li><strong>Significance</strong>: This is the first checkpoint for packets sent by local applications. If you want to restrict a local process&#39;s outbound access or mark outgoing packets, this is the starting point.</li></ul><p><strong>Returning to the &quot;train&quot; analogy</strong>: The order of these five checkpoints isn&#39;t random; it&#39;s strictly dictated by the physical connections of the railway.</p><ul><li>Locally sent packets only pass through <code>LOCAL_OUT</code> -&gt; <code>POST_ROUTING</code>.</li><li>Packets destined for the local machine only travel <code>PRE_ROUTING</code> -&gt; <code>LOCAL_IN</code>.</li><li>Forwarded packets have the longest journey: <code>PRE_ROUTING</code> -&gt; <code>FORWARD</code> -&gt; <code>POST_ROUTING</code>. If you wait at <code>LOCAL_IN</code> for a forwarded packet, you&#39;ll wait forever—it&#39;s physically disconnected.</li></ul><hr><h3 id="how-the-kernel-pulls-over-traffic-at-these-points" tabindex="-1">How the Kernel &quot;Pulls Over&quot; Traffic at These Points <a class="header-anchor" href="#how-the-kernel-pulls-over-traffic-at-these-points" aria-label="Permalink to &quot;How the Kernel &quot;Pulls Over&quot; Traffic at These Points&quot;">​</a></h3><p>Conceptual checkpoints alone aren&#39;t enough; the kernel needs a mechanism to actually attach code at these points. This mechanism is the <code>NF_HOOK</code> macro we mentioned in earlier chapters.</p><p>This macro is hardcoded into the critical paths of the network stack (that is, inside those <code>ip_rcv</code>, <code>ip_forward</code>, and other functions mentioned above). Its definition is located at <code>include/linux/netfilter.h</code>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> inline</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> NF_HOOK</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">uint8_t</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> pf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> hook</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">skb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                  struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">out</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                  int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">okfn)(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> NF_HOOK_THRESH</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(pf, hook, skb, in, out, okfn, INT_MIN);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>These few short lines of code are actually the bridge connecting the Network Stack and the Netfilter subsystem. Let&#39;s look at its parameters, each carrying critical information:</p><ul><li><strong>pf</strong>: This asks &quot;which protocol family?&quot; It&#39;s usually <code>NFPROTO_IPV4</code> or <code>NFPROTO_IPV6</code>. Netfilter needs to know this because while the processing logic for different protocols shares commonalities, the details differ.</li><li><strong>hook</strong>: This is one of the five points we discussed above (such as <code>NF_INET_PRE_ROUTING</code>). It tells the kernel &quot;which checkpoint to hang the code on right now.&quot;</li><li><strong>skb</strong>: Needless to say, this is the &quot;suspect&quot; being escorted under guard (the packet).</li><li><strong>in</strong>: The inbound network device. If the packet came from the outside, this is the <code>net_device</code> structure corresponding to that network interface.</li><li><strong>out</strong>: The outbound network device. <strong>Note a common pitfall here</strong>: At certain stages (like <code>PRE_ROUTING</code>), the kernel hasn&#39;t made a routing decision yet and has no idea which interface the packet will leave from, so this parameter will be <code>NULL</code>. Never try to dereference it at this point, or you&#39;ll trigger a direct panic.</li><li><strong>okfn</strong>: This is a &quot;if all goes well, where to next&quot; callback function pointer. If all hooks tell this packet &quot;let it pass (NF_ACCEPT),&quot; the kernel will call this function, allowing the packet to continue its original journey through the network stack.</li></ul><hr><h3 id="the-verdict-power-of-hook-functions" tabindex="-1">The &quot;Verdict Power&quot; of Hook Functions <a class="header-anchor" href="#the-verdict-power-of-hook-functions" aria-label="Permalink to &quot;The &quot;Verdict Power&quot; of Hook Functions&quot;">​</a></h3><p>When your hook function is called, you hold the packet&#39;s fate in your hands. You must return a &quot;verdict value&quot; telling the kernel what to do next. These return values are defined in <code>include/uapi/linux/netfilter.h</code>:</p><ul><li><strong>NF_DROP (0)</strong>: <strong>Death sentence</strong>. Drop the packet immediately without leaving a trace (aside from your own logs, of course). The sender won&#39;t receive any ICMP error; it&#39;s as if the packet fell into a black hole.</li><li><strong>NF_ACCEPT (1)</strong>: <strong>Let it pass</strong>. This packet is fine, or I&#39;m done processing it, so let it continue on its original path.</li><li><strong>NF_STOLEN (2)</strong>: <strong>Hijack</strong>. This is an interesting verdict. It tells the kernel &quot;forget about this packet, I&#39;ve stolen it.&quot; Subsequent network stack code will never see this packet. This means your module is now fully responsible for handling this packet—either you send it out yourself, or you free it. If you steal a packet but don&#39;t free it, you&#39;ll cause a memory leak.</li><li><strong>NF_QUEUE (3)</strong>: <strong>Hand off to a third party</strong>. Stuff the packet into a queue and pass it to a userspace process for handling. This is the implementation foundation of the <code>nfqueue</code> mechanism, allowing userspace programs (like <code>libnetfilter_queue</code>) to decide the packet&#39;s fate.</li><li><strong>NF_REPEAT (4)</strong>: <strong>Try again</strong>. Ask the kernel to call the current hook function again. This is a rare but powerful option, typically used in complex scenarios that require processing the same packet multiple times.</li></ul><hr><h3 id="registration-how-to-hang-your-hook" tabindex="-1">Registration: How to Hang Your Hook <a class="header-anchor" href="#registration-how-to-hang-your-hook" aria-label="Permalink to &quot;Registration: How to Hang Your Hook&quot;">​</a></h3><p>Knowing the checkpoint locations and how to issue a verdict, all that&#39;s left is how to dispatch your &quot;police&quot; to that post.</p><p>To register a hook callback, you need to prepare a <code>nf_hook_ops</code> structure (or an array of them). This structure is your &quot;work order.&quot; Its definition is in <code>include/linux/netfilter.h</code>:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> nf_hook_ops {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> list_head list;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 用户填充以下字段 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    nf_hookfn     </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">hook;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       /* 钩子函数指针 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> module </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">owner;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      /* 拥有此模块的指针（通常是 THIS_MODULE） */</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    u_int8_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      pf;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          /* 协议族 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  hooknum;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     /* 钩子编号 */</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 钩子按优先级升序调用 */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">           priority;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 优先级 */</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><p>There are a few fields here worth discussing in detail, as they directly determine whether your code will work correctly:</p><ul><li><p><strong>hook</strong>: This is a pointer to the actual hook function you wrote. Its prototype must strictly follow the <code>nf_hookfn</code> definition:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> nf_hookfn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">unsigned</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> hooknum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                       struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">skb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                       const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                       const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> net_device </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">out</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                       int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">okfn)(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sk_buff </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Note that even though you might only care about one hook point when registering, the <code>hooknum</code> parameter is still passed into the callback. This allows you to handle multiple hook points with a single function (though usually, for the sake of clarity, we write them separately).</p></li><li><p><strong>pf</strong> and <strong>hooknum</strong>: These two tell the kernel &quot;where to hang the function above.&quot; For example, if you want to monitor IPv4 inbound packets, <code>pf</code> would be <code>NFPROTO_IPV4</code>, and <code>hooknum</code> would be <code>NF_INET_PRE_ROUTING</code>.</p></li><li><p><strong>priority</strong>: This is a very critical mechanism.</p><p>Returning to the &quot;train checkpoint&quot; analogy: multiple departments&#39; police officers might be on duty at a single checkpoint simultaneously. Some check for drugs, some for customs, some for contraband. <strong>Who goes first?</strong> This is what <code>priority</code> determines.</p><p>A smaller numerical value means a higher priority, and it gets called sooner. The kernel provides a set of standard priority constants (such as <code>NF_IP_PRI_FIRST</code>, <code>NF_IP_PRI_CONNTRACK</code>, <code>NF_IP_PRI_NAT_SRC</code>, etc.), defined in <code>include/uapi/linux/netfilter_ipv4.h</code>.</p><p>⚠️ <strong>Be extremely careful not to slip up on the priority.</strong> If your firewall rule&#39;s priority is higher than connection tracking, you might see connection tracking completely fail—because the packet gets dropped or modified before it&#39;s ever recorded.</p></li></ul><p>Once the structure is ready, we can register it with the system. There are two APIs we can use:</p><ul><li><strong><code>nf_register_hook(struct nf_hook_ops *reg)</code></strong>: Register a single hook. Suitable for simple modules.</li><li><strong><code>nf_register_hooks(struct nf_hook_ops *reg, unsigned int n)</code></strong>: Register an array of hooks. The second parameter is the array length. When you need to attach functions across multiple protocol families or multiple hook points, this function saves you the trouble of multiple loop calls and guarantees atomicity—either all succeed, or all fail.</li></ul><p>In the next two sections, we&#39;ll see two actual registration examples. And in the diagram of the following section, we&#39;ll intuitively see how <code>priority</code> determines their execution order when multiple hooks are hung on the same checkpoint.</p><p>Now, let&#39;s write the code and truly insert our logic into the network data flow.</p>`,41)])])}const u=s(a,[["render",o]]);export{k as __pageData,u as default};
