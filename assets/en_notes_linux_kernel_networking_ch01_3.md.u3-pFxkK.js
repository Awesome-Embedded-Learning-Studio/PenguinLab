import{_ as t,o,c as n,a as i}from"./chunks/framework.8tiMeCK-.js";const p=JSON.parse('{"title":"1.3  Linux Kernel Network Development Model","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_networking/ch01_3.md","filePath":"en/notes/linux_kernel_networking/ch01_3.md","lastUpdated":null}'),r={name:"en/notes/linux_kernel_networking/ch01_3.md"};function s(a,e,l,c,d,h){return o(),n("div",null,[...e[0]||(e[0]=[i('<h1 id="_1-3-linux-kernel-network-development-model" tabindex="-1">1.3 Linux Kernel Network Development Model <a class="header-anchor" href="#_1-3-linux-kernel-network-development-model" aria-label="Permalink to &quot;1.3  Linux Kernel Network Development Model&quot;">​</a></h1><p>The networking subsystem is incredibly complex, and it evolves at breakneck speed—fast enough that if you blink, you might miss an API change.</p><p>In the previous section, we discussed devices, buffers, and various subsystems—that was the &quot;static&quot; anatomy. Now it&#39;s time to throw all of that into the &quot;dynamic&quot; fray. Linux kernel development is nothing like writing typical business logic. It follows a collaborative model that may seem archaic, even cumbersome, but is exceptionally efficient in practice.</p><p>For anyone looking to dive deep into kernel networking, understanding this model is just as important as understanding the code. Perhaps even more so.</p><p>Why? Because when you try to fix a tricky bug or adapt your code to the latest kernel, you&#39;ll find that <strong>the code itself is just the tip of the iceberg</strong>. Beneath the surface lie Git trees, mailing lists, patch conventions, and the discerning eyes of maintainers. If you don&#39;t understand these rules, your code could be flawless and still never make it into mainline.</p><h4 id="pre-development-gear-check" tabindex="-1">Pre-Development Gear Check <a class="header-anchor" href="#pre-development-gear-check" aria-label="Permalink to &quot;Pre-Development Gear Check&quot;">​</a></h4><p>Before we begin this game, we need to take inventory of our gear. There are no GUI IDE wizards here—just the command line and a text editor.</p><p>In the world of kernel development, Git isn&#39;t just a tool; it&#39;s the lingua franca.</p><p>Whatever your goal—whether it&#39;s fixing a bug that crashes a network card, submitting a patch that optimizes the <code>sk_buff</code> path, or simply understanding the history of a specific code snippet—you must master Git. Linus Torvalds originally created Git to manage kernel code, so in this domain, there is no more native or powerful tool.</p><p>Often, you&#39;ll need to track down exactly which version introduced a bug, which requires knowing how to use Git bisect. Or you might need to backport a feature to an older kernel version, which requires knowing how to revert and merge patches. Alternatively, if you want to experiment with the latest &quot;bleeding-edge&quot; features, you need to know which Git tree to pull from.</p><p>Here is a checklist of basic survival skills:</p><ul><li><strong>How to apply a patch</strong>: Received a <code>.patch</code> file from someone—how do you apply it to your codebase?</li><li><strong>How to read a patch</strong>: Can you glance at code modifications from others and immediately grasp the intent and potential risks?</li><li><strong>How to locate a problematic patch</strong>: The system suddenly hangs—how do you figure out which commit caused it?</li><li><strong>How to revert a patch</strong>: Once you&#39;ve identified the problem, how do you precisely undo that change without messing up other modifications?</li><li><strong>How to clone a Git tree</strong>: How do you get your hands on the two core repositories, <code>net</code> and <code>net-next</code>?</li><li><strong>How to rebase</strong>: How do you keep your local changes in sync with mainline&#39;s rapid pace?</li></ul><p>If you aren&#39;t familiar with Git yet, I highly recommend Scott Chacon&#39;s <em>Pro Git</em> (it&#39;s free, just search online). It&#39;s not just a tool manual; it&#39;s your passport to this community.</p><h4 id="submitting-patches-a-game-of-etiquette" tabindex="-1">Submitting Patches: A Game of Etiquette <a class="header-anchor" href="#submitting-patches-a-game-of-etiquette" aria-label="Permalink to &quot;Submitting Patches: A Game of Etiquette&quot;">​</a></h4><p>Alright, let&#39;s say you&#39;ve written a cool patch that fixes packet loss when a network card is under full load. Now you want to submit it to mainline. This is where the real challenge begins.</p><p>The kernel community has a strict, almost rigid set of etiquette. This isn&#39;t to make your life difficult, but to maintain efficiency amidst hundreds of emails every day.</p><p>First, your code must pass the <strong>kernel coding style</strong> checks. This isn&#39;t a &quot;suggestion.&quot; If your code uses 4 spaces for indentation instead of tabs, or if your braces are in the wrong place, maintainers won&#39;t even look at it—they&#39;ll bounce it back immediately.</p><p>Second, you need to test. And not just once on your PC. You have to consider different architectures and different configurations.</p><p>Then there&#39;s the submission method. Although a tiny minority of people send patches using Gmail&#39;s web interface (which is genuinely not recommended), the professional approach is to properly configure <code>git send-email</code>. This ensures your patches appear on the mailing list in the correct format (inline, with proper encoding), making it easy for others to quote and reply directly.</p><p>During this process, there are a few scripts you must know—they are the community&#39;s security checkpoints:</p><ul><li><p><strong><code>scripts/checkpatch.pl</code></strong>: This is a Perl script. Run it once before sending your patch. It acts like a strict disciplinarian checking your code style: are there trailing spaces? Do the comments conform to the standard? Even if there&#39;s just one extra space, it will throw an error. Don&#39;t find it annoying—it saves you from a lot of public embarrassment.</p></li><li><p><strong><code>scripts/get_maintainer.pl</code></strong>: This script is absolutely crucial. The kernel is so massive that you likely have no idea who maintains your network card driver. Run this script, and it will analyze the file paths you modified and tell you: &quot;The primary maintainer for this file is A, and the Cc list includes B, C, and D.&quot; If you send your patch to the wrong place, having it sink into oblivion is the best-case scenario; the worst-case is being called out on the mailing list.</p></li></ul><p>Remember, submitting patches requires patience. Even a five-line fix might take several days to be accepted. This is perfectly normal.</p><h4 id="two-worlds-net-and-net-next" tabindex="-1">Two Worlds: net and net-next <a class="header-anchor" href="#two-worlds-net-and-net-next" aria-label="Permalink to &quot;Two Worlds: net and net-next&quot;">​</a></h4><p>Linux networking subsystem development operates in a dual-track world. Understanding this is your first step toward participating.</p><p>All network development, whether it&#39;s patches or discussions (RFCs), primarily converges on a single mailing list:</p><p><strong>netdev mailing list</strong>: <code>netdev@vger.kernel.org</code> This is a high-traffic list. Hundreds of emails a day, mostly patches, code reviews, and debates over new features.</p><p>Behind this list hang two crucial Git repositories:</p><ol><li><p><strong>net (<code>http://git.kernel.org/?p=linux/kernel/git/davem/net.git</code>)</strong>: This is the world of <strong>fixes</strong>. It holds fixes for kernel code that has already been merged into mainline. If you find that a network card doesn&#39;t work on the current 5.x kernel and you fix it, your patch will eventually be merged into this branch and ultimately flow into the current stable kernels.</p></li><li><p><strong>net-next (<code>http://git.kernel.org/?p=linux/kernel/git/davem/net-next.git</code>)</strong>: This is the world of <strong>the future</strong>. The code here is prepared for the next merge window. It&#39;s filled with new features, experimental protocol rewrites, and architectural adjustments. If you&#39;re developing a brand-new network protocol or refactoring the entire TCP stack, this is your target.</p></li></ol><p>These two repositories are managed by the networking subsystem maintainer, David Miller. Periodically, he sends the changes from both trees to Linus via Pull Requests to be merged into mainline.</p><p>There is a very critical timing detail here where many newcomers stumble: the <strong>merge window closure period</strong>.</p><p>When Linus starts merging content from <code>net-next</code> into mainline, the <code>net-next</code> repository is <strong>frozen</strong>. At this point, you cannot submit new patches to <code>net-next</code>. The maintainer will post an announcement on the <code>netdev</code> mailing list: &quot;Merge window is open/closed.&quot; If you send a patch during the closure period, it will either be ignored or returned—because the mainline is undergoing drastic changes, and your patch could instantly create conflicts.</p><h4 id="exceptions-and-branches" tabindex="-1">Exceptions and Branches <a class="header-anchor" href="#exceptions-and-branches" aria-label="Permalink to &quot;Exceptions and Branches&quot;">​</a></h4><p>Although <code>netdev</code> is the home base for network development, not all networking subsystems live here.</p><p>Some subsystems are too large, too specialized, or are maintained by specific companies, so they have their own Git repositories and mailing lists:</p><ul><li><strong>Wireless</strong>: Has its own mailing list and Git tree. However, its final Pull Request still goes to <code>netdev</code>.</li><li><strong>Bluetooth</strong>: Same deal—independently maintained, but ultimately folded into the networking ecosystem.</li><li><strong>IPsec</strong>: No independent mailing list; discussed directly on <code>netdev</code>.</li><li><strong>IEEE 802.15.4 (6LoWPAN)</strong>: Also lacks an independent list.</li></ul><p>So, when you want to modify a specific module, the first thing you should do is use <code>scripts/get_maintainer.pl</code> to confirm exactly who maintains it.</p><h4 id="about-this-book-s-environment" tabindex="-1">About This Book&#39;s Environment <a class="header-anchor" href="#about-this-book-s-environment" aria-label="Permalink to &quot;About This Book&#39;s Environment&quot;">​</a></h4><p>Finally, let&#39;s have a practical talk about the code environment used in this book.</p><p>One of the worst things that can happen in software development is when the code in the book doesn&#39;t match the code on your machine. APIs change, struct fields get renamed, and following along feels like reading gibberish.</p><p>To solve this problem, all code snippets and examples in this book, unless otherwise stated, <strong>are based on kernel version 3.9</strong>.</p><p>This version was released in 2013. It&#39;s old enough that many classic mechanisms had already solidified, yet new enough to include most of the core concepts of the modern network stack (although things like <code>eBPF</code>, which are more modern, hadn&#39;t been born yet—but that&#39;s a story for later).</p><ul><li><strong>Getting the source code</strong>: You can download the tarball from <code>kernel.org</code>, or directly clone the <code>net</code> or <code>net-next</code> trees mentioned earlier using Git, and then run <code>git checkout v3.9</code>.</li><li><strong>Browsing the code</strong>: If you just want to look up a struct definition or function call, I highly recommend <strong>LXR (Linux Cross Reference)</strong>. You can go to <code>lxr.free-electrons.com</code> (or nowadays, <code>lxr.linux.no</code>, etc.) to browse the code online. Click on a variable name, and it takes you to every place it&#39;s referenced—this is an absolute lifesaver when reading complex structures like <code>sk_buff</code>.</li><li><strong>Local LXR</strong>: If you&#39;ve modified the kernel and want to build your own cross-reference index, you can even install an LXR server on your local machine.</li></ul><p>Alright, the gear check is complete, and we have our map. It&#39;s time to dive deep into the code.</p><hr><h3 id="chapter-1-echoes-building-the-foundations-of-network-understanding" tabindex="-1">Chapter 1 Echoes: Building the Foundations of Network Understanding <a class="header-anchor" href="#chapter-1-echoes-building-the-foundations-of-network-understanding" aria-label="Permalink to &quot;Chapter 1 Echoes: Building the Foundations of Network Understanding&quot;">​</a></h3><p>This chapter was just a primer, but we covered quite a bit of territory. Let&#39;s piece these fragments back together into a complete map and see what we&#39;ve actually built.</p><p>On the surface, we were learning the architecture of the Linux networking subsystem. But in essence, we were building an intuition for <strong>&quot;layering&quot;</strong> and <strong>&quot;data flow&quot;</strong>.</p><p>We first saw the concrete projection of the OSI model in Linux—the harsh reality of seven abstract layers translated into C code. We saw how <code>net_device</code>, the &quot;hardware spokesperson,&quot; manages network cards, and how <code>sk_buff</code>, the &quot;universal shipping label,&quot; is passed between the various checkpoints in the kernel.</p><p>If you ask me what the most important takeaway from this chapter is, I&#39;d say: <strong>the kernel network stack isn&#39;t a monolith; it&#39;s an assembly line made up of countless hooks</strong>.</p><ul><li>Starting from the <strong>NAPI</strong> interrupt polling in the hardware driver, packets are picked up.</li><li>They are filtered through the firewall rules of <strong>Netfilter</strong>.</li><li>They pass through the intersections of the <strong>routing subsystem</strong>.</li><li>They go through the <strong>neighbor subsystem</strong> (ARP/NDISC) to find the MAC address of the next hop.</li><li>Finally, they land in a <strong>Socket</strong>, to be caught by a user-space program.</li></ul><p>This chapter pointed out the major stations on this assembly line.</p><p>Don&#39;t forget the two core structures we mentioned at the end of the previous section—<code>net_device</code> and <code>sk_buff</code>. They are the currency of this assembly line. Appendix A lists every line of their definitions in detail; consider it your &quot;dictionary&quot; for when you write drivers later—whenever you encounter an unfamiliar field, just look it up in that chapter.</p><p>Remember those advanced features we mentioned at the end of the previous section? RDMA bypasses the CPU to move memory directly, and Namespaces turn one machine into a thousand virtual networks. These features might seem flashy, but they are still built on top of the foundational assembly line we established in this chapter. Even RDMA needs to register a network card first; even a Namespace needs its own <code>loopback</code> device.</p><p>In the journey ahead, we will no longer just &quot;look&quot; at this assembly line. We&#39;re going to start getting our hands dirty.</p><p>In Chapter 2, we&#39;ll start with <strong>Netlink Sockets</strong>. Why Netlink? Because it&#39;s the <strong>two-way radio</strong> between user space and kernel space. If you want to create virtual network cards at runtime, modify the routing table, or configure a VPN, you have to go through it. It&#39;s the master switch for many advanced network features.</p><p>Are you ready? It&#39;s time to start debugging.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: Understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: Understanding&quot;">​</a></h3><p><strong>Question</strong>: The <code>sk_buff</code> structure is the core data structure used to manage network packets in the Linux kernel network stack. As a packet is passed from L2 (the network device driver layer) to L3 (the network layer, such as IPv4), the driver typically calls the <code>eth_type_trans()</code> method. After this method completes successfully, which part of the packet does the <code>data</code> pointer in the <code>sk_buff</code> structure point to? What is the purpose of this design?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: It points to the L3 (network layer) header, such as the IP header. The purpose is to ensure that <code>skb-&gt;data</code> always points to the header currently being processed by that protocol layer, making it convenient for upper-layer protocols to read data directly.</p><p><strong>Analysis</strong>: Based on the text&#39;s description of The Socket Buffer, when a packet is at L2, <code>skb-&gt;data</code> points to the Ethernet header. The <code>eth_type_trans()</code> method moves the <code>data</code> pointer forward by 14 bytes (ETH_HLEN, the size of the Ethernet header) by calling <code>skb_pull_inline()</code>. This causes the pointer to skip the L2 header and point directly to the L3 header. This design pattern allows the various protocol layers of the kernel network stack (L3, L4) to access their respective headers through a unified interface, without having to manually calculate offsets every time.</p></details><h3 id="exercise-2-application" tabindex="-1">Exercise 2: Application <a class="header-anchor" href="#exercise-2-application" aria-label="Permalink to &quot;Exercise 2: Application&quot;">​</a></h3><p><strong>Question</strong>: Suppose you are developing a driver for an embedded device company. You need to enable promiscuous mode on a network card to support packet capture analysis with <code>tcpdump</code>. However, after running two packet capture programs (<code>tcpdump</code> and <code>wireshark</code>) simultaneously, you manually close one of them and find that the network card is still in promiscuous mode. Based on the text&#39;s description of the Promiscuity counter, explain why the network card didn&#39;t automatically exit promiscuous mode.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Because the promiscuity counter is a counter, not a boolean switch. When multiple packet capture programs are enabled, the counter is greater than 1. Closing one program only decrements the counter by 1; it does not reset it to zero.</p><p><strong>Analysis</strong>: The text explicitly states that the Linux network stack uses a promiscuity counter rather than a simple boolean value to manage promiscuous mode. Every time a sniffer like <code>tcpdump</code> or <code>wireshark</code> starts, the counter increments by 1; when closed, it decrements by 1. The network card only truly exits promiscuous mode when the counter drops to 0. In this scenario, when both programs are running, the counter becomes 2. After closing one, the counter is 1, so the network card remains in promiscuous mode to serve the other program that is still running. This mechanism supports the need for multiple sniffers to run concurrently.</p></details><h3 id="exercise-3-thinking" tabindex="-1">Exercise 3: Thinking <a class="header-anchor" href="#exercise-3-thinking" aria-label="Permalink to &quot;Exercise 3: Thinking&quot;">​</a></h3><p><strong>Question</strong>: NAPI (New API) is a hybrid interrupt/polling mechanism designed to improve network performance under high load. Compare NAPI with the traditional &quot;interrupt-driven&quot; mode and analyze why NAPI can deliver better performance when packet throughput is extremely high.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Under high load, the traditional interrupt mode leads to an &quot;interrupt storm,&quot; consuming massive amounts of CPU resources on context switching rather than efficiently processing data. NAPI switches to a polling mode to process packets in batches, significantly reducing the overhead of interrupt handling and context switching.</p><p><strong>Analysis</strong>: Based on the description of New API (NAPI), older network drivers triggered an interrupt for every single packet received. In high-traffic scenarios, this leads to frequent interrupt requests (an &quot;interrupt storm&quot;), where the CPU spends all its time saving and restoring contexts (context switching), causing throughput to plummet. NAPI&#39;s solution is: under high load, the driver stops generating an interrupt for each packet, and instead lets the kernel periodically poll the driver to retrieve packets in bulk. While this approach sacrifices a tiny bit of latency, it drastically reduces the CPU&#39;s interrupt handling overhead, thereby improving overall data processing capacity.</p></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>The core of the Linux kernel networking subsystem lies in the &quot;iron triangle&quot; processing from L2 to L4, which decouples the physical layer from the application layer and focuses on the high-speed flow, verification, and forwarding of packets. This process is not a simple linear transmission; it is full of complex logic such as routing decisions, firewall filtering, NAT translation, and fragmentation/reassembly. The kernel is essentially a precision factory that repeatedly inspects and modifies packets across the various layers of the protocol stack.</p><p>The kernel abstracts physical network cards into software objects through the <code>net_device</code> structure, cleverly managing multi-process shared resources using internal mechanisms like the promiscuity counter. At the same time, paired with the NAPI (New API) mechanism, it dynamically switches between interrupt-driven and polling modes based on network load, effectively solving the CPU livelock problem caused by frequent context switching in traditional pure interrupt modes under high-concurrency, small-packet scenarios.</p><p><code>sk_buff</code> (SKB) is the sole carrier for packets within the kernel. By maintaining pointers like <code>head</code>, <code>data</code>, and <code>tail</code>, it flexibly handles the stripping and adding of protocol headers. This design allows packets to efficiently &quot;peel off&quot; layer by layer via functions like <code>skb_pull</code> as they are passed from the link layer to the transport layer, ensuring zero-copy high performance when transitioning between different protocol layers.</p><p>Developing and debugging network code requires a deep understanding of the Git dual-track system (<code>net</code> and <code>net-next</code>) and the mailing list collaboration culture. The <code>net</code> branch is responsible for fixing stable kernel issues, while <code>net-next</code> carries new features targeting the next merge window. Mastering script tools like <code>scripts/checkpatch.pl</code> and <code>get_maintainer.pl</code>, and following strict coding style and submission etiquette, are key prerequisites for your code to be accepted by the community and merged into mainline.</p>',73)])])}const m=t(r,[["render",s]]);export{p as __pageData,m as default};
