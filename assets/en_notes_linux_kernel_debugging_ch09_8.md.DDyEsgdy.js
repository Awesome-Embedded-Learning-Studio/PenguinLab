import{_ as t,o as s,c as a,a as i}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse('{"title":"9.8 Ftrace Miscellanea and Lingering Questions (FAQ)","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch09_8.md","filePath":"en/notes/linux_kernel_debugging/ch09_8.md","lastUpdated":null}'),n={name:"en/notes/linux_kernel_debugging/ch09_8.md"};function o(r,e,l,c,d,h){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="_9-8-ftrace-miscellanea-and-lingering-questions-faq" tabindex="-1">9.8 Ftrace Miscellanea and Lingering Questions (FAQ) <a class="header-anchor" href="#_9-8-ftrace-miscellanea-and-lingering-questions-faq" aria-label="Permalink to &quot;9.8 Ftrace Miscellanea and Lingering Questions (FAQ)&quot;">​</a></h1><p>There are a few scattered but crucial topics left to cover regarding ftrace. Rather than just dumping them in a list, let&#39;s use a more intuitive format—an FAQ—to put the final pieces of this puzzle together.</p><p>These are all &quot;wait, why isn&#39;t this working?&quot; moments I&#39;ve personally run into.</p><h3 id="what-exactly-does-that-readme-say" tabindex="-1">What exactly does that README say? <a class="header-anchor" href="#what-exactly-does-that-readme-say" aria-label="Permalink to &quot;What exactly does that README say?&quot;">​</a></h3><p>You might have been looking for a &quot;quick start guide&quot; for ftrace. It&#39;s actually hidden right there in the kernel source tree, mounted directly on your system.</p><p>Just <code>cat</code> it:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /sys/kernel/tracing/README</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This is a mini-HOWTO. You&#39;ll see a large block of text containing all the basic ftrace usage. Don&#39;t expect it to read like a novel, but it is the most authoritative &quot;cheat sheet&quot; you&#39;ll find.</p><h3 id="why-do-my-files-look-different-from-yours" tabindex="-1">Why do my files look different from yours? <a class="header-anchor" href="#why-do-my-files-look-different-from-yours" aria-label="Permalink to &quot;Why do my files look different from yours?&quot;">​</a></h3><p>Sometimes you follow a tutorial, only to find that a certain file doesn&#39;t exist under <code>/sys/kernel/tracing/</code>, or some options simply refuse to appear.</p><p>This is completely normal—don&#39;t doubt your sanity.</p><p>Because <code>tracefs</code> is a pseudo-filesystem that is part of the kernel itself. What you see depends entirely on two things:</p><ol><li><strong>CPU Architecture</strong>: x86_64 is usually the first-class citizen, getting the fullest features and fastest updates. If you&#39;re working on ARM or RISC-V, some advanced features might not have been ported over yet.</li><li><strong>Kernel Version</strong>: This text is based on 5.10.60. If you&#39;re using 4.x or a very new 6.x, the interfaces might have changed.</li></ol><p>If you feel like something is missing, check these two points first.</p><h3 id="i-want-live-data-—can-i-continuously-stream-it-like-a-log" tabindex="-1">I want &quot;live data&quot;—can I continuously stream it like a log? <a class="header-anchor" href="#i-want-live-data-—can-i-continuously-stream-it-like-a-log" aria-label="Permalink to &quot;I want &quot;live data&quot;—can I continuously stream it like a log?&quot;">​</a></h3><p>In the previous section, we mentioned <code>trace</code>, which is a static snapshot file. But if you want to see flowing, &quot;happening-right-now&quot; data, you need to use <code>trace_pipe</code>.</p><p>Think of it like video:</p><ul><li><strong><code>trace</code></strong>: A photograph (static snapshot).</li><li><strong><code>trace_pipe</code></strong>: A live stream.</li></ul><p>You can use <code>tail -f</code> or any custom script to read it:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tail</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /sys/kernel/tracing/trace_pipe</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>And since it&#39;s a &quot;stream,&quot; you can pipe it directly into other tools just like a water hose. For example, if you only want to see traces containing &quot;usb&quot;:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /sys/kernel/tracing/trace_pipe</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> usb</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This is incredibly useful when tracking down sudden storms in a specific subsystem.</p><h3 id="can-i-control-the-ftrace-switch-from-within-my-code" tabindex="-1">Can I control the ftrace switch from within my code? <a class="header-anchor" href="#can-i-control-the-ftrace-switch-from-within-my-code" aria-label="Permalink to &quot;Can I control the ftrace switch from within my code?&quot;">​</a></h3><p>This is a hardcore requirement. Sometimes, in a specific driver function, you want to immediately disable tracing as soon as you detect an anomaly, preventing subsequent junk logs from washing away the crucial information.</p><p>You absolutely can. The kernel provides two APIs for this (note that they are GPL-exported):</p><ul><li><code>tracing_on()</code>: Enable tracing.</li><li><code>tracing_off()</code>: Disable tracing.</li></ul><p>This is essentially the code equivalent of writing <code>1</code> or <code>0</code> to the <code>tracing_on</code> pseudo-file. You can write it in a kernel module like this:</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* 检测到故障点，立马冻结现场 */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tracing_off</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>⚠️ <strong>Warning</strong> There is also a higher-level switch: <code>/proc/sys/kernel/ftrace_enabled</code>. Writing <code>0</code> here shuts down the entire ftrace system. This is a &quot;nuclear&quot; level operation—don&#39;t use it lightly.</p><h3 id="when-the-system-crashes-where-does-my-trace-go" tabindex="-1">When the system crashes, where does my trace go? <a class="header-anchor" href="#when-the-system-crashes-where-does-my-trace-go" aria-label="Permalink to &quot;When the system crashes, where does my trace go?&quot;">​</a></h3><p>This is a pain point, but also one of ftrace&#39;s greatest strengths.</p><p>Imagine this scenario: the system suddenly panics, or triggers an Oops. At this point, you have powerful <code>kdump</code> and <code>crash</code> tools at your disposal, which can help you save a memory snapshot of the kernel crash.</p><p>But—a snapshot only captures the exact moment of &quot;death.&quot; It tells you &quot;it died here,&quot; but not &quot;how it got here.&quot; This is the equivalent of a post-mortem missing the medical history.</p><p>This is where <code>ftrace_dump_on_oops</code> comes in.</p><p>The design philosophy behind this mechanism is very simple: since the system crashed and can&#39;t write to files anymore, we just <strong>dump the contents of the ftrace buffer directly to the console and kernel log</strong>. Because <code>kdump</code> captures these logs as well, you can look at the crash scene post-mortem and simultaneously see its final trajectory before death.</p><p>Enabling it is simple—just write <code>1</code> to a proc file:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /proc/sys/kernel/ftrace_dump_on_oops</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The default is <code>0</code> (disabled).</p><p>If you want it prepped and ready at boot, you can also add it to the kernel boot parameters:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ftrace_dump_on_oops</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Here&#39;s an advanced trick. If you&#39;re on a multi-core system, you might only care about the trace from the CPU that triggered the Oops, without wanting the noise from other CPUs. You can use this parameter:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ftrace_dump_on_oops=orig_cpu</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This will only dump the ftrace buffer of the &quot;offending CPU,&quot; making the output much cleaner.</p><h3 id="what-are-those-latency-measuring-tracers-for" tabindex="-1">What are those latency-measuring tracers for? <a class="header-anchor" href="#what-are-those-latency-measuring-tracers-for" aria-label="Permalink to &quot;What are those latency-measuring tracers for?&quot;">​</a></h3><p>You&#39;ve probably seen a bunch of weird names in <code>available_tracers</code>: <code>irqsoff</code>, <code>preemptoff</code>, <code>wakeup</code>... These are the &quot;special forces&quot; of ftrace, dedicated to measuring system latency.</p><p><strong>Why do we need them?</strong></p><p>Ideally, interrupts and preemption should toggle as fast as lightning. But in reality, driver code might accidentally keep interrupts disabled for too long. Once that exceeds a few dozen microseconds, system real-time performance is ruined—audio stutters, and the network drops packets.</p><p><strong>A few key players:</strong></p><ul><li><strong><code>irqsoff</code></strong>: Specifically catches &quot;bad guys&quot; who keep interrupts disabled for too long. It records the maximum duration hardware interrupts were disabled and tells you exactly which function caused it.</li><li><strong><code>preemptoff</code></strong>: Specifically catches situations where preemption is disabled for too long. This is another source of latency.</li><li><strong><code>preemptirqsoff</code></strong>: The ultimate combination of the above two. As long as the kernel cannot schedule (whether due to disabled interrupts or disabled preemption), it handles it.</li><li><strong><code>wakeup</code> / <code>wakeup_rt</code></strong>: Focuses on scheduling latency. How long is the gap between a task waking up and actually running? This is critical for real-time systems.</li></ul><p><strong>How do we use them?</strong></p><p>Just like normal ftrace, switch over to it:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> irqsoff</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /sys/kernel/tracing/current_tracer</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Then put your system under heavy load. As soon as any code keeps interrupts disabled for too long, ftrace will catch the call stack of the &quot;culprit.&quot;</p><blockquote><p><strong>How long is &quot;too long&quot;?</strong></p><p>For hardware interrupts or kernel preemption, anything <strong>over 10 microseconds</strong> starts warranting attention; if it hits the <strong>millisecond</strong> range, that&#39;s a serious incident.</p></blockquote><p>Many driver bugs are caught exactly this way—you think that spinlock is only held for a few lines of code, but it turns out there&#39;s a hidden <code>memcpy</code> inside, and it&#39;s uncached, directly disabling interrupts for several milliseconds.</p><h3 id="can-i-run-multiple-traces-simultaneously" tabindex="-1">Can I run multiple traces simultaneously? <a class="header-anchor" href="#can-i-run-multiple-traces-simultaneously" aria-label="Permalink to &quot;Can I run multiple traces simultaneously?&quot;">​</a></h3><p>By default, you only have one global buffer. But ftrace has a very cool <strong>Instances</strong> model.</p><p>You can think of the <code>instances</code> directory as a &quot;hypervisor.&quot; Under this directory, you can <code>mkdir</code> as many subdirectories as you want, and each subdirectory is a completely independent ftrace environment.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /sys/kernel/tracing/instances</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> my_test_trace</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Now you can see another complete set of <code>trace</code>, <code>trace_pipe</code>, <code>tracing_on</code>... under <code>/sys/kernel/tracing/instances/my_test_trace/</code>.</p><p>What&#39;s the point of this? It&#39;s incredibly useful. Suppose you want to use <code>function_graph</code> to trace the network stack, while simultaneously using <code>irqsoff</code> to monitor interrupt latency. With only a single global buffer, these two would clash. With instances, you can isolate them and let each do its own job.</p><p>This is the ultimate weapon for tackling complex system issues. Steven Rostedt (the primary author of ftrace) has a great talk specifically about this—highly recommended to look it up.</p>`,63)])])}const g=t(n,[["render",o]]);export{u as __pageData,g as default};
