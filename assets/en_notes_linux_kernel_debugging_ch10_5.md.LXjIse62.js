import{_ as s,o as t,c as n,a}from"./chunks/framework.8tiMeCK-.js";const u=JSON.parse(`{"title":"10.5  Leveraging the Kernel's Hung Task and Workqueue Stall Detectors","description":"","frontmatter":{},"headers":[],"relativePath":"en/notes/linux_kernel_debugging/ch10_5.md","filePath":"en/notes/linux_kernel_debugging/ch10_5.md","lastUpdated":null}`),i={name:"en/notes/linux_kernel_debugging/ch10_5.md"};function o(r,e,l,h,c,p){return t(),n("div",null,[...e[0]||(e[0]=[a(`<h1 id="_10-5-leveraging-the-kernel-s-hung-task-and-workqueue-stall-detectors" tabindex="-1">10.5 Leveraging the Kernel&#39;s Hung Task and Workqueue Stall Detectors <a class="header-anchor" href="#_10-5-leveraging-the-kernel-s-hung-task-and-workqueue-stall-detectors" aria-label="Permalink to &quot;10.5  Leveraging the Kernel&#39;s Hung Task and Workqueue Stall Detectors&quot;">​</a></h1><p>Following up on the previous section, we just mentioned that the system might experience tasks getting stuck — the so-called &quot;Hung Task.&quot;</p><p>But there&#39;s an interesting detail here: how does the kernel know a task is stuck? After all, if the CPU is busy running an infinite loop, who has the time to act as the &quot;referee&quot;?</p><p>The answer lies in a kernel thread called <code>khungtaskd</code>. It&#39;s a &quot;watchdog&quot; the kernel keeps specifically to catch offenders in the act — it wakes up periodically, looks around, and checks if any task has been sitting in the <code>D</code> state (uninterruptible sleep) for too long.</p><p>Now, we&#39;re going to tear this mechanism wide open. Just like fine-tuning a race car, the kernel provides a full set of sysctl parameters that let you adjust this detector&#39;s sensitivity and behavior.</p><p>Assuming you&#39;re on an x86_64 virtual machine, let&#39;s look at the default values for these parameters:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sysctl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -a</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">grep</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> hung_task</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.hung_task_all_cpu_backtrace</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.hung_task_check_count</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4194304</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.hung_task_check_interval_secs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.hung_task_panic</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.hung_task_timeout_secs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 120</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kernel.hung_task_warnings</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 10</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">$</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>Behind this string of output, every switch is worth a deep dive. Let&#39;s break them down one by one and see how they dictate the system&#39;s fate.</p><hr><h3 id="thresholds-and-trade-offs-configuring-the-hung-task-detector" tabindex="-1">Thresholds and Trade-offs: Configuring the Hung Task Detector <a class="header-anchor" href="#thresholds-and-trade-offs-configuring-the-hung-task-detector" aria-label="Permalink to &quot;Thresholds and Trade-offs: Configuring the Hung Task Detector&quot;">​</a></h3><p>All of the following parameters depend on the kernel configuration option <code>CONFIG_DETECT_HUNG_TASK</code> being enabled. If you&#39;re running a kernel on an embedded device, this section is especially critical — with limited resources, you have to strike a balance between &quot;detection accuracy&quot; and &quot;system overhead.&quot;</p><h4 id="hung-task-timeout-secs" tabindex="-1"><code>hung_task_timeout_secs</code> <a class="header-anchor" href="#hung-task-timeout-secs" aria-label="Permalink to &quot;\`hung_task_timeout_secs\`&quot;">​</a></h4><p>This is the most core parameter: <strong>the time standard for determining a &quot;stuck&quot; task</strong>.</p><p>When a task remains in uninterruptible sleep (the <code>D</code> state you see in the <code>ps</code> command) for longer than this number of seconds, the kernel determines it&#39;s a Hung Task and triggers a warning.</p><p>The default value is <strong>120 seconds</strong>.</p><p>This is a &quot;qualitative&quot; parameter. If your system has extremely high response time requirements — for example, if a real-time task must respond within 10ms — you obviously can&#39;t wait 120 seconds to sound the alarm. You can turn it down, even to 1 second. But be careful: setting it too low might cause normal I/O waits to be falsely reported as hangs.</p><p>Its valid range is <code>{0:LONG_MAX/HZ}</code>.</p><h4 id="hung-task-warnings" tabindex="-1"><code>hung_task_warnings</code> <a class="header-anchor" href="#hung-task-warnings" aria-label="Permalink to &quot;\`hung_task_warnings\`&quot;">​</a></h4><p><strong>Maximum number of warnings</strong>.</p><p>Even when a hang is detected, the kernel doesn&#39;t necessarily want to keep spamming logs. This parameter defines the maximum number of warnings the system will report. The default value is <strong>10</strong>. Each time a hung task is detected, this counter decrements by 1.</p><p>When it reaches 0, the kernel goes silent — even if there are still tasks stuck in the system, it won&#39;t print new warnings.</p><p>It&#39;s like a car&#39;s check engine light that flashes a few times and then automatically turns off so it doesn&#39;t annoy you. But this has a hidden danger: in the case of a persistent deadlock, you might only see the first few log entries and then mistakenly assume the system has quieted down.</p><p>If you don&#39;t want it to go silent, you can set it to <strong>-1</strong>, which allows unlimited warnings.</p><h4 id="hung-task-panic" tabindex="-1"><code>hung_task_panic</code> <a class="header-anchor" href="#hung-task-panic" aria-label="Permalink to &quot;\`hung_task_panic\`&quot;">​</a></h4><p><strong>Whether to escalate from &quot;warning&quot; to &quot;execution&quot;</strong>.</p><p>The default value is <strong>0</strong>. This means that when a Hung Task is detected, the kernel merely prints an alarm message (<code>KERN_WARNING</code>) and lets the task continue sitting in the <code>D</code> state.</p><p>But if you set it to <strong>1</strong>, the nature changes: as soon as a Hung Task is discovered, the kernel immediately calls <code>panic()</code> to directly reboot or halt the machine.</p><p>This switch is typically used in High Availability (HA) cluster environments: rather than letting a server linger half-dead, it&#39;s better to just reboot it and restore service.</p><h4 id="hung-task-check-count" tabindex="-1"><code>hung_task_check_count</code> <a class="header-anchor" href="#hung-task-check-count" aria-label="Permalink to &quot;\`hung_task_check_count\`&quot;">​</a></h4><p><strong>Upper limit of the detection scope</strong>.</p><p>When <code>khungtaskd</code> scans, it doesn&#39;t actually iterate over tens of thousands of tasks — that would be too slow. It only checks the number of tasks specified by this parameter.</p><p>This is actually a performance optimization. On resource-constrained embedded systems, traversing the task list is heavy work in itself. This value allows you to limit the detector&#39;s workload.</p><p>Interestingly, this value is architecture-dependent.</p><ul><li>On my x86_64 virtual machine, it&#39;s <strong>4,194,304</strong> (about 4.2 million).</li><li>But on a Raspberry Pi (ARM-32), it might only be <strong>32,768</strong>.</li></ul><p>This reflects different platforms&#39; tolerance for &quot;traversal overhead.&quot;</p><h4 id="hung-task-check-interval-secs" tabindex="-1"><code>hung_task_check_interval_secs</code> <a class="header-anchor" href="#hung-task-check-interval-secs" aria-label="Permalink to &quot;\`hung_task_check_interval_secs\`&quot;">​</a></h4><p><strong>Detection frequency</strong>.</p><p>Usually, this value is <strong>0</strong>. This means the detection interval is determined by <code>hung_task_timeout_secs</code> — that is, &quot;detect as soon as a timeout occurs.&quot;</p><p>But if you set it to a positive number (like 5), the kernel will force a scan every 5 seconds, regardless of whether any task has timed out. This value overrides the logic of <code>timeout</code>.</p><p>The valid range is <code>{0:LONG_MAX/HZ}</code>. Generally, keeping the default of 0 is fine — letting the timeout logic trigger naturally is more reasonable.</p><h4 id="hung-task-all-cpu-backtrace" tabindex="-1"><code>hung_task_all_cpu_backtrace</code> <a class="header-anchor" href="#hung-task-all-cpu-backtrace" aria-label="Permalink to &quot;\`hung_task_all_cpu_backtrace\`&quot;">​</a></h4><p><strong>Full-scene snapshot capability</strong>.</p><p>The default value is <strong>0</strong>. When set to <strong>1</strong>, as soon as a Hung Task is detected, the kernel sends an NMI (Non-Maskable Interrupt) to all CPU cores, forcing each CPU to print its current stack backtrace.</p><p>This is quite aggressive, but also extremely useful.</p><p>Imagine a thread is deadlocked, but you don&#39;t know who&#39;s holding the lock. With this option enabled, you can not only see the victim&#39;s (Hung Task&#39;s) stack, but also see what all the other CPUs are doing — it&#39;s very likely that one of them is clinging to the lock and refusing to let go.</p><p>This requires <code>CONFIG_SMP</code> and <code>CONFIG_TRACE_IRQFLAGS_SUPPORT</code> support.</p><hr><h3 id="more-hidden-deadlocks-workqueue-stall-detection" tabindex="-1">More Hidden Deadlocks: Workqueue Stall Detection <a class="header-anchor" href="#more-hidden-deadlocks-workqueue-stall-detection" aria-label="Permalink to &quot;More Hidden Deadlocks: Workqueue Stall Detection&quot;">​</a></h3><p>Does solving the task hang problem mean we&#39;re all set?</p><p>Not necessarily.</p><p>There&#39;s another trouble spot in the kernel: <strong>Workqueues</strong>. Driver developers love using them to defer time-consuming work to process context. The kernel internally maintains a bunch of kernel threads (Worker Threads) to silently digest these work items.</p><p>The question is: if a work item you submitted just lies in the queue without being executed, or executes too slowly, does that count as a fault?</p><p><strong>Yes, and it can be fatal.</strong> This could lead to storage device response timeouts, network packet loss, or you might watch the fans spin wildly while the system is completely unresponsive.</p><p>To catch this kind of &quot;slacking&quot; behavior, the kernel provides <strong>Workqueue Stall Detection</strong>.</p><h4 id="enabling-detection-config-wq-watchdog" tabindex="-1">Enabling Detection: CONFIG_WQ_WATCHDOG <a class="header-anchor" href="#enabling-detection-config-wq-watchdog" aria-label="Permalink to &quot;Enabling Detection: CONFIG_WQ_WATCHDOG&quot;">​</a></h4><p>This requires enabling the configuration option when compiling the kernel:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>CONFIG_WQ_WATCHDOG = y</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>You can find it under <code>make menuconfig</code>: <code>Kernel hacking -&gt; Debug Oops, Lockups and Hangs -&gt; Detect Workqueue Stalls</code></p><p>Once enabled, if a work queue&#39;s thread pool gets stuck while processing a work item, or if progress is absurdly slow, the kernel will print a <code>KERN_WARN</code> level alarm, accompanied by the work queue&#39;s internal state information.</p><h4 id="threshold-workqueue-watchdog-thresh" tabindex="-1">Threshold: workqueue.watchdog_thresh <a class="header-anchor" href="#threshold-workqueue-watchdog-thresh" aria-label="Permalink to &quot;Threshold: workqueue.watchdog_thresh&quot;">​</a></h4><p>This timeout threshold defaults to <strong>30 seconds</strong>.</p><p>It&#39;s controlled by the kernel boot parameter <code>workqueue.watchdog_thresh</code>, and can also be dynamically modified through the corresponding sysfs file.</p><p>If a work item hasn&#39;t finished executing within this time, an alarm will be triggered.</p><p>If you want to disable this detection (for example, if you legitimately have a long-running task), just set this value to <strong>0</strong>.</p><hr><h3 id="intentionally-breaking-things-testing-workqueue-stall-in-practice" tabindex="-1">Intentionally Breaking Things: Testing Workqueue Stall in Practice <a class="header-anchor" href="#intentionally-breaking-things-testing-workqueue-stall-in-practice" aria-label="Permalink to &quot;Intentionally Breaking Things: Testing Workqueue Stall in Practice&quot;">​</a></h3><p>Talk is cheap. Let&#39;s manually trigger a Workqueue Stall and see how the system reacts.</p><p>⚠️ <strong>Experiment Warning</strong><strong>Never run this code in a production environment!</strong><strong>This will instantly push one of your CPU cores to 100% in an infinite loop, and could directly freeze the system.</strong><strong>Make sure to run this experiment on a test machine with a multi-core CPU, leaving at least one core free to maintain system responsiveness.</strong></p><p>We&#39;ve prepared a simple kernel module, with the core logic right in the work queue&#39;s handler function.</p><p>The code logic is simple: we submit a task to the default kernel work queue (<code>system_wq</code>), and then intentionally write an infinite loop inside this task to max out the CPU.</p><p>Let&#39;s look at this code (<code>ch10/workq_stall/workq_stall.c</code>):</p><div class="language-c vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* [ ... 省略头文件包含 ... ] */</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> workq_func</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> work_struct </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">work</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    pr_info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">: workqueue handler start</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, KBUILD_MODNAME);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 正常工作完成后，故意卡住 CPU */</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    mdelay</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 模拟一点正常工作耗时</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    </span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* --- 下面是 BUG 代码 --- */</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* 这是一个死循环，专门用来触发 Workqueue Stall */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    while</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        cpu_relax</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // 空转，吃死 CPU</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* --- BUG 代码结束 --- */</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    pr_info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">: workqueue handler done</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, KBUILD_MODNAME);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* [ ... 模块初始化代码，提交工作 ... ] */</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br></div></div><p>The key here is that <code>while(1)</code> loop. <code>cpu_relax()</code> merely tells the CPU this is a busy wait, but when there are no higher-priority threads, this core will just keep spinning here idly, never reaching the end of the function, and never processing the next item in the queue.</p><p>After you <code>insmod</code> this module, within a few seconds (depending on your <code>watchdog_thresh</code> setting), your console will start outputting red alarm messages like crazy:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BUG: workqueue lockup - pool cpu0, time=30024ms, last=42s ago!</span></span>
<span class="line"><span>...</span></span>
<span class="line"><span>WARNING: CPU: 0 PID: 1234 at kernel/workqueue.c:5806 check_flush_dependency+0x...</span></span>
<span class="line"><span>...</span></span>
<span class="line"><span>Workqueue: events workq_func</span></span>
<span class="line"><span>RIP: 0010:check_flush_dependency+0...</span></span>
<span class="line"><span>...</span></span>
<span class="line"><span>Call Trace:</span></span>
<span class="line"><span> check_flush_dependency</span></span>
<span class="line"><span> process_one_work</span></span>
<span class="line"><span> worker_thread</span></span>
<span class="line"><span> ...</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br></div></div><p>At this point, if you open <code>top</code> or <code>htop</code> in another terminal, you&#39;ll see a kernel thread named something like <code>kworker/0:1</code> hogging <strong>100%</strong> of a CPU.</p><p>This is exactly the result we expected:</p><ol><li>We submitted work to the system&#39;s default work queue (events).</li><li>One of the work queue&#39;s kernel threads (kworker) took on this work.</li><li>The work function entered an infinite loop, causing this kworker thread to get stuck.</li><li>The kernel&#39;s Watchdog detected that this kworker had entered a &quot;stalled&quot; state.</li><li>The kernel angrily printed the stack trace, telling you exactly who&#39;s causing trouble (<code>workq_func</code>).</li></ol><p>This is the power of the Workqueue Stall Detector — it can precisely pinpoint faults where &quot;the thread is still alive but just isn&#39;t doing any work.&quot;</p><hr><h3 id="chapter-reflection" tabindex="-1">Chapter Reflection <a class="header-anchor" href="#chapter-reflection" aria-label="Permalink to &quot;Chapter Reflection&quot;">​</a></h3><p>In this chapter, we&#39;ve finally assembled the last piece of the kernel fault monitoring puzzle.</p><p>From the initial <strong>Kernel Panic</strong> (total crash), to <strong>Lockup</strong> (CPU spinning without responding), and now to <strong>Hung Task</strong> (tasks sleeping to death) and <strong>Workqueue Stall</strong> (work queues slacking off). We&#39;ve essentially been building a multi-dimensional monitoring system:</p><ul><li><strong>Panic</strong> addresses the problem of &quot;kernel data structures are corrupted, it can&#39;t keep living&quot;;</li><li><strong>Soft/Hard Lockup</strong> addresses the problem of &quot;the CPU is still spinning, but the logic is already stuck&quot;;</li><li><strong>Hung Task</strong> addresses the problem of &quot;a task is waiting for a resource it will never get&quot;;</li><li><strong>Workqueue Stall</strong> addresses the problem of &quot;the task queue is blocked, with severe backlog&quot;.</li></ul><p>Remember the scenario we mentioned in the chapter introduction? — The system looks like it&#39;s up, the network still responds to pings, but it&#39;s completely unresponsive otherwise. Now you should know that this is most likely a <strong>Soft Lockup</strong> or <strong>Hung Task</strong>, rather than a simple Panic. You&#39;re no longer someone who stares blankly at a black screen; you have tools (<code>sysctl</code>, <code>kexec</code>, <code>Magic SysRq</code>) to deeply probe the kernel&#39;s final state before death.</p><p>This is very important. Because only by knowing how it died can you, before the next reboot, find that bug, fix it, and prevent it from happening again.</p><p>In the next chapter, we&#39;ll push these monitoring techniques to their limits. We&#39;ll shift our perspective from &quot;the kernel messing up on its own&quot; to &quot;external sabotage&quot; — that is, hardware faults, memory bit flips, and how to use ultimate weapons like GDB and Kdump to perform an on-site autopsy on the kernel&#39;s corpse. That will be hardcore among hardcore.</p><p>Are you ready? Let&#39;s continue.</p><hr><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-understanding" tabindex="-1">Exercise 1: understanding <a class="header-anchor" href="#exercise-1-understanding" aria-label="Permalink to &quot;Exercise 1: understanding&quot;">​</a></h3><p><strong>Question</strong>: In a standard kernel Panic output, the message &#39;Kernel panic - not syncing: ...&#39; contains the phrase &#39;not syncing&#39;. What specific operational behavior does this phrase represent in the kernel source code? Why is this behavior adopted when a fatal error occurs?</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: This phrase indicates that the kernel intentionally stops flushing buffered data (syncing) to disk. This is because when the kernel panics, the system is in an unstable state, and attempting disk I/O operations could cause further data corruption or erroneous overwrites.</p><p><strong>Analysis</strong>: This question tests the understanding of a fundamental concept. According to the section &#39;Why Is the Phrase &quot;not syncing&quot; in the Kernel Panic Message?&#39;: when the system detects an unrecoverable error, the in-memory filesystem buffers may contain inconsistent data. If disk syncing is forcibly executed at this point, it could compromise filesystem integrity. &#39;not syncing&#39; explicitly informs the user that the kernel, to protect data safety, has abandoned saving data not yet written to disk.</p></details><h3 id="exercise-2-application" tabindex="-1">Exercise 2: application <a class="header-anchor" href="#exercise-2-application" aria-label="Permalink to &quot;Exercise 2: application&quot;">​</a></h3><p><strong>Question</strong>: Suppose you are writing a kernel module that needs to perform some emergency handling when the system crashes (such as recording specific hardware state to specific registers), but this handling process might contain sleep operations. Should you use <code>atomic_notifier_chain_register()</code> or <code>blocking_notifier_chain_register()</code> to register your callback function? Please explain why.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Neither should be used directly, or they require extremely careful handling, but architecturally, Panic typically demands non-blocking operations.</p><p>More precisely: if you must register to the Panic chain, you must ensure your code contains no sleep operations. Because when a Panic occurs, the system disables local interrupts and preemption, running in an atomic context. Although <code>panic_notifier_list</code> itself is a <code>ATOMIC_NOTIFIER_HEAD</code>, any attempt to sleep could cause the system to deadlock or experience even more severe errors.</p><p><strong>Analysis</strong>: This question tests scenario analysis. Although &#39;Application&#39; difficulty usually encourages actual coding, this is a critical trap question.</p><ol><li>The text explicitly states that <code>panic_notifier_list</code> is an &#39;Atomic&#39; type notifier chain (<code>ATOMIC_NOTIFIER_HEAD</code>).</li><li>The text mentions that Atomic type callbacks run in an atomic context and cannot block.</li><li>The text notes that the Panic function internally disables interrupts and stops scheduling. Therefore, if a callback function attempts to sleep, it will never be woken up, causing the system to hang.</li><li>For Panic handling, best practice is to be fast and non-blocking.</li></ol></details><h3 id="exercise-3-application" tabindex="-1">Exercise 3: application <a class="header-anchor" href="#exercise-3-application" aria-label="Permalink to &quot;Exercise 3: application&quot;">​</a></h3><p><strong>Question</strong>: Suppose your Linux server occasionally experiences kernel Oops in a production environment, causing service interruptions. For debugging, you decide to trigger a Panic and reboot when an Oops occurs. At the same time, to get more detailed context, you want to automatically print backtraces of all active tasks and memory information when a Panic happens. Please write the shell commands to configure the required <code>/proc/sys/kernel</code> parameters and their corresponding values.</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Configure Oops to trigger Panic: <code>echo 1 &gt; /proc/sys/kernel/panic_on_oops</code></p><p>Configure Panic to print task and memory information (panic_print mask calculation): <code>echo 17 &gt; /proc/sys/kernel/panic_print</code> (or 0x11)</p><p><strong>Analysis</strong>: This is a practical application question.</p><ol><li><strong>Triggering Panic</strong>: According to knowledge point <code>panic_on_oops</code>, setting it to 1 allows Oops to escalate to Panic.</li><li><strong>Printing Information</strong>: According to Table 10.2 in the text (<code>panic_print</code>): <ul><li>Show all tasks info: bit 4 (value 16)</li><li>Show memory info: bit 0 (value 1)</li><li>To get both pieces of information simultaneously, a bitwise OR operation is needed: 16 | 1 = 17.</li></ul></li><li>Combining these gives the two parameter settings.</li></ol></details><h3 id="exercise-4-thinking" tabindex="-1">Exercise 4: thinking <a class="header-anchor" href="#exercise-4-thinking" aria-label="Permalink to &quot;Exercise 4: thinking&quot;">​</a></h3><p><strong>Question</strong>: The text mentions the difference between &#39;Hard Lockup&#39; and &#39;Soft Lockup&#39;, mainly regarding whether interrupts are disabled and the role of the NMI Watchdog. Combining the <code>kdump</code> (Dump-capture kernel) mechanism, analyze the following scenario:</p><p>If a CPU disables interrupts and falls into an infinite loop, triggering the NMI Watchdog and causing a Hard Lockup warning, the main kernel can no longer schedule and run normally. In this extreme situation, how does the <code>kdump</code> mechanism work? (Note: focus on the role of NMI)</p><details><summary>Answer and Analysis</summary><p><strong>Answer</strong>: Even if the main kernel is stuck in an infinite loop due to disabled interrupts (Hard Lockup), NMI (Non-Maskable Interrupt) still has higher priority than normal interrupts and can forcibly interrupt the CPU&#39;s execution flow. The NMI Watchdog uses NMI precisely to detect hard lockups. When <code>kdump</code> is configured, the Panic trigger path (even if triggered by NMI) ultimately executes the <code>kexec</code> mechanism, leveraging the CPU&#39;s responsiveness in the NMI context to forcibly load and boot into the capture kernel, thereby preserving the memory image of the main kernel at the time of the crash.</p><p><strong>Analysis</strong>: This is a deep-thinking question that requires synthesizing knowledge of Hard Lockup, NMI, and Kdump.</p><ol><li><strong>Nature of Hard Lockup</strong>: The CPU is in an infinite loop with interrupts disabled. Normal interrupts (including timer interrupts) cannot execute, so task scheduling cannot occur, and normal software watchdogs cannot work.</li><li><strong>Special nature of NMI</strong>: NMI is a Non-Maskable Interrupt. Even when interrupts are disabled in the CPU&#39;s flags register (IF=0), NMI will still be serviced by the processor. The NMI Watchdog uses performance counters to generate NMIs.</li><li><strong>Kdump&#39;s intervention</strong>: After the NMI triggers the Panic routine, although the main kernel logic is dead, the Panic handling flow (especially the <code>kexec</code> loading part) leverages the NMI context or the CPU&#39;s ability to still execute low-level instructions, transferring control to the pre-loaded capture kernel. This allows the system to bypass the deadlocked main kernel and complete the memory dump.</li></ol></details><hr><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><p>This chapter focuses on establishing &quot;post-mortem&quot; and real-time monitoring capabilities for kernel faults. First, Panic is the kernel&#39;s act of complete surrender when encountering unfixable errors. The system stops scheduling and enters an infinite loop through the <code>panic()</code> function. During this time, it not only prints a last testament containing registers, stack traces, and KASLR offsets, but may also trigger visual alerts like keyboard LED blinking (<code>panic_blink</code>). The kernel parameters <code>panic</code> and <code>panic_print</code> can control the reboot timing and the granularity of detailed information output, respectively.</p><p>To obtain critical diagnostic information when the system freezes, using <code>netconsole</code> combined with a network receiver (like netcat) to remotely capture logs is standard practice. This solves the problem of local terminals being unable to output logs because the system is frozen. Additionally, the Magic SysRq mechanism (via <code>/proc/sysrq-trigger</code> or key combinations) can forcibly trigger a crash or execute emergency operations (like syncing disks, dumping task information) when the system is unresponsive, serving as a powerful backdoor for debugging deadlocks or hung states.</p><p>For more hidden system hangs, the kernel provides a Lockup detection mechanism based on NMI and watchdogs. Soft Lockup refers to a task spinning in an infinite loop in kernel space, preventing other processes from being scheduled (triggered after 20 seconds by default), while Hard Lockup involves an extreme infinite loop with interrupts disabled (triggered after 10 seconds by default). Both can be configured via kernel parameters to directly trigger a Panic or merely print a warning, helping developers distinguish between scheduler failure and interrupt masking.</p><p>Beyond CPU-level deadlocks, the Hung Task detector is specifically responsible for discovering tasks that have been stuck in uninterruptible sleep (D state) for too long (120 seconds by default), usually caused by waiting on I/O or lock resources. Meanwhile, the Workqueue Stall Detection mechanism monitors whether tasks in kernel work queues are indefinitely delayed in execution, preventing tasks deferred by drivers or subsystems via work queues from blocking the entire queue&#39;s operation due to infinite loops.</p><p>Developers can also register with the <code>panic_notifier_list</code> atomic notifier chain to insert custom handling logic at the exact moment of a kernel crash (such as lighting up a fault LED or recording critical hardware state). But it&#39;s important to note that such Panic handling callbacks run in a highly unstable atomic context, and executing any operation that might cause sleep or blocking is strictly prohibited — otherwise, it could prevent the system from even generating a crash dump.</p>`,110)])])}const g=s(i,[["render",o]]);export{u as __pageData,g as default};
